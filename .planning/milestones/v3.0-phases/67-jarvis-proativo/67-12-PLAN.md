---
phase: 67-jarvis-proativo
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/routes/proactive.ts
  - apps/backend-ts/src/index.ts
autonomous: true
requirements: [PROACT-06]
gap_closure: true

must_haves:
  truths:
    - "POST /api/settings/daily-summary chama ProactiveScheduler.registerDailySummaryJob(time) com o horario do body"
    - "ProactiveScheduler.registerDailySummaryJob e chamado em index.ts com o horario da config Electron (via pushProactiveConfigToBackend), nao mais hardcoded '09:00'"
    - "Alterar o horario do resumo diario via Settings UI atualiza o cron job em runtime"
  artifacts:
    - path: "apps/backend-ts/src/routes/proactive.ts"
      provides: "Router com rota POST /daily-summary adicionada"
      contains: "router.post('/daily-summary'"
  key_links:
    - from: "apps/backend-ts/src/routes/proactive.ts"
      to: "apps/backend-ts/src/proactive/scheduler.ts"
      via: "ProactiveScheduler.registerDailySummaryJob(time)"
      pattern: "registerDailySummaryJob"
    - from: "apps/desktop/src/main/ipc/proactive.ts"
      to: "apps/backend-ts/src/routes/proactive.ts"
      via: "POST /api/settings/daily-summary"
      pattern: "/api/settings/daily-summary"
---

<objective>
Adicionar rota POST /daily-summary ao proactive router e remover o hardcode '09:00' do index.ts.

Purpose: O backend registra o job de resumo diario com horario fixo '09:00' em index.ts e nao tem rota para atualizar esse horario em runtime. Quando o Electron chama pushProactiveConfigToBackend no startup (enviando POST /api/settings/daily-summary com o horario real do electron-store), o request cai em 404 silencioso. A Settings UI nunca consegue alterar o horario do cron.

Output: Rota POST /daily-summary funcional; hardcode '09:00' removido de index.ts (o horario real vem do Electron via pushProactiveConfigToBackend).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-jarvis-proativo/67-07-SUMMARY.md
@.planning/phases/67-jarvis-proativo/67-08-SUMMARY.md

<interfaces>
<!-- ProactiveScheduler API (apps/backend-ts/src/proactive/scheduler.ts): -->
<!--   static registerDailySummaryJob(timeHHMM: string): void -->
<!--     - timeHHMM: string no formato 'HH:MM' (ex: '09:00', '18:30') -->
<!--     - Cancela o job anterior se existir (chave -1 no Map) -->
<!--     - Cria novo cron job com o horario fornecido -->

<!--   static updateQuietHours(enabled: boolean, start: string, end: string): void -->
<!--     - Ja existia — padrao para a nova rota /daily-summary seguir -->

<!-- Rotas ja existentes no router (padrao a seguir): -->
<!--   router.post('/quiet-hours', (req, res) => { -->
<!--     const { enabled, start, end } = req.body; -->
<!--     ProactiveScheduler.updateQuietHours(enabled, start, end); -->
<!--     return res.status(200).json({ ok: true }); -->
<!--   }); -->

<!-- DailySummaryConfig type (apps/desktop/src/shared/ipc-types.ts): -->
<!--   interface DailySummaryConfig { -->
<!--     enabled: boolean; -->
<!--     time: string;   // HH:MM format -->
<!--   } -->

<!-- Montagem no app.ts (EXISTENTE — nao alterar): -->
<!--   app.use('/api/proactive', createProactiveRouter()); -->
<!--   app.use('/api/settings', createProactiveRouter()); -->
<!-- Ambos os prefixos funcionam — handler em ipc/proactive.ts envia para /api/settings/daily-summary -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar POST /daily-summary ao proactive router</name>
  <files>apps/backend-ts/src/routes/proactive.ts</files>
  <read_first>
    - apps/backend-ts/src/routes/proactive.ts (arquivo completo — ler antes de editar)
    - apps/backend-ts/src/proactive/scheduler.ts (linhas 166-210 — assinatura de registerDailySummaryJob e updateQuietHours para seguir o padrao)
  </read_first>
  <action>
No arquivo apps/backend-ts/src/routes/proactive.ts, adicionar a rota POST /daily-summary APOS a rota POST /folder-watch (antes do `return router;`).

Seguir exatamente o padrao da rota /quiet-hours existente:

```typescript
// ── POST /daily-summary ───────────────────────────────────────────────────

/**
 * Atualiza o horario do resumo diario e re-registra o cron job.
 * Electron envia esta request ao conectar com a config atual do electron-store
 * e quando o usuario altera o horario via Settings UI.
 *
 * Body: { enabled: boolean, time: string }  // time formato HH:MM
 */
router.post('/daily-summary', (req: Request, res: Response) => {
  const { enabled, time } = req.body as {
    enabled: boolean;
    time: string;
  };

  if (enabled && time) {
    ProactiveScheduler.registerDailySummaryJob(time);
  }

  return res.status(200).json({ ok: true });
});
```

Adicionar antes de `return router;`. Nao alterar nada mais no arquivo.

Commit atomico: "✨ feat(67-12): adicionar rota POST /daily-summary ao proactive router"
  </action>
  <verify>
    <automated>grep -n "router.post.*daily-summary" /root/jarvis/apps/backend-ts/src/routes/proactive.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna pelo menos 1 linha com "router.post('/daily-summary'"
    - grep "registerDailySummaryJob" apps/backend-ts/src/routes/proactive.ts retorna ao menos 1 linha
    - cd apps/backend-ts && npx tsc --noEmit retorna 0 erros
  </acceptance_criteria>
  <done>Rota POST /daily-summary existente no router; chama ProactiveScheduler.registerDailySummaryJob(time) quando enabled=true e time fornecido.</done>
</task>

<task type="auto">
  <name>Task 2: Remover hardcode '09:00' em index.ts</name>
  <files>apps/backend-ts/src/index.ts</files>
  <read_first>
    - apps/backend-ts/src/index.ts (linhas 100-125 — bloco onde ProactiveScheduler.registerDailySummaryJob e chamado hardcoded)
  </read_first>
  <action>
No arquivo apps/backend-ts/src/index.ts, localizar a chamada:

```typescript
ProactiveScheduler.registerDailySummaryJob('09:00');
console.log('Daily summary cron registered (default 09:00)');
```

Remover essas 2 linhas completamente. O job sera registrado pelo Electron via pushProactiveConfigToBackend (que ja envia POST /api/settings/daily-summary com o horario real do electron-store) quando o desktop conectar. O backend nao deve mais registrar com horario hardcoded.

Adicionar um comentario explicativo no lugar:

```typescript
// Phase 67 Gap 2 fix (67-12): daily summary job nao e pre-registrado aqui.
// O Electron envia POST /api/settings/daily-summary via pushProactiveConfigToBackend
// (apps/desktop/src/main/ipc/proactive.ts) com o horario real do electron-store
// apos conectar ao backend. Isso garante que o cron usa o horario configurado
// pelo usuario, nao um default hardcoded.
```

Commit atomico: "♻️ refactor(67-12): remover hardcode '09:00' do index.ts — horario vem do Electron"
  </action>
  <verify>
    <automated>grep "registerDailySummaryJob\|09:00" /root/jarvis/apps/backend-ts/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna 0 ocorrencias de 'registerDailySummaryJob' em index.ts
    - grep retorna 0 ocorrencias de '09:00' em index.ts
    - cd apps/backend-ts && npx tsc --noEmit retorna 0 erros
  </acceptance_criteria>
  <done>index.ts nao contem mais chamada hardcoded a registerDailySummaryJob. O comentario explica que o horario vem do Electron via pushProactiveConfigToBackend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Electron→backend | POST /api/settings/daily-summary carrega Bearer token; ja validado pelo middleware existente |
| body.time | Formato HH:MM precisa validacao para evitar injecao de string invalida no cron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-12-01 | Tampering | POST /daily-summary body.time | mitigate | ProactiveScheduler.registerDailySummaryJob usa node-cron que valida o formato internamente; a rota /quiet-hours ja segue o mesmo padrao (sem validacao extra no router) — aceitar risco baixo pois backend e single-user, localhost-only |
| T-67-12-02 | Denial of Service | cron job re-registration | accept | registerDailySummaryJob cancela job anterior antes de criar novo — sem acumulo de jobs. Log de aviso se enabled=false e nenhum job registrado |
</threat_model>

<verification>
Apos execucao:

```bash
# Verifica rota adicionada
grep -n "daily-summary" apps/backend-ts/src/routes/proactive.ts
# Deve mostrar: router.post('/daily-summary', ...) e registerDailySummaryJob(time)

# Verifica hardcode removido
grep "registerDailySummaryJob\|09:00" apps/backend-ts/src/index.ts
# Deve retornar vazio

# Type-check backend
cd apps/backend-ts && npx tsc --noEmit 2>&1 | grep -i error | head -10
# Deve retornar vazio

# Teste manual de integracao (opcional — documentar resultado no SUMMARY):
# curl -X POST http://localhost:3001/api/settings/daily-summary \
#   -H "Authorization: Bearer <api_key>" \
#   -H "Content-Type: application/json" \
#   -d '{"enabled":true,"time":"10:00"}'
# Deve retornar {"ok":true}
```
</verification>

<success_criteria>
- POST /api/settings/daily-summary existe no router e retorna 200
- ProactiveScheduler.registerDailySummaryJob e chamado com o time do body quando enabled=true
- index.ts nao contem mais hardcode '09:00'
- TypeScript compila sem erros em backend-ts
- PROACT-06 configuracao de horario funcional end-to-end
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/67-jarvis-proativo/67-12-SUMMARY.md`
</output>
