---
phase: 67-jarvis-proativo
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/preload/settings.ts
autonomous: true
requirements: [PROACT-04, PROACT-05, PROACT-06]
gap_closure: true

must_haves:
  truths:
    - "ipcRenderer.invoke('proactive:apply-quiet-hours', ...) no preload chama o handler correto no main process"
    - "ipcRenderer.invoke('proactive:apply-folder-watch', ...) no preload chama o handler correto no main process"
    - "ipcRenderer.invoke('proactive:apply-daily-summary', ...) no preload chama o handler correto no main process"
  artifacts:
    - path: "apps/desktop/src/preload/settings.ts"
      provides: "Preload com canais IPC alinhados ao handler: proactive:apply-* em vez de settings:apply-*"
      contains: "proactive:apply-quiet-hours"
  key_links:
    - from: "apps/desktop/src/preload/settings.ts"
      to: "apps/desktop/src/main/ipc/proactive.ts"
      via: "ipcRenderer.invoke → ipcMain.handle"
      pattern: "proactive:apply-quiet-hours|proactive:apply-folder-watch|proactive:apply-daily-summary"
---

<objective>
Corrigir mismatch de canal IPC entre preload e main process para as 3 rotas de configuracao proativa.

Purpose: O preload envia 'settings:apply-quiet-hours/folder-watch/daily-summary' mas os handlers no ipcMain registram 'proactive:apply-*'. Nenhum handler existe para os canais que o preload usa, fazendo qualquer mudanca de configuracao via Settings UI falhar silenciosamente. Este e um bug de 3 linhas.

Output: settings.ts com canais IPC corretos; configuracao de quiet hours, folder watch e daily summary funcionais via UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-jarvis-proativo/67-08-SUMMARY.md
@.planning/phases/67-jarvis-proativo/67-09-SUMMARY.md

<interfaces>
<!-- Canais registrados em ipcMain (CORRETOS — nao devem ser alterados) -->
<!-- De apps/desktop/src/main/ipc/proactive.ts linhas 58-59, 90-91, 120-121: -->
<!--   ipcMain.handle('proactive:apply-quiet-hours', ...) -->
<!--   ipcMain.handle('proactive:apply-folder-watch', ...) -->
<!--   ipcMain.handle('proactive:apply-daily-summary', ...) -->

<!-- Canais ERRADOS no preload (a corrigir): -->
<!-- apps/desktop/src/preload/settings.ts linhas 53-55: -->
<!--   ipcRenderer.invoke('settings:apply-quiet-hours', config)  ← ERRADO -->
<!--   ipcRenderer.invoke('settings:apply-folder-watch', config) ← ERRADO -->
<!--   ipcRenderer.invoke('settings:apply-daily-summary', config) ← ERRADO -->

<!-- SettingsApi interface (ipc-types.ts) — metodos e assinaturas: -->
<!--   applyQuietHours: (config: QuietHoursConfig) => Promise<{ success: boolean; error?: string }> -->
<!--   applyFolderWatch: (config: FolderWatchConfig) => Promise<{ success: boolean; error?: string }> -->
<!--   applyDailySummary: (config: DailySummaryConfig) => Promise<{ success: boolean; error?: string }> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alinhar canais IPC no preload para prefixo proactive:</name>
  <files>apps/desktop/src/preload/settings.ts</files>
  <read_first>
    - apps/desktop/src/preload/settings.ts (arquivo a modificar — ler completo antes)
    - apps/desktop/src/main/ipc/proactive.ts (linhas 58-121 — canais registrados no handler, sao a fonte da verdade)
  </read_first>
  <action>
No arquivo apps/desktop/src/preload/settings.ts, localizar as linhas 53-55 (bloco "Phase 67 — Proactive settings") e substituir os 3 canais:

ANTES:
  applyQuietHours: (config) => ipcRenderer.invoke('settings:apply-quiet-hours', config),
  applyFolderWatch: (config) => ipcRenderer.invoke('settings:apply-folder-watch', config),
  applyDailySummary: (config) => ipcRenderer.invoke('settings:apply-daily-summary', config),

DEPOIS:
  applyQuietHours: (config) => ipcRenderer.invoke('proactive:apply-quiet-hours', config),
  applyFolderWatch: (config) => ipcRenderer.invoke('proactive:apply-folder-watch', config),
  applyDailySummary: (config) => ipcRenderer.invoke('proactive:apply-daily-summary', config),

Nao alterar mais nada no arquivo. O resto do preload (onProactiveEvent, reloadLlm, etc.) esta correto.

Apos a correcao, adicionar comment explicativo acima do bloco:
  // Phase 67 — Proactive settings (PROACT-04, D-10, D-13, D-17)
  // Canal: 'proactive:apply-*' (alinhado com ipcMain.handle em ipc/proactive.ts)
  // NOTA: preload usa prefixo 'proactive:' nao 'settings:' — handlers registrados em ipc/proactive.ts

Commit atomico: "🐛 fix(67-11): alinhar canais IPC preload para proactive:apply-*"
  </action>
  <verify>
    <automated>grep "proactive:apply-quiet-hours\|proactive:apply-folder-watch\|proactive:apply-daily-summary" /root/jarvis/apps/desktop/src/preload/settings.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna 3 (tres ocorrencias de canais com prefixo proactive:apply-)
    - grep "settings:apply-" apps/desktop/src/preload/settings.ts retorna 0 ocorrencias
    - npx tsc --noEmit em apps/desktop sem erros de tipo
  </acceptance_criteria>
  <done>Os 3 canais no preload usam 'proactive:apply-*' (correspondendo exatamente aos ipcMain.handle em ipc/proactive.ts). Nenhuma referencia a 'settings:apply-*' permanece no preload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→preload | invoke crossing sandbox via contextBridge |
| preload→main | IPC channel name must match exactly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-11-01 | Tampering | preload/settings.ts | mitigate | Canais 'proactive:apply-*' somente acessiveis via contextBridge exposto como window.settings — nao acessivel diretamente pelo renderer JS |
| T-67-11-02 | Repudiation | channel rename | accept | Mudanca e puramente cosmética (prefixo); handlers ja tinham validacao HH:MM e path checks em ipc/proactive.ts (T-67-03) |
</threat_model>

<verification>
Apos execucao:

```bash
# Verifica canais corretos no preload
grep "proactive:apply-" apps/desktop/src/preload/settings.ts
# Deve retornar 3 linhas com proactive:apply-quiet-hours, proactive:apply-folder-watch, proactive:apply-daily-summary

# Verifica que canais errados foram removidos
grep "settings:apply-" apps/desktop/src/preload/settings.ts
# Deve retornar vazio (0 ocorrencias)

# Type-check
cd apps/desktop && npx tsc --noEmit 2>&1 | grep -i error | head -10
# Deve retornar vazio
```
</verification>

<success_criteria>
- Preload chama 'proactive:apply-quiet-hours' (3 canais corretos)
- Nenhuma referencia a 'settings:apply-*' em settings.ts
- TypeScript compila sem erros
- PROACT-04, PROACT-05 e PROACT-06 (parcialmente) desbloqueados
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/67-jarvis-proativo/67-11-SUMMARY.md`
</output>
