---
phase: 66-agentic-tasks
plan: 05
type: execute
wave: 4
depends_on: ['66-02', '66-03', '66-04']
files_modified:
  - apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts
  - apps/backend-ts/.env.example
  - .planning/STATE.md
autonomous: false
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
must_haves:
  truths:
    - "End-to-end backend integration test (mocked LLM, real graph + routes) shows complete plan→confirm→execute→done flow with correct SSE event sequence"
    - "AGENTIC_DISABLED env-flag documented in .env.example with default behavior comment"
    - "Manual UAT: User issues real multi-step request via voice and via text and JARVIS executes both paths end-to-end"
    - "Manual UAT: User cancels mid-execution by voice (cancela), by text (cancelar), and by button click — task stops within 1s in all 3 cases"
    - "Manual UAT: TTS sumário pt-BR is intelligible and matches UI-SPEC template for plans of 1, 3, and 7 steps"
    - "Manual UAT: Orb badge shows AGENT N/M legibly during execution and reverts to voice-mode label on terminal events"
    - "STATE.md updated with Phase 66 lessons learned + carry-forward patterns for Phase 67"
  artifacts:
    - path: "apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts"
      provides: "End-to-end backend integration test using buildTaskGraph + tasks router"
      exports: []
    - path: "apps/backend-ts/.env.example"
      provides: "Documents AGENTIC_DISABLED flag and default behavior"
      contains: "AGENTIC_DISABLED"
    - path: ".planning/STATE.md"
      provides: "Updated with Phase 66 carry-forward patterns"
      contains: "Phase 66"
  key_links:
    - from: ".planning/phases/66-agentic-tasks/66-05-UAT.md"
      to: "manual UAT execution"
      via: "checklist verified by user with real LM Studio + real PC tools + real microphone"
      pattern: "UAT|approved"
---

<objective>
Wave 4 — Verificação end-to-end: 1 teste de integração backend cobrindo o fluxo completo (plan → confirm → execute → done) com mock LLM + graph compilado; documentação do flag `AGENTIC_DISABLED` em `.env.example`; e checkpoint de verificação humana cobrindo os cenários que CI não consegue executar (voz real, TTS audível, contraste do orb badge sobre wallpapers, latência percebida).

Purpose: Confirmar AGENT-01/02/03/04 ponta-a-ponta antes de fechar a phase. Esse plan NÃO adiciona novas funcionalidades — apenas valida e documenta. É o gate final.

Output: 1 teste E2E backend, 1 entrada em .env.example, 1 STATE.md atualizado, 1 checkpoint UAT bloqueante que requer aprovação explícita do usuário ("approved" ou descrição de issues).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-agentic-tasks/66-CONTEXT.md
@.planning/phases/66-agentic-tasks/66-RESEARCH.md
@.planning/phases/66-agentic-tasks/66-UI-SPEC.md
@.planning/phases/66-agentic-tasks/66-VALIDATION.md
@.planning/phases/66-agentic-tasks/66-02-SUMMARY.md
@.planning/phases/66-agentic-tasks/66-03-SUMMARY.md
@.planning/phases/66-agentic-tasks/66-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Backend E2E integration test + AGENTIC_DISABLED env documentation</name>
  <files>apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts, apps/backend-ts/.env.example</files>
  <read_first>
    - apps/backend-ts/src/agent/__tests__/graph.test.ts (Plan 02 output) — pattern for streaming + mockChatModel
    - apps/backend-ts/src/agent/__tests__/fixtures/mockChatModel.ts (Plan 02 output)
    - apps/backend-ts/src/routes/__tests__/tasks.test.ts (Plan 03 output) — pattern for testing routes
    - apps/backend-ts/src/agent/graph.ts (Plan 02 output)
    - apps/backend-ts/src/routes/tasks.ts (Plan 03 output)
    - apps/backend-ts/.env.example (current state — append AGENTIC_DISABLED entry)
    - .planning/phases/66-agentic-tasks/66-VALIDATION.md § Per-Task Verification Map row "AGENT-01 integration agent graph end-to-end"
  </read_first>
  <action>
**Step A: Create `apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts`** — full integration covering AGENT-01:

Implement 3 representative test cases (each ~30 lines):

1. **AGENT-01: Full happy path 3 steps** — buildTaskGraph + mockChatModel returning a 3-step plan + stub ReactAgentLike. Phase 1 streams initial run (drains until interrupt). Verify `getState().tasks[0].interrupts[0].value.kind === 'plan-confirmation'`. Phase 2 streams `Command(resume:{kind:'confirm'})`. Assert event sequence is exactly: `task:step:start (id=1)` → `task:step:end (id=1, summary='Listei 14 arquivos')` → `task:step:start (id=2)` → `task:step:end (id=2, summary='Filtrei 5 PDFs')` → `task:step:start (id=3)` → `task:step:end (id=3, summary='Movi 5 PDFs')` → `task:done (summary contains 'Movi 5 PDFs')`. Final cleanup: `await taskCheckpointer.deleteThread(threadId)`.

2. **AGENT-04: Cancel-gate fires before step 1** — Same setup. Drain initial planner run. Call `await graph.updateState(config, { cancelRequested: true })`. Stream `Command(resume:{kind:'confirm'})`. Assert events contain `task:cancelled atStep:1` AND do NOT contain any `task:step:start` or `task:done`.

3. **AGENT-02 edit-loop: edit feedback re-runs planner** — Custom LLM mock that returns `firstPlan` on call 1, `editedPlan` on call 2, AND captures all input prompts. Drain phase 1. Stream `Command(resume:{kind:'edit', feedback:'use a different step'})`. Assert phase 2 events include `task:edit-loop` AND a new `task:plan` with `editedPlan` content. Assert `captured.some(p => p.includes('use a different step'))`.

Use `vi.fn()` spies and `expect(...).toMatchObject(...)` for assertions. The full test file should be 130-180 lines and run in <5s without any LM Studio dependency.

Reference Plan 02 `graph.test.ts` pattern for stream consumption:
```typescript
const events: Array<{ kind: string }> = [];
for await (const [, chunk] of await graph.stream(input, config)) {
  events.push(chunk as { kind: string });
}
```

**Step B: Append to `apps/backend-ts/.env.example`** (or create the file if missing — check first via Read):

```ini
# ─── Phase 66: Agentic Tasks ────────────────────────────────────
# Default: unset (agentic graph enabled). Set to "true" to disable
# the planner-first routing and force every chat turn through the
# legacy direct-agent path. Useful for debugging chat latency or
# when the LLM model is unreliable for withStructuredOutput.
# AGENTIC_DISABLED=true
```

WHY:
- E2E test uses ONLY mocked LLM and stub ReactAgentLike — no LM Studio dependency, runs in CI in <5s.
- 3 representative scenarios (full path success, cancel-before-start, edit-loop) — covers AGENT-01/02/04 most-critical paths automatically. Voice paths and AGENT-03 orb integration are in UAT (Task 2) because microphone + visual CI is impossible.
- `.env.example` documents the escape hatch — future developers know how to disable agentic mode for debugging.
  </action>
  <verify>
    <automated>cd /root/jarvis && pnpm --filter @jarvis/backend-ts test --run -- src/agent/__tests__/graph.e2e</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts` exists
    - `pnpm --filter @jarvis/backend-ts test --run -- src/agent/__tests__/graph.e2e` exits 0 with ≥3 passing tests
    - Test 1 asserts the EXACT 7-event sequence: 3 step:start + 3 step:end + 1 done (in that order)
    - Test 2 asserts `task:cancelled` is emitted with `atStep:1` AND no `task:step:start` events were emitted
    - Test 3 asserts the captured planner prompt array contains a string with substring `"use a different step"` AND the second `task:plan` event has `plan.steps[0].description === 'Edited step'`
    - `grep "AGENTIC_DISABLED" apps/backend-ts/.env.example` finds ≥1 match
    - `grep "Phase 66\\|Agentic Tasks" apps/backend-ts/.env.example` finds ≥1 match (section header for context)
    - Full backend suite still green: `pnpm --filter @jarvis/backend-ts test --run` exits 0
  </acceptance_criteria>
  <done>Backend E2E proves AGENT-01/02/04 integration without manual steps. Documentation of escape hatch in .env.example. CI gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual UAT — Voice + Text + Cancel + TTS + Orb (BLOCKING)</name>
  <what-built>
**Backend (Plans 02-03):**
- `apps/backend-ts/src/agent/{types,planner,executor,graph,keywords}.ts` — full agentic module (LangGraph StateGraph + interrupt + MemorySaver + AbortSignal threading)
- `apps/backend-ts/src/routes/tasks.ts` — POST `/api/tasks/:id/resume` + `/cancel` with Bearer auth + Zod validation
- `apps/backend-ts/src/routes/chat.ts` — `/api/chat/stream` extended to emit 9 `task:*` SSE events
- `apps/backend-ts/src/session/{chat-session,tool-dispatch,request-file-action}.ts` — DispatchContext extended with signal/taskMeta; signal-threading through fetch + MCP callTool
- `apps/backend-ts/src/mcp/client/tool-adapter.ts` — native `client.callTool({signal, timeout})` with pt-BR cancel message
- `.env.example` — AGENTIC_DISABLED flag documented

**Renderer (Plan 04):**
- `apps/desktop/src/renderer/src/chat/TaskCheckList.tsx` — 8 visual states from UI-SPEC, all pt-BR copy verbatim
- `apps/desktop/src/renderer/src/chat/{ChatContext,useTaskSse}.tsx` — SSE consumer + reducer
- `apps/desktop/src/renderer/components/Orb/Orb.tsx` — `agentBadgeText` prop showing `AGENT N/M`
- `apps/desktop/src/renderer/src/voice/{voiceInput/sendAudioAndHandle.ts,task-keywords.ts}` — short-circuit voice keywords to /resume or /cancel
- `apps/desktop/src/main/ipc/tasks.ts` + `apps/desktop/src/preload/index.ts` — `window.jarvis.tasks` API
- TTS sumário (D-08): pt-BR template `"Vou fazer N coisas: ..., e mais X. Confirma?"` spoken on `task:plan`

**Test infrastructure:**
- ≥35 renderer tests + ≥30 backend tests, all green in CI
- E2E backend integration test in `graph.e2e.test.ts` covers happy path + cancel + edit-loop
  </what-built>

  <how-to-verify>
**Pre-flight (do these first):**

1. Ensure LM Studio is running on `http://localhost:1234` with a 7B+ instruct model loaded (Qwen2.5-7B-Instruct or Llama-3.1-8B-Instruct recommended — RESEARCH § A1 flags small models as risky).
2. Start backend: `cd apps/backend-ts && pnpm dev` — verify console shows agentic graph routes registered.
3. Start desktop: `cd apps/desktop && pnpm dev` — verify the widget orb appears.
4. Confirm voice mode is `wake-word` (default) in tray menu.

**SCENARIO A — Multi-step task by TEXT (AGENT-01, AGENT-02, AGENT-03):**

1. Type in chat: `"Liste os arquivos da pasta Downloads, conte quantos são .pdf, e me diga o total"`
2. Wait ≤5s — JARVIS should display a TaskCheckList with 3 buttons (Confirmar / Editar / Cancelar) and a numbered plan with ≥2 steps.
3. **Verify the chat bubble** shows: heading `Aqui está o plano:`, numbered steps in pt-BR, disclaimer `JARVIS não desfaz ações já executadas...`, 3 buttons.
4. **Verify the TTS** speaks: `Vou fazer N coisas: ..., Confirma?` (pt-BR, kokoro voice).
5. Click `Confirmar`. The bubble transforms into State 4 (executing): running step has a left border in cyan/accent; completed steps show ✅; pending steps show ○.
6. **Verify the Orb badge** shows `AGENT 1/N`, then `AGENT 2/N`, etc., as steps complete.
7. After all steps complete (≤30s), the bubble shows State 6 (done) with ✅ + summary text in pt-BR.
8. **Verify the Orb badge** reverted to `WW` (or your voice mode label).

**SCENARIO B — Multi-step task by VOICE (AGENT-01, AGENT-04 voice):**

1. Say: `"Hey JARVIS"` (wake word).
2. Say: `"Organiza minha pasta Downloads agrupando por tipo"`.
3. Wait ≤5s — TaskCheckList appears + TTS speaks the plan summary.
4. Say: `"vai"` (or `"sim"` / `"confirmar"`).
5. **Verify** task starts executing — Orb shows `AGENT N/M`, chat bubble updates per step.
6. While task is executing (during step 2 or 3), say: `"cancela"`.
7. **Verify** task halts within 1 second — chat bubble transitions to State 7 (`Cancelado no passo N`); steps after the cancel point show dashed-line-through styling.

**SCENARIO C — Cancel by BUTTON (AGENT-04 button):**

1. Trigger another multi-step task (text or voice).
2. Click `Cancelar` button while task is executing.
3. **Verify** task halts within 1 second + State 7 rendering.

**SCENARIO D — Edit feedback (AGENT-02 edit):**

1. Trigger a task: `"Faz um backup dos meus arquivos importantes"`.
2. When plan appears, click `Editar`.
3. The bubble transitions to State 2: input field with placeholder `Como você quer ajustar o plano?`.
4. Type: `"foca só nos arquivos Excel"` and press Enter.
5. **Verify** State 3 (`Replanejando…` with spinner) appears briefly, then a new plan replaces it.
6. **Verify** TTS speaks the new plan summary.

**SCENARIO E — Step failure recovery (AGENT-04 partial):**

1. Trigger a task that touches a folder that does not exist (e.g., `"Lista arquivos da pasta /tmp/jarvis-nope-666"`).
2. **Verify** when execution reaches the failing step, the bubble transitions to State 5 with 3 buttons (`Continuar` / `Replanejar` / `Abortar`) AND TTS speaks the failure message.
3. Click `Continuar`. **Verify** task resumes at step N+1 (skipped step shows ❌ + error sub-line).
4. Repeat the test, this time clicking `Replanejar`. **Verify** State 3 → new plan that avoids the failing approach.
5. Repeat the test, this time clicking `Abortar`. **Verify** State 8 (`Tarefa interrompida no passo N.`) rendering.

**SCENARIO F — TTS sumário variations (AGENT-02 perceptual):**

1. Trigger a 1-step task: `"Tira print da tela"`. Verify TTS template `Vou fazer 1 coisa: ...`.
2. Trigger a 3-step task. Verify TTS template `Vou fazer 3 coisas: ..., ..., e .... Confirma?`.
3. Trigger a 7-step task: `"Organiza Downloads por tipo, depois Documents, depois Desktop, e me manda um resumo"`. Verify TTS template `Vou fazer 7 coisas: ..., ..., e mais 5. Confirma?`.

**SCENARIO G — Orb badge contrast (AGENT-03 perceptual):**

1. Trigger any task that runs ≥10s.
2. Move the JARVIS widget over a LIGHT desktop wallpaper. **Verify** badge text legible.
3. Move over a DARK wallpaper. **Verify** legibility.
4. Move over a busy/colorful image. **Verify** legibility.

**SCENARIO H — Failure modes (defensive):**

1. Stop LM Studio. Trigger a task. **Verify** bubble shows `Não consegui planejar essa tarefa.` (UI-SPEC empty-plan copy) — NOT a JS stack trace, NOT a hung spinner.
2. Restart LM Studio. **Verify** subsequent tasks work normally.

**Acceptance:** All scenarios A through H must pass. Any "fail" requires either a fix (re-run the affected plan) or a documented deferral to v3.1+.
  </how-to-verify>

  <files>(no files — manual verification only)</files>
  <action>Manual UAT scenarios A through H per `<how-to-verify>` block above. User runs each scenario and reports results.</action>
  <verify>(manual — see `<how-to-verify>` block)</verify>
  <done>User typed "approved" OR reported failures routed to gap-closure planning per failed scenario letter.</done>
  <resume-signal>Type "approved" to proceed (closing Phase 66) — OR describe issues per scenario letter (e.g., "Scenario B step 6: cancel by voice doesn't fire — STT recognized 'cancela' but task continued") so we can route to gap-closure planning.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md with Phase 66 carry-forward patterns + close phase</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — preserve all v2.x carry-forward, add Phase 66 entries)
    - .planning/phases/66-agentic-tasks/66-02-SUMMARY.md (Plan 02 outcomes)
    - .planning/phases/66-agentic-tasks/66-03-SUMMARY.md (Plan 03 outcomes)
    - .planning/phases/66-agentic-tasks/66-04-SUMMARY.md (Plan 04 outcomes)
  </read_first>
  <action>
**Append to `.planning/STATE.md` § "## Accumulated Context > ### Decisions"** (after the existing Phase 65 entries) — these are the carry-forward patterns Phase 67 (Proativo) will likely reuse:

```markdown
- [Phase 66-01]: Wave 0 stub-first pattern — create vitest test files with `it.todo()` BEFORE Wave 1 implementation. Allows real `<verify>` commands in plans (no "MISSING" placeholders) and forces interface contracts to be defined upfront.
- [Phase 66-01]: Renderer mirrors backend keyword constants byte-for-byte; parity test reads backend file at test time and asserts equality to prevent drift. Pattern applicable to any pt-BR copy that must match across backend + renderer.
- [Phase 66-02]: LangGraph 1.x `Annotation.Root({...})` channels need explicit `(_, x) => x` overwrite reducers — default reducer behavior is opaque and Pitfall 8 (edit-feedback ignored) bites silently. Always declare reducer.
- [Phase 66-02]: `MemorySaver.deleteThread(threadId)` MUST be called explicitly on terminal events (`task:done|cancelled|error`). No automatic GC. Document at module level.
- [Phase 66-02]: Per-step ReAct instruction goes as `HumanMessage` NOT `SystemMessage` — Anthropic Claude rejects multiple system messages; OpenAI/LM Studio silently concatenate. Pitfall 5 explicit.
- [Phase 66-02]: `interrupt()` from `@langchain/langgraph` THROWS `GraphInterrupt`. Never wrap in `try/catch` without re-throwing on `isGraphInterrupt(err)`. Will swallow the pause mechanism otherwise.
- [Phase 66-02]: `createReactAgent` is `@deprecated` in 1.2.8 (moved to `langchain` package as `createAgent`). Phase 66 deliberately keeps the deprecated import — migration is tech-debt for v3.x. Document at site of use.
- [Phase 66-03]: AbortSignal threading pattern — `AbortSignal.any([config.signal, AbortSignal.timeout(N)])` composes outer cancel with inner timeout. Node 22 stable. Required at every fetch boundary (request-file-action, MCP tool-adapter).
- [Phase 66-03]: MCP tool cancellation returns pt-BR string `"Tool {name} cancelado pelo usuário."` to the LLM — NOT throw. Throwing would make the LLM retry; string-return makes the LLM report cleanly. Phase 65 D-16 shape preserved.
- [Phase 66-03]: D-17 audit log enrichment — when both `mcp-external` AND `agentic-task` apply, `source: 'agentic-task'` takes precedence; `mcpServerName` retained as separate field so origin is not lost.
- [Phase 66-03]: SSE event protocol uses `event: task:plan\ndata: {...}\n\n` format (named events, NOT bare `data:`). Renderer EventSource consumer uses `addEventListener('task:plan', ...)`. Pitfall 7 explicit.
- [Phase 66-03]: `streamMode: ['custom', 'messages'] as const` returns `[mode, chunk]` tuples in async iterator. The `custom` mode receives `config.writer(payload)` calls from inside nodes — used to emit task:* events without intermixing with token chunks.
- [Phase 66-03]: `taskId` format = `chat-{chatSessionId}-task-{uuid}` validated by regex `/^chat-[\w-]+-task-[0-9a-f-]{36}$/` at route boundary. Defense in depth for malformed input even though single-user backend.
- [Phase 66-03]: Connection lifecycle: SSE closes after each phase (initial → interrupt; resume → next interrupt or terminal). Renderer opens NEW SSE on POST /resume response. Cleaner than long-lived connections (RESEARCH Open Questions #5).
- [Phase 66-04]: TaskCheckList is a single component rendering 8 discriminated-union states — no fallback double-render, no separate components per state. Reduces surface area and matches UI-SPEC § Component Inventory contract.
- [Phase 66-04]: Orb `agentBadgeText` prop is OPTIONAL — when undefined, falls back to existing voice-mode label. NO change to voice-mode color (D-12). Pattern: optional override prop with fallback to existing behavior.
- [Phase 66-04]: TTS sumário uses determined-string templates (no extra LLM call) for latency. UI-SPEC locks the templates verbatim per N≤3 vs N>3 branches.
- [Phase 66-04]: sendAudioAndHandle short-circuit pattern: read activeTask from ChatContext, match keyword, call IPC bridge → bypass /api/chat. Falls through to existing flow when no match. Pattern reusable for any "voice triggers a non-chat action" case.
- [Phase 66-05]: BLOCKING manual UAT covers 8 scenarios (A-H) including microphone-driven cancellation, TTS perceptual quality, and badge contrast over varying wallpapers — perceptual gates not feasible in CI.
```

**Update `.planning/STATE.md` § "## Current Position":**
- Phase: 67
- Last activity: <today's date>
- Update progress percentage to reflect Phase 66 completion

**Update `.planning/STATE.md` § "## Milestone History" or v3.0 section:**
- Mark Phase 66 as complete (date)

WHY:
- Carry-forward patterns are the highest-leverage knowledge transfer to Phase 67 — Proativo will reuse: SSE event extension, AbortSignal threading, scheduled-task cancellation, multi-window broadcast.
- STATE.md is the canonical pickup point for fresh sessions per repo convention. Updating it after each phase (not after each plan) keeps signal-to-noise high.
- Each entry is short and action-oriented — future Claude can grep for keyword and find the lesson.
  </action>
  <verify>
    <automated>grep -c "Phase 66" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains ≥10 new bullets prefixed with `[Phase 66-XX]:` (one per major lesson learned)
    - `.planning/STATE.md` § "## Current Position" updated: Phase: 67 (or whichever is next), last activity = today
    - `grep "Phase 66" .planning/STATE.md` returns count ≥10
    - The bullets cover: stub-first Wave 0 pattern, Annotation reducer pitfall, MemorySaver lifecycle, HumanMessage vs SystemMessage, AbortSignal.any, MCP cancel message, SSE event format, taskId regex, agentBadgeText optional prop pattern, TTS deterministic template
    - File preserves ALL existing v2.x and v3.0 carry-forward bullets — `wc -l .planning/STATE.md` is GREATER than before this task (only insertions)
    - No false claims (e.g., do NOT write that Phase 66 SqliteSaver was implemented — it explicitly was NOT, per CONTEXT.md D-03 and Deferred)
  </acceptance_criteria>
  <done>STATE.md is the single source of truth for phase carry-forward. Phase 67 planner can read this file and immediately understand what patterns to reuse and what to avoid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| E2E test | Pure code, no runtime input crosses |
| .env.example | Static documentation file, no secrets |
| STATE.md | Internal planning artifact |
| Manual UAT | Human-in-the-loop verification — user is the trust authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05-01 | Tampering | E2E test could pass while real LM Studio integration is broken (mock-only test) | mitigate | Manual UAT (Task 2) covers the LM Studio integration with real model + real tools. UAT is BLOCKING — phase cannot close without it. T-66-05-01 is mitigated by the human gate. |
| T-66-05-02 | Information Disclosure | STATE.md could leak details about internal codebase decisions if synced to a public repo | accept | STATE.md is intentionally an internal planning artifact (`.planning/` directory pattern from earlier phases). Single-user local repo; if user later open-sources, they can review the directory. Documented in earlier phase carry-forward (PROJECT.md privacy lock applies to user data, not planning notes). |
| T-66-05-03 | Spoofing | UAT script tells user "approved" — but user could approve without actually testing | accept | Single-user workflow; user is the product owner AND tester. Trusting the user to actually run the scenarios is the workflow's premise. RESEARCH § Manual-Only Verifications acknowledges this is the only way. |

**Disposition rationale:** All low-severity given single-user MVP scope. UAT BLOCKING gate is the primary risk mitigation for the entire phase.
</threat_model>

<verification>
**Plan 05 complete when:**

```bash
# Backend E2E test green
cd /root/jarvis && pnpm --filter @jarvis/backend-ts test --run -- src/agent/__tests__/graph.e2e

# Full backend + renderer suite green
pnpm --filter @jarvis/backend-ts test --run
pnpm --filter @jarvis/desktop test --run

# Documentation present
grep "AGENTIC_DISABLED" apps/backend-ts/.env.example
grep -c "Phase 66" .planning/STATE.md   # ≥10

# Manual UAT approved (record in commit message)
# User has typed "approved" or all 8 scenarios were verified pass
```

All commands exit 0 AND user has approved Task 2 checkpoint.
</verification>

<success_criteria>
- [ ] `apps/backend-ts/src/agent/__tests__/graph.e2e.test.ts` exists with 3 passing tests (happy path, cancel-before-start, edit-loop)
- [ ] `apps/backend-ts/.env.example` documents `AGENTIC_DISABLED` flag with default-behavior comment
- [ ] FULL backend suite green: `pnpm --filter @jarvis/backend-ts test --run` exits 0
- [ ] FULL renderer suite green: `pnpm --filter @jarvis/desktop test --run` exits 0
- [ ] BLOCKING manual UAT: User has tested all 8 scenarios (A-H) and typed "approved" OR all issues found in the failed scenario(s) have been routed to gap-closure planning
- [ ] `.planning/STATE.md` updated with ≥10 carry-forward bullets prefixed `[Phase 66-XX]:` covering all major lessons learned
- [ ] `.planning/STATE.md` § Current Position points to Phase 67 (or whichever is next per ROADMAP)
- [ ] `.planning/STATE.md` preserves ALL existing carry-forward content (only insertions, no deletions)
- [ ] AGENT-01 verified: multi-step task executes end-to-end automatically (UAT Scenario A or B)
- [ ] AGENT-02 verified: numbered plan + confirmation gate works via button + voice + text (UAT Scenarios A, B, D)
- [ ] AGENT-03 verified: real-time progress in chat AND orb badge shows AGENT N/M (UAT Scenarios A + G)
- [ ] AGENT-04 verified: cancel by button + text + voice halts within 1s (UAT Scenarios B, C)
- [ ] All `<threat_model>` mitigations satisfied: E2E test exists; UAT bloqueante is the human gate
</success_criteria>

<output>
After completion, create `.planning/phases/66-agentic-tasks/66-05-SUMMARY.md` with:
- E2E test count (3) + behaviors covered (happy path / cancel / edit-loop)
- UAT outcome: which scenarios passed, which were deferred (if any), with one-line user note per failed scenario
- Phase 66 totals: total plans (5), total tasks (~13), total tests added (~70 across backend + renderer), total files created/modified
- Decision matrix final state: D-01 through D-17 — all marked COMPLETE or explicitly DEFERRED with phase-future reference
- Carry-forward summary: top 3 patterns Phase 67 should leverage (SSE event extension, AbortSignal.any composition, multi-modal voice short-circuit)
- Recommendation: `/gsd-evolve-project` if PROJECT.md needs updates after this milestone, then `/gsd-plan-phase 67` for Proativo
</output>
