---
phase: 20-e2e-validation-python-comparison
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/scripts/e2e-compare.ts
  - package.json
  - apps/backend-ts/package.json
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-06
must_haves:
  truths:
    - "pnpm e2e executa o script e2e-compare.ts com tsx sem erros de módulo"
    - "O script verifica health de ambos os backends antes de iniciar — aborta com mensagem clara se algum estiver offline"
    - "O script envia 20 mensagens de texto para Python (8000) e TypeScript (8001) e coleta respostas"
    - "Comparação de texto usa cosine similarity com threshold configurável via TEXT_SIM_THRESHOLD (default 0.85)"
    - "Tool calls marcados como NOT_APPLICABLE quando tabela tool_calls está vazia em ambos os backends (Phase 18 pendente)"
    - "ChromaDB comparison usa embeddings das respostas reais de cada backend (não o mesmo texto duas vezes)"
    - "SQLite state compara role+content das mensagens (sem timestamps) entre os dois .db files"
    - "Performance mede latência de cada request e reporta ratio TS/Python"
    - "Relatório final mostra PASS/FAIL por categoria com exit code 0 se >=90% pass, 1 se <90%"
    - "Voice validation ausente é documentada como known limitation no relatório"
  artifacts:
    - path: "apps/backend-ts/scripts/e2e-compare.ts"
      provides: "Script standalone E2E que compara Python vs TypeScript backends"
      min_lines: 200
    - path: "package.json"
      provides: "Comando pnpm e2e no root"
      contains: "e2e"
    - path: "apps/backend-ts/package.json"
      provides: "Script e2e no backend-ts"
      contains: "e2e"
  key_links:
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "apps/backend-ts/src/memory/embeddings.ts"
      via: "import { embedText }"
      pattern: "embedText"
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "http://localhost:8000 e http://localhost:8001"
      via: "fetch POST /chat e GET /health"
      pattern: "PYTHON_URL|TS_URL"
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "SQLite .db files de ambos os backends"
      via: "better-sqlite3 readonly"
      pattern: "better-sqlite3|E2E_PYTHON_DB_PATH|E2E_TS_DB_PATH"
---

<objective>
Implementar o script standalone `apps/backend-ts/scripts/e2e-compare.ts` que valida paridade de outputs entre os backends Python e TypeScript, e adicionar o comando `pnpm e2e` ao root package.json e ao apps/backend-ts/package.json.

Purpose: Validar que o backend TypeScript produz outputs semanticamente equivalentes ao Python para os mesmos inputs antes do cutover em Phase 21. O script é a evidência de que a migração foi bem-sucedida.

Output: Script executável que produz relatório colorido PASS/FAIL por categoria com exit code baseado em threshold de 90%.

**Nota sobre localização do script (resolução de Q6 do CONTEXT.md):**
O CONTEXT.md Q6 indicou `/root/jarvis/scripts/e2e-compare.ts` (root-level), mas o script foi movido para `apps/backend-ts/scripts/e2e-compare.ts` por necessidade técnica: o import de `embedText` requer resolução ESM relativa — `../src/memory/embeddings.js` — que só funciona dentro do pacote `apps/backend-ts/`. Um script na raiz do monorepo não consegue importar de `apps/backend-ts/src/` via ESM sem path aliases ou symlinks. A localização em `apps/backend-ts/scripts/` segue o mesmo padrão de `embedding-parity.ts` já existente. O comando `pnpm e2e` no root delega para `pnpm --filter @jarvis/backend-ts e2e`, mantendo a UX idêntica.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-e2e-validation-python-comparison/20-CONTEXT.md
@.planning/phases/20-e2e-validation-python-comparison/20-RESEARCH.md

<interfaces>
<!-- Contratos relevantes. Executor usa diretamente sem explorar o codebase. -->

De apps/backend-ts/src/memory/embeddings.ts:
```typescript
export const EMBEDDING_MODEL = 'Xenova/all-MiniLM-L6-v2';
export const EMBEDDING_DIM = 384;

export async function embedText(text: string): Promise<Float32Array>
export async function embedBatch(texts: string[]): Promise<Float32Array[]>
```

De apps/backend-ts/scripts/embedding-parity.ts (padrão de cosine a reusar):
```typescript
function cosine(a: Float32Array, b: number[]): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    na += a[i] * a[i];
    nb += b[i] * b[i];
  }
  return dot / (Math.sqrt(na) * Math.sqrt(nb));
}
```

De apps/backend-ts/src/memory/schema.ts — tabelas SQLite do TS backend:
- `messages`: colunas id, conversation_id, role, content, created_at
- `tool_calls`: colunas id, timestamp, tool_name, params_json, outcome, output, error
- `user_profile`: colunas id, key, value, source, created_at

**Resoluções de Open Questions do RESEARCH.md:**

1. "Onde fica o SQLite do TS em dev?" → RESOLVIDO:
   - TypeScript: `DATABASE_PATH` env var, default `process.cwd()/jarvis.sqlite`
   - Quando backend-ts inicia de `apps/backend-ts/`, o DB fica em `apps/backend-ts/jarvis.sqlite`
   - Script E2E usa `E2E_TS_DB_PATH` env var (default: `apps/backend-ts/jarvis.sqlite` relativo à raiz do repo)

2. "Como enumerar tools para VAL-03?" → RESOLVIDO:
   - Consultar tabela SQLite `tool_calls` (se existir)
   - Se `tool_calls` estiver vazia em AMBOS os backends → retornar `NOT_APPLICABLE` (Phase 18 incompleta)
   - Script documenta isso no relatório como estado esperado

3. "Phases 18/19 completas?" → RESOLVIDO:
   - Pre-flight verifica health (backends respondendo), não feature completeness
   - VAL-03 usa NOT_APPLICABLE quando sem tool calls → não penaliza o overall score
   - Script documenta no relatório se VAL-03 foi NOT_APPLICABLE

SQLite paths usados pelo script:
- Python: env `E2E_PYTHON_DB_PATH`, default `data/jarvis.db` (relativo à raiz do repo)
- TypeScript: env `E2E_TS_DB_PATH`, default `apps/backend-ts/jarvis.sqlite` (relativo à raiz do repo)

Backend APIs:
- Python POST /chat: body `{message: string}` → resposta `{message: string}`
- TypeScript POST /chat: body `{message: string}` → resposta `{message: string}` (idêntico)
- Ambos GET /health: → resposta `{status: "ok"}`

Script está em `apps/backend-ts/scripts/` — mesmo diretório que `embedding-parity.ts`.
Import de embeddings: `import { embedText } from '../src/memory/embeddings.js'`
Runtime: `tsx scripts/e2e-compare.ts` via `pnpm --filter @jarvis/backend-ts e2e`
Root package.json: `pnpm --filter @jarvis/backend-ts e2e`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar e2e-compare.ts com pre-flight, loop de 20 requests e comparadores corrigidos</name>
  <files>apps/backend-ts/scripts/e2e-compare.ts</files>

  <read_first>
    - apps/backend-ts/scripts/embedding-parity.ts (padrão exato de script standalone com tsx — estrutura, imports, cosine function)
    - apps/backend-ts/src/memory/embeddings.ts (assinatura exata de embedText — retorna Promise&lt;Float32Array&gt;)
    - apps/backend-ts/src/memory/schema.ts (nomes das tabelas: messages, tool_calls, user_profile — confirmar colunas)
    - apps/backend-ts/src/memory/db.ts (confirmar path default do SQLite TS: `process.env.DATABASE_PATH || path.join(process.cwd(), 'jarvis.sqlite')`)
  </read_first>

  <action>
Criar `apps/backend-ts/scripts/e2e-compare.ts` completo. O script é standalone com tsx, sem vitest.

**Estrutura do arquivo (implementar exatamente nesta ordem):**

**Seção 1 — Header JSDoc com usage e env vars:**
```typescript
/**
 * E2E Comparison Script: Python (port 8000) vs TypeScript (port 8001)
 *
 * Valida paridade de outputs entre os dois backends para os mesmos inputs.
 * Categorias: Text Similarity (VAL-02), Tool Calls (VAL-03), SQLite State (VAL-04),
 * ChromaDB Embeddings (VAL-05), Performance (VAL-06).
 *
 * Voice validation SKIPPED: providers são diferentes por design
 * (nodejs-whisper vs faster-whisper, Speecht5 vs kokoro).
 * Known limitation documentada: TTS/STT parity não é validada nesta fase.
 *
 * Localização: apps/backend-ts/scripts/e2e-compare.ts
 * (Nota: localizado aqui em vez da raiz do repo por necessidade de resolução ESM —
 *  o import de embedText requer path relativo dentro do pacote backend-ts)
 *
 * Usage:
 *   pnpm e2e                         (via root package.json)
 *   pnpm --filter @jarvis/backend-ts e2e
 *
 * Env vars:
 *   FASTAPI_URL         Python backend URL (default: http://localhost:8000)
 *   BACKEND_TS_URL      TypeScript backend URL (default: http://localhost:8001)
 *   TEXT_SIM_THRESHOLD  Cosine similarity threshold for text (default: 0.85)
 *   E2E_PYTHON_DB_PATH  Path to Python SQLite .db (default: <repo-root>/data/jarvis.db)
 *   E2E_TS_DB_PATH      Path to TypeScript SQLite .db (default: <repo-root>/apps/backend-ts/jarvis.sqlite)
 *   E2E_PASS_THRESHOLD  Overall pass % threshold (default: 0.90)
 *
 * Exit codes:
 *   0 — overall pass rate >= E2E_PASS_THRESHOLD
 *   1 — overall pass rate < E2E_PASS_THRESHOLD OR pre-flight failed
 */
```

**Seção 2 — Imports e Config:**
```typescript
import { existsSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import Database from 'better-sqlite3';
import { embedText } from '../src/memory/embeddings.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const REPO_ROOT = resolve(__dirname, '../../../');

const PYTHON_URL = process.env.FASTAPI_URL ?? 'http://localhost:8000';
const TS_URL = process.env.BACKEND_TS_URL ?? 'http://localhost:8001';
const TEXT_SIM_THRESHOLD = parseFloat(process.env.TEXT_SIM_THRESHOLD ?? '0.85');
const PASS_THRESHOLD = parseFloat(process.env.E2E_PASS_THRESHOLD ?? '0.90');
const PYTHON_DB_PATH = process.env.E2E_PYTHON_DB_PATH ?? resolve(REPO_ROOT, 'data/jarvis.db');
const TS_DB_PATH = process.env.E2E_TS_DB_PATH ?? resolve(REPO_ROOT, 'apps/backend-ts/jarvis.sqlite');
```

**Seção 3 — ANSI colors e helpers de output:**
```typescript
const GREEN = '\x1b[32m';
const RED = '\x1b[31m';
const YELLOW = '\x1b[33m';
const CYAN = '\x1b[36m';
const RESET = '\x1b[0m';
const BOLD = '\x1b[1m';

function passStr(msg: string) { return `${GREEN}✅ PASS${RESET} ${msg}`; }
function failStr(msg: string) { return `${RED}❌ FAIL${RESET} ${msg}`; }
function warnStr(msg: string) { return `${YELLOW}⚠️  WARN${RESET} ${msg}`; }
```

**Seção 4 — TEST_INPUTS (20 mensagens independentes de contexto):**
```typescript
const TEST_INPUTS: string[] = [
  'Olá, qual é seu nome?',
  'Que horas são agora?',
  'Qual é a capital do Brasil?',
  'Quanto é 2 mais 2?',
  'Me diga algo interessante sobre astronomia.',
  'Qual é a linguagem de programação mais popular?',
  'O que é machine learning?',
  'Me recomende um filme de ficção científica.',
  'Qual é a diferença entre RAM e armazenamento?',
  'Como funciona a internet?',
  'O que é TypeScript?',
  'Me explique o que é um banco de dados.',
  'Qual é o animal mais rápido do mundo?',
  'O que é open source?',
  'Me diga uma curiosidade sobre o espaço.',
  'Qual é a diferença entre Python e JavaScript?',
  'O que é uma API REST?',
  'Me explique recursão em programação.',
  'Qual é o maior planeta do sistema solar?',
  'O que é cloud computing?',
];
```

**Seção 5 — Tipos internos:**
```typescript
interface ChatResponse {
  reply: string;
  latencyMs: number;
}

// 'NOT_APPLICABLE' usado quando tabela tool_calls está vazia (Phase 18 pendente)
type CompareOutcome = 'PASS' | 'FAIL' | 'NOT_APPLICABLE' | 'SKIP';

interface TestResult {
  input: string;
  python: ChatResponse | null;
  ts: ChatResponse | null;
  error?: string;
  textSim: number;
  textPass: CompareOutcome;
  toolCallsOutcome: CompareOutcome;
  sqlitePass: CompareOutcome;
  chromadbSim: number;
  chromadbPass: CompareOutcome;
  perfPass: CompareOutcome;
  perfRatio: number;
}
```

**Seção 6 — Pre-flight check (OBRIGATÓRIO — implementar antes do loop):**
```typescript
/**
 * Verifica que ambos os backends estão online antes de iniciar o suite.
 * Lança Error com instrução clara de como iniciar cada backend se algum estiver offline.
 */
async function preflight(): Promise<void> {
  const [pyOk, tsOk] = await Promise.all([
    fetch(`${PYTHON_URL}/health`).then(r => r.ok).catch(() => false),
    fetch(`${TS_URL}/health`).then(r => r.ok).catch(() => false),
  ]);
  if (!pyOk) {
    throw new Error(
      `Python backend (${PYTHON_URL}) não responde.\n` +
      `  Para iniciar: cd src/jarvis && uvicorn main:app --port 8000`
    );
  }
  if (!tsOk) {
    throw new Error(
      `TypeScript backend (${TS_URL}) não responde.\n` +
      `  Para iniciar: pnpm --filter @jarvis/backend-ts dev`
    );
  }
  console.log(`${GREEN}✅ Pre-flight OK${RESET} — ambos os backends estão online`);
}
```

**Seção 7 — Helpers:**
```typescript
async function sendChat(baseUrl: string, message: string): Promise<ChatResponse> {
  const t0 = performance.now();
  const res = await fetch(`${baseUrl}/chat`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message }),
  });
  const latencyMs = performance.now() - t0;
  if (!res.ok) {
    throw new Error(`${baseUrl}/chat retornou HTTP ${res.status}`);
  }
  const data = await res.json() as { message: string };
  return { reply: data.message, latencyMs };
}

function cosine(a: Float32Array, b: Float32Array): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    na += a[i] * a[i];
    nb += b[i] * b[i];
  }
  return dot / (Math.sqrt(na) * Math.sqrt(nb));
}

function openDbReadonly(dbPath: string): Database.Database | null {
  if (!existsSync(dbPath)) {
    console.warn(warnStr(`SQLite não encontrado: ${dbPath}`));
    return null;
  }
  return new Database(dbPath, { readonly: true });
}
```

**Seção 8 — Comparadores (com correções B-01 e B-02):**

**compareText** (VAL-02) — embeddings das respostas reais:
```typescript
async function compareText(
  pyReply: string,
  tsReply: string,
): Promise<{ sim: number; outcome: CompareOutcome }> {
  const [pyVec, tsVec] = await Promise.all([embedText(pyReply), embedText(tsReply)]);
  const sim = cosine(pyVec, tsVec);
  return { sim, outcome: sim >= TEXT_SIM_THRESHOLD ? 'PASS' : 'FAIL' };
}
```

**compareToolCalls** (VAL-03) — FIX B-01: NOT_APPLICABLE quando ambos têm zero rows:
```typescript
function compareToolCalls(
  pyDb: Database.Database | null,
  tsDb: Database.Database | null,
  afterTimestamp: string,
): { outcome: CompareOutcome; reason?: string } {
  if (!pyDb || !tsDb) {
    return { outcome: 'SKIP', reason: 'SQLite não disponível' };
  }

  // Verifica se a tabela tool_calls existe antes de consultar
  const pyHasTable = pyDb
    .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='tool_calls'")
    .get() != null;
  const tsHasTable = tsDb
    .prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='tool_calls'")
    .get() != null;

  if (!pyHasTable || !tsHasTable) {
    return {
      outcome: 'NOT_APPLICABLE',
      reason: 'Tabela tool_calls ausente — Phase 18 pode estar incompleta',
    };
  }

  type ToolRow = { tool_name: string; params_json: string | null };
  const pyTools = pyDb
    .prepare("SELECT tool_name, params_json FROM tool_calls WHERE timestamp >= ? ORDER BY id")
    .all(afterTimestamp) as ToolRow[];
  const tsTools = tsDb
    .prepare("SELECT tool_name, params_json FROM tool_calls WHERE timestamp >= ? ORDER BY id")
    .all(afterTimestamp) as ToolRow[];

  // FIX B-01: zero rows em AMBOS = NOT_APPLICABLE, não PASS vacuous
  if (pyTools.length === 0 && tsTools.length === 0) {
    return {
      outcome: 'NOT_APPLICABLE',
      reason: 'Nenhum tool call registrado em nenhum backend — Phase 18 pode estar incompleta',
    };
  }

  if (pyTools.length !== tsTools.length) {
    return { outcome: 'FAIL' };
  }

  const allMatch = pyTools.every((r, i) => r.tool_name === tsTools[i].tool_name);
  return { outcome: allMatch ? 'PASS' : 'FAIL' };
}
```

**compareSqliteMessages** (VAL-04):
```typescript
function compareSqliteMessages(
  pyDb: Database.Database | null,
  tsDb: Database.Database | null,
  inputMessage: string,
): CompareOutcome {
  if (!pyDb || !tsDb) return 'SKIP';

  type MsgRow = { role: string; content: string };
  // Compara apenas role e content (não timestamps — serão sempre diferentes)
  const pyMsg = pyDb
    .prepare("SELECT role, content FROM messages WHERE content = ? ORDER BY id DESC LIMIT 1")
    .get(inputMessage) as MsgRow | undefined;
  const tsMsg = tsDb
    .prepare("SELECT role, content FROM messages WHERE content = ? ORDER BY id DESC LIMIT 1")
    .get(inputMessage) as MsgRow | undefined;

  if (!pyMsg || !tsMsg) return 'FAIL';
  return pyMsg.role === tsMsg.role ? 'PASS' : 'FAIL';
}
```

**compareChromadb** (VAL-05) — FIX B-02: embeddings das respostas REAIS de cada backend:
```typescript
/**
 * VAL-05: Compara a similaridade semântica entre as respostas REAIS dos dois backends.
 *
 * FIX (B-02): A versão anterior chamava embedText(text) duas vezes para o MESMO texto,
 * produzindo sempre ~1.0 — sinal nulo. Agora embeda a resposta real do Python e a
 * resposta real do TypeScript e compara ENTRE ELAS. Isso reutiliza os vetores do
 * fluxo compareText (VAL-02) e aplica ao VAL-05, dando sinal real sobre
 * equivalência semântica dos backends via ChromaDB (mesmo modelo de embedding).
 *
 * threshold: 0.95 (mais restrito que VAL-02, pois estamos comparando qualidade
 * de embedding entre backends — esperamos alta similaridade se ambos estiverem corretos)
 */
async function compareChromadb(
  pythonReply: string,
  tsReply: string,
): Promise<{ sim: number; outcome: CompareOutcome }> {
  const [pyVec, tsVec] = await Promise.all([embedText(pythonReply), embedText(tsReply)]);
  const sim = cosine(pyVec, tsVec);
  return { sim, outcome: sim >= 0.95 ? 'PASS' : 'FAIL' };
}
```

**Seção 9 — função main():**

A função `main()` deve:

1. Chamar `await preflight()` — se lançar, capturar no try/catch externo e sair com exit code 1
2. Abrir os DBs com `openDbReadonly(PYTHON_DB_PATH)` e `openDbReadonly(TS_DB_PATH)`
3. Exibir cabeçalho e aviso sobre download do modelo Xenova na primeira execução
4. Para cada input em TEST_INPUTS (loop sequencial para não sobrecarregar os backends):
   a. Registrar `beforeTimestamp = new Date().toISOString()` antes dos requests
   b. Chamar `[pyRes, tsRes] = await Promise.all([sendChat(PYTHON_URL, input), sendChat(TS_URL, input)])` em paralelo
   c. Se qualquer sendChat lançar: capturar, marcar todas as categorias desse input como `'FAIL'`, logar o erro, e continuar para o próximo input
   d. Chamar `compareText(pyRes.reply, tsRes.reply)` → textSim, textPass
   e. Chamar `compareToolCalls(pyDb, tsDb, beforeTimestamp)` → toolCallsOutcome
   f. Chamar `compareSqliteMessages(pyDb, tsDb, input)` → sqlitePass
   g. Chamar `compareChromadb(pyRes.reply, tsRes.reply)` → chromadbSim, chromadbPass (FIX B-02: passa as respostas reais, não o input)
   h. Calcular `perfRatio = tsRes.latencyMs / pyRes.latencyMs`, `perfPass = perfRatio <= 1.10 ? 'PASS' : 'FAIL'`
   i. Acumular em `results: TestResult[]`
5. Fechar os DBs: `pyDb?.close(); tsDb?.close()`
6. Imprimir o relatório final — ver formato exato abaixo
7. Calcular overall e chamar `process.exit(overall >= PASS_THRESHOLD ? 0 : 1)`

**Formato exato do relatório (implementar como template literal):**

```
===== E2E COMPARISON REPORT =====
Text Similarity: 18/20 PASS (2 FAIL) — avg similarity: 0.91
Tool Calls:      NOT_APPLICABLE (no tool calls registered — Phase 18 incomplete)
SQLite State:    20/20 PASS
ChromaDB:        19/20 PASS (1 FAIL) — avg similarity: 0.97
Performance:     20/20 PASS (avg TS/Python ratio: 1.04)
=================================
[KNOWN LIMITATION] Voice/audio validation skipped: STT/TTS providers differ by design
(nodejs-whisper vs faster-whisper, Speecht5 vs kokoro). Not a bug — documented tradeoff.
=================================
Overall: 97/100 (97%) — THRESHOLD: 90%
```

**Contagem no overall:**
- PASS = 1 ponto
- FAIL = 0 pontos
- NOT_APPLICABLE = não conta no numerador nem no denominador (não penaliza)
- SKIP = não conta (DB indisponível — não penaliza)

**Encapsulamento do main():**
```typescript
main().catch((err: unknown) => {
  console.error(`${RED}[e2e] Falha fatal:${RESET}`, err instanceof Error ? err.message : err);
  process.exit(1);
});
```
  </action>

  <verify>
    <automated>wc -l /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts && grep 'compareToolCalls\|compareText\|compareChromadb\|compareSqlite\|preflight' /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `apps/backend-ts/scripts/e2e-compare.ts` existe com pelo menos 200 linhas (`wc -l` >= 200)
    - `grep "preflight" apps/backend-ts/scripts/e2e-compare.ts` mostra definição e chamada da função de pre-flight
    - `grep "NOT_APPLICABLE" apps/backend-ts/scripts/e2e-compare.ts` mostra ao menos 2 ocorrências (tool_calls vazio e tabela ausente)
    - `grep "pythonReply\|tsReply" apps/backend-ts/scripts/e2e-compare.ts` confirma que compareChromadb recebe as respostas reais (não o input text)
    - `grep "E2E_PYTHON_DB_PATH\|E2E_TS_DB_PATH\|TEXT_SIM_THRESHOLD\|E2E_PASS_THRESHOLD" apps/backend-ts/scripts/e2e-compare.ts` confirma env vars configuráveis
    - `grep "KNOWN LIMITATION\|nodejs-whisper\|faster-whisper" apps/backend-ts/scripts/e2e-compare.ts` confirma documentação do voice skip
    - `grep "process\.exit" apps/backend-ts/scripts/e2e-compare.ts` mostra exit code baseado em threshold
    - `grep -c "compareText\|compareToolCalls\|compareSqliteMessages\|compareChromadb" apps/backend-ts/scripts/e2e-compare.ts` >= 4 (uma ocorrência de definição de cada)
    - Arquivo contém `===== E2E COMPARISON REPORT =====` como template do relatório
    - `grep "Promise\.all\|sendChat" apps/backend-ts/scripts/e2e-compare.ts` confirma requests paralelos para os dois backends
    - `grep "sqlite_master" apps/backend-ts/scripts/e2e-compare.ts` confirma check de existência da tabela tool_calls
  </acceptance_criteria>

  <done>Script e2e-compare.ts completo com: pre-flight check, NOT_APPLICABLE para tool calls vazios (B-01 corrigido), compareChromadb usando respostas reais dos backends (B-02 corrigido), e todas as categorias de validação (VAL-01 a VAL-06) com voice documentado como known limitation.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar pnpm e2e ao root package.json e ao apps/backend-ts/package.json</name>
  <files>package.json, apps/backend-ts/package.json</files>

  <read_first>
    - package.json (scripts existentes no root — não sobrescrever nada)
    - apps/backend-ts/package.json (scripts existentes: dev, build, test, parity:embeddings, fixtures:tools — confirmar nomes)
  </read_first>

  <action>
**1. Modificar `apps/backend-ts/package.json`** — adicionar script `e2e` aos scripts existentes:
```json
"e2e": "tsx scripts/e2e-compare.ts"
```
Inserir após `"parity:embeddings": "tsx scripts/embedding-parity.ts"` (ou no final do objeto scripts, se a chave não existir).

**2. Modificar root `package.json`** — adicionar script `e2e` aos scripts existentes:
```json
"e2e": "pnpm --filter @jarvis/backend-ts e2e"
```
Inserir após `"typecheck"` nos scripts do root (ou no final do objeto scripts).

**Não alterar nenhum outro campo** de nenhum dos dois arquivos. Apenas adicionar a chave `"e2e"` ao objeto `"scripts"` em cada um.
  </action>

  <verify>
    <automated>grep '"e2e"' /root/jarvis/package.json /root/jarvis/apps/backend-ts/package.json</automated>
  </verify>

  <acceptance_criteria>
    - `grep '"e2e"' /root/jarvis/package.json` retorna linha contendo `"pnpm --filter @jarvis/backend-ts e2e"`
    - `grep '"e2e"' /root/jarvis/apps/backend-ts/package.json` retorna linha contendo `"tsx scripts/e2e-compare.ts"`
    - `node -e "JSON.parse(require('fs').readFileSync('/root/jarvis/package.json','utf8'))"` — sem erro de parse (JSON válido)
    - `node -e "JSON.parse(require('fs').readFileSync('/root/jarvis/apps/backend-ts/package.json','utf8'))"` — sem erro de parse (JSON válido)
    - Scripts existentes (dev, build, test, parity:embeddings, etc.) permanecem inalterados
  </acceptance_criteria>

  <done>pnpm e2e no root invoca o script e2e-compare.ts via tsx no backend-ts. Todos os scripts existentes preservados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→backends | Script faz fetch para localhost:8000 e localhost:8001 — sem auth, apenas desenvolvimento local |
| script→SQLite | Script abre .db files em modo readonly — sem escrita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Information Disclosure | e2e-compare.ts SQLite read | accept | Arquivo aberto em readonly. Script E2E é ferramenta de desenvolvimento local, não exposto em produção. Dados lidos são logs de conversa local. |
| T-20-04 | Denial of Service | e2e-compare.ts loop 20 requests | accept | 20 requests para localhost — sem impacto externo. Script é pontual, não contínuo. Pre-flight garante que backends estão prontos antes de iniciar. |
| T-20-05 | Spoofing | e2e-compare.ts pre-flight health check | accept | Pre-flight não autentica — apenas verifica HTTP 200 em /health. Risco de spoofing é nulo em ambiente local (localhost). |
</threat_model>

<verification>
Após completar as duas tasks, verificar que:
1. `wc -l /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — resultado >= 200 linhas
2. `grep 'compareToolCalls\|compareText\|compareChromadb\|compareSqlite\|preflight' /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — todas as funções presentes
3. `grep '"e2e"' /root/jarvis/package.json` — retorna script correto
4. `grep '"e2e"' /root/jarvis/apps/backend-ts/package.json` — retorna script correto
5. `grep "NOT_APPLICABLE" /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — ao menos 2 ocorrências
6. `grep "pythonReply\|tsReply" /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — confirma B-02 corrigido (compareChromadb usa respostas reais)
7. `grep "preflight" /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — confirma B-03 (pre-flight presente)
8. `cd /root/jarvis/apps/backend-ts && npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "e2e-compare" || echo "No TS errors in e2e-compare"` — sem erros de tipo no script
</verification>

<success_criteria>
- Script e2e-compare.ts implementado com todas as 5 categorias de validação (VAL-01 a VAL-06)
- Pre-flight check verifica health de ambos os backends antes do loop (B-03)
- VAL-03 (Tool Calls) reporta NOT_APPLICABLE quando tool_calls está vazia em ambos — não PASS vacuous (B-01)
- VAL-05 (ChromaDB) usa embeddings das respostas reais de cada backend para comparação cruzada — não o mesmo texto duas vezes (B-02)
- Open Questions do RESEARCH.md resolvidas e documentadas no script e no plano (B-04)
- Voice validation documentada como known limitation (per D-01 do CONTEXT.md)
- pnpm e2e funciona a partir da raiz do monorepo
- Exit code 0 quando overall >= 90%, exit code 1 quando < 90% ou pre-flight falha
- Relatório colorido com formato exato especificado no CONTEXT.md (per D-06)
- Env vars configuráveis: TEXT_SIM_THRESHOLD, E2E_PYTHON_DB_PATH, E2E_TS_DB_PATH, E2E_PASS_THRESHOLD
- Localização do script em apps/backend-ts/scripts/ documentada (resolução de Q6 do CONTEXT.md — W-02)
</success_criteria>

<output>
Após completar, criar `.planning/phases/20-e2e-validation-python-comparison/20-02-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
