---
phase: 21-cutover-python-deprecation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jarvis/
  - src/
  - Dockerfile.python
  - docker-compose.yml
  - pyproject.toml
  - requirements-docker.txt
autonomous: true
requirements:
  - VAL-10

must_haves:
  truths:
    - "O diretório src/jarvis/ não existe mais no monorepo"
    - "O arquivo Dockerfile.python não existe mais na raiz"
    - "O arquivo pyproject.toml não existe mais na raiz"
    - "O arquivo requirements-docker.txt não existe mais na raiz"
    - "docker-compose.yml contém apenas backend-ts e gateway (sem python-service)"
    - "docker-compose.yml não tem depends_on referenciando python-service"
    - "docker-compose.yml não tem environment FASTAPI_URL referenciando python-service"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Compose com 2 serviços: gateway + backend-ts"
      contains: "backend-ts"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile.backend-ts"
      via: "build.dockerfile"
      pattern: "Dockerfile\\.backend-ts"
---

<objective>
Remover completamente o backend Python do monorepo (D-02) e o serviço python-service do Docker Compose (D-03): deletar src/jarvis/, Dockerfile.python, e limpar docker-compose.yml.

Purpose: Finalizar a deprecação física do Python. Após esse plano, o monorepo não contém mais nenhum artefato Python além de configurações herdadas do sistema.

Output: src/jarvis/ inexistente, Dockerfile.python inexistente, docker-compose.yml com apenas gateway + backend-ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-cutover-python-deprecation/21-CONTEXT.md

<interfaces>
<!-- Estado atual de docker-compose.yml — extraído antes de qualquer edição -->

Estado atual do docker-compose.yml tem 3 serviços:

1. python-service (linhas 2-21) — REMOVER COMPLETO:
   ```yaml
   python-service:
     build:
       context: .
       dockerfile: Dockerfile.python
     expose: ["8000"]
     networks: [jarvis-net]
     volumes: [./data:/app/data]
     env_file: .env
     extra_hosts: [host.docker.internal:host-gateway]
     healthcheck: {test: curl localhost:8000/health/ready, ...}
     restart: unless-stopped
   ```

2. gateway (linhas 23-37) — MANTER, MAS LIMPAR:
   - Remover: `environment: - FASTAPI_URL=http://python-service:8000`
   - Remover: `depends_on: python-service: condition: service_healthy`
   - Manter: ports, networks, env_file, restart

3. backend-ts (linhas 39-63) — MANTER INTACTO

Resultado esperado: gateway + backend-ts apenas, sem referências a python-service.

Estado atual de src/jarvis/:
Subdiretórios: api/, core/, executor/, llm/, memory/, platform/, tools/
Arquivos: __init__.py, __main__.py, config.py + __pycache__/
Tudo será deletado (rm -rf).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar src/jarvis/, src/ e Dockerfile.python</name>
  <files>
    src/jarvis/
    src/
    Dockerfile.python
  </files>
  <read_first>
    Dockerfile.python
    pyproject.toml
    requirements-docker.txt
  </read_first>
  <action>
    Deletar permanentemente (per D-02):

    ```bash
    # Deletar backend Python inteiro
    rm -rf /root/jarvis/src/jarvis/

    # Se src/ ficou vazio, remover também
    if [ -z "$(ls -A /root/jarvis/src/ 2>/dev/null)" ]; then
      rmdir /root/jarvis/src/
    fi

    # Deletar Dockerfile Python
    rm /root/jarvis/Dockerfile.python

    # Deletar dependências Python da raiz do monorepo
    rm /root/jarvis/pyproject.toml
    rm /root/jarvis/requirements-docker.txt
    ```

    Não criar arquivo de migração ou histórico. Remoção definitiva.
    O histórico de código está preservado no git — sem necessidade de README de deprecação.
  </action>
  <verify>
    <automated>ls /root/jarvis/src/jarvis 2>/dev/null | wc -l</automated>
    Deve retornar 0.
    <automated>ls /root/jarvis/Dockerfile.python 2>/dev/null | wc -l</automated>
    Deve retornar 0.
    <automated>ls /root/jarvis/pyproject.toml 2>/dev/null | wc -l</automated>
    Deve retornar 0.
    <automated>ls /root/jarvis/requirements-docker.txt 2>/dev/null | wc -l</automated>
    Deve retornar 0.
  </verify>
  <acceptance_criteria>
    - `ls /root/jarvis/src/jarvis 2>/dev/null | wc -l` retorna 0 (diretório não existe)
    - `ls /root/jarvis/src/ 2>/dev/null | wc -l` retorna 0 (src/ vazio foi removido) OU src/ não existe
    - `ls /root/jarvis/Dockerfile.python 2>/dev/null | wc -l` retorna 0 (arquivo não existe)
    - `find /root/jarvis/src -name "*.py" 2>/dev/null | wc -l` retorna 0
    - `ls /root/jarvis/pyproject.toml 2>/dev/null | wc -l` retorna 0 (arquivo não existe)
    - `ls /root/jarvis/requirements-docker.txt 2>/dev/null | wc -l` retorna 0 (arquivo não existe)
  </acceptance_criteria>
  <done>src/jarvis/, Dockerfile.python, pyproject.toml e requirements-docker.txt não existem mais. src/ removido se estava vazio.</done>
</task>

<task type="auto">
  <name>Task 2: Limpar docker-compose.yml — remover python-service</name>
  <files>
    docker-compose.yml
  </files>
  <read_first>
    docker-compose.yml
  </read_first>
  <action>
    Editar docker-compose.yml (per D-03) para remover completamente o serviço python-service e suas referências:

    **Resultado final esperado do arquivo:**
    ```yaml
    services:
      gateway:
        build:
          context: .
          dockerfile: Dockerfile.node
        ports:
          - "3000:3000"
        networks:
          - jarvis-net
        env_file: .env
        restart: unless-stopped

      backend-ts:
        build:
          context: .
          dockerfile: Dockerfile.backend-ts
        container_name: jarvis-backend-ts
        expose:
          - "8001"
        networks:
          - jarvis-net
        volumes:
          - ./data:/app/data
        env_file: .env
        environment:
          - BACKEND_TS_PORT=8001
          - NODE_ENV=production
        extra_hosts:
          - "host.docker.internal:host-gateway"
        healthcheck:
          test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
          interval: 10s
          timeout: 5s
          retries: 3
          start_period: 30s
        restart: unless-stopped

    networks:
      jarvis-net:
        driver: bridge
    ```

    Remoções específicas:
    1. Bloco inteiro `python-service:` (linhas 2-21)
    2. No bloco `gateway:`: remover `environment: - FASTAPI_URL=http://python-service:8000`
    3. No bloco `gateway:`: remover `depends_on:` e suas sub-linhas `python-service: condition: service_healthy`

    O backend-ts mantém o comentário interno (linhas de comment podem ser removidos pois referenciavam "Phase 21").
  </action>
  <verify>
    <automated>grep -c "python-service" /root/jarvis/docker-compose.yml</automated>
    Deve retornar 0.
    <automated>grep -c "FASTAPI_URL" /root/jarvis/docker-compose.yml</automated>
    Deve retornar 0.
    <automated>grep -c "depends_on" /root/jarvis/docker-compose.yml</automated>
    Deve retornar 0.
    <automated>docker compose -f /root/jarvis/docker-compose.yml config --quiet 2>&1 | wc -l</automated>
    Deve retornar 0 (sem erros de validação YAML).
  </verify>
  <acceptance_criteria>
    - `grep "python-service" /root/jarvis/docker-compose.yml` retorna vazio (sem match)
    - `grep "Dockerfile.python" /root/jarvis/docker-compose.yml` retorna vazio (sem match)
    - `grep "FASTAPI_URL" /root/jarvis/docker-compose.yml` retorna vazio (sem match)
    - `grep "depends_on" /root/jarvis/docker-compose.yml` retorna vazio (sem match)
    - `docker compose -f /root/jarvis/docker-compose.yml config --quiet` completa sem erros
    - O arquivo contém exatamente 2 serviços: `gateway` e `backend-ts`
  </acceptance_criteria>
  <done>docker-compose.yml contém apenas gateway e backend-ts. Nenhuma referência a python-service, FASTAPI_URL ou depends_on permanece.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker-compose→serviços | Compose orquestra containers na rede interna jarvis-net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | docker-compose.yml | accept | Remoção de depends_on é segura — gateway não precisa mais esperar python-service que não existe |
| T-21-05 | Denial of Service | src/jarvis/ removido | accept | Código Python não estava em produção no runtime Docker — remoção não afeta disponibilidade |
| T-21-06 | Information Disclosure | Dockerfile.python removido | mitigate | Verificar que nenhuma secret estava hardcoded no Dockerfile antes de deletar (ler antes de rm) |
</threat_model>

<verification>
Verificação completa do Plan 02:

```bash
# 1. Python backend deletado
ls /root/jarvis/src/jarvis 2>/dev/null | wc -l            # deve ser 0
ls /root/jarvis/Dockerfile.python 2>/dev/null | wc -l     # deve ser 0

# 2. docker-compose.yml sem python-service
grep "python-service" /root/jarvis/docker-compose.yml     # sem output
grep "FASTAPI_URL" /root/jarvis/docker-compose.yml        # sem output
grep "depends_on" /root/jarvis/docker-compose.yml         # sem output

# 3. docker-compose YAML válido
docker compose -f /root/jarvis/docker-compose.yml config --quiet

# 4. Apenas 2 serviços no Compose
grep "^  [a-z]" /root/jarvis/docker-compose.yml           # deve listar: gateway, backend-ts
```
</verification>

<success_criteria>
- src/jarvis/ inexistente
- src/ inexistente (ou vazio se outras coisas existirem)
- Dockerfile.python inexistente
- pyproject.toml inexistente
- requirements-docker.txt inexistente
- docker-compose.yml com 2 serviços: gateway e backend-ts
- Nenhuma referência a python-service, FASTAPI_URL, Dockerfile.python ou depends_on no Compose
- docker compose config --quiet sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-cutover-python-deprecation/21-02-SUMMARY.md`
</output>
