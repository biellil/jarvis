---
phase: 21-cutover-python-deprecation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/src/config.ts
  - apps/gateway/src/routes/chat.ts
  - apps/gateway/src/middleware/backendRouter.ts
  - apps/gateway/src/middleware/backendRouter.test.ts
autonomous: true
requirements:
  - VAL-08
  - VAL-09

must_haves:
  truths:
    - "Gateway roteia 100% dos requests para backendTsUrl sem qualquer lógica condicional"
    - "Nenhum arquivo backendRouter.ts ou backendRouter.test.ts existe em apps/gateway/src/middleware/"
    - "config.ts não contém fastapiUrl nem leitura de FASTAPI_URL"
    - "routes/chat.ts não importa nem usa resolveUpstreamUrl"
    - "Testes do gateway passam (sem backendRouter.test.ts quebrado)"
  artifacts:
    - path: "apps/gateway/src/config.ts"
      provides: "Configuração do gateway com apenas backendTsUrl, gatewayPort, apiKey"
      contains: "backendTsUrl"
    - path: "apps/gateway/src/routes/chat.ts"
      provides: "3 rotas chat hardcoded para config.backendTsUrl"
      contains: "config.backendTsUrl"
  key_links:
    - from: "apps/gateway/src/routes/chat.ts"
      to: "config.backendTsUrl"
      via: "import direto"
      pattern: "config\\.backendTsUrl"
---

<objective>
Remover a lógica de feature flag do gateway (D-01): deletar backendRouter.ts e backendRouter.test.ts, e hardcodar TypeScript como único backend em config.ts e routes/chat.ts.

Purpose: O gateway deixa de ser roteador condicional e se torna proxy simples para o backend TypeScript. Elimina a complexidade do feature flag VAL-07 e completa o cutover de tráfego (VAL-08).

Output: Gateway sem Python routing — config.ts com 3 campos, routes/chat.ts com config.backendTsUrl direto, sem backendRouter.* em middleware/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-cutover-python-deprecation/21-CONTEXT.md

<interfaces>
<!-- Estado atual dos arquivos a modificar — extraído antes de qualquer edição -->

De apps/gateway/src/config.ts (estado ATUAL — será simplificado):
```typescript
export const config = {
  fastapiUrl: process.env.FASTAPI_URL ?? "http://localhost:8000",   // REMOVER
  backendTsUrl: process.env.BACKEND_TS_URL ?? "http://localhost:8001", // MANTER
  gatewayPort: parseInt(process.env.GATEWAY_PORT ?? "3000", 10),    // MANTER
  apiKey: process.env.JARVIS_API_KEY as string | undefined,          // MANTER
} as const;
```

De apps/gateway/src/routes/chat.ts (estado ATUAL — 3 ocorrências de resolveUpstreamUrl):
- Linha 6: `import { resolveUpstreamUrl } from "../middleware/backendRouter.js";`
- Linha 20: `` `${resolveUpstreamUrl(req)}/chat` ``
- Linha 69: `` `${resolveUpstreamUrl(req)}/chat/stream?message=...` ``
- Linha 128: `` `${resolveUpstreamUrl(req)}/chat/audio` ``

Após a mudança, todas as 3 devem usar `config.backendTsUrl` diretamente.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar backendRouter.ts e backendRouter.test.ts</name>
  <files>
    apps/gateway/src/middleware/backendRouter.ts
    apps/gateway/src/middleware/backendRouter.test.ts
  </files>
  <read_first>
    apps/gateway/src/middleware/backendRouter.ts
    apps/gateway/src/middleware/backendRouter.test.ts
  </read_first>
  <action>
    Deletar permanentemente os dois arquivos (per D-01):

    ```bash
    rm /root/jarvis/apps/gateway/src/middleware/backendRouter.ts
    rm /root/jarvis/apps/gateway/src/middleware/backendRouter.test.ts
    ```

    Esses arquivos implementam o feature flag X-Backend-Version que deixa de existir.
    Não criar arquivo de histórico ou deprecação — remoção definitiva.
  </action>
  <verify>
    <automated>ls /root/jarvis/apps/gateway/src/middleware/backendRouter.ts 2>/dev/null | wc -l</automated>
    Deve retornar 0 (arquivo não existe).
    <automated>ls /root/jarvis/apps/gateway/src/middleware/backendRouter.test.ts 2>/dev/null | wc -l</automated>
    Deve retornar 0 (arquivo não existe).
  </verify>
  <acceptance_criteria>
    - `ls apps/gateway/src/middleware/backendRouter.ts 2>/dev/null | wc -l` retorna 0
    - `ls apps/gateway/src/middleware/backendRouter.test.ts 2>/dev/null | wc -l` retorna 0
    - `ls apps/gateway/src/middleware/` lista apenas: errorHandler.ts, validate.ts
  </acceptance_criteria>
  <done>Nenhum dos dois arquivos existe mais em apps/gateway/src/middleware/</done>
</task>

<task type="auto">
  <name>Task 2: Simplificar config.ts e routes/chat.ts — TypeScript hardcoded</name>
  <files>
    apps/gateway/src/config.ts
    apps/gateway/src/routes/chat.ts
  </files>
  <read_first>
    apps/gateway/src/config.ts
    apps/gateway/src/routes/chat.ts
  </read_first>
  <action>
    **Arquivo 1: apps/gateway/src/config.ts** (per D-01)

    Substituir o conteúdo completo por:
    ```typescript
    export const config = {
      backendTsUrl: process.env.BACKEND_TS_URL ?? "http://localhost:8001",
      gatewayPort: parseInt(process.env.GATEWAY_PORT ?? "3000", 10),
      apiKey: process.env.JARVIS_API_KEY as string | undefined,
    } as const;
    ```

    Remover linha `fastapiUrl` — FASTAPI_URL não é mais lida em nenhum lugar.

    **Arquivo 2: apps/gateway/src/routes/chat.ts** (per D-01)

    Fazer as seguintes alterações cirúrgicas:
    1. Remover a linha de import: `import { resolveUpstreamUrl } from "../middleware/backendRouter.js";`
    2. Substituir as 3 ocorrências de `resolveUpstreamUrl(req)` por `config.backendTsUrl`:
       - Linha 20: `fetch(\`${resolveUpstreamUrl(req)}/chat\`` → `fetch(\`${config.backendTsUrl}/chat\``
       - Linha 69: `fetch(\`${resolveUpstreamUrl(req)}/chat/stream?...` → `fetch(\`${config.backendTsUrl}/chat/stream?...`
       - Linha 128: `fetch(\`${resolveUpstreamUrl(req)}/chat/audio\`` → `fetch(\`${config.backendTsUrl}/chat/audio\``

    O import de `config` já existe na linha 4 — não duplicar.

    Verificar que o comentário `// GW-01: POST /chat` em linha 17 e similares são mantidos.
  </action>
  <verify>
    <automated>grep -c "fastapiUrl" /root/jarvis/apps/gateway/src/config.ts</automated>
    Deve retornar 0.
    <automated>grep -c "resolveUpstreamUrl" /root/jarvis/apps/gateway/src/routes/chat.ts</automated>
    Deve retornar 0.
    <automated>grep -c "backendTsUrl" /root/jarvis/apps/gateway/src/routes/chat.ts</automated>
    Deve retornar 3 (uma por rota: /chat, /chat/stream, /chat/audio).
    <automated>cd /root/jarvis && pnpm --filter @jarvis/gateway build 2>&1 | tail -5</automated>
    Deve compilar sem erros de TypeScript.
  </verify>
  <acceptance_criteria>
    - `grep "fastapiUrl" apps/gateway/src/config.ts` retorna vazio (sem match)
    - `grep "FASTAPI_URL" apps/gateway/src/config.ts` retorna vazio (sem match)
    - `grep "resolveUpstreamUrl" apps/gateway/src/routes/chat.ts` retorna vazio (sem match)
    - `grep "backendRouter" apps/gateway/src/routes/chat.ts` retorna vazio (sem match)
    - `grep -c "config.backendTsUrl" apps/gateway/src/routes/chat.ts` retorna 3
    - `pnpm --filter @jarvis/gateway build` completa sem erros TypeScript
    - `pnpm --filter @jarvis/gateway test --run` passa (testes existentes, sem backendRouter.test.ts)
  </acceptance_criteria>
  <done>
    config.ts tem apenas 3 campos (backendTsUrl, gatewayPort, apiKey). routes/chat.ts usa config.backendTsUrl diretamente nas 3 rotas. Build e testes passam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→gateway | Requests externos chegam no gateway sem autenticação de rede |
| gateway→backend-ts | Tráfego interno, sem TLS (rede Docker interna) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing | gateway/routes/chat.ts | accept | X-Backend-Version header removido — não há mais superfície de roteamento condicional por header |
| T-21-02 | Tampering | config.ts | mitigate | backendTsUrl lido de env var com default seguro localhost — nunca aceitar URL de header de request |
| T-21-03 | Information Disclosure | backendRouter removido | accept | Remoção elimina lógica de branching — superfície de ataque reduzida, não aumentada |
</threat_model>

<verification>
Verificação completa do Plan 01:

```bash
# 1. Arquivos deletados
ls /root/jarvis/apps/gateway/src/middleware/backendRouter.ts 2>/dev/null | wc -l   # deve ser 0
ls /root/jarvis/apps/gateway/src/middleware/backendRouter.test.ts 2>/dev/null | wc -l  # deve ser 0

# 2. config.ts sem fastapiUrl
grep "fastapiUrl\|FASTAPI_URL" /root/jarvis/apps/gateway/src/config.ts  # sem output

# 3. routes/chat.ts sem resolveUpstreamUrl, com backendTsUrl
grep "resolveUpstreamUrl\|backendRouter" /root/jarvis/apps/gateway/src/routes/chat.ts  # sem output
grep -c "config.backendTsUrl" /root/jarvis/apps/gateway/src/routes/chat.ts  # deve ser 3

# 4. Build e testes passam
cd /root/jarvis && pnpm --filter @jarvis/gateway build
cd /root/jarvis && pnpm --filter @jarvis/gateway test --run
```
</verification>

<success_criteria>
- backendRouter.ts e backendRouter.test.ts deletados
- config.ts com exatamente 3 campos: backendTsUrl, gatewayPort, apiKey
- routes/chat.ts usa config.backendTsUrl diretamente nas 3 rotas (POST /chat, GET /chat/stream, POST /chat/audio)
- Zero referências a fastapiUrl, FASTAPI_URL, resolveUpstreamUrl ou backendRouter em qualquer arquivo do gateway
- pnpm --filter @jarvis/gateway build sem erros
- pnpm --filter @jarvis/gateway test --run: todos os testes passam
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-cutover-python-deprecation/21-01-SUMMARY.md`
</output>
