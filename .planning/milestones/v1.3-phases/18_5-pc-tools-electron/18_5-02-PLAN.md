---
phase: 18_5-pc-tools-electron
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/backend-client.ts
  - apps/desktop/src/main/__tests__/backend-client.test.ts
autonomous: true
requirements: [TOOL-TS-04]
must_haves:
  truths:
    - "Main process tem um client único que injeta Authorization: Bearer em todos os requests"
    - "JARVIS_API_KEY ausente no startup → função loadBackendConfig() lança erro claro"
    - "postToolCallResult reporta outcome (success/error/user_denied) ao POST /api/tool-calls/:id/result"
  artifacts:
    - path: apps/desktop/src/main/backend-client.ts
      provides: "loadBackendConfig(), createBackendClient(), postToolCallResult(), buildChatStreamUrl()"
    - path: apps/desktop/src/main/__tests__/backend-client.test.ts
      provides: "Testes vitest com fetch mockado pra todas as rotas"
  key_links:
    - from: backend-client.ts
      to: process.env
      via: "JARVIS_API_KEY, JARVIS_BACKEND_URL"
      pattern: "process\\.env"
---

<objective>
Centralizar todo o acesso HTTP ao gateway/backend em um único módulo autenticado. Lê config do env (`JARVIS_BACKEND_URL`, `JARVIS_API_KEY`), injeta `Authorization: Bearer <key>` em todos os requests, expõe helpers tipados.

Purpose: sem esse módulo cada consumer duplicaria a lógica de auth e URL base. Base pro 18_5-04 (action-executor que reporta outcome) e 18_5-05 (ipc/chat que abre o stream).
Output: `backend-client.ts` + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18_5-pc-tools-electron/18_5-CONTEXT.md
@apps/backend-ts/src/routes/tool-calls.ts
@apps/gateway/src/routes/chat.ts

<interfaces>
Endpoint `POST /api/tool-calls/:id/result` (apps/backend-ts/src/routes/tool-calls.ts):
- Body schema (zod):
  - `{success: true, output?: string | null}` → 204
  - `{success: false, output?: string | null, error: string}` (error="user_denied" mapeia pra cancelled)
- Responses: 204 (ok), 400 (body inválido), 404 (id não existe)

Gateway base URL: padrão `http://localhost:3000` → `/api/chat/stream`, `/api/tool-calls/:id/result`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: loadBackendConfig + buildChatStreamUrl</name>
  <files>apps/desktop/src/main/backend-client.ts, apps/desktop/src/main/__tests__/backend-client.test.ts</files>
  <behavior>
    - `loadBackendConfig(env = process.env): {backendUrl: string, apiKey: string}`:
      - Lê `JARVIS_BACKEND_URL` (default `http://localhost:3000`) e `JARVIS_API_KEY` (sem default).
      - Se `JARVIS_API_KEY` ausente/vazio → throw `Error('JARVIS_API_KEY env var is required (generate with: openssl rand -hex 32)')`.
      - Normaliza backendUrl removendo trailing slash.
    - `buildChatStreamUrl(backendUrl: string, message: string): string` → `${backendUrl}/api/chat/stream?message=<encoded>`.
    - `buildToolCallResultUrl(backendUrl: string, id: number): string` → `${backendUrl}/api/tool-calls/${id}/result`.
    - Testes: config ausente lança, config ok retorna objeto, URL builders usam encodeURIComponent.
  </behavior>
  <action>RED → GREEN. Puro — zero fetch nesta task.</action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/main/__tests__/backend-client.test.ts</automated>
  </verify>
  <done>Config loader + URL builders testados e exportados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createBackendClient + postToolCallResult</name>
  <files>apps/desktop/src/main/backend-client.ts, apps/desktop/src/main/__tests__/backend-client.test.ts</files>
  <behavior>
    - `createBackendClient(config, fetchImpl = globalThis.fetch)` retorna objeto com:
      - `postToolCallResult(id: number, result: ToolResult): Promise&lt;void&gt;` onde `ToolResult = {success: true, output?: string | null} | {success: false, output?: string | null, error: string}`.
      - Faz `POST ${backendUrl}/api/tool-calls/${id}/result` com headers `{Authorization: 'Bearer ' + apiKey, Content-Type: 'application/json'}` e body JSON.
      - 204 → resolve. 400/404/5xx → throw `Error('tool-call result failed: HTTP <status> <body>')`.
      - Network error → throw original error com mensagem prefixada `'tool-call result network error: ...'`.
    - `getChatStreamRequest(message: string): {url: string, headers: Record&lt;string,string&gt;}` — utilitário pro sse-client (não faz fetch, só prepara).
    - Testes: success 204 resolve, 404 throw, 401 throw, rede morta throw, header auth presente em todos os casos.
  </behavior>
  <action>Injetar fetchImpl pros testes. Testes usam `vi.fn()` simulando Response. Conferir que Authorization header aparece em CADA chamada.</action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/main/__tests__/backend-client.test.ts</automated>
  </verify>
  <done>Client autenticado 100% testado; postToolCallResult cobre todos os outcomes.</done>
</task>

</tasks>

<verification>
- Testes verdes
- `grep -n "Authorization" apps/desktop/src/main/backend-client.ts` mostra o header sendo injetado
- Nenhum URL hardcoded fora do default em `loadBackendConfig`
</verification>

<success_criteria>
- Todo request HTTP do Electron pro backend/gateway passa por esse módulo
- API key obrigatória no startup (fail-fast)
</success_criteria>

<output>
`.planning/phases/18_5-pc-tools-electron/18_5-02-SUMMARY.md` com assinaturas e exemplo de uso.
</output>
