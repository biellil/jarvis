---
phase: 18_5-pc-tools-electron
plan: 05
type: execute
wave: 3
depends_on: [18_5-01, 18_5-02, 18_5-04]
files_modified:
  - apps/desktop/src/main/ipc/chat.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/main/__tests__/ipc-chat.test.ts
autonomous: true
requirements: [TOOL-TS-01, TOOL-TS-04]
must_haves:
  truths:
    - "O IPC chat:send-text agora consome GET /api/chat/stream via sse-client ao invés de POST /api/chat"
    - "Tokens do stream são acumulados no main e retornados ao renderer via IPC response quando o stream termina"
    - "Eventos action são despachados pro action-executor imediatamente (paralelo ao acumulo de tokens)"
    - "Startup do main falha rápido se JARVIS_API_KEY ausente"
  artifacts:
    - path: apps/desktop/src/main/ipc/chat.ts
      provides: "setupChatHandlers(deps) refatorado consumindo sse-client + action-executor"
    - path: apps/desktop/src/main/index.ts
      provides: "Bootstrap que chama loadBackendConfig, createBackendClient, cria action-executor e injeta no setupChatHandlers"
  key_links:
    - from: ipc/chat.ts
      to: sse-client.ts
      via: "openChatStream({onToken, onAction, ...})"
      pattern: "openChatStream"
    - from: ipc/chat.ts
      to: action-executor.ts
      via: "actionExecutor.enqueue(event)"
      pattern: "\\.enqueue\\("
---

<objective>
Refatorar `apps/desktop/src/main/ipc/chat.ts` para consumir SSE (via 18_5-01) ao invés de `POST /chat`. Despachar eventos `action` pro `actionExecutor` (via 18_5-04) em tempo real. Acumular tokens no main process e retornar a resposta completa ao renderer quando o stream terminar (UX streaming completo é deferido — ver CONTEXT § deferred).

Purpose: este é o plano que conecta todos os outros. É onde o pipeline "stream → tokens+actions → execução" vira realidade.
Output: ipc/chat.ts refatorado + bootstrap no main/index.ts + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18_5-pc-tools-electron/18_5-CONTEXT.md
@.planning/phases/18_5-pc-tools-electron/18_5-01-SUMMARY.md
@.planning/phases/18_5-pc-tools-electron/18_5-02-SUMMARY.md
@.planning/phases/18_5-pc-tools-electron/18_5-04-SUMMARY.md
@apps/desktop/src/main/ipc/chat.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/preload/index.ts

<interfaces>
Contrato IPC existente (shared/ipc-types.ts, NÃO alterar):
- `chat:send-text` → `(message: string) => Promise<SendTextResponse>` onde `SendTextResponse = {success: true, data: {reply: string}} | {success: false, error: string}`

O renderer ainda espera receber o reply completo como string no fim. Streaming UX (tokens aparecendo) fica deferido.

Modulos disponíveis nesta wave:
- `openChatStream` de `../sse-client`
- `loadBackendConfig`, `createBackendClient`, `buildChatStreamUrl` de `../backend-client`
- `createActionExecutor` de `../action-executor`
- `ACTION_HANDLERS`, `REQUIRES_CONFIRMATION` de `../actions`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar setupChatHandlers pra receber deps injetadas</name>
  <files>apps/desktop/src/main/ipc/chat.ts, apps/desktop/src/main/__tests__/ipc-chat.test.ts</files>
  <behavior>
    - Mudar assinatura pra `setupChatHandlers(deps: {openStream: typeof openChatStream, config: BackendConfig, actionExecutor: ActionExecutor})`.
    - Handler `chat:send-text`:
      1. Cria `AbortController`, `tokens: string[] = []`.
      2. Chama `await deps.openStream({url: config.backendUrl + '/api/chat/stream', apiKey: config.apiKey, message, onToken: (t) => tokens.push(t), onAction: (ev) => deps.actionExecutor.enqueue(ev), onEnd: () => {}, onError: () => {}, signal: controller.signal})`.
      3. Quando a promise resolve → retorna `{success: true, data: {reply: tokens.join('')}}`.
      4. Timeout: `setTimeout(() => controller.abort(), 60000)` (60s total pra um turn).
      5. Qualquer erro → retorna `{success: false, error: err.message}`.
    - Handler `chat:send-audio`: mantém implementação atual por ora (fora de escopo desta fase — audio ainda usa POST /chat/audio do gateway).
    - Testes com `setupChatHandlers` chamado diretamente (sem `ipcMain` real): injetar `openStream` mock que simula tokens + actions, verificar que:
      - tokens são concatenados no reply
      - actions são encaminhadas pro actionExecutor.enqueue
      - erro do stream vira Result.error
      - timeout aborta o controller
  </behavior>
  <action>
Manter o `ipcMain.handle(IPC_CHANNELS.CHAT_SEND_TEXT, ...)` mas com a nova lógica interna. Pra testabilidade, extrair a lógica pura em uma função `handleSendText(message, deps): Promise&lt;SendTextResponse&gt;` e chamar a partir do handler do ipcMain — os testes chamam a função pura direto, sem precisar mockar ipcMain.

NÃO mexer no handler chat:send-audio nesta task — mantém o fetch atual.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/main/__tests__/ipc-chat.test.ts</automated>
  </verify>
  <done>setupChatHandlers refatorado e testado com deps injetadas. Reply completo concatenado, actions encaminhadas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bootstrap no main/index.ts</name>
  <files>apps/desktop/src/main/index.ts, apps/desktop/src/main/ipc/index.ts</files>
  <behavior>
    - `main/index.ts` no `app.whenReady().then(...)`, ANTES de `createWindow()`:
      1. `const config = loadBackendConfig()` — se lançar, `console.error(err.message); app.exit(1); return;` (fail-fast).
      2. `const backendClient = createBackendClient(config)`
      3. `const actionExecutor = createActionExecutor({handlers: ACTION_HANDLERS, requiresConfirmation: REQUIRES_CONFIRMATION, backendClient, dialog})` onde `dialog` vem do import `import { dialog } from 'electron'`.
      4. Passa `{openStream: openChatStream, config, actionExecutor}` pro `setupIpcHandlers(deps)`.
    - `ipc/index.ts` propaga as deps: `setupIpcHandlers(chatDeps)` chama `setupChatHandlers(chatDeps)`.
    - `app.on('before-quit', ...)` chama `actionExecutor.shutdown()` (await implícito é ok com `event.preventDefault()` + then → quit).
  </behavior>
  <action>
Wire final. Atenção à ordem: config primeiro, depois client, depois executor, depois ipc. Se config falhar, saia com exit code 1 e mensagem clara: `"ERRO: JARVIS_API_KEY env var required. Gere com: openssl rand -hex 32"`.

Smoke test manual documentado no SUMMARY (não automatizado): `JARVIS_API_KEY=test pnpm --filter desktop dev` deve subir sem crash. Sem a env → sai com erro claro.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit</automated>
  </verify>
  <done>main/index.ts boota com todas as deps cabeadas; fail-fast sem API key; tsc verde.</done>
</task>

</tasks>

<verification>
- `pnpm --filter desktop vitest run` verde (incluindo novos testes ipc-chat)
- `pnpm --filter desktop tsc --noEmit` verde
- `grep -n "POST.*chat" apps/desktop/src/main/ipc/chat.ts` NÃO mostra mais `POST /api/chat` pro send-text (audio ainda usa)
- `grep -n "JARVIS_API_KEY" apps/desktop/src/main/index.ts` mostra o fail-fast
</verification>

<success_criteria>
- Send-text passa por SSE + action-executor
- Send-audio mantém comportamento atual
- Startup falha explicitamente sem API key
- Todos os testes verdes
</success_criteria>

<output>
`.planning/phases/18_5-pc-tools-electron/18_5-05-SUMMARY.md` com fluxo ponta-a-ponta e nota do smoke test manual.
</output>
