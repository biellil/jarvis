---
phase: 18-pc-tools-backend
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/session/tool-dispatch.ts
  - apps/backend-ts/test/session/chat-session-tools.test.ts
autonomous: true
requirements: [TOOL-TS-08, TOOL-TS-09]
must_haves:
  truths:
    - "ChatSession registra as 9 PC tools + recall_memory no createReactAgent"
    - "Quando o agent invoca uma PC tool, ToolLogger.logDispatch grava uma row 'dispatched' e retorna id"
    - "ChatSession aceita um callback onToolDispatched(id, payload) no construtor/factory que é invocado síncrono-ish após a row ser inserida"
    - "recall_memory NÃO dispara dispatch (só as 9 PC tools)"
    - "Se nenhum callback for passado, nada quebra (compat com chamadas sem SSE)"
  artifacts:
    - path: "apps/backend-ts/src/session/tool-dispatch.ts"
      provides: "DispatchEvent type, ToolDispatcher class que envolve uma PC tool e dispara logDispatch + callback antes de retornar o payload ao agent"
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "ChatSession.create() aceita opts.onToolDispatched, wrapa as PC tools via ToolDispatcher antes de passar ao createReactAgent"
  key_links:
    - from: "agent tool invocation"
      to: "ToolLogger.logDispatch"
      via: "ToolDispatcher wrapper callback"
    - from: "ToolDispatcher wrapper"
      to: "onToolDispatched callback"
      via: "chamada síncrona após insert, antes do return da tool"
---

<objective>
Plugar as 9 PC tools no `createReactAgent` da `ChatSession` e interceptar cada invocação para (a) gravar audit via `ToolLogger.logDispatch` e (b) disparar um callback `onToolDispatched(id, payload)` que o router SSE vai usar no plano 04 para emitir `event: action`.

Purpose: Este é o ponto de costura entre "o agent decidiu chamar uma tool" e "o resto do sistema precisa saber". Ao invés de usar middleware LangGraph complexo (que exigiria acessar o grafo), envolvemos cada tool num wrapper que executa a callback antes de retornar o payload — simples e testável.
Output: ChatSession estendido + módulo tool-dispatch + testes integração.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-pc-tools-backend/18-CONTEXT.md
@apps/backend-ts/src/session/chat-session.ts
@apps/backend-ts/src/session/tools.ts

<interfaces>
Atual `ChatSession.create`:
```ts
static async create(opts: ChatSessionOptions): Promise<ChatSession> {
  const convId = await opts.memory.startConversation();
  const recallMemoryTool = createRecallMemoryTool(opts.memory);
  const agent = createReactAgent({
    llm: opts.llm,
    tools: [recallMemoryTool],
    prompt: SYSTEM_PROMPT,
  });
  ...
}
```

Desejado:
```ts
export interface DispatchEvent {
  toolCallId: number;
  action: string;
  args: Record<string, unknown>;
  requiresConfirmation: boolean;
}

export type OnToolDispatched = (ev: DispatchEvent) => void;

export interface ChatSessionOptions {
  llm: BaseChatModel;
  memory: MemoryManager;
  toolLogger?: ToolLogger;                    // injetável pra testes
  onToolDispatched?: OnToolDispatched;        // ativo durante o request (mutável via setter)
}
```

E as 9 PC tools são envolvidas via `wrapPcTool(originalTool, ctx)` onde ctx tem referências para ToolLogger e um getter de `onToolDispatched` (permitindo o router injetar a callback por-request sem recriar a ChatSession).

Strategy: ChatSession guarda um campo `_dispatchListener: OnToolDispatched | null` e expõe `setDispatchListener(fn)` / `clearDispatchListener()`. O wrapper, ao executar, chama `ctx.getListener()?.(event)` — assim um request HTTP pode setar seu listener antes de `send()` e limpar no finally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo tool-dispatch (wrapper + tests unitários)</name>
  <files>apps/backend-ts/src/session/tool-dispatch.ts, apps/backend-ts/test/session/tool-dispatch.test.ts</files>
  <behavior>
    - `wrapPcTool(originalTool, ctx)` retorna uma nova tool LangChain com mesmo name/description/schema mas callback envolvido.
    - Ao ser invocada, ela: (1) executa o original pra obter payload; (2) chama `ctx.logger.logDispatch(name, args)` para obter id; (3) chama `ctx.getListener()?.(event)` com o DispatchEvent; (4) retorna o resultado original ao agent (string JSON).
    - Se `logDispatch` retornar null (DB erro), o wrapper NÃO invoca listener (não tem id pra referenciar) mas ainda retorna o payload ao agent (degradação suave).
    - `requiresConfirmation` é extraído do payload (true se `payload.requires_confirmation === true`, senão false).
    - `wrapAllPcTools(tools, ctx)` itera e wrappa cada uma.
  </behavior>
  <action>
Criar `apps/backend-ts/src/session/tool-dispatch.ts`:

```ts
import { tool, type StructuredToolInterface } from '@langchain/core/tools';
import type { ToolLogger } from '../memory/store.js';

export interface DispatchEvent {
  toolCallId: number;
  action: string;
  args: Record<string, unknown>;
  requiresConfirmation: boolean;
}

export type OnToolDispatched = (ev: DispatchEvent) => void;

export interface DispatchContext {
  logger: ToolLogger;
  getListener: () => OnToolDispatched | null;
}

export function wrapPcTool(original: StructuredToolInterface, ctx: DispatchContext): StructuredToolInterface {
  const wrapped = tool(
    async (input: Record<string, unknown>) => {
      // Invoca a original — retorna a content string (JSON.stringified payload)
      const rawResult = await original.invoke(input);
      // Reconstrói o payload. Preferível: a tool original usa responseFormat content_and_artifact,
      // mas via .invoke() sem config só pegamos a content. Fazemos parse.
      let payload: Record<string, unknown>;
      try {
        payload = typeof rawResult === 'string' ? JSON.parse(rawResult) : (rawResult as Record<string, unknown>);
      } catch {
        // Não é JSON — passa pro agent sem dispatch
        return rawResult;
      }
      const action = String(payload.action ?? original.name);
      const args = (payload.args as Record<string, unknown>) ?? {};
      const requiresConfirmation = payload.requires_confirmation === true;

      const id = ctx.logger.logDispatch(original.name, args);
      if (id !== null) {
        const listener = ctx.getListener();
        if (listener) {
          try {
            listener({ toolCallId: id, action, args, requiresConfirmation });
          } catch (exc) {
            console.warn(`DispatchListener threw: ${(exc as Error).message}`);
          }
        }
      }
      return rawResult;
    },
    {
      name: original.name,
      description: original.description,
      schema: (original as unknown as { schema: unknown }).schema,
    },
  );
  return wrapped as unknown as StructuredToolInterface;
}

export function wrapAllPcTools(tools: StructuredToolInterface[], ctx: DispatchContext): StructuredToolInterface[] {
  return tools.map((t) => wrapPcTool(t, ctx));
}
```

Testes `tool-dispatch.test.ts`:
- Mock ToolLogger com `logDispatch: vi.fn(() => 42)`, tmp DB opcional.
- Criar open_app tool via `createOpenAppTool()` do plan 01.
- Listener spy.
- Wrappar, invocar com `{app_name:'firefox'}`, verificar:
  - logDispatch called with 'open_app', `{app:'firefox'}`
  - listener called with `{toolCallId:42, action:'open_app', args:{app:'firefox'}, requiresConfirmation:false}`
  - return value is JSON string do payload original
- Teste delete_file: verificar `requiresConfirmation:true`
- Teste "sem listener": `getListener` retorna null → logDispatch ainda chamado, mas nenhuma exceção.
- Teste "logDispatch retorna null" → listener NÃO chamado.
- Teste "listener throws" → warning log mas sem propagar exception.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm test -- tool-dispatch</automated>
  </verify>
  <done>Todos os testes passam; wrapper preserva contrato LangChain (name/description/schema).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChatSession wiring com PC tools + listener setter</name>
  <files>apps/backend-ts/src/session/chat-session.ts, apps/backend-ts/test/session/chat-session-tools.test.ts</files>
  <behavior>
    - `ChatSession.create({llm, memory, toolLogger})` constrói o agent com `[recallMemoryTool, ...wrapAllPcTools(createAllPcTools(), ctx)]`.
    - Se `toolLogger` não for passado, instancia `new ToolLogger()` default.
    - `session.setDispatchListener(fn)` / `session.clearDispatchListener()` manipulam o listener ativo.
    - Integration test com LLM mock (ou chatModel stub que força uma tool_call de open_app) verifica: após `session.send(...)`, logDispatch foi chamado e listener recebeu DispatchEvent.
    - recall_memory NÃO dispara dispatch (não é PC tool, não é wrappada).
  </behavior>
  <action>
Atualizar `chat-session.ts`:

1. Novos imports:
   ```ts
   import { ToolLogger } from '../memory/store.js';
   import { createAllPcTools } from './pc-tools.js';
   import { wrapAllPcTools, type OnToolDispatched, type DispatchContext } from './tool-dispatch.js';
   ```

2. Estender `ChatSessionOptions`:
   ```ts
   export interface ChatSessionOptions {
     llm: BaseChatModel;
     memory: MemoryManager;
     toolLogger?: ToolLogger;
   }
   ```

3. Adicionar campo privado `private _dispatchListener: OnToolDispatched | null = null;` e `private readonly _toolLogger: ToolLogger;`

4. No `create`:
   ```ts
   const toolLogger = opts.toolLogger ?? new ToolLogger();
   const recallMemoryTool = createRecallMemoryTool(opts.memory);
   // Placeholder pra permitir que o ChatSession tenha o listener mutável.
   let listenerRef: OnToolDispatched | null = null;
   const ctx: DispatchContext = {
     logger: toolLogger,
     getListener: () => listenerRef,
   };
   const pcToolsWrapped = wrapAllPcTools(createAllPcTools(), ctx);
   const agent = createReactAgent({
     llm: opts.llm,
     tools: [recallMemoryTool, ...pcToolsWrapped],
     prompt: SYSTEM_PROMPT,
   });
   const session = new ChatSession(opts.llm, opts.memory, convId, agent, toolLogger);
   // Bind: o session guarda um closure que escreve em listenerRef
   (session as any)._setListenerRef = (fn: OnToolDispatched | null) => { listenerRef = fn; };
   return session;
   ```
   (Ou alternativa mais limpa: ChatSession guarda ambos `_listener` e o ctx compartilha `getListener: () => this._listener` via bind.)

5. Métodos públicos:
   ```ts
   setDispatchListener(fn: OnToolDispatched): void { this._dispatchListener = fn; /* sync to ref */ }
   clearDispatchListener(): void { this._dispatchListener = null; }
   ```
   Reconciliar com ctx: preferível usar um objeto `{ current: null }` capturado por closure e exposto via setter — evita `any` casts. Refatorar para:
   ```ts
   const listenerBox = { current: null as OnToolDispatched | null };
   const ctx = { logger: toolLogger, getListener: () => listenerBox.current };
   const session = new ChatSession(..., listenerBox);
   // session.setDispatchListener(fn) { this.listenerBox.current = fn; }
   ```

6. Garantir: `send()` e `sendStream()` não precisam alteração — o wrapper roda dentro do `agent.invoke`.

Testes `chat-session-tools.test.ts`:
- Usar FakeChatModel (stub) que retorna uma AIMessage com `tool_calls: [{name: 'open_app', args: {app_name: 'firefox'}, id: 't1'}]` na primeira chamada, e AIMessage final "Abri o firefox" na segunda.
  - Se LangChain.js FakeChatModel complexo, usar um runnable mock simples que satisfaz a interface mínima usada pelo createReactAgent (generate/invoke com tool_calls).
  - Alternativa mais simples: usar `@langchain/core/utils/testing` `FakeListChatModel` ou criar um `BaseChatModel` extendido mínimo.
- Memory mock que retorna convId=1 e saveTurn no-op.
- ToolLogger em tmp DB via `createStoreForTests`.
- Spy no listener.
- Criar session, `session.setDispatchListener(spy)`, await `session.send('abre o firefox')`.
- Assert: spy called once com `{toolCallId: <number>, action: 'open_app', args: {app: 'firefox'}, requiresConfirmation: false}`.
- Assert: SQLite tem 1 row em `tool_calls` com outcome='dispatched', toolName='open_app'.
- Test 2: `clearDispatchListener()` antes do send → spy não é chamado mas row ainda é inserida.

Nota: Se construir um FakeChatModel funcional for muito invasivo, documentar no summary e mover o test mais completo pro plano 04 (router) onde já haverá um chat-session integration; manter aqui só testes diretos do wrapper + setDispatchListener/clearDispatchListener mecânica.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm test -- chat-session</automated>
  </verify>
  <done>ChatSession expõe setDispatchListener/clearDispatchListener; agent tem 10 tools (1 recall + 9 PC); testes verdes; testes Fase 17 continuam verdes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts test` all green
- `grep -c "createAllPcTools\|wrapAllPcTools" apps/backend-ts/src/session/chat-session.ts` ≥ 2
- ChatSession type continua compat com callers da Fase 17 (toolLogger é opcional)
</verification>

<success_criteria>
- Agent ReAct agora tem acesso às 9 PC tools
- Cada invocação grava em tool_calls via logDispatch
- Hook `setDispatchListener` pronto para o router SSE consumir (plano 04)
</success_criteria>

<output>
Criar `.planning/phases/18-pc-tools-backend/18-03-SUMMARY.md`
</output>
