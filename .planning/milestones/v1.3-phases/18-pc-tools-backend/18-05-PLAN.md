---
phase: 18-pc-tools-backend
plan: 05
type: execute
wave: 5
depends_on: [02, 04]
files_modified:
  - apps/backend-ts/src/routes/tool-calls.ts
  - apps/backend-ts/src/app.ts
  - apps/backend-ts/test/routes/tool-calls.test.ts
autonomous: true
requirements: [TOOL-TS-08]
must_haves:
  truths:
    - "POST /tool-calls/:id/result aceita body {success: boolean, output?: string|null, error?: string|null}"
    - "Validação Zod: success obrigatório; se success=true então error deve ser null/ausente; se success=false então error é string requerida"
    - "Se success=true → outcome='success'; se success=false E error === 'user_denied' → outcome='cancelled'; caso contrário → outcome='error'"
    - "Atualiza via ToolLogger.updateOutcome(id, outcome, output ?? null, error ?? null)"
    - "204 No Content em sucesso; 404 quando updateOutcome retorna false (id inexistente); 400 quando body inválido"
    - "Sem auth (single user, documentado)"
    - "Wired no createApp quando toolLogger é injetado"
  artifacts:
    - path: "apps/backend-ts/src/routes/tool-calls.ts"
      provides: "createToolCallsRouter(toolLogger)"
    - path: "apps/backend-ts/src/app.ts"
      provides: "createApp opts.toolLogger + router mounting"
  key_links:
    - from: "Electron client"
      to: "tool_calls table row"
      via: "POST /tool-calls/:id/result → ToolLogger.updateOutcome"
---

<objective>
Criar o endpoint `POST /tool-calls/:id/result` que reconcilia o audit log com o outcome real do Electron executor. Validação Zod, mapping de success/error → outcome enum, delegação ao ToolLogger.updateOutcome, wiring no createApp.

Purpose: Fecha o ciclo de audit. Sem isto, toda row em `tool_calls` fica eternamente como 'dispatched' e não sabemos se a ação realmente aconteceu.
Output: Router novo + app.ts wiring + testes supertest cobrindo 204/404/400 e mapping de user_denied → cancelled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-pc-tools-backend/18-CONTEXT.md
@apps/backend-ts/src/app.ts
@apps/backend-ts/src/routes/chat.ts
@apps/backend-ts/src/memory/store.ts

<interfaces>
Request body:
```json
{ "success": true, "output": "Opened firefox", "error": null }
```
ou
```json
{ "success": false, "output": null, "error": "user_denied" }
```
ou
```json
{ "success": false, "error": "EACCES: permission denied" }
```

Zod schema:
```ts
const ToolResultSchema = z.discriminatedUnion('success', [
  z.object({
    success: z.literal(true),
    output: z.string().nullable().optional(),
    error: z.null().optional(),
  }),
  z.object({
    success: z.literal(false),
    output: z.string().nullable().optional(),
    error: z.string().min(1),
  }),
]);
```

Mapping:
- success=true → outcome='success'
- success=false, error==='user_denied' → outcome='cancelled'
- success=false, else → outcome='error'

Responses:
- 204 No Content (sucesso — corpo vazio)
- 400 {"detail": "..."} quando :id não é inteiro válido OU body falha Zod
- 404 {"detail": "tool call not found"} quando updateOutcome retorna false
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router tool-calls + testes supertest</name>
  <files>apps/backend-ts/src/routes/tool-calls.ts, apps/backend-ts/test/routes/tool-calls.test.ts</files>
  <behavior>
    - POST /tool-calls/42/result com body válido success=true → 204, row 42 agora outcome='success', output preenchido.
    - POST com success=false error='user_denied' → 204, outcome='cancelled'.
    - POST com success=false error='EACCES' → 204, outcome='error', error preenchido.
    - POST com body ausente ou inválido (ex: success=true + error='foo') → 400.
    - POST com :id não numérico (ex: 'abc') → 400.
    - POST com :id inexistente → 404.
    - ToolLogger é injetado via factory.
  </behavior>
  <action>
Criar `apps/backend-ts/src/routes/tool-calls.ts`:

```ts
import { Router, type Request, type Response } from 'express';
import { z } from 'zod';
import type { ToolLogger } from '../memory/store.js';

const ToolResultSchema = z.discriminatedUnion('success', [
  z.object({
    success: z.literal(true),
    output: z.string().nullable().optional(),
    error: z.null().optional(),
  }),
  z.object({
    success: z.literal(false),
    output: z.string().nullable().optional(),
    error: z.string().min(1),
  }),
]);

export function createToolCallsRouter(toolLogger: ToolLogger): Router {
  const router = Router();

  router.post('/tool-calls/:id/result', (req: Request, res: Response) => {
    const idRaw = req.params.id;
    const id = Number.parseInt(idRaw, 10);
    if (!Number.isInteger(id) || id <= 0 || String(id) !== idRaw) {
      res.status(400).json({ detail: 'invalid tool call id' });
      return;
    }

    const parsed = ToolResultSchema.safeParse(req.body);
    if (!parsed.success) {
      res.status(400).json({ detail: 'invalid body', errors: parsed.error.flatten() });
      return;
    }

    const data = parsed.data;
    let outcome: 'success' | 'error' | 'cancelled';
    let error: string | null = null;
    if (data.success) {
      outcome = 'success';
    } else if (data.error === 'user_denied') {
      outcome = 'cancelled';
      error = data.error;
    } else {
      outcome = 'error';
      error = data.error;
    }
    const output = data.output ?? null;

    const ok = toolLogger.updateOutcome(id, outcome, output, error);
    if (!ok) {
      res.status(404).json({ detail: 'tool call not found' });
      return;
    }
    res.status(204).end();
  });

  return router;
}
```

Testes `tool-calls.test.ts` (supertest):
- `beforeEach`: criar ToolLogger em tmp DB, inserir via `logDispatch('open_app', {app:'firefox'})` → seed id.
- Criar app via `createApp({toolLogger})` (após Task 2).
- POST /tool-calls/:id/result body `{success:true, output:'Opened'}` → expect 204; direct SELECT confirma outcome='success', output='Opened'.
- POST body `{success:false, error:'user_denied'}` → 204; outcome='cancelled'.
- POST body `{success:false, error:'perm denied'}` → 204; outcome='error'.
- POST /tool-calls/abc/result → 400.
- POST /tool-calls/99999/result com body válido → 404.
- POST com body `{}` → 400.
- POST com `{success:true, error:'oops'}` → 400 (discriminated union rejeita).
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm test -- tool-calls</automated>
  </verify>
  <done>Todos os cenários cobertos; 204/400/404 funcionando; mapping user_denied→cancelled validado.</done>
</task>

<task type="auto">
  <name>Task 2: Wire no createApp</name>
  <files>apps/backend-ts/src/app.ts, apps/backend-ts/test/routes/tool-calls.test.ts</files>
  <action>
Estender `createApp`:

```ts
import { createToolCallsRouter } from "./routes/tool-calls.js";
import type { ToolLogger } from "./memory/store.js";

export interface CreateAppOptions {
  session?: ChatSession;
  lock?: SessionLock;
  toolLogger?: ToolLogger;
}

export function createApp(opts: CreateAppOptions = {}) {
  const app = express();
  app.use(express.json());
  app.use("/", healthRouter);

  if (opts.session && opts.lock) {
    app.use("/", createChatRouter(opts.session, opts.lock));
  }
  if (opts.toolLogger) {
    app.use("/", createToolCallsRouter(opts.toolLogger));
  }
  app.use(errorHandler);
  return app;
}
```

Atualizar o startup / entry point (`main.ts` ou `server.ts` — descobrir qual existe) para passar o `toolLogger` da ChatSession ao `createApp`. Se ChatSession não expõe `toolLogger`, adicionar getter `get toolLogger(): ToolLogger` ou passar o mesmo instance ao createApp no bootstrap.

Atualizar tests do plano 05 task 1 pra criar o app via createApp({toolLogger}) (já previsto acima).

Manter backward-compat: se toolLogger não é passado, o endpoint simplesmente não existe (404 do Express padrão).
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm test && pnpm build</automated>
  </verify>
  <done>createApp suporta toolLogger opcional; startup bootstrap injeta; build TS compila; todos testes verdes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts test` all green (inclusive Fase 17 chat router intacto)
- `pnpm --filter backend-ts build` compila sem erros
- Smoke manual opcional: curl POST /tool-calls/1/result com body válido após dispatch real
</verification>

<success_criteria>
Todos os 6 critérios da Fase 18 do ROADMAP.md satisfeitos:
1. 9 tools registradas no createReactAgent ✓ (plano 03)
2. Payload idêntico ao Python ✓ (plano 01 snapshot tests)
3. Cada chamada gravada com outcome='dispatched' ✓ (plano 03)
4. SSE emite event: action ✓ (plano 04)
5. POST /tool-calls/:id/result aceita {success, output, error} e atualiza audit ✓ (plano 05)
6. Tools de leitura sem side effect; destrutivas marcam requires_confirmation ✓ (plano 01)
</success_criteria>

<output>
Criar `.planning/phases/18-pc-tools-backend/18-05-SUMMARY.md`
</output>
