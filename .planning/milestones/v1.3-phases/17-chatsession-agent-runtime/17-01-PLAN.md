---
phase: 17-chatsession-agent-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/session/system-prompt.ts
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/session/index.ts
  - apps/backend-ts/src/session/chat-session.test.ts
autonomous: true
requirements:
  - LLM-TS-04
  - LLM-TS-06
must_haves:
  truths:
    - "ChatSession pode ser instanciada com LLM + MemoryManager e cria uma conversa nova no SQLite no constructor"
    - "ChatSession.send(text) chama o LLM (sem agent ainda), adiciona HumanMessage/AIMessage em history e persiste ambos no SQLite"
    - "SYSTEM_PROMPT é portado literalmente do Python (src/jarvis/core/session.py) como string exportada"
    - "history começa com [SystemMessage(SYSTEM_PROMPT)] e cresce a cada turn"
  artifacts:
    - path: "apps/backend-ts/src/session/system-prompt.ts"
      provides: "SYSTEM_PROMPT constant"
      exports: ["SYSTEM_PROMPT"]
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "ChatSession class (skeleton, sem agent/stream)"
      exports: ["ChatSession", "ChatSessionOptions"]
    - path: "apps/backend-ts/src/session/index.ts"
      provides: "barrel export"
    - path: "apps/backend-ts/src/session/chat-session.test.ts"
      provides: "vitest tests cobrindo send() e persistência"
  key_links:
    - from: "ChatSession constructor"
      to: "MemoryManager.startConversation()"
      via: "conversationId persistido em this._convId"
    - from: "ChatSession.send()"
      to: "MemoryManager.saveTurn(convId, user, assistant)"
      via: "após turn completo, antes do return"
    - from: "ChatSession.send()"
      to: "llm.invoke(messages)"
      via: "chamada direta na BaseChatModel (ainda sem createReactAgent)"
---

<objective>
Criar o esqueleto da classe `ChatSession` em TypeScript com history em memória, SYSTEM_PROMPT portado literal do Python, integração com `MemoryManager` (conversationId, saveTurn) e um `send(text)` simples não-streaming que chama `llm.invoke()` direto — sem agent, sem tools, sem streaming ainda. Endpoints e LangGraph ficam para planos seguintes.

Purpose: estabelecer o contrato base (`history: BaseMessage[]`, `_convId`, ciclo save-on-turn) sobre o qual os próximos planos vão construir. Permite testar persistência isoladamente antes de complicar com agent runtime.

Output: módulo `session/` com `ChatSession` skeleton, testes vitest verdes, zero dependência nova fora de `@langchain/core`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-chatsession-agent-runtime/17-CONTEXT.md
@src/jarvis/core/session.py
@src/jarvis/api/models.py
@apps/backend-ts/src/llm/factory.ts
@apps/backend-ts/src/memory/manager.ts
@apps/backend-ts/src/memory/index.ts

<interfaces>
From apps/backend-ts/src/memory/manager.ts:
```typescript
export class MemoryManager {
  constructor(opts?: MemoryManagerOptions);
  startConversation(): Promise<number | null>;
  endConversation(convId: number): Promise<void>;
  saveTurn(convId: number, userText: string, assistantText: string): Promise<void>;
  buildContext(userText: string): Promise<string>;
  learnFromTurn(llm: BaseChatModel, userText: string): Promise<void>;
  close(): void;
}
```

From @langchain/core/messages:
```typescript
class SystemMessage { constructor(content: string); }
class HumanMessage  { constructor(content: string); }
class AIMessage     { constructor(content: string); }
type BaseMessage = SystemMessage | HumanMessage | AIMessage | ToolMessage;
```

From @langchain/core/language_models/chat_models:
```typescript
abstract class BaseChatModel {
  invoke(messages: BaseMessage[]): Promise<AIMessage>;
  stream(messages: BaseMessage[]): AsyncIterable<AIMessageChunk>;
}
```

Python literal to port (src/jarvis/core/session.py, line 54-58):
```python
SYSTEM_PROMPT = (
    "You are JARVIS, a helpful personal assistant. "
    "You remember everything from our conversations and help the user "
    "with tasks, questions, and anything they need."
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar SYSTEM_PROMPT + testes vitest para ChatSession skeleton (RED)</name>
  <files>apps/backend-ts/src/session/system-prompt.ts, apps/backend-ts/src/session/chat-session.test.ts</files>
  <behavior>
    - SYSTEM_PROMPT exportado é EXATAMENTE a string literal do Python (3 sentenças, sem trailing newline)
    - Teste 1: `new ChatSession({ llm, memory })` chama `memory.startConversation()` e guarda o convId retornado
    - Teste 2: `session.history` inicia com length 1 e primeiro elemento é SystemMessage com content === SYSTEM_PROMPT
    - Teste 3: `await session.send("oi")` retorna string (resposta mockada do llm), e após a chamada `history` tem length 3 (System + Human("oi") + AI(response))
    - Teste 4: `send()` chama `memory.saveTurn(convId, "oi", "<resposta>")` exatamente uma vez
    - Teste 5: Se `memory.startConversation()` retorna null, `send()` NÃO chama `saveTurn` (degrada gracefully, não crasha)
    - Teste 6: Dois `send()` consecutivos acumulam history corretamente: System + Human1 + AI1 + Human2 + AI2 (length 5)
    - Usar mocks de `BaseChatModel` (objeto com `invoke: vi.fn().mockResolvedValue(new AIMessage("pong"))`) e `MemoryManager` (mock com `startConversation` e `saveTurn` como `vi.fn()`)
  </behavior>
  <action>
    1. Criar `apps/backend-ts/src/session/system-prompt.ts` exportando `export const SYSTEM_PROMPT = "You are JARVIS, a helpful personal assistant. You remember everything from our conversations and help the user with tasks, questions, and anything they need.";` — uma linha, literal do Python.
    2. Criar `apps/backend-ts/src/session/chat-session.test.ts` com os 6 testes acima usando vitest. Importar `ChatSession` de `./chat-session.js` (ainda não existe → RED). Rodar `pnpm --filter backend-ts test session/chat-session` e confirmar que falham por "cannot find module" ou assertion errors.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/chat-session.test.ts 2>&1 | grep -E "(FAIL|Cannot find module)" </automated>
  </verify>
  <done>Arquivo de testes existe, SYSTEM_PROMPT.ts existe com a string literal correta, rodar vitest mostra testes falhando (RED confirmado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar ChatSession class (GREEN)</name>
  <files>apps/backend-ts/src/session/chat-session.ts, apps/backend-ts/src/session/index.ts</files>
  <behavior>
    - ChatSession aceita `{ llm: BaseChatModel, memory: MemoryManager }` no constructor
    - Constructor dispara `memory.startConversation()` (async) e armazena a Promise de convId — OU usa um método `init()` separado. Para evitar async constructor, criar static `async ChatSession.create(opts): Promise<ChatSession>` que resolve o convId antes de retornar, E manter o constructor privado. Os testes devem usar `ChatSession.create()`.
    - `history: BaseMessage[]` inicia com `[new SystemMessage(SYSTEM_PROMPT)]`
    - `send(text: string): Promise<string>`:
      1. Append `new HumanMessage(text)` em history
      2. Chama `llm.invoke(this.history)` → AIMessage
      3. Append o AIMessage em history
      4. Se `_convId !== null`, `await memory.saveTurn(_convId, text, aiContent)` (dentro de try/catch, loga warn mas não propaga — paridade com Python)
      5. Retorna `String(aiMessage.content)`
    - Expor `get historyLength()` ou deixar `history` público readonly para os testes
    - Ajustar os testes do Task 1 para usar `await ChatSession.create(...)` ao invés de `new ChatSession(...)`
  </behavior>
  <action>
    1. Criar `chat-session.ts` implementando a classe conforme `<behavior>`. Imports: `BaseChatModel` de `@langchain/core/language_models/chat_models`, mensagens de `@langchain/core/messages`, `MemoryManager` de `../memory/index.js`, `SYSTEM_PROMPT` de `./system-prompt.js`.
    2. Usar factory estático `static async create(opts: ChatSessionOptions): Promise<ChatSession>` que chama `startConversation()` antes de construir. Constructor interno recebe o convId já resolvido.
    3. Criar `session/index.ts` reexportando `ChatSession`, `ChatSessionOptions`, `SYSTEM_PROMPT`.
    4. Ajustar `chat-session.test.ts` (do Task 1) para usar `await ChatSession.create({ llm, memory })`.
    5. Rodar testes e confirmar GREEN.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/chat-session.test.ts</automated>
  </verify>
  <done>Todos os 6 testes passam. `pnpm --filter backend-ts typecheck` passa sem erros. `session/index.ts` exporta ChatSession.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/session/` → todos passam
- `pnpm --filter backend-ts typecheck` → zero erros
- SYSTEM_PROMPT confere char-a-char com Python (diff manual contra src/jarvis/core/session.py:54-58)
</verification>

<success_criteria>
Atende parcialmente (base) aos success criteria #1 e #3 da Fase 17:
- #3 (Conversation history persiste no SQLite após cada mensagem) — validado em teste unit com mock de MemoryManager.saveTurn.
- Base para #1 (POST /chat) — a classe está pronta para ser chamada pelo endpoint do Plan 17-04.
</success_criteria>

<output>
Após completar: criar `.planning/phases/17-chatsession-agent-runtime/17-01-SUMMARY.md` documentando arquivos criados, decisões (factory estático vs constructor async), e próximo passo (Plan 17-02 — agent + tool).

Commit: `✨ feat(17-01): cria ChatSession skeleton com history e persistência SQLite`
</output>
