---
phase: 17-chatsession-agent-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - apps/backend-ts/src/session/tools.ts
  - apps/backend-ts/src/session/tools.test.ts
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/session/chat-session.test.ts
  - apps/backend-ts/src/session/index.ts
  - apps/backend-ts/package.json
autonomous: true
requirements:
  - LLM-TS-05
  - LLM-TS-07
must_haves:
  truths:
    - "Existe a tool `recall_memory(query)` que internamente chama `memoryManager.buildContext(query)`"
    - "ChatSession usa `createReactAgent` de @langchain/langgraph ao invés de chamar llm.invoke direto"
    - "O agente consegue decidir chamar `recall_memory` durante o ciclo ReAct (smoke test mock verifica que a tool é invocada quando o LLM emite tool_call)"
    - "Quando `buildContext` retorna string vazia, a tool retorna fallback em pt-BR `Nenhuma memória relevante encontrada.`"
    - "O histórico mantido em ChatSession continua sendo BaseMessage[] e cresce com as mensagens (incluindo ToolMessage quando aplicável)"
  artifacts:
    - path: "apps/backend-ts/src/session/tools.ts"
      provides: "factory createRecallMemoryTool(memory) → StructuredTool"
      exports: ["createRecallMemoryTool"]
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "ChatSession com createReactAgent runtime"
    - path: "apps/backend-ts/src/session/tools.test.ts"
      provides: "testes unit da tool recall_memory"
  key_links:
    - from: "createRecallMemoryTool"
      to: "MemoryManager.buildContext(query)"
      via: "await dentro da função func da tool"
    - from: "ChatSession.send()"
      to: "reactAgent.invoke({ messages: this.history })"
      via: "agent runtime ao invés de llm.invoke"
    - from: "ChatSession constructor"
      to: "createReactAgent({ llm, tools: [recallMemoryTool], prompt: SYSTEM_PROMPT })"
      via: "construído uma vez na factory create()"
---

<objective>
Adicionar a tool `recall_memory` (D-Q1=1b, D-Q4=4b) e integrar `createReactAgent` de `@langchain/langgraph` ao `ChatSession`. A partir deste plano, `send()` passa pelo agente ReAct ao invés de chamar o LLM diretamente — exercitando o loop Reason→Act→Observe ponta-a-ponta.

Purpose: validar o encanamento do agent runtime com uma tool real antes de adicionar streaming e endpoints. Quando a Fase 18 chegar, adicionar PC tools é só push no array `tools: [...]`.

Output: ChatSession agent-powered, tool `recall_memory` testada, paridade com Python quanto a "agente decide quando puxar memória" (divergência consciente 4b ↔ 5a documentada).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-chatsession-agent-runtime/17-CONTEXT.md
@.planning/phases/17-chatsession-agent-runtime/17-01-SUMMARY.md
@apps/backend-ts/src/memory/manager.ts
@apps/backend-ts/src/session/chat-session.ts

<interfaces>
From @langchain/langgraph (expected 1.1.x API):
```typescript
import { createReactAgent } from '@langchain/langgraph/prebuilt';

const agent = createReactAgent({
  llm: BaseChatModel,
  tools: StructuredToolInterface[],
  prompt?: string,  // system prompt
});

// Invocation:
const result = await agent.invoke({ messages: BaseMessage[] });
// result.messages: BaseMessage[] — includes tool calls + final AIMessage
```

From @langchain/core/tools:
```typescript
import { tool } from '@langchain/core/tools';
import { z } from 'zod';

const myTool = tool(
  async ({ query }: { query: string }) => { return "result string"; },
  {
    name: "recall_memory",
    description: "...em português...",
    schema: z.object({ query: z.string() }),
  }
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar @langchain/langgraph + criar tool recall_memory com testes</name>
  <files>apps/backend-ts/package.json, apps/backend-ts/src/session/tools.ts, apps/backend-ts/src/session/tools.test.ts</files>
  <behavior>
    - `createRecallMemoryTool(memory)` retorna uma StructuredTool com `name === "recall_memory"`
    - Schema valida `{ query: string }` (zod)
    - Description é em português explicando quando usar (sensibilidade de LLMs locais)
    - Invocar a tool com `{query: "café"}` chama `memory.buildContext("café")` exatamente uma vez
    - Quando buildContext retorna string não-vazia, a tool retorna essa string
    - Quando buildContext retorna "" (vazio), a tool retorna `"Nenhuma memória relevante encontrada."`
    - Quando buildContext rejeita, a tool retorna `"Erro ao buscar memórias: <msg>"` (não propaga throw — agent não deve travar)
  </behavior>
  <action>
    1. `cd apps/backend-ts && pnpm add @langchain/langgraph` (versão compatível com @langchain/core 1.x — deixar pnpm resolver).
    2. Criar `src/session/tools.ts` com `createRecallMemoryTool(memory: MemoryManager)` usando `tool()` de `@langchain/core/tools` e `z.object({ query: z.string() })`. Description (pt-BR): `"Busca memórias relevantes de conversas passadas e fatos do perfil do usuário. Use quando precisar lembrar algo que o usuário disse antes ou referenciar preferências dele."`
    3. Criar `src/session/tools.test.ts` com testes cobrindo: tool name, schema validation, buildContext chamado, fallback vazio, fallback erro. Mock de MemoryManager via `{ buildContext: vi.fn() } as unknown as MemoryManager`.
    4. Rodar `pnpm vitest run src/session/tools.test.ts` — deve passar GREEN.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/tools.test.ts</automated>
  </verify>
  <done>Tests passam. `@langchain/langgraph` em package.json. Tool exporta e é invocável.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar ChatSession para usar createReactAgent</name>
  <files>apps/backend-ts/src/session/chat-session.ts, apps/backend-ts/src/session/chat-session.test.ts, apps/backend-ts/src/session/index.ts</files>
  <behavior>
    - `ChatSession.create({ llm, memory })` agora também constrói o agent via `createReactAgent({ llm, tools: [createRecallMemoryTool(memory)], prompt: SYSTEM_PROMPT })` e guarda em `this._agent`
    - `send(text)`:
      1. Append HumanMessage em history
      2. `const result = await this._agent.invoke({ messages: this.history })`
      3. Substitui ou estende history com `result.messages` (seguir o que o agent retorna — o agent acumula ToolMessage + AIMessage final internamente). Decisão: `this.history = result.messages` (inclui system + toda a cadeia ReAct).
      4. Extrai a última AIMessage de `result.messages` como resposta final (`String(lastAi.content)`)
      5. Persiste no SQLite via `memory.saveTurn(convId, text, finalText)` (try/catch, warn-only)
      6. Retorna finalText
    - Teste novo: mock de llm que na primeira chamada emite um tool_call pra `recall_memory`, e na segunda retorna AIMessage("sei, você gosta de café"). Verificar que `memory.buildContext` é chamado E que `send()` retorna o texto final. (Observação: se criar um mock fiel de createReactAgent for inviável, o teste pode mockar `createReactAgent` inteiro via `vi.mock('@langchain/langgraph/prebuilt', ...)` para retornar um agent stub com `invoke` controlado — documentar escolha no SUMMARY.)
    - Testes antigos do Plan 17-01 (send simples) continuam passando, ajustados para o novo fluxo via agent mock.
  </behavior>
  <action>
    1. Editar `chat-session.ts`: importar `createReactAgent` de `@langchain/langgraph/prebuilt`, importar `createRecallMemoryTool` de `./tools.js`. No `create()`, depois de resolver convId, construir o agent. No `send()`, substituir `llm.invoke(history)` pelo fluxo acima.
    2. Ajustar `chat-session.test.ts`: usar `vi.mock('@langchain/langgraph/prebuilt')` para fornecer um `createReactAgent` stub que devolve um objeto com `invoke: vi.fn()`. Adicionar teste do ciclo ReAct com tool_call simulado.
    3. Reexportar `createRecallMemoryTool` no `session/index.ts`.
    4. Rodar a suíte session inteira.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/</automated>
  </verify>
  <done>Todos os testes de `session/` passam incluindo o ciclo ReAct com tool_call mockado. Typecheck passa. `ChatSession._agent` existe e é usado em `send()`.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/session/` — todos passam
- `pnpm --filter backend-ts typecheck` — zero erros
- `package.json` lista `@langchain/langgraph` nas dependencies
- `pnpm ls @langchain/core --filter backend-ts` confirma uma única versão 1.x (sem duplicatas vindas do langgraph)
</verification>

<success_criteria>
Atende aos success criteria da Fase 17:
- #4 "Semantic retrieval injeta memórias" — via tool calling (divergência consciente 4b ↔ 5a documentada no CONTEXT)
- #5 "Agent ReAct loop funciona sem travar" — validado via teste unit com mock de tool_call
</success_criteria>

<output>
Criar `17-02-SUMMARY.md` documentando: versão do @langchain/langgraph instalada, como o mock de createReactAgent foi estruturado nos testes, e a decisão de sobrescrever `this.history` com `result.messages`.

Commit: `✨ feat(17-02): integra createReactAgent e tool recall_memory na ChatSession`
</output>
