---
phase: 19-voice-backend
plan: 08
type: execute
wave: 5
depends_on: [19-07]
files_modified:
  - apps/gateway/src/routes/chat.ts
  - apps/gateway/src/routes/chat-audio.test.ts
  - apps/gateway/src/config.ts
autonomous: true
requirements: [VOICE-TS-01, VOICE-TS-02, VOICE-TS-05]
must_haves:
  truths:
    - "POST /api/chat/audio no gateway proxia pro backend TS /chat/audio (não mais FastAPI Python)"
    - "Forward do header Authorization quando presente; senão injeta Bearer ${config.apiKey}"
    - "Forward do multipart body preservando filename e content-type"
    - "Limit 25MB (match backend)"
    - "Error/status do backend são propagados 1:1 pro cliente"
  artifacts:
    - path: "apps/gateway/src/routes/chat.ts"
      provides: "POST /chat/audio handler apontando pro backend TS"
  key_links:
    - from: "gateway POST /api/chat/audio"
      to: "backend-ts POST /chat/audio"
      via: "fetch multipart com FormData"
      pattern: "backendTsUrl.*chat/audio"
---

<objective>
Atualizar o gateway Express pra rotear `/api/chat/audio` pro backend TypeScript (fase 19) em vez do FastAPI Python (fase 13). Mantém multer, aumenta limit pra 25MB, faz forward de Authorization.

Depends_on: 19-07 (endpoint backend precisa existir).
Output: Route atualizada + teste supertest com backend stubado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@apps/gateway/src/routes/chat.ts
@apps/gateway/src/config.ts

<interfaces>
Gateway config atual já tem `config.fastapiUrl`. Verificar se já existe `config.backendTsUrl` (provavelmente sim, adicionado em fases 14/17) — se não, adicionar lendo `BACKEND_TS_URL` (default `http://localhost:8001`).

Handler atual em chat.ts linha 102+ aponta pra fastapiUrl. Vamos trocar pra backendTsUrl + aumentar limit pra 25MB + forward Authorization.

Decisão: NÃO manter backward compat com Python audio (Phase 13). Fase 19 é o cutover para TS em voice; Python continua ativo só pra chat text até Phase 20/21.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atualizar POST /chat/audio do gateway pra backend TS + config</name>
  <files>apps/gateway/src/routes/chat.ts, apps/gateway/src/config.ts</files>
  <behavior>
    - config.backendTsUrl existe (verificar; se não, adicionar)
    - Multer upload limits.fileSize = 25MB (antes: 10MB)
    - POST /chat/audio faz fetch pra `${backendTsUrl}/chat/audio`
    - Header Authorization forwardado do request original; senão `Bearer ${config.apiKey}` se presente
    - FormData inclui filename original (req.file.originalname) e contentType original (req.file.mimetype)
    - Response status/body propagados (incluindo 400/429/500)
  </behavior>
  <action>
    1. Verificar `apps/gateway/src/config.ts` — se não existe `backendTsUrl`, adicionar:
       ```typescript
       backendTsUrl: process.env.BACKEND_TS_URL ?? 'http://localhost:8001',
       ```
    2. Editar `apps/gateway/src/routes/chat.ts`:
       - Trocar `limits: { fileSize: 10 * 1024 * 1024 }` → `25 * 1024 * 1024`
       - Substituir handler `POST /chat/audio`:
       ```typescript
       chatRouter.post("/chat/audio", upload.single("audio"), async (req, res, next) => {
         try {
           if (!req.file) {
             return next(Object.assign(new Error("Audio file is required"), { status: 400, code: "MISSING_FILE" }));
           }
           const formData = new FormData();
           formData.append(
             "audio",
             new Blob([req.file.buffer], { type: req.file.mimetype || 'audio/webm' }),
             req.file.originalname || 'audio.webm',
           );
           const headers: Record<string, string> = {};
           const incomingAuth = req.headers.authorization;
           if (incomingAuth) headers['Authorization'] = incomingAuth;
           else if (config.apiKey) headers['Authorization'] = `Bearer ${config.apiKey}`;
       
           const upstream = await fetch(`${config.backendTsUrl}/chat/audio`, {
             method: "POST",
             headers,
             body: formData,
           });
           const bodyText = await upstream.text();
           res.status(upstream.status);
           const ct = upstream.headers.get('content-type');
           if (ct) res.setHeader('Content-Type', ct);
           res.send(bodyText);
         } catch (err) {
           next(err);
         }
       });
       ```
  </action>
  <verify>
    <automated>cd apps/gateway && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Gateway compila, handler atualizado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes supertest do proxy</name>
  <files>apps/gateway/src/routes/chat-audio.test.ts</files>
  <behavior>
    Casos (com global.fetch mockado):
    1. Sem arquivo → 400 MISSING_FILE (sem hit no backend)
    2. Happy path → fetch chamado com URL backend-ts, FormData contendo audio, Authorization header forwardado; response 200 propagado
    3. Backend retorna 429 → gateway propaga 429
    4. Backend retorna 500 com code TTS_FAILED → gateway propaga status + body
    5. Sem Authorization no request mas apiKey na config → injetado Bearer
    6. Sem nenhum → nenhum header Authorization (mas request ainda vai)
  </behavior>
  <action>
    Criar app de teste via createApp do gateway (ou montagem manual do router). Usar vi.stubGlobal('fetch', ...). Enviar via supertest `.attach('audio', buffer, {filename, contentType})`. Verificar a URL recebida por fetch e os headers.
  </action>
  <verify>
    <automated>cd apps/gateway && pnpm vitest run src/routes/chat-audio.test.ts</automated>
  </verify>
  <done>6 casos de teste verdes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter gateway vitest run` verde
- `pnpm --filter gateway build` (ou tsc --noEmit) limpo
- Outros testes do gateway não quebram
</verification>

<success_criteria>
1. Gateway /api/chat/audio aponta pro backend TS (não Python)
2. Limit 25MB compatível com backend
3. Authorization forward funcional
4. Error mapping transparente
5. Fase 19.5 (Electron) vai bater no gateway e rotear pro backend TS sem mudar nada do lado cliente
</success_criteria>

<output>
`.planning/phases/19-voice-backend/19-08-SUMMARY.md`
</output>
