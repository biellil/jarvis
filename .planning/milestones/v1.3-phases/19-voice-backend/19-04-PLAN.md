---
phase: 19-voice-backend
plan: 04
type: execute
wave: 2
depends_on: [19-02, 19-03]
files_modified:
  - apps/backend-ts/src/voice/tts/index.ts
  - apps/backend-ts/src/voice/tts/fallback.ts
  - apps/backend-ts/src/voice/tts/index.test.ts
  - apps/backend-ts/src/voice/tts/fallback.test.ts
autonomous: true
requirements: [VOICE-TS-02]
must_haves:
  truths:
    - "createTTSProvider() lê TTS_PROVIDER env var (default 'elevenlabs')"
    - "Wrapper FallbackTTSProvider chama provider primário, se throw chama provider fallback e loga warning"
    - "Em TTS_PROVIDER=elevenlabs sem ELEVENLABS_API_KEY, factory loga warning e retorna LocalTTSProvider direto (sem fallback wrapper)"
    - "Audit log pode inspecionar .name do provider que efetivamente sintetizou (resultante expõe lastProviderUsed ou o método retorna metadata)"
  artifacts:
    - path: "apps/backend-ts/src/voice/tts/fallback.ts"
      provides: "class FallbackTTSProvider implements TTSProvider"
    - path: "apps/backend-ts/src/voice/tts/index.ts"
      provides: "createTTSProvider() factory + re-exports"
  key_links:
    - from: "FallbackTTSProvider.synthesize"
      to: "LocalTTSProvider.synthesize"
      via: "try/catch no primary, logga warning, chama secondary"
      pattern: "try.*primary.*catch.*secondary"
---

<objective>
Wrapper de fallback automático cloud→local e factory createTTSProvider() que lê env var pra decidir o provider default. Consolida módulo tts/.

Depends_on: 19-02 (ElevenLabs) e 19-03 (Local).
Output: tts/fallback.ts + tts/index.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@apps/backend-ts/src/voice/tts/provider.ts
@apps/backend-ts/src/voice/tts/elevenlabs.ts
@apps/backend-ts/src/voice/tts/local.ts

<interfaces>
```typescript
// tts/fallback.ts
export class FallbackTTSProvider implements TTSProvider {
  readonly name: string; // ex 'elevenlabs+local'
  constructor(primary: TTSProvider, secondary: TTSProvider);
  async synthesize(text: string): Promise<TTSResult & { providerUsed: string }>;
}

// tts/index.ts
export function createTTSProvider(): TTSProvider;
```

Note: `synthesize` result precisa carregar o nome do provider que efetivamente sintetizou pro audit log (plano 19-06). Estender TTSResult com `providerUsed?: string` OU retornar tupla. Escolha: estender TTSResult opcional — não quebra ElevenLabs/Local isoladamente, e voice-handler lê `result.providerUsed ?? provider.name`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender TTSResult com providerUsed + FallbackTTSProvider</name>
  <files>apps/backend-ts/src/voice/tts/provider.ts, apps/backend-ts/src/voice/tts/fallback.ts, apps/backend-ts/src/voice/tts/fallback.test.ts</files>
  <behavior>
    - TTSResult ganha campo opcional `providerUsed?: string`
    - FallbackTTSProvider.name === `${primary.name}+${secondary.name}` (ex 'elevenlabs+local')
    - synthesize: tenta primary; se throw → `console.warn('[voice] TTS primary ${primary.name} failed: ${err}; falling back to ${secondary.name}')` → chama secondary → retorna result com providerUsed=secondary.name
    - Se primary succeed, retorna result com providerUsed=primary.name (injeta o campo)
    - Se secondary também throw, propaga com mensagem combinada `Both TTS providers failed: primary=${e1}; secondary=${e2}`
  </behavior>
  <action>
    1. Editar `provider.ts`: adicionar `providerUsed?: string` em TTSResult.
    2. Criar `fallback.ts` com FallbackTTSProvider conforme behavior.
    3. Testes: mock 2 providers fake implementando TTSProvider. Casos: primary success → providerUsed='primary'; primary throws, secondary success → warn + providerUsed='secondary'; both throw → Error combinado; name concatenado correto.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/tts/fallback.test.ts</automated>
  </verify>
  <done>FallbackTTSProvider testado com os 3 cenários.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createTTSProvider() factory com env dispatch</name>
  <files>apps/backend-ts/src/voice/tts/index.ts, apps/backend-ts/src/voice/tts/index.test.ts</files>
  <behavior>
    - Re-export TTSProvider, TTSResult, ElevenLabsTTSProvider, LocalTTSProvider, FallbackTTSProvider
    - createTTSProvider():
      - Lê TTS_PROVIDER (default 'elevenlabs')
      - Se 'local' → retorna new LocalTTSProvider() (sem fallback — já é local)
      - Se 'elevenlabs':
        - Se ELEVENLABS_API_KEY ausente → warn "ELEVENLABS_API_KEY not set, using local only" → return LocalTTSProvider
        - Senão → return new FallbackTTSProvider(new ElevenLabsTTSProvider(), new LocalTTSProvider())
      - Valor desconhecido → warn + return LocalTTSProvider
  </behavior>
  <action>
    Criar index.ts implementando factory. Teste com stubs de process.env (ELEVENLABS_API_KEY, TTS_PROVIDER) via vi.stubEnv + afterEach unstub. Verificar tipo concreto retornado (instanceof) em cada branch + console.warn chamado quando esperado.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/tts/index.test.ts</automated>
  </verify>
  <done>Factory testada para todos os branches de env.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/voice/tts` 100% verde
- Build limpo
</verification>

<success_criteria>
1. FallbackTTSProvider implementa fallback automático com audit-friendly providerUsed
2. createTTSProvider() respeita TTS_PROVIDER e ELEVENLABS_API_KEY
3. Local-only path funciona sem cloud deps
</success_criteria>

<output>
`.planning/phases/19-voice-backend/19-04-SUMMARY.md`
</output>
