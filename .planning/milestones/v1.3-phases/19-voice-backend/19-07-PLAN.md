---
phase: 19-voice-backend
plan: 07
type: execute
wave: 4
depends_on: [19-06]
files_modified:
  - apps/backend-ts/package.json
  - apps/backend-ts/src/routes/chat-audio.ts
  - apps/backend-ts/src/routes/chat-audio.test.ts
  - apps/backend-ts/src/app.ts
  - apps/backend-ts/src/index.ts
autonomous: true
requirements: [VOICE-TS-01, VOICE-TS-02, VOICE-TS-05]
must_haves:
  truths:
    - "POST /chat/audio aceita multipart/form-data com campo 'audio' (max 25MB)"
    - "Sem arquivo → 400"
    - "Session lock já ocupado → 429"
    - "EMPTY_AUDIO/NO_SPEECH → 400"
    - "STT_FAILED/LLM_FAILED/TTS_FAILED → 500 com {detail, code}"
    - "Success → 200 JSON {transcription, message, audio_base64, audio_format, stt_provider, tts_provider}"
    - "Lock liberado em finally mesmo em exceção"
    - "Endpoint wired no createApp + VoiceHandler bootstrapped no index.ts com providers reais + ffmpeg check"
  artifacts:
    - path: "apps/backend-ts/src/routes/chat-audio.ts"
      provides: "createChatAudioRouter(handler, lock)"
  key_links:
    - from: "POST /chat/audio"
      to: "VoiceHandler.handle"
      via: "multer memoryStorage → req.file.buffer → handler.handle"
      pattern: "req\\.file\\.buffer"
---

<objective>
Expor o endpoint HTTP `POST /chat/audio` no backend TS usando multer pra multipart parsing, reusando o SessionLock global da Fase 17 e o VoiceHandler do plano 19-06. Wiring no bootstrap (index.ts) instancia os providers reais.

Depends_on: 19-06.
Output: Rota + testes supertest + bootstrap completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@apps/backend-ts/src/routes/chat.ts
@apps/backend-ts/src/app.ts
@apps/backend-ts/src/index.ts
@apps/backend-ts/src/voice/voice-handler.ts
@apps/backend-ts/src/session/lock.ts

<interfaces>
```typescript
// routes/chat-audio.ts
export function createChatAudioRouter(
  voiceHandler: VoiceHandler,
  lock: SessionLock,
): Router;
```

Response shape (parity com CONTEXT.md):
```json
{
  "transcription": "...",
  "message": "...",
  "audio_base64": "base64 string",
  "audio_format": "mp3",
  "stt_provider": "local",
  "tts_provider": "elevenlabs"
}
```

Error mapping:
- code EMPTY_AUDIO / NO_SPEECH → 400
- lock busy → 429
- code STT_FAILED / LLM_FAILED / TTS_FAILED → 500
- unknown → 500

Multer config: memoryStorage, limits.fileSize 25MB, single('audio'), reject non-audio content-type (whitelist: audio/webm, audio/ogg, audio/wav, audio/mpeg, audio/mp4, audio/x-wav, audio/webm;codecs=opus).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install multer + tipos, criar router POST /chat/audio</name>
  <files>apps/backend-ts/package.json, apps/backend-ts/src/routes/chat-audio.ts</files>
  <behavior>
    - multer e @types/multer adicionados como deps (multer em deps, @types em devDeps)
    - createChatAudioRouter retorna Express Router
    - Rota POST /chat/audio usa upload.single('audio') middleware
    - Error handling completo conforme <interfaces>
    - Lock tryAcquire/release padrão (mesmo do chat.ts)
  </behavior>
  <action>
    1. `pnpm --filter backend-ts add multer` + `pnpm --filter backend-ts add -D @types/multer`
    2. Criar `apps/backend-ts/src/routes/chat-audio.ts`:
       ```typescript
       import { Router, type Request, type Response } from 'express';
       import multer from 'multer';
       import type { VoiceHandler } from '../voice/voice-handler.js';
       import type { SessionLock } from '../session/lock.js';
       
       const BUSY_DETAIL = 'Session busy — try again later';
       const ALLOWED_MIME = new Set([
         'audio/webm', 'audio/ogg', 'audio/wav', 'audio/x-wav',
         'audio/mpeg', 'audio/mp4', 'audio/mp3',
       ]);
       
       export function createChatAudioRouter(handler: VoiceHandler, lock: SessionLock): Router {
         const router = Router();
         const upload = multer({
           storage: multer.memoryStorage(),
           limits: { fileSize: 25 * 1024 * 1024 },
           fileFilter: (_req, file, cb) => {
             const mt = (file.mimetype || '').split(';')[0].trim().toLowerCase();
             if (ALLOWED_MIME.has(mt)) cb(null, true);
             else cb(new Error(`Unsupported audio content-type: ${file.mimetype}`));
           },
         });
       
         router.post('/chat/audio', (req, res, next) => {
           upload.single('audio')(req, res, (err) => {
             if (err) {
               res.status(400).json({ detail: err.message, code: 'INVALID_UPLOAD' });
               return;
             }
             handleRequest(req, res, handler, lock).catch(next);
           });
         });
       
         return router;
       }
       
       async function handleRequest(req: Request, res: Response, handler: VoiceHandler, lock: SessionLock) {
         if (!req.file || !req.file.buffer || req.file.buffer.length === 0) {
           res.status(400).json({ detail: 'audio file required', code: 'MISSING_FILE' });
           return;
         }
         const release = lock.tryAcquire();
         if (!release) {
           res.status(429).json({ detail: BUSY_DETAIL });
           return;
         }
         try {
           const out = await handler.handle(req.file.buffer);
           res.json({
             transcription: out.transcription,
             message: out.message,
             audio_base64: out.audio.toString('base64'),
             audio_format: out.audioFormat,
             stt_provider: out.sttProvider,
             tts_provider: out.ttsProvider,
           });
         } catch (err) {
           const e = err as Error & { code?: string };
           const code = e.code ?? 'UNKNOWN';
           const status = code === 'EMPTY_AUDIO' || code === 'NO_SPEECH' ? 400 : 500;
           res.status(status).json({ detail: e.message, code });
         } finally {
           release();
         }
       }
       ```
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>Router compila.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes supertest do endpoint (todos os branches)</name>
  <files>apps/backend-ts/src/routes/chat-audio.test.ts</files>
  <behavior>
    Casos (usando supertest + stub do VoiceHandler + SessionLock real):
    1. POST sem arquivo → 400 MISSING_FILE
    2. POST com content-type errado (text/plain) → 400 INVALID_UPLOAD
    3. Lock ocupado (chamar tryAcquire antes) → 429
    4. Happy path → 200, JSON com todos os campos + audio_base64 decodável
    5. VoiceHandler throw code NO_SPEECH → 400
    6. VoiceHandler throw code STT_FAILED → 500
    7. Lock liberado após cada resposta (verificável: depois de um request, tryAcquire() retorna função não-null)
  </behavior>
  <action>
    Usar supertest + express app minimal com o router montado. Stub VoiceHandler como `{ handle: vi.fn() }`. Enviar multipart via `.attach('audio', Buffer.from([...]), { filename: 'r.webm', contentType: 'audio/webm' })`.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/routes/chat-audio.test.ts</automated>
  </verify>
  <done>Todos os 7 casos passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire createApp + bootstrap no index.ts com providers reais</name>
  <files>apps/backend-ts/src/app.ts, apps/backend-ts/src/index.ts</files>
  <behavior>
    - createApp aceita opcional `voiceHandler?: VoiceHandler`; monta router se presente (junto com session/lock)
    - index.ts (main()) instancia stt via createSTTProvider(), tts via createTTSProvider(), VoiceHandler com session+stt+tts+store, passa pro createApp
    - Startup chama assertFfmpegAvailable() (warning não-fatal)
    - Startup loga providers selecionados: `[voice] STT=local TTS=elevenlabs+local`
  </behavior>
  <action>
    1. Editar `app.ts`:
       ```typescript
       export interface CreateAppOptions {
         session?: ChatSession;
         lock?: SessionLock;
         toolLogger?: ToolLogger;
         voiceHandler?: VoiceHandler;
       }
       // ...
       if (opts.session && opts.lock && opts.voiceHandler) {
         app.use('/', createChatAudioRouter(opts.voiceHandler, opts.lock));
       }
       ```
    2. Editar `index.ts` entre o bootstrap do ChatSession e `createApp`:
       ```typescript
       // Step 5b: Voice pipeline
       assertFfmpegAvailable();
       const stt = createSTTProvider();
       const tts = createTTSProvider();
       const voiceStore = new MemoryStore(); // usa DB default
       const voiceHandler = new VoiceHandler({ session, stt, tts, store: voiceStore, conversationId: null });
       console.log(`[voice] STT=${stt.name} TTS=${tts.name}`);
       ```
       E passar `voiceHandler` pro `createApp({...})`.
    3. Imports necessários no topo do index.ts.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm exec tsc --noEmit && pnpm vitest run src/routes/chat-audio.test.ts</automated>
  </verify>
  <done>Bootstrap wired, build limpo, testes verdes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run` suite completa verde
- `pnpm --filter backend-ts build` OK
- Smoke manual opcional: `curl -X POST http://localhost:8001/chat/audio -F audio=@test/fixtures/audio/small.wav` retorna 200 OU 500 (depende de STT real ter modelo baixado) — não bloqueante
</verification>

<success_criteria>
1. POST /chat/audio existe no backend TS com spec completa de CONTEXT.md
2. Lock compartilhado com /chat funciona (429 cross-endpoint)
3. Error mapping correto (400/429/500)
4. Bootstrap wires providers reais
</success_criteria>

<output>
`.planning/phases/19-voice-backend/19-07-SUMMARY.md`
</output>
