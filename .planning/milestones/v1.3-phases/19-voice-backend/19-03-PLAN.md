---
phase: 19-voice-backend
plan: 03
type: execute
wave: 1
depends_on: [19-02]
files_modified:
  - apps/backend-ts/src/voice/tts/local.ts
  - apps/backend-ts/src/voice/tts/wav-encoder.ts
  - apps/backend-ts/src/voice/tts/local.test.ts
  - apps/backend-ts/src/voice/tts/wav-encoder.test.ts
autonomous: true
requirements: [VOICE-TS-02]
must_haves:
  truths:
    - "LocalTTSProvider usa @xenova/transformers pipeline('text-to-speech', 'Xenova/speecht5_tts') pra sintetizar"
    - "Output Float32Array PCM é convertido para Buffer WAV via encoder custom"
    - "synthesize(text) retorna {audio: Buffer, format: 'wav'}"
    - "Pipeline é lazy — só carrega no primeiro synthesize"
    - "LocalTTSProvider.name === 'local'"
  artifacts:
    - path: "apps/backend-ts/src/voice/tts/local.ts"
      provides: "class LocalTTSProvider implements TTSProvider"
    - path: "apps/backend-ts/src/voice/tts/wav-encoder.ts"
      provides: "function float32ToWav(samples, sampleRate): Buffer"
  key_links:
    - from: "apps/backend-ts/src/voice/tts/local.ts"
      to: "@xenova/transformers"
      via: "pipeline('text-to-speech', 'Xenova/speecht5_tts')"
      pattern: "text-to-speech"
---

<objective>
Implementar LocalTTSProvider usando Transformers.js (já instalado na Fase 16) com modelo Speecht5, com encoder auxiliar Float32Array → WAV Buffer. Fallback local para o wrapper do plano 19-04.

Depends_on: 19-02 (precisa da interface TTSProvider).
Output: apps/backend-ts/src/voice/tts/local.ts + wav-encoder.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@apps/backend-ts/src/voice/tts/provider.ts

<interfaces>
Transformers.js text-to-speech pipeline (já em uso via @xenova/transformers 2.17.2 para embeddings na Fase 16):
```typescript
import { pipeline } from '@xenova/transformers';
const synthesizer = await pipeline('text-to-speech', 'Xenova/speecht5_tts', { quantized: false });
const speaker_embeddings = 'https://huggingface.co/datasets/Xenova/transformers.js-docs/resolve/main/speaker_embeddings.bin';
const out = await synthesizer('Hello world', { speaker_embeddings });
// out = { audio: Float32Array, sampling_rate: 16000 }
```

WAV format (PCM16LE mono):
- RIFF header 44 bytes: "RIFF", size, "WAVE", "fmt ", 16, 1 (PCM), channels, sampleRate, byteRate, blockAlign, 16, "data", dataSize
- samples: Float32Array [-1,1] → Int16Array via Math.round(s*32767)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: wav-encoder (Float32Array → WAV Buffer)</name>
  <files>apps/backend-ts/src/voice/tts/wav-encoder.ts, apps/backend-ts/src/voice/tts/wav-encoder.test.ts</files>
  <behavior>
    - float32ToWav(samples: Float32Array, sampleRate: number): Buffer
    - Output buffer tem cabeçalho WAV válido (44 bytes) + PCM16LE data
    - header bytes 0-3 === 'RIFF', bytes 8-11 === 'WAVE', bytes 36-39 === 'data'
    - channels=1, bitsPerSample=16
    - amostras clipam em [-1, 1] antes de converter para Int16
    - buffer.length === 44 + samples.length * 2
  </behavior>
  <action>
    Implementar encoder WAV mono PCM16LE manualmente (não adicionar deps). Função pura. Testes: verificar header RIFF/WAVE/fmt/data; verificar sample rate no offset 24 LE; verificar clipping (sample 1.5 → 32767, -1.5 → -32768); verificar length.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/tts/wav-encoder.test.ts</automated>
  </verify>
  <done>Encoder WAV funcional e testado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LocalTTSProvider via Transformers.js Speecht5</name>
  <files>apps/backend-ts/src/voice/tts/local.ts, apps/backend-ts/src/voice/tts/local.test.ts</files>
  <behavior>
    - LocalTTSProvider.name === 'local'
    - Pipeline lazy-loaded (instância singleton na classe)
    - synthesize(text) → chama pipeline(text, {speaker_embeddings}) → recebe {audio: Float32Array, sampling_rate} → passa por float32ToWav → retorna {audio: Buffer, format: 'wav'}
    - Text vazio → throw
    - Loga warning se texto contém caracteres não-ASCII (Speecht5 é EN-only, qualidade pt-BR ruim)
    - Speaker embeddings URL configurável via env TTS_LOCAL_SPEAKER_EMBEDDINGS (default = URL do Xenova docs)
  </behavior>
  <action>
    1. Usar Context7 se quiser confirmar shape do output de pipeline text-to-speech do @xenova/transformers 2.17.x.
    2. Implementar:
       ```typescript
       import { pipeline } from '@xenova/transformers';
       import { float32ToWav } from './wav-encoder.js';
       import type { TTSProvider, TTSResult } from './provider.js';
       
       const DEFAULT_SPEAKER_EMBEDDINGS =
         'https://huggingface.co/datasets/Xenova/transformers.js-docs/resolve/main/speaker_embeddings.bin';
       
       export class LocalTTSProvider implements TTSProvider {
         readonly name = 'local';
         private _pipeline: any = null;
         private async getPipeline() {
           if (!this._pipeline) {
             this._pipeline = await pipeline('text-to-speech', 'Xenova/speecht5_tts', { quantized: false });
           }
           return this._pipeline;
         }
         async synthesize(text: string): Promise<TTSResult> {
           if (!text || text.trim().length === 0) throw new Error('LocalTTSProvider: empty text');
           if (/[áàâãéêíóôõúç]/i.test(text)) {
             console.warn('[voice] LocalTTSProvider: Speecht5 is English-only; pt-BR quality will be poor');
           }
           const synth = await this.getPipeline();
           const speakerEmbeddings = process.env.TTS_LOCAL_SPEAKER_EMBEDDINGS ?? DEFAULT_SPEAKER_EMBEDDINGS;
           const out = await synth(text, { speaker_embeddings: speakerEmbeddings });
           const samples: Float32Array = out.audio;
           const sampleRate: number = out.sampling_rate ?? 16000;
           return { audio: float32ToWav(samples, sampleRate), format: 'wav' };
         }
       }
       ```
    3. Teste com `vi.mock('@xenova/transformers', () => ({ pipeline: vi.fn().mockResolvedValue(vi.fn().mockResolvedValue({ audio: new Float32Array([0, 0.5, -0.5]), sampling_rate: 16000 })) }))`. Casos: synthesize retorna Buffer WAV; lazy load chamado uma vez; empty throws; warning acentos; name === 'local'.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/tts/local.test.ts</automated>
  </verify>
  <done>LocalTTSProvider retorna WAV válido, testado com pipeline mockado.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/voice/tts` passa
- Build TS limpo
</verification>

<success_criteria>
1. LocalTTSProvider implementa TTSProvider usando Transformers.js Speecht5
2. Output é WAV Buffer válido via encoder custom
3. Pipeline carregado lazy (1ª chamada)
4. Warning logado pra pt-BR
</success_criteria>

<output>
`.planning/phases/19-voice-backend/19-03-SUMMARY.md`
</output>
