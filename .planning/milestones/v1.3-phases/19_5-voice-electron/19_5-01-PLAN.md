---
phase: 19_5-voice-electron
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/ipc/chat.ts
  - apps/desktop/src/main/ipc/__tests__/chat-send-audio.test.ts
autonomous: true
requirements: [VOICE-TS-04, VOICE-TS-01, VOICE-TS-02]
must_haves:
  truths:
    - "Main handler usa backendConfig (URL + API key) ao invés de URL hardcoded"
    - "Requisição carrega header Authorization: Bearer <key>"
    - "Upload multipart usa content-type audio/webm e filename recording.webm"
    - "Handler parseia o response shape novo {transcription, message, audio_base64, audio_format, stt_provider, tts_provider}"
    - "Erros HTTP com body {code, detail} viram Result.error estruturado {code, message}"
    - "Erros 4xx não fazem retry; 5xx/network fazem retry com backoff"
    - "Timeout de 60s aborta e devolve erro estruturado"
  artifacts:
    - path: apps/desktop/src/main/ipc/chat.ts
      provides: "handleSendAudio pura + handler ipcMain atualizado"
    - path: apps/desktop/src/main/ipc/__tests__/chat-send-audio.test.ts
      provides: "testes vitest mockando fetch + loadBackendConfig"
  key_links:
    - from: apps/desktop/src/main/ipc/chat.ts
      to: apps/desktop/src/main/backend-client.ts
      via: "deps.config (BackendConfig)"
      pattern: "deps\\.config\\.(backendUrl|apiKey)"
    - from: apps/desktop/src/main/ipc/chat.ts
      to: apps/desktop/src/shared/ipc-types.ts
      via: "SendAudioResponse shape novo"
      pattern: "SendAudioResponse"
---

<objective>
Refatorar o IPC handler `chat:send-audio` em `apps/desktop/src/main/ipc/chat.ts` para consumir o endpoint novo do gateway `/api/chat/audio` (Fase 19) usando autenticação da Fase 18.5, content-type correto (`audio/webm`), parsing do response shape novo e error mapping estruturado.

Purpose: Fechar o loop de voz no lado cliente — o endpoint backend já existe e retorna áudio TTS; este plano faz o Electron falar com ele corretamente.
Output: Handler `handleSendAudio` puro (testável sem ipcMain), suite de testes vitest, integração via `setupChatHandlers`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19_5-voice-electron/19_5-CONTEXT.md
@apps/desktop/src/main/ipc/chat.ts
@apps/desktop/src/main/backend-client.ts
@apps/desktop/src/shared/ipc-types.ts
@apps/backend-ts/src/routes/chat-audio.ts

<interfaces>
From apps/desktop/src/main/backend-client.ts:
```typescript
export interface BackendConfig {
  backendUrl: string; // no trailing slash
  apiKey: string;
}
export function loadBackendConfig(env?: Record<string, string | undefined>): BackendConfig;
```

From apps/backend-ts/src/routes/chat-audio.ts — success response:
```json
{
  "transcription": "string",
  "message": "string",
  "audio_base64": "string",
  "audio_format": "mp3" | "wav",
  "stt_provider": "string",
  "tts_provider": "string"
}
```
Error response: `{ "detail": "string", "code": "EMPTY_AUDIO|NO_SPEECH|STT_FAILED|LLM_FAILED|TTS_FAILED|MISSING_FILE|INVALID_UPLOAD|UNKNOWN" }` com status 400/429/500.

New SendAudioResponse shape (definido no Plano 19_5-02 — mesma wave, mas este plano assume o shape):
```typescript
type SendAudioResponse =
  | { success: true; data: {
      transcription: string;
      message: string;
      audioBase64: string;
      audioFormat: 'mp3' | 'wav';
      sttProvider: string;
      ttsProvider: string;
    } }
  | { success: false; error: { code: string; message: string } };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handleSendAudio puro + fetch autenticado pro gateway</name>
  <files>apps/desktop/src/main/ipc/chat.ts</files>
  <behavior>
    - Exporta função pura `handleSendAudio(audioBuffer: Buffer, deps: ChatHandlerDeps): Promise<SendAudioResponse>`
    - Monta URL como `${deps.config.backendUrl}/api/chat/audio` (NÃO usa constante GATEWAY_AUDIO_URL hardcoded — remove)
    - Monta FormData com Blob `type: 'audio/webm'` e filename `'recording.webm'`
    - Injeta header `Authorization: Bearer ${deps.config.apiKey}`
    - Timeout sobe pra 60000ms (`AUDIO_REQUEST_TIMEOUT_MS = 60000`)
    - Retry com backoff existente; `shouldRetry` retorna false se `status >= 400 && status < 500` (inclui 429)
    - Sucesso: parseia JSON e devolve `{success: true, data: {transcription, message, audioBase64: audio_base64, audioFormat: audio_format, sttProvider: stt_provider, ttsProvider: tts_provider}}`
    - Erro HTTP: lê body, tenta parsear JSON, extrai `code` e `detail`; devolve `{success: false, error: {code: code ?? 'HTTP_<status>', message: detail ?? fallback}}`. Erros não-JSON viram `code: 'HTTP_<status>'`, `message: bodyText || 'HTTP <status>'`
    - Erro de rede (fetch throw sem status): `{success: false, error: {code: 'NETWORK', message: err.message}}`
    - Timeout/AbortError: `{success: false, error: {code: 'TIMEOUT', message: 'Request timeout after 60 seconds'}}`
    - Atualiza `setupChatHandlers` pra chamar `handleSendAudio(audioBuffer, deps)` dentro do `ipcMain.handle`
  </behavior>
  <action>
    1. Remover constante `GATEWAY_AUDIO_URL`; adicionar `const AUDIO_REQUEST_TIMEOUT_MS = 60000`.
    2. Criar `async function handleSendAudio(audioBuffer: Buffer, deps: ChatHandlerDeps): Promise<SendAudioResponse>` seguindo behavior acima. Reusar `retryWithBackoff` existente.
    3. Dentro do operation: `new FormData()`, `new Blob([audioBuffer as unknown as BlobPart], {type: 'audio/webm'})`, `formData.append('audio', audioBlob, 'recording.webm')`.
    4. `fetch(url, {method: 'POST', body: formData, headers: {Authorization: \`Bearer ${deps.config.apiKey}\`}, signal: controller.signal})`. NÃO setar Content-Type manualmente — FormData define boundary.
    5. Response NOT ok: ler `response.text()`, `try { JSON.parse }`, montar Error com `status` e `bodyCode`/`bodyDetail` anexados, throw. `shouldRetry` lê `err.status`.
    6. Após retryWithBackoff: catch converte erros em `{success:false, error}` estruturado conforme behavior.
    7. Sucesso dentro do try: `const raw = await response.json()`; retornar `{success: true, data: {transcription: raw.transcription, message: raw.message, audioBase64: raw.audio_base64, audioFormat: raw.audio_format, sttProvider: raw.stt_provider, ttsProvider: raw.tts_provider}}`.
    8. Atualizar `ipcMain.handle(IPC_CHANNELS.CHAT_SEND_AUDIO, ...)` pra delegar em `handleSendAudio(audioBuffer, deps)`.
    9. NÃO mexer em `handleSendText` nem em `setupChatHandlers` além da mudança no handler de audio.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/main/ipc/__tests__/chat-send-audio.test.ts</automated>
  </verify>
  <done>
    Função `handleSendAudio` exportada, handler ipcMain atualizado, todos os testes do Task 2 passam, `pnpm tsc --noEmit` sem erros em chat.ts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes vitest pro handleSendAudio (mock fetch + loadBackendConfig)</name>
  <files>apps/desktop/src/main/ipc/__tests__/chat-send-audio.test.ts</files>
  <behavior>
    - Mock `global.fetch` via `vi.fn()`; helper `makeDeps(config?)` cria `ChatHandlerDeps` com backendUrl `http://localhost:3000`, apiKey `test-key`
    - Testes obrigatórios:
      1. `success` — fetch 200 + body shape novo → devolve `{success:true, data:{transcription, message, audioBase64, audioFormat:'mp3', sttProvider, ttsProvider}}`
      2. `URL correta` — chama `http://localhost:3000/api/chat/audio`
      3. `auth header` — request carrega `Authorization: Bearer test-key`
      4. `content-type webm` — FormData blob type === 'audio/webm' e filename === 'recording.webm' (inspecionar o body.get('audio'))
      5. `error body JSON` — 400 com `{code:'NO_SPEECH', detail:'no speech'}` → `{success:false, error:{code:'NO_SPEECH', message:'no speech'}}`
      6. `error body não-JSON` — 500 com texto plano → `{success:false, error:{code:'HTTP_500', message:<text>}}`
      7. `4xx não retrya` — mock fetch retorna 400; `fetch` foi chamado exatamente 1 vez
      8. `5xx retrya` — primeiro call 500, segundo 200 → sucesso, fetch chamado 2x
      9. `network error` — fetch throw `TypeError('fetch failed')` → `{success:false, error:{code:'NETWORK', ...}}`
      10. `timeout` — fetch nunca resolve; vitest fake timers avançam 60s → `{success:false, error:{code:'TIMEOUT', ...}}`
  </behavior>
  <action>
    1. Criar arquivo. `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    2. `import { handleSendAudio, type ChatHandlerDeps } from '../chat';`
    3. `beforeEach`: `vi.stubGlobal('fetch', vi.fn())`. `afterEach`: `vi.unstubAllGlobals(); vi.useRealTimers();`
    4. Helper `makeDeps(): ChatHandlerDeps` retorna `{openStream: vi.fn(), config: {backendUrl: 'http://localhost:3000', apiKey: 'test-key'}, actionExecutor: {enqueue: vi.fn()} as any}`
    5. Helper `jsonResponse(status, body)` retorna `{ok: status>=200&&status<300, status, json: async () => body, text: async () => JSON.stringify(body)}`
    6. Helper `textResponse(status, text)` similar mas `text: async () => text` e `json` throws.
    7. Para o teste de content-type/filename: interceptar fetch args; o `body` é uma `FormData`; `body.get('audio')` retorna Blob com `.type` e o 3o arg do append é o filename — verificar via `File.name` (o Electron FormData usa File quando filename fornecido).
    8. Para teste de timeout: `vi.useFakeTimers()`, fetch retorna Promise pendente, `handleSendAudio` é `await`ado em Promise.race; `vi.advanceTimersByTime(60000)`.
    9. Para retry: mockar `fetch` com `.mockImplementationOnce` x N. Como `retryWithBackoff` usa `setTimeout`, precisa `vi.useFakeTimers()` + `await vi.runAllTimersAsync()` ou reduzir delays. Mais simples: permitir delays reais mas limitar para [0, 10, 10] via não mexer — é aceitável; usar `vi.useFakeTimers({shouldAdvanceTime: true})` ou rodar real (testes ~4s).
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/main/ipc/__tests__/chat-send-audio.test.ts</automated>
  </verify>
  <done>
    10 testes passando, cobrindo success, URL, auth header, content-type webm + filename, error body JSON/não-JSON, no-retry 4xx, retry 5xx, network error, timeout.
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` passa em apps/desktop
- `pnpm vitest run src/main/ipc` passa (inclui chat-send-audio.test.ts e testes preexistentes de send-text)
- Nenhum `GATEWAY_AUDIO_URL` hardcoded remanescente em chat.ts
- `grep -n 'audio/wav\|recording.wav' apps/desktop/src/main/ipc/chat.ts` retorna vazio
</verification>

<success_criteria>
Handler `handleSendAudio` puro e testado, usa `deps.config` para URL+auth, content-type webm, parseia response shape novo, error mapping estruturado, 4xx no-retry, timeout 60s. Testes mockam fetch e cobrem todos os caminhos.
</success_criteria>

<output>
Criar `.planning/phases/19_5-voice-electron/19_5-01-SUMMARY.md`
</output>
