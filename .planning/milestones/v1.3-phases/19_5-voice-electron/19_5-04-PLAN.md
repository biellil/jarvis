---
phase: 19_5-voice-electron
plan: 04
type: execute
wave: 2
depends_on: [19_5-01, 19_5-02, 19_5-03]
files_modified:
  - apps/desktop/src/renderer/src/App.tsx
  - apps/desktop/src/renderer/src/components/Toast.tsx
  - apps/desktop/src/renderer/src/lib/errorMessages.ts
  - apps/desktop/src/renderer/src/lib/__tests__/errorMessages.test.ts
  - apps/desktop/src/renderer/src/components/__tests__/App.voice.test.tsx
autonomous: true
requirements: [VOICE-TS-04]
must_haves:
  truths:
    - "Após sendAudio com sucesso, transcription aparece como HumanMessage e message como AIMessage no chat"
    - "playTTSResponse é chamado com audioBase64 e audioFormat do response"
    - "Erros do backend viram toast pt-BR (não HumanMessage/AIMessage)"
    - "TTS_FAILED ainda insere a message no chat mas mostra toast"
    - "PTT hotkey (Fase 13) continua disparando sendAudio como antes"
    - "Toast auto-fecha em 5s e pode ser fechado manualmente"
  artifacts:
    - path: apps/desktop/src/renderer/src/components/Toast.tsx
      provides: "Componente Toast controlado via props"
    - path: apps/desktop/src/renderer/src/lib/errorMessages.ts
      provides: "mapErrorCode(code, message): string — mapping pt-BR"
    - path: apps/desktop/src/renderer/src/App.tsx
      provides: "handleAudioResponse + toast state + player integration"
  key_links:
    - from: apps/desktop/src/renderer/src/App.tsx
      to: apps/desktop/src/renderer/src/audio/ttsPlayer.ts
      via: "import playTTSResponse"
      pattern: "playTTSResponse"
    - from: apps/desktop/src/renderer/src/App.tsx
      to: window.jarvis.sendAudio
      via: "consome SendAudioResponse shape novo"
      pattern: "sendAudio"
---

<objective>
Integrar o response novo no `App.tsx`: inserir `transcription` como HumanMessage, `message` como AIMessage, chamar `playTTSResponse(audioBase64, audioFormat)` e exibir erros via Toast com mensagens pt-BR. Respeitar o caso especial `TTS_FAILED` (insere message mas avisa do áudio).

Purpose: Último plano da fase — fecha o loop de voz end-to-end no Electron. PTT existente continua funcionando.
Output: App.tsx integrado, componente Toast, util `errorMessages.ts`, testes de renderização.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19_5-voice-electron/19_5-CONTEXT.md
@apps/desktop/src/renderer/src/App.tsx
@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/renderer/src/audio/ttsPlayer.ts

<interfaces>
From ipc-types.ts (post Plan 02):
```typescript
type SendAudioResponse =
  | { success: true; data: { transcription: string; message: string; audioBase64: string; audioFormat: 'mp3'|'wav'; sttProvider: string; ttsProvider: string } }
  | { success: false; error: { code: string; message: string } };
```

From ttsPlayer.ts (post Plan 03):
```typescript
export function playTTSResponse(base64: string, format: 'mp3'|'wav'): Promise<void>;
export function stopTTSPlayback(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/errorMessages.ts com mapping pt-BR</name>
  <files>apps/desktop/src/renderer/src/lib/errorMessages.ts, apps/desktop/src/renderer/src/lib/__tests__/errorMessages.test.ts</files>
  <behavior>
    - `mapErrorCode(code: string, fallback?: string): string` retorna mensagem pt-BR conforme tabela do CONTEXT:
      - EMPTY_AUDIO → "Não recebi nenhum áudio. Tenta de novo."
      - NO_SPEECH → "Não consegui ouvir sua mensagem. Fala mais perto do microfone?"
      - STT_FAILED → "Erro ao transcrever áudio. Tenta de novo."
      - LLM_FAILED → "Erro ao processar sua mensagem."
      - TTS_FAILED → "Resposta pronta, mas não consegui gerar o áudio."
      - NETWORK → "Sem conexão com o servidor."
      - TIMEOUT → "O servidor demorou pra responder. Tenta de novo."
      - NO_API_KEY → "API key do JARVIS não configurada."
      - HTTP_429 → "JARVIS está ocupado. Aguarde um instante."
      - HTTP_500 / HTTP_5xx → "Erro interno. Tenta de novo em alguns segundos."
      - HTTP_4xx outros → fallback ?? "Requisição inválida."
      - default → fallback ?? "Erro desconhecido."
    - `isRecoverableWithMessage(code: string): boolean` retorna true só pra `TTS_FAILED` (caso especial — mostra texto e avisa do áudio)
    - Testes cobrem todos os códigos da tabela + default
  </behavior>
  <action>
    1. Criar `lib/errorMessages.ts` exportando `mapErrorCode` e `isRecoverableWithMessage`. Usar `switch` com prefixo-match pra HTTP_4xx/5xx: `if (code.startsWith('HTTP_4')) ... if (code.startsWith('HTTP_5')) ...`.
    2. Criar `__tests__/errorMessages.test.ts` com ~12 casos (1 por código + default).
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/renderer/src/lib/__tests__/errorMessages.test.ts</automated>
  </verify>
  <done>
    Função pura, testada, sem deps externas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Componente Toast</name>
  <files>apps/desktop/src/renderer/src/components/Toast.tsx</files>
  <action>
    1. Componente funcional `Toast({message, variant='error', onClose, autoCloseMs=5000})`:
    ```tsx
    import { useEffect } from 'react';
    export interface ToastProps {
      message: string;
      variant?: 'error' | 'warning' | 'info';
      onClose: () => void;
      autoCloseMs?: number;
    }
    export function Toast({message, variant='error', onClose, autoCloseMs=5000}: ToastProps) {
      useEffect(() => {
        const t = setTimeout(onClose, autoCloseMs);
        return () => clearTimeout(t);
      }, [onClose, autoCloseMs]);
      return (
        <div
          role="alert"
          className={`toast toast-${variant}`}
          onClick={onClose}
          style={{
            position: 'fixed', bottom: 20, left: '50%', transform: 'translateX(-50%)',
            padding: '12px 20px', borderRadius: 8, cursor: 'pointer',
            background: variant === 'error' ? '#dc2626' : variant === 'warning' ? '#f59e0b' : '#3b82f6',
            color: 'white', zIndex: 9999, maxWidth: 400,
            WebkitAppRegion: 'no-drag' as any,
          }}
        >
          {message}
        </div>
      );
    }
    ```
    2. `WebkitAppRegion: 'no-drag'` pra permitir click apesar da drag region do App.tsx.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | grep Toast.tsx || echo "ok"</automated>
  </verify>
  <done>
    Componente exporta `Toast` e `ToastProps`, compila sem erros.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrar em App.tsx (handleAudioResponse + player + toast)</name>
  <files>apps/desktop/src/renderer/src/App.tsx, apps/desktop/src/renderer/src/components/__tests__/App.voice.test.tsx</files>
  <behavior>
    - App.tsx (ou um hook extraído) expõe função `handleAudioResponse(response: SendAudioResponse)`:
      - `success: true`:
        - insere `transcription` como HumanMessage no estado de mensagens do chat (via OrbContext/ChatInput store existente OU dispatch direto — usar o que já existe)
        - insere `message` como AIMessage
        - chama `playTTSResponse(audioBase64, audioFormat).catch(err => setToast({message: 'Erro ao tocar áudio', variant: 'warning'}))`
      - `success: false`:
        - Se `isRecoverableWithMessage(error.code)` (TTS_FAILED): ainda precisa da message? NÃO — o backend NÃO devolve message em erro. Ajuste: TTS_FAILED SÓ mostra toast (variant warning). [Correção vs CONTEXT: o response shape de erro não tem message field; TTS_FAILED do backend atual vira 500. Caso o backend no futuro retorne success+flag, ajustar. Por ora: toast warning pra TTS_FAILED, error pra resto.]
        - Mostra `<Toast message={mapErrorCode(error.code, error.message)} variant={isRecoverableWithMessage(error.code) ? 'warning' : 'error'} />`
    - PTT continua funcionando: o listener que já existe (`window.jarvis.ipcRenderer.on('ptt:stop', ...)` ou dentro do useAudioRecorder/ChatInput) agora chama `handleAudioResponse` com o resultado de `window.jarvis.sendAudio(bytes)`.
    - Toast state: `const [toast, setToast] = useState<{message: string; variant: 'error'|'warning'} | null>(null)`. Render `{toast && <Toast {...toast} onClose={() => setToast(null)} />}` fora do container draggable (ou com `no-drag`).
    - Cleanup: `useEffect(() => () => stopTTSPlayback(), [])` no unmount.
  </behavior>
  <action>
    1. Reler `apps/desktop/src/renderer/src/App.tsx` e `apps/desktop/src/renderer/src/components/ChatInput` para identificar onde o sendAudio já é invocado (PTT flow existente). Se o fluxo atual está dentro de `ChatInput` ou de um hook, estender ali; senão, criar `useVoiceHandler` em `src/renderer/src/hooks/useVoiceHandler.ts` e chamar do AppContent.
    2. Importar `playTTSResponse, stopTTSPlayback` de `./audio/ttsPlayer` (ajustar path).
    3. Importar `mapErrorCode, isRecoverableWithMessage` de `./lib/errorMessages`.
    4. Importar `Toast` de `./components/Toast`.
    5. Adicionar state `toast` no AppContent, renderizar condicionalmente.
    6. Extrair `handleAudioResponse(response)` dentro do AppContent (ou hook).
    7. Substituir o handler atual que processa retorno de sendAudio (se existe) pelo novo — que consome o shape novo.
    8. Em `handleAudioResponse`, para cada mensagem inserida, usar o mesmo store/mecanismo que o ChatInput usa pro text flow (provavelmente um contexto ou hook global).
    9. Se não houver store claro, adicionar mensagens via emissão de evento customizado `window.dispatchEvent(new CustomEvent('jarvis:message', {detail: {...}}))` que ChatInput escuta — MAS prefira o mecanismo existente. Ler ChatInput antes de decidir.
    10. useEffect de cleanup chama `stopTTSPlayback()` no unmount.
    11. Testes em `components/__tests__/App.voice.test.tsx` usando `@testing-library/react`:
        - Mock `window.jarvis.sendAudio` e `ttsPlayer.playTTSResponse` via `vi.mock`
        - Test 1: success response → playTTSResponse chamado com (audioBase64, 'mp3')
        - Test 2: success response → transcription e message aparecem como texto renderizado (assumindo ChatInput renderiza histórico) — SE não for fácil testar via DOM, spy no método do store.
        - Test 3: error NO_SPEECH → Toast com texto "Não consegui ouvir sua mensagem..."; playTTSResponse NÃO chamado
        - Test 4: error TTS_FAILED → Toast variant warning com texto correspondente
        - Test 5: toast fecha auto em 5s (vi.useFakeTimers + advance)
        - Test 6: toast fecha on click
    12. Se testar o App inteiro for muito complexo (OrbProvider, etc.), extrair `handleAudioResponse` pra uma função pura exportada (`src/renderer/src/voice/handleAudioResponse.ts`) que recebe `{setToast, addMessage, player}` como deps — e testar essa função pura com mocks. **Preferir esta abordagem** — é mais simples que renderizar App completo.
    13. Se extrair: criar `apps/desktop/src/renderer/src/voice/handleAudioResponse.ts` com assinatura:
    ```typescript
    interface Deps {
      addHumanMessage: (text: string) => void;
      addAgentMessage: (text: string) => void;
      setToast: (t: {message: string; variant: 'error'|'warning'} | null) => void;
      playTTS: (b64: string, fmt: 'mp3'|'wav') => Promise<void>;
    }
    export async function handleAudioResponse(resp: SendAudioResponse, deps: Deps): Promise<void>;
    ```
    E testar essa função pura. App.tsx só injeta as deps reais.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/renderer/src/components/__tests__/App.voice.test.tsx src/renderer/src/voice 2>&1 | tail -30</automated>
  </verify>
  <done>
    handleAudioResponse testado (função pura), App.tsx integra toast + player, PTT continua disparando o fluxo, `pnpm tsc --noEmit` em apps/desktop sem erros.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação manual end-to-end do loop de voz</name>
  <what-built>
    - IPC handler chat:send-audio refatorado (auth, webm, shape novo)
    - Types atualizados
    - ttsPlayer com cancel de anterior
    - App.tsx integra transcription+message no chat e toca TTS, erros viram toast
  </what-built>
  <how-to-verify>
    1. Backend: `cd apps/backend-ts && pnpm dev` (com JARVIS_API_KEY configurado e STT/TTS providers OK)
    2. Gateway: `cd apps/gateway && pnpm dev`
    3. Electron: `cd apps/desktop && JARVIS_API_KEY=<same> JARVIS_BACKEND_URL=http://localhost:3000 pnpm dev`
    4. Aperte PTT (hotkey Fase 13), fale "Olá JARVIS, que horas são?", solte
    5. Espere:
       - Sua transcription aparece como HumanMessage no chat
       - Resposta do agente aparece como AIMessage
       - Áudio TTS toca nos speakers
    6. Aperte PTT durante áudio tocando, fale outra coisa — o áudio anterior para e o novo toca
    7. Grave um PTT em silêncio (não fala nada) — deve aparecer toast "Não consegui ouvir sua mensagem..."
    8. Pare backend TS e tente PTT — deve aparecer toast "Sem conexão com o servidor." ou "Erro interno..."
  </how-to-verify>
  <resume-signal>Digite "aprovado" ou descreva o que quebrou</resume-signal>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` em apps/desktop sem erros
- `pnpm vitest run` em apps/desktop passa
- Fluxo manual PTT → transcription+message+áudio funcionando
- Erros viram toast pt-BR
- PTT da Fase 13 não foi modificado (grep `ptt-hotkey` sem mudanças)
</verification>

<success_criteria>
Loop de voz end-to-end fechado: PTT → webm → gateway autenticado → backend → transcription+message+TTS → chat + playback. Erros viram toast. Áudio novo cancela anterior.
</success_criteria>

<output>
Criar `.planning/phases/19_5-voice-electron/19_5-04-SUMMARY.md`
</output>
