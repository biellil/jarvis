---
phase: 19_5-voice-electron
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/src/audio/ttsPlayer.ts
  - apps/desktop/src/renderer/src/audio/__tests__/ttsPlayer.test.ts
  - apps/desktop/src/renderer/hooks/useAudioRecorder.ts
autonomous: true
requirements: [VOICE-TS-04]
must_haves:
  truths:
    - "AudioContext é singleton lazy — criado no primeiro playTTSResponse e reutilizado"
    - "playTTSResponse(base64, format) decodifica via decodeAudioData e toca via AudioBufferSourceNode"
    - "Novo play cancela o source anterior imediatamente (antes do decode do novo)"
    - "onended limpa currentSource só se ainda for o mesmo"
    - "useAudioRecorder.stopRecording devolve Uint8Array webm bruto (sem conversão pra WAV)"
  artifacts:
    - path: apps/desktop/src/renderer/src/audio/ttsPlayer.ts
      provides: "playTTSResponse, stopTTSPlayback, __resetForTests"
    - path: apps/desktop/src/renderer/src/audio/__tests__/ttsPlayer.test.ts
      provides: "testes vitest com AudioContext mockado"
  key_links:
    - from: apps/desktop/src/renderer/hooks/useAudioRecorder.ts
      to: apps/desktop/src/main/ipc/chat.ts
      via: "stopRecording retorna webm bytes → IPC sendAudio"
      pattern: "new Blob.*audio/webm"
---

<objective>
Criar módulo de playback TTS no renderer usando Web Audio API (AudioContext + decodeAudioData + AudioBufferSourceNode) com singleton lazy, cancelamento de áudio anterior, e simplificar `useAudioRecorder` pra devolver bytes WebM brutos (D-3 — backend aceita webm diretamente, conversão WAV era workaround).

Purpose: O main handler agora manda webm puro (Plan 01) e o response traz TTS base64 (mp3/wav). Este plano implementa as duas pontas do renderer: captura simplificada + player novo.
Output: Módulo `ttsPlayer.ts` + testes + useAudioRecorder refatorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19_5-voice-electron/19_5-CONTEXT.md
@apps/desktop/src/renderer/hooks/useAudioRecorder.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo ttsPlayer.ts (singleton lazy AudioContext)</name>
  <files>apps/desktop/src/renderer/src/audio/ttsPlayer.ts</files>
  <behavior>
    - `playTTSResponse(base64: string, _format: 'mp3' | 'wav'): Promise<void>` — decodifica e toca
    - AudioContext único lazy via `getAudioContext()`; se `state === 'suspended'`, chama `.resume()`
    - `currentSource` module-level; novo play chama `currentSource?.stop()` IMEDIATAMENTE (antes do decode) e zera referência
    - base64 → Uint8Array via `Uint8Array.from(atob(base64), c => c.charCodeAt(0))`; passa `.buffer` pra `decodeAudioData`
    - Cria `BufferSource`, connect destination, `source.onended = () => { if (currentSource === source) currentSource = null; }`, `source.start()`, atribui `currentSource = source`
    - `stopTTSPlayback()` — para manualmente; usado no cleanup
    - `__resetForTests()` — reset de módulo-level state (exported, usado só em testes)
    - Erros de decode propagam (caller trata)
  </behavior>
  <action>
    1. Criar diretório `apps/desktop/src/renderer/src/audio/`.
    2. Implementar:
    ```typescript
    let audioContext: AudioContext | null = null;
    let currentSource: AudioBufferSourceNode | null = null;

    function getAudioContext(): AudioContext {
      if (!audioContext) {
        audioContext = new AudioContext();
      }
      return audioContext;
    }

    export async function playTTSResponse(base64: string, _format: 'mp3' | 'wav'): Promise<void> {
      // Cancel previous IMMEDIATELY (even before decode finishes)
      if (currentSource) {
        try { currentSource.stop(); } catch { /* already stopped */ }
        currentSource = null;
      }

      const ctx = getAudioContext();
      if (ctx.state === 'suspended') {
        await ctx.resume();
      }

      const bytes = Uint8Array.from(atob(base64), (c) => c.charCodeAt(0));
      const buffer = await ctx.decodeAudioData(bytes.buffer);

      const source = ctx.createBufferSource();
      source.buffer = buffer;
      source.connect(ctx.destination);
      source.onended = () => {
        if (currentSource === source) currentSource = null;
      };
      source.start();
      currentSource = source;
    }

    export function stopTTSPlayback(): void {
      if (currentSource) {
        try { currentSource.stop(); } catch { /* ignore */ }
        currentSource = null;
      }
    }

    export function __resetForTests(): void {
      currentSource = null;
      audioContext = null;
    }
    ```
    3. Nada mais. Não criar hook React neste task (App.tsx importa direto — Plano 04).
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/renderer/src/audio/__tests__/ttsPlayer.test.ts</automated>
  </verify>
  <done>
    Módulo exporta playTTSResponse, stopTTSPlayback, __resetForTests. Testes do Task 2 passam.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes ttsPlayer (mock AudioContext)</name>
  <files>apps/desktop/src/renderer/src/audio/__tests__/ttsPlayer.test.ts</files>
  <behavior>
    - Mock `AudioContext` global via `vi.stubGlobal('AudioContext', MockAudioContext)` antes de importar o módulo (ou reset via __resetForTests)
    - `MockBufferSource`: {buffer, connect, start, stop, onended}
    - `MockAudioContext`: {state:'running', destination:{}, resume: vi.fn().mockResolvedValue(undefined), createBufferSource: vi.fn(() => new MockBufferSource()), decodeAudioData: vi.fn().mockResolvedValue({duration:1} as any)}
    - `atob` já existe em jsdom/happy-dom; caso não, stub com `Buffer.from(s, 'base64').toString('binary')`
    - Testes:
      1. `toca com sucesso` — base64 válido → decodeAudioData chamado, createBufferSource chamado, source.start chamado, currentSource != null
      2. `cancela anterior` — dois playTTSResponse consecutivos → primeiro source.stop chamado uma vez
      3. `onended limpa referência` — dispara onended manualmente → currentSource null
      4. `resume se suspended` — state='suspended' → ctx.resume chamado
      5. `singleton context` — duas chamadas usam mesma instância de AudioContext (construtor chamado 1x)
      6. `stopTTSPlayback para source corrente` — após play, stopTTSPlayback → source.stop chamado, currentSource null
      7. `stop em source antigo não crasha` — source.stop lança → nada explode
  </behavior>
  <action>
    1. Criar arquivo; `beforeEach`: `__resetForTests(); vi.stubGlobal('AudioContext', MockAudioContextCtor)`; `afterEach`: `vi.unstubAllGlobals()`.
    2. Helper `MockBufferSource` e `MockAudioContextCtor` com contadores de construtor.
    3. Importar dinamicamente ou estaticamente — como há reset, pode ser estático.
    4. Para teste de cancelar anterior: chamar `await playTTSResponse('AAAA', 'mp3')` primeiro, capturar source1 do createBufferSource call 0, chamar de novo, checar `source1.stop` toHaveBeenCalled().
    5. Para "resume se suspended": setar `ctxInstance.state = 'suspended'` no construtor mock.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm vitest run src/renderer/src/audio/__tests__/ttsPlayer.test.ts</automated>
  </verify>
  <done>
    7 testes passando.
  </done>
</task>

<task type="auto">
  <name>Task 3: Simplificar useAudioRecorder pra devolver webm bruto</name>
  <files>apps/desktop/src/renderer/hooks/useAudioRecorder.ts</files>
  <action>
    1. Remover import `audiobuffer-to-wav` e qualquer conversão AudioContext/decodeAudioData/audioBufferToWav.
    2. `stopRecording` agora resolve com `Uint8Array` do WebM bruto:
    ```typescript
    const webmBlob = new Blob(chunksRef.current, { type: 'audio/webm' });
    const arrayBuffer = await webmBlob.arrayBuffer();
    const bytes = new Uint8Array(arrayBuffer);
    // cleanup refs
    resolve(bytes);
    ```
    3. Remover a criação do `AudioContext({sampleRate:16000})`, `decodeAudioData`, `audioContext.close()` — não precisamos mais, backend converte.
    4. Atualizar comentários do header do arquivo: "Phase 19.5: WebM/Opus bruto pro backend — conversão STT roda server-side".
    5. Manter: error handling NotAllowedError/NotFoundError, chunksRef, streamRef cleanup.
    6. Package.json: deixar `audiobuffer-to-wav` por enquanto (remoção de dep fica pra cleanup futuro — não quebrar build).
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | grep useAudioRecorder || echo "no useAudioRecorder errors"</automated>
  </verify>
  <done>
    stopRecording retorna Uint8Array webm; sem AudioContext no hook; arquivo sem imports não-usados.
  </done>
</task>

</tasks>

<verification>
- `pnpm vitest run src/renderer/src/audio` passa
- `pnpm tsc --noEmit` sem erros novos em useAudioRecorder.ts
- `grep -n "audiobuffer-to-wav" apps/desktop/src/renderer/hooks/useAudioRecorder.ts` vazio
</verification>

<success_criteria>
Módulo ttsPlayer funcional e testado, useAudioRecorder devolve webm cru pro IPC.
</success_criteria>

<output>
Criar `.planning/phases/19_5-voice-electron/19_5-03-SUMMARY.md`
</output>
