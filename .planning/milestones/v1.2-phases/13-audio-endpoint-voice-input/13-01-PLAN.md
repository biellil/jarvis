---
phase: 13-audio-endpoint-voice-input
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [package.json, apps/gateway/package.json, apps/desktop/package.json, pyproject.toml, apps/gateway/src/routes/__tests__/chat.test.ts, apps/desktop/src/main/ipc/__tests__/chat.test.ts, src/jarvis/api/routes/test_chat.py]
autonomous: true
requirements: [AUDIO-01, AUDIO-02, ACTV-03]
must_haves:
  truths:
    - "Dependencies are installed for multipart handling"
    - "Test scaffolds exist for all three tiers"
    - "Tests can be run without implementation"
  artifacts:
    - path: "apps/gateway/src/routes/__tests__/chat.test.ts"
      provides: "Test for POST /api/chat/audio endpoint"
      contains: "POST /api/chat/audio"
    - path: "apps/desktop/src/main/ipc/__tests__/chat.test.ts"
      provides: "Test for IPC sendAudio handler"
      contains: "sendAudio"
    - path: "src/jarvis/api/routes/test_chat.py"
      provides: "Test for FastAPI audio endpoint"
      contains: "test_chat_audio_endpoint"
  key_links:
    - from: "package.json"
      to: "workspace packages"
      via: "pnpm install"
      pattern: "multer.*audiobuffer-to-wav"
---

<objective>
Install required dependencies and create test scaffolds for audio endpoint implementation.

Purpose: Establish foundation with all dependencies and test-first approach (per Nyquist Rule)
Output: Dependencies installed, test files ready for RED→GREEN cycle
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-audio-endpoint-voice-input/13-CONTEXT.md
@.planning/phases/13-audio-endpoint-voice-input/13-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns from existing codebase that executors will use -->

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export interface IpcResult<T = void> {
  success: boolean;
  data?: T;
  error?: string;
}

export const IPC_CHANNELS = {
  CHAT_SEND_TEXT: 'chat:send-text',
  // Add: CHAT_SEND_AUDIO: 'chat:send-audio'
} as const;
```

From apps/gateway/src/routes/chat.ts (proxy pattern):
```typescript
const upstream = await fetch(`${config.fastapiUrl}/chat`, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify(req.body),
});
```

From src/jarvis/core/voice.py:
```python
async def transcribe(self, audio_path: str) -> str:
    """Transcribes audio file to text"""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies for multipart handling</name>
  <files>apps/gateway/package.json, apps/desktop/package.json, pyproject.toml</files>
  <action>
    Install required packages per D-08 and research findings:

    Gateway (apps/gateway):
    - Add multer@2.1.1 for Express multipart handling
    - Add @types/multer@1.4.11 for TypeScript types

    Desktop (apps/desktop):
    - Add audiobuffer-to-wav@1.0.0 for WAV conversion per D-08

    Python:
    - Add python-multipart==0.0.18 to pyproject.toml dependencies

    Run pnpm install in monorepo root to install Node dependencies.
    DO NOT run pip install yet - let the executor handle Python deps.
  </action>
  <verify>
    <automated>pnpm list multer audiobuffer-to-wav</automated>
  </verify>
  <done>All dependencies listed in package.json files and installable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolds for audio endpoints</name>
  <files>apps/gateway/src/routes/__tests__/chat.test.ts, apps/desktop/src/main/ipc/__tests__/chat.test.ts, src/jarvis/api/routes/test_chat.py</files>
  <behavior>
    - Test 1: Gateway POST /api/chat/audio accepts multipart and returns JSON
    - Test 2: Desktop IPC sendAudio handler sends buffer and returns response
    - Test 3: FastAPI /chat/audio endpoint transcribes and returns message
  </behavior>
  <action>
    Create failing tests following existing patterns:

    Gateway test (add to existing chat.test.ts):
    - Test POST /api/chat/audio with mock multipart upload
    - Expect 404 initially (endpoint doesn't exist yet)
    - Mock FastAPI response for GREEN phase

    Desktop IPC test (add to existing chat.test.ts or create if missing):
    - Test IPC_CHANNELS.CHAT_SEND_AUDIO handler
    - Mock gateway fetch to return success
    - Expect handler to not exist initially

    FastAPI test (add to existing test_chat.py):
    - Test POST /chat/audio with UploadFile fixture
    - Mock WhisperTranscriber.transcribe()
    - Mock ChatSession.send()
    - Expect 404 initially

    All tests should fail (RED) since implementations don't exist yet.
    Use existing test patterns from the codebase.
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/gateway test -- chat.test.ts 2>&1 | grep -E "(FAIL|PASS)"</automated>
  </verify>
  <done>Three test files created with failing tests for audio endpoints</done>
</task>

<task type="auto">
  <name>Task 3: Update shared types with audio interfaces</name>
  <files>apps/desktop/src/shared/ipc-types.ts</files>
  <action>
    Add audio-related types to shared IPC types following existing patterns:

    1. Add SendAudioData interface:
       - reply: string (transcribed text + response)

    2. Add SendAudioResponse type:
       - Type alias for IpcResult<SendAudioData>

    3. Add CHAT_SEND_AUDIO to IPC_CHANNELS constant:
       - 'chat:send-audio' following naming convention

    4. Update JarvisAPI interface:
       - Add sendAudio method signature that accepts Uint8Array and returns Promise<SendAudioResponse>

    This provides the type foundation that both Wave 1 and Wave 2 will implement against.
    Reference existing SendTextData/SendTextResponse pattern.
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop tsc --noEmit</automated>
  </verify>
  <done>Audio IPC types defined and TypeScript compilation passes</done>
</task>

</tasks>

<verification>
- Dependencies installed: `pnpm list multer audiobuffer-to-wav` shows packages
- Python dependency added to pyproject.toml
- Test files exist and run (fail expected)
- TypeScript types compile without errors
</verification>

<success_criteria>
- [ ] multer and @types/multer installed in gateway
- [ ] audiobuffer-to-wav installed in desktop
- [ ] python-multipart added to pyproject.toml
- [ ] Three test files created with RED tests
- [ ] IPC types updated with audio interfaces
- [ ] No implementation code written yet (test-first)
</success_criteria>

<output>
After completion, create `.planning/phases/13-audio-endpoint-voice-input/13-01-SUMMARY.md`
</output>