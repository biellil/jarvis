---
phase: 12-hotkey-text-chat
plan: 04
type: execute
wave: 2
depends_on: [12-02, 12-03]
files_modified:
  - apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.tsx
  - apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.test.tsx
  - apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.css
  - apps/desktop/src/renderer/components/SpeechBubble/index.ts
  - apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx
  - apps/desktop/src/renderer/App.css
  - apps/desktop/src/main/index.ts
autonomous: true
requirements: [ACTV-02]

must_haves:
  truths:
    - "Response appears in speech bubble above orb"
    - "Bubble grows to fit content without scrolling"
    - "Bubble remains visible until next message"
    - "Orb transitions through states correctly"
    - "Window resizes when bubble appears"
  artifacts:
    - path: "apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.tsx"
      provides: "Speech bubble display component"
      min_lines: 30
      exports: ["SpeechBubble"]
    - path: "apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.css"
      provides: "CSS-only bubble styling"
      contains: "clip-path"
  key_links:
    - from: "ChatInput.tsx"
      to: "SpeechBubble"
      via: "conditional rendering"
      pattern: "SpeechBubble.*reply"
    - from: "ChatInput.tsx"
      to: "setState('responding')"
      via: "orb state transition"
      pattern: "setState.*responding"
---

<objective>
Implement speech bubble display and complete orb state orchestration

Purpose: Show JARVIS responses visually and provide feedback during processing
Output: Complete chat interaction with visual feedback
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hotkey-text-chat/12-CONTEXT.md
@.planning/phases/12-hotkey-text-chat/12-RESEARCH.md

# Components to integrate with
@apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx
@apps/desktop/src/renderer/components/Orb/OrbContext.tsx
@apps/desktop/src/main/index.ts
</context>

<interfaces>
<!-- Interfaces from Plan 02 and 03 outputs -->

From updated ChatInput (Plan 02):
```typescript
// ChatInput now calls window.jarvis.sendText
// Sets setState('processing') on submit
// Needs to handle response and show bubble
```

From updated IPC (Plan 03):
```typescript
export interface SendTextData {
  reply: string;  // Changed from 'received'
}
```

From main/index.ts:
```typescript
// BrowserWindow config
const mainWindow = new BrowserWindow({
  width: 128,
  height: 128,
  // ... other config
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SpeechBubble component</name>
  <files>apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.tsx, apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.test.tsx, apps/desktop/src/renderer/components/SpeechBubble/SpeechBubble.css, apps/desktop/src/renderer/components/SpeechBubble/index.ts</files>
  <behavior>
    - Test 1: Renders text content passed as prop
    - Test 2: Has speech-bubble CSS class
    - Test 3: Empty text renders nothing
    - Test 4: Long text doesn't overflow horizontally
  </behavior>
  <action>
    Create SpeechBubble component per D-11, D-12, D-13.

    Component (SpeechBubble.tsx):
    - Simple functional component: ({ text }: { text: string })
    - Return null if !text
    - Return div with className="speech-bubble" containing text

    Styling (SpeechBubble.css) per Research Pattern 5:
    - Position: absolute, bottom: 140px (above 96px orb + spacing)
    - Width: max-content, max-width: 280px (prevent horizontal overflow)
    - Background: linear-gradient(135deg, #3b82f6, #8b5cf6)
    - Color: white, padding: 12px 16px
    - clip-path for bubble shape with tail pointing down (D-11)
    - Animation: fadeIn 0.3s ease-out
    - No max-height, grows vertically as needed (D-12)

    Reference: Research line 326-367 for CSS bubble pattern
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/renderer/components/SpeechBubble/SpeechBubble.test.tsx</automated>
  </verify>
  <done>SpeechBubble component created with CSS-only styling</done>
</task>

<task type="auto">
  <name>Task 2: Integrate bubble and complete state transitions</name>
  <files>apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx</files>
  <action>
    Update ChatInput to show bubble and orchestrate orb states.

    State management:
    - Add reply state: const [reply, setReply] = useState('')
    - Import SpeechBubble component

    Update handleSubmit per Research Pattern 4:
    - Clear previous reply: setReply('')
    - setState('processing') before sendText (already done)
    - On success: setState('responding'), setReply(result.data.reply)
    - After 2 seconds: setState('idle')
    - On error: setState('idle'), setReply(`Error: ${result.error}`)
    - Bubble stays visible until next message (D-13)

    Render bubble conditionally:
    - After input section: {reply && <SpeechBubble text={reply} />}

    Reference: Research line 275-302 for complete flow
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/renderer/components/ChatInput/ChatInput.test.tsx</automated>
  </verify>
  <done>ChatInput shows bubble, orb states transition correctly</done>
</task>

<task type="auto">
  <name>Task 3: Handle dynamic window resizing</name>
  <files>apps/desktop/src/main/index.ts, apps/desktop/src/renderer/App.css</files>
  <action>
    Make window resize when bubble appears (Research Pitfall 4 solution).

    In main/index.ts:
    - Add IPC handler for 'window:resize'
    - Accept height parameter
    - Call mainWindow.setSize(128, height)
    - Keep width fixed at 128px

    In renderer (ChatInput or App):
    - When bubble appears: Calculate total height needed
    - Send IPC to resize window
    - When bubble clears: Resize back to 128x128

    Alternative simpler approach:
    - Set window to larger fixed height (e.g., 300px)
    - Use CSS to keep content at bottom
    - Transparent background means extra space invisible

    Choose approach based on testing results.
  </action>
  <verify>
    <automated>pnpm --filter desktop dev</automated>
  </verify>
  <done>Window accommodates bubble without clipping</done>
</task>

</tasks>

<verification>
1. Start all services (gateway, FastAPI, desktop)
2. Press Ctrl+Shift+J to show widget
3. Click button, type "Hello"
4. Press Enter - orb turns processing (blue/violet)
5. Response arrives - orb turns responding (blue rings)
6. Bubble appears above orb with response
7. After 2s - orb returns to idle
8. Bubble stays visible
9. Send another message - previous bubble clears
10. Test error: Stop gateway, send message - see error bubble
</verification>

<success_criteria>
- Speech bubble appears above orb with response text
- Bubble uses CSS clip-path, no JavaScript positioning
- Orb transitions: idle → processing → responding → idle
- Bubble persists until next message sent
- Window resizes or bubble fits without clipping
- Error responses show in bubble
- Full chain works: renderer → IPC → gateway → FastAPI → back
</success_criteria>

<output>
After completion, create `.planning/phases/12-hotkey-text-chat/12-04-SUMMARY.md`
</output>