---
phase: 12-hotkey-text-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/hotkey.ts
  - apps/desktop/src/main/tray.ts
  - apps/desktop/src/main/ipc/hotkey.ts
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/main/__tests__/hotkey.test.ts
autonomous: true
requirements: [ACTV-01]

must_haves:
  truths:
    - "Ctrl+Shift+J pressed globally triggers widget show/hide"
    - "Hotkey configuration persists between sessions"
    - "Tray submenu shows available hotkey options"
    - "Selected hotkey has radio button checked"
    - "Hotkey failure doesn't crash the app"
  artifacts:
    - path: "apps/desktop/src/main/hotkey.ts"
      provides: "Global shortcut registration module"
      min_lines: 50
      exports: ["registerHotkey", "unregisterAll", "changeHotkey"]
    - path: "apps/desktop/src/main/tray.ts"
      provides: "Tray menu with hotkey submenu"
      contains: "Configure Hotkey"
    - path: "apps/desktop/src/main/__tests__/hotkey.test.ts"
      provides: "Unit tests for hotkey module"
      min_lines: 40
  key_links:
    - from: "tray.ts"
      to: "hotkey.ts"
      via: "changeHotkey() call on menu click"
      pattern: "changeHotkey\\(.*accelerator"
    - from: "hotkey.ts"
      to: "electron-store"
      via: "persistence of selected hotkey"
      pattern: "store\\.get\\('hotkey"
---

<objective>
Implement global hotkey activation system with configurable shortcuts via tray submenu

Purpose: Enable users to show/hide widget from anywhere with Ctrl+Shift+J (or alternatives)
Output: Working hotkey module with tray configuration UI and persistence
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-hotkey-text-chat/12-CONTEXT.md
@.planning/phases/12-hotkey-text-chat/12-RESEARCH.md

# Existing patterns to follow
@apps/desktop/src/main/position.ts  # electron-store usage pattern
@apps/desktop/src/main/tray.ts      # Current tray implementation
@apps/desktop/src/shared/ipc-types.ts # IPC type patterns
</context>

<interfaces>
<!-- Key types and contracts the executor needs from existing code -->

From apps/desktop/src/main/position.ts:
```typescript
import Store from 'electron-store';

interface PositionConfig {
  x: number;
  y: number;
}

const store = new Store<{ position?: PositionConfig }>();

export function savePosition(x: number, y: number): void {
  store.set('position', { x, y });
}

export function getPosition(): PositionConfig | null {
  return store.get('position') || null;
}
```

From apps/desktop/src/main/tray.ts:
```typescript
export function createTray(mainWindow: BrowserWindow): void;
export function destroyTray(): void;
```

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export interface IpcResult<T = void> {
  success: boolean;
  data?: T;
  error?: string;
}

export const IPC_CHANNELS = {
  CHAT_SEND_TEXT: 'chat:send-text',
  // Add new channels here
} as const;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hotkey module with persistence</name>
  <files>apps/desktop/src/main/hotkey.ts, apps/desktop/src/main/__tests__/hotkey.test.ts</files>
  <behavior>
    - Test 1: registerHotkey() returns true when successful
    - Test 2: registerHotkey() returns false when hotkey is taken
    - Test 3: changeHotkey() unregisters old and registers new
    - Test 4: Store persists hotkey selection across restarts
  </behavior>
  <action>
    Create hotkey module that manages global shortcuts with electron-store persistence.

    Implementation per D-05 to D-10:
    - Default hotkey: 'CmdOrCtrl+Shift+J' (D-05)
    - Store hotkey preference using electron-store following position.ts pattern (D-09)
    - globalShortcut.register() returns boolean - check and handle failure (D-10)
    - Show/hide mainWindow on hotkey press
    - Export registerHotkey(mainWindow), unregisterAll(), changeHotkey(accelerator, mainWindow)
    - Use Store<{ hotkey?: { accelerator: string } }> interface

    Avoid: Don't throw on registration failure - return boolean (Research Pattern 1)
    Reference research: apps/desktop/src/main/hotkey.ts Pattern 1 line 125-162
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/main/__tests__/hotkey.test.ts</automated>
  </verify>
  <done>Hotkey module created with tests passing, persistence working</done>
</task>

<task type="auto">
  <name>Task 2: Add hotkey submenu to tray</name>
  <files>apps/desktop/src/main/tray.ts</files>
  <action>
    Extend existing tray menu with "Configure Hotkey" submenu per D-06, D-07, D-08.

    Implementation:
    - Add submenu after "Hide" item: "Configure Hotkey" with arrow indicator
    - Include 4 radio MenuItem options per research line 173-178:
      * Ctrl+Shift+J (default, checked based on store)
      * Ctrl+Alt+J
      * Ctrl+Shift+Space
      * Ctrl+` (backtick)
    - Each MenuItem type='radio', checked property based on current store value
    - On click: Call changeHotkey() from hotkey module (D-08 - immediate change)
    - Radio group auto-manages selection state (Research Pattern 2)

    Reference: Research line 169-212 for tray submenu pattern
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/main/__tests__/tray.test.ts</automated>
  </verify>
  <done>Tray menu shows hotkey submenu with radio options, selection persists</done>
</task>

<task type="auto">
  <name>Task 3: Create hotkey IPC handler for renderer status</name>
  <files>apps/desktop/src/main/ipc/hotkey.ts, apps/desktop/src/shared/ipc-types.ts</files>
  <action>
    Create IPC handler so renderer can query current hotkey status.

    In shared/ipc-types.ts:
    - Add HOTKEY_GET_STATUS to IPC_CHANNELS
    - Add HotkeyStatus interface: { accelerator: string, registered: boolean }
    - Add GetHotkeyStatusResponse type using IpcResult pattern

    In main/ipc/hotkey.ts:
    - Create setupHotkeyHandlers() following chat.ts pattern
    - Handle HOTKEY_GET_STATUS: Return current accelerator and registration status
    - Register in main/ipc/index.ts setupIpcHandlers()

    This enables future UI to show current hotkey and registration state.
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/main/__tests__/ipc-hotkey.test.ts</automated>
  </verify>
  <done>IPC handler returns hotkey status, integrated in handler registry</done>
</task>

</tasks>

<verification>
1. Start app: `pnpm --filter desktop dev`
2. Press Ctrl+Shift+J anywhere - widget shows/hides
3. Right-click tray icon - see "Configure Hotkey" submenu
4. Select different hotkey - immediately works
5. Restart app - selected hotkey persists
6. Block hotkey with another app - tray Show still works
</verification>

<success_criteria>
- Global hotkey Ctrl+Shift+J toggles widget visibility
- Tray submenu shows 4 hotkey options with radio selection
- Selected hotkey persists across app restarts
- Registration failure handled gracefully with tray fallback
- All tests passing: `pnpm --filter desktop test`
</success_criteria>

<output>
After completion, create `.planning/phases/12-hotkey-text-chat/12-01-SUMMARY.md`
</output>