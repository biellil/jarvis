---
phase: 12-hotkey-text-chat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx
  - apps/desktop/src/renderer/components/ChatInput/ChatInput.test.tsx
  - apps/desktop/src/renderer/components/ChatInput/index.ts
  - apps/desktop/src/renderer/App.tsx
  - apps/desktop/src/renderer/App.css
autonomous: true
requirements: [ACTV-02]

must_haves:
  truths:
    - "Button appears near orb when widget is visible"
    - "Clicking button reveals text input below orb"
    - "Pressing Enter in input field triggers message send"
    - "Input field clears after message sent"
    - "Input field auto-focuses when revealed"
  artifacts:
    - path: "apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx"
      provides: "Text input component with button toggle"
      min_lines: 60
      exports: ["ChatInput"]
    - path: "apps/desktop/src/renderer/components/ChatInput/ChatInput.test.tsx"
      provides: "Unit tests for ChatInput"
      min_lines: 50
    - path: "apps/desktop/src/renderer/App.css"
      provides: "Styling for input and button"
      contains: ".chat-input"
  key_links:
    - from: "ChatInput.tsx"
      to: "window.jarvis.sendText"
      via: "onSubmit handler"
      pattern: "window\\.jarvis\\.sendText"
    - from: "ChatInput.tsx"
      to: "OrbContext"
      via: "state transitions"
      pattern: "useOrbContext"
---

<objective>
Create text input UI component with button toggle for sending messages

Purpose: Provide fallback text input when voice is unavailable (D-03)
Output: Working ChatInput component integrated below orb
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hotkey-text-chat/12-CONTEXT.md
@.planning/phases/12-hotkey-text-chat/12-RESEARCH.md

# Existing components
@apps/desktop/src/renderer/components/Orb/OrbContext.tsx
@apps/desktop/src/renderer/App.tsx
@apps/desktop/src/shared/ipc-types.ts
</context>

<interfaces>
<!-- Key types and contracts from existing code -->

From apps/desktop/src/renderer/components/Orb/OrbContext.tsx:
```typescript
export type OrbState = 'idle' | 'listening' | 'processing' | 'responding';

export function useOrbContext() {
  return { state: OrbState, setState: (newState: OrbState) => void };
}
```

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export interface SendTextData {
  received: string;
}

export type SendTextResponse = IpcResult<SendTextData>;

declare global {
  interface Window {
    jarvis: {
      sendText: (message: string) => Promise<SendTextResponse>;
    }
  }
}
```

From apps/desktop/src/renderer/App.tsx:
```jsx
function App() {
  return (
    <OrbProvider>
      <div className="app-container">
        <Orb />
        {/* Add ChatInput here */}
      </div>
    </OrbProvider>
  );
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChatInput component with toggle button</name>
  <files>apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx, apps/desktop/src/renderer/components/ChatInput/ChatInput.test.tsx, apps/desktop/src/renderer/components/ChatInput/index.ts</files>
  <behavior>
    - Test 1: Button renders with keyboard icon
    - Test 2: Clicking button toggles input visibility
    - Test 3: Input auto-focuses when shown
    - Test 4: Enter key submits form
    - Test 5: Input clears after submit
    - Test 6: Empty input doesn't submit
  </behavior>
  <action>
    Create ChatInput component per D-01, D-02, D-04.

    Component structure:
    - Button with keyboard icon (D-01) - use Unicode ⌨ or SVG icon
    - Click button → toggle showInput state
    - When showInput true → render input field below (D-02)
    - Input uses controlled component pattern (Research Pattern 4)
    - onSubmit: Check trim(), setState('processing'), call window.jarvis.sendText
    - Don't handle response yet (Plan 04 will add bubble)
    - Clear input after send, maintain focus (Research Pitfall 3)
    - Position button discretely near orb (D-03 discretion)

    Style considerations:
    - Button: small, semi-transparent, hover effect
    - Input: dark background matching widget theme, white text
    - Smooth slide-down animation when input appears

    Reference: Research line 266-320 for controlled form pattern
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/renderer/components/ChatInput/ChatInput.test.tsx</automated>
  </verify>
  <done>ChatInput component created with tests passing, toggle working</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ChatInput into App</name>
  <files>apps/desktop/src/renderer/App.tsx, apps/desktop/src/renderer/App.css</files>
  <action>
    Add ChatInput component to App below the Orb.

    In App.tsx:
    - Import ChatInput from './components/ChatInput'
    - Add <ChatInput /> after <Orb /> inside container
    - Ensure OrbProvider wraps both components

    In App.css:
    - Add styles for .chat-input-container (position below orb)
    - .chat-button: position: absolute, bottom: -30px, left: 50%, transform: translateX(-50%)
    - .chat-input: width: 200px, background: rgba(0,0,0,0.8), border: 1px solid cyan
    - Animation: slide-down 0.3s ease when input appears
    - Ensure input doesn't overflow widget bounds (128x128 base)

    Widget should expand vertically when input shown (dynamic height).
  </action>
  <verify>
    <automated>pnpm --filter desktop dev</automated>
  </verify>
  <done>ChatInput integrated in App, positioned below orb, styles applied</done>
</task>

<task type="auto">
  <name>Task 3: Add orb state transitions on send</name>
  <files>apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx</files>
  <action>
    Connect ChatInput to OrbContext for state transitions per ACTV-02.

    Update ChatInput.tsx:
    - Import and use useOrbContext hook
    - On form submit: setState('processing') before sendText call
    - After sendText returns: setState('idle') for now
    - Plan 04 will handle 'responding' state with bubble

    This provides immediate visual feedback when user sends message.

    Reference: Research line 273-276 for state transition pattern
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/renderer/components/ChatInput/ChatInput.test.tsx</automated>
  </verify>
  <done>Orb transitions to processing when message sent</done>
</task>

</tasks>

<verification>
1. Start app: `pnpm --filter desktop dev`
2. See button below orb (keyboard icon)
3. Click button - input slides down
4. Input auto-focuses
5. Type message and press Enter
6. Orb changes to processing state
7. Input clears but stays focused
8. Can send multiple messages
</verification>

<success_criteria>
- Button appears below orb with keyboard icon
- Click toggles input field visibility with animation
- Enter key sends message via window.jarvis.sendText
- Input clears after send but maintains focus
- Orb transitions to processing state on send
- Empty messages don't send
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/12-hotkey-text-chat/12-02-SUMMARY.md`
</output>