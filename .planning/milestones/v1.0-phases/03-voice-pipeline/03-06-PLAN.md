---
phase: 03-voice-pipeline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jarvis/__main__.py
  - tests/test_ptt_tts.py
autonomous: true
gap_closure: true
requirements:
  - CONV-03
  - CONV-04

must_haves:
  truths:
    - "After /ptt push-to-talk recording, JARVIS speaks the response aloud via kokoro TTS"
    - "State message [falando] appears in terminal after /ptt transcription before TTS playback"
  artifacts:
    - path: "src/jarvis/__main__.py"
      provides: "TTS wiring on /ptt path — response captured and forwarded to tts.speak()"
      contains: "response = await session.send(transcript)"
    - path: "tests/test_ptt_tts.py"
      provides: "Regression test confirming /ptt path calls tts.speak()"
      min_lines: 20
  key_links:
    - from: "src/jarvis/__main__.py (/ptt path, ~line 231)"
      to: "tts.speak(response)"
      via: "response = await session.send(transcript) + if tts and response block"
      pattern: "response = await session\\.send\\(transcript\\)"
---

<objective>
Fix the push-to-talk (/ptt) command path so JARVIS speaks responses aloud via kokoro TTS after PTT recordings.

Purpose: The /ptt path at line 231 of __main__.py discards the return value of session.send(transcript), so tts.speak() is never called. All other voice paths (wake word, /voice file, text input) correctly capture the response and call TTS. This is a blocker for CONV-03 (voice responses via kokoro) on the primary interactive voice input method.

Output: Patched __main__.py with TTS wiring on /ptt path + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-voice-pipeline/03-VERIFICATION.md

<interfaces>
<!-- Existing patterns the executor must replicate exactly -->

From src/jarvis/__main__.py — Wake word path (lines 164-167), the CORRECT pattern:
```python
response = await session.send(transcript)
if tts and response:
    console.print("\n[dim][falando]...[/dim]")
    await tts.speak(response)
```

From src/jarvis/__main__.py — /voice file path (lines 275-279), same CORRECT pattern:
```python
response = await session.send(transcript)
# CONV-03: Speak response sentence by sentence (SC2 streaming)
if tts and response:
    console.print("\n[dim][falando]...[/dim]")
    await tts.speak(response)
```

From src/jarvis/__main__.py — BROKEN /ptt path (lines 230-232):
```python
console.print("[bold cyan]JARVIS:[/bold cyan] ", end="")
await session.send(transcript)
continue
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TTS to /ptt path in __main__.py</name>
  <files>src/jarvis/__main__.py</files>
  <read_first>
    - src/jarvis/__main__.py (full file — understand /ptt path at lines 225-232 and reference patterns at lines 164-167 and 275-279)
  </read_first>
  <action>
    In src/jarvis/__main__.py, find the /ptt command path. At line 231, change:

    ```python
    await session.send(transcript)
    ```

    to:

    ```python
    response = await session.send(transcript)
    # CONV-03: Speak response after push-to-talk (SC2 streaming)
    if tts and response:
        console.print("\n[dim][falando]...[/dim]")
        await tts.speak(response)
    ```

    This is identical to the pattern at lines 275-279 (/voice file path) and lines 164-167 (wake word path). Do NOT change anything else in the file.
  </action>
  <verify>
    <automated>grep -n "response = await session.send(transcript)" src/jarvis/__main__.py | wc -l</automated>
    Expected: 3 occurrences (wake word path, /ptt path, /voice path). Before fix there were 2.
  </verify>
  <acceptance_criteria>
    - Line ~231 reads `response = await session.send(transcript)` (not bare `await session.send(transcript)`)
    - Lines immediately after contain `if tts and response:` followed by `console.print("\n[dim][falando]...[/dim]")` and `await tts.speak(response)`
    - `grep -c "response = await session.send(transcript)" src/jarvis/__main__.py` returns 3
    - `grep -c "await tts.speak(response)" src/jarvis/__main__.py` returns 4 (wake word, /ptt, /voice, text-input paths)
    - `PYTHONPATH=src pytest tests/ -x -q` passes all existing tests with no regressions
  </acceptance_criteria>
  <done>The /ptt path captures the session response and calls tts.speak(), matching the pattern used by all other voice paths. All 158+ existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression test for /ptt TTS wiring</name>
  <files>tests/test_ptt_tts.py</files>
  <read_first>
    - src/jarvis/__main__.py (the fixed /ptt path)
    - tests/test_voice_integration.py (existing test patterns for mocking session, tts, mic)
  </read_first>
  <action>
    Create tests/test_ptt_tts.py with a regression test that verifies the /ptt command path calls tts.speak() with the session response.

    The test should:
    1. Mock ChatSession.send to return a known response string (e.g., "Resposta do JARVIS")
    2. Mock KokoroTTS.speak as an AsyncMock
    3. Mock MicCapture.record_until_release to return a temp WAV path
    4. Mock WhisperTranscriber.transcribe to return a known transcript
    5. Simulate the /ptt command flow (the relevant section of main_async)
    6. Assert that tts.speak was called with the session response string

    Follow existing test patterns from tests/test_voice_integration.py for mock setup. Use pytest-asyncio for async tests. Use PYTHONPATH=src convention.

    Include a second test that verifies tts.speak is NOT called when tts_enabled is False (tts object is None).
  </action>
  <verify>
    <automated>PYTHONPATH=src pytest tests/test_ptt_tts.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_ptt_tts.py exists with at least 2 test functions
    - Test 1 asserts tts.speak is called with session response after /ptt
    - Test 2 asserts no crash when tts is None (tts_enabled=False)
    - `PYTHONPATH=src pytest tests/test_ptt_tts.py -x -v` passes
    - `PYTHONPATH=src pytest tests/ -x -q` passes (no regressions)
  </acceptance_criteria>
  <done>Regression test exists proving that the /ptt path calls tts.speak(response). Future changes that break this wiring will be caught.</done>
</task>

</tasks>

<verification>
1. `grep -c "response = await session.send(transcript)" src/jarvis/__main__.py` returns 3 (was 2 before fix)
2. `grep -c "await tts.speak(response)" src/jarvis/__main__.py` returns 4 (was 3 before fix)
3. `PYTHONPATH=src pytest tests/ -x -q` — all tests pass, including new regression test
4. Manual grep for the /ptt block shows the TTS pattern:
   `grep -A4 "# CONV-03: Speak response after push-to-talk" src/jarvis/__main__.py`
</verification>

<success_criteria>
- The /ptt push-to-talk path in __main__.py captures the session.send() return value and calls tts.speak(response) with the [falando] state message
- All four voice response paths (wake word, /ptt, /voice file, text input) now have identical TTS wiring
- A regression test in tests/test_ptt_tts.py guards against future breakage
- All existing tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-pipeline/03-06-SUMMARY.md`
</output>
