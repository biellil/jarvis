---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - .gitignore
  - src/jarvis/__init__.py
  - src/jarvis/config.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_llm_factory.py
  - tests/test_capabilities.py
  - tests/test_session.py
  - tests/test_startup.py
  - tests/test_platform.py
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "pytest tests/ -x -q runs successfully (0 failures, stubs may skip/xfail)"
    - "pip install -e . succeeds without errors"
    - "python -c 'from jarvis.config import settings' succeeds"
    - ".env.example contains all config keys per D-06 through D-09"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project packaging, entry point, pytest config, all dependencies"
      contains: "langchain-core>=1.2.22"
    - path: "src/jarvis/config.py"
      provides: "Pydantic BaseSettings singleton"
      exports: ["Settings", "settings"]
    - path: ".env.example"
      provides: "Config template with all env vars"
      contains: "LLM_PROVIDER"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for all tests"
      contains: "def mock_settings"
  key_links:
    - from: "pyproject.toml"
      to: "src/jarvis/"
      via: "setuptools packages.find where=src"
      pattern: 'where = \["src"\]'
    - from: "src/jarvis/config.py"
      to: ".env"
      via: "pydantic-settings env_file"
      pattern: 'env_file=".env"'
---

<objective>
Create the project scaffold, install all dependencies, define the config layer, and create test stubs for every Phase 1 requirement.

Purpose: Every subsequent plan depends on pyproject.toml existing, dependencies being installed, the Settings class being importable, and test files being present for TDD-style verification.
Output: Installable Python package with config layer and test stubs for CONV-01, LLM-01, LLM-02, ARCH-01, ARCH-03, ARCH-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project scaffold, pyproject.toml, and install dependencies</name>
  <files>pyproject.toml, .gitignore, .env.example, src/jarvis/__init__.py, src/jarvis/__main__.py, src/jarvis/core/__init__.py, src/jarvis/llm/__init__.py, src/jarvis/platform/__init__.py</files>
  <read_first>CLAUDE.md, .planning/phases/01-foundation/01-RESEARCH.md (Pattern 7: pyproject.toml section), .planning/phases/01-foundation/01-CONTEXT.md (D-06 through D-11)</read_first>
  <action>
Create the full project directory structure per D-10:

```
src/
  jarvis/
    __init__.py          # __version__ = "0.1.0"
    __main__.py          # placeholder: from jarvis.core.session import main; main() — just a pass for now
    core/
      __init__.py        # empty
    llm/
      __init__.py        # empty
    platform/
      __init__.py        # empty
tests/
  __init__.py            # empty
```

Create `pyproject.toml` exactly as documented in RESEARCH.md Pattern 7:
- `[build-system]` with setuptools>=68, wheel
- `[project]` name="jarvis", version="0.1.0", requires-python=">=3.10"
- Dependencies list (exact versions from RESEARCH.md):
  - langchain==1.2.14
  - langgraph==1.1.4
  - langchain-openai==1.1.12
  - langchain-anthropic==1.4.0
  - langchain-core>=1.2.22  (per ARCH-03 — lower bound, NOT exact pin)
  - langgraph-checkpoint-sqlite>=3.0.1  (per ARCH-03 — lower bound, NOT exact pin)
  - pydantic==2.12.5
  - pydantic-settings==2.13.1
  - python-dotenv==1.2.2
  - rich==14.3.3
  - loguru==0.7.3
  - httpx==0.28.1
  - packaging
- `[project.optional-dependencies]` dev = ["pytest==9.0.2", "pytest-asyncio==1.3.0"]
- `[project.scripts]` jarvis = "jarvis.__main__:main"
- `[tool.setuptools.packages.find]` where = ["src"]
- `[tool.pytest.ini_options]` addopts = ["--import-mode=importlib"], asyncio_mode = "auto", testpaths = ["tests"]

Create `.env.example` per D-06/D-07/D-08/D-09 (exact content from RESEARCH.md Code Examples section):
```
# Provider: lmstudio | openai | anthropic
LLM_PROVIDER=lmstudio
LLM_MODEL=

# LM Studio (used when LLM_PROVIDER=lmstudio)
LM_STUDIO_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=

# OpenAI (used when LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Anthropic / Claude (used when LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=
```

Update `.gitignore` to include `.env` (per D-08), `__pycache__`, `*.egg-info`, `.pytest_cache`, `dist/`, `build/`.

Run: `pip install -e ".[dev]"` to install all dependencies.
  </action>
  <verify>
    <automated>cd /root/jarvis && pip install -e ".[dev]" 2>&1 | tail -5 && python -c "import jarvis; print(jarvis.__version__)" && pytest --co -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `langchain-core>=1.2.22`
    - pyproject.toml contains `langgraph-checkpoint-sqlite>=3.0.1`
    - pyproject.toml contains `jarvis = "jarvis.__main__:main"`
    - pyproject.toml contains `where = ["src"]`
    - pyproject.toml contains `asyncio_mode = "auto"`
    - .env.example contains `LLM_PROVIDER=lmstudio`
    - .env.example contains `LM_STUDIO_URL=http://localhost:1234/v1`
    - .env.example contains `OPENAI_API_KEY=`
    - .env.example contains `ANTHROPIC_API_KEY=`
    - .gitignore contains `.env`
    - src/jarvis/__init__.py contains `__version__`
    - `pip install -e ".[dev]"` exits 0
    - `python -c "import jarvis"` exits 0
  </acceptance_criteria>
  <done>pyproject.toml installable, all dependencies available, directory structure matches D-10, .env.example has all config keys</done>
</task>

<task type="auto">
  <name>Task 2: Create Settings config class and test stubs for all Phase 1 requirements</name>
  <files>src/jarvis/config.py, tests/__init__.py, tests/conftest.py, tests/test_llm_factory.py, tests/test_capabilities.py, tests/test_session.py, tests/test_startup.py, tests/test_platform.py</files>
  <read_first>CLAUDE.md, .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: pydantic-settings, Validation Architecture section), .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map, Wave 0 Requirements)</read_first>
  <action>
Create `src/jarvis/config.py` exactly as documented in RESEARCH.md Pattern 1:

```python
from pydantic import Field
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )

    # Provider selection (per D-07)
    llm_provider: str = Field(default="lmstudio", pattern="^(lmstudio|openai|anthropic)$")
    llm_model: str = Field(default="")

    # LM Studio (per D-09)
    lm_studio_url: str = Field(default="http://localhost:1234/v1")
    lm_studio_model: str = Field(default="")

    # Cloud providers (per D-08)
    openai_api_key: str = Field(default="")
    anthropic_api_key: str = Field(default="")


settings = Settings()
```

Create `tests/conftest.py` with shared fixtures:
- `mock_settings` fixture: returns a `Settings` instance with `llm_provider="lmstudio"`, `lm_studio_url="http://localhost:1234/v1"`, `lm_studio_model="test-model"`. Use `model_construct()` to bypass validation.
- `mock_env` fixture: uses `monkeypatch` to set `LLM_PROVIDER`, `LM_STUDIO_URL`, `LM_STUDIO_MODEL` env vars.

Create test stub files (each with `pytest.mark.xfail` on tests so they are collected but don't fail the suite):

**tests/test_llm_factory.py** (covers LLM-01):
- `test_create_llm_lmstudio()` — xfail: factory returns ChatOpenAI when provider=lmstudio
- `test_create_llm_openai()` — xfail: factory returns ChatOpenAI when provider=openai
- `test_create_llm_anthropic()` — xfail: factory returns ChatAnthropic when provider=anthropic
- `test_provider_switch()` — xfail: changing LLM_PROVIDER env var returns different class
- `test_unknown_provider_raises()` — xfail: unknown provider raises ValueError

**tests/test_capabilities.py** (covers LLM-02):
- `test_vision_detection()` — xfail: model name with "vision" sets vision=True
- `test_tool_detection()` — xfail: model name with "instruct" sets tool_calling=True
- `test_context_window_detection()` — xfail: model name with "128k" sets context_window=131072
- `test_no_capabilities()` — xfail: plain model name returns all False/None

**tests/test_session.py** (covers CONV-01):
- `test_send_message()` — xfail: session.send() appends HumanMessage and AIMessage to history
- `test_history_accumulates()` — xfail: multiple sends grow history correctly
- `test_system_prompt_present()` — xfail: history starts with SystemMessage

**tests/test_startup.py** (covers ARCH-03, ARCH-04):
- `test_version_pins()` — xfail: validate_versions() passes when correct versions installed
- `test_lm_studio_unreachable()` — xfail: validate_lm_studio_reachable() exits 1 on bad URL
- `test_missing_package()` — xfail: validate_versions() exits 1 when package missing

**tests/test_platform.py** (covers ARCH-01):
- `test_get_platform_returns_correct_class()` — xfail: get_platform() returns LinuxPlatform on Linux
- `test_platform_has_get_os_name()` — xfail: platform instance has get_os_name method

Each test stub should have a clear docstring describing the expected behavior and a `pytest.mark.xfail(reason="Not implemented yet")` decorator. The test body should contain the actual test logic (imports, assertions) so that when the implementation lands, removing xfail makes the test green.
  </action>
  <verify>
    <automated>cd /root/jarvis && python -c "from jarvis.config import settings; print(f'provider={settings.llm_provider}')" && pytest tests/ --co -q 2>&1 | tail -5 && pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/jarvis/config.py contains `class Settings(BaseSettings)`
    - src/jarvis/config.py contains `from pydantic_settings import BaseSettings`
    - src/jarvis/config.py contains `llm_provider: str = Field(default="lmstudio"`
    - src/jarvis/config.py contains `settings = Settings()`
    - tests/conftest.py contains `def mock_settings`
    - tests/test_llm_factory.py contains `test_create_llm_lmstudio`
    - tests/test_llm_factory.py contains `test_provider_switch`
    - tests/test_capabilities.py contains `test_vision_detection`
    - tests/test_session.py contains `test_send_message`
    - tests/test_startup.py contains `test_version_pins`
    - tests/test_startup.py contains `test_lm_studio_unreachable`
    - tests/test_platform.py contains `test_get_platform_returns_correct_class`
    - `pytest tests/ --co -q` collects at least 15 tests
    - `pytest tests/ -x -q` exits 0 (xfail tests do not count as failures)
  </acceptance_criteria>
  <done>Settings class importable with correct defaults, all test stubs collected by pytest, xfail tests do not break the suite, conftest provides mock_settings fixture</done>
</task>

</tasks>

<verification>
- `pip install -e ".[dev]"` exits 0
- `python -c "from jarvis.config import settings; print(settings.llm_provider)"` prints "lmstudio"
- `pytest tests/ --co -q` collects 15+ tests
- `pytest tests/ -x -q` exits 0 (all xfail, no errors)
- `cat .env.example` shows all config keys per D-06 through D-09
- `grep "langchain-core>=1.2.22" pyproject.toml` matches (ARCH-03)
- `grep "langgraph-checkpoint-sqlite>=3.0.1" pyproject.toml` matches (ARCH-03)
</verification>

<success_criteria>
- Installable Python package (`pip install -e .` works)
- Settings singleton importable with correct defaults
- All Phase 1 test stubs exist and are collected by pytest
- pyproject.toml has ARCH-03 version pins as lower bounds
- .env.example documents all config keys
- Directory structure matches D-10
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
