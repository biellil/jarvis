---
phase: 33-cross-platform-support
plan: 02
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - apps/desktop/src/main/index.ts
  - apps/desktop/electron-builder.yml
  - README.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - PLAT-04
  - PLAT-05
  - PLAT-06

must_haves:
  truths:
    - "JARVIS hides from macOS Dock on startup — app.dock.hide() called conditionally on darwin"
    - "electron-builder.yml bundles whisper prebuilds for darwin-arm64, darwin-x64, linux-x64, linux-x64-cuda, linux-x64-vulkan"
    - "New prebuild packages installed as devDependencies in apps/desktop/package.json"
    - "README documents Linux X11 compositor requirement for transparency"
    - "All index.platform.test.ts tests pass GREEN after index.ts change"
  artifacts:
    - path: "apps/desktop/src/main/index.ts"
      provides: "macOS dock-hide conditional in app.whenReady()"
      contains: "app.dock.hide()"
    - path: "apps/desktop/electron-builder.yml"
      provides: "extraResources entries for mac and linux whisper prebuilds"
      contains: "@fugood/node-whisper-darwin-arm64"
    - path: "README.md"
      provides: "Linux X11 compositor requirement documentation"
      contains: "compositor"
  key_links:
    - from: "apps/desktop/src/main/index.ts"
      to: "app.dock"
      via: "conditional process.platform === 'darwin' check"
      pattern: "process\\.platform === 'darwin'.*app\\.dock\\.hide"
    - from: "apps/desktop/electron-builder.yml"
      to: "node_modules/@fugood/node-whisper-darwin-arm64"
      via: "extraResources from path"
      pattern: "node-whisper-darwin-arm64"
---

<objective>
Implement the two code changes that make JARVIS run correctly on macOS and Linux:
1. Add `app.dock.hide()` on macOS startup so JARVIS lives exclusively in the menu bar (D-01)
2. Bundle macOS and Linux whisper.node prebuilds in electron-builder.yml (D-04, D-06)
3. Install prebuild packages as devDependencies
4. Document Linux X11 compositor requirement in README (D-08)

Purpose: After this plan, a developer can build and run JARVIS on macOS or Linux with the correct behavior — no Dock icon on macOS, correct whisper binary bundled for each platform.

Output: Modified index.ts (1 conditional block), modified electron-builder.yml (5 new extraResources entries), modified README.md (Linux compositor note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cross-platform-support/33-CONTEXT.md
@.planning/phases/33-cross-platform-support/33-RESEARCH.md
@.planning/phases/33-cross-platform-support/33-01-SUMMARY.md
</context>

<interfaces>
<!-- Key interfaces executor needs — extracted from codebase -->

From apps/desktop/src/main/index.ts (current app.whenReady block structure):
```typescript
app.whenReady().then(async () => {
  // permission handler setup...
  
  // fail-fast config check...
  let config;
  try { config = loadBackendConfig(); } catch { ... }

  // Phase 29 GPU detection...
  const useWhisperCpp = process.env['USE_WHISPER_CPP'] === 'true';
  if (useWhisperCpp) {
    if (app.isPackaged) {
      // Module.globalPaths setup...
    }
    await initializeGpuDetection();
  }
  
  // ... VRAM detection, TTS provider, backendClient, setupIpcHandlers ...
  
  createWindow();
  // ... IPC handlers ...
  createTray(mainWindow!);
  registerHotkey(mainWindow!);
  registerPttHotkey(mainWindow!);

  app.on('activate', () => {
    if (BrowserWindow.getAllWindows().length === 0) {
      createWindow();
    }
  });
});
```

Correct insertion point for dock.hide(): BEFORE `createWindow()`, AFTER the `loadBackendConfig()` block.
Specifically: add it right after the permission handlers, before any window creation.

From apps/desktop/electron-builder.yml (current extraResources section, end of file):
```yaml
extraResources:
  - from: resources/wakeword-models
    to: wakeword-models
    filter:
      - "*.onnx"
      - "LICENSE*"
  - from: "../../node_modules/@fugood/whisper.node"
    to: "node_modules/@fugood/whisper.node"
    filter:
      - "lib/**"
      - "package.json"
  - from: "../../node_modules/@fugood/node-whisper-win32-x64"
    to: "node_modules/@fugood/node-whisper-win32-x64"
    filter:
      - "index.node"
      - "package.json"
  - from: "../../node_modules/@fugood/node-whisper-win32-x64-cuda"
    ...
  - from: "../../node_modules/@fugood/node-whisper-win32-x64-vulkan"
    ...
  - from: "resources/models/whisper"
    to: "models/whisper"
    filter:
      - "ggml-tiny.bin"
      - "ggml-base.bin"
      - "ggml-large-v3.bin"

asar: true
win:
  target: nsis
mac:
  target: dmg
  extendInfo:
    NSMicrophoneUsageDescription: "JARVIS precisa do microfone..."
linux:
  target: AppImage
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add app.dock.hide() to index.ts (PLAT-01, D-01)</name>
  <files>apps/desktop/src/main/index.ts</files>
  <read_first>
    - apps/desktop/src/main/index.ts (MANDATORY — read full file before editing)
    - apps/desktop/src/main/__tests__/index.platform.test.ts (the RED tests that must turn GREEN)
    - .planning/phases/33-cross-platform-support/33-RESEARCH.md (Pattern 1 — macOS Dock Hiding)
  </read_first>
  <behavior>
    - Test: index.platform.test.ts `"contains darwin platform check for dock.hide()"` → GREEN after this task
    - Test: index.platform.test.ts `"calls app.dock.hide() on darwin"` → GREEN after this task
    - Test: existing tests must remain GREEN (no regressions)
  </behavior>
  <action>
    Read `apps/desktop/src/main/index.ts` in full.

    In the `app.whenReady().then(async () => {` block, add the following code block IMMEDIATELY AFTER the permission handler registration (after the `console.log('[permission] handlers registered...')` line, before the `let config;` block):

    ```typescript
    // Phase 33 (PLAT-01, D-01): Hide from macOS Dock — JARVIS lives in menu bar only.
    // skipTaskbar:true is a no-op on macOS; app.dock.hide() is the correct equivalent.
    // The app.on('activate') handler below still works — recreates window on Cmd+Tab.
    if (process.platform === 'darwin') {
      app.dock.hide();
      console.log('[macOS] Dock hidden — app lives in menu bar only');
    }
    ```

    That is the ONLY change to index.ts. Do not modify any other lines. Do not change the `app.on('activate')` handler (leave it intact — D-03 says preserve it).
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test src/main/__tests__/index.platform.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    index.ts contains the darwin conditional block with app.dock.hide().
    All 5 tests in index.platform.test.ts pass GREEN.
    pnpm --filter @jarvis/desktop test exits 0.
  </done>
  <acceptance_criteria>
    - apps/desktop/src/main/index.ts contains `if (process.platform === 'darwin') {`
    - apps/desktop/src/main/index.ts contains `app.dock.hide()`
    - The dock.hide() block appears BEFORE `createWindow()` in the file
    - `pnpm --filter @jarvis/desktop test src/main/__tests__/index.platform.test.ts` exits 0 (all 5 tests GREEN)
    - `pnpm --filter @jarvis/desktop test` exits 0 (no regressions)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bundle mac/linux whisper prebuilds in electron-builder.yml (PLAT-01–06, D-04)</name>
  <files>
    apps/desktop/electron-builder.yml
    apps/desktop/package.json
  </files>
  <read_first>
    - apps/desktop/electron-builder.yml (MANDATORY — read full file before editing)
    - apps/desktop/package.json (to check existing devDependencies before adding)
    - .planning/phases/33-cross-platform-support/33-RESEARCH.md (Pattern 3 — electron-builder Multi-Platform Packaging, Standard Stack table)
  </read_first>
  <action>
    **Step 1: Install prebuild packages as devDependencies**

    Run from the monorepo root:
    ```bash
    pnpm --filter @jarvis/desktop add -D \
      @fugood/node-whisper-darwin-arm64@1.0.18 \
      @fugood/node-whisper-darwin-x64@1.0.18 \
      @fugood/node-whisper-linux-x64@1.0.18 \
      @fugood/node-whisper-linux-x64-cuda@1.0.18 \
      @fugood/node-whisper-linux-x64-vulkan@1.0.18
    ```

    Note: If pnpm install fails for any of these (package not found on registry), skip that variant and continue with the ones that succeed. Log which ones were skipped.

    **Step 2: Verify packages are present in node_modules**

    After install, run:
    ```bash
    ls ../../node_modules/@fugood/ | grep node-whisper
    ```
    (from apps/desktop directory, or adjust path). Only add extraResources entries for packages that were successfully installed.

    **Step 3: Add extraResources entries to electron-builder.yml**

    Read the current electron-builder.yml. After the last win32 extraResources entry (after `@fugood/node-whisper-win32-x64-vulkan`), add the following entries — only for packages confirmed present in node_modules:

    ```yaml
      # Phase 33 (D-04, D-06): macOS prebuilds — Apple Silicon (Metal GPU)
      - from: "../../node_modules/@fugood/node-whisper-darwin-arm64"
        to: "node_modules/@fugood/node-whisper-darwin-arm64"
        filter:
          - "index.node"
          - "package.json"
      # Phase 33 (D-04, D-06): macOS prebuilds — Intel Mac (CPU/Metal GPU)
      - from: "../../node_modules/@fugood/node-whisper-darwin-x64"
        to: "node_modules/@fugood/node-whisper-darwin-x64"
        filter:
          - "index.node"
          - "package.json"
      # Phase 33 (D-04, D-06): Linux prebuilds — x64 CPU baseline
      - from: "../../node_modules/@fugood/node-whisper-linux-x64"
        to: "node_modules/@fugood/node-whisper-linux-x64"
        filter:
          - "index.node"
          - "package.json"
      # Phase 33 (D-04, D-06): Linux prebuilds — x64 CUDA GPU
      - from: "../../node_modules/@fugood/node-whisper-linux-x64-cuda"
        to: "node_modules/@fugood/node-whisper-linux-x64-cuda"
        filter:
          - "index.node"
          - "package.json"
      # Phase 33 (D-04, D-06): Linux prebuilds — x64 Vulkan GPU
      - from: "../../node_modules/@fugood/node-whisper-linux-x64-vulkan"
        to: "node_modules/@fugood/node-whisper-linux-x64-vulkan"
        filter:
          - "index.node"
          - "package.json"
    ```

    Add these BEFORE the `- from: "resources/models/whisper"` entry (keep whisper models last).

    **Step 4: Verify YAML is syntactically valid**

    ```bash
    node -e "const yaml = require('js-yaml'); yaml.load(require('fs').readFileSync('apps/desktop/electron-builder.yml', 'utf-8')); console.log('YAML valid');"
    ```

    Or simply verify the file structure looks correct by reading it back.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('apps/desktop/electron-builder.yml','utf-8'); ['darwin-arm64','darwin-x64','linux-x64'].forEach(p=>{ if(!content.includes(p)) throw new Error('Missing: '+p); }); console.log('OK: all platform entries present');" 2>&1</automated>
  </verify>
  <done>
    electron-builder.yml contains extraResources entries for darwin-arm64, darwin-x64, linux-x64 (minimum).
    CUDA/Vulkan linux variants also present if packages were available on npm.
    apps/desktop/package.json devDependencies lists the newly installed prebuild packages.
  </done>
  <acceptance_criteria>
    - apps/desktop/electron-builder.yml contains `@fugood/node-whisper-darwin-arm64`
    - apps/desktop/electron-builder.yml contains `@fugood/node-whisper-darwin-x64`
    - apps/desktop/electron-builder.yml contains `@fugood/node-whisper-linux-x64`
    - Each new entry has `filter: ["index.node", "package.json"]`
    - Each new entry has the correct `to:` path matching `node_modules/@fugood/node-whisper-{variant}`
    - apps/desktop/package.json devDependencies contains at least `@fugood/node-whisper-darwin-arm64`
    - `pnpm --filter @jarvis/desktop test` exits 0 (no test regressions from package.json change)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Document Linux X11 compositor requirement in README (D-08)</name>
  <files>README.md</files>
  <read_first>
    - README.md (MANDATORY — read full file to find the correct insertion point)
    - .planning/phases/33-cross-platform-support/33-RESEARCH.md (Pitfall 2 — Linux Transparency Failing Without Compositor, exact text to use)
    - .planning/phases/33-cross-platform-support/33-CONTEXT.md (D-08 — accept as limitation, document in README, no code fallback)
  </read_first>
  <action>
    Read README.md in full. Find the appropriate section — likely an "Installation", "Requirements", "Platform Support", or "Getting Started" section.

    Add a new section or subsection for Linux requirements. The exact content to add:

    ```markdown
    ### Linux (X11)

    JARVIS targets X11 on Linux. A compositing window manager is required for the
    transparent orb window to render correctly. Without a compositor, the orb
    appears as a black rectangle.

    **Enable compositor (if not already running):**
    - GNOME, KDE, Cinnamon: compositor enabled by default
    - Minimal X11 setups: install and run Picom:
      ```bash
      sudo apt install picom
      picom -b
      ```
    - Then launch JARVIS normally.

    > Wayland support is planned for v2 (PLAT-08).
    ```

    If README.md doesn't exist yet, create it with a minimal structure.

    If a Linux section already exists in README.md, append these compositor notes to it rather than creating a duplicate section.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const content=fs.readFileSync('README.md','utf-8'); if(!content.includes('compositor')) throw new Error('Missing compositor note'); if(!content.includes('picom')||!content.includes('Picom')) throw new Error('Missing Picom instructions'); console.log('OK: Linux compositor note present');" 2>&1</automated>
  </verify>
  <done>
    README.md contains Linux X11 compositor documentation with Picom instructions.
  </done>
  <acceptance_criteria>
    - README.md contains the word "compositor"
    - README.md contains "Picom" or "picom" (the compositor tool)
    - README.md contains a Wayland deferred note (PLAT-08 or v2)
    - README.md contains "X11" in the Linux section
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:

```bash
pnpm --filter @jarvis/desktop test
```

Confirm that:
1. index.platform.test.ts — all 5 tests GREEN
2. tray.platform.test.ts — all tests GREEN
3. All pre-existing tests still GREEN

Check electron-builder.yml manually:
- Open file, verify darwin and linux extraResources entries are present with correct indentation
- Verify existing win32 entries are untouched
</verification>

<success_criteria>
- apps/desktop/src/main/index.ts contains `app.dock.hide()` guarded by `process.platform === 'darwin'`
- apps/desktop/electron-builder.yml contains extraResources entries for darwin-arm64, darwin-x64, linux-x64 (minimum 3 new entries)
- README.md documents Linux X11 compositor requirement with Picom instructions
- pnpm --filter @jarvis/desktop test exits 0
- index.platform.test.ts is fully GREEN (was RED in Wave 0)
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-platform-support/33-02-SUMMARY.md`
</output>
