---
phase: 33-cross-platform-support
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src/main/__tests__/index.platform.test.ts
  - apps/desktop/src/main/__tests__/tray.platform.test.ts
autonomous: true
requirements:
  - PLAT-01
  - PLAT-03
  - PLAT-04
  - PLAT-06

must_haves:
  truths:
    - "index.platform.test.ts exists and tests app.dock.hide() behavior on darwin and window-all-closed on win32/linux"
    - "tray.platform.test.ts exists and tests that createTray() uses the correct icon path pattern"
    - "pnpm --filter @jarvis/desktop test exits 0 after Wave 0 files are created"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/index.platform.test.ts"
      provides: "Platform branch tests for darwin dock-hide and window-all-closed behavior"
      exports: []
    - path: "apps/desktop/src/main/__tests__/tray.platform.test.ts"
      provides: "Source-level assertions for tray icon path and Tray construction"
      exports: []
  key_links:
    - from: "apps/desktop/src/main/__tests__/index.platform.test.ts"
      to: "apps/desktop/src/main/index.ts"
      via: "fs.readFileSync source-level assertion (same pattern as existing tray.test.ts)"
      pattern: "readFileSync.*index\\.ts"
---

<objective>
Create Wave 0 test scaffolds required by the VALIDATION.md Nyquist contract.

Purpose: VALIDATION.md marks `index.platform.test.ts` and `tray.platform.test.ts` as Wave 0 gaps — execution cannot start without them. These tests use source-level assertions (same pattern as `tray.test.ts`) to verify the macOS dock-hide conditional and cross-platform tray construction are correctly present in the source files.

Output: Two new test files that pass immediately (since they check properties already present or to be added in Wave 1). The tests for `app.dock.hide()` will be written as "source contains this pattern" checks — they will initially assert the pattern IS present once Wave 1 adds it, but scaffolded now so Wave 1 executor can run them as the RED signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cross-platform-support/33-CONTEXT.md
@.planning/phases/33-cross-platform-support/33-VALIDATION.md
</context>

<interfaces>
<!-- Pattern from existing tray.test.ts — executor must replicate this pattern -->

From apps/desktop/src/main/__tests__/tray.test.ts (source-level assertion pattern):
```typescript
import fs from 'node:fs';
import path from 'node:path';

const traySource = fs.readFileSync(
  path.join(__dirname, '../tray.ts'),
  'utf-8'
);

describe('...', () => {
  it('...', () => {
    expect(traySource).toContain('some string');
    expect(traySource).toMatch(/some regex/);
  });
});
```

From apps/desktop/src/main/index.ts (current state — NO dock.hide() yet):
- Line 21: `import { app, BrowserWindow, dialog, ipcMain, screen, session } from 'electron';`
- Line 269: `app.on('window-all-closed', () => {`
- Line 271: `if (process.platform !== 'darwin') {`
- Line 272: `app.quit();`
- Line 273: `}`
- Line 274: `});`
- No `app.dock.hide()` present yet — Wave 1 will add it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create index.platform.test.ts</name>
  <files>apps/desktop/src/main/__tests__/index.platform.test.ts</files>
  <read_first>
    - apps/desktop/src/main/__tests__/tray.test.ts (source-level assertion pattern to replicate)
    - apps/desktop/src/main/index.ts (the source file being tested — read current state)
    - apps/desktop/src/main/__tests__/index.test.ts (existing index test for import/describe style reference)
    - .planning/phases/33-cross-platform-support/33-VALIDATION.md (what this test must cover)
  </read_first>
  <behavior>
    - Test 1 (PLAT-01/PLAT-04): Source contains `if (process.platform === 'darwin')` — marks expected Wave 1 addition
    - Test 2 (PLAT-01/PLAT-04): Source contains `app.dock.hide()` — marks expected Wave 1 addition
    - Test 3 (PLAT-01/PLAT-04): Source contains `app.on('window-all-closed'` — already present, verifies preservation
    - Test 4 (PLAT-01/PLAT-04): Source contains `if (process.platform !== 'darwin')` inside window-all-closed — already present, verifies preservation
    - Test 5 (PLAT-01/PLAT-04): Source contains `app.on('activate'` — already present, verifies preservation
  </behavior>
  <action>
    Create `apps/desktop/src/main/__tests__/index.platform.test.ts` using source-level assertions (readFileSync pattern from tray.test.ts).

    File must:
    1. Import `fs` and `path` from node:
    2. Read `../index.ts` via `fs.readFileSync(path.join(__dirname, '../index.ts'), 'utf-8')`
    3. Describe block: `'index.ts platform support (Phase 33)'`

    Tests to include:

    **describe 'macOS: Dock hiding (PLAT-01, D-01)':**
    - `"contains darwin platform check for dock.hide()"` → `expect(indexSource).toContain("process.platform === 'darwin'")`
    - `"calls app.dock.hide() on darwin"` → `expect(indexSource).toContain('app.dock.hide()')`

    **describe 'Cross-platform: window lifecycle (PLAT-01, PLAT-04)':**
    - `"contains window-all-closed handler"` → `expect(indexSource).toContain("app.on('window-all-closed'")`
    - `"quits on non-darwin platforms"` → `expect(indexSource).toMatch(/window-all-closed[\s\S]*?platform !== 'darwin'[\s\S]*?app\.quit\(\)/)`
    - `"contains activate handler for macOS Dock click"` → `expect(indexSource).toContain("app.on('activate'")`

    **Note on TDD:** Tests for `app.dock.hide()` will FAIL (RED) when this file is first created because Wave 1 hasn't added it yet. That is correct — Wave 1 will make them GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test src/main/__tests__/index.platform.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    File exists at apps/desktop/src/main/__tests__/index.platform.test.ts.
    Tests for window-all-closed and activate pass (GREEN).
    Tests for app.dock.hide() fail (RED) — correct since Wave 1 hasn't added it yet.
    pnpm --filter @jarvis/desktop test does not crash with syntax errors.
  </done>
  <acceptance_criteria>
    - apps/desktop/src/main/__tests__/index.platform.test.ts exists
    - File contains `fs.readFileSync` reading `../index.ts`
    - File contains `app.dock.hide()` as a test assertion string
    - File contains `process.platform === 'darwin'` as a test assertion string
    - File contains `window-all-closed` as a test assertion string
    - `pnpm --filter @jarvis/desktop test src/main/__tests__/index.platform.test.ts` exits without TypeScript compilation errors (some tests may be RED — that is expected)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tray.platform.test.ts</name>
  <files>apps/desktop/src/main/__tests__/tray.platform.test.ts</files>
  <read_first>
    - apps/desktop/src/main/__tests__/tray.test.ts (existing tray test — extend the same file OR create new, must not duplicate describe blocks)
    - apps/desktop/src/main/tray.ts (the source file being tested — read full content)
    - .planning/phases/33-cross-platform-support/33-VALIDATION.md (what this test must cover for PLAT-03, PLAT-06)
  </read_first>
  <action>
    Create `apps/desktop/src/main/__tests__/tray.platform.test.ts` using the same source-level readFileSync pattern.

    File must:
    1. Import `fs` and `path` from node:
    2. Read `../tray.ts` via `fs.readFileSync(path.join(__dirname, '../tray.ts'), 'utf-8')`
    3. Describe block: `'tray.ts cross-platform assertions (Phase 33, PLAT-03, PLAT-06)'`

    Tests to include:

    **describe 'Tray icon path (PLAT-03, PLAT-06)':**
    - `"uses PNG icon from resources/tray directory"` → `expect(traySource).toContain('resources/tray')`
    - `"icon filename ends in .png"` → `expect(traySource).toMatch(/icon-\d+x\d+\.png/)`
    - `"creates Tray with nativeImage or path string"` → `expect(traySource).toMatch(/new Tray\(/)`

    **describe 'Tray menu items (PLAT-03, PLAT-06)':**
    - `"contains Settings menu item"` — this is a FUTURE item for Phase 34. Write it as a comment: `// TODO Phase 34: Settings menu item will be added here` and skip the assertion.
    - `"contains Quit menu item"` → `expect(traySource).toContain("label: 'Quit'")`
    - `"Quit handler calls app.quit()"` → `expect(traySource).toMatch(/label: 'Quit'[\s\S]*?app\.quit\(\)/)`

    **describe 'Tray module exports (PLAT-03, PLAT-06)':**
    - `"exports createTray function"` → `expect(traySource).toContain('export function createTray(')`
    - `"exports destroyTray function"` → `expect(traySource).toContain('export function destroyTray(')`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test src/main/__tests__/tray.platform.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    File exists. All tests in tray.platform.test.ts pass (GREEN) — tray.ts already has the required patterns.
    `pnpm --filter @jarvis/desktop test` still exits 0 (no regressions).
  </done>
  <acceptance_criteria>
    - apps/desktop/src/main/__tests__/tray.platform.test.ts exists
    - File contains `fs.readFileSync` reading `../tray.ts`
    - File contains assertion for `new Tray(`
    - File contains assertion for `label: 'Quit'`
    - File contains assertions for `export function createTray(` and `export function destroyTray(`
    - `pnpm --filter @jarvis/desktop test src/main/__tests__/tray.platform.test.ts` exits 0 (all GREEN)
    - `pnpm --filter @jarvis/desktop test` exits 0 (no regressions from adding new file)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run the full desktop test suite to confirm no regressions:

```bash
pnpm --filter @jarvis/desktop test
```

Confirm:
- index.platform.test.ts exists and compiles
- tray.platform.test.ts exists and all assertions pass
- Existing tests (tray.test.ts, index.test.ts, position.test.ts etc.) still pass
- index.platform.test.ts has some FAILING tests (for app.dock.hide()) — this is correct RED state for Wave 1
</verification>

<success_criteria>
- apps/desktop/src/main/__tests__/index.platform.test.ts created with 5 test assertions
- apps/desktop/src/main/__tests__/tray.platform.test.ts created with all GREEN tests
- pnpm --filter @jarvis/desktop test runs without crash
- VALIDATION.md Wave 0 gaps are closed (both files now exist)
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-platform-support/33-01-SUMMARY.md`
</output>
