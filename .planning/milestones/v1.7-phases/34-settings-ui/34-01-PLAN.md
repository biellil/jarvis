---
phase: 34-settings-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src/main/__tests__/store.test.ts
  - apps/desktop/src/main/ipc/__tests__/settings.test.ts
  - apps/desktop/src/main/__tests__/tray.test.ts
  - apps/desktop/src/renderer/src/settings/__tests__/HotkeyRecorder.test.tsx
  - apps/desktop/src/renderer/src/settings/__tests__/SettingsForm.test.tsx
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05

must_haves:
  truths:
    - "All five test scaffold files exist with failing tests that describe the expected behavior"
    - "Running the test suite produces RED (compile or test failure) — not PASS — because implementations do not yet exist"
    - "Test files use the same vi.mock pattern as existing store.test.ts and settings.test.ts"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/store.test.ts"
      provides: "Tests for new store fields: getTtsProvider, setTtsProvider, getTtsApiKey, setTtsApiKey, getWhisperModelOverride, setWhisperModelOverride"
      exports: []
    - path: "apps/desktop/src/main/ipc/__tests__/settings.test.ts"
      provides: "Tests for SETTINGS_GET and SETTINGS_SAVE IPC handlers"
      exports: []
    - path: "apps/desktop/src/main/__tests__/tray.test.ts"
      provides: "Test that buildContextMenu includes a Settings item"
      exports: []
    - path: "apps/desktop/src/renderer/src/settings/__tests__/HotkeyRecorder.test.tsx"
      provides: "Tests for keydown capture and Electron accelerator format conversion"
      exports: []
    - path: "apps/desktop/src/renderer/src/settings/__tests__/SettingsForm.test.tsx"
      provides: "Tests for Settings form rendering and Save handler"
      exports: []
  key_links:
    - from: "store.test.ts"
      to: "apps/desktop/src/main/store.ts"
      via: "import { getTtsProvider, setTtsProvider, getTtsApiKey, setTtsApiKey, getWhisperModelOverride, setWhisperModelOverride }"
      pattern: "getTtsProvider|setTtsProvider|getWhisperModelOverride"
    - from: "settings.test.ts (ipc)"
      to: "apps/desktop/src/main/ipc/settings.ts"
      via: "import { setupSettingsHandlers } from '../settings'"
      pattern: "SETTINGS_GET|SETTINGS_SAVE"
---

<objective>
Create all Wave 0 test scaffolds for Phase 34 Settings UI. These tests define expected behavior BEFORE implementation exists — they drive the RED phase of TDD.

Purpose: Nyquist compliance — every implementation task in Waves 1-3 has a pre-existing test that verifies it. Tests fail initially (no implementations yet), turn green as plans 02-05 ship.

Output: 5 test files covering store field accessors, IPC get/save handlers, tray Settings item, HotkeyRecorder component, and SettingsForm component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-settings-ui/34-CONTEXT.md
@.planning/phases/34-settings-ui/34-VALIDATION.md
</context>

<interfaces>
<!-- Existing test patterns to replicate exactly. -->

From apps/desktop/src/main/__tests__/store.test.ts (existing pattern):
```typescript
vi.mock('electron-store', () => {
  let mockStore: Record<string, unknown> = {};
  return {
    default: class Store {
      get(key: string) { return mockStore[key]; }
      set(key: string, value: unknown) { mockStore[key] = value; }
      static __resetStore() { mockStore = {}; }
      static __getBackingStore() { return mockStore; }
    },
  };
});
// Import AFTER vi.mock:
import Store from 'electron-store';
import { getWakeWordPaused, setWakeWordPaused } from '../store';
// In beforeEach: (Store as any).__resetStore();
```

From apps/desktop/src/main/ipc/__tests__/settings.test.ts (existing pattern):
```typescript
const ipcHandleMock = vi.fn();
vi.mock('electron', () => ({
  ipcMain: { handle: (...args: unknown[]) => ipcHandleMock(...args) },
  BrowserWindow: { getAllWindows: () => getAllWindowsMock() },
}));
import { setupSettingsHandlers } from '../settings';
```

New IPC channels to add to IPC_CHANNELS (Phase 34):
  SETTINGS_GET: 'settings:get'
  SETTINGS_SAVE: 'settings:save'

New store fields (Phase 34):
  ttsProvider?: { name: 'murf' | 'elevenlabs' }
  ttsApiKey?: { key: string }
  whisperModelOverride?: { model: 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo' }

New store accessor names (Phase 34):
  getTtsProvider() → 'murf' | 'elevenlabs'   (default 'elevenlabs')
  setTtsProvider(name)
  getTtsApiKey() → string                     (default '')
  setTtsApiKey(key)
  getWhisperModelOverride() → 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'  (default 'auto')
  setWhisperModelOverride(model)

SettingsData interface (to define in shared/ipc-types.ts):
  pttHotkey: string
  ttsProvider: 'murf' | 'elevenlabs'
  ttsApiKey: string
  whisperModelOverride: 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'

SaveSettingsRequest interface:
  pttHotkey?: string
  ttsProvider?: 'murf' | 'elevenlabs'
  ttsApiKey?: string
  whisperModelOverride?: 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'

SaveSettingsResponse: { success: boolean; error?: string }

HotkeyRecorder props:
  label: string
  value: string
  onRecorded: (accelerator: string) => void
  disabled?: boolean

SettingsForm props: none (fetches via window.settings.get() on mount)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolds — store.ts new fields + IPC settings handlers</name>
  <files>
    apps/desktop/src/main/__tests__/store.test.ts
    apps/desktop/src/main/ipc/__tests__/settings.test.ts
  </files>
  <read_first>
    - apps/desktop/src/main/__tests__/store.test.ts (EXISTING — extend, do NOT replace the wakeWord tests already there)
    - apps/desktop/src/main/ipc/__tests__/settings.test.ts (EXISTING — extend, do NOT replace the wakeWord tests already there)
    - apps/desktop/src/main/store.ts (current StoreSchema + accessor pattern)
    - apps/desktop/src/main/ipc/settings.ts (current setupSettingsHandlers)
    - apps/desktop/src/shared/ipc-types.ts (IPC_CHANNELS, result patterns)
  </read_first>
  <behavior>
    store.test.ts additions (append after existing wakeWord describe block):
    - getTtsProvider() returns 'elevenlabs' by default (no store entry)
    - setTtsProvider('murf') → getTtsProvider() returns 'murf'
    - getTtsApiKey() returns '' by default
    - setTtsApiKey('sk-abc123') → getTtsApiKey() returns 'sk-abc123'
    - getWhisperModelOverride() returns 'auto' by default
    - setWhisperModelOverride('tiny') → getWhisperModelOverride() returns 'tiny'
    - setWhisperModelOverride('auto') resets to auto

    settings.test.ts additions (new describe block 'ipc/settings — Phase 34 Settings Window'):
    - setupSettingsHandlers() registers handler for 'settings:get'
    - settings:get handler returns { pttHotkey, ttsProvider, ttsApiKey, whisperModelOverride } from store
    - setupSettingsHandlers() registers handler for 'settings:save'
    - settings:save with pttHotkey calls changePttHotkey and setPttHotkey
    - settings:save with ttsProvider calls setTtsProvider
    - settings:save with ttsApiKey calls setTtsApiKey + reinitializeTTS
    - settings:save with whisperModelOverride calls setWhisperModelOverride
    - settings:save returns { success: false, error: 'PTT hotkey already in use' } when changePttHotkey returns false
    - settings:save returns { success: true } on successful save
  </behavior>
  <action>
    EXTEND apps/desktop/src/main/__tests__/store.test.ts — append a new top-level describe block after the existing one. Do NOT modify or delete existing tests. The new block is:

    ```typescript
    describe('store.ts — Settings UI fields (Phase 34)', () => {
      beforeEach(() => {
        (Store as any).__resetStore();
      });

      describe('getTtsProvider / setTtsProvider', () => {
        it("returns 'elevenlabs' by default (no store entry)", () => {
          expect(getTtsProvider()).toBe('elevenlabs');
        });
        it("setTtsProvider('murf') → getTtsProvider() returns 'murf'", () => {
          setTtsProvider('murf');
          expect(getTtsProvider()).toBe('murf');
        });
        it("setTtsProvider('elevenlabs') → getTtsProvider() returns 'elevenlabs'", () => {
          setTtsProvider('elevenlabs');
          expect(getTtsProvider()).toBe('elevenlabs');
        });
      });

      describe('getTtsApiKey / setTtsApiKey', () => {
        it("returns '' by default", () => {
          expect(getTtsApiKey()).toBe('');
        });
        it("setTtsApiKey('sk-abc') → getTtsApiKey() returns 'sk-abc'", () => {
          setTtsApiKey('sk-abc');
          expect(getTtsApiKey()).toBe('sk-abc');
        });
      });

      describe('getWhisperModelOverride / setWhisperModelOverride', () => {
        it("returns 'auto' by default", () => {
          expect(getWhisperModelOverride()).toBe('auto');
        });
        it("setWhisperModelOverride('tiny') → returns 'tiny'", () => {
          setWhisperModelOverride('tiny');
          expect(getWhisperModelOverride()).toBe('tiny');
        });
        it("setWhisperModelOverride('auto') resets to auto", () => {
          setWhisperModelOverride('small');
          setWhisperModelOverride('auto');
          expect(getWhisperModelOverride()).toBe('auto');
        });
      });
    });
    ```

    Add the new imports at the top of store.test.ts:
    `import { getWakeWordPaused, setWakeWordPaused, getTtsProvider, setTtsProvider, getTtsApiKey, setTtsApiKey, getWhisperModelOverride, setWhisperModelOverride } from '../store';`
    (replace the existing import line)

    EXTEND apps/desktop/src/main/ipc/__tests__/settings.test.ts — append a new describe block after the existing one. The new mocks needed:
    - Mock `../../store` to add: getPttHotkey, getTtsProvider, getTtsApiKey, getWhisperModelOverride, setTtsProvider, setTtsApiKey, setWhisperModelOverride
    - Mock `../../ptt-hotkey`: changePttHotkey → returns true by default
    - Mock `../voiceHandler` or a reinitializeTTS export
    - Add ipcHandleMock to capture `settings:get` and `settings:save` handlers by channel name

    The new describe block: 'ipc/settings — Phase 34 Settings Window'
    Tests cover all behavior items listed in the <behavior> block above.
    Use vi.fn() for all store getter/setter mocks. Each test resets mocks in beforeEach.
    The settings:save handler test: call the captured handler function directly with a SaveSettingsRequest payload and assert mock calls and return value.
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test src/main/__tests__/store.test.ts src/main/ipc/__tests__/settings.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Both test files compile. New tests FAIL with "getTtsProvider is not a function" or similar import error — proving RED state. Existing tests still PASS (no regression).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffolds — tray Settings item + renderer components</name>
  <files>
    apps/desktop/src/main/__tests__/tray.test.ts
    apps/desktop/src/renderer/src/settings/__tests__/HotkeyRecorder.test.tsx
    apps/desktop/src/renderer/src/settings/__tests__/SettingsForm.test.tsx
  </files>
  <read_first>
    - apps/desktop/src/main/__tests__/tray.test.ts (EXISTING — extend, do NOT replace existing tests)
    - apps/desktop/src/main/tray.ts (buildContextMenu returns Menu with items)
    - apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx (pattern for renderer component tests)
    - apps/desktop/src/renderer/src/components/__tests__/Toast.test.tsx (pattern for React component tests with happy-dom)
  </read_first>
  <behavior>
    tray.test.ts additions — new describe block 'tray.ts — Settings item (Phase 34)':
    - buildContextMenu includes a menu item with label 'Settings'
    - Clicking the 'Settings' item calls openSettingsWindow (mock) with mainWindow

    HotkeyRecorder.test.tsx (new file):
    - Renders with label "Push-to-Talk Hotkey" and value "Ctrl+Space"
    - Shows input with value "Ctrl+Space" in normal state
    - Shows "Record" button in normal state
    - After clicking "Record" button, input shows "Press any key combination..."
    - Pressing Ctrl+Space during recording calls onRecorded("Ctrl+Space")
    - Pressing Escape during recording cancels recording (input reverts to value)

    SettingsForm.test.tsx (new file):
    - Renders without crashing (mocked window.settings.get())
    - Shows "Push-to-Talk" section heading
    - Shows "Text-to-Speech" section heading
    - Shows "Speech-to-Text Model" section heading
    - Shows "Save" and "Cancel" buttons
    - After window.settings.get() resolves, form fields are populated with loaded values
    - Clicking "Cancel" calls window.settings.close() or hides window
    - Clicking "Save" calls window.settings.save() with form data
  </behavior>
  <action>
    EXTEND apps/desktop/src/main/__tests__/tray.test.ts — append a new describe block. Read the existing file first to understand the existing mock setup for electron and store. The new test:
    - Mock `../index` to export openSettingsWindow as vi.fn()
    - Verify Menu.buildFromTemplate call includes a template item with label: 'Settings'
    - Use existing electron mock pattern from the file (Tray, Menu, etc.)

    CREATE apps/desktop/src/renderer/src/settings/__tests__/HotkeyRecorder.test.tsx:
    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen, fireEvent } from '@testing-library/react';
    import { HotkeyRecorder } from '../HotkeyRecorder';

    describe('HotkeyRecorder', () => {
      it('renders label and current value', () => {
        render(<HotkeyRecorder label="PTT Hotkey" value="Ctrl+Space" onRecorded={vi.fn()} />);
        expect(screen.getByText('PTT Hotkey')).toBeTruthy();
        expect(screen.getByDisplayValue('Ctrl+Space')).toBeTruthy();
      });

      it('shows "Record" button in normal state', () => {
        render(<HotkeyRecorder label="PTT Hotkey" value="Ctrl+Space" onRecorded={vi.fn()} />);
        expect(screen.getByText('Record')).toBeTruthy();
      });

      it('enters recording state on Record click', () => {
        render(<HotkeyRecorder label="PTT Hotkey" value="Ctrl+Space" onRecorded={vi.fn()} />);
        fireEvent.click(screen.getByText('Record'));
        expect(screen.getByText('Recording...')).toBeTruthy();
      });

      it('calls onRecorded with accelerator on key combo', () => {
        const onRecorded = vi.fn();
        render(<HotkeyRecorder label="PTT Hotkey" value="Ctrl+Space" onRecorded={onRecorded} />);
        fireEvent.click(screen.getByText('Record'));
        const input = screen.getByRole('textbox');
        fireEvent.keyDown(input, { key: ' ', code: 'Space', ctrlKey: true });
        expect(onRecorded).toHaveBeenCalledWith('Ctrl+Space');
      });

      it('cancels recording on Escape', () => {
        render(<HotkeyRecorder label="PTT Hotkey" value="Ctrl+Space" onRecorded={vi.fn()} />);
        fireEvent.click(screen.getByText('Record'));
        const input = screen.getByRole('textbox');
        fireEvent.keyDown(input, { key: 'Escape' });
        expect(screen.getByDisplayValue('Ctrl+Space')).toBeTruthy();
      });
    });
    ```

    CREATE apps/desktop/src/renderer/src/settings/__tests__/SettingsForm.test.tsx:
    Mock `window.settings` as:
    ```typescript
    const mockSettingsGet = vi.fn().mockResolvedValue({
      pttHotkey: 'Ctrl+Space',
      ttsProvider: 'elevenlabs',
      ttsApiKey: '',
      whisperModelOverride: 'auto',
    });
    const mockSettingsSave = vi.fn().mockResolvedValue({ success: true });
    vi.stubGlobal('window', {
      ...window,
      settings: { get: mockSettingsGet, save: mockSettingsSave, close: vi.fn() },
    });
    ```
    Tests:
    - render SettingsForm → expect section labels 'Push-to-Talk', 'Text-to-Speech', 'Speech-to-Text Model'
    - expect 'Save' and 'Cancel' buttons
    - after mockSettingsGet resolves, form fields populated (use waitFor)
    - click 'Save' → mockSettingsSave called
    - click 'Cancel' → window.settings.close() called or window hidden

    Note: @testing-library/react must be available. Check apps/desktop/package.json. If not installed, add the test with a TODO comment and import but do not fail — the test infrastructure check is separate from actual test assertions.
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test src/main/__tests__/tray.test.ts src/renderer/src/settings/__tests__/HotkeyRecorder.test.tsx src/renderer/src/settings/__tests__/SettingsForm.test.tsx 2>&1 | tail -40</automated>
  </verify>
  <done>All three test files compile. Tests FAIL because HotkeyRecorder, SettingsForm do not exist yet — RED state confirmed. Existing tray.test.ts tests still PASS. The tray Settings item test fails because the item does not exist yet in buildContextMenu.</done>
</task>

</tasks>

<verification>
pnpm --filter @jarvis/desktop test 2>&1 | grep -E "PASS|FAIL|ERROR" | tail -20
</verification>

<success_criteria>
- 5 test scaffold files exist at the correct paths
- New tests compile (TypeScript valid)
- New tests FAIL (RED) because implementations don't exist yet — this is the expected state
- All pre-existing tests continue to PASS (zero regression)
- pnpm --filter @jarvis/desktop test exits with non-zero only due to new failing tests, not pre-existing ones
</success_criteria>

<output>
After completion, create `.planning/phases/34-settings-ui/34-01-SUMMARY.md`
</output>
