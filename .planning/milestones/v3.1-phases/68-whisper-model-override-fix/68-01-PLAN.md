---
phase: 68-whisper-model-override-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/voiceInput/whisperModelResolver.ts
  - apps/desktop/src/main/voiceInput/selectWhisperModel.ts
  - apps/desktop/src/main/store.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/shared/ipc-types.ts
autonomous: true
requirements:
  - WBUG-01
  - WBUG-02

must_haves:
  truths:
    - "selectWhisperModel('base', 'tiny') retorna 'tiny' — override do usuário é respeitado"
    - "selectWhisperModel('medium', 'small') retorna 'base' — mapping OPTION_TO_MODEL aplicado"
    - "selectWhisperModel('medium', 'large-v3-turbo') retorna 'large' — mapping OPTION_TO_MODEL aplicado"
    - "getWhisperModelOverride() retorna 'base' quando store salvo tem valor 'auto' (legado)"
    - "index.ts inicializa selectedModel lendo o store diretamente — sem chamar detectVramAndSelectModel"
    - "WhisperModelOption não inclui 'auto' — TypeScript rejeita 'auto' como valor válido"
  artifacts:
    - path: "apps/desktop/src/main/voiceInput/whisperModelResolver.ts"
      provides: "OPTION_TO_MODEL exportado como named export"
      contains: "export const OPTION_TO_MODEL"
    - path: "apps/desktop/src/main/voiceInput/selectWhisperModel.ts"
      provides: "selectWhisperModel usando OPTION_TO_MODEL importado"
      contains: "import.*OPTION_TO_MODEL.*whisperModelResolver"
    - path: "apps/desktop/src/main/store.ts"
      provides: "getWhisperModelOverride normaliza 'auto' legado → 'base'"
      contains: "model === 'auto'"
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "WhisperModelOption sem 'auto'"
      contains: "export type WhisperModelOption = 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'"
    - path: "apps/desktop/src/main/index.ts"
      provides: "startup sem detectVramAndSelectModel"
      contains: "getWhisperModelOverride()"
  key_links:
    - from: "apps/desktop/src/main/voiceInput/selectWhisperModel.ts"
      to: "apps/desktop/src/main/voiceInput/whisperModelResolver.ts"
      via: "import { OPTION_TO_MODEL }"
      pattern: "OPTION_TO_MODEL\\[override\\]"
    - from: "apps/desktop/src/main/index.ts"
      to: "apps/desktop/src/main/store.ts"
      via: "getWhisperModelOverride()"
      pattern: "getWhisperModelOverride\\(\\)"
---

<objective>
Corrigir o bug central do pipeline STT: `selectWhisperModel.ts` usa uma allowlist desatualizada e ignora override do usuário para 'small' e 'large-v3-turbo', sempre caindo no vramModel (geralmente 'medium'). O fix cirúrgico faz a função usar `OPTION_TO_MODEL` (exportado de `whisperModelResolver.ts`), e remove a detecção VRAM do startup em `index.ts`, substituindo por leitura direta do store.

Purpose: Usuário configura 'tiny' em Settings e o pipeline STT carrega 'tiny' — sem fallback silencioso.
Output: 5 arquivos modificados, zero novos arquivos, nenhuma mudança arquitetural.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/68-whisper-model-override-fix/68-CONTEXT.md
</context>

<interfaces>
<!-- Contratos existentes que o executor DEVE usar. Extraídos dos arquivos fonte. -->

De apps/desktop/src/main/voiceInput/whisperModelResolver.ts (estado atual):
```typescript
// PRIVADO hoje — precisa ser exportado (D-02):
const OPTION_TO_MODEL: Partial<Record<WhisperModelOption, WhisperModel>> = {
  tiny: 'tiny',
  base: 'base',
  small: 'base',        // D-12: no URL for small; documented fallback
  medium: 'medium',
  'large-v3-turbo': 'large',  // D-12: same ggml-large-v3.bin
};
// Nota: 'auto' NÃO aparece em OPTION_TO_MODEL — tratado separadamente em resolveWhisperModel.
// Após D-03 remover 'auto' do tipo, o mapa já está completo.

export function resolveWhisperModel(option: WhisperModelOption, vramMb: number): WhisperModel;
```

De apps/desktop/src/main/voiceInput/selectWhisperModel.ts (estado atual — buggy):
```typescript
// Esta função DEVE ser reescrita usando OPTION_TO_MODEL (D-01):
export function selectWhisperModel(vramModel: WhisperModel, override: WhisperModelOption): WhisperModel;
```

De apps/desktop/src/main/store.ts linha 219-225 (estado atual):
```typescript
export function getWhisperModelOverride(): 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo' {
  return store.get('whisperModelOverride')?.model ?? 'auto';
}
export function setWhisperModelOverride(model: 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'): void {
  store.set('whisperModelOverride', { model });
}
```

De apps/desktop/src/main/index.ts linhas 218-238 (estado atual — a remover):
```typescript
let selectedModel: WhisperModel = 'base';
if (useWhisperCpp) {
  try {
    selectedModel = await detectVramAndSelectModel();                   // ← REMOVER
    console.log(`[voice] Model selected by VRAM: ${selectedModel}`);   // ← REMOVER
  } catch (err) {
    console.error('[voice] VRAM detection failed, defaulting to base model:', err);
    selectedModel = 'base';
  }
  const override = getWhisperModelOverride();
  const finalModel = selectWhisperModel(selectedModel, override);      // ← SIMPLIFICAR
  if (finalModel !== selectedModel) {
    console.log(`[voice] Applying user override: ${finalModel} (was: ${selectedModel})`);
  }
  selectedModel = finalModel;
}
```

De apps/desktop/src/shared/ipc-types.ts linha 347 (estado atual):
```typescript
export type WhisperModelOption = 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo';
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Exportar OPTION_TO_MODEL e reescrever selectWhisperModel</name>
  <read_first>
    - apps/desktop/src/main/voiceInput/whisperModelResolver.ts (arquivo completo — exportar OPTION_TO_MODEL)
    - apps/desktop/src/main/voiceInput/selectWhisperModel.ts (arquivo completo — substituir lógica)
    - apps/desktop/src/shared/ipc-types.ts linha 347 (tipo WhisperModelOption — remover 'auto')
  </read_first>
  <files>
    apps/desktop/src/main/voiceInput/whisperModelResolver.ts,
    apps/desktop/src/main/voiceInput/selectWhisperModel.ts,
    apps/desktop/src/shared/ipc-types.ts
  </files>
  <action>
**1. whisperModelResolver.ts — exportar OPTION_TO_MODEL (D-02):**

Alterar `const OPTION_TO_MODEL` para `export const OPTION_TO_MODEL`. Nenhuma outra mudança neste arquivo. A assinatura de `resolveWhisperModel` permanece idêntica. A key 'auto' não precisa ser adicionada ao mapa — o mapa nunca a conteve.

**2. selectWhisperModel.ts — fix cirúrgico (D-01):**

Reescrever o arquivo inteiro. A interface pública `selectWhisperModel(vramModel, override)` mantém a mesma assinatura para não quebrar chamadores. O comportamento muda:

```typescript
/**
 * selectWhisperModel.ts — Pure model selection helper (Phase 68 fix).
 *
 * Bug fix (WBUG-01): usa OPTION_TO_MODEL de whisperModelResolver em vez de allowlist
 * desatualizada. Qualquer override explícito (não-'auto') retorna o modelo mapeado
 * — nunca o vramModel. vramModel mantido na assinatura por compatibilidade mas
 * ignorado quando override está presente.
 *
 * D-01: surgical fix — mínimo risco, mínima mudança de interface.
 * D-03: 'auto' removido do tipo WhisperModelOption; branch mantido defensivamente.
 */
import type { WhisperModel } from './vramDetection.js';
import type { WhisperModelOption } from '../../shared/ipc-types.js';
import { OPTION_TO_MODEL } from './whisperModelResolver.js';

/**
 * selectWhisperModel — aplica override do usuário.
 *
 * @param vramModel - Modelo detectado por VRAM (ignorado quando override explícito)
 * @param override  - Preferência do usuário em Settings
 * @returns         - Modelo final para inferência STT
 */
export function selectWhisperModel(
  vramModel: WhisperModel,
  override: WhisperModelOption,
): WhisperModel {
  // D-03: branch defensivo caso 'auto' legado chegue (store não migrado)
  if ((override as string) === 'auto') {
    return vramModel;
  }
  return OPTION_TO_MODEL[override] ?? vramModel;
}
```

**3. ipc-types.ts — remover 'auto' do tipo (D-03):**

Localizar linha 347:
```typescript
export type WhisperModelOption = 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo';
```
Alterar para:
```typescript
// Phase 68 D-03: 'auto' removed — explicit model choice only; default 'base' via store.ts
export type WhisperModelOption = 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo';
```

Verificar se algum outro uso de `WhisperModelOption` no mesmo arquivo fica inconsistente e ajustar se necessário. `resolveWhisperModel` em `whisperModelResolver.ts` ainda tem branch `if (option === 'auto')` — isso vai causar erro TypeScript pois 'auto' não é mais um valor válido do tipo. Remover esse branch de `resolveWhisperModel` também: substituir por apenas `return OPTION_TO_MODEL[option] ?? selectModelByVram(vramMb);`. A função `selectModelByVram` pode permanecer (usada como fallback defensivo).
  </action>
  <verify>
    <automated>cd /root/jarvis && npx tsc --noEmit -p apps/desktop/tsconfig.json 2>&1 | grep -E "error|whisperModel|selectWhisper|OPTION_TO_MODEL" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "export const OPTION_TO_MODEL" apps/desktop/src/main/voiceInput/whisperModelResolver.ts retorna match
    - grep "import.*OPTION_TO_MODEL.*whisperModelResolver" apps/desktop/src/main/voiceInput/selectWhisperModel.ts retorna match
    - grep "SUPPORTED_MODELS\|warn.*not supported" apps/desktop/src/main/voiceInput/selectWhisperModel.ts retorna vazio
    - grep "WhisperModelOption" apps/desktop/src/shared/ipc-types.ts | grep "'auto'" retorna vazio
    - grep "export type WhisperModelOption = 'tiny'" apps/desktop/src/shared/ipc-types.ts retorna match
    - TypeScript compilation sem erros nos arquivos modificados
  </acceptance_criteria>
  <done>OPTION_TO_MODEL exportado; selectWhisperModel usa o mapa; 'auto' removido do tipo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atualizar store.ts e index.ts (D-04, D-06, D-07)</name>
  <read_first>
    - apps/desktop/src/main/store.ts linhas 218-225 (getWhisperModelOverride / setWhisperModelOverride)
    - apps/desktop/src/main/index.ts linhas 40-44 e 218-238 (imports e call site VRAM)
    - apps/desktop/src/main/voiceInput/selectWhisperModel.ts (assinatura pós-Task 1 — entender como chamar)
  </read_first>
  <files>
    apps/desktop/src/main/store.ts,
    apps/desktop/src/main/index.ts
  </files>
  <action>
**1. store.ts — normalizar 'auto' legado (D-04, D-07):**

Localizar `getWhisperModelOverride` (linha ~219). Alterar assinatura e lógica de retorno:

```typescript
// Phase 68 D-03/D-04: 'auto' removido do tipo WhisperModelOption.
// D-07: valor 'auto' legado no store (ou ausente) → retorna 'base' como default.
export function getWhisperModelOverride(): WhisperModelOption {
  const stored = store.get('whisperModelOverride')?.model;
  if (!stored || stored === 'auto') return 'base';
  return stored as WhisperModelOption;
}
```

Alterar `setWhisperModelOverride` para usar `WhisperModelOption` no lugar da união literal:
```typescript
export function setWhisperModelOverride(model: WhisperModelOption): void {
  store.set('whisperModelOverride', { model });
}
```

Alterar `StoreSchema.whisperModelOverride` (linha ~35) para:
```typescript
whisperModelOverride?: { model: 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo' };
```
(remover 'auto' do schema do store também — D-03)

Adicionar import de `WhisperModelOption` no topo se não estiver presente (já está na lista de imports na linha 12-17 — adicionar `WhisperModelOption` ao destructuring existente).

**2. index.ts — substituir bloco VRAM por leitura direta do store (D-06):**

Localizar o bloco nas linhas 218-238:
```typescript
// Phase 30 (STT-02): VRAM-based model selection...
let selectedModel: WhisperModel = 'base';
if (useWhisperCpp) {
  try {
    selectedModel = await detectVramAndSelectModel();
    ...
  } catch (err) {
    ...
  }
  const override = getWhisperModelOverride();
  const finalModel = selectWhisperModel(selectedModel, override);
  ...
  selectedModel = finalModel;
}
```

Substituir pelo seguinte (D-06 — sem detectVramAndSelectModel):
```typescript
// Phase 68 (WBUG-01, D-06): Leitura direta do store; sem VRAM detection.
// D-04: 'auto' legado → 'base' via getWhisperModelOverride().
let selectedModel: WhisperModel = 'base';
if (useWhisperCpp) {
  const override = getWhisperModelOverride();
  selectedModel = selectWhisperModel('base', override);
  console.log(`[voice] Whisper model: ${selectedModel} (override: ${override})`);
}
```

Remover os imports que ficarem órfãos após a mudança:
- Remover `import { detectVramAndSelectModel, type WhisperModel } from './voiceInput/vramDetection.js';` — mas `WhisperModel` ainda pode ser necessário para o tipo da variável. Se `WhisperModel` ainda for usado como tipo, manter apenas o import de tipo: `import type { WhisperModel } from './voiceInput/vramDetection.js';`
- `initializeGpuDetection` — verificar se ainda é chamada no bloco acima (linhas 210-216). Se sim, manter; se não, remover.
- `selectWhisperModel` import de `./voiceInput/selectWhisperModel.js` — MANTER (ainda é usado).
  </action>
  <verify>
    <automated>cd /root/jarvis && npx tsc --noEmit -p apps/desktop/tsconfig.json 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep "detectVramAndSelectModel()" apps/desktop/src/main/index.ts retorna vazio (chamada removida)
    - grep "getWhisperModelOverride()" apps/desktop/src/main/index.ts retorna match (leitura direta mantida)
    - grep "model === 'auto'\|!stored" apps/desktop/src/main/store.ts retorna match (normalização legado)
    - grep "WhisperModelOption" apps/desktop/src/main/store.ts | grep "auto" retorna vazio (schema sem 'auto')
    - grep "export function getWhisperModelOverride.*WhisperModelOption" apps/desktop/src/main/store.ts retorna match
    - npx tsc --noEmit não reporta erros em index.ts ou store.ts
  </acceptance_criteria>
  <done>store.ts normaliza 'auto' legado; index.ts inicia seleção de modelo via store sem VRAM detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron-store → runtime | Valor 'auto' legado no store JSON pode vir corrompido ou inválido |
| renderer → main IPC | `whisperModelOverride` enviado pelo settings UI não deve conter 'auto' após D-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | store.ts getWhisperModelOverride | mitigate | Read path normaliza qualquer valor inválido/legado ('auto', undefined, null) → 'base'; não persiste lixo |
| T-68-02 | Denial of Service | index.ts startup | accept | vramDetection removida — bug de GPU detection não pode mais travar o startup STT |
| T-68-03 | Information Disclosure | console.log modelo selecionado | accept | Log local apenas; sem PII; confirma comportamento esperado para debug |
</threat_model>

<verification>
Após execução deste plano:
1. `grep "export const OPTION_TO_MODEL" apps/desktop/src/main/voiceInput/whisperModelResolver.ts` — deve ter match
2. `grep "SUPPORTED_MODELS" apps/desktop/src/main/voiceInput/selectWhisperModel.ts` — deve estar vazio
3. `grep "'auto'" apps/desktop/src/shared/ipc-types.ts` — deve estar vazio no contexto de WhisperModelOption
4. `grep "detectVramAndSelectModel()" apps/desktop/src/main/index.ts` — deve estar vazio
5. `cd /root/jarvis && npx tsc --noEmit -p apps/desktop/tsconfig.json` — zero erros
</verification>

<success_criteria>
- selectWhisperModel usa OPTION_TO_MODEL e respeita qualquer override explícito independente do vramModel
- WhisperModelOption não inclui 'auto' — tipo narrowado para 4 opções explícitas
- Store normaliza 'auto' legado → 'base' sem migração explícita
- index.ts inicia modelo via store, sem detectVramAndSelectModel
- Zero erros TypeScript após mudanças
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/68-whisper-model-override-fix/68-01-SUMMARY.md`
</output>
