---
phase: 70
plan: 03
type: execute
wave: 2
depends_on:
  - "70-01"
files_modified:
  - apps/desktop/src/renderer/src/settings/sections/LlmSection.tsx
  - apps/desktop/src/renderer/src/settings/sections/McpSection.tsx
  - apps/desktop/src/renderer/src/settings/sections/__tests__/LlmSection.test.tsx
  - apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
  - apps/desktop/src/renderer/src/lib/tokenizer.ts
  - apps/desktop/src/main/ipc/settings.ts
  - apps/desktop/src/main/ipc/mcp-settings.ts
  - apps/desktop/src/main/ipc/index.ts
  - apps/desktop/src/main/ipc/__tests__/settings.test.ts
  - apps/desktop/src/preload/settings.ts
  - apps/desktop/src/shared/ipc-types.ts
autonomous: true
requirements:
  - SIMP-01
  - SIMP-02
  - SIMP-04
user_setup: []
must_haves:
  truths:
    - "Settings UI não renderiza dropdown provider, API keys, LM Studio URL, streaming toggle"
    - "Settings UI não renderiza section Servidor MCP (toggle, lista clientes, botao Reconectar)"
    - "Window preload bridge window.mcp inteiro inexistente"
    - "IPC handlers LLM_SET_PROVIDER, RELOAD_LLM, STREAMING_LM_STUDIO_EVENTS_SET removidos de settings.ts"
    - "IPC file mcp-settings.ts deletado; setupMcpSettingsHandlers nao mais chamado em ipc/index.ts"
    - "settings:get payload sem lmStudioUrl, llmProvider, *ApiKey, streamingLMStudioEventsEnabled"
    - "Types LlmProvider, ReloadLlmRequest, McpClientStatus removidos de ipc-types.ts"
    - "tokenizer.ts deletado (unico consumer era LlmSection)"
    - "pnpm -r typecheck verde apos strip"
  artifacts:
    - path: apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
      provides: Layout sem state/handlers/nav LLM e MCP
    - path: apps/desktop/src/main/ipc/settings.ts
      provides: Handlers sem LLM_SET_PROVIDER/RELOAD_LLM/STREAMING_LM_STUDIO_EVENTS_SET; payload settings:get sem 6 fields LLM
    - path: apps/desktop/src/preload/settings.ts
      provides: SettingsApi sem reloadLlm/setLlmProvider/setStreamingLMStudioEventsEnabled; sem exposeInMainWorld('mcp', ...)
    - path: apps/desktop/src/shared/ipc-types.ts
      provides: Sem types LlmProvider/ReloadLlmRequest/McpClientStatus; sem channels LLM_*/STREAMING_*/MCP_CLIENT_*
  key_links:
    - from: apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
      to: SettingsApi (preload bridge)
      via: window.settings.get() retorna shape sem LLM/MCP
      pattern: "window.settings.get"
    - from: apps/desktop/src/main/ipc/index.ts
      to: nada (mcp-settings deletado)
      via: ausencia de setupMcpSettingsHandlers
      pattern: "setupMcpSettingsHandlers"
---

<objective>
**Strip cirurgico de UI + IPC + types LLM/MCP** — implementa SIMP-01 (Settings UI sem LLM section), SIMP-02 (Settings UI sem MCP section), e finaliza SIMP-04 (payload `settings:get` sem fields LLM).

Diff agressivo conforme RESEARCH.md Integration Map (~500 linhas removidas):
- Renderer: `LlmSection.tsx`, `McpSection.tsx`, `LlmSection.test.tsx`, `tokenizer.ts` deletados; `SettingsLayout.tsx` perde state/handlers/nav/imports/props/types LLM+MCP.
- Main IPC: `mcp-settings.ts` deletado; `settings.ts` perde 3 handlers + 7 payload fields; `ipc/index.ts` perde 2 linhas (import + call setupMcpSettingsHandlers).
- Preload: 4 SettingsApi methods + 5 channel consts + bloco inteiro `exposeInMainWorld('mcp', ...)` removidos.
- Shared types: `LlmProvider`, `ReloadLlmRequest`, `McpClientStatus`, channels LLM_*/STREAMING_*/MCP_CLIENT_* removidos.
- Tests: `settings.test.ts` mocks LLM strip; `LlmSection.test.tsx` deletado.

**Purpose:** Apos Plan 01 (migracao + store strip) e Plan 02 (backend dead-route delete), todo o caminho UI→IPC→HTTP→backend de mudanca de provider e dead. Este plan demole o lado consumer (renderer/IPC/preload) e os types orfaos. Resultado: Settings UI mais simples, surface area minimal, contratos `.env`-only.

**Depende de Plan 01** porque `settings:get` payload muda — Plan 01 deleta accessors de `store.ts` que `ipc/settings.ts` referenciava; Plan 03 strippa o consumer. Workspace typecheck final (Task 6) so passa apos ambos fluirem.

**Output:** 5 arquivos deletados (`LlmSection.tsx`, `McpSection.tsx`, `LlmSection.test.tsx`, `mcp-settings.ts`, `tokenizer.ts`); 7 arquivos modificados (`SettingsLayout`, `ipc/settings.ts`, `ipc/index.ts`, `preload/settings.ts`, `ipc-types.ts`, `ipc/__tests__/settings.test.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-llm-config-migration/70-CONTEXT.md
@.planning/phases/70-llm-config-migration/70-RESEARCH.md
@.planning/phases/70-llm-config-migration/70-VALIDATION.md
@.planning/phases/70-llm-config-migration/70-01-PLAN.md

<interfaces>
Contratos a remover. Executor nao precisa explorar — esses sao os simbolos a procurar e matar.

From apps/desktop/src/shared/ipc-types.ts (a stripar):

```typescript
// Types a REMOVER (research confirmou zero consumers fora deste plan):
type LlmProvider = 'lmstudio' | 'openai' | 'anthropic' | 'gemini';
interface ReloadLlmRequest { /* ... */ }
interface McpClientStatus { /* ... */ }

// Channels a REMOVER:
// §293: RELOAD_LLM = 'llm:reload'
// LLM_SET_PROVIDER = 'llm:set-provider'
// STREAMING_LM_STUDIO_EVENTS_SET = 'streaming-lm-studio-events:set'
// §325-327: MCP_CLIENT_RELOAD, MCP_CLIENT_GET_STATUS, MCP_CLIENT_STATUS_CHANGED

// SettingsData fields a REMOVER:
//   lmStudioUrl, llmProvider, streamingLMStudioEventsEnabled,
//   openaiApiKey, anthropicApiKey, geminiApiKey
// (mcpServerEnabled ja foi removido em P69 D-08; confirmar)

// SettingsApi methods a REMOVER (§539):
//   reloadLlm, setLlmProvider, setStreamingLMStudioEventsEnabled

// Window typing — prop `mcp` em Window: REMOVER
```

From apps/desktop/src/preload/settings.ts (a stripar):

```typescript
// Channel constants a REMOVER:
//   RELOAD_LLM_CHANNEL (§18), LLM_SET_PROVIDER_CHANNEL, STREAMING_LM_STUDIO_EVENTS_SET_CHANNEL,
//   MCP_CLIENT_RELOAD_CHANNEL, MCP_CLIENT_GET_STATUS_CHANNEL, MCP_CLIENT_STATUS_CHANGED_CHANNEL

// SettingsApi methods (window.settings.*) a REMOVER:
//   reloadLlm (§51), setLlmProvider, setStreamingLMStudioEventsEnabled

// Bloco inteiro a REMOVER (~25 linhas):
//   contextBridge.exposeInMainWorld('mcp', { reload, getStatus, onStatusChanged });
```

From apps/desktop/src/main/ipc/settings.ts (a stripar):

```typescript
// Payload settings:get §83-93 — remover 6 fields:
//   lmStudioUrl, llmProvider, streamingLMStudioEventsEnabled,
//   openaiApiKey, anthropicApiKey, geminiApiKey

// Handler §226-244 — ipcMain.handle(IPC_CHANNELS.LLM_SET_PROVIDER, ...) INTEIRO
// Handler §277-309 — ipcMain.handle(IPC_CHANNELS.STREAMING_LM_STUDIO_EVENTS_SET, ...) INTEIRO
// Handler §316-364 — ipcMain.handle(IPC_CHANNELS.RELOAD_LLM, ...) INTEIRO
// Broadcast §234 — mainWindow.webContents.send('llm:provider-changed', ...) junto com handler

// settings:set: remover store.setLlmProvider/setLmStudioUrl/setGeminiApiKey/etc se invocados
```

From apps/desktop/src/renderer/src/settings/SettingsLayout.tsx (a stripar):

```typescript
// Imports §12: LlmSection, McpSection, LlmProvider, ReloadLlmRequest, estimateContextTokens
// State vars §108-118: lmStudioUrl, llmProvider, streamingLMStudioEventsEnabled,
//                      openaiApiKey, anthropicApiKey, geminiApiKey, mcpClients
// Handlers: handleLmStudioUrlChange, handleLlmProviderChange, handleReloadLlm,
//           handleStreamingLMStudioEventsChange, handleMcpClientReload
// useEffect listener §252-258: onStreamingLMStudioEventsChanged
// SectionKey union — remover 'llm' e 'mcp-server'
// NAV_ITEMS §28, §31 — remover "LLM" e "Servidor MCP"
// SettingsSectionProps §49-77: remover prop entries LLM/MCP
// Fetch unpack §156-164, §181-183: remover 7 fields
// Props pass-down §469-494: remover props LLM/MCP
// Switch §525-540 ('llm') + §554-555 ('mcp-server'): deletar blocos
// activeSection default §137 = 'ptt' — NAO MUDAR (research confirmou)
```

From apps/desktop/src/main/ipc/index.ts (a stripar):

```typescript
import { setupMcpSettingsHandlers } from './mcp-settings.js';  // REMOVE
setupMcpSettingsHandlers(mainWindow);                            // REMOVE (§36)
```

From apps/desktop/src/main/ipc/__tests__/settings.test.ts (mocks a stripar):

```typescript
// Mock block §36-43: getLmStudioUrl, getLlmProvider, getStreamingLMStudioEventsEnabled,
//                    getOpenaiApiKey, getAnthropicApiKey, getGeminiApiKey
// Expectations §289, §322: remover refs aos 6 fields
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar arquivos UI/IPC obsoletos (LlmSection, McpSection, LlmSection.test, mcp-settings.ts, tokenizer.ts)</name>

  <files>
    apps/desktop/src/renderer/src/settings/sections/LlmSection.tsx (DELETE)
    apps/desktop/src/renderer/src/settings/sections/McpSection.tsx (DELETE)
    apps/desktop/src/renderer/src/settings/sections/__tests__/LlmSection.test.tsx (DELETE)
    apps/desktop/src/renderer/src/lib/tokenizer.ts (DELETE)
    apps/desktop/src/main/ipc/mcp-settings.ts (DELETE)
  </files>

  <read_first>
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-13, D-16)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — cascade verifications confirmando consumers unicos)
    - apps/desktop/src/renderer/src/lib/tokenizer.ts (esperado export estimateContextTokens)
    - listar apps/desktop/src/renderer/src/settings/sections/__tests__/
  </read_first>

  <action>
    1. Verificar cascade negativo (matches devem ser SO o proprio arquivo + consumers a serem stripped neste plan):
       - `grep -rn "LlmSection" apps/desktop/src/` — esperado: o arquivo + SettingsLayout
       - `grep -rn "McpSection" apps/desktop/src/` — esperado: o arquivo + SettingsLayout
       - `grep -rn "estimateContextTokens" apps/desktop/src/` — esperado: tokenizer.ts (definicao) + LlmSection (consumer)
       - `grep -rn "setupMcpSettingsHandlers" apps/desktop/src/` — esperado: mcp-settings.ts (definicao) + ipc/index.ts (caller)
       Se qualquer outro caller aparecer, parar e reportar.

    2. Deletar arquivos:
       - `rm apps/desktop/src/renderer/src/settings/sections/LlmSection.tsx`
       - `rm apps/desktop/src/renderer/src/settings/sections/McpSection.tsx`
       - `rm apps/desktop/src/renderer/src/settings/sections/__tests__/LlmSection.test.tsx`
       - `rm apps/desktop/src/renderer/src/lib/tokenizer.ts`
       - `rm apps/desktop/src/main/ipc/mcp-settings.ts`

    3. Confirmar `McpSection.test.tsx` ausente (research confirmou nao existir): `ls apps/desktop/src/renderer/src/settings/sections/__tests__/` — se contra-expectativa existir, deletar tambem e reportar.

    NAO rodar typecheck — esperado quebrar ate Tasks 2-5 stripparem consumers.
  </action>

  <verify>
    <automated>! test -f apps/desktop/src/renderer/src/settings/sections/LlmSection.tsx && ! test -f apps/desktop/src/renderer/src/settings/sections/McpSection.tsx && ! test -f apps/desktop/src/renderer/src/settings/sections/__tests__/LlmSection.test.tsx && ! test -f apps/desktop/src/renderer/src/lib/tokenizer.ts && ! test -f apps/desktop/src/main/ipc/mcp-settings.ts</automated>
  </verify>

  <acceptance_criteria>
    - 5 arquivos deletados (5x `! test -f` pass)
    - SUMMARY documenta resultado de `ls` do dir __tests__
  </acceptance_criteria>

  <done>
    5 arquivos deletados (4 sources + 1 test). McpSection.test.tsx confirmado ausente. Typecheck ainda quebra (esperado).
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip SettingsLayout.tsx — state, handlers, nav, imports, props, switch cases (D-14)</name>

  <files>apps/desktop/src/renderer/src/settings/SettingsLayout.tsx</files>

  <read_first>
    - apps/desktop/src/renderer/src/settings/SettingsLayout.tsx (arquivo inteiro)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-14)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map linhas exatas)
  </read_first>

  <action>
    Strip cirurgico de SettingsLayout.tsx. Aplicar cada item:

    1. Imports (§12): remover `LlmSection`, `McpSection`, `LlmProvider`, `ReloadLlmRequest`, `estimateContextTokens`.
    2. State vars (§108-118): remover `useState` para `lmStudioUrl`, `llmProvider`, `streamingLMStudioEventsEnabled`, `openaiApiKey`, `anthropicApiKey`, `geminiApiKey`, `mcpClients`.
    3. Handlers: remover `handleLmStudioUrlChange`, `handleLlmProviderChange`, `handleReloadLlm`, `handleStreamingLMStudioEventsChange`, `handleMcpClientReload` (funcoes inteiras).
    4. useEffect listener (§252-258): remover bloco com `onStreamingLMStudioEventsChanged`.
    5. NAV_ITEMS: remover entries `'llm'` ("LLM") e `'mcp-server'` ("Servidor MCP").
    6. SectionKey union: remover literais `'llm'` e `'mcp-server'`.
    7. SettingsSectionProps interface (§49-77): remover prop fields LLM/MCP.
    8. Fetch unpack (§156-164, §181-183): remover 6 fields LLM no destructuring + sets subsequentes.
    9. Props pass-down (§469-494): remover props LLM/MCP do JSX que passa para SettingsSection.
    10. Switch cases (§525-540 'llm', §554-555 'mcp-server'): deletar ambos os blocos `case`.
    11. activeSection default (§137) = `'ptt'`: NAO MUDAR (research confirmou).

    Apos edits, NAO rodar typecheck (resolve em Task 6).
  </action>

  <verify>
    <automated>! grep -E "LlmSection|McpSection|estimateContextTokens" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx && ! grep -E "lmStudioUrl|llmProvider|streamingLMStudioEventsEnabled|geminiApiKey|openaiApiKey|anthropicApiKey|handleMcpClientReload" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `! grep "LlmSection" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx`
    - `! grep "McpSection" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx`
    - `! grep "estimateContextTokens" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx`
    - `! grep -E "lmStudioUrl|llmProvider|geminiApiKey|openaiApiKey|anthropicApiKey|streamingLMStudioEventsEnabled" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` retorna 0 matches
    - `! grep -E "'llm'|'mcp-server'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` retorna 0 matches
    - `grep "'ptt'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` permanece (default nao mudou)
    - Sanity: outros section keys preservados — `grep "'whisper'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` retorna >=1
  </acceptance_criteria>

  <done>
    SettingsLayout.tsx limpo de tudo LLM/MCP — imports, state, handlers, nav, types, switch. activeSection default `'ptt'` inalterado. Outros sections (whisper, ptt, hotkey, kokoro, etc.) intactos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip ipc/settings.ts (handlers + payload) e ipc/index.ts (setupMcpSettingsHandlers)</name>

  <files>
    apps/desktop/src/main/ipc/settings.ts
    apps/desktop/src/main/ipc/index.ts
  </files>

  <read_first>
    - apps/desktop/src/main/ipc/settings.ts (arquivo inteiro — entender shape do payload settings:get linhas 83-93 e estrutura dos 3 handlers)
    - apps/desktop/src/main/ipc/index.ts (arquivo inteiro — localizar import e call de setupMcpSettingsHandlers)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-11, D-12, D-16, D-18)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map)
  </read_first>

  <action>
    **A) `apps/desktop/src/main/ipc/settings.ts`:**

    1. Payload de `settings:get` (linhas 83-93) — remover 6 fields:
       - `lmStudioUrl`, `llmProvider`, `streamingLMStudioEventsEnabled`,
       - `openaiApiKey`, `anthropicApiKey`, `geminiApiKey`
       - (`mcpServerEnabled` ja saiu em P69 D-08; verificar e remover se sobrevivente)

    2. Handler `IPC_CHANNELS.LLM_SET_PROVIDER` (linhas 226-244) — remover bloco `ipcMain.handle(IPC_CHANNELS.LLM_SET_PROVIDER, ...)` INTEIRO, incluindo broadcast `mainWindow.webContents.send('llm:provider-changed', ...)` em §234 (D-12).

    3. Handler `IPC_CHANNELS.STREAMING_LM_STUDIO_EVENTS_SET` (linhas 277-309) — remover bloco INTEIRO.

    4. Handler `IPC_CHANNELS.RELOAD_LLM` (linhas 316-364) — remover bloco INTEIRO (inclui fetch para `/internal/reload-llm` que Plan 02 ja deletou).

    5. Em `settings:set` (se aceitar payload LLM/MCP):
       - Remover `store.setLmStudioUrl(...)`, `store.setLlmProvider(...)`, `store.setGeminiApiKey(...)`, `store.setOpenaiApiKey(...)`, `store.setAnthropicApiKey(...)`, `store.setStreamingLMStudioEventsEnabled(...)`.
       - Plan 01 ja deletou esses accessors do `store.ts` — typecheck flagaria, mas grep negativo aqui evita esperar typecheck.

    6. Imports: remover qualquer import de `IPC_CHANNELS.LLM_SET_PROVIDER`, `RELOAD_LLM`, `STREAMING_LM_STUDIO_EVENTS_SET` se estiverem listados separadamente. Tambem remover import de `LlmProvider`, `ReloadLlmRequest` types.

    **B) `apps/desktop/src/main/ipc/index.ts`:**

    1. Remover linha `import { setupMcpSettingsHandlers } from './mcp-settings.js';` (ou path conforme projeto).
    2. Remover chamada `setupMcpSettingsHandlers(mainWindow)` (linha ~36 conforme research).
    3. Outros setup handlers (settings, whisper, etc.) PRESERVAR intactos.
  </action>

  <verify>
    <automated>! grep -E "LLM_SET_PROVIDER|RELOAD_LLM|STREAMING_LM_STUDIO_EVENTS_SET|llm:provider-changed" apps/desktop/src/main/ipc/settings.ts && ! grep -E "lmStudioUrl:|llmProvider:|geminiApiKey:|openaiApiKey:|anthropicApiKey:|streamingLMStudioEventsEnabled:" apps/desktop/src/main/ipc/settings.ts && ! grep "setupMcpSettingsHandlers" apps/desktop/src/main/ipc/index.ts && ! grep "from './mcp-settings" apps/desktop/src/main/ipc/index.ts</automated>
  </verify>

  <acceptance_criteria>
    - `! grep "LLM_SET_PROVIDER" apps/desktop/src/main/ipc/settings.ts`
    - `! grep "RELOAD_LLM" apps/desktop/src/main/ipc/settings.ts`
    - `! grep "STREAMING_LM_STUDIO_EVENTS_SET" apps/desktop/src/main/ipc/settings.ts`
    - `! grep "llm:provider-changed" apps/desktop/src/main/ipc/settings.ts`
    - `! grep -E "lmStudioUrl:|llmProvider:|geminiApiKey:|openaiApiKey:|anthropicApiKey:|streamingLMStudioEventsEnabled:" apps/desktop/src/main/ipc/settings.ts` retorna 0 matches
    - `! grep "setupMcpSettingsHandlers" apps/desktop/src/main/ipc/index.ts`
    - `! grep "mcp-settings" apps/desktop/src/main/ipc/index.ts`
    - Outros handlers preservados — `grep "ipcMain.handle" apps/desktop/src/main/ipc/settings.ts` retorna >=2 (settings:get, settings:set + outros sobreviventes)
  </acceptance_criteria>

  <done>
    `ipc/settings.ts` perde 3 handlers + 6 fields do payload settings:get + broadcast orfao. `ipc/index.ts` sem import e call de `setupMcpSettingsHandlers`. Outros handlers preservados intactos.
  </done>
</task>

<task type="auto">
  <name>Task 4: Strip preload/settings.ts (channels + SettingsApi methods + bloco window.mcp)</name>

  <files>apps/desktop/src/preload/settings.ts</files>

  <read_first>
    - apps/desktop/src/preload/settings.ts (arquivo inteiro — localizar channel consts §18, SettingsApi methods §51, bloco mcp §116-140)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-11, D-16)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — preload mutate)
  </read_first>

  <action>
    Edit `apps/desktop/src/preload/settings.ts`:

    1. Channel consts (§18 and around): remover:
       - `RELOAD_LLM_CHANNEL`
       - `LLM_SET_PROVIDER_CHANNEL`
       - `STREAMING_LM_STUDIO_EVENTS_SET_CHANNEL`
       - `MCP_CLIENT_RELOAD_CHANNEL`
       - `MCP_CLIENT_GET_STATUS_CHANNEL`
       - `MCP_CLIENT_STATUS_CHANGED_CHANNEL`
       (Outros channels preservar.)

    2. SettingsApi methods em `contextBridge.exposeInMainWorld('settings', { ... })` ou similar:
       - Remover method `reloadLlm: (req) => ipcRenderer.invoke(RELOAD_LLM_CHANNEL, req)` (linha §51).
       - Remover `setLlmProvider`.
       - Remover `setStreamingLMStudioEventsEnabled`.
       - Outros methods (get, set, onWhisperProgress, etc.) PRESERVAR.

    3. Bloco INTEIRO `contextBridge.exposeInMainWorld('mcp', { ... })` (linhas ~116-140) — deletar completamente.

    4. Imports de types `LlmProvider`, `ReloadLlmRequest`, `McpClientStatus`: remover.
  </action>

  <verify>
    <automated>! grep -E "RELOAD_LLM_CHANNEL|LLM_SET_PROVIDER_CHANNEL|STREAMING_LM_STUDIO_EVENTS_SET_CHANNEL|MCP_CLIENT_" apps/desktop/src/preload/settings.ts && ! grep "exposeInMainWorld.*mcp" apps/desktop/src/preload/settings.ts && ! grep -E "reloadLlm:|setLlmProvider:|setStreamingLMStudioEventsEnabled:" apps/desktop/src/preload/settings.ts</automated>
  </verify>

  <acceptance_criteria>
    - `! grep "RELOAD_LLM_CHANNEL" apps/desktop/src/preload/settings.ts`
    - `! grep "LLM_SET_PROVIDER_CHANNEL" apps/desktop/src/preload/settings.ts`
    - `! grep "STREAMING_LM_STUDIO_EVENTS_SET_CHANNEL" apps/desktop/src/preload/settings.ts`
    - `! grep "MCP_CLIENT_" apps/desktop/src/preload/settings.ts`
    - `! grep "exposeInMainWorld.*['\"]mcp['\"]" apps/desktop/src/preload/settings.ts`
    - `! grep -E "reloadLlm:|setLlmProvider:|setStreamingLMStudioEventsEnabled:" apps/desktop/src/preload/settings.ts`
    - Sanity preserved — `grep "exposeInMainWorld.*settings" apps/desktop/src/preload/settings.ts` retorna 1 match (bridge settings preservado)
  </acceptance_criteria>

  <done>
    Preload sem channels LLM_*/STREAMING_*/MCP_CLIENT_*, sem methods LLM em SettingsApi, sem `window.mcp` bridge. `window.settings` bridge intact com methods sobreviventes (get, set, etc.).
  </done>
</task>

<task type="auto">
  <name>Task 5: Strip shared/ipc-types.ts (types + channels + SettingsData/SettingsApi fields)</name>

  <files>apps/desktop/src/shared/ipc-types.ts</files>

  <read_first>
    - apps/desktop/src/shared/ipc-types.ts (arquivo inteiro)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-11, D-16, D-18)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — types orphan cascade verified)
  </read_first>

  <action>
    Edit `apps/desktop/src/shared/ipc-types.ts`:

    1. Types a REMOVER (research confirmou cascade limpo):
       - `export type LlmProvider = ...;`
       - `export interface ReloadLlmRequest { ... }`
       - `export interface McpClientStatus { ... }`

    2. IPC_CHANNELS entries a REMOVER (procurar pelas constantes — em torno de §293, §325-327):
       - `RELOAD_LLM: 'llm:reload'`
       - `LLM_SET_PROVIDER: 'llm:set-provider'`
       - `STREAMING_LM_STUDIO_EVENTS_SET: 'streaming-lm-studio-events:set'`
       - `MCP_CLIENT_RELOAD: 'mcp:client-reload'`
       - `MCP_CLIENT_GET_STATUS: 'mcp:client-get-status'`
       - `MCP_CLIENT_STATUS_CHANGED: 'mcp:client-status-changed'`

    3. `SettingsData` interface — remover 6 fields:
       - `lmStudioUrl`, `llmProvider`, `streamingLMStudioEventsEnabled`,
       - `openaiApiKey`, `anthropicApiKey`, `geminiApiKey`

    4. `SettingsApi` interface (§539 conforme research) — remover methods:
       - `reloadLlm: (...) => ...`
       - `setLlmProvider: (...) => ...`
       - `setStreamingLMStudioEventsEnabled: (...) => ...`

    5. Window typing (`declare global { interface Window { ... } }`):
       - Remover prop `mcp: { ... }` se presente como property em `Window`.

    Apos edits, verificar cascade verdadeiramente limpo:
    - `grep -rn "LlmProvider\b" apps/desktop/src/` — esperado: zero matches (research confirmou orphan apos strip).
    - `grep -rn "ReloadLlmRequest\b" apps/desktop/src/` — zero matches.
    - `grep -rn "McpClientStatus\b" apps/desktop/src/` — zero matches.
    Se algum match remanescente, parar e reportar — pode indicar que research underestimou cascade.
  </action>

  <verify>
    <automated>! grep -E "LlmProvider|ReloadLlmRequest|McpClientStatus" apps/desktop/src/shared/ipc-types.ts && ! grep -E "RELOAD_LLM|LLM_SET_PROVIDER|STREAMING_LM_STUDIO_EVENTS_SET|MCP_CLIENT_RELOAD|MCP_CLIENT_GET_STATUS|MCP_CLIENT_STATUS_CHANGED" apps/desktop/src/shared/ipc-types.ts && ! grep -rn "LlmProvider\b" apps/desktop/src/</automated>
  </verify>

  <acceptance_criteria>
    - `! grep -E "LlmProvider|ReloadLlmRequest|McpClientStatus" apps/desktop/src/shared/ipc-types.ts`
    - `! grep -E "RELOAD_LLM|LLM_SET_PROVIDER|STREAMING_LM_STUDIO_EVENTS_SET|MCP_CLIENT_" apps/desktop/src/shared/ipc-types.ts`
    - `! grep -E "lmStudioUrl|llmProvider|geminiApiKey|openaiApiKey|anthropicApiKey|streamingLMStudioEventsEnabled" apps/desktop/src/shared/ipc-types.ts` (fields removidos de SettingsData)
    - Cascade limpo: `! grep -rn "LlmProvider\b" apps/desktop/src/ apps/backend-ts/src/` — zero matches (type globally extinct).
    - Sanity preservado — `grep "IPC_CHANNELS" apps/desktop/src/shared/ipc-types.ts` retorna >=1 (objeto preservado, so 6 entries removidas).
  </acceptance_criteria>

  <done>
    `ipc-types.ts` sem 3 types LLM/MCP, sem 6 channels, sem 6 fields em SettingsData, sem 3 methods em SettingsApi, sem prop mcp em Window. Cascade global verified: LlmProvider/ReloadLlmRequest/McpClientStatus extintos.
  </done>
</task>

<task type="auto">
  <name>Task 6: Strip ipc/__tests__/settings.test.ts mocks + run workspace typecheck e tests (D-19)</name>

  <files>apps/desktop/src/main/ipc/__tests__/settings.test.ts</files>

  <read_first>
    - apps/desktop/src/main/ipc/__tests__/settings.test.ts (arquivo inteiro — localizar mock block §36-43 e expectations §289, §322)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-19)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — settings.test.ts mutate strip mocks)
  </read_first>

  <action>
    **A) Strip mocks em `settings.test.ts`:**

    1. Mock block para `../../store.js` (linhas §36-43 conforme research) — remover mocks dos getters LLM:
       - `getLmStudioUrl`
       - `getLlmProvider`
       - `getStreamingLMStudioEventsEnabled`
       - `getOpenaiApiKey`
       - `getAnthropicApiKey`
       - `getGeminiApiKey`
       (E correspondentes setters se mockados.)

    2. Test expectations em §289, §322 (e quaisquer outros) — remover assertions sobre os 6 fields LLM no payload de `settings:get` ou refs a handlers deletados (`LLM_SET_PROVIDER`, `RELOAD_LLM`, etc.).

    3. Se algum `describe`/`it` block inteiro testar especificamente LLM handlers deletados (e.g., `describe('LLM_SET_PROVIDER', ...)`), deletar o bloco inteiro.

    4. Imports orfaos: se `LlmProvider`, `ReloadLlmRequest` imports sobrevivem no test, remover.

    **B) Workspace typecheck + tests:**

    1. `pnpm -r typecheck` — deve passar limpo agora. Plan 01 + Plan 02 + Tasks 1-5 deste plan + Task 6 cobrem todos consumers dos simbolos removidos. Se quebrar, identificar arquivo orfao e:
       - Se for caso obvio (e.g., test obsoleto, comentario com import quebrado), corrigir localmente.
       - Se for arquivo nao listado nos files_modified deste plano, reportar como cascade nao previsto pelo research.

    2. `pnpm --filter @jarvis/desktop test:run` — todos tests sobreviventes passam.

    3. `pnpm --filter @jarvis/backend-ts test:run` — passa (ja validado em Plan 02 mas reconfirmar para o phase como um todo).
  </action>

  <verify>
    <automated>! grep -E "getLmStudioUrl|getLlmProvider|getGeminiApiKey|getOpenaiApiKey|getAnthropicApiKey|getStreamingLMStudioEventsEnabled" apps/desktop/src/main/ipc/__tests__/settings.test.ts && ! grep -E "LLM_SET_PROVIDER|RELOAD_LLM|STREAMING_LM_STUDIO_EVENTS_SET" apps/desktop/src/main/ipc/__tests__/settings.test.ts && pnpm -r typecheck && pnpm --filter @jarvis/desktop test:run && pnpm --filter @jarvis/backend-ts test:run</automated>
  </verify>

  <acceptance_criteria>
    - `! grep -E "getLmStudioUrl|getLlmProvider|getGeminiApiKey|getOpenaiApiKey|getAnthropicApiKey|getStreamingLMStudioEventsEnabled" apps/desktop/src/main/ipc/__tests__/settings.test.ts`
    - `! grep -E "LLM_SET_PROVIDER|RELOAD_LLM|STREAMING_LM_STUDIO_EVENTS_SET|llm:reload" apps/desktop/src/main/ipc/__tests__/settings.test.ts`
    - `pnpm -r typecheck` exits 0 (workspace inteiro verde)
    - `pnpm --filter @jarvis/desktop test:run` exits 0
    - `pnpm --filter @jarvis/backend-ts test:run` exits 0
    - Sanity: migration test ainda passa — `pnpm --filter @jarvis/desktop test:run src/main/migrations/__tests__/llm-config.test.ts` exits 0 (Plan 01 deliverables intactos)
  </acceptance_criteria>

  <done>
    `settings.test.ts` sem mocks LLM e sem expectations LLM. `pnpm -r typecheck` verde. Tests desktop + backend-ts passam. Migracao de Plan 01 continua green. Phase 70 strip cirurgico completo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ↔ main IPC | Channels `llm:*`, `streaming-lm-studio-events:set`, `mcp:client-*` antes expostos — agora extintos |
| preload contextBridge | `window.settings.reloadLlm/...` e `window.mcp` antes expostos no contexto isolado do renderer — agora extintos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-08 | Elevation of Privilege | Renderer compromissed poderia chamar `window.settings.reloadLlm({ provider, apiKey })` para envenenar provider | mitigate | Tasks 3-5 deletam handler, channel, preload bridge method, type. Surface area removida do contextBridge. |
| T-70-09 | Information Disclosure | `settings:get` retornando API keys em payload IPC exposia em DevTools renderer | mitigate | Task 3 strip dos 6 fields LLM do payload — keys jamais cruzam o IPC para o renderer. |
| T-70-10 | Tampering | `window.mcp.reload` exposto poderia ser misusado por renderer comprometido para flood requests | mitigate | Task 4 remove bloco inteiro `exposeInMainWorld('mcp', ...)`. Sem surface area. |
</threat_model>

<verification>
- 5 arquivos deletados (`! test -f` em cada).
- SettingsLayout sem refs LLM/MCP (grep negativos).
- ipc/settings.ts sem 3 handlers + 6 payload fields.
- ipc/index.ts sem setupMcpSettingsHandlers.
- preload/settings.ts sem channels LLM/MCP + sem window.mcp bridge.
- ipc-types.ts sem types LlmProvider/ReloadLlmRequest/McpClientStatus + sem 6 channels.
- Cascade global: zero matches de `LlmProvider`/`ReloadLlmRequest`/`McpClientStatus` em todo monorepo.
- `pnpm -r typecheck` exits 0.
- Tests desktop e backend-ts passam.
</verification>

<success_criteria>
1. SIMP-01 cumprida — Settings UI sem dropdown provider, API keys, LM Studio URL, streaming toggle (smoke manual: `pnpm dev` → abrir Settings → confirmar ausencia).
2. SIMP-02 cumprida — Settings UI sem section "Servidor MCP" (toggle + lista clientes + Reconectar) (smoke manual igual).
3. SIMP-04 finalizada — `settings:get` payload nao inclui mais fields LLM; renderer nunca recebe API keys via IPC.
4. Cascade limpo confirmado — `LlmProvider`/`ReloadLlmRequest`/`McpClientStatus` extintos do monorepo via grep.
5. Workspace typecheck verde + tests verdes — `pnpm -r typecheck` e tests desktop/backend-ts passam.
</success_criteria>

<output>
After completion, create `.planning/phases/70-llm-config-migration/70-03-SUMMARY.md`.
</output>
