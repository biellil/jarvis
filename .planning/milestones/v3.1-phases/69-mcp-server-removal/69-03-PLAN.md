---
phase: 69-mcp-server-removal
plan: 03
type: execute
wave: 2
depends_on:
  - "69-02"
files_modified:
  - apps/desktop/src/renderer/src/settings/sections/McpSection.tsx
  - apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
autonomous: true
requirements:
  - MCP-RM-01

must_haves:
  truths:
    - "Componente McpSection.tsx renderiza apenas o sub-block 'Cliente MCP' — sub-block 'Servidor MCP' (botão Habilitar/Desabilitar + status text + counter de clientes conectados) ausente"
    - "Props interface `McpSectionProps` não declara mais `enabled`, `connectedClients`, `onToggle`, `isToggling` — componente recebe zero props"
    - "SettingsLayout.tsx não tem mais state hooks `mcpEnabled`, `mcpClients`, `mcpToggling`"
    - "SettingsLayout.tsx não tem mais a função `handleMcpToggle`"
    - "SettingsLayout.tsx não chama `setMcpEnabled(data.mcpServerEnabled ?? false)` no useEffect de load"
    - "SettingsLayout.tsx não importa `McpClientInfo` (type apagado em Plan 02)"
    - "JSX do case `'mcp-server'` em renderSection passa zero props para `<McpSection />`"
    - "Section key `'mcp-server'` permanece (será removida em Phase 70) — label 'Servidor MCP' permanece (idem)"
    - "`pnpm --filter desktop typecheck` exit 0 (full typecheck verde após Plan 03)"
    - "Suite completa `pnpm --filter desktop test` exit 0"
    - "Critério 3 do ROADMAP §Phase 69: `pnpm --filter backend-ts test src/mcp/client/__tests__/e2e-mock-server.test.ts` continua exit 0 (MCP-RM-02)"
  artifacts:
    - path: "apps/desktop/src/renderer/src/settings/sections/McpSection.tsx"
      provides: "Componente McpSection apenas com sub-block Cliente MCP"
      contains: "Cliente MCP,reloadClient,getClientStatus,onClientStatusChanged"
      not_contains: "Servidor MCP,onToggle,connectedClients,isToggling,enabled,clientCount"
    - path: "apps/desktop/src/renderer/src/settings/SettingsLayout.tsx"
      provides: "SettingsLayout sem state/handlers do MCP Server"
      contains: "McpSection"
      not_contains: "mcpEnabled,mcpClients,mcpToggling,handleMcpToggle,setMcpEnabled,McpClientInfo,window.mcp?.toggle,window.mcp?.getConnectedClients"
  key_links:
    - from: "apps/desktop/src/renderer/src/settings/SettingsLayout.tsx renderSection() case mcp-server"
      to: "McpSection (zero props)"
      via: "JSX render"
      pattern: "<McpSection\\s*/>"
      note: "Componente passa a ser auto-suficiente (sub-block Cliente lê via window.mcp diretamente)"
    - from: "apps/desktop/src/renderer/src/settings/sections/McpSection.tsx"
      to: "window.mcp.reloadClient / getClientStatus / onClientStatusChanged"
      via: "useEffect + useCallback (lógica do sub-block Cliente preservada)"
      pattern: "window.*mcp.*reloadClient|getClientStatus|onClientStatusChanged"
      note: "Bridge mantido pelo Plan 02 — funciona sem refactor adicional"
---

<objective>
Limpar a Settings UI do renderer Electron removendo todos os state hooks, handlers e JSX relacionados ao MCP **Server**, mantendo intacto o sub-block "Cliente MCP" do componente `McpSection`. Esta é a parte final do MCP-RM-01: depois deste plan, o renderer compila contra os types do Plan 02 (sem `MCP_TOGGLE`, sem `McpClientInfo`, sem `mcpServerEnabled` no `SettingsData`) e o usuário deixa de ver o toggle "Habilitar/Desabilitar Servidor MCP" + status text + counter de clientes.

Purpose: Completar a remoção atômica MCP-RM-01 (a parte Electron renderer). Wave 2 porque depende dos types/canais redefinidos em Plan 02. Filosofia cirúrgica (D-11..D-14 do CONTEXT.md):
- Section key 'mcp-server' e label "Servidor MCP" do nav PERMANECEM (D-13, D-14) — Phase 70 remove a section inteira junto da migração LLM
- Sub-block "Cliente MCP" (Field Reconectar + status label) preservado intacto — é o que faz o JARVIS conectar a servers MCP externos via `.env`
- McpSection deixa de receber props (vira componente self-contained, lê via `window.mcp` diretamente)

Por que Wave 2: Plan 02 apaga `IPC_CHANNELS.MCP_TOGGLE`, `McpClientInfo`, `SettingsData.mcpServerEnabled`, e `SettingsApi.mcp.toggle/getConnectedClients`. Se Plan 03 rodasse antes, o renderer teria refs dangling. Wave 2 garante a ordem: types apagados primeiro, então o renderer alinha.

Output: 2 arquivos modificados; `pnpm --filter desktop typecheck` exit 0; `pnpm --filter desktop test` exit 0; gate `e2e-mock-server.test.ts` exit 0; critério 3 do ROADMAP §Phase 69 satisfeito.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-mcp-server-removal/69-CONTEXT.md
@.planning/phases/69-mcp-server-removal/69-02-SUMMARY.md
</context>

<interfaces>
Contratos pós-Plan 02 que o renderer deve consumir após este plan:

apps/desktop/src/shared/ipc-types.ts (estado após Plan 02):
  - SettingsData NÃO contém `mcpServerEnabled` (Plan 02 Task 5)
  - SettingsApi.mcp declara apenas `reloadClient`, `getClientStatus` (Plan 02 Task 5)
  - Não existe mais `McpClientInfo` (Plan 02 Task 5)

apps/desktop/src/preload/settings.ts (estado após Plan 02):
  - `window.mcp.toggle` NÃO existe
  - `window.mcp.getConnectedClients` NÃO existe
  - `window.mcp.reloadClient`, `getClientStatus`, `onClientStatusChanged` permanecem (Plan 02 Task 4)

apps/desktop/src/renderer/src/settings/sections/McpSection.tsx (estado atual a editar — line numbers do snapshot lido no planejamento):
  - L11: `import type { McpClientInfo, McpClientStatus } from '../../../../shared/ipc-types';` — remover McpClientInfo
  - L13-18: interface McpSectionProps { enabled, connectedClients, onToggle, isToggling } — apagar inteira
  - L43: signature `export function McpSection({ enabled, connectedClients, onToggle, isToggling }: McpSectionProps)` — vira `export function McpSection()`
  - L44-46: comentário "Servidor MCP" + statusText + clientCount — apagar 3 linhas
  - L99-122: bloco JSX inteiro do sub-block Servidor MCP `<Field>...</Field>` — apagar
  - L48 e L124: comentários do sub-block Cliente — preservar (são guideposts)

apps/desktop/src/renderer/src/settings/SettingsLayout.tsx (estado atual a editar — line numbers do snapshot):
  - L4: import inclui `McpClientInfo` — remover
  - L125-128: comentário + 3 state hooks (mcpEnabled, mcpClients, mcpToggling) — apagar
  - L174-175: comentário "Phase 64 — MCP Server enabled" + linha `setMcpEnabled(data.mcpServerEnabled ?? false);` — apagar
  - L459-482: função `handleMcpToggle` inteira — apagar
  - L585-593: JSX case 'mcp-server' com `<McpSection enabled={...} ... />` — reduzir para `<McpSection />`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Limpar McpSection.tsx removendo sub-block Servidor MCP e props</name>
  <files>apps/desktop/src/renderer/src/settings/sections/McpSection.tsx</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx (arquivo completo, 149 linhas)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-11, D-14)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` com as seguintes mudanças exatas:

    1. **Atualizar o JSDoc de cabeçalho (linhas 1-8)** — substituir o bloco original por:
       ```tsx
       /**
        * McpSection — Phase 65 (MCP-CLI-01, D-10)
        *
        * One sub-block:
        *  - Cliente MCP — connection status + Reconectar button (self-contained,
        *    reads/subscribes via window.mcp directly to avoid touching SettingsLayout's contract)
        *
        * Phase 69: Servidor MCP sub-block removed (toggle + connected clients).
        */
       ```

    2. **Linha 11** — Alterar import. Linha atual:
       ```tsx
       import type { McpClientInfo, McpClientStatus } from '../../../../shared/ipc-types';
       ```
       Para:
       ```tsx
       import type { McpClientStatus } from '../../../../shared/ipc-types';
       ```
       O type `McpClientInfo` foi removido em Plan 02 Task 5 — mantê-lo importado quebra typecheck.

    3. **Linhas 13-18** — REMOVER a interface inteira:
       ```tsx
       interface McpSectionProps {
         enabled: boolean;
         connectedClients: McpClientInfo[];
         onToggle: (enabled: boolean) => Promise<void>;
         isToggling: boolean;
       }
       ```

    4. **Linha 43** — Alterar a assinatura do componente. Linha atual:
       ```tsx
       export function McpSection({ enabled, connectedClients, onToggle, isToggling }: McpSectionProps) {
       ```
       Para:
       ```tsx
       export function McpSection() {
       ```

    5. **Linhas 44-46** — REMOVER as 3 linhas:
       ```tsx
         // ===== Phase 64 — Servidor MCP =====
         const statusText = enabled ? 'Ativo' : 'Inativo';
         const clientCount = connectedClients.length;
       ```

    6. **Linhas 99-122** — REMOVER o JSX inteiro do sub-block Servidor MCP (24 linhas):
       ```tsx
             {/* Phase 64 — Servidor MCP */}
             <Field>
               <Field.Label>Servidor MCP</Field.Label>
               <Field.Control>
                 <div className="flex items-center gap-sm">
                   <Button
                     variant={enabled ? 'secondary' : 'primary'}
                     size="sm"
                     onClick={() => void onToggle(!enabled)}
                     disabled={isToggling}
                   >
                     {enabled ? 'Desabilitar' : 'Habilitar'}
                   </Button>
                   <span className="text-sm text-fg-muted">
                     Servidor MCP: {statusText}
                   </span>
                 </div>
               </Field.Control>
               <Field.Helper>
                 {enabled
                   ? `Clientes conectados: ${clientCount}`
                   : 'Habilite para expor tools do JARVIS via protocolo MCP (Claude Desktop, Cursor, Windsurf)'}
               </Field.Helper>
             </Field>
       ```
       Após a remoção, o wrapper `<div className="space-y-base">` (L98) é seguido diretamente pelo comentário `{/* Phase 65 — Cliente MCP (D-10) */}` do sub-block Cliente.

    Preservar intactos:
    - Função `formatClientStatusLabel` (L20-34)
    - Interface `McpWindowApi` (L37-41)
    - State hooks `[clientStatus, setClientStatus]` e `[reloading, setReloading]` (L49-50)
    - `useEffect` com `getClientStatus()` + `onClientStatusChanged` subscribe (L52-79)
    - `handleReconnect` callback (L81-93)
    - Variável `clientStatusLabel` (L95)
    - O wrapper `<div className="space-y-base">` (L97-98) e seu fechamento (L147)
    - JSX inteiro do sub-block "Cliente MCP" (L124-146)
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "McpSectionProps\|onToggle\|connectedClients\|isToggling\|Servidor MCP" apps/desktop/src/renderer/src/settings/sections/McpSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "McpSectionProps" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 0
    - `grep -c "McpClientInfo" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 0
    - `grep -c "Servidor MCP" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 0
    - `grep -c "onToggle\|isToggling\|connectedClients\|statusText\|clientCount" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 0
    - `grep -c "Cliente MCP" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → ≥1 (preservado)
    - `grep -c "reloadClient\|getClientStatus\|onClientStatusChanged" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → ≥3 (preservados)
    - `grep -c "export function McpSection()" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 1 (zero props)
    - `grep -c "formatClientStatusLabel" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → ≥2 (definição + uso, preservado)
  </acceptance_criteria>
  <done>
    `McpSection` aceita zero props, renderiza apenas o sub-block Cliente MCP, importa apenas `McpClientStatus` do shared/ipc-types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpar SettingsLayout.tsx removendo state/handlers/JSX do MCP Server</name>
  <files>apps/desktop/src/renderer/src/settings/SettingsLayout.tsx</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx (focar em L4 import, L125-128 state, L174-175 load handler, L459-482 handleMcpToggle, L585-593 JSX case)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-12, D-13)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx`:

    1. **Linha 4** — Remover `McpClientInfo` da lista de imports. Linha atual:
       ```tsx
       import type { WhisperModelOption, TtsProviderOption, WhisperDownloadProgress, LlmProvider, ReloadLlmRequest, KokoroDownloadProgress, McpClientInfo, QuietHoursConfig, FolderWatchConfig, DailySummaryConfig } from '../../../shared/ipc-types';
       ```
       Para:
       ```tsx
       import type { WhisperModelOption, TtsProviderOption, WhisperDownloadProgress, LlmProvider, ReloadLlmRequest, KokoroDownloadProgress, QuietHoursConfig, FolderWatchConfig, DailySummaryConfig } from '../../../shared/ipc-types';
       ```

    2. **Linhas 125-128** — REMOVER as 4 linhas:
       ```tsx
         // Phase 64 — MCP Server toggle (MCP-SRV-03, D-11, D-12)
         const [mcpEnabled, setMcpEnabled] = useState(false);
         const [mcpClients, setMcpClients] = useState<McpClientInfo[]>([]);
         const [mcpToggling, setMcpToggling] = useState(false);
       ```

    3. **Linhas 174-175** — REMOVER as 2 linhas do useEffect de load (dentro do `.then((data) => { ... })`):
       ```tsx
             // Phase 64 — MCP Server enabled
             setMcpEnabled(data.mcpServerEnabled ?? false);
       ```
       A linha anterior (`setScreenshotHotkey(data.screenshotHotkey ?? 'CmdOrCtrl+Shift+S');`) passa a ser seguida diretamente pelo bloco `// Phase 67 — Proactive settings`.

    4. **Linhas 459-482** — REMOVER a função `handleMcpToggle` inteira:
       ```tsx
         // Phase 64 — MCP server toggle handler (MCP-SRV-03, D-11, D-12, D-13)
         const handleMcpToggle = async (enabled: boolean): Promise<void> => {
           setMcpToggling(true);
           try {
             const result = await window.mcp?.toggle(enabled);
             if (result?.success) {
               setMcpEnabled(enabled);
               if (enabled) {
                 const clients = await window.mcp?.getConnectedClients() ?? [];
                 setMcpClients(clients);
               } else {
                 setMcpClients([]);
               }
             } else if (result && !result.success) {
               showToast('error', `Servidor MCP: ${result.error ?? 'Erro desconhecido'}`);
             }
           } catch (err: unknown) {
             const msg = err instanceof Error ? err.message : String(err);
             console.error('[McpSection] toggle error:', msg);
             showToast('error', `Servidor MCP: ${msg}`);
           } finally {
             setMcpToggling(false);
           }
         };
       ```
       A linha anterior (`};` que fecha `handleDailySummaryChange`) passa a ser seguida diretamente pelo comentário `// --- Section props (passed to section components) ---` (L484 atual).

    5. **Linhas 585-593** — REDUZIR o case `'mcp-server'` no `renderSection()`. Bloco atual:
       ```tsx
             case 'mcp-server':
               return (
                 <McpSection
                   enabled={mcpEnabled}
                   connectedClients={mcpClients}
                   onToggle={handleMcpToggle}
                   isToggling={mcpToggling}
                 />
               );
       ```
       Vira:
       ```tsx
             case 'mcp-server':
               return <McpSection />;
       ```

    Preservar intactos (per D-13, D-14):
    - Section key `'mcp-server'` no `SectionKey` union (L21) — Phase 70 remove
    - Entry `{ key: 'mcp-server', label: 'Servidor MCP', Icon: Server }` no `NAV_ITEMS` (L31) — Phase 70 remove
    - Import `import { McpSection } from './sections/McpSection';` (L12) — componente ainda renderiza (com zero props)
    - Import do ícone `Server` from 'lucide-react' (L2) — usado pelo nav item
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "mcpEnabled\|mcpClients\|mcpToggling\|handleMcpToggle\|setMcpEnabled\|McpClientInfo\|window.mcp?.toggle\|window.mcp?.getConnectedClients" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mcpEnabled" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "mcpClients" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "mcpToggling" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "handleMcpToggle" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "setMcpEnabled" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "McpClientInfo" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "window.mcp?.toggle\|window.mcp?.getConnectedClients" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "mcpServerEnabled" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0
    - `grep -c "<McpSection />" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 1 (zero props)
    - `grep -c "'mcp-server'" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → ≥2 (SectionKey union + nav item + case — preservado per D-13)
    - `grep -c "Servidor MCP" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 1 (label no NAV_ITEMS, preservado per D-14)
    - `grep -c "from './sections/McpSection'" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 1 (import preservado)
  </acceptance_criteria>
  <done>
    SettingsLayout sem state/handlers/JSX-props do MCP Server; `<McpSection />` montado sem props; section key e nav label preservados para Phase 70 remover.
  </done>
</task>

<task type="auto">
  <name>Task 3: Validar typecheck full + suite completa do desktop + gate MCP-RM-02</name>
  <files>(nenhum arquivo modificado — verificação final do Phase 69)</files>
  <read_first>
    - /root/jarvis/apps/desktop/package.json (scripts typecheck e test)
    - /root/jarvis/apps/backend-ts/package.json (gate test do client)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-16 — gate de regressão MCP-RM-02)
  </read_first>
  <action>
    Validar que Phase 69 completa todos os critérios de sucesso do ROADMAP §Phase 69:

    1. **Typecheck full do desktop:**
       ```
       cd /root/jarvis && pnpm --filter desktop typecheck
       ```
       Esperado: exit 0. Esta é a primeira vez na Phase 69 que typecheck deve passar full (main + preload + renderer) — Plan 02 deixou refs dangling de renderer que Plan 03 acabou de limpar. Erros esperados aqui indicam bugs nas edições anteriores das Tasks 1-2 deste plan:
       - "Cannot find name 'McpClientInfo'" → ainda há import residual
       - "Property 'mcpServerEnabled' does not exist on type 'SettingsData'" → algum local ainda usa data.mcpServerEnabled
       - "Property 'toggle' does not exist on type" → algum chamador residual de window.mcp?.toggle
       - "Type '{ ... }' is missing the following properties: enabled, connectedClients, onToggle, isToggling" → JSX ainda passa props
       
       Se algum erro aparecer, voltar para a Task que produziu o erro (Task 1 = McpSection.tsx; Task 2 = SettingsLayout.tsx) e corrigir.

    2. **Suite completa de testes do desktop:**
       ```
       cd /root/jarvis && pnpm --filter desktop test
       ```
       Esperado: exit 0. Inclui:
       - `src/main/ipc/__tests__/settings.test.ts` (já atualizado em Plan 02 Task 6)
       - `src/main/ipc/__tests__/chat.test.ts`, `chat-send-audio.test.ts`, `proactive.test.ts`
       - Qualquer outro test sob `apps/desktop/src/**/__tests__/`
       
       Se houver falha em outros tests (ex: algum mock de `SettingsData` que ainda tem `mcpServerEnabled`), atualizar conforme necessário — mas só os testes que falharem; não tocar nada que passa.

    3. **Gate de regressão MCP-RM-02 (critério 3 do ROADMAP §Phase 69):**
       ```
       cd /root/jarvis && pnpm --filter backend-ts test -- src/mcp/client/__tests__/e2e-mock-server.test.ts
       ```
       Esperado: exit 0. Como Plan 01, 02, 03 não tocam `apps/backend-ts/src/mcp/client/`, este teste deve passar inalterado. Se falhar, é sinal grave de que alguma deleção acidentalmente atingiu o client — investigar antes de declarar Phase 69 completo.

    4. **Build full do backend-ts (gate MCP-RM-01 backend):**
       ```
       cd /root/jarvis && pnpm --filter backend-ts test
       ```
       Esperado: exit 0. Confirma que nenhum test residual do backend referencia código apagado em Plan 01.

    5. **Grep agregado final** — confirmar que nenhum dos símbolos do MCP Server existe em todo o repo (excluindo `.planning/`):
       ```
       grep -rn --include="*.ts" --include="*.tsx" \
         "createMcpServer\|registerMemoryTools\|registerFileActionTools\|MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|mcpServerEnabled\|McpClientInfo\b" \
         /root/jarvis/apps/
       ```
       Esperado: zero matches. Se houver alguma referência sobrevivente, investigar e limpar.

    Se todos os 5 passos passarem, Phase 69 está completa atendendo MCP-RM-01 e MCP-RM-02. Documentar no SUMMARY.
  </action>
  <verify>
    <automated>cd /root/jarvis && pnpm --filter desktop typecheck && pnpm --filter desktop test && pnpm --filter backend-ts test -- src/mcp/client/__tests__/e2e-mock-server.test.ts && bash -c 'matches=$(grep -rn --include="*.ts" --include="*.tsx" "createMcpServer\|registerMemoryTools\|registerFileActionTools\|MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|mcpServerEnabled" apps/ 2>/dev/null | wc -l); test "$matches" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `cd /root/jarvis && pnpm --filter desktop typecheck` → exit 0 (full typecheck verde)
    - `cd /root/jarvis && pnpm --filter desktop test` → exit 0 (suite completa)
    - `cd /root/jarvis && pnpm --filter backend-ts test -- src/mcp/client/__tests__/e2e-mock-server.test.ts` → exit 0 (gate MCP-RM-02)
    - `cd /root/jarvis && pnpm --filter backend-ts test` → exit 0
    - `grep -rn --include="*.ts" --include="*.tsx" "createMcpServer\|registerMemoryTools\|registerFileActionTools\|MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|mcpServerEnabled" /root/jarvis/apps/ | wc -l` → 0
    - `grep -rn --include="*.ts" --include="*.tsx" "McpClientInfo" /root/jarvis/apps/ | wc -l` → 0 (type apagado, zero refs)
  </acceptance_criteria>
  <done>
    Phase 69 completa: MCP Server removido inteiramente do backend-ts (Plan 01) + Electron main/preload/shared (Plan 02) + Electron renderer (Plan 03); MCP Client preservado e validado pelo gate e2e-mock-server.test.ts; toda a suite typecheck+test passa; zero refs dangling no monorepo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer Settings UI ← state hooks/handlers do componente | Estado React local (`mcpEnabled`, `mcpClients`, `mcpToggling`) deixa de existir após Plan 03. Não há "data leftover" porque o componente unmount/remount já libera memória React. |
| Renderer → preload bridge (`window.mcp`) | Plan 02 removeu `toggle` e `getConnectedClients` do bridge. Plan 03 remove os call sites. Boundary surface shrinks atomicamente — nada chama o que não existe. |
| Componente McpSection ← prop interface | Antes: 4 props server-side. Agora: zero props. Acoplamento entre SettingsLayout e McpSection cai a zero — componente vira self-contained (lê via `window.mcp` nos useEffect). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-10 | Information Disclosure | State leftover de `mcpClients: McpClientInfo[]` no componente | accept | State React é heap-local; ao remover o `useState`, GC libera a referência. Não há persistência (electron-store) sendo limpa em Plan 03 — isso ficou no Plan 02 D-09. Severidade: zero. |
| T-69-11 | Tampering | Sub-block "Cliente MCP" no McpSection.tsx | mitigate | Plan 03 não toca o sub-block Cliente (linhas 124-146). Task 1 acceptance criteria valida que `reloadClient`/`getClientStatus`/`onClientStatusChanged` permanecem no arquivo. Task 3 valida que `e2e-mock-server.test.ts` (gate MCP-RM-02) continua passando. |
| T-69-12 | Denial of Service | JSX `<McpSection />` sem props após edição | mitigate | TypeScript exige que props sejam consistentes com a interface. Após Task 1 remover `McpSectionProps`, a assinatura passa a aceitar zero props — Task 2 reduz o JSX a `<McpSection />`. Task 3 valida com typecheck full. Se algum desalinhamento sobrar, typecheck bloqueia. Severidade: low. |
| T-69-13 | Information Disclosure | Section key/label "Servidor MCP" remanescente no nav | accept | D-13/D-14 do CONTEXT.md: nav label permanece intencionalmente para Phase 70 limpar junto com a seção LLM. Usuário ainda vê o item "Servidor MCP" no sidebar — mas ao clicar, vê apenas o sub-block "Cliente MCP". Não há vazamento (a info exibida é status MCP Client, não server). Severidade: zero (UX intencional, transiente até P70). |
| T-69-14 | Repudiation | Logs com "Phase 64" e "MCP server toggle" remanescentes em comentários | accept | Plan 03 não busca/limpa todos os comentários históricos "Phase 64" — apenas os blocos JSX/funções relacionadas a deletion. Comentários inertes não causam regressão. Phase 70 ou complete-milestone audit pode limpar. Severidade: zero. |

**Severidade geral:** Low. Deletion controlada de UI. Sub-block Cliente preservado e validado pelo gate de regressão.
</threat_model>

<verification>
- `grep -c "McpSectionProps\|onToggle\|connectedClients\|isToggling\|Servidor MCP" apps/desktop/src/renderer/src/settings/sections/McpSection.tsx` → 0 (Task 1)
- `grep -c "mcpEnabled\|mcpClients\|mcpToggling\|handleMcpToggle\|setMcpEnabled\|McpClientInfo\|window.mcp?.toggle\|window.mcp?.getConnectedClients\|mcpServerEnabled" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 0 (Task 2)
- `grep -c "<McpSection />" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` → 1 (Task 2 — zero props)
- `pnpm --filter desktop typecheck` exit 0 (Task 3)
- `pnpm --filter desktop test` exit 0 (Task 3)
- `pnpm --filter backend-ts test -- src/mcp/client/__tests__/e2e-mock-server.test.ts` exit 0 (Task 3 — gate MCP-RM-02)
- Grep agregado em apps/ por símbolos do MCP Server → zero matches (Task 3)
</verification>

<success_criteria>
1. `McpSection.tsx` aceita zero props; sub-block "Servidor MCP" (toggle + status + counter) ausente; sub-block "Cliente MCP" intacto.
2. `SettingsLayout.tsx` sem state hooks `mcpEnabled`/`mcpClients`/`mcpToggling`; sem função `handleMcpToggle`; sem import `McpClientInfo`; JSX `<McpSection />` com zero props.
3. Section key `'mcp-server'` e label "Servidor MCP" no nav PERMANECEM (D-13, D-14) — Phase 70 limpa.
4. `pnpm --filter desktop typecheck` exit 0 (full).
5. `pnpm --filter desktop test` exit 0 (suite completa).
6. `pnpm --filter backend-ts test -- src/mcp/client/__tests__/e2e-mock-server.test.ts` exit 0 — critério 3 do ROADMAP §Phase 69 e gate MCP-RM-02 satisfeitos.
7. Grep agregado em `/root/jarvis/apps/` por todos os símbolos do MCP Server (`createMcpServer`, `registerMemoryTools`, `registerFileActionTools`, `MCP_TOGGLE`, `MCP_GET_CONNECTED_CLIENTS`, `getMcpServerEnabled`, `setMcpServerEnabled`, `mcpServerEnabled`, `McpClientInfo`) → zero matches.
8. **Phase 69 complete:** MCP-RM-01 e MCP-RM-02 atendidos.
</success_criteria>

<output>
After completion, create `.planning/phases/69-mcp-server-removal/69-03-SUMMARY.md` documentando:
- 2 arquivos modificados (paths absolutos) com resumo das edições
- Estado preservado do sub-block "Cliente MCP" + section key 'mcp-server'
- Resultado de `pnpm --filter desktop typecheck` e `pnpm --filter desktop test`
- Resultado do gate `e2e-mock-server.test.ts` (MCP-RM-02 passou)
- Confirmação de zero matches no grep agregado (MCP-RM-01 atomicamente completo)
- Nota: Phase 70 vai remover section key 'mcp-server' + label "Servidor MCP" do nav
</output>
