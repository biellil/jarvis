---
phase: 69-mcp-server-removal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/ipc/mcp-settings.ts
  - apps/desktop/src/main/ipc/settings.ts
  - apps/desktop/src/main/store.ts
  - apps/desktop/src/preload/settings.ts
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/main/ipc/__tests__/settings.test.ts
autonomous: true
requirements:
  - MCP-RM-01

must_haves:
  truths:
    - "IPC handler `mcp:toggle` ausente do registro do main process (mcp-settings.ts)"
    - "IPC handler `mcp:get-connected-clients` ausente do registro do main process"
    - "Bridge `window.mcp` expõe apenas 3 métodos client-side (reloadClient, getClientStatus, onClientStatusChanged) — `toggle` e `getConnectedClients` ausentes"
    - "`IPC_CHANNELS.MCP_TOGGLE` e `IPC_CHANNELS.MCP_GET_CONNECTED_CLIENTS` ausentes de `shared/ipc-types.ts`"
    - "Type `McpClientInfo` ausente de `shared/ipc-types.ts` (só era usado por getConnectedClients)"
    - "Type `SettingsApi['mcp']` não declara `toggle`/`getConnectedClients` — apenas os 2 métodos client (reloadClient, getClientStatus)"
    - "Schema `StoreSchema.mcpServerEnabled` ausente de `store.ts`; accessors `getMcpServerEnabled`/`setMcpServerEnabled` ausentes"
    - "Payload de `settings:get` (handler em `ipc/settings.ts`) não contém mais a propriedade `mcpServerEnabled`"
    - "Type `SettingsData.mcpServerEnabled` ausente de `shared/ipc-types.ts`"
    - "Teste `ipc/__tests__/settings.test.ts` atualizado — mock `getMcpServerEnabled` removido e expectations não mencionam mais `mcpServerEnabled`"
    - "`pnpm --filter desktop typecheck` passa (Electron main + preload + shared)"
    - "`pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts` passa"
  artifacts:
    - path: "apps/desktop/src/main/ipc/mcp-settings.ts"
      provides: "MCP IPC handlers — apenas client-side (MCP_CLIENT_RELOAD, MCP_CLIENT_GET_STATUS, broadcast MCP_CLIENT_STATUS_CHANGED)"
      contains: "MCP_CLIENT_RELOAD"
      not_contains: "MCP_TOGGLE,MCP_GET_CONNECTED_CLIENTS,getMcpServerEnabled,setMcpServerEnabled"
    - path: "apps/desktop/src/main/ipc/settings.ts"
      provides: "Settings IPC sem mcpServerEnabled no payload"
      not_contains: "getMcpServerEnabled,mcpServerEnabled"
    - path: "apps/desktop/src/main/store.ts"
      provides: "Electron store sem MCP server schema/accessors"
      not_contains: "mcpServerEnabled,getMcpServerEnabled,setMcpServerEnabled"
    - path: "apps/desktop/src/preload/settings.ts"
      provides: "Bridge window.mcp enxuto (3 métodos client-side apenas)"
      contains: "reloadClient,getClientStatus,onClientStatusChanged"
      not_contains: "MCP_TOGGLE_CHANNEL,MCP_GET_CONNECTED_CLIENTS_CHANNEL,getConnectedClients"
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "Types IPC sem server-side surface"
      not_contains: "MCP_TOGGLE,MCP_GET_CONNECTED_CLIENTS,McpClientInfo"
    - path: "apps/desktop/src/main/ipc/__tests__/settings.test.ts"
      provides: "Teste de settings.ts atualizado sem mcpServerEnabled"
      not_contains: "getMcpServerEnabled,mcpServerEnabled"
  key_links:
    - from: "apps/desktop/src/preload/settings.ts"
      to: "window.mcp bridge (renderer)"
      via: "contextBridge.exposeInMainWorld('mcp', mcpWithSubscription)"
      pattern: "exposeInMainWorld\\(['\"]mcp['\"]"
      note: "Bridge preservado mas com apenas 3 métodos client"
    - from: "apps/desktop/src/main/ipc/mcp-settings.ts (setupMcpSettingsHandlers)"
      to: "IPC_CHANNELS.MCP_CLIENT_RELOAD, MCP_CLIENT_GET_STATUS"
      via: "ipcMain.handle"
      pattern: "ipcMain\\.handle.*MCP_CLIENT_"
      note: "Handlers client preservados (3 deles); 2 server removidos"
    - from: "apps/desktop/src/main/ipc/settings.ts SETTINGS_GET handler"
      to: "SettingsData payload"
      via: "object literal sem mcpServerEnabled"
      pattern: "settings:get.*mcpServerEnabled"
      note: "Property removida do return — renderer não recebe mais"
---

<objective>
Remover do processo Electron (main + preload + shared types) toda a superfície de IPC e store relacionada ao MCP **Server**. Apaga:
- 2 handlers IPC server-side (`mcp:toggle`, `mcp:get-connected-clients`) de `ipc/mcp-settings.ts`
- Schema field `mcpServerEnabled` e accessors `getMcpServerEnabled`/`setMcpServerEnabled` de `store.ts`
- Import + propriedade `mcpServerEnabled` do payload `settings:get` em `ipc/settings.ts`
- 2 canais (`MCP_TOGGLE`, `MCP_GET_CONNECTED_CLIENTS`) e o type `McpClientInfo` de `shared/ipc-types.ts`
- Propriedades `toggle`/`getConnectedClients` do type `SettingsApi['mcp']`
- Propriedade opcional `SettingsData.mcpServerEnabled`
- Canais inlined `MCP_TOGGLE_CHANNEL`/`MCP_GET_CONNECTED_CLIENTS_CHANNEL` e os métodos correspondentes do objeto `mcp` em `preload/settings.ts`
- Mock `getMcpServerEnabled` + expectations `mcpServerEnabled` em `ipc/__tests__/settings.test.ts`

Purpose: Atender o requisito MCP-RM-01 (parte Electron). Após Plan 01 ter apagado o backend-ts, a UI Electron mantinha um toggle que só persistia um booleano órfão — nada mais funcionalmente útil. Esta deleção é cirúrgica (D-01..D-10 do CONTEXT.md): manter o bridge `window.mcp` vivo com apenas os 3 métodos client (D-01) para que `McpSection.tsx` (sub-block Cliente MCP — Plan 03) continue funcionando sem refactor.

Preserve intacto: handlers `MCP_CLIENT_RELOAD`, `MCP_CLIENT_GET_STATUS`, broadcast `MCP_CLIENT_STATUS_CHANGED`; canais `MCP_CLIENT_*` em `IPC_CHANNELS`; type `McpClientStatus`; tudo do MCP Client (gate MCP-RM-02).

Sem migração ativa do electron-store (D-10): chave `mcpServerEnabled` órfã no settings.json de usuários é inócua — electron-store ignora chaves fora do schema.

Output: 6 arquivos modificados; `pnpm --filter desktop typecheck` verde; suite `ipc/__tests__/settings.test.ts` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-mcp-server-removal/69-CONTEXT.md
</context>

<interfaces>
<!-- Estado atual dos contratos a ajustar (extraídos com line numbers verificados no momento do planejamento) -->

apps/desktop/src/shared/ipc-types.ts:
  - L321-323: IPC_CHANNELS.MCP_TOGGLE = 'mcp:toggle', MCP_GET_CONNECTED_CLIENTS = 'mcp:get-connected-clients'  ← REMOVE
  - L324-327: IPC_CHANNELS.MCP_CLIENT_RELOAD, MCP_CLIENT_GET_STATUS, MCP_CLIENT_STATUS_CHANGED  ← KEEP
  - L433-435: SettingsData.mcpServerEnabled?: boolean  ← REMOVE
  - L466-471: interface McpClientInfo { name, connectedAt, lastActivity }  ← REMOVE (só usado por getConnectedClients)
  - L473-485: interface McpClientStatus { status, serverName, toolCount, error }  ← KEEP
  - L554-566: SettingsApi.mcp?: { toggle, getConnectedClients, reloadClient, getClientStatus }  ← KEEP shape mas remover toggle + getConnectedClients

apps/desktop/src/preload/settings.ts:
  - L2: import McpClientInfo  ← REMOVE (não mais usado)
  - L109-113: comentário Phase 64 + const MCP_TOGGLE_CHANNEL = 'mcp:toggle'; const MCP_GET_CONNECTED_CLIENTS_CHANNEL  ← REMOVE
  - L114-117: MCP_CLIENT_* channels  ← KEEP
  - L119-125: const mcp: SettingsApi['mcp'] = { toggle, getConnectedClients, reloadClient, getClientStatus }  ← REMOVE toggle + getConnectedClients
  - L127-143: McpRendererApi + mcpWithSubscription  ← KEEP (3 métodos client agora)
  - L145: contextBridge.exposeInMainWorld('mcp', mcpWithSubscription)  ← KEEP

apps/desktop/src/main/ipc/mcp-settings.ts (arquivo inteiro lido):
  - L14: import McpClientInfo  ← REMOVE (handler getConnectedClients sai)
  - L15: import { getMcpServerEnabled, setMcpServerEnabled }  ← REMOVE inteiro (não importa mais nada do store)
  - L34-49: ipcMain.handle(MCP_TOGGLE, ...)  ← REMOVE
  - L51-56: ipcMain.handle(MCP_GET_CONNECTED_CLIENTS, ...)  ← REMOVE
  - L62-90: ipcMain.handle(MCP_CLIENT_RELOAD, ...)  ← KEEP
  - L92-105: ipcMain.handle(MCP_CLIENT_GET_STATUS, ...)  ← KEEP
  - L20-26: broadcastClientStatus helper  ← KEEP

apps/desktop/src/main/ipc/settings.ts:
  - L43: import getMcpServerEnabled  ← REMOVE da lista de imports
  - L100-101: linha "mcpServerEnabled: getMcpServerEnabled()" (com comentário "Phase 64 — MCP Server toggle ...")  ← REMOVE

apps/desktop/src/main/store.ts:
  - L72-74: comentário "Phase 64 — MCP Server enabled flag" + linha `mcpServerEnabled?: boolean`  ← REMOVE
  - L448-460: bloco "Phase 64 — MCP Server enabled flag" com getMcpServerEnabled + setMcpServerEnabled  ← REMOVE

apps/desktop/src/main/ipc/__tests__/settings.test.ts:
  - L86-87: bloco "Phase 62+ — MCP server flag" + linha `getMcpServerEnabled: () => false,`  ← REMOVE
  - L301-302: expectation `mcpServerEnabled: false,` no result de SETTINGS_GET "returns all four settings fields"  ← REMOVE
  - L336-337: expectation `mcpServerEnabled: false,` no result de SETTINGS_GET "returns defaults"  ← REMOVE
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remover handlers server-side de mcp-settings.ts</name>
  <files>apps/desktop/src/main/ipc/mcp-settings.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts (arquivo completo — 106 linhas)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-07)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` com as seguintes mudanças exatas:

    1. **Atualizar comentário de cabeçalho do arquivo (linhas 1-12)** — remover a seção que descreve "Phase 64 handlers (server-side)" e ajustar para refletir apenas Phase 65 client. Novo cabeçalho:
       ```ts
       /**
        * MCP Settings IPC Handlers — Phase 65 (MCP-CLI-01)
        *
        * Phase 65 handlers (client-side):
        *   mcp-client:reload        — POSTs to backend /internal/mcp-client/reload, broadcasts result
        *   mcp-client:get-status    — GETs from backend /internal/mcp-client/status (cached read)
        *   mcp-client:status-changed — push channel (no handler; broadcast from reload result)
        *
        * Phase 69: MCP Server handlers (mcp:toggle, mcp:get-connected-clients) removed.
        */
       ```

    2. **Linha 14** — alterar a linha de import:
       ```ts
       import { IPC_CHANNELS, type McpClientInfo, type McpClientStatus } from '../../shared/ipc-types.js';
       ```
       para:
       ```ts
       import { IPC_CHANNELS, type McpClientStatus } from '../../shared/ipc-types.js';
       ```
       (remover `type McpClientInfo` da lista — Plan 02 Task 5 vai apagar esse type de `ipc-types.ts`).

    3. **Linha 15** — REMOVER completamente a linha:
       ```ts
       import { getMcpServerEnabled, setMcpServerEnabled } from '../store.js';
       ```
       Após esta remoção, o arquivo não importa mais nada de `../store.js`.

    4. **Bloco linhas 29-56** — REMOVER completamente o seguinte bloco (incluindo o comentário separador):
       ```ts
         // ============================================
         // Phase 64 — MCP Server (preserved untouched)
         // ============================================

         // Toggle MCP server enable/disable preference
         ipcMain.handle(IPC_CHANNELS.MCP_TOGGLE, async (_event, enabled: boolean): Promise<{
           success: boolean;
           status: 'started' | 'stopped' | 'unchanged';
           error?: string;
         }> => {
           try {
             const current = getMcpServerEnabled();
             if (current === enabled) {
               return { success: true, status: 'unchanged' };
             }
             setMcpServerEnabled(enabled);
             return { success: true, status: enabled ? 'started' : 'stopped' };
           } catch (err) {
             return { success: false, status: 'unchanged', error: (err as Error).message };
           }
         });

         // Get connected clients list
         // Phase 64: stdio has no back-channel, returns empty list.
         // v3.1 (HTTP Streamable) will populate this from real session data.
         ipcMain.handle(IPC_CHANNELS.MCP_GET_CONNECTED_CLIENTS, async (): Promise<McpClientInfo[]> => {
           return [];
         });
       ```

    5. **Linhas 58-60** — REMOVER apenas o separador "Phase 65 — MCP Client (new)" (vira o único bloco do arquivo, separador redundante). Substituir por uma linha em branco simples antes do primeiro `ipcMain.handle(IPC_CHANNELS.MCP_CLIENT_RELOAD, ...)`.

    O arquivo final deve ter `setupMcpSettingsHandlers()` com EXATAMENTE 3 chamadas a `ipcMain.handle` (todas client-side: `MCP_CLIENT_RELOAD`, `MCP_CLIENT_GET_STATUS`) e o helper `broadcastClientStatus` preservado. Nota: `MCP_CLIENT_STATUS_CHANGED` é um canal de broadcast (não tem handler `.handle()`), só é emitido por `broadcastClientStatus()`.

    Não renomear o arquivo (D-07 do CONTEXT.md: rename para `mcp-client-settings.ts` é deferido para limpeza pós-P70).
  </action>
  <verify>
    <automated>cd /root/jarvis && grep -c "ipcMain.handle" apps/desktop/src/main/ipc/mcp-settings.ts | grep -q "^2$" && ! grep -q "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled" apps/desktop/src/main/ipc/mcp-settings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ipcMain.handle" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → 2 (apenas MCP_CLIENT_RELOAD e MCP_CLIENT_GET_STATUS)
    - `grep -c "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → 0
    - `grep -c "getMcpServerEnabled\|setMcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → 0
    - `grep -c "from '../store.js'" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → 0 (não importa mais do store)
    - `grep -c "McpClientInfo" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → 0
    - `grep -c "MCP_CLIENT_RELOAD\|MCP_CLIENT_GET_STATUS\|MCP_CLIENT_STATUS_CHANGED" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → ≥3 (preservados — uso em handle + broadcast)
    - `grep -c "setupMcpSettingsHandlers" /root/jarvis/apps/desktop/src/main/ipc/mcp-settings.ts` → ≥1 (função export preservada)
  </acceptance_criteria>
  <done>
    `mcp-settings.ts` reduzido a setup de 3 handlers client-side + helper de broadcast; zero referências a símbolos server-side; sem import dangling de `../store.js`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remover mcpServerEnabled de ipc/settings.ts (payload + import)</name>
  <files>apps/desktop/src/main/ipc/settings.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/main/ipc/settings.ts (focar em §10-44 imports e §66-103 SETTINGS_GET handler)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-08)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/main/ipc/settings.ts`:

    1. **Linha 43** — REMOVER a linha exata `  getMcpServerEnabled,` da lista de imports do bloco `import { ... } from '../store';` (linhas 10-44). Após a edição, a lista de imports não deve conter `getMcpServerEnabled`.

    2. **Linhas 100-101** — REMOVER as duas linhas:
       ```ts
             // Phase 64 — MCP Server toggle (MCP-SRV-03, D-11)
             mcpServerEnabled: getMcpServerEnabled(),
       ```
       no objeto literal retornado pelo handler `IPC_CHANNELS.SETTINGS_GET`. A linha anterior (`screenshotHotkey: getScreenshotHotkey(),`) passa a ser a última propriedade antes do `}` que fecha o `return {`.

    Atenção: NÃO confundir com a entrada `screenshotHotkey` (linha 99) — só remover o comentário "Phase 64 — MCP Server toggle" e a linha imediatamente abaixo.

    Não tocar em mais nada do arquivo — todos os outros handlers (`SETTINGS_SAVE`, `LM_STUDIO_SET_URL`, `LLM_SET_PROVIDER`, `RELOAD_LLM`, etc.) permanecem intactos.
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "getMcpServerEnabled\|mcpServerEnabled" apps/desktop/src/main/ipc/settings.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "getMcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/settings.ts` → 0
    - `grep -c "mcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/settings.ts` → 0
    - `grep -c "Phase 64" /root/jarvis/apps/desktop/src/main/ipc/settings.ts` → 0 (o comentário "Phase 64 — MCP Server toggle" sai junto)
    - `grep -c "screenshotHotkey: getScreenshotHotkey()" /root/jarvis/apps/desktop/src/main/ipc/settings.ts` → 1 (linha vizinha preservada, regressão check)
    - `grep -c "IPC_CHANNELS.SETTINGS_GET" /root/jarvis/apps/desktop/src/main/ipc/settings.ts` → ≥1 (handler ainda registrado)
  </acceptance_criteria>
  <done>
    `getMcpServerEnabled` removido dos imports e do payload de `settings:get`; nenhum outro handler afetado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apagar schema + accessors mcpServerEnabled de store.ts</name>
  <files>apps/desktop/src/main/store.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/main/store.ts (focar em §72-74 schema e §448-460 accessors)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-09, D-10 — sem migração ativa)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/main/store.ts`:

    1. **Linhas 72-74** — REMOVER as três linhas exatas:
       ```ts
         // Phase 64 — MCP Server enabled flag (MCP-SRV-03, D-11)
         /** When true, MCP stdio server is enabled. Default: false. */
         mcpServerEnabled?: boolean;
       ```
       da interface `StoreSchema`. Após remoção, a linha anterior (`screenshotHotkey?: HotkeyConfig;` na L71) passa a ser seguida diretamente pela próxima entrada do schema (`// Phase 67 — Proactive settings...` na L75).

    2. **Linhas 448-460** — REMOVER o bloco inteiro:
       ```ts
       // ============================================================
       // Phase 64 — MCP Server enabled flag (MCP-SRV-03, D-11)
       // Default: false — MCP server is off until user enables it.
       // Pattern mirrors getStreamingTtsEnabled/setStreamingTtsEnabled verbatim.
       // ============================================================

       export function getMcpServerEnabled(): boolean {
         return store.get('mcpServerEnabled', false);
       }

       export function setMcpServerEnabled(enabled: boolean): void {
         store.set('mcpServerEnabled', enabled);
       }
       ```
       O bloco anterior (`Phase 63 — Screenshot hotkey`) termina antes; o bloco seguinte (`Phase 67 — Proactive Settings`) começa depois. Deixar exatamente uma linha em branco entre o bloco que sobrevive antes e o que sobrevive depois.

    **Justificativa D-10 (sem migração):** chave `mcpServerEnabled` em settings.json de usuários existentes ficará órfã. `electron-store` ignora chaves fora do schema — não causa crash, não vaza nada (era apenas um booleano cosmético; o stdio server nunca rodou). Plano 69 não escreve código de migração. Mesma filosofia do Phase 68 D-07 (read-time normalization sem migração ativa).

    Não alterar mais nada em `store.ts` — todos os outros accessors (Phase 23, 34, 39, 40, 52, 53, 57, 60, 62, 63, 67) ficam idênticos.
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "mcpServerEnabled\|getMcpServerEnabled\|setMcpServerEnabled" apps/desktop/src/main/store.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mcpServerEnabled" /root/jarvis/apps/desktop/src/main/store.ts` → 0
    - `grep -c "getMcpServerEnabled" /root/jarvis/apps/desktop/src/main/store.ts` → 0
    - `grep -c "setMcpServerEnabled" /root/jarvis/apps/desktop/src/main/store.ts` → 0
    - `grep -c "Phase 64" /root/jarvis/apps/desktop/src/main/store.ts` → 0 (comentários da Phase 64 saem junto)
    - `grep -c "Phase 67 — Proactive Settings" /root/jarvis/apps/desktop/src/main/store.ts` → ≥1 (regressão check — bloco vizinho preservado)
    - `grep -c "screenshotHotkey" /root/jarvis/apps/desktop/src/main/store.ts` → ≥2 (regressão check — accessor + schema preservados)
  </acceptance_criteria>
  <done>
    Schema field `mcpServerEnabled` removido, accessors `getMcpServerEnabled`/`setMcpServerEnabled` removidos, comentários "Phase 64" relacionados ao MCP Server removidos, demais accessors intactos.
  </done>
</task>

<task type="auto">
  <name>Task 4: Strip métodos server-side do bridge window.mcp no preload</name>
  <files>apps/desktop/src/preload/settings.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/preload/settings.ts (focar em §1-2 imports e §109-145 MCP bridge)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/preload/settings.ts`:

    1. **Linha 2** — REMOVER `McpClientInfo` da lista de imports do `import type { ... }`. A linha atual é:
       ```ts
       import type { SettingsApi, WhisperApi, WhisperDownloadProgress, KokoroApi, KokoroDownloadProgress, McpClientInfo, McpClientStatus, ProactiveEvent } from '../shared/ipc-types';
       ```
       Vira:
       ```ts
       import type { SettingsApi, WhisperApi, WhisperDownloadProgress, KokoroApi, KokoroDownloadProgress, McpClientStatus, ProactiveEvent } from '../shared/ipc-types';
       ```

    2. **Linhas 109-113** — REMOVER:
       ```ts
       // Phase 64 (MCP-SRV-03) — MCP server control bridge.
       // Phase 65 (MCP-CLI-01, D-10) — extends with MCP client reload + status + status push.
       // Inlined channel strings to avoid shared chunk extraction (same pattern as whisper/kokoro above).
       const MCP_TOGGLE_CHANNEL = 'mcp:toggle';
       const MCP_GET_CONNECTED_CLIENTS_CHANNEL = 'mcp:get-connected-clients';
       ```
       Substituir esse bloco de 5 linhas por:
       ```ts
       // Phase 65 (MCP-CLI-01, D-10) — MCP client bridge (reload + status + status push).
       // Phase 69: MCP server bridge (toggle, getConnectedClients) removed.
       // Inlined channel strings to avoid shared chunk extraction (same pattern as whisper/kokoro above).
       ```

    3. **Linhas 119-125** — REMOVER `toggle` e `getConnectedClients` do objeto `mcp`. O bloco atual é:
       ```ts
       const mcp: SettingsApi['mcp'] = {
         toggle: (enabled: boolean) => ipcRenderer.invoke(MCP_TOGGLE_CHANNEL, enabled) as Promise<{ success: boolean; status: 'started' | 'stopped' | 'unchanged'; error?: string }>,
         getConnectedClients: () => ipcRenderer.invoke(MCP_GET_CONNECTED_CLIENTS_CHANNEL) as Promise<McpClientInfo[]>,
         // Phase 65 (MCP-CLI-01, D-10) — reload + status reads (handlers in Plan 03 Task 4)
         reloadClient: () => ipcRenderer.invoke(MCP_CLIENT_RELOAD_CHANNEL) as Promise<McpClientStatus>,
         getClientStatus: () => ipcRenderer.invoke(MCP_CLIENT_GET_STATUS_CHANNEL) as Promise<McpClientStatus>,
       };
       ```
       Vira:
       ```ts
       const mcp: SettingsApi['mcp'] = {
         // Phase 65 (MCP-CLI-01, D-10) — reload + status reads
         reloadClient: () => ipcRenderer.invoke(MCP_CLIENT_RELOAD_CHANNEL) as Promise<McpClientStatus>,
         getClientStatus: () => ipcRenderer.invoke(MCP_CLIENT_GET_STATUS_CHANNEL) as Promise<McpClientStatus>,
       };
       ```

    Manter intactos: `MCP_CLIENT_RELOAD_CHANNEL`, `MCP_CLIENT_GET_STATUS_CHANNEL`, `MCP_CLIENT_STATUS_CHANGED_CHANNEL` consts (L115-117); interface `McpRendererApi` e `mcpWithSubscription` (L127-143); `contextBridge.exposeInMainWorld('mcp', mcpWithSubscription)` (L145).

    Após a edição, o bridge `window.mcp` exposto ao renderer terá exatamente 3 métodos: `reloadClient`, `getClientStatus`, `onClientStatusChanged`.
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "MCP_TOGGLE_CHANNEL\|MCP_GET_CONNECTED_CLIENTS_CHANNEL\|McpClientInfo\|getConnectedClients" apps/desktop/src/preload/settings.ts && grep -c "reloadClient\|getClientStatus\|onClientStatusChanged" apps/desktop/src/preload/settings.ts | awk '$1 >= 3 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MCP_TOGGLE_CHANNEL" /root/jarvis/apps/desktop/src/preload/settings.ts` → 0
    - `grep -c "MCP_GET_CONNECTED_CLIENTS_CHANNEL" /root/jarvis/apps/desktop/src/preload/settings.ts` → 0
    - `grep -c "McpClientInfo" /root/jarvis/apps/desktop/src/preload/settings.ts` → 0
    - `grep -c "toggle:\s*(enabled" /root/jarvis/apps/desktop/src/preload/settings.ts` → 0 (método toggle removido)
    - `grep -c "getConnectedClients" /root/jarvis/apps/desktop/src/preload/settings.ts` → 0
    - `grep -c "reloadClient" /root/jarvis/apps/desktop/src/preload/settings.ts` → ≥1 (preservado)
    - `grep -c "getClientStatus" /root/jarvis/apps/desktop/src/preload/settings.ts` → ≥1
    - `grep -c "onClientStatusChanged" /root/jarvis/apps/desktop/src/preload/settings.ts` → ≥1
    - `grep -c "exposeInMainWorld\\(['\"]mcp['\"]" /root/jarvis/apps/desktop/src/preload/settings.ts` → 1 (bridge preservado)
  </acceptance_criteria>
  <done>
    Bridge `window.mcp` exposto com apenas 3 métodos client-side; canais `MCP_TOGGLE_CHANNEL`/`MCP_GET_CONNECTED_CLIENTS_CHANNEL` e import `McpClientInfo` removidos; preload compila.
  </done>
</task>

<task type="auto">
  <name>Task 5: Strip types server-side de shared/ipc-types.ts</name>
  <files>apps/desktop/src/shared/ipc-types.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/shared/ipc-types.ts (focar em §321-327 IPC_CHANNELS, §433-440 SettingsData, §465-485 McpClientInfo/McpClientStatus, §554-567 SettingsApi.mcp)
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-03)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/shared/ipc-types.ts`:

    1. **Linhas 321-323** — REMOVER as 3 linhas:
       ```ts
         // Phase 64 — MCP Server (MCP-SRV-03)
         MCP_TOGGLE: 'mcp:toggle',
         MCP_GET_CONNECTED_CLIENTS: 'mcp:get-connected-clients',
       ```
       do objeto `IPC_CHANNELS`. As linhas seguintes (`// Phase 65 — MCP Client (MCP-CLI-01, D-10)` + os 3 canais `MCP_CLIENT_*`) permanecem intactas.

    2. **Linhas 433-435** — REMOVER:
       ```ts
         // Phase 64 — MCP Server toggle (MCP-SRV-03, D-11)
         /** Whether MCP server is enabled. Default: false. */
         mcpServerEnabled?: boolean;
       ```
       da interface `SettingsData`. A linha anterior (`screenshotHotkey: string;` na L432) passa a preceder diretamente o próximo bloco (`// Phase 67 — Proactive settings...`).

    3. **Linhas 465-471** — REMOVER inteiramente:
       ```ts
       // Phase 64 — MCP Server client tracking (MCP-SRV-03, D-08, D-09)
       export interface McpClientInfo {
         /** Display name — "Unknown Client via stdio" for Phase 64 stdio clients */
         name: string;
         connectedAt: string;   // ISO 8601 string (serializable over IPC)
         lastActivity: string;  // ISO 8601 string
       }
       ```
       Esse tipo só era usado como retorno de `getConnectedClients()` (já removido em Task 4 do preload e Task 1 do mcp-settings.ts). Manter `McpClientStatus` (L473-485 — usado pelo MCP Client).

    4. **Linhas 554-566** — Substituir o bloco `mcp?: { ... }` do `SettingsApi`. Bloco atual:
       ```ts
         // Phase 64 — MCP Server toggle + client status (MCP-SRV-03, D-11)
         // Exposed separately via window.mcp contextBridge (not part of window.settings)
         mcp?: {
           /** Enable or disable the MCP stdio server. Returns status. */
           toggle: (enabled: boolean) => Promise<{ success: boolean; status: 'started' | 'stopped' | 'unchanged'; error?: string }>;
           /** Get list of currently connected MCP clients. */
           getConnectedClients: () => Promise<McpClientInfo[]>;
           // Phase 65 — MCP Client reload + status (MCP-CLI-01, D-10)
           /** Trigger backend to disconnect and reconnect to the configured MCP server. */
           reloadClient: () => Promise<McpClientStatus>;
           /** Get current MCP client connection status (cached, no network call). */
           getClientStatus: () => Promise<McpClientStatus>;
         };
       ```
       Vira:
       ```ts
         // Phase 65 — MCP Client reload + status (MCP-CLI-01, D-10)
         // Exposed separately via window.mcp contextBridge (not part of window.settings).
         // Phase 69: server-side methods (toggle, getConnectedClients) removed.
         mcp?: {
           /** Trigger backend to disconnect and reconnect to the configured MCP server. */
           reloadClient: () => Promise<McpClientStatus>;
           /** Get current MCP client connection status (cached, no network call). */
           getClientStatus: () => Promise<McpClientStatus>;
         };
       ```

    Não tocar em mais nada — `McpClientStatus` (L473-485), `IPC_CHANNELS.MCP_CLIENT_RELOAD`/`MCP_CLIENT_GET_STATUS`/`MCP_CLIENT_STATUS_CHANGED` (L324-327), declaração global `window.mcp?: SettingsApi['mcp']` (L656) permanecem intactos.
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|interface McpClientInfo\|mcpServerEnabled" apps/desktop/src/shared/ipc-types.ts && grep -c "MCP_CLIENT_RELOAD\|McpClientStatus" apps/desktop/src/shared/ipc-types.ts | awk '$1 >= 2 { exit 0 } { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MCP_TOGGLE" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 0
    - `grep -c "MCP_GET_CONNECTED_CLIENTS" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 0
    - `grep -c "interface McpClientInfo" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 0
    - `grep -c "McpClientInfo" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 0 (também não aparece em SettingsApi.mcp)
    - `grep -c "mcpServerEnabled" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 0
    - `grep -c "MCP_CLIENT_RELOAD\|MCP_CLIENT_GET_STATUS\|MCP_CLIENT_STATUS_CHANGED" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → ≥3 (preservados)
    - `grep -c "interface McpClientStatus" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → 1 (preservado)
    - `grep -c "reloadClient\|getClientStatus" /root/jarvis/apps/desktop/src/shared/ipc-types.ts` → ≥2 (preservados em SettingsApi.mcp)
  </acceptance_criteria>
  <done>
    Canais `MCP_TOGGLE`/`MCP_GET_CONNECTED_CLIENTS`, type `McpClientInfo`, propriedade `SettingsData.mcpServerEnabled` e métodos `toggle`/`getConnectedClients` do `SettingsApi.mcp` removidos. `McpClientStatus` e canais `MCP_CLIENT_*` intactos.
  </done>
</task>

<task type="auto">
  <name>Task 6: Atualizar mock e expectations em ipc/__tests__/settings.test.ts</name>
  <files>apps/desktop/src/main/ipc/__tests__/settings.test.ts</files>
  <read_first>
    - /root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts (focar em §86-88 mock store, §275-303 expectations SETTINGS_GET "returns all four fields", §310-338 "returns defaults")
    - /root/jarvis/.planning/phases/69-mcp-server-removal/69-CONTEXT.md (D-17)
  </read_first>
  <action>
    Editar `/root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts`:

    1. **Linhas 86-88** — REMOVER do bloco `vi.mock('../../store', () => ({ ... }))`:
       ```ts
         // Phase 62+ — MCP server flag
         getMcpServerEnabled: () => false,
       ```
       (são as 2 últimas linhas dentro do mock, imediatamente antes do `}))`). Após remoção, a linha que sobra antes do fechamento `}))` é `setScreenshotHotkey: vi.fn(),`.

       Atenção: o comentário usa o label "Phase 62+ — MCP server flag" (foi um label confuso historicamente), mas a chave é `getMcpServerEnabled` — essa é a chave que precisa sair.

    2. **Linhas 301-302** — REMOVER do objeto `result` esperado no teste `it('returns all four settings fields from store', ...)`:
       ```ts
             // Phase 64 — MCP Server toggle
             mcpServerEnabled: false,
       ```
       (são as 2 últimas linhas antes do `});` que fecha o `expect(result).toEqual({...})`).

    3. **Linhas 336-337** — REMOVER do objeto `result` esperado no teste `it('returns defaults when store has no overrides', ...)`:
       ```ts
             // Phase 64 — MCP Server toggle
             mcpServerEnabled: false,
       ```
       (também as 2 últimas linhas antes do `});` que fecha esse `expect(result).toEqual({...})`).

    4. **Grep final defensivo:** após as 3 edições, rodar:
       ```
       grep -n "mcpServerEnabled\|getMcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts
       ```
       Deve retornar zero matches. Se restar algum, é resíduo a limpar.

    Não tocar em mais nada — todos os outros tests (Phase 23, 34, 40, 53 etc.) ficam idênticos.
  </action>
  <verify>
    <automated>cd /root/jarvis && ! grep -q "mcpServerEnabled\|getMcpServerEnabled" apps/desktop/src/main/ipc/__tests__/settings.test.ts && pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts` → 0
    - `grep -c "getMcpServerEnabled" /root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts` → 0
    - `cd /root/jarvis && pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts` → exit 0 (suite passa após edição)
    - `grep -c "Phase 64" /root/jarvis/apps/desktop/src/main/ipc/__tests__/settings.test.ts` → 0 (comentários "Phase 64 — MCP Server toggle" também saem)
  </acceptance_criteria>
  <done>
    Mock `getMcpServerEnabled` removido; expectations sem `mcpServerEnabled`; suite de testes passa.
  </done>
</task>

<task type="auto">
  <name>Task 7: Validar typecheck e suite completa do desktop</name>
  <files>(nenhum arquivo modificado — verificação)</files>
  <read_first>
    - /root/jarvis/apps/desktop/tsconfig.json (confirmar paths de typecheck)
    - /root/jarvis/apps/desktop/package.json (confirmar scripts `typecheck` e `test`)
  </read_first>
  <action>
    Validar que as edições das Tasks 1-6 são consistentes (sem refs dangling, sem types órfãos):

    1. **Typecheck do desktop:**
       ```
       cd /root/jarvis && pnpm --filter desktop typecheck
       ```
       Esperado: exit 0. Erros típicos a procurar se falhar:
       - "Cannot find name 'McpClientInfo'" → algum arquivo ainda importa o type apagado
       - "Property 'mcpServerEnabled' does not exist" → algum consumer do SettingsData ainda referencia
       - "Cannot find name 'MCP_TOGGLE'" / "MCP_GET_CONNECTED_CLIENTS" → renderer ou outro arquivo do main ainda usa
       
       Se houver erros, investigar e corrigir antes de prosseguir. O Plan 03 vai limpar o renderer (SettingsLayout.tsx + McpSection.tsx) — então é ESPERADO que aqui, sem Plan 03 ainda rodado, possa haver erros vindos do renderer (já que renderer ainda referencia `mcpServerEnabled` em data.mcpServerEnabled, `window.mcp.toggle`, `window.mcp.getConnectedClients`, e `McpClientInfo`).
       
       **IMPORTANTE:** Como Plan 02 e Plan 03 são wave-dependentes (Plan 03 depende-de Plan 02), o typecheck full do desktop pode falhar até Plan 03 rodar. Para validar Plan 02 isoladamente, executar:
       ```
       cd /root/jarvis && npx tsc --noEmit --project apps/desktop/tsconfig.main.json 2>&1 | head -50
       cd /root/jarvis && npx tsc --noEmit --project apps/desktop/tsconfig.preload.json 2>&1 | head -50
       ```
       Os projetos main e preload devem passar isoladamente. O projeto renderer falha até Plan 03. Se `tsconfig.main.json` ou `tsconfig.preload.json` não existirem como projects separados, fazer fallback: `pnpm --filter desktop typecheck` pode falhar parcialmente; aceitar erros que mencionam apenas arquivos sob `src/renderer/` (eles serão resolvidos por Plan 03).

    2. **Suite de testes desktop — só os tests de main/ipc:**
       ```
       cd /root/jarvis && pnpm --filter desktop test -- src/main/ipc/__tests__/
       ```
       Esperado: exit 0. Inclui `settings.test.ts` (Task 6), `chat.test.ts`, `chat-send-audio.test.ts`, `proactive.test.ts`.

    3. **Grep final por refs dangling no main+preload+shared:**
       ```
       grep -rn "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|interface McpClientInfo\|: McpClientInfo\|mcpServerEnabled" /root/jarvis/apps/desktop/src/main /root/jarvis/apps/desktop/src/preload /root/jarvis/apps/desktop/src/shared 2>/dev/null
       ```
       Esperado: zero matches. Se houver, é resíduo a limpar.

    Se algum dos passos falhar fora do esperado (i.e., erro em main/preload/shared, não em renderer/), NÃO criar arquivos — voltar para a Task que produziu o erro.
  </action>
  <verify>
    <automated>cd /root/jarvis && pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts && bash -c 'count=$(grep -rn "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|mcpServerEnabled" apps/desktop/src/main apps/desktop/src/preload apps/desktop/src/shared 2>/dev/null | wc -l); test "$count" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - `cd /root/jarvis && pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts` → exit 0
    - `grep -rn "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|getMcpServerEnabled\|setMcpServerEnabled\|mcpServerEnabled" /root/jarvis/apps/desktop/src/main /root/jarvis/apps/desktop/src/preload /root/jarvis/apps/desktop/src/shared | wc -l` → 0
    - `grep -rn "interface McpClientInfo\|: McpClientInfo\|McpClientInfo\\[\\]" /root/jarvis/apps/desktop/src/main /root/jarvis/apps/desktop/src/preload /root/jarvis/apps/desktop/src/shared | wc -l` → 0
    - Nota: typecheck full (`pnpm --filter desktop typecheck`) pode falhar até Plan 03 rodar — documentar a observação no SUMMARY se o gate parcial typecheck-main ou typecheck-preload existir, ou aceitar o full typecheck como gate post-Plan-03.
  </acceptance_criteria>
  <done>
    Plan 02 entrega Electron main + preload + shared types limpos; suite ipc/__tests__/settings.test.ts passa; zero refs dangling sob main/preload/shared. Renderer ainda quebrado (esperado — Plan 03 conserta).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → main IPC (via preload contextBridge) | Renderer chama `window.mcp.*` que vai para `ipcMain.handle(IPC_CHANNELS.MCP_*)`. Plan 02 reduz a superfície de IPC server-side a zero — só client-side resta. |
| Main → electron-store (filesystem) | Antes: `setMcpServerEnabled` escrevia booleano. Agora: nada escreve `mcpServerEnabled` — chave morre por inanição. |
| Renderer ←→ main (D-13 — bridge survives) | `window.mcp` continua exposto com 3 métodos client. Removidos: `toggle`, `getConnectedClients`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-05 | Tampering | electron-store settings.json (chave órfã `mcpServerEnabled`) | accept | D-10 do CONTEXT.md: electron-store ignora chaves fora do schema; chave órfã não causa crash, não vaza dados (o stdio nunca rodou). Sem migração ativa por design — minimiza risco de migração mal-feita corromper outras chaves. Severidade: zero. |
| T-69-06 | Denial of Service | Renderer chamando `window.mcp.toggle(true)` após upgrade | mitigate | Plan 03 (Wave 2) remove a chamada do SettingsLayout.tsx. Entre Plan 02 e Plan 03 o renderer fica quebrado em typecheck — mas é estado transiente intra-phase, nunca ship. Acceptance criteria de Task 7 documenta que typecheck full só passa após Plan 03. |
| T-69-07 | Information Disclosure | `getMcpServerEnabled()` removido | accept | A função retornava apenas um booleano (`true`/`false`) sem PII. Remoção não vaza nada. Severidade: zero. |
| T-69-08 | Elevation of Privilege | Bridge `window.mcp.toggle` removida | mitigate | Antes: renderer podia invocar `mcp:toggle` e persistir um flag — não havia escalation real (handler era no-op cosmético per scout). Agora: renderer não pode mais invocar o canal (não existe). Severidade: low → zero. |
| T-69-09 | Repudiation | Mock test outdated | mitigate | Task 6 atualiza o teste `settings.test.ts` para refletir o novo contrato. Sem isso, suite passaria com expectations falsas. Severidade: low (apenas DX). |

**Severidade geral:** Low. Deletion controlada do surface IPC; sem dados sensíveis; bridge `window.mcp` preservado com surface reduzida.
</threat_model>

<verification>
- `grep -c "ipcMain.handle" apps/desktop/src/main/ipc/mcp-settings.ts` → 2 (Task 1)
- `grep -c "mcpServerEnabled\|getMcpServerEnabled" apps/desktop/src/main/ipc/settings.ts` → 0 (Task 2)
- `grep -c "mcpServerEnabled\|getMcpServerEnabled\|setMcpServerEnabled" apps/desktop/src/main/store.ts` → 0 (Task 3)
- `grep -c "MCP_TOGGLE_CHANNEL\|MCP_GET_CONNECTED_CLIENTS_CHANNEL\|McpClientInfo\|getConnectedClients" apps/desktop/src/preload/settings.ts` → 0 (Task 4)
- `grep -c "MCP_TOGGLE\|MCP_GET_CONNECTED_CLIENTS\|interface McpClientInfo\|mcpServerEnabled" apps/desktop/src/shared/ipc-types.ts` → 0 (Task 5)
- `grep -c "mcpServerEnabled\|getMcpServerEnabled" apps/desktop/src/main/ipc/__tests__/settings.test.ts` → 0 (Task 6)
- `pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts` exit 0 (Task 6 + 7)
- Grep agregado em main+preload+shared → zero matches dos símbolos apagados (Task 7)
</verification>

<success_criteria>
1. `mcp-settings.ts` registra exatamente 2 handlers IPC (MCP_CLIENT_RELOAD, MCP_CLIENT_GET_STATUS) + broadcast helper.
2. `ipc/settings.ts` SETTINGS_GET payload não contém `mcpServerEnabled`; sem import de `getMcpServerEnabled`.
3. `store.ts` sem field `mcpServerEnabled` no schema; sem `getMcpServerEnabled`/`setMcpServerEnabled` exports.
4. `preload/settings.ts` expõe bridge `window.mcp` com 3 métodos (`reloadClient`, `getClientStatus`, `onClientStatusChanged`); sem `toggle`/`getConnectedClients`/`McpClientInfo`.
5. `shared/ipc-types.ts` sem `IPC_CHANNELS.MCP_TOGGLE`/`MCP_GET_CONNECTED_CLIENTS`, sem `interface McpClientInfo`, sem `SettingsData.mcpServerEnabled`; `SettingsApi.mcp` declara apenas `reloadClient`/`getClientStatus`.
6. `ipc/__tests__/settings.test.ts` sem mock `getMcpServerEnabled`, sem expectations `mcpServerEnabled`; suite passa.
7. Grep agregado em main+preload+shared retorna zero matches dos 7 símbolos apagados.
8. Suite `ipc/__tests__/settings.test.ts` exit 0.

Nota: typecheck full do desktop (`pnpm --filter desktop typecheck`) só passa após Plan 03 rodar — Wave 2 garante a ordem.
</success_criteria>

<output>
After completion, create `.planning/phases/69-mcp-server-removal/69-02-SUMMARY.md` documentando:
- 6 arquivos modificados (paths absolutos)
- Resumo das edições por arquivo (linhas removidas)
- Estado do bridge `window.mcp` (3 métodos client-side)
- Resultado de `pnpm --filter desktop test -- src/main/ipc/__tests__/settings.test.ts`
- Observação sobre o typecheck full ficar pendente até Plan 03
- Próximo passo: Plan 03 (renderer SettingsLayout + McpSection)
</output>
