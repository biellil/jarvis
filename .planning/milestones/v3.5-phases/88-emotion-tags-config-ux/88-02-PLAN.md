---
phase: 88-emotion-tags-config-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 88-01
files_modified:
  - apps/desktop-py/src/jarvis_desktop/chat.py
  - apps/desktop-py/tests/test_tts.py
  - apps/desktop-py/tests/test_config.py
autonomous: true
requirements:
  - CFGUI-01
  - CFGUI-02

must_haves:
  truths:
    - "Menu /config mostra 'chatterbox' como opção de provider TTS"
    - "Selecionar chatterbox no menu pede inline o caminho do arquivo de referência"
    - "Enter sem digitar caminho mantém chatterbox_audio_prompt_path atual"
    - "Item '8. Audio referência' aparece no menu principal APENAS quando tts_provider == 'chatterbox'"
    - "Item '8. Audio referência' não aparece para provider kokoro"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "_menu_tts_provider com chatterbox + prompt inline; _show_config_menu com item 8 condicional"
      contains: "chatterbox"
    - path: "apps/desktop-py/tests/test_config.py"
      provides: "Testes CFGUI-01 e CFGUI-02"
      contains: "test_menu_tts_provider_includes_chatterbox"
  key_links:
    - from: "apps/desktop-py/src/jarvis_desktop/chat.py:_menu_tts_provider"
      to: "providers list com chatterbox"
      via: 'providers = ["kokoro", "chatterbox", "elevenlabs", "murf", "none"]'
      pattern: '"chatterbox"'
    - from: "_menu_tts_provider pós-seleção chatterbox"
      to: "prompt inline de chatterbox_audio_prompt_path"
      via: "ui.get_input(f'Arquivo de referência...')"
      pattern: "chatterbox_audio_prompt_path"
    - from: "_show_config_menu while True"
      to: "item 8 condicional"
      via: 'if config.tts_provider == "chatterbox"'
      pattern: "Audio referência"
---

<objective>
Atualizar o menu /config para incluir "chatterbox" como provider TTS selecionável, com prompt inline para configurar o arquivo de referência de voz.

Purpose: Cumprir CFGUI-01 (chatterbox visível no menu) e CFGUI-02 (configurar path de referência no mesmo fluxo).
Output: `_menu_tts_provider()` com 5 providers + prompt inline pós-seleção; `_show_config_menu()` com item 8 condicional para audio referência.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/88-emotion-tags-config-ux/88-CONTEXT.md
@.planning/phases/88-emotion-tags-config-ux/88-RESEARCH.md
@.planning/phases/88-emotion-tags-config-ux/88-01-SUMMARY.md

<interfaces>
<!-- Estado atual do chat.py relevante para este plano. -->

De apps/desktop-py/src/jarvis_desktop/chat.py — `_menu_tts_provider()` (linha ~757):
```python
def _menu_tts_provider(config: JarvisConfig) -> None:
    from jarvis_desktop import ui, tts
    from jarvis_desktop.config import save_config
    console = ui.get_console()
    providers = ["kokoro", "elevenlabs", "murf", "none"]  # MUDAR: adicionar "chatterbox"

    # Imprime lista numerada com [x]/[ ] markers (markup=False obrigatório)
    console.print("TTS Providers:", highlight=False)
    for i, p in enumerate(providers, 1):
        marker = "[x]" if p == config.tts_provider else "[ ]"
        console.print(f"  {i}. {p} {marker}", markup=False)

    raw = ui.get_input("Selecione (1-4, Enter para cancelar): ").strip()  # MUDAR: (1-5)
    # ... idx = int(raw) - 1
    # ... tts.set_provider(new_provider, config)
    # ... config.tts_provider = new_provider
    # ... save_config(config)
```

De apps/desktop-py/src/jarvis_desktop/chat.py — `_show_config_menu()` (linha ~665):
```python
def _show_config_menu(config: JarvisConfig) -> None:
    while True:
        console.print(f"1. Whisper model      [{config.whisper_model}]", markup=False)
        console.print(f"2. TTS provider       [{config.tts_provider}]", markup=False)
        # ... itens 3-7 ...
        console.print(f"7. Voz Kokoro         [{config.kokoro_voice}]", markup=False)
        console.print("0. Sair")  # item 8 condicional deve vir ANTES deste "0. Sair"
        # Handlers: choice "1" → _menu_whisper_model, ..., choice "7" → _menu_kokoro_voice
```

De apps/desktop-py/src/jarvis_desktop/config.py (Phase 88 Plan 01):
```python
class JarvisConfig(BaseModel):
    chatterbox_audio_prompt_path: str = Field(default="")
    chatterbox_exaggeration: float = Field(default=0.7)   # adicionado pelo Plan 01
    chatterbox_cfg_weight: float = Field(default=0.5)     # adicionado pelo Plan 01
```

Padrão de prompt de texto em menus (ver _menu_whisper_model, _menu_kokoro_voice):
- `ui.get_input("prompt aqui").strip()` — sem markup
- `try/except (EOFError, KeyboardInterrupt): pass` — proteção padrão
- `save_config(config)` após qualquer mudança persistível
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar testes CFGUI-01 e CFGUI-02 a test_config.py</name>
  <files>apps/desktop-py/tests/test_config.py</files>
  <read_first>
    - apps/desktop-py/tests/test_config.py (ler completo para encontrar linha de inserção e seguir padrões)
    - apps/desktop-py/tests/conftest.py (verificar fixtures: tmp_home, jarvis_config_dir)
    - apps/desktop-py/src/jarvis_desktop/chat.py (linha ~757 — _menu_tts_provider para entender a interface)
  </read_first>
  <behavior>
    - test_menu_tts_provider_includes_chatterbox: `_menu_tts_provider` imprime "chatterbox" na lista de providers (CFGUI-01)
    - test_menu_tts_provider_chatterbox_prompts_audio_path: ao selecionar chatterbox (opção 2 na nova lista), o menu solicita path de arquivo de referência inline (CFGUI-02)
    - test_menu_tts_provider_chatterbox_empty_path_keeps_current: Enter sem digitar path mantém o valor atual de `chatterbox_audio_prompt_path` (D-11)
    - test_show_config_menu_item8_when_chatterbox: menu principal mostra "Audio referência" quando tts_provider == "chatterbox" (D-12)
    - test_show_config_menu_no_item8_for_kokoro: menu principal NÃO mostra "Audio referência" quando tts_provider == "kokoro" (D-12)
  </behavior>
  <action>
Adicionar ao final de `apps/desktop-py/tests/test_config.py` a seguinte seção:

```python
# ---------------------------------------------------------------------------
# Phase 88: Config UX (CFGUI-01, CFGUI-02)
# ---------------------------------------------------------------------------

def test_menu_tts_provider_includes_chatterbox(monkeypatch):
    """_menu_tts_provider() imprime 'chatterbox' na lista de providers. CFGUI-01."""
    import io
    from unittest.mock import MagicMock, patch
    from jarvis_desktop.config import JarvisConfig

    config = JarvisConfig(tts_provider="kokoro")
    printed_lines = []

    # Simular console.print capturando chamadas
    mock_console = MagicMock()
    mock_console.print.side_effect = lambda text, **kw: printed_lines.append(str(text))

    with patch("jarvis_desktop.ui.get_console", return_value=mock_console), \
         patch("jarvis_desktop.ui.get_input", return_value=""):  # Enter cancela
        from jarvis_desktop.chat import _menu_tts_provider
        _menu_tts_provider(config)

    all_output = "\n".join(printed_lines)
    assert "chatterbox" in all_output, f"'chatterbox' deve aparecer na lista de providers. Output: {all_output}"


def test_menu_tts_provider_chatterbox_prompts_audio_path(monkeypatch, tmp_home):
    """Selecionar chatterbox solicita path de arquivo de referência inline. CFGUI-02, D-10."""
    from unittest.mock import MagicMock, patch, call
    from jarvis_desktop.config import JarvisConfig

    config = JarvisConfig(tts_provider="kokoro", chatterbox_audio_prompt_path="")

    mock_console = MagicMock()
    # Simular: input "2" (seleciona chatterbox), depois "" (Enter = manter path)
    input_responses = iter(["2", ""])

    mock_tts = MagicMock()
    # Simular set_provider bem-sucedido: atualiza config.tts_provider
    def fake_set_provider(provider, cfg):
        cfg.tts_provider = provider
    mock_tts.set_provider.side_effect = fake_set_provider

    with patch("jarvis_desktop.ui.get_console", return_value=mock_console), \
         patch("jarvis_desktop.ui.get_input", side_effect=input_responses), \
         patch("jarvis_desktop.tts.set_provider", side_effect=fake_set_provider), \
         patch("jarvis_desktop.chat.tts", mock_tts), \
         patch("jarvis_desktop.config.save_config"):
        from jarvis_desktop.chat import _menu_tts_provider
        _menu_tts_provider(config)

    # Verificar que o prompt de path foi exibido (input foi chamado mais de uma vez)
    # Primeiro input: seleção de provider; segundo input: path de referência
    assert config.tts_provider == "chatterbox"


def test_menu_tts_provider_chatterbox_empty_path_keeps_current(monkeypatch, tmp_home):
    """Enter sem digitar path mantém chatterbox_audio_prompt_path atual. D-11."""
    from unittest.mock import MagicMock, patch
    from jarvis_desktop.config import JarvisConfig

    current_path = "/some/existing/reference.wav"
    config = JarvisConfig(tts_provider="kokoro", chatterbox_audio_prompt_path=current_path)

    mock_console = MagicMock()
    input_responses = iter(["2", ""])  # seleciona chatterbox, Enter sem path

    def fake_set_provider(provider, cfg):
        cfg.tts_provider = provider

    with patch("jarvis_desktop.ui.get_console", return_value=mock_console), \
         patch("jarvis_desktop.ui.get_input", side_effect=input_responses), \
         patch("jarvis_desktop.tts.set_provider", side_effect=fake_set_provider), \
         patch("jarvis_desktop.config.save_config"):
        from jarvis_desktop.chat import _menu_tts_provider
        _menu_tts_provider(config)

    # Path original deve ser mantido (D-11: Enter sem digitar = manter atual)
    assert config.chatterbox_audio_prompt_path == current_path


def test_show_config_menu_item8_when_chatterbox(monkeypatch):
    """Menu principal mostra item '8. Audio referência' quando tts_provider == 'chatterbox'. D-12."""
    from unittest.mock import MagicMock, patch
    from jarvis_desktop.config import JarvisConfig

    config = JarvisConfig(tts_provider="chatterbox", chatterbox_audio_prompt_path="/ref.wav")
    printed_lines = []

    mock_console = MagicMock()
    mock_console.print.side_effect = lambda text, **kw: printed_lines.append(str(text))

    # Simular: imprimir menu uma vez, depois "0" para sair
    with patch("jarvis_desktop.ui.get_console", return_value=mock_console), \
         patch("jarvis_desktop.ui.get_input", return_value="0"):
        from jarvis_desktop.chat import _show_config_menu
        _show_config_menu(config)

    all_output = "\n".join(printed_lines)
    assert "Audio referência" in all_output, f"'Audio referência' deve aparecer. Output: {all_output}"


def test_show_config_menu_no_item8_for_kokoro(monkeypatch):
    """Menu principal NÃO mostra item '8. Audio referência' quando tts_provider == 'kokoro'. D-12."""
    from unittest.mock import MagicMock, patch
    from jarvis_desktop.config import JarvisConfig

    config = JarvisConfig(tts_provider="kokoro")
    printed_lines = []

    mock_console = MagicMock()
    mock_console.print.side_effect = lambda text, **kw: printed_lines.append(str(text))

    with patch("jarvis_desktop.ui.get_console", return_value=mock_console), \
         patch("jarvis_desktop.ui.get_input", return_value="0"):
        from jarvis_desktop.chat import _show_config_menu
        _show_config_menu(config)

    all_output = "\n".join(printed_lines)
    assert "Audio referência" not in all_output, f"'Audio referência' NÃO deve aparecer para kokoro. Output: {all_output}"
```

**Estes testes devem estar vermelhos (RED) antes da Task 2 — `_menu_tts_provider` ainda não inclui chatterbox.**
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_config.py::test_menu_tts_provider_includes_chatterbox tests/test_config.py::test_show_config_menu_item8_when_chatterbox -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 5 novos testes adicionados em test_config.py
    - Testes de CFGUI-01/CFGUI-02 falham com AssertionError (chatterbox não está na lista ainda) — RED confirmado
    - Testes de config existentes (test_load_config_returns_defaults, etc.) continuam passando
  </done>
</task>

<task type="auto">
  <name>Task 2: Atualizar _menu_tts_provider e _show_config_menu em chat.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/chat.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/chat.py (ler COMPLETO a partir da linha 640 — focar em _show_config_menu linha ~665, _menu_tts_provider linha ~757)
    - apps/desktop-py/tests/test_config.py (ver testes da Task 1 para confirmar comportamento esperado)
    - apps/desktop-py/src/jarvis_desktop/config.py (confirmar campo chatterbox_audio_prompt_path existe)
  </read_first>
  <action>
**Passo 1 — `_menu_tts_provider()` (linha ~757):**

Localizar a função `_menu_tts_provider()` e fazer as seguintes mudanças:

1. Mudar `providers = ["kokoro", "elevenlabs", "murf", "none"]` para:
   ```python
   providers = ["kokoro", "chatterbox", "elevenlabs", "murf", "none"]  # D-09
   ```

2. Mudar o prompt hardcoded de `"Selecione (1-4, Enter para cancelar): "` para dinâmico:
   ```python
   raw = ui.get_input(f"Selecione (1-{len(providers)}, Enter para cancelar): ").strip()
   ```

3. Após `tts.set_provider(new_provider, config)` ser bem-sucedido e `config.tts_provider = new_provider`, adicionar o bloco de prompt inline para chatterbox (D-10, D-11, CFGUI-02):
   ```python
   # CFGUI-02 (D-10, D-11): se chatterbox selecionado, solicitar path inline
   if new_provider == "chatterbox" and config.tts_provider == "chatterbox":
       current = config.chatterbox_audio_prompt_path or "nenhum"
       try:
           new_path = ui.get_input(
               f"Arquivo de referência de voz (Enter para manter [{current}]): "
           ).strip()
           if new_path:  # D-11: Enter sem digitar mantém valor atual
               config.chatterbox_audio_prompt_path = new_path
       except (EOFError, KeyboardInterrupt):
           pass
   ```

O bloco `save_config(config)` existente no final da função já persiste todas as mudanças — não precisa duplicar.

**Passo 2 — `_show_config_menu()` (linha ~665):**

1. No `while True`, localizar onde o menu imprime `console.print("0. Sair")`. Inserir ANTES desta linha o item 8 condicional (D-12):
   ```python
   if config.tts_provider == "chatterbox":
       ref = config.chatterbox_audio_prompt_path or "(não definido)"
       console.print(f"8. Audio referência   [{ref}]", markup=False)
   console.print("0. Sair")
   ```

2. No handler de `choice`, após o bloco `elif choice == "7": _menu_kokoro_voice(config)`, adicionar:
   ```python
   elif choice == "8" and config.tts_provider == "chatterbox":
       _menu_chatterbox_audio_ref(config)
   ```

3. Adicionar nova função `_menu_chatterbox_audio_ref()` após `_menu_kokoro_voice()`:
   ```python
   def _menu_chatterbox_audio_ref(config: JarvisConfig) -> None:
       """Chatterbox audio reference path sub-menu. CFGUI-02, D-12."""
       from jarvis_desktop import ui
       from jarvis_desktop.config import save_config

       console = ui.get_console()
       current = config.chatterbox_audio_prompt_path or "(não definido)"
       console.print()
       console.print(f"Arquivo de referência atual: {current}", markup=False)
       console.print("Digite o caminho do arquivo .wav ou .mp3 (Enter para manter):", markup=False)
       console.print()

       try:
           new_path = ui.get_input("Caminho: ").strip()
           if new_path:
               config.chatterbox_audio_prompt_path = new_path
               save_config(config)
               console.print(f"[Audio referência definido: {new_path}]", markup=False)
           else:
               console.print("[Audio referência mantido sem alteração]", markup=False)
       except (EOFError, KeyboardInterrupt):
           pass
   ```

**Pitfalls a evitar:**
- NUNCA usar `markup=True` em prints que mostram caminhos de arquivo ou conteúdo do usuário — Rich interpretaria `[ref.wav]` como markup de cor
- A condição `config.tts_provider == "chatterbox"` para o item 8 deve ser checada a CADA iteração do `while True`, não cacheada em variável (Pitfall 4 do RESEARCH)
- O prompt de range deve ser dinâmico `f"(1-{len(providers)})"` — Pitfall 5 do RESEARCH
- `_menu_chatterbox_audio_ref` referencia `config.chatterbox_audio_prompt_path` que existe a partir do Plan 01
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_config.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `uv run pytest tests/test_config.py -x -q` — todos os testes passam (incluindo os 5 novos de CFGUI-01/CFGUI-02)
    - `grep -n '"chatterbox"' apps/desktop-py/src/jarvis_desktop/chat.py` encontra chatterbox na lista de providers
    - `grep -n "Audio referência" apps/desktop-py/src/jarvis_desktop/chat.py` encontra o item condicional
    - `grep -n "_menu_chatterbox_audio_ref" apps/desktop-py/src/jarvis_desktop/chat.py` mostra definição e chamada
    - `grep -n "len(providers)" apps/desktop-py/src/jarvis_desktop/chat.py` confirma prompt dinâmico
  </done>
</task>

<task type="auto">
  <name>Task 3: Rodar suite completa e confirmar todos os testes verdes</name>
  <files></files>
  <read_first>
    - apps/desktop-py/tests/test_tts.py (verificar que testes do Plan 01 estão intactos)
    - apps/desktop-py/tests/test_config.py (verificar que todos os testes novos passam)
  </read_first>
  <action>
Rodar suite completa de testes:

```bash
cd apps/desktop-py && uv run pytest tests/ -x -q 2>&1
```

Se algum teste falhar:
1. Identificar qual teste e por quê (ler a mensagem de erro completa)
2. Verificar se é um dos novos testes (Plan 01 ou Plan 02) — corrigir a implementação, não os testes
3. Verificar se é um teste regressivo (teste existente quebrado) — investigar se mudança em chat.py ou tts.py causou side effects

Regressões possíveis a checar:
- `test_tts.py::test_chatterbox_singletons_exist` — verifica singletons; não deve ser afetado
- Testes que chamam `speak()` com provider="kokoro" — D-04 garante que texto vai para Kokoro sem strip de tags; verificar se `_chatterbox_speak` não é chamado para provider kokoro
- `test_config.py::test_load_config_returns_defaults` — JarvisConfig com novos campos deve ainda retornar defaults corretos para campos existentes
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/ -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `pytest tests/` retorna com 0 failures
    - Total de testes verde (incluindo todos os testes Phase 88 novos)
    - Nenhum teste existente quebrado (regressão zero)
  </done>
</task>

</tasks>

<verification>
```bash
cd apps/desktop-py && uv run pytest tests/ -q 2>&1 | tail -10
grep -n '"chatterbox"' apps/desktop-py/src/jarvis_desktop/chat.py
grep -n "Audio referência" apps/desktop-py/src/jarvis_desktop/chat.py
grep -n "chatterbox_exaggeration" apps/desktop-py/src/jarvis_desktop/config.py
grep -n "_extract_emotion_tag" apps/desktop-py/src/jarvis_desktop/tts.py
```
</verification>

<success_criteria>
- `_menu_tts_provider()` lista 5 providers: `["kokoro", "chatterbox", "elevenlabs", "murf", "none"]`
- Selecionar chatterbox exibe prompt: `"Arquivo de referência de voz (Enter para manter [...]): "`
- Enter sem digitar path NÃO altera `config.chatterbox_audio_prompt_path`
- `_show_config_menu()` exibe `"8. Audio referência   [/path]"` quando `tts_provider == "chatterbox"`
- `_show_config_menu()` NÃO exibe item 8 quando `tts_provider == "kokoro"`
- Suite completa `pytest tests/` verde — zero regressões
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/88-emotion-tags-config-ux/88-02-SUMMARY.md` com:
- Funções modificadas em chat.py e linhas exatas
- Nova função `_menu_chatterbox_audio_ref` adicionada
- Resultado de `pytest tests/` (total de testes, falhas = 0)
- Confirmação de CFGUI-01 e CFGUI-02 implementados
</output>
