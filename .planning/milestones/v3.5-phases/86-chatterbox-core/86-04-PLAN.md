---
phase: 86
plan: 04
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - apps/desktop-py/src/jarvis_desktop/tts.py
autonomous: true
requirements: [CHTB-01, CHTB-03, CHTB-04]
requirements_addressed: [CHTB-01, CHTB-03, CHTB-04]

must_haves:
  truths:
    - "init_tts() com config.tts_provider='chatterbox' dispara warmup em thread daemon (D-01) e retorna em <1s"
    - "Warmup com cascade de device tenta próximo device em erro (D-14)"
    - "Warmup completo seta _chatterbox_warmup_event e armazena engine + device escolhido"
    - "Warmup imprime label de device no formato D-21 ('GPU (CUDA)', 'GPU (MPS)', 'GPU (DirectML)' ou 'CPU')"
    - "speak() com provider=chatterbox aguarda warmup até 15s antes de gerar (D-05); timeout → fallback Kokoro"
    - "Erro runtime em engine.generate() marca _chatterbox_disabled=True e cai para Kokoro pela sessão (D-08)"
    - "ImportError no warmup marca _chatterbox_available=False e cai para Kokoro (D-09)"
    - "config.tts_provider NUNCA é alterado em disco em fallback (D-10)"
    - "Audio do Chatterbox toca via sounddevice a 24kHz sem resample"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "_start_chatterbox_warmup — thread daemon, cascade de device (D-14), Event signaling (D-05), labels D-21"
      contains: "_start_chatterbox_warmup"
      min_lines: 80
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "_chatterbox_speak — wait Event 15s (D-05), fallback Kokoro em timeout/runtime/import error (D-08/D-09/D-10)"
      contains: "_chatterbox_speak"
      min_lines: 70
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "branch em init_tts() que dispara _start_chatterbox_warmup quando tts_provider='chatterbox' (D-02), preservando carga do Kokoro como fallback offline universal (D-24)"
      contains: 'if config.tts_provider == "chatterbox"'
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "branch em speak() que roteia para _chatterbox_speak antes dos cloud providers"
      contains: '_chatterbox_speak(text, config)'
  key_links:
    - from: "init_tts(config)"
      to: "_start_chatterbox_warmup(config)"
      via: "branch quando config.tts_provider == 'chatterbox'"
      pattern: "if config.tts_provider == \"chatterbox\""
    - from: "speak(text, config)"
      to: "_chatterbox_speak(text, config)"
      via: "branch antes dos cloud providers (Chatterbox é offline, prioridade igual ao Kokoro)"
      pattern: "_chatterbox_speak"
    - from: "_chatterbox_speak"
      to: "_chatterbox_warmup_event.wait(timeout=15)"
      via: "threading.Event API"
      pattern: "_chatterbox_warmup_event.wait"
    - from: "_chatterbox_speak runtime error"
      to: "_kokoro_speak"
      via: "fallback após marcar _chatterbox_disabled=True"
      pattern: "_kokoro_speak"
---

<objective>
Fechar a Phase 86 implementando o warmup async (CHTB-03), o caminho `_chatterbox_speak` com fallback runtime para Kokoro (CHTB-04), e integrando ambos nas funções públicas `init_tts()` e `speak()` (CHTB-01 parte final). Os 10 testes Chatterbox restantes ficam GREEN. Smoke manual (Manual-Only em VALIDATION.md) é o último passo antes de declarar phase verde.

Purpose: Sem este plan, Chatterbox é instalável (Plan 02) e selecionável (Plan 03), mas a primeira fala teria cold start de 5-10s (Pitfall 3) e qualquer erro derrubaria o TTS. Este plan entrega o comportamento prometido pelo ROADMAP success criteria 1-4.
Output: tts.py final com ~180 linhas novas Phase 86 totais (Plan 03 + Plan 04), toda a suite de testes Chatterbox verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md
@apps/desktop-py/src/jarvis_desktop/tts.py
@apps/desktop-py/tests/test_tts.py

<interfaces>
<!-- Padrão obrigatório de _kokoro_speak (linhas 227-280 de tts.py) — replicar literalmente -->

From apps/desktop-py/src/jarvis_desktop/tts.py (_kokoro_speak — pattern de speak privado):
```python
def _kokoro_speak(text: str, config: JarvisConfig) -> None:
    global _engine, _is_playing
    import sounddevice as sd
    # Lazy init com double-checked locking
    if _engine is None:
        try:
            with _lock:
                if _engine is None:
                    _engine = _create_kokoro_engine(config)
        except Exception as exc:
            _console().print(f"[TTS] Kokoro indisponível: {exc} — voz silenciosa.")
            return
    try:
        _stop_event.clear()
        from jarvis_desktop import ui as _ui
        _ui.set_state("speaking")
        _is_playing = True
        # ... synthesis + sd.play(audio_data, samplerate=_KOKORO_SAMPLE_RATE) ...
        sd.wait()
    except Exception as exc:
        _console().print(f"[TTS] Erro ao falar: {exc}")
    finally:
        _is_playing = False
        _ui.set_state("idle")
```

Símbolos do Plan 03 (já existem em tts.py após Plan 03 completar):
- `_chatterbox_engine`, `_chatterbox_disabled`, `_chatterbox_available`, `_chatterbox_warmup_event`, `_chatterbox_device`, `_CHATTERBOX_SAMPLE_RATE`
- `_detect_chatterbox_device() -> list[str]`
- `_create_chatterbox_engine(config, device) -> ChatterboxMultilingualTTS`
- `set_provider(provider, config)` aceita "chatterbox"

API de geração (RESEARCH linha 436-440):
```python
wav_tensor = engine.generate(text, language_id="pt")
# wav_tensor é torch.Tensor (1, N) em GPU memory possivelmente
# CONVERSION OBRIGATÓRIA: .squeeze().cpu().numpy().astype(np.float32) — Pitfall 4
audio_data = wav_tensor.squeeze().cpu().numpy().astype(np.float32)
```

speak() flow atual (linhas 90-130):
```python
def speak(text: str, config: JarvisConfig) -> None:
    if not text.strip(): return
    if config.tts_provider == "none": return
    # Cloud paths (elevenlabs / murf) se !local_only e key
    # ...
    _kokoro_speak(text, config)  # fallback final
```

init_tts() atual (linhas 53-87):
```python
def init_tts(config: JarvisConfig) -> None:
    global _engine
    with _lock:
        if config.tts_provider == "none": return
        if _engine is not None: return
        # ... carrega Kokoro ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar _start_chatterbox_warmup com cascade + Event signaling</name>
  <files>apps/desktop-py/src/jarvis_desktop/tts.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/tts.py (após Plan 03 — ler `_detect_chatterbox_device` e `_create_chatterbox_engine`)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (Pattern 3 linhas 330-391)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-01, D-02, D-03, D-04, D-06, D-09, D-14, D-21, D-22, D-23)
  </read_first>
  <behavior>
    - Test test_warmup_completes_event_set: _start_chatterbox_warmup completa, _chatterbox_warmup_event.is_set() == True em < 5s, _chatterbox_engine != None
    - Test test_warmup_device_cascade: quando primeiro device falha (cuda → RuntimeError), tenta segundo (cpu) e seta _chatterbox_device='cpu'
    - Test test_import_error_disables_session: ImportError no warmup marca _chatterbox_available=False, mensagem com "uv sync --extra chatterbox" no stdout
    - Idempotência: chamar _start_chatterbox_warmup duas vezes em sequência NÃO dispara duas threads
  </behavior>
  <action>
Adicionar `_start_chatterbox_warmup` em `apps/desktop-py/src/jarvis_desktop/tts.py` logo após `_create_chatterbox_engine` (do Plan 03):

```python
def _start_chatterbox_warmup(config: JarvisConfig) -> None:
    """Dispara warmup do Chatterbox em background thread daemon (D-01).

    Idempotente: se warmup já completou (_chatterbox_warmup_event setado) ou se
    sessão já marcou _chatterbox_available=False (D-09), retorna imediatamente.

    Aplica cascade de device (D-14): se primeiro device falhar no load ou no
    smoke test do warmup, tenta próximo da cadeia antes de declarar indisponível.

    Texto do warmup: "olá" (D-03 — texto curto fixo PT-BR; áudio descartado).
    NÃO usa audio_prompt_path (D-04 — voice cloning é Phase 87).

    Args:
        config: JarvisConfig — passado adiante para _create_chatterbox_engine.
                Esta phase não consome campos específicos do config no warmup.
    """
    global _chatterbox_engine, _chatterbox_device, _chatterbox_available

    # Idempotência (D-06: thread daemon, sem cleanup elegante)
    if _chatterbox_warmup_event.is_set():
        return
    if _chatterbox_available is False:
        # D-09: sessão já decidiu que Chatterbox não está instalado
        return

    def _warmup_worker() -> None:
        global _chatterbox_engine, _chatterbox_device, _chatterbox_available

        # Validar import lazy (D-09) — primeiro gate
        try:
            from chatterbox.mtl_tts import ChatterboxMultilingualTTS  # noqa: F401
        except ImportError:
            _console().print(
                "[TTS] Chatterbox não instalado. Rode: uv sync --extra chatterbox",
                highlight=False,
            )
            _chatterbox_available = False
            _chatterbox_warmup_event.set()  # Libera quem espera (vai cair pra Kokoro via D-05)
            return

        # Cascade de device (D-14)
        devices = _detect_chatterbox_device()
        last_error: Optional[BaseException] = None

        # D-21: labels EXATAS — "GPU (CUDA)" / "GPU (MPS)" / "GPU (DirectML)" / "CPU"
        device_labels = {
            "cuda": "GPU (CUDA)",
            "mps": "GPU (MPS)",
            "directml": "GPU (DirectML)",
            "cpu": "CPU",
        }

        for device in devices:
            try:
                _console().print(
                    f"[TTS] Chatterbox: aquecendo ({device.upper()})...",
                    highlight=False,
                )
                engine = _create_chatterbox_engine(config, device)
                # D-03: warmup com texto mínimo PT-BR; áudio descartado (não vai pro sounddevice)
                _ = engine.generate("olá", language_id="pt")

                with _lock:
                    _chatterbox_engine = engine
                    _chatterbox_device = device
                    _chatterbox_available = True

                # D-21: imprimir label no formato exato exigido pelo CONTEXT
                label = device_labels.get(device, device)
                _console().print(f"[TTS] Chatterbox: {label}. Pronto.", highlight=False)
                _chatterbox_warmup_event.set()
                return
            except Exception as exc:
                last_error = exc
                _console().print(
                    f"[TTS] Chatterbox: {device} falhou ({type(exc).__name__}) — tentando próximo.",
                    highlight=False,
                )
                continue

        # Cascade esgotada (D-14) → Chatterbox indisponível pela sessão (D-08-like)
        _console().print(
            f"[TTS] Chatterbox indisponível ({type(last_error).__name__ if last_error else 'unknown'}) — usando Kokoro pela sessão.",
            highlight=False,
        )
        _chatterbox_available = False
        _chatterbox_warmup_event.set()

    thread = threading.Thread(
        target=_warmup_worker,
        daemon=True,  # D-06: morre com o processo
        name="chatterbox-warmup",
    )
    thread.start()
```

Pontos críticos:
- `threading.Thread(daemon=True)` é não-negociável (D-06).
- `Event.set()` no FINAL de cada caminho (success, ImportError, cascade-exhausted). Isso libera quem espera em `_chatterbox_speak` (D-05).
- Texto `"olá"` (3 chars) em vez de `"."` (Pitfall 3 / A3 do RESEARCH — discricionário, mas 3 chars são mais robustos).
- Mensagem de ImportError contém literalmente `"uv sync --extra chatterbox"` (test_import_error_disables_session grep-checks).
- `type(exc).__name__` em vez de stack completo (V7 do threat model — não vaza paths).
- Atomicidade na atribuição final de engine/device/available é protegida por `_lock` (D-25).
- **D-21 device labels**: dicionário `device_labels` mapeia `cuda → "GPU (CUDA)"`, `mps → "GPU (MPS)"`, `directml → "GPU (DirectML)"`, `cpu → "CPU"`. O `_console().print(f"[TTS] Chatterbox: {label}. Pronto.")` usa o label resolvido — formato exato auditável via grep no acceptance criteria.
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_tts.py::test_warmup_completes_event_set tests/test_tts.py::test_warmup_device_cascade tests/test_tts.py::test_import_error_disables_session -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def _start_chatterbox_warmup" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep "threading.Thread(.*daemon=True\|daemon=True" apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha
    - `grep 'engine.generate("olá", language_id="pt")\|engine.generate(.olá., language_id=.pt.)' apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha (D-03)
    - `grep "_chatterbox_warmup_event.set()" apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 3 ocorrências (3 caminhos: success, ImportError, cascade-exhausted)
    - **D-21 device labels (revisão pós-checker — enforcement obrigatório):** `grep -E 'GPU \(CUDA\)|GPU \(MPS\)|GPU \(DirectML\)' apps/desktop-py/src/jarvis_desktop/tts.py | wc -l` retorna `>= 3` (uma linha por label GPU presente em `device_labels`)
    - **D-21 label CPU presente:** `grep -E '"CPU"' apps/desktop-py/src/jarvis_desktop/tts.py | wc -l` retorna `>= 1` (label `"CPU"` no dicionário `device_labels`)
    - `pytest tests/test_tts.py::test_warmup_completes_event_set -x` exit 0
    - `pytest tests/test_tts.py::test_warmup_device_cascade -x` exit 0
    - `pytest tests/test_tts.py::test_import_error_disables_session -x` exit 0
  </acceptance_criteria>
  <done>
    `_start_chatterbox_warmup` implementada: thread daemon, cascade de device, Event signaling em todos os 3 caminhos, mensagens com prefixo [TTS], ImportError trata D-09 corretamente, labels D-21 exatas (`GPU (CUDA)` / `GPU (MPS)` / `GPU (DirectML)` / `CPU`). 3 testes Chatterbox verdes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar _chatterbox_speak + integrar branches em init_tts e speak</name>
  <files>apps/desktop-py/src/jarvis_desktop/tts.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/tts.py (após Task 1 — ler init_tts, speak, _kokoro_speak, _start_chatterbox_warmup)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (Pattern 4 linhas 393-464, Pitfall 4 linha 516)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-02, D-05, D-08, D-10, D-22, D-24)
  </read_first>
  <behavior>
    - Test test_init_tts_warmup_non_blocking: init_tts(config=chatterbox) retorna em <1s (warmup é thread em background)
    - Test test_warmup_skipped_when_kokoro_provider: init_tts(config=kokoro) NÃO chama _start_chatterbox_warmup (não tenta gerar via Chatterbox)
    - Test test_speak_waits_for_warmup: _chatterbox_speak chama engine.generate pelo menos 1 vez após warmup completar
    - Test test_speak_warmup_timeout_falls_back: warmup que nunca completa em 15s → _chatterbox_speak cai para _kokoro_speak (sd.play foi chamado pelo Kokoro mock)
    - Test test_chatterbox_runtime_error_fallback: RuntimeError em engine.generate marca _chatterbox_disabled=True e chama _kokoro_speak
    - Test test_chatterbox_disabled_stays_disabled: com _chatterbox_disabled=True, próxima _chatterbox_speak vai direto para _kokoro_speak sem tentar engine.generate
    - Test test_fallback_does_not_persist_config_change: após fallback, load_config() do disco retorna tts_provider='chatterbox' (NUNCA mudado em disco)
  </behavior>
  <action>
Realizar 3 alterações em `apps/desktop-py/src/jarvis_desktop/tts.py`:

**(1) Adicionar `_chatterbox_speak` logo após `_start_chatterbox_warmup`:**

```python
def _chatterbox_speak(text: str, config: JarvisConfig) -> None:
    """Synthesize text with Chatterbox and play via sounddevice.

    Replica o pattern de _kokoro_speak (set_state, _is_playing, _stop_event, finally)
    e adiciona:
      - D-05: bloqueia até 15s aguardando _chatterbox_warmup_event
      - D-08: erro runtime marca _chatterbox_disabled=True e chama _kokoro_speak
      - D-10: NUNCA altera config.tts_provider (estado de degradação só em memória)

    Args:
        text: Texto para sintetizar (será passado com language_id="pt").
        config: JarvisConfig — usado apenas para Kokoro fallback se algo falhar.
    """
    global _is_playing, _chatterbox_disabled, _chatterbox_engine
    import sounddevice as sd
    import numpy as np

    # Caminhos rápidos de fallback (sem tocar engine)
    if _chatterbox_disabled or _chatterbox_available is False:
        # D-08 (runtime error sticky) ou D-09 (ImportError sticky) — Kokoro direto
        _kokoro_speak(text, config)
        return

    # Garantir que warmup já foi disparado (D-02 — quando user troca via set_provider,
    # warmup é disparado no Plan 03's set_provider OU aqui sob demanda)
    if not _chatterbox_warmup_event.is_set() and _chatterbox_engine is None:
        _start_chatterbox_warmup(config)

    # D-05: bloquear até 15s aguardando warmup
    if not _chatterbox_warmup_event.is_set():
        _console().print("[TTS] aguardando inicialização...", highlight=False)
        completed = _chatterbox_warmup_event.wait(timeout=15.0)
        if not completed:
            _console().print(
                "[TTS] timeout aguardando Chatterbox — usando Kokoro.",
                highlight=False,
            )
            _kokoro_speak(text, config)
            return

    # Re-checar disponibilidade após warmup (warmup pode ter declarado indisponível)
    if _chatterbox_engine is None or _chatterbox_disabled or _chatterbox_available is False:
        _kokoro_speak(text, config)
        return

    try:
        _stop_event.clear()
        from jarvis_desktop import ui as _ui
        _ui.set_state("speaking")   # D-24 / Phase 75 D-05
        _is_playing = True

        # Geração síncrona (Chatterbox não tem streaming nativo).
        # NOTA: durante .generate(), _stop_event não pode interromper (limitation, RESEARCH Q3).
        wav_tensor = _chatterbox_engine.generate(text, language_id="pt")

        # Pitfall 4 do RESEARCH: tensor em GPU exige .cpu() antes de .numpy().
        # Pitfall A4: .squeeze() para garantir forma 1D antes do sounddevice.
        audio_data = wav_tensor.squeeze().cpu().numpy().astype(np.float32)

        if _stop_event.is_set():
            return

        _console().print("[TTS] falando (Chatterbox)...", highlight=False)
        sd.play(audio_data, samplerate=_CHATTERBOX_SAMPLE_RATE)
        sd.wait()  # Bloqueia até playback terminar (sd.stop() pode interromper via stop_tts)
        if _stop_event.is_set():
            sd.stop()

    except Exception as exc:
        # D-08: marca disabled pela sessão (sticky) + Kokoro fallback
        # D-10: NUNCA mexer em config.tts_provider — estado fica em memória
        _chatterbox_disabled = True
        _console().print(
            f"[TTS] Chatterbox falhou ({type(exc).__name__}: {exc}) — usando Kokoro pela sessão.",
            highlight=False,
        )
        # Sair do estado speaking antes do fallback para Kokoro re-setar
        _is_playing = False
        try:
            from jarvis_desktop import ui as _ui
            _ui.set_state("idle")
        except Exception:
            pass
        _kokoro_speak(text, config)
        return
    finally:
        _is_playing = False
        try:
            from jarvis_desktop import ui as _ui
            _ui.set_state("idle")
        except Exception:
            pass
```

**(2) Estender `init_tts()` (linha 53)** — adicionar branch chatterbox antes do branch Kokoro:

Substituir o corpo de `init_tts` por:

```python
def init_tts(config: JarvisConfig) -> None:
    """Initialize TTS engine at startup. Safe to call multiple times.

    Phase 75: carrega Kokoro com voz configurada.
    Phase 86: se provider=chatterbox, dispara warmup async (D-01, D-02). Kokoro
              continua sendo carregado como fallback offline universal.

    Args:
        config: JarvisConfig com tts_provider, kokoro_voice fields
    """
    global _engine
    with _lock:
        if config.tts_provider == "none":
            _console().print("[TTS] TTS desabilitado.")
            return

        # D-02: dispara warmup Chatterbox SÓ se provider=chatterbox
        if config.tts_provider == "chatterbox":
            _start_chatterbox_warmup(config)
            # NÃO retorna — Kokoro também é carregado abaixo como fallback offline (D-24)

        if _engine is not None:
            return  # Kokoro já inicializado

        _console().print(f"[TTS] Inicializando Kokoro (voz: {config.kokoro_voice})...")
        try:
            _engine = _create_kokoro_engine(config)
            _console().print("[TTS] Pronto.")
        except RuntimeError as exc:
            if "espeak-ng" in str(exc).lower() or "espeak" in str(exc).lower():
                _console().print(
                    "[TTS] espeak-ng não encontrado — voz PT-BR indisponível. Texto exibido normalmente."
                )
            else:
                _console().print(f"[TTS] Erro ao carregar Kokoro: {exc} — TTS desabilitado.")
            _engine = None
        except Exception as exc:
            _console().print(f"[TTS] Erro ao carregar Kokoro: {exc} — TTS desabilitado.")
            _engine = None
```

**(3) Estender `speak()` (linha 90)** — adicionar branch chatterbox ANTES dos cloud providers (Chatterbox é offline, prioridade igual a Kokoro):

Substituir o corpo de `speak` por:

```python
def speak(text: str, config: JarvisConfig) -> None:
    """Synthesize and play text using the configured TTS provider.

    Provider selection order (Phase 86):
      1. tts_provider="chatterbox" → _chatterbox_speak (fallback Kokoro embutido em D-05/D-08/D-09)
      2. tts_provider="elevenlabs" + key + !local_only → try ElevenLabs, fallback Kokoro
      3. tts_provider="murf" + key + !local_only → try Murf, fallback Kokoro
      4. tts_provider="kokoro" OR any failure OR local_only=True → Kokoro offline
      5. Kokoro unavailable (engine=None) → silent

    Args:
        text: Full response text to speak
        config: JarvisConfig controlling provider selection
    """
    if not text.strip():
        return

    if config.tts_provider == "none":
        return

    # Phase 86: Chatterbox path (offline, mesma prioridade que Kokoro).
    # _chatterbox_speak já contém a lógica completa de fallback para Kokoro
    # nos casos D-05 (timeout), D-08 (runtime error), D-09 (ImportError).
    if config.tts_provider == "chatterbox":
        _chatterbox_speak(text, config)
        return

    # Cloud provider path (D-06, D-10)
    if not config.local_only:
        if config.tts_provider == "elevenlabs":
            if config.elevenlabs_api_key:
                if _elevenlabs_speak(text, config.elevenlabs_api_key):
                    return
                _console().print("[TTS] ElevenLabs indisponível — usando Kokoro offline.")
            else:
                _console().print("[TTS] ElevenLabs sem chave — usando Kokoro offline.")
        elif config.tts_provider == "murf":
            if config.murf_api_key:
                if _murf_speak(text, config.murf_api_key):
                    return
                _console().print("[TTS] Murf indisponível — usando Kokoro offline.")
            else:
                _console().print("[TTS] Murf sem chave — usando Kokoro offline.")

    # Kokoro offline path (primary or fallback)
    _kokoro_speak(text, config)
```

Pontos críticos:
- `_chatterbox_speak` faz seu próprio fallback para Kokoro internamente — `speak()` NÃO precisa duplicar essa lógica para o caso chatterbox.
- `init_tts` continua carregando Kokoro mesmo quando provider=chatterbox (D-24 — Kokoro é fallback offline universal, _kokoro_speak precisa de engine pronto).
- `sd.play(audio_data, samplerate=_CHATTERBOX_SAMPLE_RATE)` usa o sample rate Phase 86 (24000, igual ao Kokoro — sem resample).
- `.squeeze().cpu().numpy().astype(np.float32)` é a fórmula EXATA do RESEARCH (Pitfall 4 + A4). NÃO simplificar.
- D-10 reforçado: NUNCA `config.tts_provider = "kokoro"` em path de fallback. Test test_fallback_does_not_persist_config_change valida exatamente isso re-lendo config do disco.
- `_is_playing = False` + `set_state("idle")` ANTES de chamar `_kokoro_speak` no caminho except (Kokoro vai re-setar).
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_tts.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def _chatterbox_speak" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep 'wav_tensor.squeeze().cpu().numpy().astype(np.float32)' apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1 linha (Pitfall 4 exato)
    - `grep '_chatterbox_warmup_event.wait(timeout=15' apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha (D-05)
    - `grep 'language_id="pt"' apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 2 ocorrências (warmup + _chatterbox_speak)
    - `grep '_chatterbox_disabled = True' apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha em _chatterbox_speak (D-08 sticky)
    - `grep 'config.tts_provider = "kokoro"\|config.tts_provider = .kokoro.' apps/desktop-py/src/jarvis_desktop/tts.py` retorna 0 (D-10 — fallback NÃO altera config)
    - `grep 'if config.tts_provider == "chatterbox"' apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 2 ocorrências (1 em init_tts + 1 em speak)
    - `pytest tests/test_tts.py -x --tb=short` exit 0 — TODA a suite verde (7 Kokoro/cloud + 15 Chatterbox = 22 testes)
    - Backwards-compat preservada: testes test_init_tts, test_kokoro_speak, test_elevenlabs_fallback, test_murf_fallback, test_local_only_mode, test_stop_tts, test_espeak_ng_missing_handling todos verdes
    - `python -c "import ast; ast.parse(open('apps/desktop-py/src/jarvis_desktop/tts.py').read())"` exit 0
  </acceptance_criteria>
  <done>
    `_chatterbox_speak` implementado com fallback completo (D-05/D-08/D-10). `init_tts` dispara warmup quando provider=chatterbox + ainda carrega Kokoro (D-24). `speak` roteia chatterbox antes dos cloud providers. Suite de testes 22/22 verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário (texto do chat) → `_chatterbox_speak(text, config)` | Texto digitado pelo usuário vai para `engine.generate(text, language_id="pt")`. |
| `_chatterbox_engine` → torch (CPU/GPU memory) | Geração consome RAM/VRAM proporcional ao tamanho do texto. |
| thread daemon → módulo state | `_warmup_worker` escreve em `_chatterbox_engine`, `_chatterbox_device`, `_chatterbox_available` (proteção via `_lock`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | DoS | Resource exhaustion — CUDA OOM em runtime | mitigate | D-08: erro marca `_chatterbox_disabled=True` em memória, próxima fala vai para Kokoro. Sem hot-swap (D-15). Aceito que texto enorme em CPU lento pode travar — usuário tem stop_tts(). |
| T-86-V5-02 | Tampering | Texto do usuário injetado em `engine.generate()` | accept | Não há injection clássico (não é shell/SQL). Phase 88 vai sanitizar emotion tags `[unknown]` em texto. Phase 86 passa raw text. |
| T-86-V7-02 | Info disclosure | `str(exc)` em mensagem de fallback pode vazar paths | mitigate | Mensagem usa `{type(exc).__name__}: {exc}` — exc string vem do torch/chatterbox que tipicamente é curta ("CUDA out of memory"); paths absolutos raros. Aceito como trade-off de debuggability. |
| T-86-CONCURR-01 | Tampering | Race condition entre thread de warmup e set_provider() | mitigate | `with _lock:` em set_provider e na atribuição final do warmup worker. `threading.Event` é thread-safe por design. |

Severidade: LOW/MEDIUM. Single-user local-only sem network exposure direto.
</threat_model>

<verification>
Após Tasks 1 e 2 — suite completa deve passar:
```bash
cd apps/desktop-py && uv run pytest tests/test_tts.py -x --tb=short
```
Esperado: 22 testes verdes (7 existentes Kokoro/cloud + 15 novos Chatterbox).

**Smoke manual (Manual-Only — VALIDATION.md linha 78):**
```bash
cd apps/desktop-py && uv sync --extra chatterbox
uv run python -c "from jarvis_desktop.config import JarvisConfig; from jarvis_desktop.tts import init_tts, speak; c = JarvisConfig(tts_provider='chatterbox'); init_tts(c); import time; time.sleep(20); speak('Olá, eu sou o JARVIS.', c)"
```
Esperado: print "Chatterbox: aquecendo (CUDA/MPS/DirectML/CPU)...", depois "Chatterbox: ... Pronto." (com label D-21 exato: "GPU (CUDA)" / "GPU (MPS)" / "GPU (DirectML)" / "CPU"), depois "falando (Chatterbox)..." e áudio em PT-BR audível.

**Anti-regression:**
```bash
cd apps/desktop-py && uv run pytest -x --tb=short
```
Esperado: suíte inteira do projeto verde (não só test_tts.py).
</verification>

<success_criteria>
- [ ] `_start_chatterbox_warmup` implementada — Thread daemon, cascade, Event signaling, labels D-21 exatas (CHTB-03)
- [ ] `_chatterbox_speak` implementado — D-05 timeout, D-08 disabled-session, D-10 sem-persistir, D-22 mensagens, D-24 sample rate 24kHz (CHTB-04)
- [ ] `init_tts` dispara warmup quando provider=chatterbox; ainda carrega Kokoro como fallback offline (D-24)
- [ ] `speak` roteia para `_chatterbox_speak` antes dos cloud providers
- [ ] Suite de testes 22/22 verde (7 Kokoro/cloud existentes + 15 Chatterbox novos)
- [ ] CHTB-01 completo (set_provider + cascade + warmup operacional), CHTB-03 completo (warmup async + D-05 timeout), CHTB-04 completo (fallback runtime + sticky disabled + sem-persistir)
- [ ] D-21 device labels enforced via acceptance criterion automatizado (grep retorna >= 3 ocorrências GPU + CPU presente)
- [ ] Smoke manual documentado em SUMMARY para o usuário rodar antes de fechar phase
</success_criteria>

<output>
Após completar Tasks 1 e 2, criar `.planning/phases/86-identificacao-de-voz-speaker-recognition/86-04-SUMMARY.md` com:
- Suite final de testes: 22/22 verdes
- Caminho do smoke manual (VALIDATION.md "Manual-Only Verifications" linha 78) com instruções e expectativas claras
- Lista das 4 funções públicas/privadas finais: `init_tts` (estendida), `speak` (estendida), `_start_chatterbox_warmup`, `_chatterbox_speak`
- Confirmação dos 4 success criteria do ROADMAP Phase 86:
  1. Usuário seleciona "chatterbox" no /config e ouve áudio — coberto (depende de Phase 88 para /config UX; smoke valida path direto)
  2. uv sync não conflita — coberto por Plan 02 + smoke
  3. Warmup elimina cold start — coberto por _start_chatterbox_warmup + D-01
  4. Erros caem silenciosamente para Kokoro — coberto por D-05/D-08/D-09 implementados em _chatterbox_speak
- Confirmação D-21: labels exatas ("GPU (CUDA)" / "GPU (MPS)" / "GPU (DirectML)" / "CPU") presentes em `_start_chatterbox_warmup` — verificado via `grep -E 'GPU \(CUDA\)|GPU \(MPS\)|GPU \(DirectML\)' apps/desktop-py/src/jarvis_desktop/tts.py | wc -l` retorna `>= 3`
- Reminder de que Phase 87 (voice cloning) e Phase 88 (emotion tags + /config UX) são as fases seguintes
</output>
