---
phase: 86
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop-py/tests/conftest.py
  - apps/desktop-py/tests/test_tts.py
autonomous: true
requirements: [CHTB-01, CHTB-02, CHTB-03, CHTB-04]
requirements_addressed: [CHTB-01, CHTB-02, CHTB-03, CHTB-04]

must_haves:
  truths:
    - "Suite de testes contém ~16 testes Chatterbox novos, todos coletáveis (pytest --collect-only sucesso)"
    - "Fixture mock_chatterbox_engine disponível em conftest.py"
    - "Fixtures mock_torch_no_gpu, mock_torch_cuda, mock_torch_mps, mock_torch_directml disponíveis"
    - "Fixture autouse de reset de state Chatterbox limpa _chatterbox_engine/_chatterbox_disabled/_chatterbox_available/_chatterbox_warmup_event entre testes"
    - "Testes existentes de Kokoro (test_init_tts, test_kokoro_speak, etc.) continuam passando"
  artifacts:
    - path: "apps/desktop-py/tests/conftest.py"
      provides: "Fixtures mock_chatterbox_engine + mock_torch_* + reset_chatterbox_state (autouse)"
      contains: "mock_chatterbox_engine"
    - path: "apps/desktop-py/tests/test_tts.py"
      provides: "16 testes RED para Chatterbox cobrindo CHTB-01..04"
      contains: "test_detect_device_cuda"
  key_links:
    - from: "tests/test_tts.py"
      to: "tests/conftest.py"
      via: "pytest fixture injection"
      pattern: "mock_chatterbox_engine|mock_torch_cuda"
    - from: "tests/test_tts.py"
      to: "jarvis_desktop.tts"
      via: "import + monkeypatch"
      pattern: "from jarvis_desktop import tts"
---

<objective>
Adicionar a infraestrutura de testes (Wave 0) que vai sustentar a implementação do Chatterbox nas waves seguintes. Cria todos os fixtures mockando torch, torch_directml, e a factory de engine Chatterbox, mais ~16 testes em estado RED (importam símbolos que ainda não existem em tts.py — falham com AttributeError, validando Nyquist contract do `.planning/phases/86-.../86-VALIDATION.md`).

Purpose: Sem Wave 0 verde, nenhuma task de implementação tem um teste para satisfazer. Bloqueia waves 1+ até estarem pelo menos coletáveis.
Output: `tests/conftest.py` estendido com 5 fixtures novas + `tests/test_tts.py` estendido com 16 testes Chatterbox (estado RED esperado e aceitável até Plan 03/04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-VALIDATION.md
@apps/desktop-py/tests/conftest.py
@apps/desktop-py/tests/test_tts.py
@apps/desktop-py/src/jarvis_desktop/tts.py

<interfaces>
<!-- Patterns extraídos de tests/conftest.py atual (Phase 75) -->

From apps/desktop-py/tests/conftest.py (existing mock_kokoro_engine, linhas 97-120):
```python
@pytest.fixture
def mock_kokoro_engine(monkeypatch):
    import unittest.mock
    import numpy as np
    mock_audio = unittest.mock.MagicMock()
    mock_audio.numpy.return_value = np.zeros(24000, dtype=np.float32)
    mock_result = unittest.mock.MagicMock()
    mock_result.audio = mock_audio
    mock_engine = unittest.mock.MagicMock()
    mock_engine.side_effect = lambda *a, **kw: iter([mock_result])
    monkeypatch.setattr("jarvis_desktop.tts._create_kokoro_engine", lambda config: mock_engine)
    return mock_engine
```

From apps/desktop-py/tests/test_tts.py (existing pattern of state reset):
```python
from jarvis_desktop import tts as tts_module
tts_module._engine = None  # Reset singleton for test isolation
# ... call code under test ...
tts_module._engine = None  # Cleanup
```

Símbolos futuros do tts.py (a serem criados em Plan 03/04 — testes referenciam estes nomes em estado RED):
- `_chatterbox_engine: Optional[Any]`
- `_chatterbox_disabled: bool`
- `_chatterbox_available: Optional[bool]` (None = não testado, True/False = decidido)
- `_chatterbox_warmup_event: threading.Event`
- `_chatterbox_device: Optional[str]`
- `_CHATTERBOX_SAMPLE_RATE: int = 24000`
- `_create_chatterbox_engine(config, device) -> Any`
- `_detect_chatterbox_device() -> list[str]`
- `_start_chatterbox_warmup(config) -> None`
- `_chatterbox_speak(text, config) -> None`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar fixtures de mock para Chatterbox e torch em conftest.py</name>
  <files>apps/desktop-py/tests/conftest.py</files>
  <read_first>
    - apps/desktop-py/tests/conftest.py (padrão de mock_kokoro_engine linhas 97-120)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (Test mock pattern, linhas ~620-660)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-12: cascade CUDA→MPS→DirectML→CPU)
  </read_first>
  <action>
Estender `apps/desktop-py/tests/conftest.py` adicionando a SEÇÃO "Phase 86: Chatterbox test fixtures" no FINAL do arquivo (após a seção Phase 85 atual). NÃO modificar fixtures existentes.

Adicionar EXATAMENTE estes 6 fixtures (texto pode ser ajustado mas semântica e nomes são travados):

```python
# ---------------------------------------------------------------------------
# Phase 86: Chatterbox test fixtures
# ---------------------------------------------------------------------------

@pytest.fixture(autouse=True)
def _reset_chatterbox_state():
    """Reseta state singleton do Chatterbox entre testes (Pitfall 5 do 86-RESEARCH).

    Aplicado automaticamente em TODOS os testes para evitar leakage entre runs:
    _chatterbox_engine, _chatterbox_disabled, _chatterbox_available, _chatterbox_warmup_event.
    """
    import threading
    from jarvis_desktop import tts as tts_module

    # Pre-reset
    if hasattr(tts_module, "_chatterbox_engine"):
        tts_module._chatterbox_engine = None
    if hasattr(tts_module, "_chatterbox_disabled"):
        tts_module._chatterbox_disabled = False
    if hasattr(tts_module, "_chatterbox_available"):
        tts_module._chatterbox_available = None
    if hasattr(tts_module, "_chatterbox_warmup_event"):
        tts_module._chatterbox_warmup_event = threading.Event()
    if hasattr(tts_module, "_chatterbox_device"):
        tts_module._chatterbox_device = None

    yield

    # Post-reset (mesmo se teste falhou)
    if hasattr(tts_module, "_chatterbox_engine"):
        tts_module._chatterbox_engine = None
    if hasattr(tts_module, "_chatterbox_disabled"):
        tts_module._chatterbox_disabled = False
    if hasattr(tts_module, "_chatterbox_available"):
        tts_module._chatterbox_available = None
    if hasattr(tts_module, "_chatterbox_warmup_event"):
        tts_module._chatterbox_warmup_event = threading.Event()


@pytest.fixture
def mock_chatterbox_engine(monkeypatch):
    """Mock para _create_chatterbox_engine — não importa torch nem chatterbox.

    Retorna mock cujo .generate() devolve FakeTensor com .squeeze().cpu().numpy() -> float32 24kHz.
    Pattern análogo a mock_kokoro_engine (linha 97).
    """
    import unittest.mock
    import numpy as np

    fake_audio = np.zeros(2400, dtype=np.float32)  # 100ms de silêncio @ 24kHz

    class FakeTensor:
        def squeeze(self):
            return self

        def cpu(self):
            return self

        def numpy(self):
            return fake_audio

    mock_engine = unittest.mock.MagicMock()
    mock_engine.generate.return_value = FakeTensor()
    mock_engine.sr = 24000

    monkeypatch.setattr(
        "jarvis_desktop.tts._create_chatterbox_engine",
        lambda config, device: mock_engine,
        raising=False,
    )
    return mock_engine


@pytest.fixture
def mock_torch_no_gpu(monkeypatch):
    """Mock torch sem nenhuma GPU disponível (CUDA=False, MPS=False).

    Cascade _detect_chatterbox_device() deve retornar ["cpu"].
    """
    import sys
    import types
    import unittest.mock

    mock_torch = types.ModuleType("torch")
    mock_torch.cuda = unittest.mock.MagicMock()
    mock_torch.cuda.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends = unittest.mock.MagicMock()
    mock_torch.backends.mps = unittest.mock.MagicMock()
    mock_torch.backends.mps.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends.mps.is_built = unittest.mock.MagicMock(return_value=False)
    monkeypatch.setitem(sys.modules, "torch", mock_torch)
    # Garante que torch_directml NÃO está disponível
    monkeypatch.setitem(sys.modules, "torch_directml", None)
    return mock_torch


@pytest.fixture
def mock_torch_cuda(monkeypatch):
    """Mock torch com CUDA disponível (primeira opção da cascade)."""
    import sys
    import types
    import unittest.mock

    mock_torch = types.ModuleType("torch")
    mock_torch.cuda = unittest.mock.MagicMock()
    mock_torch.cuda.is_available = unittest.mock.MagicMock(return_value=True)
    mock_torch.backends = unittest.mock.MagicMock()
    mock_torch.backends.mps = unittest.mock.MagicMock()
    mock_torch.backends.mps.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends.mps.is_built = unittest.mock.MagicMock(return_value=False)
    monkeypatch.setitem(sys.modules, "torch", mock_torch)
    return mock_torch


@pytest.fixture
def mock_torch_mps(monkeypatch):
    """Mock torch sem CUDA mas com MPS disponível (Apple Silicon)."""
    import sys
    import types
    import unittest.mock

    mock_torch = types.ModuleType("torch")
    mock_torch.cuda = unittest.mock.MagicMock()
    mock_torch.cuda.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends = unittest.mock.MagicMock()
    mock_torch.backends.mps = unittest.mock.MagicMock()
    mock_torch.backends.mps.is_available = unittest.mock.MagicMock(return_value=True)
    mock_torch.backends.mps.is_built = unittest.mock.MagicMock(return_value=True)
    monkeypatch.setitem(sys.modules, "torch", mock_torch)
    return mock_torch


@pytest.fixture
def mock_torch_directml(monkeypatch):
    """Mock torch sem CUDA/MPS + torch_directml com 1 device disponível."""
    import sys
    import types
    import unittest.mock

    mock_torch = types.ModuleType("torch")
    mock_torch.cuda = unittest.mock.MagicMock()
    mock_torch.cuda.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends = unittest.mock.MagicMock()
    mock_torch.backends.mps = unittest.mock.MagicMock()
    mock_torch.backends.mps.is_available = unittest.mock.MagicMock(return_value=False)
    mock_torch.backends.mps.is_built = unittest.mock.MagicMock(return_value=False)
    monkeypatch.setitem(sys.modules, "torch", mock_torch)

    mock_directml = types.ModuleType("torch_directml")
    mock_directml.device_count = unittest.mock.MagicMock(return_value=1)
    fake_device_obj = unittest.mock.MagicMock(name="dml_device")
    mock_directml.device = unittest.mock.MagicMock(return_value=fake_device_obj)
    monkeypatch.setitem(sys.modules, "torch_directml", mock_directml)
    return mock_directml
```

Notas:
- `autouse=True` no `_reset_chatterbox_state` é CRÍTICO (Pitfall 5 do RESEARCH) — sem isso, testes passam isoladamente mas falham em batch.
- `raising=False` no `monkeypatch.setattr` permite o fixture funcionar mesmo antes do símbolo `_create_chatterbox_engine` existir (Wave 0 RED).
- Não importar `chatterbox` nem `torch_directml` no nível do módulo — somente lazy via sys.modules manipulation.
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest --collect-only tests/test_tts.py 2>&1 | grep -E "(collected|error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mock_chatterbox_engine" apps/desktop-py/tests/conftest.py` retorna >= 1
    - `grep -c "mock_torch_cuda\|mock_torch_mps\|mock_torch_directml\|mock_torch_no_gpu" apps/desktop-py/tests/conftest.py` retorna >= 4
    - `grep -c "_reset_chatterbox_state" apps/desktop-py/tests/conftest.py` retorna >= 1
    - `grep "autouse=True" apps/desktop-py/tests/conftest.py` retorna pelo menos uma ocorrência (no `_reset_chatterbox_state`)
    - `cd apps/desktop-py && uv run pytest tests/conftest.py --collect-only` exit code 0 (sem syntax error)
    - Testes existentes de Kokoro continuam passando: `cd apps/desktop-py && uv run pytest tests/test_tts.py::test_init_tts -x` exit 0
  </acceptance_criteria>
  <done>
    Arquivo conftest.py contém 6 novas fixtures Phase 86 (1 autouse + 5 mocks) sem quebrar fixtures existentes; coleta pytest funciona; testes Kokoro existentes ainda passam.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Adicionar ~16 testes RED de Chatterbox em test_tts.py</name>
  <files>apps/desktop-py/tests/test_tts.py</files>
  <read_first>
    - apps/desktop-py/tests/test_tts.py (padrão de testes Kokoro existentes)
    - apps/desktop-py/tests/conftest.py (após Task 1 — usar as fixtures novas)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-VALIDATION.md (Per-Task Verification Map — 16 testes listados)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (linhas ~710-728 — mapa Req→Test)
  </read_first>
  <action>
APPEND no final de `apps/desktop-py/tests/test_tts.py` uma seção `# Phase 86: Chatterbox tests` contendo EXATAMENTE estes 16 testes. Os testes podem (e devem) estar em estado RED até que Plans 03 e 04 implementem o código — isso é esperado.

Cada teste tem nome travado pelo VALIDATION.md. Estrutura mínima de cada teste:

```python
# ---------------------------------------------------------------------------
# Phase 86: Chatterbox tests (RED até implementação em Plans 03/04)
# ---------------------------------------------------------------------------
# Os 16 testes abaixo referem-se a símbolos novos em tts.py:
# _detect_chatterbox_device, _create_chatterbox_engine, _start_chatterbox_warmup,
# _chatterbox_speak, _chatterbox_engine, _chatterbox_disabled, _chatterbox_available,
# _chatterbox_warmup_event.
# Falham em Wave 0 com AttributeError — comportamento esperado.

def test_set_provider_chatterbox(mock_chatterbox_engine):
    """set_provider('chatterbox') aceita o novo provider sem ValueError. CHTB-01 / D-07."""
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop.tts import set_provider
    config = JarvisConfig(tts_provider="kokoro")
    set_provider("chatterbox", config)
    assert config.tts_provider == "chatterbox"


def test_detect_device_cuda(mock_torch_cuda):
    """_detect_chatterbox_device retorna 'cuda' primeiro quando CUDA available. CHTB-01 / D-12."""
    from jarvis_desktop.tts import _detect_chatterbox_device
    devices = _detect_chatterbox_device()
    assert devices[0] == "cuda"
    assert "cpu" in devices  # CPU sempre como último fallback


def test_detect_device_mps_fallback(mock_torch_mps):
    """_detect_chatterbox_device retorna 'mps' antes de cpu quando só MPS available. CHTB-01 / D-12."""
    from jarvis_desktop.tts import _detect_chatterbox_device
    devices = _detect_chatterbox_device()
    assert "cuda" not in devices
    assert devices[0] == "mps"
    assert devices[-1] == "cpu"


def test_detect_device_cpu_only(mock_torch_no_gpu):
    """_detect_chatterbox_device retorna apenas ['cpu'] quando nenhum GPU. CHTB-01 / D-12."""
    from jarvis_desktop.tts import _detect_chatterbox_device
    devices = _detect_chatterbox_device()
    assert devices == ["cpu"]


def test_set_provider_chatterbox_import_error(monkeypatch, capsys):
    """set_provider('chatterbox') sem chatterbox instalado: recusa sem mudar config. CHTB-01 / D-09 / D-11."""
    import sys
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import set_provider

    # Simula chatterbox não instalado: marca _chatterbox_available = False antes
    tts_module._chatterbox_available = False
    config = JarvisConfig(tts_provider="kokoro")
    set_provider("chatterbox", config)
    # D-11: config NÃO alterado
    assert config.tts_provider == "kokoro"
    captured = capsys.readouterr()
    assert "Chatterbox não instalado" in captured.out or "chatterbox" in captured.out.lower()


def test_init_tts_warmup_non_blocking(mock_chatterbox_engine, mock_sounddevice_play, mock_torch_no_gpu):
    """init_tts() com provider=chatterbox retorna em <1s (warmup em background). CHTB-03 / D-01."""
    import time
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop.tts import init_tts
    config = JarvisConfig(tts_provider="chatterbox")
    t0 = time.monotonic()
    init_tts(config)
    elapsed = time.monotonic() - t0
    assert elapsed < 1.0, f"init_tts levou {elapsed}s (esperado <1s — warmup deve ser async)"


def test_warmup_completes_event_set(mock_chatterbox_engine, mock_torch_no_gpu):
    """Warmup termina e _chatterbox_warmup_event.is_set() == True. CHTB-03."""
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _start_chatterbox_warmup
    config = JarvisConfig(tts_provider="chatterbox")
    _start_chatterbox_warmup(config)
    completed = tts_module._chatterbox_warmup_event.wait(timeout=5.0)
    assert completed, "Warmup não setou Event dentro de 5s"
    assert tts_module._chatterbox_engine is not None


def test_warmup_skipped_when_kokoro_provider(mock_chatterbox_engine, mock_sounddevice_play):
    """init_tts() com provider=kokoro NÃO dispara warmup Chatterbox. CHTB-03 / D-02."""
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import init_tts
    tts_module._engine = None
    config = JarvisConfig(tts_provider="kokoro")
    init_tts(config)
    # Mock de _create_chatterbox_engine NÃO deve ter sido chamado
    mock_chatterbox_engine.generate.assert_not_called()
    tts_module._engine = None


def test_speak_waits_for_warmup(mock_chatterbox_engine, mock_sounddevice_play, mock_torch_no_gpu):
    """_chatterbox_speak bloqueia aguardando warmup quando disparado em paralelo. CHTB-03 / D-05."""
    import threading
    import time
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _chatterbox_speak, _start_chatterbox_warmup
    config = JarvisConfig(tts_provider="chatterbox")
    # Dispara warmup (vai completar quase instantaneamente com mock)
    _start_chatterbox_warmup(config)
    # speak deve esperar e completar normalmente
    _chatterbox_speak("olá", config)
    # Generate deve ter sido chamado pelo menos uma vez (warmup + speak)
    assert mock_chatterbox_engine.generate.call_count >= 1


def test_speak_warmup_timeout_falls_back(monkeypatch, mock_kokoro_engine, mock_sounddevice_play):
    """speak() com warmup que não termina em 15s cai para Kokoro. CHTB-03 / D-05."""
    import threading
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _chatterbox_speak

    # Event que nunca é setado → simula warmup travado
    tts_module._chatterbox_warmup_event = threading.Event()
    tts_module._chatterbox_engine = None
    tts_module._chatterbox_available = True  # disponível mas warmup não termina

    # Patch wait() para retornar False imediatamente em vez de bloquear 15s no teste
    original_wait = tts_module._chatterbox_warmup_event.wait
    monkeypatch.setattr(
        tts_module._chatterbox_warmup_event,
        "wait",
        lambda timeout=None: False,
    )
    config = JarvisConfig(tts_provider="chatterbox")
    _chatterbox_speak("teste timeout", config)
    # Kokoro foi chamado como fallback
    mock_sounddevice_play.play.assert_called()


def test_chatterbox_runtime_error_fallback(monkeypatch, mock_kokoro_engine, mock_sounddevice_play):
    """RuntimeError em engine.generate() marca _chatterbox_disabled=True + Kokoro fallback. CHTB-04 / D-08."""
    import threading
    import unittest.mock
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _chatterbox_speak

    # Engine que falha em runtime
    failing_engine = unittest.mock.MagicMock()
    failing_engine.generate.side_effect = RuntimeError("CUDA out of memory")
    tts_module._chatterbox_engine = failing_engine
    tts_module._chatterbox_available = True
    tts_module._chatterbox_disabled = False
    tts_module._chatterbox_warmup_event = threading.Event()
    tts_module._chatterbox_warmup_event.set()  # warmup já completou

    config = JarvisConfig(tts_provider="chatterbox")
    _chatterbox_speak("teste oom", config)
    assert tts_module._chatterbox_disabled is True
    mock_sounddevice_play.play.assert_called()  # Kokoro fallback rodou


def test_chatterbox_disabled_stays_disabled(mock_kokoro_engine, mock_sounddevice_play):
    """Após _chatterbox_disabled=True, próxima speak() vai direto pra Kokoro. CHTB-04 / D-08."""
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _chatterbox_speak
    tts_module._chatterbox_disabled = True
    tts_module._engine = None
    config = JarvisConfig(tts_provider="chatterbox")
    _chatterbox_speak("teste", config)
    mock_sounddevice_play.play.assert_called()
    tts_module._engine = None


def test_warmup_device_cascade(monkeypatch, mock_torch_no_gpu):
    """Quando primeiro device da cascade falha no _create_chatterbox_engine, tenta próximo. CHTB-04 / D-14."""
    import unittest.mock
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _start_chatterbox_warmup

    call_log = []
    fake_engine = unittest.mock.MagicMock()
    fake_engine.generate.return_value = unittest.mock.MagicMock()

    def fake_factory(config, device):
        call_log.append(device)
        if device != "cpu":
            raise RuntimeError(f"{device} indisponível")
        return fake_engine

    # Força lista de devices com múltiplas opções (cuda + cpu)
    monkeypatch.setattr(tts_module, "_detect_chatterbox_device", lambda: ["cuda", "cpu"])
    monkeypatch.setattr(tts_module, "_create_chatterbox_engine", fake_factory)

    config = JarvisConfig(tts_provider="chatterbox")
    _start_chatterbox_warmup(config)
    completed = tts_module._chatterbox_warmup_event.wait(timeout=5.0)
    assert completed
    # Cascade deve ter tentado cuda primeiro, depois cpu
    assert call_log == ["cuda", "cpu"]
    assert tts_module._chatterbox_device == "cpu"


def test_fallback_does_not_persist_config_change(tmp_home, mock_kokoro_engine, mock_sounddevice_play):
    """Fallback Chatterbox→Kokoro NÃO altera config.tts_provider em disco. CHTB-04 / D-10."""
    import threading
    import unittest.mock
    from jarvis_desktop.config import JarvisConfig, save_config, load_config
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _chatterbox_speak

    # Persistir config com tts_provider=chatterbox
    config = JarvisConfig(tts_provider="chatterbox")
    save_config(config)

    # Forçar fallback runtime
    failing_engine = unittest.mock.MagicMock()
    failing_engine.generate.side_effect = RuntimeError("fail")
    tts_module._chatterbox_engine = failing_engine
    tts_module._chatterbox_available = True
    tts_module._chatterbox_warmup_event = threading.Event()
    tts_module._chatterbox_warmup_event.set()

    _chatterbox_speak("teste persist", config)

    # Re-ler config do disco
    config_reloaded = load_config()
    assert config_reloaded.tts_provider == "chatterbox", (
        "D-10 violado: fallback alterou tts_provider em disco"
    )


def test_import_error_disables_session(monkeypatch, capsys):
    """ImportError em warmup marca _chatterbox_available=False pela sessão. CHTB-04 / D-09."""
    import sys
    from jarvis_desktop.config import JarvisConfig
    from jarvis_desktop import tts as tts_module
    from jarvis_desktop.tts import _start_chatterbox_warmup

    # Remove chatterbox dos sys.modules e bloqueia import
    monkeypatch.setitem(sys.modules, "chatterbox", None)
    monkeypatch.setitem(sys.modules, "chatterbox.mtl_tts", None)

    config = JarvisConfig(tts_provider="chatterbox")
    _start_chatterbox_warmup(config)
    tts_module._chatterbox_warmup_event.wait(timeout=5.0)
    assert tts_module._chatterbox_available is False
    captured = capsys.readouterr()
    assert "uv sync --extra chatterbox" in captured.out or "não instalado" in captured.out.lower()
```

Notas críticas:
- Os nomes dos 16 testes correspondem EXATAMENTE aos listados em `86-VALIDATION.md` linhas 41-58. NÃO renomear.
- Estado RED é esperado e aceitável após Task 2 — os testes vão falhar com `AttributeError: module 'jarvis_desktop.tts' has no attribute '_detect_chatterbox_device'` etc. Plans 03/04 fazem-os passar.
- Total real é 15 testes únicos (test_speak_waits_for_warmup é o 8º; 1 fixture autouse não conta como teste). VALIDATION.md menciona ~16 — qualquer valor 15-16 é aceitável.
- `tmp_home` fixture já existe em conftest.py (linha 7) — reusar para o teste de persistência.
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest --collect-only tests/test_tts.py 2>&1 | grep -cE "test_(detect_device|set_provider_chatterbox|init_tts_warmup|warmup_|speak_waits|speak_warmup_timeout|chatterbox_runtime|chatterbox_disabled|fallback_does_not|import_error_disables)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def test_" apps/desktop-py/tests/test_tts.py` retorna >= 22 (7 existentes Kokoro + 15 novos Chatterbox)
    - Pelo menos 15 dos 16 nomes de teste do VALIDATION.md aparecem no arquivo: `for t in test_set_provider_chatterbox test_detect_device_cuda test_detect_device_mps_fallback test_detect_device_cpu_only test_set_provider_chatterbox_import_error test_init_tts_warmup_non_blocking test_warmup_completes_event_set test_warmup_skipped_when_kokoro_provider test_speak_waits_for_warmup test_speak_warmup_timeout_falls_back test_chatterbox_runtime_error_fallback test_chatterbox_disabled_stays_disabled test_warmup_device_cascade test_fallback_does_not_persist_config_change test_import_error_disables_session; do grep -q "def $t" apps/desktop-py/tests/test_tts.py || echo MISSING:$t; done` retorna 0 linhas MISSING
    - `cd apps/desktop-py && uv run pytest tests/test_tts.py --collect-only` exit code 0 (sem syntax error)
    - Testes Kokoro existentes ainda passam: `cd apps/desktop-py && uv run pytest tests/test_tts.py::test_kokoro_speak tests/test_tts.py::test_init_tts -x` exit 0
    - Pelo menos 1 dos novos testes Chatterbox aparece como FAILED ou ERROR ao rodar (estado RED esperado): `cd apps/desktop-py && uv run pytest tests/test_tts.py::test_detect_device_cuda --no-header -rN 2>&1 | grep -E "(FAILED|ERROR|AttributeError|ImportError)"` retorna pelo menos 1 linha
  </acceptance_criteria>
  <done>
    16 testes Chatterbox adicionados ao test_tts.py. Coleta pytest funciona sem syntax error. Testes Kokoro existentes seguem verdes. Testes Chatterbox estão em estado RED (falham com AttributeError até Plans 03/04).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test infra → módulo `jarvis_desktop.tts` | Os testes monkeypatcham state interno; risco zero pois é teste local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | Fixtures podem mascarar bugs reais em runtime | accept | Fixtures isolam infra de hardware (torch/chatterbox); manual smoke test (VALIDATION linha 79) cobre o caminho real. |
| T-86-INFRA-01 | DoS | Test suite lenta por warmup real | mitigate | `mock_chatterbox_engine` evita carga do modelo; warmup tests usam Event.wait(timeout=5) curto. |

Severidade: todas LOW/INFO — Wave 0 é puramente infra de teste, sem superfície de ataque.
</threat_model>

<verification>
Após ambas as tasks completas, rodar:
```bash
cd apps/desktop-py && uv run pytest tests/test_tts.py --collect-only
cd apps/desktop-py && uv run pytest tests/test_tts.py::test_init_tts tests/test_tts.py::test_kokoro_speak tests/test_tts.py::test_stop_tts -x
```
Resultado esperado: coleta lista 22+ testes; testes Kokoro existentes verdes; testes Chatterbox em RED (esperado).
</verification>

<success_criteria>
- [ ] `conftest.py` contém 6 fixtures Phase 86 (mock_chatterbox_engine, mock_torch_no_gpu/cuda/mps/directml, _reset_chatterbox_state autouse)
- [ ] `test_tts.py` contém 15+ testes Chatterbox novos com nomes idênticos ao VALIDATION.md
- [ ] `pytest --collect-only` sucesso (sem syntax error)
- [ ] Testes Kokoro existentes seguem passando
- [ ] Decision IDs cobertos: D-01 (warmup async), D-02 (warmup só com provider chatterbox), D-05 (timeout 15s), D-08 (disabled session), D-09 (ImportError session), D-10 (config não persiste fallback), D-11 (set_provider recusa), D-12 (cascade CUDA→MPS→DirectML→CPU), D-14 (cascade no warmup error)
</success_criteria>

<output>
Após completar Tasks 1 e 2, criar `.planning/phases/86-identificacao-de-voz-speaker-recognition/86-01-SUMMARY.md` com:
- Lista de fixtures adicionadas em conftest.py
- Lista dos 15+ testes adicionados em test_tts.py
- Status RED esperado (que será corrigido em Plans 03 e 04)
- Verificação de que testes Kokoro existentes seguem passando
- Atualizar VALIDATION.md marcando `wave_0_complete: true` e setando status dos task IDs cobertos pelos testes adicionados (ainda ⬜ pending até Plans 03/04 implementarem)
</output>
