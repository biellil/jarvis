---
phase: 86
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/desktop-py/src/jarvis_desktop/tts.py
autonomous: true
requirements: [CHTB-01]
requirements_addressed: [CHTB-01]

must_haves:
  truths:
    - "Símbolos `_chatterbox_engine`, `_chatterbox_disabled`, `_chatterbox_available`, `_chatterbox_warmup_event`, `_chatterbox_device`, `_CHATTERBOX_SAMPLE_RATE` existem em tts.py"
    - "Função `_detect_chatterbox_device()` retorna lista ordenada cobrindo cascade CUDA→MPS→DirectML→CPU"
    - "Função `_create_chatterbox_engine(config, device)` faz lazy import de ChatterboxMultilingualTTS (não chatterbox.tts)"
    - "`set_provider('chatterbox', config)` valida disponibilidade e recusa graciosamente se ImportError"
    - "set_provider continua aceitando os providers existentes (kokoro, elevenlabs, murf, none)"
    - "Testes de cascade de device (test_detect_device_*) passam após este plan"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "Símbolos novos para Chatterbox + função _detect_chatterbox_device + _create_chatterbox_engine + set_provider estendido"
      contains: "_create_chatterbox_engine"
  key_links:
    - from: "set_provider('chatterbox', config)"
      to: "_chatterbox_available flag"
      via: "tentativa de import lazy + atribuição em-memória (D-09)"
      pattern: "from chatterbox.mtl_tts import ChatterboxMultilingualTTS"
    - from: "_create_chatterbox_engine"
      to: "ChatterboxMultilingualTTS.from_pretrained"
      via: "torch_device object (não string) para directml"
      pattern: "ChatterboxMultilingualTTS.from_pretrained"
---

<objective>
Adicionar a base do Chatterbox em `tts.py`: variáveis singleton novas, cascade de detecção de device, factory de engine com lazy imports, e extensão de `set_provider()` para aceitar `"chatterbox"` com validação de import gracioso. NÃO inclui warmup nem `_chatterbox_speak()` (esses ficam no Plan 04). Os testes `test_detect_device_*` e `test_set_provider_chatterbox*` ficam GREEN após este plan.

Purpose: Implementa CHTB-01 (parte: aceitação do provider + cascade de device + ImportError handling). Mantém Kokoro como padrão; todo código novo é aditivo.
Output: tts.py estendido com ~120 linhas novas, sem regressão nos paths existentes (Kokoro / ElevenLabs / Murf).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md
@apps/desktop-py/src/jarvis_desktop/tts.py
@apps/desktop-py/src/jarvis_desktop/config.py
@apps/desktop-py/tests/test_tts.py

<interfaces>
<!-- Padrões existentes em tts.py que devem ser replicados literalmente -->

From apps/desktop-py/src/jarvis_desktop/tts.py (linha 40 — singleton pattern):
```python
_engine: Optional[Any] = None   # Kokoro engine instance (lazy-loaded)
_lock = threading.Lock()
_stop_event = threading.Event()
_is_playing: bool = False
_KOKORO_SAMPLE_RATE = 24000
```

From apps/desktop-py/src/jarvis_desktop/tts.py (linhas 204-224 — factory pattern):
```python
def _create_kokoro_engine(config: JarvisConfig) -> Any:
    import warnings
    from kokoro import KPipeline  # Lazy import
    lang_code = config.kokoro_voice[0] if config.kokoro_voice else "p"
    with warnings.catch_warnings():
        warnings.simplefilter("ignore")
        return KPipeline(lang_code=lang_code, repo_id="hexgrad/Kokoro-82M")
```

From apps/desktop-py/src/jarvis_desktop/tts.py (linhas 151-184 — set_provider pattern):
```python
def set_provider(provider: str, config: "JarvisConfig") -> None:
    valid_providers = {"kokoro", "elevenlabs", "murf", "none"}
    if provider not in valid_providers:
        raise ValueError(f"[TTS] Provider desconhecido: {provider!r}. Válidos: {sorted(valid_providers)}")
    with _lock:
        config.tts_provider = provider
        if provider == "kokoro":
            _engine = None
            console.print(f"[TTS] Provider definido: kokoro ...", highlight=False)
        else:
            console.print(f"[TTS] Provider definido: {provider}.", highlight=False)
```

API exata do Chatterbox (RESEARCH linhas 552-560, 566-578):
```python
from chatterbox.mtl_tts import ChatterboxMultilingualTTS

# device pode ser string ("cuda", "mps", "cpu") OU torch.device object (necessário para directml)
model = ChatterboxMultilingualTTS.from_pretrained(device=torch_device)

# generate exige language_id como kwarg (sem ele, default English ruim)
wav = model.generate("Olá", language_id="pt")  # Returns torch.Tensor shape (1, N), float32, 24kHz
# model.sr == 24000
```

DirectML pattern (RESEARCH Pattern 1 + Pitfall em torch_directml.device_count):
```python
if device == "directml":
    import torch_directml
    torch_device = torch_directml.device()  # Object, não string
else:
    torch_device = device  # string OK
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar singletons + _detect_chatterbox_device em tts.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/tts.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/tts.py (linhas 1-50 — header + singletons existentes)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (linhas 299-328 — Pattern 2 cascade)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-12, D-13, D-25)
  </read_first>
  <behavior>
    - Test test_detect_device_cuda: cascade retorna ['cuda', 'cpu'] quando só CUDA disponível
    - Test test_detect_device_mps_fallback: cascade retorna ['mps', 'cpu'] (sem CUDA)
    - Test test_detect_device_cpu_only: cascade retorna ['cpu'] quando sem nenhum GPU
    - Singletons _chatterbox_engine=None, _chatterbox_disabled=False, _chatterbox_available=None, _chatterbox_warmup_event (Event não-setado), _chatterbox_device=None, _CHATTERBOX_SAMPLE_RATE=24000 acessíveis após import
  </behavior>
  <action>
Estender `apps/desktop-py/src/jarvis_desktop/tts.py`:

**(1) Adicionar singletons** logo após linha 46 (`_KOKORO_SAMPLE_RATE = 24000`):

```python
# ---------------------------------------------------------------------------
# Phase 86: Chatterbox singletons (D-25 — separados do Kokoro para warmup paralelo)
# ---------------------------------------------------------------------------
_chatterbox_engine: Optional[Any] = None     # ChatterboxMultilingualTTS instance, lazy
_chatterbox_disabled: bool = False           # D-08 — fallback runtime persiste pela sessão
_chatterbox_available: Optional[bool] = None # D-09 — None=não testado, True=OK, False=ImportError
_chatterbox_warmup_event = threading.Event() # D-05 — sinaliza fim do warmup; .wait(timeout=15)
_chatterbox_device: Optional[str] = None     # "cuda" | "mps" | "directml" | "cpu" — set após warmup

_CHATTERBOX_SAMPLE_RATE = 24000  # S3GEN_SR confirmado no source (chatterbox/models/s3gen/const.py)
```

**(2) Adicionar `_detect_chatterbox_device` no final do arquivo** (após `_murf_speak`):

```python
# ---------------------------------------------------------------------------
# Phase 86: Chatterbox helpers (CHTB-01)
# ---------------------------------------------------------------------------

def _detect_chatterbox_device() -> list[str]:
    """Cascade de detecção de device para Chatterbox (D-12).

    Ordem: CUDA → MPS → DirectML → CPU. Apenas verifica disponibilidade
    básica via APIs públicas do torch — NÃO carrega modelo (carga vem no warmup).

    DirectML é pulado silenciosamente se `torch_directml` não estiver instalado
    (D-19: extra opcional dentro do grupo `chatterbox`).

    Returns:
        Lista ordenada de device strings disponíveis. Sempre inclui "cpu" como
        último fallback universal. Exemplos:
          ["cuda", "cpu"] — máquina com NVIDIA GPU
          ["mps", "cpu"] — macOS Apple Silicon
          ["directml", "cpu"] — Windows AMD GPU com torch-directml instalado
          ["cpu"] — máquina sem GPU compatível
    """
    import torch  # Lazy import (D-20)

    candidates: list[str] = []

    # 1. CUDA (NVIDIA Linux/Windows; ROCm Linux quando PyTorch ROCm build)
    if torch.cuda.is_available():
        candidates.append("cuda")

    # 2. MPS (macOS Apple Silicon)
    if (
        hasattr(torch.backends, "mps")
        and torch.backends.mps.is_available()
        and torch.backends.mps.is_built()
    ):
        candidates.append("mps")

    # 3. DirectML (AMD/Intel GPU no Windows). D-19: pacote opcional, ausência não é erro.
    try:
        import torch_directml  # type: ignore[import-not-found]
        if torch_directml.device_count() > 0:
            candidates.append("directml")
    except ImportError:
        pass

    # 4. CPU sempre como fallback universal
    candidates.append("cpu")

    return candidates
```

Pontos críticos:
- Lazy import `import torch` dentro da função (D-20). NUNCA no topo do módulo.
- `hasattr(torch.backends, "mps")` para versões muito antigas de torch que não têm o backend MPS (defensivo, baixo custo).
- `torch_directml.device_count() > 0` é o check correto (RESEARCH Anti-Pattern: import bare não basta).
- Mantém `_lock`, `_stop_event`, `_is_playing` existentes; NÃO renomeia nem move (são compartilhados com Kokoro).
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_tts.py::test_detect_device_cuda tests/test_tts.py::test_detect_device_mps_fallback tests/test_tts.py::test_detect_device_cpu_only -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^_chatterbox_engine" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep -c "^_chatterbox_disabled" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep -c "^_chatterbox_available" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep -c "_chatterbox_warmup_event = threading.Event" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep -c "_CHATTERBOX_SAMPLE_RATE = 24000" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep -c "^def _detect_chatterbox_device" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep "import torch_directml" apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha (dentro da função, lazy)
    - `grep "^import torch\b\|^from torch\b" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 0 (lazy only — torch nunca no topo)
    - `pytest tests/test_tts.py::test_detect_device_cuda -x` exit 0
    - `pytest tests/test_tts.py::test_detect_device_mps_fallback -x` exit 0
    - `pytest tests/test_tts.py::test_detect_device_cpu_only -x` exit 0
    - Testes Kokoro existentes ainda passam: `pytest tests/test_tts.py::test_init_tts tests/test_tts.py::test_kokoro_speak -x` exit 0
  </acceptance_criteria>
  <done>
    Singletons Phase 86 declarados, `_detect_chatterbox_device` implementada com lazy import torch e torch_directml, 3 testes de cascade verdes, sem regressão Kokoro.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar _create_chatterbox_engine + estender set_provider para 'chatterbox'</name>
  <files>apps/desktop-py/src/jarvis_desktop/tts.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/tts.py (após Task 1)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (Pattern 1 linhas 263-292, Anti-Patterns linha 466-477, Pitfall 2 linha 502)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-07, D-09, D-11, D-20, D-25)
  </read_first>
  <behavior>
    - Test test_set_provider_chatterbox: set_provider("chatterbox", config) muda config.tts_provider para "chatterbox" sem erro quando _chatterbox_available != False
    - Test test_set_provider_chatterbox_import_error: set_provider("chatterbox", config) com _chatterbox_available=False NÃO altera config.tts_provider, imprime mensagem com "uv sync --extra chatterbox" ou "não instalado"
    - set_provider continua rejeitando providers inválidos com ValueError (provider 'foo' inexistente)
    - set_provider continua aceitando "kokoro", "elevenlabs", "murf", "none" sem mudança de comportamento
  </behavior>
  <action>
Estender `apps/desktop-py/src/jarvis_desktop/tts.py`:

**(1) Adicionar `_create_chatterbox_engine` logo após `_detect_chatterbox_device`**:

```python
def _create_chatterbox_engine(config: JarvisConfig, device: str) -> Any:
    """Instancia ChatterboxMultilingualTTS no device escolhido (D-20 lazy import).

    USAR `chatterbox.mtl_tts.ChatterboxMultilingualTTS`, NÃO `chatterbox.tts.ChatterboxTTS`.
    O `ChatterboxTTS` é English-only e não aceita `language_id` (Pitfall 2 do RESEARCH).

    Args:
        config: JarvisConfig (não usa campos nesta phase — Phase 87 vai consumir
                cloned_voice_path quando audio_prompt_path entrar em jogo).
        device: "cuda" | "mps" | "directml" | "cpu" (string da cascade).

    Returns:
        Instância de ChatterboxMultilingualTTS pronta para .generate(text, language_id="pt").

    Raises:
        ImportError: se chatterbox-tts não estiver instalado. Caller deve marcar
                     _chatterbox_available = False (D-09).
        RuntimeError: se device escolhido falhar no load. Caller deve tentar
                      próximo device da cascade (D-14).
    """
    import warnings
    from chatterbox.mtl_tts import ChatterboxMultilingualTTS  # Lazy (D-20). NÃO de chatterbox.tts.

    # DirectML usa device object (não string). Outros backends aceitam string.
    if device == "directml":
        import torch_directml  # type: ignore[import-not-found]
        torch_device: Any = torch_directml.device()
    else:
        torch_device = device

    with warnings.catch_warnings():
        warnings.simplefilter("ignore")  # Suprime UserWarning/FutureWarning do torch no init
        return ChatterboxMultilingualTTS.from_pretrained(device=torch_device)
```

**(2) Estender `set_provider()`** — modificar a função existente (linhas 151-184). Adicionar `"chatterbox"` ao set válido e branch específico:

Substituir o corpo de `set_provider` por:

```python
def set_provider(provider: str, config: "JarvisConfig") -> None:
    """Switch TTS provider at runtime (from config menu).

    For Kokoro: resets _engine so next speak() call lazy-initializes with updated config.
    For Chatterbox (Phase 86): valida import lazy; se falhar, mantém provider anterior (D-11).
                               Se já marcado _chatterbox_available=False (D-09), recusa sem
                               alterar config. Warmup é disparado em init_tts/Plan 04, não aqui.
    For cloud providers (ElevenLabs, Murf): no engine needed — API keys read at call time.
    Updates config.tts_provider in-place; caller must call save_config() after.

    Args:
        provider: "kokoro" | "chatterbox" | "elevenlabs" | "murf" | "none"
        config: JarvisConfig instance to update (tts_provider field written in-place)

    Raises:
        ValueError: if provider is not one of the supported values
    """
    global _engine, _chatterbox_available, _chatterbox_engine

    from jarvis_desktop import ui
    console = ui.get_console()

    valid_providers = {"kokoro", "chatterbox", "elevenlabs", "murf", "none"}  # D-07
    if provider not in valid_providers:
        raise ValueError(f"[TTS] Provider desconhecido: {provider!r}. Válidos: {sorted(valid_providers)}")

    with _lock:
        if provider == "chatterbox":
            # D-09: se já detectado ImportError em sessão anterior, recusa sem alterar config
            if _chatterbox_available is False:
                console.print(
                    "[TTS] Chatterbox não instalado. Rode: uv sync --extra chatterbox",
                    highlight=False,
                )
                # D-11: NÃO altera config.tts_provider — usuário continua no provider anterior
                return

            # Testa import lazy (D-09)
            try:
                from chatterbox.mtl_tts import ChatterboxMultilingualTTS  # noqa: F401
                _chatterbox_available = True
            except ImportError:
                _chatterbox_available = False
                console.print(
                    "[TTS] Chatterbox não instalado. Rode: uv sync --extra chatterbox",
                    highlight=False,
                )
                # D-11: NÃO altera config.tts_provider
                return

            # Import OK — aceita o provider. Reset do engine para forçar warmup novo.
            config.tts_provider = "chatterbox"
            _chatterbox_engine = None
            console.print(
                "[TTS] Provider definido: chatterbox (warmup na próxima fala ou em init_tts).",
                highlight=False,
            )
            return

        # Branches existentes (kokoro/elevenlabs/murf/none) — manter comportamento original
        config.tts_provider = provider

        if provider == "kokoro":
            # Reset engine so next speak() lazy-initializes with current config
            _engine = None
            console.print("[TTS] Provider definido: kokoro (inicializa na próxima fala).", highlight=False)
        else:
            # Cloud providers / "none" são stateless — sem engine para resetar
            # _engine (Kokoro) remains as offline fallback per speak() logic
            console.print(f"[TTS] Provider definido: {provider}.", highlight=False)
```

Pontos críticos:
- O `valid_providers` agora tem 5 entries (D-07). NÃO esquecer `"none"`.
- `_chatterbox_available is False` é check específico (não `not _chatterbox_available`) porque `None` ≠ `False` (estado inicial = não testado).
- `from chatterbox.mtl_tts import ChatterboxMultilingualTTS` — caminho EXATO. NUNCA `from chatterbox.tts` ou `from chatterbox import ChatterboxMultilingualTTS` (Pitfall 2).
- O reset de `_chatterbox_engine = None` força warmup novo (Plan 04 detecta isso e dispara).
- Mensagem de erro contém literalmente `"uv sync --extra chatterbox"` (test_set_provider_chatterbox_import_error grep-checks isso).
- `with _lock:` protege transição (atomicidade, D-25).
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_tts.py::test_set_provider_chatterbox tests/test_tts.py::test_set_provider_chatterbox_import_error -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def _create_chatterbox_engine" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1
    - `grep "from chatterbox.mtl_tts import ChatterboxMultilingualTTS" apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 2 ocorrências (em `_create_chatterbox_engine` e em `set_provider`)
    - `grep "from chatterbox.tts import" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 0 (NUNCA usar a versão English-only — Pitfall 2)
    - `grep "valid_providers = {.*chatterbox.*}" apps/desktop-py/src/jarvis_desktop/tts.py` retorna 1 linha (provider adicionado ao set)
    - `grep "uv sync --extra chatterbox" apps/desktop-py/src/jarvis_desktop/tts.py` retorna pelo menos 1 linha (mensagem de erro D-09)
    - `pytest tests/test_tts.py::test_set_provider_chatterbox -x` exit 0
    - `pytest tests/test_tts.py::test_set_provider_chatterbox_import_error -x` exit 0
    - Backwards-compat: `pytest tests/test_tts.py::test_init_tts tests/test_tts.py::test_kokoro_speak tests/test_tts.py::test_elevenlabs_fallback tests/test_tts.py::test_murf_fallback tests/test_tts.py::test_local_only_mode -x` exit 0 (5 testes Kokoro/cloud existentes seguem verdes)
    - `python -c "import ast; ast.parse(open('apps/desktop-py/src/jarvis_desktop/tts.py').read())"` exit 0 (syntax válida)
  </acceptance_criteria>
  <done>
    `_create_chatterbox_engine` adicionada com lazy import de `chatterbox.mtl_tts.ChatterboxMultilingualTTS`; `set_provider` aceita `"chatterbox"` com 3 caminhos (já-disabled, novo-ImportError, success); testes test_set_provider_chatterbox* verdes; testes Kokoro/ElevenLabs/Murf existentes intactos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → set_provider("chatterbox") | Input é string controlada via menu /config; validada contra set fixo (no injection risk). |
| chatterbox.mtl_tts module → torch.load (pickle) | Modelo baixado pelo `from_pretrained` (Task em Plan 04, não aqui). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-V5-01 | Tampering | Input não-validado em `set_provider(provider)` | mitigate | Validação contra set fixo `{"kokoro","chatterbox","elevenlabs","murf","none"}` antes de qualquer ação; raise `ValueError` para input desconhecido. |
| T-86-V7-01 | Info disclosure | Mensagem de erro em ImportError pode vazar path do site-packages | mitigate | Mensagem hardcoded sem `str(exc)` no caminho do D-09; só instrução `uv sync --extra chatterbox`. |
| T-86-02 | Tampering | Pickle deserialization em torch.load | accept | Não trigarado neste plan (factory só importa classe; `from_pretrained` é chamado pelo Plan 04). |

Severidade: LOW. Mitigations via input whitelist + mensagens hardcoded.
</threat_model>

<verification>
Após Tasks 1 e 2:
```bash
cd apps/desktop-py && uv run pytest tests/test_tts.py::test_detect_device_cuda tests/test_tts.py::test_detect_device_mps_fallback tests/test_tts.py::test_detect_device_cpu_only tests/test_tts.py::test_set_provider_chatterbox tests/test_tts.py::test_set_provider_chatterbox_import_error -x --tb=short
```
Resultado esperado: 5 testes verdes. Plan 04 cuida do warmup/speak (10 testes restantes ficam em RED até lá).

Backwards-compat:
```bash
cd apps/desktop-py && uv run pytest tests/test_tts.py::test_init_tts tests/test_tts.py::test_kokoro_speak tests/test_tts.py::test_elevenlabs_fallback tests/test_tts.py::test_murf_fallback tests/test_tts.py::test_local_only_mode tests/test_tts.py::test_stop_tts tests/test_tts.py::test_espeak_ng_missing_handling -x
```
Resultado esperado: 7 testes verdes.
</verification>

<success_criteria>
- [ ] 6 novos símbolos singleton declarados (`_chatterbox_engine`, `_chatterbox_disabled`, `_chatterbox_available`, `_chatterbox_warmup_event`, `_chatterbox_device`, `_CHATTERBOX_SAMPLE_RATE`)
- [ ] `_detect_chatterbox_device()` implementada com cascade CUDA→MPS→DirectML→CPU e lazy import torch (D-12, D-20)
- [ ] `_create_chatterbox_engine()` importa de `chatterbox.mtl_tts` (NUNCA `chatterbox.tts`); aceita device string ou cria `torch_directml.device()` object (D-20, Pitfall 2)
- [ ] `set_provider("chatterbox", config)` aceita o provider, lida com 3 caminhos (já-disabled, novo-ImportError, success), nunca altera config em caso de ImportError (D-09, D-11)
- [ ] 5 testes Chatterbox verdes (3 cascade + 2 set_provider)
- [ ] 7 testes Kokoro/cloud existentes seguem verdes
- [ ] CHTB-01 (partial — aceitação do provider + cascade detection) coberto. CHTB-03/CHTB-04 ficam para Plan 04.
</success_criteria>

<output>
Após completar Tasks 1 e 2, criar `.planning/phases/86-identificacao-de-voz-speaker-recognition/86-03-SUMMARY.md` com:
- Lista dos novos símbolos em tts.py
- Status do teste suite (5 verdes Chatterbox + 7 verdes Kokoro = 12 total; 10 testes Chatterbox ainda em RED esperando Plan 04)
- Reconfirmação de Pitfall 2 (mtl_tts, não tts) com grep evidence
- Confirmação de que nenhum import torch/chatterbox saiu do nível lazy
</output>
