---
phase: 86
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/desktop-py/pyproject.toml
  - apps/desktop-py/scripts/smoke_chatterbox_install.sh
autonomous: true
requirements: [CHTB-02]
requirements_addressed: [CHTB-02]

must_haves:
  truths:
    - "Usuário pode executar `uv sync --extra chatterbox` sem conflito de versão"
    - "Após o sync, `from faster_whisper import WhisperModel` continua funcionando"
    - "Grupo opcional `chatterbox` está declarado em `[project.optional-dependencies]`"
    - "Override de torch==2.6.0 está documentado em `[tool.uv].override-dependencies`"
  artifacts:
    - path: "apps/desktop-py/pyproject.toml"
      provides: "Extra opcional `chatterbox` + override de torch para resolver conflito directml↔chatterbox"
      contains: "chatterbox-tts==0.1.7"
    - path: "apps/desktop-py/scripts/smoke_chatterbox_install.sh"
      provides: "Script de smoke test que valida sync sem quebrar faster-whisper"
      min_lines: 10
  key_links:
    - from: "pyproject.toml [project.optional-dependencies].chatterbox"
      to: "[tool.uv].override-dependencies"
      via: "uv resolver"
      pattern: "torch==2.6.0"
    - from: "scripts/smoke_chatterbox_install.sh"
      to: "faster_whisper.WhisperModel"
      via: "python -c smoke import"
      pattern: "from faster_whisper import WhisperModel"
---

<objective>
Empacotar o Chatterbox como extra opcional em `apps/desktop-py/pyproject.toml` (grupo `chatterbox`) com todas as deps que normalmente seriam instaladas pelo pacote (já que `chatterbox-tts` é instalado efetivamente com `--no-deps` via override), e configurar `[tool.uv].override-dependencies` para forçar `torch==2.6.0` (em vez do `torch==2.4.1` exigido pelo `torch-directml`). Cria também script de smoke install.

Purpose: Resolver o conflito documentado no RESEARCH (Pitfall 1) entre `torch==2.6.0` exigido pelo chatterbox-tts 0.1.7 e `torch==2.4.1` exigido pelo torch-directml 0.2.5.dev240914, mantendo `faster-whisper==1.2.1` + `ctranslate2 <5,>=4.0` funcional.
Output: `pyproject.toml` atualizado + `scripts/smoke_chatterbox_install.sh` para CI/manual validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md
@.planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md
@apps/desktop-py/pyproject.toml

<interfaces>
<!-- Padrão atual de extras em pyproject.toml -->

From apps/desktop-py/pyproject.toml (existing voice-cloning extras):
```toml
[project.optional-dependencies]
dev = [...]
voice-cloning = [
    "librosa>=0.10.0",
]
```

From apps/desktop-py/pyproject.toml (existing tool.uv override):
```toml
[tool.uv]
override-dependencies = [
    "tflite-runtime; sys_platform == 'linux' and python_version < '3.12'",
]
```

Versões EXATAS verificadas no RESEARCH (linhas 222-231):
- chatterbox-tts == 0.1.7 (PyPI 2026-03-26)
- torch == 2.6.0 (par com torchaudio 2.6.0 e torchvision 0.21.0)
- torch-directml == 0.2.5.dev240914 (Windows only)
- transformers == 5.2.0
- diffusers == 0.29.0
- librosa == 0.11.0
- safetensors == 0.5.3
- conformer == 0.3.2
- pykakasi == 2.3.0
- ctranslate2 == 4.7.2 (já transitiva via faster-whisper)
</interfaces>
</context>

<traceability_notes>
**D-18 / CHTB-02 reinterpretation (revisão pós-checker):**

CONTEXT.md D-18 e ROADMAP CHTB-02 mencionam literalmente "via `--no-deps`". A implementação NÃO usa o flag `--no-deps` literal porque:

1. **`uv` não suporta `--no-deps` por-pacote em `pyproject.toml`** (RESEARCH linha 204) — o flag só existe no CLI (`pip install --no-deps`), não como diretiva declarativa em manifesto.
2. **Equivalência funcional adotada** = `[tool.uv].override-dependencies` (força `torch==2.6.0`) + listagem explícita das transitives compatíveis no extra `chatterbox`.
3. **Resultado idêntico ao `--no-deps`**: `chatterbox-tts` e `torch-directml` NÃO arrastam suas próprias pins de `torch` (cumprindo a intenção declarada de D-18).

Esta nota também aparece como comentário inline em `pyproject.toml` linkando para este plan (86-02-PLAN.md), garantindo traceability futura.
</traceability_notes>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar extra 'chatterbox' e override de torch em pyproject.toml</name>
  <files>apps/desktop-py/pyproject.toml</files>
  <read_first>
    - apps/desktop-py/pyproject.toml (state atual, especialmente `[project.optional-dependencies]` linhas 26-35 e `[tool.uv]` linhas 48-53)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (linhas 174-220: estratégia "Opção A — override-dependencies")
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-CONTEXT.md (D-17, D-18, D-19)
  </read_first>
  <action>
**Traceability D-18 / CHTB-02 (literal text "via --no-deps"):**

CONTEXT.md D-18 e ROADMAP CHTB-02 dizem literalmente "via `--no-deps`". A implementação NÃO usa o flag literal porque:
- `uv` não suporta `--no-deps` por-pacote em `pyproject.toml` (RESEARCH linha 204) — é flag CLI, não diretiva de manifesto.
- **Equivalência funcional adotada:** `[tool.uv].override-dependencies` (força `torch==2.6.0`) + listagem explícita das transitives compatíveis no extra `chatterbox`.
- **Resultado idêntico ao `--no-deps`:** `chatterbox-tts` e `torch-directml` NÃO arrastam suas próprias pins conflitantes de `torch` (cumprindo a intenção de D-18).
- Comentário inline em `pyproject.toml` linka esta justificativa para auditoria futura.

---

Editar `apps/desktop-py/pyproject.toml` para:

1. **Adicionar grupo `chatterbox` em `[project.optional-dependencies]`** (logo após `voice-cloning`):

```toml
chatterbox = [
    # CHTB-02 (Phase 86) — Chatterbox TTS multilingual com voice cloning + emotion control.
    # NOTA D-18: equivalente funcional a `--no-deps` — uv não suporta --no-deps em pyproject.toml,
    # então usamos override-dependencies (abaixo) + listagem explícita das transitives.
    # Ver justificativa completa em .planning/phases/86-identificacao-de-voz-speaker-recognition/86-02-PLAN.md
    "chatterbox-tts==0.1.7",
    # torch 2.6.0 explícito — confirmado compatível com ctranslate2<5,>=4.0 (faster-whisper)
    "torch==2.6.0",
    "torchaudio==2.6.0",
    # Deps transitivas pinadas pelo chatterbox-tts 0.1.7 (instaladas explicitamente para
    # garantir versões exatas; o resolver do uv vai aplicar override-dependencies para torch).
    "transformers==5.2.0",
    "diffusers==0.29.0",
    "librosa==0.11.0",
    "safetensors==0.5.3",
    "conformer==0.3.2",
    "pykakasi==2.3.0",
    "s3tokenizer",
    "resemble-perth>=1.0.0",
    "omegaconf",
    "pyloudnorm",
    "spacy-pkuseg",
    # Windows AMD/Intel GPU — opcional, pulado em Linux/macOS via marker
    "torch-directml==0.2.5.dev240914; sys_platform == 'win32'",
]
```

2. **Estender `[tool.uv].override-dependencies`** (preservar o override existente de `tflite-runtime`):

```toml
[tool.uv]
# tflite-runtime has no cp312 wheels — openwakeword uses onnxruntime on non-Linux;
# override prevents uv cross-platform resolution failure on Python 3.12
#
# torch / torchvision override (Phase 86, D-18): força torch 2.6.0 mesmo quando
# torch-directml 0.2.5.dev240914 pin torch==2.4.1. Confirmado funcional em produção
# pela comunidade Chatterbox-TTS-Server (RESEARCH Pitfall 1, A1).
# Equivalência funcional de `--no-deps` declarado em CHTB-02 / D-18 — ver 86-02-PLAN.md.
override-dependencies = [
    "tflite-runtime; sys_platform == 'linux' and python_version < '3.12'",
    "torch==2.6.0",
    "torchvision==0.21.0",
]
```

Não modificar nada mais (dependencies de `[project]`, scripts, hatch config, pytest config, dependency-groups).

**Justificativa para listar deps transitivas em vez de usar `--no-deps`:**
- `uv` não suporta `--no-deps` por-pacote em `pyproject.toml` (RESEARCH linha 204)
- Listar transitivas + override = mesmo efeito que `--no-deps`, mas com `uv sync` como entrypoint único (RESEARCH "Opção A recomendada")

**NÃO incluir** (deferred ou fora de escopo):
- Versão de gradio (~200MB desnecessário, RESEARCH linha 149)
- chatterbox-tts sem version pin (queremos 0.1.7 exato)
- Mexer em deps existentes (faster-whisper, kokoro, sounddevice etc.)

**Atenção ao Pitfall 8 (A5 do RESEARCH):** `transformers==5.2.0` pode quebrar outros usos. Auditar via `grep -r "^import transformers\|^from transformers" apps/desktop-py/src/` antes — atualmente NÃO há uso direto no projeto (kokoro pode ter como transitiva flexível). Se a auditoria revelar uso direto, criar entrada de Open Question em SUMMARY mas seguir com 5.2.0 (CHTB-02 requer).
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "import tomllib; data = tomllib.loads(open('pyproject.toml').read()); assert 'chatterbox' in data['project']['optional-dependencies'], 'extra chatterbox missing'; assert 'chatterbox-tts==0.1.7' in data['project']['optional-dependencies']['chatterbox'], 'chatterbox-tts pin missing'; assert 'torch==2.6.0' in data['tool']['uv']['override-dependencies'], 'torch override missing'; print('pyproject ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^chatterbox = \[" apps/desktop-py/pyproject.toml` retorna 1 (grupo declarado)
    - `grep "chatterbox-tts==0.1.7" apps/desktop-py/pyproject.toml` retorna pelo menos 1 linha
    - `grep "torch==2.6.0" apps/desktop-py/pyproject.toml` retorna pelo menos 2 linhas (em `chatterbox` extras + em `override-dependencies`)
    - `grep "torch-directml==0.2.5.dev240914; sys_platform == 'win32'" apps/desktop-py/pyproject.toml` retorna 1 linha
    - `grep "tflite-runtime" apps/desktop-py/pyproject.toml` retorna 1 linha (override original preservado)
    - `cd apps/desktop-py && python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"` exit 0 (TOML válido)
    - Dependências de `[project].dependencies` (faster-whisper, kokoro, sounddevice, etc.) inalteradas: `diff <(grep -A 20 '^dependencies = \[' apps/desktop-py/pyproject.toml) ...` — count de linhas de deps continua igual (21 elementos)
    - **D-18 traceability (revisão pós-checker):** `grep -E "D-18|--no-deps|86-02-PLAN" apps/desktop-py/pyproject.toml` retorna pelo menos 2 linhas (comentário inline no extra + comentário no override-dependencies linkando para o plan)
    - **A5 audit (revisão pós-checker — transformers 5.2.0 vs kokoro):** `grep -rE "^(import|from) transformers" apps/desktop-py/src/ | wc -l` retorna `0` (kokoro NÃO usa `transformers` diretamente — confirma que pin `transformers==5.2.0` no extra chatterbox não quebra Phase 75 / Kokoro)
  </acceptance_criteria>
  <done>
    pyproject.toml inclui extra `chatterbox` com 14+ entradas, override-dependencies estendido com `torch==2.6.0` + `torchvision==0.21.0`, TOML válido, deps existentes intactas. Comentários inline registram traceability D-18/CHTB-02 (equivalência funcional a `--no-deps`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar script de smoke install para validar sync sem quebrar faster-whisper</name>
  <files>apps/desktop-py/scripts/smoke_chatterbox_install.sh</files>
  <read_first>
    - apps/desktop-py/pyproject.toml (após Task 1 — referência para o comando de install)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-VALIDATION.md (linha 48 — comando de smoke install)
    - .planning/phases/86-identificacao-de-voz-speaker-recognition/86-RESEARCH.md (Pitfall 1, Pitfall 8)
  </read_first>
  <action>
Criar `apps/desktop-py/scripts/` (mkdir -p) e dentro o arquivo `smoke_chatterbox_install.sh` com permissão de execução. Conteúdo EXATO:

```bash
#!/usr/bin/env bash
# Phase 86 — Smoke test de instalação do Chatterbox (CHTB-02).
#
# Valida que `uv sync --extra chatterbox` resolve sem conflito e que
# faster-whisper continua funcional após o sync.
#
# Uso:
#   ./scripts/smoke_chatterbox_install.sh
#
# Sai com exit 0 em sucesso, exit não-zero em qualquer falha.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
APP_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$APP_DIR"

echo "[smoke] cwd: $APP_DIR"
echo "[smoke] Etapa 1/3 — uv sync --extra chatterbox"
uv sync --extra chatterbox

echo "[smoke] Etapa 2/3 — verificar faster-whisper ainda importa"
uv run python -c "from faster_whisper import WhisperModel; print('faster-whisper OK')"

echo "[smoke] Etapa 3/3 — verificar chatterbox importa (lazy — sem load do modelo)"
uv run python -c "from chatterbox.mtl_tts import ChatterboxMultilingualTTS; print('chatterbox OK')"

echo "[smoke] Sucesso — Chatterbox + faster-whisper coexistem."
```

Após criar, dar permissão de execução:
```bash
chmod +x apps/desktop-py/scripts/smoke_chatterbox_install.sh
```

**NÃO incluir** no script:
- Carregamento real do modelo (`from_pretrained`) — baixa ~800MB, fora do escopo de smoke
- Geração de áudio — Phase 86 não cobre playback automatizado
- Validação de GPU específica — VALIDATION lista isso como Manual-Only

**Por que `set -euo pipefail`:** garante que qualquer falha (sync, import) aborta o script imediatamente com exit não-zero, fácil de usar em CI.
  </action>
  <verify>
    <automated>test -x apps/desktop-py/scripts/smoke_chatterbox_install.sh && bash -n apps/desktop-py/scripts/smoke_chatterbox_install.sh && echo "script ok"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/desktop-py/scripts/smoke_chatterbox_install.sh` exit 0
    - `test -x apps/desktop-py/scripts/smoke_chatterbox_install.sh` exit 0 (executável)
    - `bash -n apps/desktop-py/scripts/smoke_chatterbox_install.sh` exit 0 (syntax válida)
    - `grep "uv sync --extra chatterbox" apps/desktop-py/scripts/smoke_chatterbox_install.sh` retorna 1 linha
    - `grep "from faster_whisper import WhisperModel" apps/desktop-py/scripts/smoke_chatterbox_install.sh` retorna 1 linha
    - `grep "from chatterbox.mtl_tts import ChatterboxMultilingualTTS" apps/desktop-py/scripts/smoke_chatterbox_install.sh` retorna 1 linha
    - `grep "set -euo pipefail" apps/desktop-py/scripts/smoke_chatterbox_install.sh` retorna 1 linha
  </acceptance_criteria>
  <done>
    Script de smoke instalação criado, executável, com 3 etapas (sync + faster-whisper import check + chatterbox lazy import check). NÃO executar o script aqui (baixa ~800MB de modelo no primeiro `from_pretrained`, fora de escopo da task — execução real é manual conforme VALIDATION.md "Manual-Only Verifications").
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → PyPI / HuggingFace | Download de pacotes (chatterbox-tts, torch) e modelo (~800MB) vem de origens externas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-04 | Tampering | Dependency confusion / typosquatting em `chatterbox-tts` no PyPI | mitigate | Pin exato `chatterbox-tts==0.1.7` + `torch==2.6.0` + override; sem ranges abertos. uv valida hashes via uv.lock. |
| T-86-02 | Tampering | Pickle deserialization no torch.load (chatterbox baixa pesos via HF + pickle) | accept | Aceito — repo oficial `ResembleAI/chatterbox` (5k+ stars). Phase 86 não permite path do modelo controlado pelo usuário (futuro: pin commit hash com `revision=`). |
| T-86-01 | Tampering | Modelo HuggingFace adulterado | accept | huggingface_hub valida hash do snapshot; confiança no repo oficial. |
| T-86-PKG-01 | DoS | `--no-deps` workaround bypassa o resolver e pode permitir versão incompatível em ambiente diferente | mitigate | Override de torch==2.6.0 fixo em [tool.uv]; smoke script valida co-existência com faster-whisper em CI. |

Severidade: LOW (single-user local, sem auth/network exposure). Mitigations via pin estrito + smoke validation.
</threat_model>

<verification>
Após Tasks 1 e 2:
```bash
cd apps/desktop-py && python -c "import tomllib; tomllib.loads(open('pyproject.toml').read())"
test -x apps/desktop-py/scripts/smoke_chatterbox_install.sh
bash -n apps/desktop-py/scripts/smoke_chatterbox_install.sh
```

**Execução real do smoke** (Manual-Only — não rodar autonomamente):
```bash
./apps/desktop-py/scripts/smoke_chatterbox_install.sh
```
Esperado: 3 etapas verdes; tempo total ~3-10min na primeira execução (download de torch + chatterbox + transformers); subsequentes ~10s (cache uv).
</verification>

<success_criteria>
- [ ] Extra `chatterbox` declarado em `[project.optional-dependencies]` com 14+ entradas
- [ ] `torch==2.6.0` e `torchvision==0.21.0` em `[tool.uv].override-dependencies` (sem remover `tflite-runtime` existente)
- [ ] `torch-directml` com marker `sys_platform == 'win32'`
- [ ] Script `scripts/smoke_chatterbox_install.sh` executável, com `set -euo pipefail`
- [ ] CHTB-02 requirement coberto (uv sync resolve + faster-whisper continua funcional)
- [ ] Deps existentes de `[project].dependencies` inalteradas
- [ ] Traceability D-18 / CHTB-02 (literal "--no-deps") preservada via comentários inline em pyproject.toml e seção `<traceability_notes>` deste plan
- [ ] Auditoria A5 (transformers vs kokoro) com acceptance criterion automatizado: `grep transformers em apps/desktop-py/src/` retorna 0
</success_criteria>

<output>
Após completar Tasks 1 e 2, criar `.planning/phases/86-identificacao-de-voz-speaker-recognition/86-02-SUMMARY.md` com:
- Diff resumido de pyproject.toml (entries adicionadas)
- Caminho do script smoke criado
- Aviso para usuário: smoke real (`./scripts/smoke_chatterbox_install.sh`) deve ser rodado manualmente antes de declarar Phase 86 verde (Manual-Only Verification — VALIDATION.md)
- Confirmar A5 (transformers 5.2.0 não conflita): rodar `grep -r "^import transformers\|^from transformers" apps/desktop-py/src/` e reportar findings (esperado: 0 — coberto pelo acceptance criterion da Task 1)
- Confirmar traceability D-18: comentários inline em pyproject.toml linkam para 86-02-PLAN.md (equivalência funcional `--no-deps` ↔ override-dependencies + listagem explícita)
</output>
</content>
</invoke>