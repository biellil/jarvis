---
phase: 89
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-py/pyproject.toml
  - apps/desktop-py/src/jarvis_desktop/speaker.py
  - apps/desktop-py/tests/conftest.py
  - apps/desktop-py/tests/test_speaker.py
autonomous: true
requirements: [SPK-01, SPK-02, SPK-03, SPK-04, SPK-05, SPK-06, SPK-10]
requirements_addressed: [SPK-01, SPK-02, SPK-03, SPK-04, SPK-05, SPK-06, SPK-10]
tags: [speaker-recognition, embeddings, ml, audio]

must_haves:
  truths:
    - "Módulo speaker.py existe e expõe identify_speaker(), ProfileStore CRUD e enroll_speaker()"
    - "VoiceEncoder é singleton com threading.Lock (não reinstanciado por chamada)"
    - "identify_speaker(audio_numpy_16k, config) retorna dict {name, confidence, is_known, candidate_name} — candidate_name é o melhor match independente do threshold, usado pelo hybrid injection no Plan 03"
    - "Speaker com cosine ≥ threshold é classificado como is_known=True com nome correto"
    - "Speaker com cosine < threshold é classificado como is_known=False e name='unknown'"
    - "ProfileStore salva/carrega/lista/remove perfis como .npy files em ~/.jarvis/speakers/"
    - "Enrollment grava 5 utterances, calcula embedding médio via encoder.embed_speaker(), salva .npy shape (256,)"
    - "Optional dependency group [speaker] no pyproject.toml com webrtcvad-wheels antes de resemblyzer"
    - "Testes em test_speaker.py cobrem SPK-01..SPK-06 e SPK-10 com mock_voice_encoder fixture"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/speaker.py"
      provides: "VoiceEncoder singleton, ProfileStore, identify_speaker(), enroll_speaker(), _cosine_similarity()"
      exports: ["identify_speaker", "enroll_speaker", "save_profile", "load_profile", "list_profiles", "delete_profile", "_get_encoder", "_cosine_similarity"]
      min_lines: 150
    - path: "apps/desktop-py/pyproject.toml"
      provides: "Optional dependency group [speaker] = webrtcvad-wheels + resemblyzer; uv override-dependencies"
      contains: "speaker ="
    - path: "apps/desktop-py/tests/test_speaker.py"
      provides: "Testes unitários SPK-01..SPK-06, SPK-10"
      contains: "test_identify_speaker_returns_dict"
      min_lines: 120
    - path: "apps/desktop-py/tests/conftest.py"
      provides: "mock_voice_encoder fixture"
      contains: "def mock_voice_encoder"
  key_links:
    - from: "apps/desktop-py/src/jarvis_desktop/speaker.py"
      to: "resemblyzer.VoiceEncoder + resemblyzer.preprocess_wav"
      via: "lazy import dentro de _get_encoder()"
      pattern: "from resemblyzer import"
    - from: "apps/desktop-py/src/jarvis_desktop/speaker.py"
      to: "~/.jarvis/speakers/{name}.npy"
      via: "np.save / np.load com Path.home()"
      pattern: "Path.home\\(\\) / .jarvis. / .speakers."
    - from: "apps/desktop-py/tests/test_speaker.py"
      to: "speaker.identify_speaker / ProfileStore CRUD"
      via: "pytest unit tests com mock_voice_encoder + tmp_home"
      pattern: "from jarvis_desktop import speaker"
---

<objective>
Implementar o módulo `speaker.py` (núcleo do speaker recognition) com:
- VoiceEncoder singleton (lazy load resemblyzer) protegido por threading.Lock — padrão idêntico ao `_model` em stt.py
- ProfileStore: save / load / list / delete em `~/.jarvis/speakers/{name}.npy` (NumPy float32 shape (256,))
- `identify_speaker(audio, config)` → dict `{name, confidence, is_known}` usando cosine similarity contra todos os perfis
- `enroll_speaker(name, config, n=5)` → grava N utterances via `stt.record_until_silence`, calcula embedding médio via `encoder.embed_speaker()`, salva `.npy`
- Sanitização do nome do perfil (substituir espaço por `_`, rejeitar `..`, `/`, `\`, caracteres de path traversal — mitigation T-89-02)
- Optional dependency group `[speaker]` no pyproject.toml com `webrtcvad-wheels==2.0.14` listado ANTES de `resemblyzer==0.1.4`
- Suite de testes RED→GREEN cobrindo SPK-01..SPK-06 e SPK-10 com fixture `mock_voice_encoder`

**Honra:** D-01, D-02, D-03, D-04, D-05, D-07, D-14, e parte de D-13 (constante de N utterances).

Purpose: Sem este módulo isolado e testado, integrar speaker ID no pipeline de voz e no enrollment via /config seria escrever lógica complexa no caminho crítico — quebra o princípio singleton/lazy do projeto e torna debug impossível.

Output: `speaker.py` ~200 linhas, `test_speaker.py` ~180 linhas, novo grupo `[speaker]` em `pyproject.toml`, nova fixture `mock_voice_encoder` em `conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-CONTEXT.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-RESEARCH.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-VALIDATION.md
@apps/desktop-py/src/jarvis_desktop/stt.py
@apps/desktop-py/src/jarvis_desktop/config.py
@apps/desktop-py/pyproject.toml
@apps/desktop-py/tests/conftest.py

<interfaces>
<!-- Contratos extraídos do codebase que o executor precisa replicar -->

Padrão de singleton em stt.py (lines 40-43):
```python
_model: Optional[WhisperModel] = None
_lock = threading.Lock()
# Lazy init dentro de função pública com `with _lock: if _model is None: _model = WhisperModel(...)`
```

Função existente em stt.py:
```python
def record_until_silence(
    threshold_ms: int = 500,
    sample_rate: int = 16000,  # _SAMPLE_RATE
    max_duration_s: int = 60,
) -> np.ndarray:
    # Retorna float32 NumPy array 16kHz mono, shape (N,)
```

API resemblyzer (versão 0.1.4):
```python
from resemblyzer import VoiceEncoder, preprocess_wav
encoder = VoiceEncoder()  # carrega ~30MB pretrained.pt — CPU default
processed: np.ndarray = preprocess_wav(audio_np_or_path, source_sr=16000)
embedding: np.ndarray = encoder.embed_utterance(processed)  # shape (256,) float32 L2-normed
mean_emb: np.ndarray = encoder.embed_speaker(list_of_processed_wavs)  # shape (256,) float32
```

JarvisConfig pattern (config.py):
```python
class JarvisConfig(BaseModel):
    speaker_recognition_enabled: bool = Field(default=False, description="...")
    speaker_threshold: float = Field(default=0.75, description="...")
    # NOTA: estes campos NÃO são adicionados nesta plan. São adicionados no Plan 02.
    # Mas identify_speaker() já precisa LER speaker_threshold via getattr(config, 'speaker_threshold', 0.75).
```

UI pattern em stt.py:
```python
from jarvis_desktop import ui as _ui
_ui.get_console().print("[STT] ...")
# Speaker module usa prefixo "[SPK]"
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied speaker name → filesystem path | Nome digitado no /config vira `{name}.npy` em `~/.jarvis/speakers/` — risco de path traversal |
| disk audio file → resemblyzer | Não aplicável nesta plan (enrollment grava via mic, não lê de disco) |
| resemblyzer model file (~/.cache/torch/hub) | Confiança total — modelo oficial PyTorch hub, vem do pacote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | `save_profile(name, ...)` | mitigate | Sanitizar `name`: rejeitar se contém `/`, `\`, `..`, ou começa com `.`; substituir espaços por `_`; validar resultante com regex `^[A-Za-z0-9_\-]+$`. Implementado em helper `_safe_profile_name(name)` que levanta `ValueError` em entradas inválidas. |
| T-89-01-02 | DoS | `enroll_speaker()` | mitigate | Limite hard de 60s por utterance (já vem de `record_until_silence(max_duration_s=60)`); validar `len(audio) >= 16000 * 2` (2s mínimo) antes de processar — descarta e repete sem laço infinito (máx 3 retries por slot). |
| T-89-01-03 | Information Disclosure | `~/.jarvis/speakers/*.npy` | accept | Embeddings 256-dim L2-normed não permitem reconstrução de áudio. Arquivo local-only, permissão padrão do diretório home do usuário. Risco aceito. |
| T-89-01-04 | Denial of Service | VoiceEncoder model load | mitigate | Lazy + singleton — carrega só na primeira chamada. Sem retry agressivo em caso de falha de download; ImportError de resemblyzer é capturado e propagado como `RuntimeError("[SPK] resemblyzer não instalado — instale com pip install jarvis-desktop[speaker]")`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add [speaker] dependency group + mock_voice_encoder fixture</name>
  <files>apps/desktop-py/pyproject.toml, apps/desktop-py/tests/conftest.py</files>
  <read_first>
    - apps/desktop-py/pyproject.toml (ver padrão existente `[project.optional-dependencies]` com `chatterbox` em D-18; ver `[tool.uv].override-dependencies`)
    - apps/desktop-py/tests/conftest.py (ver `mock_kokoclone_encoder` lines 229-256 como template — mesmo padrão de `types.ModuleType` + `monkeypatch.setitem(sys.modules, ...)`)
    - .planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-RESEARCH.md (Common Pitfalls / Pitfall 1: webrtcvad-wheels DEVE vir antes de resemblyzer)
  </read_first>
  <behavior>
    - `pyproject.toml` tem novo grupo `[project.optional-dependencies] → speaker = ["webrtcvad-wheels==2.0.14", "resemblyzer==0.1.4"]` (nessa ordem)
    - `[tool.uv].override-dependencies` inclui linha `"webrtcvad-wheels==2.0.14"` para forçar o fork sobre `webrtcvad>=2.0.10` que resemblyzer declara
    - `tests/conftest.py` ganha fixture `mock_voice_encoder(monkeypatch)` que injeta um módulo fake `resemblyzer` em `sys.modules` com:
      - `VoiceEncoder` classe cuja instância tem `.embed_utterance(processed) -> np.ndarray shape (256,) float32` (default: vetor random fixo via `np.random.RandomState(42).randn(256).astype(np.float32) / norm`)
      - `.embed_speaker(list_of_wavs) -> np.ndarray shape (256,) float32` (default: média dos `embed_utterance` chamados em cada wav)
      - `preprocess_wav(audio, source_sr=16000)` → retorna o `audio` sem mudança (passthrough)
      - Fixture retorna o mock instance para tests verificarem assert_called_*
    - Fixture `mock_voice_encoder_call_counter` opcional: lambda que conta instanciações de `VoiceEncoder` (para teste singleton SPK-06)
  </behavior>
  <action>
    **Parte A — pyproject.toml:**
    Adicionar APÓS o grupo `chatterbox` (linha ~63) e ANTES de `[build-system]`:
    ```toml
    speaker = [
        # Phase 89 — Speaker recognition via resemblyzer GE2E d-vector.
        # webrtcvad-wheels DEVE vir antes de resemblyzer (Pitfall 1, 89-RESEARCH):
        # resemblyzer 0.1.4 declara `webrtcvad>=2.0.10` mas o original não tem wheel cp313
        # Windows; webrtcvad-wheels 2.0.14 é drop-in replacement com wheel cp313.
        "webrtcvad-wheels==2.0.14",
        "resemblyzer==0.1.4",
    ]
    ```
    Adicionar em `[tool.uv].override-dependencies` (lista existente):
    ```toml
    "webrtcvad-wheels==2.0.14",
    ```
    (mantenha os 4 overrides existentes — tflite-runtime, torch, torchvision, torchaudio — apenas adicione `webrtcvad-wheels` como 5ª linha)

    **Parte B — conftest.py:**
    Adicionar nova seção no final do arquivo (após linha 507 — fim das fixtures Phase 87):
    ```python
    # ---------------------------------------------------------------------------
    # Phase 89: Speaker Recognition test fixtures
    # ---------------------------------------------------------------------------

    @pytest.fixture
    def mock_voice_encoder(monkeypatch):
        """Mock resemblyzer.VoiceEncoder + preprocess_wav para evitar download do modelo.

        Injeta módulo fake `resemblyzer` em sys.modules com:
          - VoiceEncoder().embed_utterance(audio) → np.ndarray (256,) float32 L2-normed
          - VoiceEncoder().embed_speaker(wavs) → média dos embed_utterance dos wavs
          - preprocess_wav(audio, source_sr=...) → passthrough do audio
          - VoiceEncoder.call_count rastreia instanciações (para teste singleton SPK-06)

        Pattern análogo a mock_kokoclone_encoder (linha 229).

        Returns:
            unittest.mock.MagicMock: a instância retornada por VoiceEncoder().
        """
        import sys
        import types
        import unittest.mock
        import numpy as np

        # Embedding determinístico mas distinguível: cada chamada usa um seed do conteúdo
        def _make_embedding(seed: int = 42) -> np.ndarray:
            rng = np.random.RandomState(seed)
            vec = rng.randn(256).astype(np.float32)
            return vec / (np.linalg.norm(vec) + 1e-8)

        default_embedding = _make_embedding(42)

        instance = unittest.mock.MagicMock()
        instance.embed_utterance = unittest.mock.MagicMock(return_value=default_embedding)

        def _embed_speaker(wavs):
            # Média dos embed_utterance chamados em cada wav (D-14 via embed_speaker())
            embs = [instance.embed_utterance(w) for w in wavs]
            mean = np.mean(embs, axis=0)
            return (mean / (np.linalg.norm(mean) + 1e-8)).astype(np.float32)

        instance.embed_speaker = unittest.mock.MagicMock(side_effect=_embed_speaker)

        VoiceEncoder_class = unittest.mock.MagicMock(return_value=instance)

        fake_resemblyzer = types.ModuleType("resemblyzer")
        fake_resemblyzer.VoiceEncoder = VoiceEncoder_class
        fake_resemblyzer.preprocess_wav = lambda audio, source_sr=None: audio
        monkeypatch.setitem(sys.modules, "resemblyzer", fake_resemblyzer)

        # Reset singleton state em speaker.py para isolar testes
        try:
            from jarvis_desktop import speaker as spk
            spk._encoder = None
        except (ImportError, AttributeError):
            pass

        # Anexa o class mock à instância para testes contarem instanciações
        instance._encoder_class = VoiceEncoder_class
        return instance
    ```
  </action>
  <verify>
    <automated>cd apps/desktop-py &amp;&amp; python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert d['project']['optional-dependencies']['speaker'] == ['webrtcvad-wheels==2.0.14','resemblyzer==0.1.4'], d['project']['optional-dependencies']['speaker']; assert 'webrtcvad-wheels==2.0.14' in d['tool']['uv']['override-dependencies']; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'webrtcvad-wheels==2.0.14' apps/desktop-py/pyproject.toml` (presente 2 vezes: optional-deps + override)
    - `grep -q 'resemblyzer==0.1.4' apps/desktop-py/pyproject.toml`
    - Em `pyproject.toml`, dentro do array `speaker = [...]`, `webrtcvad-wheels==2.0.14` aparece ANTES de `resemblyzer==0.1.4` (verificar com `python -c "import tomllib; print(tomllib.load(open('apps/desktop-py/pyproject.toml','rb'))['project']['optional-dependencies']['speaker'])" | grep -E "^\['webrtcvad-wheels"`)
    - `grep -q "def mock_voice_encoder" apps/desktop-py/tests/conftest.py`
    - `grep -q 'fake_resemblyzer.VoiceEncoder = VoiceEncoder_class' apps/desktop-py/tests/conftest.py`
    - `grep -q 'fake_resemblyzer.preprocess_wav' apps/desktop-py/tests/conftest.py`
    - `cd apps/desktop-py && python -c "import sys; sys.path.insert(0, 'tests'); import conftest; print('ok')"` (módulo importa sem erros de sintaxe)
  </acceptance_criteria>
  <done>
    pyproject.toml tem grupo `[speaker]` com webrtcvad-wheels antes de resemblyzer; uv override-dependencies inclui webrtcvad-wheels; conftest.py expõe fixture `mock_voice_encoder` com VoiceEncoder mock + preprocess_wav passthrough + reset de singleton.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED tests for speaker.py (SPK-01..06, SPK-10)</name>
  <files>apps/desktop-py/tests/test_speaker.py</files>
  <read_first>
    - apps/desktop-py/tests/test_voice_cloning.py (ver padrão de teste com mock_kokoclone_encoder + tmp_home + tmp_path)
    - apps/desktop-py/tests/conftest.py (após Task 1: usar nova fixture mock_voice_encoder + tmp_home existente)
    - .planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-VALIDATION.md (Per-Task Verification Map — nomes exatos dos testes: test_identify_speaker_returns_dict, test_identify_speaker_above_threshold, test_identify_speaker_below_threshold, test_profile_store_crud, test_embed_utterance_from_numpy, test_voice_encoder_singleton, test_enroll_saves_npy)
  </read_first>
  <behavior>
    Sete (7) testes pytest no novo arquivo `apps/desktop-py/tests/test_speaker.py`:

    - **test_identify_speaker_returns_dict** (SPK-01): com 0 perfis em ~/.jarvis/speakers/, `identify_speaker(audio, config)` retorna `{"name": "unknown", "confidence": 0.0, "is_known": False, "candidate_name": "unknown"}` — assert via subset `{"name","confidence","is_known","candidate_name"} <= set(result.keys())`.
    - **test_identify_speaker_above_threshold** (SPK-02): salva perfil "Biel" com embedding fixo X; mocka `embed_utterance` para retornar **o mesmo** X; chama `identify_speaker(audio, config_with_threshold_0.75)` → assert `name=="Biel"`, `is_known is True`, `confidence >= 0.99`.
    - **test_identify_speaker_below_threshold** (SPK-03): salva perfil "Biel" com embedding X; mocka `embed_utterance` para retornar vetor ortogonal a X (`y` tal que `np.dot(X, y) ≈ 0`); → assert `name=="unknown"`, `is_known is False`, `confidence < 0.75`.
    - **test_profile_store_crud** (SPK-04): exercita `save_profile("alice", emb)` → arquivo `~/.jarvis/speakers/alice.npy` existe; `load_profile("alice")` retorna o mesmo array (np.allclose); `list_profiles()` retorna `["alice"]`; `delete_profile("alice")` retorna True e arquivo some; `delete_profile("alice")` retorna False na 2ª chamada.
    - **test_embed_utterance_from_numpy** (SPK-05): chama `identify_speaker(np.zeros(16000*3, dtype=np.float32), config)` sem perfis registrados — NÃO levanta exceção; verifica que `mock_voice_encoder.embed_utterance.call_count == 1`.
    - **test_voice_encoder_singleton** (SPK-06): chama `identify_speaker(audio, config)` 5 vezes; assert `mock_voice_encoder._encoder_class.call_count == 1` (VoiceEncoder instanciado UMA vez).
    - **test_enroll_saves_npy** (SPK-10): mocka `stt.record_until_silence` para retornar `np.zeros(16000*4, dtype=np.float32)`; chama `enroll_speaker("biel", config, n_utterances=5)`; assert `(home / ".jarvis" / "speakers" / "biel.npy").exists()`; `np.load(...).shape == (256,)`; `np.load(...).dtype == np.float32`; `mock_voice_encoder.embed_speaker.call_count == 1` (chamado UMA vez com 5 wavs).

    Todos os testes usam fixtures `tmp_home`, `mock_voice_encoder`. Testes que envolvem enroll também monkeypatcham `jarvis_desktop.stt.record_until_silence`.

    Estes testes DEVEM FALHAR (RED) antes da Task 3 (porque `speaker.py` ainda não existe). Verify command roda os testes e espera falha por ImportError ou módulo ausente.
  </behavior>
  <action>
    Criar `apps/desktop-py/tests/test_speaker.py`. Estrutura completa:

    ```python
    """Tests for jarvis_desktop.speaker (Phase 89, SPK-01..SPK-06, SPK-10)."""
    from __future__ import annotations

    import numpy as np
    import pytest


    # -------------------------------------------------------------------------
    # SPK-01: identify_speaker retorna dict
    # -------------------------------------------------------------------------

    def test_identify_speaker_returns_dict(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        audio = np.zeros(16000 * 3, dtype=np.float32)

        result = speaker.identify_speaker(audio, config)

        assert isinstance(result, dict)
        # candidate_name foi adicionado para hybrid injection (Plan 03) — aceita subset
        assert {"name", "confidence", "is_known", "candidate_name"} <= set(result.keys())
        # Sem perfis cadastrados → unknown
        assert result["name"] == "unknown"
        assert result["confidence"] == 0.0
        assert result["is_known"] is False


    # -------------------------------------------------------------------------
    # SPK-02: speaker acima do threshold
    # -------------------------------------------------------------------------

    def test_identify_speaker_above_threshold(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        # Embedding determinístico do perfil "Biel"
        biel_emb = np.random.RandomState(123).randn(256).astype(np.float32)
        biel_emb = biel_emb / np.linalg.norm(biel_emb)
        speaker.save_profile("Biel", biel_emb)

        # Mock retorna o MESMO embedding para a fala do turno
        mock_voice_encoder.embed_utterance.return_value = biel_emb

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        result = speaker.identify_speaker(np.zeros(16000 * 3, dtype=np.float32), config)

        assert result["name"] == "Biel"
        assert result["is_known"] is True
        assert result["confidence"] >= 0.99


    # -------------------------------------------------------------------------
    # SPK-03: speaker abaixo do threshold
    # -------------------------------------------------------------------------

    def test_identify_speaker_below_threshold(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        # Perfil: vetor [1, 0, 0, ...]
        biel_emb = np.zeros(256, dtype=np.float32)
        biel_emb[0] = 1.0
        speaker.save_profile("Biel", biel_emb)

        # Turno: vetor ortogonal [0, 1, 0, ...] → cosine ≈ 0
        turn_emb = np.zeros(256, dtype=np.float32)
        turn_emb[1] = 1.0
        mock_voice_encoder.embed_utterance.return_value = turn_emb

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        result = speaker.identify_speaker(np.zeros(16000 * 3, dtype=np.float32), config)

        assert result["name"] == "unknown"
        assert result["is_known"] is False
        assert result["confidence"] < 0.75


    # -------------------------------------------------------------------------
    # SPK-04: ProfileStore CRUD
    # -------------------------------------------------------------------------

    def test_profile_store_crud(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker

        emb = np.random.RandomState(7).randn(256).astype(np.float32)
        speaker.save_profile("alice", emb)

        profile_path = tmp_home / ".jarvis" / "speakers" / "alice.npy"
        assert profile_path.exists()

        loaded = speaker.load_profile("alice")
        assert np.allclose(loaded, emb)

        names = speaker.list_profiles()
        assert names == ["alice"]

        assert speaker.delete_profile("alice") is True
        assert not profile_path.exists()
        assert speaker.delete_profile("alice") is False


    # -------------------------------------------------------------------------
    # SPK-05: preprocess_wav + embed_utterance aceita NumPy 16kHz
    # -------------------------------------------------------------------------

    def test_embed_utterance_from_numpy(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        audio = np.zeros(16000 * 3, dtype=np.float32)

        # Não levanta exceção — passa NumPy direto sem conversão
        result = speaker.identify_speaker(audio, config)

        assert result is not None
        assert mock_voice_encoder.embed_utterance.call_count == 1


    # -------------------------------------------------------------------------
    # SPK-06: VoiceEncoder singleton
    # -------------------------------------------------------------------------

    def test_voice_encoder_singleton(tmp_home, mock_voice_encoder):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        audio = np.zeros(16000 * 3, dtype=np.float32)

        for _ in range(5):
            speaker.identify_speaker(audio, config)

        # VoiceEncoder instanciado UMA vez (singleton, threading.Lock)
        assert mock_voice_encoder._encoder_class.call_count == 1


    # -------------------------------------------------------------------------
    # SPK-10: Enrollment salva .npy com shape (256,)
    # -------------------------------------------------------------------------

    def test_enroll_saves_npy(tmp_home, mock_voice_encoder, monkeypatch):
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        # Mock record_until_silence para retornar 4s de áudio (passa validação de duração ≥2s)
        fake_audio = np.zeros(16000 * 4, dtype=np.float32)
        monkeypatch.setattr("jarvis_desktop.stt.record_until_silence", lambda **kw: fake_audio)

        config = JarvisConfig(speaker_recognition_enabled=True, speaker_threshold=0.75)
        speaker.enroll_speaker("biel", config, n_utterances=5)

        profile_path = tmp_home / ".jarvis" / "speakers" / "biel.npy"
        assert profile_path.exists()

        loaded = np.load(str(profile_path))
        assert loaded.shape == (256,)
        assert loaded.dtype == np.float32

        # D-14: embed_speaker chamado UMA vez com 5 wavs (média interna)
        assert mock_voice_encoder.embed_speaker.call_count == 1
        called_wavs = mock_voice_encoder.embed_speaker.call_args[0][0]
        assert len(called_wavs) == 5
    ```

    Validações pós-criação:
    - Arquivo tem `import pytest` no topo
    - Os 7 nomes de testes batem EXATAMENTE com os listados na 89-VALIDATION.md
  </action>
  <verify>
    <automated>cd apps/desktop-py &amp;&amp; pytest tests/test_speaker.py --collect-only -q 2>&amp;1 | grep -E "test_identify_speaker_returns_dict|test_identify_speaker_above_threshold|test_identify_speaker_below_threshold|test_profile_store_crud|test_embed_utterance_from_numpy|test_voice_encoder_singleton|test_enroll_saves_npy" | wc -l | grep -q "^7$" &amp;&amp; echo "OK: 7 tests collected"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop-py/tests/test_speaker.py` existe e tem ≥120 linhas
    - `grep -c "^def test_" apps/desktop-py/tests/test_speaker.py` retorna `7`
    - Cada um destes nomes está presente (grep): `test_identify_speaker_returns_dict`, `test_identify_speaker_above_threshold`, `test_identify_speaker_below_threshold`, `test_profile_store_crud`, `test_embed_utterance_from_numpy`, `test_voice_encoder_singleton`, `test_enroll_saves_npy`
    - `cd apps/desktop-py && pytest tests/test_speaker.py --collect-only -q` retorna exatamente 7 itens coletados
    - `cd apps/desktop-py && pytest tests/test_speaker.py -x 2>&1 | tail -5` mostra falha (ModuleNotFoundError jarvis_desktop.speaker OU AttributeError — esperado RED antes da Task 3)
  </acceptance_criteria>
  <done>
    7 testes coletados pelo pytest, todos falhando porque `jarvis_desktop.speaker` ainda não existe (RED phase). Nomes batem exatamente com a 89-VALIDATION.md.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement speaker.py (GREEN — fazer os 7 testes passarem)</name>
  <files>apps/desktop-py/src/jarvis_desktop/speaker.py, apps/desktop-py/src/jarvis_desktop/config.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/stt.py (linhas 22-50: padrão singleton com `_model`, `_lock = threading.Lock()`, `_SAMPLE_RATE = 16000`, lazy import)
    - apps/desktop-py/src/jarvis_desktop/config.py (TODO o arquivo — adicionar 2 campos novos seguindo padrão `Field(default=..., description="...")`)
    - apps/desktop-py/tests/test_speaker.py (do Task 2 — entender exatamente quais asserts precisam passar)
    - .planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-RESEARCH.md (seção Architecture Patterns → Pattern 1-4: VoiceEncoder singleton, preprocess_wav, cosine similarity, ProfileStore)
  </read_first>
  <behavior>
    Após esta task, `pytest tests/test_speaker.py -x` passa com 7/7 verde.

    **config.py** ganha 2 campos novos (sem mexer nos existentes):
    ```python
    speaker_recognition_enabled: bool = Field(
        default=False,
        description="Phase 89 — Habilita identificação de voz após cada captura (insere [SPK] log e injeta speaker no contexto LLM)."
    )
    speaker_threshold: float = Field(
        default=0.75,
        description="Phase 89 — Cosine similarity threshold para classificar speaker como conhecido (D-05; range 0.0–1.0)."
    )
    ```

    **speaker.py** expõe:
    - `_encoder: Optional["VoiceEncoder"]` + `_encoder_lock: threading.Lock` (module-level)
    - `_SAMPLE_RATE = 16000`, `_EMBEDDING_DIM = 256`, `_MIN_UTTERANCE_SAMPLES = 16000 * 2` (2s mínimo)
    - `_SPEAKERS_DIR = Path.home() / ".jarvis" / "speakers"` (computado dinâmico via função `_speakers_dir()` para honrar `tmp_home` que muda `HOME`)
    - `_safe_profile_name(name: str) -> str` — sanitiza: strip, replace espaço por `_`, valida regex `^[A-Za-z0-9_\-]{1,64}$`, levanta `ValueError` em path traversal (`..`, `/`, `\`, começa com `.`)
    - `_get_encoder() -> "VoiceEncoder"` — singleton com lock, lazy import `from resemblyzer import VoiceEncoder`, ImportError vira `RuntimeError("[SPK] resemblyzer não instalado — instale com pip install jarvis-desktop[speaker]")`
    - `_cosine_similarity(a, b) -> float` — guard para vetor zero (1e-8)
    - `save_profile(name, embedding) -> None` — sanitiza nome, cria diretório, `np.save(str(path), embedding.astype(np.float32))`
    - `load_profile(name) -> np.ndarray` — `np.load(str(path))`
    - `list_profiles() -> list[str]` — `glob("*.npy")` retorna `[p.stem for p in ...]` ordenado
    - `delete_profile(name) -> bool` — sanitiza, unlink se existe, retorna bool
    - `load_all_profiles() -> dict[str, np.ndarray]` — usado por identify
    - `identify_speaker(audio: np.ndarray, config) -> dict[str, Any]` — preprocessa, embeda, compara com todos os perfis via cosine, aplica threshold de `getattr(config, "speaker_threshold", 0.75)`
    - `enroll_speaker(name: str, config, n_utterances: int = 5) -> None` — sanitiza nome, grava N utterances via `stt.record_until_silence(threshold_ms=config.silence_threshold_ms)`, valida cada uma `len(audio) >= _MIN_UTTERANCE_SAMPLES` (descarta e repete até max 3 tentativas/slot), aplica `preprocess_wav` em cada, chama `encoder.embed_speaker(processed_list)`, `save_profile(name, mean_embedding)`. Logs `[SPK]` via `ui.get_console()`.
  </behavior>
  <action>
    **Parte A — apps/desktop-py/src/jarvis_desktop/config.py:**
    Após o último campo (`chatterbox_cfg_weight`, linha 104-111), adicionar antes do fechamento da classe:
    ```python
        # Phase 89: Speaker recognition (D-01, D-05, SPK-01..10)
        speaker_recognition_enabled: bool = Field(
            default=False,
            description=(
                "Phase 89 — Habilita identificação de voz após cada captura. "
                "Quando True, voice_modes chama speaker.identify_speaker() entre "
                "record_until_silence() e transcribe(); resultado é enviado ao chat_loop "
                "que injeta nome no contexto do LLM (D-08). False = pipeline inalterado."
            ),
        )
        speaker_threshold: float = Field(
            default=0.75,
            description=(
                "Phase 89 — Cosine similarity threshold para classificar speaker como conhecido "
                "(D-05; range 0.0–1.0). resemblyzer GE2E produz embeddings L2-normed; 0.75 reduz "
                "falsos positivos para uso pessoal com 1 speaker primário. Confiança < threshold "
                "→ speaker tratado como unknown."
            ),
        )
    ```

    **Parte B — Criar novo arquivo apps/desktop-py/src/jarvis_desktop/speaker.py:**
    ```python
    """JARVIS Speaker Recognition module (Phase 89).

    Public API:
      identify_speaker(audio, config) -> dict  — Identifica speaker a partir de NumPy 16kHz
      enroll_speaker(name, config, n=5) -> None — Grava N utterances e salva embedding médio
      save_profile / load_profile / list_profiles / delete_profile — ProfileStore CRUD
      _get_encoder() -> VoiceEncoder            — Singleton accessor (threading.Lock)
      _cosine_similarity(a, b) -> float         — Cosine similarity entre embeddings L2-normed

    Decisions honored:
      D-01: Multi-user — ProfileStore gerencia N perfis em ~/.jarvis/speakers/
      D-02: Cada perfil = arquivo .npy (shape (256,) float32 L2-normed)
      D-03: resemblyzer GE2E d-vector
      D-04: Cosine similarity para comparação
      D-05: Threshold default 0.75 (lido de config.speaker_threshold)
      D-13: 5 utterances de ~4s para enrollment
      D-14: encoder.embed_speaker() retorna média dos embeddings
      Pitfall 1 (89-RESEARCH): webrtcvad-wheels antes de resemblyzer
      Pitfall 3 (89-RESEARCH): Singleton com threading.Lock
      T-89-01-01: _safe_profile_name sanitiza contra path traversal
    """
    from __future__ import annotations

    import re
    import threading
    from pathlib import Path
    from typing import TYPE_CHECKING, Any, Optional

    import numpy as np

    if TYPE_CHECKING:
        from resemblyzer import VoiceEncoder
        from jarvis_desktop.config import JarvisConfig

    # -------------------------------------------------------------------------
    # Module-level singleton state (padrão idêntico a stt.py:40-43)
    # -------------------------------------------------------------------------

    _encoder: Optional["VoiceEncoder"] = None
    _encoder_lock = threading.Lock()

    _SAMPLE_RATE: int = 16000
    _EMBEDDING_DIM: int = 256
    _MIN_UTTERANCE_SAMPLES: int = 16000 * 2  # 2s minimum (Pitfall 5: <2s = embedding ruim)
    _DEFAULT_N_UTTERANCES: int = 5            # D-13
    _MAX_RETRIES_PER_SLOT: int = 3            # T-89-01-02 (anti-DoS)
    _NAME_RE = re.compile(r"^[A-Za-z0-9_\-]{1,64}$")


    # -------------------------------------------------------------------------
    # Helpers
    # -------------------------------------------------------------------------

    def _console():
        """Lazy console — evita import circular com ui module."""
        from jarvis_desktop import ui
        return ui.get_console()


    def _speakers_dir() -> Path:
        """Diretório de perfis. Computado dinâmico para honrar tmp_home em testes."""
        return Path.home() / ".jarvis" / "speakers"


    def _safe_profile_name(name: str) -> str:
        """Sanitiza nome de perfil — mitigation T-89-01-01 (path traversal).

        Regras:
          - Strip whitespace
          - Replace espaços por '_'
          - Rejeitar se contém '/', '\\', '..', ou começa com '.'
          - Regex: ^[A-Za-z0-9_\\-]{1,64}$ (apenas ASCII safe)

        Raises:
            ValueError se nome inválido.
        """
        if not isinstance(name, str):
            raise ValueError(f"Nome do perfil deve ser str, recebeu {type(name).__name__}")
        cleaned = name.strip().replace(" ", "_")
        if not cleaned:
            raise ValueError("Nome do perfil não pode ser vazio")
        if ".." in cleaned or "/" in cleaned or "\\" in cleaned or cleaned.startswith("."):
            raise ValueError(f"Nome de perfil inválido (path traversal): {name!r}")
        if not _NAME_RE.match(cleaned):
            raise ValueError(
                f"Nome de perfil inválido: {name!r}. "
                f"Use apenas A-Z, a-z, 0-9, _ ou - (até 64 caracteres)."
            )
        return cleaned


    def _get_encoder() -> "VoiceEncoder":
        """Singleton VoiceEncoder com threading.Lock (Pattern 1 / Pitfall 3 do RESEARCH)."""
        global _encoder
        with _encoder_lock:
            if _encoder is None:
                try:
                    from resemblyzer import VoiceEncoder
                except ImportError as exc:
                    raise RuntimeError(
                        "[SPK] resemblyzer não instalado — "
                        "instale com `pip install jarvis-desktop[speaker]` "
                        "(ou `uv sync --extra speaker`)."
                    ) from exc
                _console().print("[SPK] Carregando modelo de voz (resemblyzer GE2E ~30MB)...")
                _encoder = VoiceEncoder()
                _console().print("[SPK] Pronto.")
        return _encoder


    def _cosine_similarity(a: np.ndarray, b: np.ndarray) -> float:
        """Cosine similarity com guard para vetor zero (Pattern 3 do RESEARCH)."""
        norm_a = float(np.linalg.norm(a))
        norm_b = float(np.linalg.norm(b))
        if norm_a < 1e-8 or norm_b < 1e-8:
            return 0.0
        return float(np.dot(a, b) / (norm_a * norm_b))


    # -------------------------------------------------------------------------
    # ProfileStore CRUD (D-01, D-02; SPK-04)
    # -------------------------------------------------------------------------

    def save_profile(name: str, embedding: np.ndarray) -> None:
        """Salva embedding como .npy em ~/.jarvis/speakers/{name}.npy (T-89-01-01 sanitiza name)."""
        safe = _safe_profile_name(name)
        dir_ = _speakers_dir()
        dir_.mkdir(parents=True, exist_ok=True)
        path = dir_ / f"{safe}.npy"
        np.save(str(path), embedding.astype(np.float32))


    def load_profile(name: str) -> np.ndarray:
        """Carrega embedding de ~/.jarvis/speakers/{name}.npy."""
        safe = _safe_profile_name(name)
        path = _speakers_dir() / f"{safe}.npy"
        return np.load(str(path))


    def list_profiles() -> list[str]:
        """Lista nomes dos perfis (sem extensão), ordenados alfabeticamente."""
        dir_ = _speakers_dir()
        if not dir_.exists():
            return []
        return sorted(p.stem for p in dir_.glob("*.npy"))


    def delete_profile(name: str) -> bool:
        """Remove perfil. Retorna True se removeu, False se não existia."""
        safe = _safe_profile_name(name)
        path = _speakers_dir() / f"{safe}.npy"
        if path.exists():
            path.unlink()
            return True
        return False


    def load_all_profiles() -> dict[str, np.ndarray]:
        """Carrega todos os perfis em dict {name: embedding}."""
        return {name: load_profile(name) for name in list_profiles()}


    # -------------------------------------------------------------------------
    # Identification (D-04, D-05, D-08; SPK-01, SPK-02, SPK-03, SPK-05, SPK-06)
    # -------------------------------------------------------------------------

    def identify_speaker(audio: np.ndarray, config: "JarvisConfig") -> dict[str, Any]:
        """Identifica speaker a partir de áudio NumPy 16kHz (D-06).

        Pipeline:
          1. preprocess_wav(audio, source_sr=16000) — normaliza volume, remove silêncio
          2. encoder.embed_utterance(processed) → embedding (256,) L2-normed
          3. cosine_similarity contra cada perfil salvo
          4. Aplica threshold (config.speaker_threshold, default 0.75)

        Returns:
            dict com keys exatas:
              name (str)            — candidate_name se is_known else "unknown"
              confidence (float)    — melhor cosine score
              is_known (bool)       — best_score >= threshold
              candidate_name (str)  — melhor match independente do threshold (usado
                                       pelo hybrid injection no Plan 03 para distinguir
                                       "[Biel?]:" (baixa confiança) de "[unknown]:" (zero match))
            Sem perfis → {"name":"unknown","confidence":0.0,"is_known":False,"candidate_name":"unknown"}
        """
        from resemblyzer import preprocess_wav

        encoder = _get_encoder()
        processed = preprocess_wav(audio, source_sr=_SAMPLE_RATE)
        turn_emb = encoder.embed_utterance(processed)

        profiles = load_all_profiles()
        if not profiles:
            return {
                "name": "unknown",
                "confidence": 0.0,
                "is_known": False,
                "candidate_name": "unknown",
            }

        best_name = "unknown"
        best_score = 0.0
        for name, profile_emb in profiles.items():
            score = _cosine_similarity(turn_emb, profile_emb)
            if score > best_score:
                best_score = score
                best_name = name

        threshold = float(getattr(config, "speaker_threshold", 0.75))
        is_known = best_score >= threshold

        return {
            "name": best_name if is_known else "unknown",
            "confidence": best_score,
            "is_known": is_known,
            "candidate_name": best_name,
        }


    # -------------------------------------------------------------------------
    # Enrollment (D-12, D-13, D-14; SPK-10)
    # -------------------------------------------------------------------------

    def enroll_speaker(
        name: str,
        config: "JarvisConfig",
        n_utterances: int = _DEFAULT_N_UTTERANCES,
    ) -> None:
        """Grava N utterances via microfone, calcula embedding médio, salva perfil.

        D-13: N=5 default. D-14: usa encoder.embed_speaker() (média interna).
        T-89-01-02: máx 3 retries por slot evita loop infinito em mic ruim.
        """
        from resemblyzer import preprocess_wav
        from jarvis_desktop import stt

        safe = _safe_profile_name(name)  # falha early se nome inválido (T-89-01-01)
        encoder = _get_encoder()

        console = _console()
        console.print(f"[SPK] Iniciando enrollment de '{safe}' — {n_utterances} amostras.")
        console.print("[SPK] Fale naturalmente por ~4s após cada prompt 'Gravando...'.")

        processed_wavs: list[np.ndarray] = []
        slot = 1
        while slot <= n_utterances:
            retries = 0
            captured = None
            while retries < _MAX_RETRIES_PER_SLOT:
                console.print(f"[SPK] Gravando {slot}/{n_utterances}... (fale agora)")
                audio = stt.record_until_silence(
                    threshold_ms=getattr(config, "silence_threshold_ms", 500),
                )
                if len(audio) >= _MIN_UTTERANCE_SAMPLES:
                    captured = audio
                    break
                console.print(
                    f"[SPK] Áudio muito curto ({len(audio) / _SAMPLE_RATE:.1f}s < 2s) — repita."
                )
                retries += 1
            if captured is None:
                console.print(
                    f"[SPK] Falha ao gravar amostra {slot} após {_MAX_RETRIES_PER_SLOT} tentativas. Abortando."
                )
                return

            processed_wavs.append(preprocess_wav(captured, source_sr=_SAMPLE_RATE))
            slot += 1

        # D-14: embed_speaker recebe lista de wavs processados, retorna média
        mean_embedding = encoder.embed_speaker(processed_wavs)
        save_profile(safe, mean_embedding)
        console.print(f"[SPK] Perfil '{safe}' salvo em ~/.jarvis/speakers/{safe}.npy")
    ```

    **Parte C — Reset do singleton entre testes:**
    A fixture `mock_voice_encoder` em conftest.py (Task 1) já reseta `speaker._encoder = None`, então não há trabalho extra aqui.
  </action>
  <verify>
    <automated>cd apps/desktop-py &amp;&amp; pytest tests/test_speaker.py -x -v 2>&amp;1 | tail -20 &amp;&amp; pytest tests/test_speaker.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop-py/src/jarvis_desktop/speaker.py` existe e tem ≥150 linhas (`wc -l` ≥ 150)
    - `grep -q "def identify_speaker" apps/desktop-py/src/jarvis_desktop/speaker.py`
    - `grep -q "def enroll_speaker" apps/desktop-py/src/jarvis_desktop/speaker.py`
    - `grep -q "def _safe_profile_name" apps/desktop-py/src/jarvis_desktop/speaker.py`
    - `grep -q "_encoder_lock = threading.Lock" apps/desktop-py/src/jarvis_desktop/speaker.py`
    - `grep -q "from resemblyzer import" apps/desktop-py/src/jarvis_desktop/speaker.py`
    - `grep -q "speaker_recognition_enabled" apps/desktop-py/src/jarvis_desktop/config.py`
    - `grep -q "speaker_threshold" apps/desktop-py/src/jarvis_desktop/config.py`
    - `cd apps/desktop-py && pytest tests/test_speaker.py -x` retorna exit code 0 (7 testes passam)
    - `cd apps/desktop-py && pytest tests/test_speaker.py -x 2>&1 | grep -E "^7 passed|passed in"`
    - `cd apps/desktop-py && pytest tests/ -x` retorna exit code 0 (todos os outros testes ainda passam — nada quebrado)
  </acceptance_criteria>
  <done>
    speaker.py expõe identify_speaker/enroll_speaker/ProfileStore CRUD. config.py tem speaker_recognition_enabled e speaker_threshold. 7/7 testes test_speaker.py verdes. Suite completa (`pytest tests/`) verde.
  </done>
</task>

</tasks>

<verification>
- `cd apps/desktop-py && pytest tests/test_speaker.py -x` → 7/7 passed
- `cd apps/desktop-py && pytest tests/ -x` → suite completa verde (sem regressão)
- `python -c "from jarvis_desktop import speaker; print(speaker.list_profiles())"` em ambiente com `[speaker]` instalado → não levanta exceção
- `python -c "import tomllib; assert 'webrtcvad-wheels==2.0.14' in tomllib.load(open('apps/desktop-py/pyproject.toml','rb'))['project']['optional-dependencies']['speaker'][0]"`
</verification>

<success_criteria>
- ✅ speaker.py expõe identify_speaker, enroll_speaker, save_profile, load_profile, list_profiles, delete_profile, _get_encoder, _cosine_similarity
- ✅ VoiceEncoder é singleton (test_voice_encoder_singleton verde — 1 instanciação para 5 chamadas)
- ✅ identify_speaker retorna dict {name, confidence, is_known} (test_identify_speaker_returns_dict verde)
- ✅ Threshold ≥0.75 → is_known=True; <0.75 → name="unknown" (test_identify_speaker_above/below_threshold verdes)
- ✅ ProfileStore CRUD funciona com .npy (test_profile_store_crud verde)
- ✅ Enrollment salva .npy shape (256,) float32 via embed_speaker() (test_enroll_saves_npy verde)
- ✅ _safe_profile_name rejeita path traversal (mitigation T-89-01-01)
- ✅ config.py tem speaker_recognition_enabled (False) e speaker_threshold (0.75)
- ✅ pyproject.toml tem [speaker] com webrtcvad-wheels ANTES de resemblyzer; override-dependencies inclui webrtcvad-wheels
</success_criteria>

<output>
Após completar, criar `.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-01-SUMMARY.md`
</output>
