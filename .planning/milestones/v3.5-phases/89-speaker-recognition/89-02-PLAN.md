---
phase: 89
plan: 02
type: execute
wave: 2
depends_on: [89-01]
files_modified:
  - apps/desktop-py/src/jarvis_desktop/chat.py
  - apps/desktop-py/tests/test_config_menu.py
autonomous: true
requirements: [SPK-09, SPK-10]
requirements_addressed: [SPK-09, SPK-10]
tags: [config-ux, enrollment, menu]

must_haves:
  truths:
    - "Menu /config exibe item '9. Reconhecimento voz' (toggle on/off do speaker_recognition_enabled)"
    - "Menu /config exibe item '10. Perfis de voz' que abre submenu de enrollment/listagem/remoção (D-12, D-15)"
    - "Submenu de perfis tem 3 ações: 'Adicionar perfil' (enrollment), 'Listar perfis', 'Remover perfil'"
    - "Adicionar perfil pede nome via ui.get_input(), sanitiza via speaker._safe_profile_name, chama speaker.enroll_speaker(name, config, n=5) — D-12, D-13"
    - "Listar perfis usa speaker.list_profiles() e imprime cada nome (D-15)"
    - "Remover perfil pede índice do perfil listado, confirma, chama speaker.delete_profile(name) (D-15)"
    - "Nome inválido (path traversal, vazio, caracteres especiais) é rejeitado com mensagem em pt-BR sem crash (T-89-02)"
    - "Testes test_config_menu.py cobrem SPK-09 (item existe + abre submenu)"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "Itens 9 e 10 no menu /config; sub-funções _menu_speaker_recognition + _menu_speaker_profiles + _enroll_speaker_via_menu + _list_speaker_profiles + _delete_speaker_profile_via_menu"
      contains: "_menu_speaker_profiles"
    - path: "apps/desktop-py/tests/test_config_menu.py"
      provides: "Teste test_config_menu_speaker_option (SPK-09) + teste de sanitização de nome"
      contains: "test_config_menu_speaker_option"
  key_links:
    - from: "apps/desktop-py/src/jarvis_desktop/chat.py:_show_config_menu"
      to: "_menu_speaker_recognition + _menu_speaker_profiles"
      via: "elif choice == '9' / elif choice == '10'"
      pattern: "_menu_speaker_(recognition|profiles)"
    - from: "_enroll_speaker_via_menu"
      to: "speaker.enroll_speaker"
      via: "import speaker as spk; spk.enroll_speaker(name, config, n_utterances=5)"
      pattern: "spk\\.enroll_speaker"
    - from: "_delete_speaker_profile_via_menu / _list_speaker_profiles"
      to: "speaker.list_profiles + speaker.delete_profile"
      via: "import speaker as spk"
      pattern: "spk\\.(list_profiles|delete_profile)"
---

<objective>
Adicionar enrollment UX completa ao menu `/config` em `chat.py`:
- Novo item `9. Reconhecimento voz [sim/nao]` → toggle de `config.speaker_recognition_enabled`
- Novo item `10. Perfis de voz [N cadastrados]` → submenu com 3 ações:
  1. Adicionar perfil (pede nome, valida via `_safe_profile_name`, chama `speaker.enroll_speaker(name, config, n_utterances=5)`)
  2. Listar perfis (imprime nomes de `speaker.list_profiles()`)
  3. Remover perfil (lista, pede índice, confirma "(s/N)", chama `speaker.delete_profile(name)`)

Todas as mensagens de UI em pt-BR. Erros de sanitização (path traversal, nome vazio) capturados como `ValueError` de `speaker._safe_profile_name` e mostrados ao usuário sem crash.

**Honra:** D-12 (menu enroll), D-13 (n=5 utterances default), D-15 (operações de gerenciamento: listar, remover).

Purpose: Sem UX no menu, usuário não consegue cadastrar perfil — feature fica letra morta. Manter consistência com o padrão `_menu_chatterbox_audio_ref` da Phase 88.

Output: ~120 linhas novas em chat.py (5 funções privadas), ~80 linhas novas em test_config_menu.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-CONTEXT.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-RESEARCH.md
@.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-01-PLAN.md
@apps/desktop-py/src/jarvis_desktop/chat.py
@apps/desktop-py/src/jarvis_desktop/speaker.py
@apps/desktop-py/tests/test_config_menu.py
@apps/desktop-py/tests/conftest.py

<interfaces>
<!-- Contratos extraídos do codebase -->

Função existente em chat.py (linha 665):
```python
def _show_config_menu(config: JarvisConfig) -> None:
    """Interactive terminal config menu."""
    # ... while True com prints de itens 1-8, leitura de ui.get_input("> "), branches por choice
```

Padrão de toggle (linha 701-704 do chat.py):
```python
elif choice == "4":
    config.agentic_confirm = not config.agentic_confirm
    status = "sim" if config.agentic_confirm else "nao"
    console.print(f"[Confirmar planos: {status}]", highlight=False)
```

Padrão de submenu inline (linha 888-909 — _menu_chatterbox_audio_ref):
```python
def _menu_chatterbox_audio_ref(config: JarvisConfig) -> None:
    from jarvis_desktop import ui
    from jarvis_desktop.config import save_config
    console = ui.get_console()
    current = config.chatterbox_audio_prompt_path or "(não definido)"
    # ...console.print + ui.get_input + save_config
```

API existente em speaker.py (do Plan 01):
```python
def enroll_speaker(name: str, config: "JarvisConfig", n_utterances: int = 5) -> None: ...
def list_profiles() -> list[str]: ...
def delete_profile(name: str) -> bool: ...
def _safe_profile_name(name: str) -> str: ...  # levanta ValueError
```

UI helpers (em apps/desktop-py/src/jarvis_desktop/ui.py — não modificar):
```python
def get_console(): ...
def get_input(prompt: str) -> str: ...
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-typed speaker name → filesystem path | Input via `ui.get_input()` no menu vira filename — risco de path traversal |
| user-typed index for delete → list_profiles lookup | Índice fora do range ou negativo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-02 | Tampering | `_enroll_speaker_via_menu` (nome digitado) | mitigate | Capturar `ValueError` de `speaker._safe_profile_name(name)` e mostrar erro em pt-BR — `console.print(f"[Nome inválido: {exc}]")`. Não chamar `enroll_speaker` se sanitização falhar. |
| T-89-02-02 | Tampering | `_delete_speaker_profile_via_menu` (índice) | mitigate | `int(raw)` em try/except ValueError; verificar `0 <= idx < len(profiles)`; se inválido, print "Índice inválido" e retorna sem efeito. Confirmação `(s/N)` com default "n" antes de chamar delete_profile. |
| T-89-02-03 | DoS | Enrollment infinito | mitigate | `speaker.enroll_speaker` já tem `_MAX_RETRIES_PER_SLOT=3` (Plan 01 / T-89-01-02). Aborta se 3 tentativas seguidas falham. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for menu items 9 (toggle) e 10 (perfis)</name>
  <files>apps/desktop-py/tests/test_config_menu.py</files>
  <read_first>
    - apps/desktop-py/tests/test_config_menu.py (TODO o arquivo — entender padrões de mock do ui.get_input e do _show_config_menu existentes)
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 665-720 (_show_config_menu — para ver como itens são impressos e qual número está livre — atualmente 1-8)
    - apps/desktop-py/tests/conftest.py (fixtures tmp_home, mock_voice_encoder do Plan 01)
    - .planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-VALIDATION.md (nome exato do teste: test_config_menu_speaker_option)
  </read_first>
  <behavior>
    Quatro (4) novos testes em `test_config_menu.py`:

    - **test_config_menu_speaker_option** (SPK-09): mocka `ui.get_input` para retornar `"0"` (sair imediato); chama `_show_config_menu(config)`; captura stdout via capsys; assert "9. Reconhecimento voz" presente E "10. Perfis de voz" presente na saída.
    - **test_config_menu_speaker_toggle_enables** (SPK-09): config com `speaker_recognition_enabled=False`; mock input retorna sequência `["9", "0"]`; chama `_show_config_menu`; assert `config.speaker_recognition_enabled is True` após.
    - **test_enroll_speaker_via_menu_rejects_invalid_name** (T-89-02): mock input retorna `["10", "1", "../etc/passwd", "0", "0"]`; chama `_show_config_menu`; capsys captura saída; assert "Nome inválido" presente; assert `speaker.enroll_speaker` NÃO foi chamado (monkeypatch substitui por mock que rastreia call_count).
    - **test_list_speaker_profiles_via_menu** (SPK-09/D-15): salva 2 perfis (alice, bob) via `speaker.save_profile`; mock input retorna `["10", "2", "0", "0"]` (10=Perfis, 2=Listar, 0=sai submenu, 0=sai menu principal); assert stdout contém "alice" E "bob".

    Todos os testes usam fixtures: `tmp_home`, `mock_voice_encoder`, monkeypatch para `jarvis_desktop.ui.get_input` (sequência via lista + iter).

    Testes devem FALHAR (RED) antes da Task 2 (menu ainda não tem itens 9/10).
  </behavior>
  <action>
    Abrir `apps/desktop-py/tests/test_config_menu.py`. No final do arquivo, adicionar:

    ```python
    # ---------------------------------------------------------------------------
    # Phase 89: Speaker Recognition menu tests (SPK-09, T-89-02)
    # ---------------------------------------------------------------------------

    import numpy as np


    def _make_input_sequence(monkeypatch, responses: list[str]) -> None:
        """Patch ui.get_input para retornar os valores em sequência."""
        it = iter(responses)
        def fake_input(prompt: str = "") -> str:
            try:
                return next(it)
            except StopIteration:
                raise EOFError("input sequence exhausted")
        monkeypatch.setattr("jarvis_desktop.ui.get_input", fake_input)


    def test_config_menu_speaker_option(tmp_home, mock_voice_encoder, monkeypatch, capsys):
        """SPK-09: menu /config exibe itens 9 (toggle) e 10 (perfis)."""
        from jarvis_desktop.chat import _show_config_menu
        from jarvis_desktop.config import JarvisConfig

        config = JarvisConfig()
        _make_input_sequence(monkeypatch, ["0"])  # sai imediato

        _show_config_menu(config)
        captured = capsys.readouterr()
        out = captured.out + captured.err

        assert "9. Reconhecimento voz" in out
        assert "10. Perfis de voz" in out


    def test_config_menu_speaker_toggle_enables(tmp_home, mock_voice_encoder, monkeypatch):
        """SPK-09: opção 9 alterna config.speaker_recognition_enabled."""
        from jarvis_desktop.chat import _show_config_menu
        from jarvis_desktop.config import JarvisConfig

        config = JarvisConfig(speaker_recognition_enabled=False)
        _make_input_sequence(monkeypatch, ["9", "0"])

        _show_config_menu(config)

        assert config.speaker_recognition_enabled is True


    def test_enroll_speaker_via_menu_rejects_invalid_name(
        tmp_home, mock_voice_encoder, monkeypatch, capsys
    ):
        """T-89-02: nome com path traversal é rejeitado, enroll_speaker NÃO é chamado."""
        from jarvis_desktop.chat import _show_config_menu
        from jarvis_desktop.config import JarvisConfig
        from jarvis_desktop import speaker as spk

        enroll_calls = []
        monkeypatch.setattr(
            spk,
            "enroll_speaker",
            lambda name, config, n_utterances=5: enroll_calls.append((name, n_utterances)),
        )

        config = JarvisConfig()
        # 10 = Perfis de voz, 1 = Adicionar perfil, "../etc/passwd" = nome inválido,
        # depois 0 sai do submenu, depois 0 sai do menu principal
        _make_input_sequence(monkeypatch, ["10", "1", "../etc/passwd", "0", "0"])

        _show_config_menu(config)

        captured = capsys.readouterr()
        out = captured.out + captured.err
        assert "Nome inválido" in out or "inválido" in out
        assert enroll_calls == []  # enroll_speaker NÃO foi chamado


    def test_list_speaker_profiles_via_menu(
        tmp_home, mock_voice_encoder, monkeypatch, capsys
    ):
        """D-15/SPK-09: Listar perfis imprime nomes cadastrados."""
        from jarvis_desktop.chat import _show_config_menu
        from jarvis_desktop.config import JarvisConfig
        from jarvis_desktop import speaker as spk

        emb = np.zeros(256, dtype=np.float32)
        spk.save_profile("alice", emb)
        spk.save_profile("bob", emb)

        config = JarvisConfig()
        # 10=Perfis, 2=Listar, 0=sai submenu, 0=sai principal
        _make_input_sequence(monkeypatch, ["10", "2", "0", "0"])

        _show_config_menu(config)
        captured = capsys.readouterr()
        out = captured.out + captured.err

        assert "alice" in out
        assert "bob" in out
    ```

    Não modificar testes existentes em test_config_menu.py.
  </action>
  <verify>
    <automated>cd apps/desktop-py &amp;&amp; pytest tests/test_config_menu.py -k "speaker" --collect-only -q 2>&amp;1 | grep -cE "test_(config_menu_speaker_option|config_menu_speaker_toggle_enables|enroll_speaker_via_menu_rejects_invalid_name|list_speaker_profiles_via_menu)" | grep -q "^4$" &amp;&amp; echo "OK 4 tests"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def test_config_menu_speaker_option" apps/desktop-py/tests/test_config_menu.py`
    - `grep -q "def test_config_menu_speaker_toggle_enables" apps/desktop-py/tests/test_config_menu.py`
    - `grep -q "def test_enroll_speaker_via_menu_rejects_invalid_name" apps/desktop-py/tests/test_config_menu.py`
    - `grep -q "def test_list_speaker_profiles_via_menu" apps/desktop-py/tests/test_config_menu.py`
    - `cd apps/desktop-py && pytest tests/test_config_menu.py -k "speaker" --collect-only -q` coleta exatamente 4 testes
    - `cd apps/desktop-py && pytest tests/test_config_menu.py -k "speaker" -x 2>&1 | tail -5` mostra FALHA (RED) — menu ainda não tem itens 9/10
  </acceptance_criteria>
  <done>
    4 testes novos coletados pelo pytest e FALHANDO (RED), porque _show_config_menu ainda não imprime "9. Reconhecimento voz" nem "10. Perfis de voz".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar itens 9/10 no /config menu + submenu de perfis</name>
  <files>apps/desktop-py/src/jarvis_desktop/chat.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 665-720 (_show_config_menu — onde adicionar prints e branches)
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 846-909 (_menu_kokoro_voice + _menu_chatterbox_audio_ref — padrão de submenu)
    - apps/desktop-py/src/jarvis_desktop/speaker.py (Plan 01 — API enroll_speaker, list_profiles, delete_profile, _safe_profile_name)
    - apps/desktop-py/tests/test_config_menu.py (Task 1 — entender exatamente quais asserts precisam passar)
  </read_first>
  <behavior>
    Após esta task, `pytest tests/test_config_menu.py -k "speaker"` passa 4/4 verde.

    Mudanças em `_show_config_menu` (linha 665-720):
    - Adicionar 2 prints novos após item 8 (chatterbox condicional), antes de "0. Sair":
      ```
      9. Reconhecimento voz [sim|nao]
      10. Perfis de voz [N cadastrados]
      ```
    - Adicionar 2 branches `elif choice == "9"` (toggle) e `elif choice == "10"` (abre submenu)

    Novas funções privadas:
    - `_menu_speaker_recognition(config)` — toggle inline (mesmo padrão item 4/5/6)
    - `_menu_speaker_profiles(config)` — abre submenu loop com 3 opções
    - `_enroll_speaker_via_menu(config)` — pede nome via ui.get_input, valida via `speaker._safe_profile_name` (catch ValueError → print "Nome inválido"), chama `speaker.enroll_speaker(name, config, n_utterances=5)`
    - `_list_speaker_profiles_via_menu()` — `speaker.list_profiles()` + print enumerado ou "Nenhum perfil cadastrado"
    - `_delete_speaker_profile_via_menu()` — lista, pede índice, valida range, pede confirmação `(s/N)`, chama `speaker.delete_profile(name)`
  </behavior>
  <action>
    **Parte A — Modificar `_show_config_menu` (linhas 665-720 de chat.py):**

    Localizar o bloco de prints antes de `console.print("0. Sair")` (linha ~685). DEPOIS do bloco `if config.tts_provider == "chatterbox": ... item 8 ...` e ANTES de `console.print("0. Sair")`, adicionar:

    ```python
            # Phase 89: Speaker recognition (SPK-09, D-12, D-15)
            spk_status = "sim" if config.speaker_recognition_enabled else "nao"
            console.print(f"9. Reconhecimento voz  [{spk_status}]", markup=False)
            from jarvis_desktop import speaker as _spk
            n_profiles = len(_spk.list_profiles())
            console.print(f"10. Perfis de voz      [{n_profiles} cadastrados]", markup=False)
    ```

    No `if/elif` chain de branches (linhas 693-720), DEPOIS do `elif choice == "8" and config.tts_provider == "chatterbox":` e ANTES do `else: console.print("Opção inválida...")`, adicionar:

    ```python
            elif choice == "9":
                _menu_speaker_recognition(config)
            elif choice == "10":
                _menu_speaker_profiles(config)
    ```

    **Parte B — Adicionar 5 funções novas no final de chat.py (após _menu_chatterbox_audio_ref, linha 909):**

    ```python
    # ---------------------------------------------------------------------------
    # Phase 89: Speaker Recognition menu (SPK-09, D-12, D-15)
    # ---------------------------------------------------------------------------

    def _menu_speaker_recognition(config: JarvisConfig) -> None:
        """Toggle speaker_recognition_enabled (SPK-09, D-08 enabler)."""
        from jarvis_desktop import ui
        from jarvis_desktop.config import save_config

        console = ui.get_console()
        config.speaker_recognition_enabled = not config.speaker_recognition_enabled
        save_config(config)
        status = "sim" if config.speaker_recognition_enabled else "nao"
        console.print(f"[Reconhecimento de voz: {status}]", highlight=False)


    def _menu_speaker_profiles(config: JarvisConfig) -> None:
        """Submenu de gerenciamento de perfis de voz (D-12, D-15).

        3 ações:
          1. Adicionar perfil (enrollment com 5 utterances)
          2. Listar perfis
          3. Remover perfil
          0. Voltar
        """
        from jarvis_desktop import ui

        console = ui.get_console()
        while True:
            console.print()
            console.print("Perfis de voz:", highlight=False)
            console.print("  1. Adicionar perfil", highlight=False)
            console.print("  2. Listar perfis", highlight=False)
            console.print("  3. Remover perfil", highlight=False)
            console.print("  0. Voltar", highlight=False)
            console.print()

            try:
                choice = ui.get_input("> ").strip()
            except (EOFError, KeyboardInterrupt):
                return

            if choice == "0":
                return
            elif choice == "1":
                _enroll_speaker_via_menu(config)
            elif choice == "2":
                _list_speaker_profiles_via_menu()
            elif choice == "3":
                _delete_speaker_profile_via_menu()
            else:
                console.print(f"[Opção inválida: {choice!r}]", highlight=False)


    def _enroll_speaker_via_menu(config: JarvisConfig) -> None:
        """Adicionar perfil: pede nome, sanitiza, grava 5 utterances (D-12, D-13, T-89-02)."""
        from jarvis_desktop import ui, speaker as spk

        console = ui.get_console()
        try:
            name = ui.get_input("Nome do perfil: ").strip()
        except (EOFError, KeyboardInterrupt):
            return

        if not name:
            console.print("[Nome vazio — cancelado]", highlight=False)
            return

        # T-89-02: sanitização early — evita chamar enroll_speaker com nome perigoso.
        try:
            safe = spk._safe_profile_name(name)
        except ValueError as exc:
            console.print(f"[Nome inválido: {exc}]", highlight=False)
            return

        # Confirmar sobrescrita se já existe
        if safe in spk.list_profiles():
            try:
                confirm = ui.get_input(
                    f"Perfil '{safe}' já existe. Sobrescrever? (s/N): "
                ).strip().lower()
            except (EOFError, KeyboardInterrupt):
                return
            if confirm != "s":
                console.print("[Cancelado]", highlight=False)
                return

        try:
            # D-13: n_utterances=5 default
            spk.enroll_speaker(safe, config, n_utterances=5)
        except RuntimeError as exc:
            # ImportError de resemblyzer vira RuntimeError em _get_encoder
            console.print(f"[Erro ao gravar perfil: {exc}]", highlight=False)


    def _list_speaker_profiles_via_menu() -> None:
        """Listar perfis (D-15)."""
        from jarvis_desktop import ui, speaker as spk

        console = ui.get_console()
        profiles = spk.list_profiles()
        if not profiles:
            console.print("[Nenhum perfil cadastrado]", highlight=False)
            return

        console.print()
        console.print(f"Perfis cadastrados ({len(profiles)}):", highlight=False)
        for i, name in enumerate(profiles, 1):
            console.print(f"  {i}. {name}", highlight=False)
        console.print()


    def _delete_speaker_profile_via_menu() -> None:
        """Remover perfil (D-15, T-89-02-02)."""
        from jarvis_desktop import ui, speaker as spk

        console = ui.get_console()
        profiles = spk.list_profiles()
        if not profiles:
            console.print("[Nenhum perfil cadastrado]", highlight=False)
            return

        console.print()
        console.print("Selecione o perfil a remover:", highlight=False)
        for i, name in enumerate(profiles, 1):
            console.print(f"  {i}. {name}", highlight=False)
        console.print("  0. Cancelar", highlight=False)
        console.print()

        try:
            raw = ui.get_input("> ").strip()
        except (EOFError, KeyboardInterrupt):
            return

        if raw == "0" or not raw:
            return

        try:
            idx = int(raw) - 1
        except ValueError:
            console.print(f"[Entrada inválida: {raw!r}]", highlight=False)
            return

        if not (0 <= idx < len(profiles)):
            console.print("[Índice fora do intervalo]", highlight=False)
            return

        target = profiles[idx]
        try:
            confirm = ui.get_input(f"Remover '{target}'? (s/N): ").strip().lower()
        except (EOFError, KeyboardInterrupt):
            return

        if confirm != "s":
            console.print("[Cancelado]", highlight=False)
            return

        if spk.delete_profile(target):
            console.print(f"[Perfil '{target}' removido]", highlight=False)
        else:
            console.print(f"[Perfil '{target}' já não existia]", highlight=False)
    ```

    Verificar que nenhuma outra função foi modificada (apenas adições).
  </action>
  <verify>
    <automated>cd apps/desktop-py &amp;&amp; pytest tests/test_config_menu.py -k "speaker" -x -v 2>&amp;1 | tail -10 &amp;&amp; pytest tests/test_config_menu.py -k "speaker" -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def _menu_speaker_recognition" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "def _menu_speaker_profiles" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "def _enroll_speaker_via_menu" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "def _list_speaker_profiles_via_menu" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "def _delete_speaker_profile_via_menu" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q '9. Reconhecimento voz' apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q '10. Perfis de voz' apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "spk._safe_profile_name(name)" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `grep -q "spk.enroll_speaker(safe, config, n_utterances=5)" apps/desktop-py/src/jarvis_desktop/chat.py`
    - `cd apps/desktop-py && pytest tests/test_config_menu.py -k "speaker" -x` retorna exit 0 (4 testes passam)
    - `cd apps/desktop-py && pytest tests/ -x` retorna exit 0 (suite completa verde, incluindo Plan 01)
  </acceptance_criteria>
  <done>
    Menu /config tem itens 9 (toggle) e 10 (perfis com submenu). 5 funções privadas implementadas. 4 testes test_config_menu.py speaker verdes. Suite completa verde.
  </done>
</task>

</tasks>

<verification>
- `cd apps/desktop-py && pytest tests/test_config_menu.py -k "speaker" -x` → 4/4 passed
- `cd apps/desktop-py && pytest tests/test_speaker.py tests/test_config_menu.py -x` → todos passam (Plan 01 + Plan 02)
- `cd apps/desktop-py && pytest tests/ -x` → suite completa verde
- Inspeção manual do menu (não automatizada — verificação opcional):
  ```bash
  python -c "from jarvis_desktop.chat import _show_config_menu; from jarvis_desktop.config import JarvisConfig; print('Test ok')"
  ```
</verification>

<success_criteria>
- ✅ Menu /config tem item "9. Reconhecimento voz [sim|nao]" (SPK-09)
- ✅ Menu /config tem item "10. Perfis de voz [N cadastrados]" (SPK-09)
- ✅ Toggle do item 9 alterna config.speaker_recognition_enabled e persiste via save_config
- ✅ Submenu de perfis tem: 1. Adicionar, 2. Listar, 3. Remover, 0. Voltar (D-15)
- ✅ Adicionar perfil sanitiza nome (rejeita path traversal — T-89-02) e chama enroll_speaker(n=5) (D-13)
- ✅ Listar perfis imprime nomes de speaker.list_profiles()
- ✅ Remover perfil pede índice, valida range, confirma (s/N), chama speaker.delete_profile
- ✅ Sobrescrita de perfil existente requer confirmação
- ✅ Todas mensagens em pt-BR
- ✅ Erros (nome inválido, perfil ausente, índice fora do range) não causam crash — mostrados como `[mensagem]`
</success_criteria>

<output>
Após completar, criar `.planning/phases/89-identifica-o-de-voz-speaker-recognition-backlog/89-02-SUMMARY.md`
</output>
