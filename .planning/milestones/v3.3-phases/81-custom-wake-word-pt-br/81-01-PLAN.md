---
phase: 81-custom-wake-word-pt-br
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop-py/tests/test_train_wake_word.py
  - apps/desktop-py/tools/train_wake_word.py
autonomous: true
requirements:
  - WAKE-01
  - WAKE-02
  - WAKE-03
  - WAKE-04
  - WAKE-05

must_haves:
  truths:
    - "Test suite contains xfail stubs for all WAKE-01 through WAKE-05 behaviors"
    - "train_wake_word.py exists with PEP 723 inline script metadata and a runnable --help flag"
    - "Running uv run tools/train_wake_word.py --help exits 0 without import errors"
  artifacts:
    - path: "apps/desktop-py/tests/test_train_wake_word.py"
      provides: "xfail stubs for WAKE-01..05 test coverage"
      contains: "pytest.mark.xfail"
    - path: "apps/desktop-py/tools/train_wake_word.py"
      provides: "PEP 723 script scaffold"
      contains: "# /// script"
  key_links:
    - from: "apps/desktop-py/tests/test_train_wake_word.py"
      to: "apps/desktop-py/tools/train_wake_word.py"
      via: "subprocess.run or importlib import of main()"
      pattern: "train_wake_word"
---

<objective>
Wave 0 scaffold: create the test stubs for all WAKE requirements and a minimal PEP 723 script skeleton that makes `uv run tools/train_wake_word.py --help` succeed. No implementation logic yet — this plan establishes the Nyquist contract so every downstream wave has a failing test to turn green.

Purpose: Nyquist compliance — no implementation wave may begin without a failing automated test to verify against.
Output: test_train_wake_word.py (9 xfail stubs) + tools/train_wake_word.py (PEP 723 stub with --help).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-custom-wake-word-pt-br/81-CONTEXT.md
@.planning/phases/81-custom-wake-word-pt-br/81-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase that this plan replicates. -->

From apps/desktop-py/tests/conftest.py:
```python
@pytest.fixture
def tmp_home(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> Path:
    """Redirect ~/.jarvis to a temporary directory for test isolation."""
    fake_home = tmp_path / "home"
    fake_home.mkdir()
    monkeypatch.setenv("HOME", str(fake_home))
    monkeypatch.setenv("USERPROFILE", str(fake_home))
    return fake_home

@pytest.fixture
def jarvis_config_dir(tmp_home: Path) -> Path:
    config_dir = tmp_home / ".jarvis"
    config_dir.mkdir(parents=True, exist_ok=True)
    return config_dir
```

From apps/desktop-py/src/jarvis_desktop/config.py:
```python
class JarvisConfig(BaseModel):
    wake_word_threshold: float = Field(default=0.7, ...)
```

xfail pattern from existing tests (e.g. test_voice_modes.py):
```python
@pytest.mark.xfail(reason="Wave 0 stub — implementation pending", strict=True)
def test_something():
    raise NotImplementedError
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_train_wake_word.py with xfail stubs</name>
  <files>apps/desktop-py/tests/test_train_wake_word.py</files>
  <read_first>
    - apps/desktop-py/tests/conftest.py (fixture patterns to reuse: tmp_home, jarvis_config_dir)
    - apps/desktop-py/tests/test_voice_modes.py (xfail pattern and module reset pattern)
    - apps/desktop-py/tests/test_config_persistence.py (config save patterns)
    - .planning/phases/81-custom-wake-word-pt-br/81-RESEARCH.md (test map in Validation Architecture section)
  </read_first>
  <action>
Create `apps/desktop-py/tests/test_train_wake_word.py` with the following structure.

**Module docstring:**
```
Tests for train_wake_word.py — Phase 81 Custom Wake Word pt-BR.

Covers WAKE-01 through WAKE-05. All tests are Wave 0 xfail stubs.
Implementation plans (81-02) turn these green.
```

**Imports:**
```python
import subprocess
import sys
from pathlib import Path

import pytest

from jarvis_desktop.config import JarvisConfig
```

**Define a module-level constant:**
```python
SCRIPT_PATH = Path(__file__).parent.parent / "tools" / "train_wake_word.py"
```

**Write exactly these 9 test functions, all decorated with `@pytest.mark.xfail(reason="Wave 0 stub — implementation pending", strict=True)`:**

1. `test_pep723_metadata` (WAKE-01)
   - Assert: `SCRIPT_PATH.exists()`
   - Read the file text: `text = SCRIPT_PATH.read_text()`
   - Assert: `"# /// script"` in text
   - Assert: `"requires-python"` in text
   - Assert: `"openwakeword==0.6.0"` in text
   - Assert: `"scikit-learn>=1.3"` in text
   - Assert: `"torch>=2.0"` in text

2. `test_script_runs_with_help` (WAKE-01)
   - Run: `subprocess.run([sys.executable, "-m", "uv", "run", str(SCRIPT_PATH), "--help"], capture_output=True, timeout=30)`
   - Actually use uv CLI: `subprocess.run(["uv", "run", str(SCRIPT_PATH), "--help"], capture_output=True, timeout=60, text=True)`
   - Assert returncode == 0
   - Assert "ei jarvis" or "train" in result.stdout.lower()

3. `test_recording_session_minimum_samples` (WAKE-02)
   - Docstring: "Recording session must enforce minimum 20 samples before training"
   - `raise NotImplementedError("Wave 0 stub")`

4. `test_countdown_ux` (WAKE-02)
   - Docstring: "Countdown '3...2...1... fale agora' must be printed during recording"
   - `raise NotImplementedError("Wave 0 stub")`

5. `test_rms_feedback_per_sample` (WAKE-02)
   - Docstring: "RMS feedback must be shown after each recorded sample (sinal: boa / sinal: fraco)"
   - `raise NotImplementedError("Wave 0 stub")`

6. `test_ptt_flag` (WAKE-02)
   - Docstring: "When --ptt flag is passed, recording waits for keypress instead of auto-recording"
   - `raise NotImplementedError("Wave 0 stub")`

7. `test_model_save_path` (WAKE-03)
   - Uses `tmp_home` fixture (from conftest.py)
   - Docstring: "After training completes, .pkl is saved to ~/.jarvis/models/wake_word_custom.pkl"
   - `raise NotImplementedError("Wave 0 stub")`

8. `test_threshold_calibration` (WAKE-05)
   - Uses `tmp_home` and `jarvis_config_dir` fixtures
   - Docstring: "Auto-calibrated threshold is written to ~/.jarvis/config.json as wake_word_threshold"
   - `raise NotImplementedError("Wave 0 stub")`

9. `test_colab_fallback_path` (WAKE-01, D-01)
   - Docstring: "If median score < 0.25 on positive samples, Colab URL is shown and training is skipped"
   - `raise NotImplementedError("Wave 0 stub")`

For stubs 3–9, decorate each with `@pytest.mark.xfail(reason="Wave 0 stub — implementation pending", strict=True)`. Stubs 1 and 2 (`test_pep723_metadata` and `test_script_runs_with_help`) should also be xfail stubs initially (they become real once Task 2 creates the script file).

NOTE: At Wave 0, SCRIPT_PATH does not exist yet, so test_pep723_metadata and test_script_runs_with_help will fail on the assertion — they are xfail so this is expected. Once 81-02 implements the script, those two tests must be updated to remove xfail.
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_train_wake_word.py -v 2>&1 | head -40</automated>
  </verify>
  <done>
    - File exists at apps/desktop-py/tests/test_train_wake_word.py
    - pytest collects exactly 9 tests from this file
    - All 9 tests show as xfailed (expected failures) — 0 errors, 0 unexpected failures
    - grep confirms "# WAKE-01" "# WAKE-02" "# WAKE-03" "# WAKE-04 — covered by test_voice_modes.py" "# WAKE-05" appear as comments in the file
  </done>
  <acceptance_criteria>
    - `test_train_wake_word.py` file exists at apps/desktop-py/tests/test_train_wake_word.py
    - File contains the string `SCRIPT_PATH = Path(__file__).parent.parent / "tools" / "train_wake_word.py"`
    - File contains 9 function definitions starting with `def test_`
    - All 9 functions are decorated with `@pytest.mark.xfail`
    - Running `uv run pytest apps/desktop-py/tests/test_train_wake_word.py -v` exits 0 with 9 xfailed (no errors, no passes, no failures)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tools/train_wake_word.py skeleton with PEP 723 header and --help</name>
  <files>apps/desktop-py/tools/train_wake_word.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/voice_modes.py (lines 1-50: module docstring pattern)
    - apps/desktop-py/src/jarvis_desktop/stt.py (lines 1-30: docstring + argparse pattern reference)
    - .planning/phases/81-custom-wake-word-pt-br/81-CONTEXT.md (D-09: exact PEP 723 dependencies list)
    - .planning/phases/81-custom-wake-word-pt-br/81-RESEARCH.md (Pattern 1: PEP 723 Script Isolation and PEP 723 Script Header example)
  </read_first>
  <action>
Create the `apps/desktop-py/tools/` directory and the file `apps/desktop-py/tools/train_wake_word.py`.

**Exact PEP 723 header (copy verbatim):**
```python
# /// script
# requires-python = ">=3.10"
# dependencies = [
#   "openwakeword==0.6.0",
#   "scikit-learn>=1.3",
#   "torch>=2.0",
#   "scipy>=1.10",
#   "sounddevice==0.5.5",
#   "rich>=13.0",
#   "numpy>=1.24",
# ]
# ///
```

**Module docstring (immediately after the PEP 723 block):**
```python
"""Script de treinamento interativo de wake word para JARVIS (Português Brasileiro).

Grava amostras de "ei jarvis", treina um modelo verifier via scikit-learn
e instala o resultado em ~/.jarvis/models/wake_word_custom.pkl.

Uso:
  uv run apps/desktop-py/tools/train_wake_word.py [--ptt] [--dry-run]

Opções:
  --ptt       Modo push-to-talk (pressione tecla para gravar)
  --dry-run   Executa sem microfone real (testa o fluxo com áudio sintético)

Requerimentos:
  uv 0.4+ (suporte PEP 723)
  Python 3.10 (criado automaticamente pelo uv)
"""
```

**Imports (standard library only — no heavy deps at module level):**
```python
import argparse
import sys
from pathlib import Path
```

**Constants block:**
```python
# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------
_JARVIS_DIR = Path.home() / ".jarvis"
_MODELS_DIR = _JARVIS_DIR / "models"
_CACHE_DIR = _JARVIS_DIR / "cache"
_POSITIVE_DIR = _CACHE_DIR / "training_positive"
_NEGATIVE_DIR = _CACHE_DIR / "training_negative"
_CUSTOM_PKL = _MODELS_DIR / "wake_word_custom.pkl"

# ---------------------------------------------------------------------------
# Recording constants (match openwakeword + voice_modes.py)
# ---------------------------------------------------------------------------
_CHUNK_SIZE: int = 1280        # samples per chunk
_SAMPLE_RATE: int = 16000      # Hz
_RECORD_DURATION_S: float = 2.5  # seconds per sample (D-03)
_MIN_SAMPLES: int = 20         # minimum before training (D-06)
_REC_SAMPLES_TARGET: int = 30  # recommended (D-06)
_MEDIAN_THRESHOLD: float = 0.25  # branch threshold (D-01)
_TARGET_FPR: float = 0.05      # target false-positive rate for threshold calibration (WAKE-05)
```

**Stub functions (all raise NotImplementedError with a descriptive message):**

```python
def _record_sample(index: int, total: int, ptt: bool = False) -> "np.ndarray":
    """Record a single 'ei jarvis' sample with countdown UX (D-03).
    
    D-03: Countdown + auto-stop (2.5s window via sounddevice).
    D-04: If ptt=True, waits for keypress before recording.
    D-05: Shows RMS feedback after recording.
    
    Returns: numpy array shape (N,) at 16kHz float32.
    """
    raise NotImplementedError("Implemented in Plan 81-02")


def _run_recording_session(n_target: int = _REC_SAMPLES_TARGET, ptt: bool = False) -> list:
    """Interactive recording session for positive samples (D-06, WAKE-02).
    
    Returns list of np.ndarray (one per sample). Min: _MIN_SAMPLES.
    """
    raise NotImplementedError("Implemented in Plan 81-02")


def _calibrate_baseline(samples: list) -> float:
    """Run samples through hey_jarvis_v0.1.onnx, return median score (D-01)."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _download_negative_corpus() -> None:
    """Download AudioSet/FMA slice to _NEGATIVE_DIR (D-07). Idempotent."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _record_ambient_noise(duration_s: int = 300) -> None:
    """Record 5 min of ambient noise into _NEGATIVE_DIR (D-07)."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _generate_tts_negatives() -> None:
    """Generate negative TTS phrases via kokoro (D-08) into _NEGATIVE_DIR."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _train_verifier() -> object:
    """Train sklearn verifier via openwakeword.train_custom_verifier() (WAKE-03)."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _calibrate_threshold(verifier: object) -> float:
    """Auto-calibrate detection threshold at ~5% FPR on negative corpus (WAKE-05)."""
    raise NotImplementedError("Implemented in Plan 81-02")


def _show_colab_fallback() -> None:
    """Display Colab fallback URL when median baseline < _MEDIAN_THRESHOLD (D-01)."""
    print(
        "\n[AVISO] Baseline de detecção insuficiente para treinamento local.\n"
        "Use o notebook oficial do openwakeword no Colab para treinar um modelo completo:\n"
        "  https://colab.research.google.com/github/dscripka/openWakeWord/blob/main/"
        "notebooks/training_custom_model.ipynb\n\n"
        "Após o treino, copie o arquivo .onnx gerado para:\n"
        "  ~/.jarvis/models/wake_word_custom.onnx\n"
        "\nSuas gravações foram salvas em:\n"
        f"  {_POSITIVE_DIR}\n"
        "Você pode fazer upload delas diretamente no Colab."
    )
```

**main() function:**
```python
def main() -> None:
    """Entry point for train_wake_word.py."""
    parser = argparse.ArgumentParser(
        description="Treina um wake word customizado 'ei jarvis' para JARVIS (pt-BR).",
        formatter_class=argparse.RawDescriptionHelpFormatter,
    )
    parser.add_argument(
        "--ptt",
        action="store_true",
        help="Modo push-to-talk: pressione uma tecla para iniciar cada gravação",
    )
    parser.add_argument(
        "--dry-run",
        action="store_true",
        help="Testa o fluxo sem microfone real (áudio sintético)",
    )
    args = parser.parse_args()

    print("=" * 60)
    print("  JARVIS — Treinamento de Wake Word (ei jarvis | pt-BR)")
    print("=" * 60)
    print()
    print("Este script vai:")
    print("  1. Gravar suas amostras de 'ei jarvis'")
    print("  2. Treinar um modelo verifier personalizado")
    print("  3. Instalar o modelo em ~/.jarvis/models/")
    print("  4. Calibrar o threshold de detecção automaticamente")
    print()

    if args.dry_run:
        print("[DRY-RUN] Modo de teste ativo — sem microfone real.")
        print("[DRY-RUN] Implementação completa disponível após Plan 81-02.")
        sys.exit(0)

    # Full flow — implemented in Plan 81-02
    raise NotImplementedError(
        "Fluxo completo implementado no Plan 81-02. "
        "Use --dry-run para testar o scaffold."
    )


if __name__ == "__main__":
    main()
```

The `--help` path must work without any heavy imports (no torch, no openwakeword at module level). All heavy imports must be inside the function bodies.
  </action>
  <verify>
    <automated>cd apps/desktop-py && python tools/train_wake_word.py --help</automated>
  </verify>
  <done>
    - File exists at apps/desktop-py/tools/train_wake_word.py
    - `python tools/train_wake_word.py --help` exits 0 and prints the description in pt-BR
    - `python tools/train_wake_word.py --dry-run` exits 0 and prints "[DRY-RUN]"
    - File contains `# /// script` on line 1
    - File contains `requires-python = ">=3.10"`
    - File contains all 8 stub function definitions
  </done>
  <acceptance_criteria>
    - File starts with `# /// script` (line 1)
    - File contains `requires-python = ">=3.10"`
    - File contains `"openwakeword==0.6.0"` in the PEP 723 block
    - File contains `"torch>=2.0"` in the PEP 723 block
    - File contains `_CUSTOM_PKL = _MODELS_DIR / "wake_word_custom.pkl"`
    - `python apps/desktop-py/tools/train_wake_word.py --help` exits 0
    - `python apps/desktop-py/tools/train_wake_word.py --dry-run` exits 0
    - Running `uv run pytest apps/desktop-py/tests/test_train_wake_word.py::test_pep723_metadata -v` still xfails (strict) — xfail is removed in 81-02 once the full implementation makes it pass
  </acceptance_criteria>
</task>

</tasks>

<verification>
After both tasks:
- `uv run pytest apps/desktop-py/tests/ -v` — full suite green (existing tests pass, new stubs xfail)
- `python apps/desktop-py/tools/train_wake_word.py --help` exits 0
- `python apps/desktop-py/tools/train_wake_word.py --dry-run` exits 0
- `grep "# /// script" apps/desktop-py/tools/train_wake_word.py` returns line 1
</verification>

<success_criteria>
- 9 xfail stubs collected by pytest (0 errors, 0 unexpected passes)
- Existing full test suite (44+ tests) remains green
- train_wake_word.py scaffold is importable without heavy deps
- --help and --dry-run flags work with standard Python (no uv isolation needed for these paths)
</success_criteria>

<output>
After completion, create `.planning/phases/81-custom-wake-word-pt-br/81-01-SUMMARY.md`
</output>
