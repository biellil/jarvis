---
phase: 12-hotkey-text-chat
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - apps/desktop/src/main/ipc/chat.ts
  - apps/desktop/src/main/__tests__/ipc-chat.test.ts
  - apps/gateway/src/routes/chat.ts
  - apps/gateway/src/__tests__/chat.test.ts
autonomous: true
requirements: [ACTV-02]

must_haves:
  truths:
    - "IPC handler calls gateway with message"
    - "Gateway proxies to FastAPI successfully"
    - "Response returns through IPC chain"
    - "Network errors handled gracefully"
    - "Timeout prevents indefinite hang"
  artifacts:
    - path: "apps/desktop/src/main/ipc/chat.ts"
      provides: "Extended chat handler with HTTP call"
      contains: "fetch.*localhost:3000"
    - path: "apps/desktop/src/main/__tests__/ipc-chat.test.ts"
      provides: "Tests for HTTP integration"
      min_lines: 60
  key_links:
    - from: "main/ipc/chat.ts"
      to: "localhost:3000/api/chat"
      via: "fetch POST request"
      pattern: "fetch.*POST.*api/chat"
    - from: "chat.ts handler"
      to: "IpcResult"
      via: "error wrapping"
      pattern: "success: false.*error:"
---

<objective>
Implement IPC handler that calls gateway HTTP endpoint to complete the chat flow

Purpose: Connect Electron frontend to FastAPI backend through gateway
Output: Working end-to-end message flow from renderer to FastAPI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-hotkey-text-chat/12-CONTEXT.md
@.planning/phases/12-hotkey-text-chat/12-RESEARCH.md

# Existing code
@apps/desktop/src/main/ipc/chat.ts
@apps/desktop/src/shared/ipc-types.ts
@apps/gateway/src/routes/chat.ts
</context>

<interfaces>
<!-- Existing interfaces to extend -->

From apps/desktop/src/main/ipc/chat.ts:
```typescript
export function setupChatHandlers(): void {
  ipcMain.handle(
    IPC_CHANNELS.CHAT_SEND_TEXT,
    async (_event, message: string): Promise<SendTextResponse>
  );
}
```

From apps/gateway/src/routes/chat.ts (expected):
```typescript
// POST /api/chat
// Body: { message: string }
// Response: { response: string } or { error: string }
```

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export interface SendTextData {
  received: string;  // Currently just echoes
  // Will become: reply: string
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IPC chat handler with gateway HTTP call</name>
  <files>apps/desktop/src/main/ipc/chat.ts, apps/desktop/src/main/__tests__/ipc-chat.test.ts</files>
  <behavior>
    - Test 1: Successful POST returns reply from gateway
    - Test 2: Network error returns IpcResult with error
    - Test 3: Timeout after 10 seconds returns timeout error
    - Test 4: Non-200 status returns error with status code
    - Test 5: Invalid JSON response handled gracefully
  </behavior>
  <action>
    Update chat.ts handler to call gateway per Research Pattern 3.

    Implementation:
    - Replace echo logic with fetch to gateway
    - URL: 'http://localhost:3000/api/chat' (not hardcoded - use const)
    - Method: POST with JSON body { message }
    - Add AbortController with 10s timeout (Research Pitfall 5)
    - Parse JSON response, extract 'response' field
    - Return { success: true, data: { reply: response } }
    - Catch all errors, return { success: false, error: message }
    - Never throw across IPC boundary (Research Pitfall 2)

    Update SendTextData interface:
    - Change 'received' to 'reply' field
    - Update tests to expect 'reply' not 'received'

    Reference: Research line 219-259 for IPC+fetch pattern
    Reference: Research line 574-596 for timeout pattern
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/main/__tests__/ipc-chat.test.ts</automated>
  </verify>
  <done>IPC handler calls gateway, tests pass with mocked fetch</done>
</task>

<task type="auto">
  <name>Task 2: Verify gateway chat endpoint</name>
  <files>apps/gateway/src/routes/chat.ts, apps/gateway/src/__tests__/chat.test.ts</files>
  <action>
    Ensure gateway /api/chat endpoint works correctly.

    Verify existing implementation has:
    - POST /api/chat route registered
    - Accepts { message: string } body
    - Proxies to FastAPI http://localhost:8000/chat
    - Returns { response: string } on success
    - Returns error structure on failure

    If tests don't exist, create basic test:
    - Mock fetch to FastAPI
    - Test successful proxy
    - Test error handling

    This ensures the chain works when integrated.
  </action>
  <verify>
    <automated>pnpm --filter gateway test src/__tests__/chat.test.ts</automated>
  </verify>
  <done>Gateway chat endpoint verified or fixed</done>
</task>

<task type="auto">
  <name>Task 3: Add integration smoke test</name>
  <files>apps/desktop/src/main/__tests__/integration-chat.test.ts</files>
  <action>
    Create integration test that validates full chain.

    Test setup:
    - Mock gateway server on port 3001 (avoid conflict)
    - Configure IPC handler to use mock port
    - Send message through IPC
    - Verify mock server receives POST with correct body
    - Return mock response
    - Verify IPC returns expected reply

    This proves the chain works end-to-end without real services.

    Can use MSW or simple Express server for mocking.
  </action>
  <verify>
    <automated>pnpm --filter desktop test src/main/__tests__/integration-chat.test.ts</automated>
  </verify>
  <done>Integration test validates full IPC → HTTP → response chain</done>
</task>

</tasks>

<verification>
1. Start gateway: `pnpm --filter gateway dev`
2. Start FastAPI: `cd apps/jarvis && python -m jarvis.api`
3. Start desktop: `pnpm --filter desktop dev`
4. Type message in widget input
5. Check gateway logs - see POST /api/chat
6. Check FastAPI logs - see request received
7. Response appears in DevTools console
8. Network error handling: Stop gateway, try again - see error
</verification>

<success_criteria>
- IPC handler sends POST to gateway with message
- Gateway proxies to FastAPI successfully
- Response flows back through IPC chain
- Network errors return IpcResult with error message
- 10-second timeout prevents hanging
- All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/12-hotkey-text-chat/12-03-SUMMARY.md`
</output>