---
phase: 45-voice-pipeline-bug-fixes
plan: 03
type: execute
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - apps/desktop/src/main/__tests__/index.test.ts
autonomous: true
gap_closure: true
requirements: [PATCH-01, PATCH-02]

must_haves:
  truths:
    - "npx vitest run src/main/__tests__/index.test.ts exits 0 with 3 tests passed and 0 errors"
    - "No unhandled Vitest error: No 'setVoiceModeManager' export is defined on the '../ptt-hotkey' mock"
    - "All 3 vi.doMock('../ptt-hotkey', ...) blocks in index.test.ts export setVoiceModeManager as vi.fn()"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/index.test.ts"
      provides: "Startup integration test with correct ptt-hotkey mock"
      contains: "setVoiceModeManager: vi.fn()"
  key_links:
    - from: "apps/desktop/src/main/__tests__/index.test.ts"
      to: "apps/desktop/src/main/ptt-hotkey.ts"
      via: "vi.doMock('../ptt-hotkey', ...) — must expose all exported symbols used by index.ts"
      pattern: "setVoiceModeManager: vi\\.fn\\(\\)"
---

<objective>
Fix the test regression introduced by PATCH-01 in index.test.ts. The three vi.doMock('../ptt-hotkey', ...) blocks in index.test.ts are missing setVoiceModeManager: vi.fn(). When index.ts calls setVoiceModeManager(voiceModeManager) at startup (line 301), Vitest's strict mock enforcement throws an unhandled error for all three tests.

Purpose: Restore full test suite green — production code is correct, only the mock is incomplete.
Output: index.test.ts with setVoiceModeManager added to all 3 ptt-hotkey mock objects; all 3 index tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-voice-pipeline-bug-fixes/45-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setVoiceModeManager: vi.fn() to all 3 ptt-hotkey mocks in index.test.ts</name>
  <files>apps/desktop/src/main/__tests__/index.test.ts</files>

  <read_first>
    apps/desktop/src/main/__tests__/index.test.ts
  </read_first>

  <action>
Open apps/desktop/src/main/__tests__/index.test.ts and locate the three vi.doMock('../ptt-hotkey', ...) blocks. Each block currently looks like:

    vi.doMock('../ptt-hotkey', () => ({
      registerPttHotkey: vi.fn().mockReturnValue(true),
      unregisterPttHotkey: vi.fn(),
    }));

Add setVoiceModeManager: vi.fn() as the FIRST property in each of the three blocks so they become:

    vi.doMock('../ptt-hotkey', () => ({
      setVoiceModeManager: vi.fn(),
      registerPttHotkey: vi.fn().mockReturnValue(true),
      unregisterPttHotkey: vi.fn(),
    }));

The three blocks appear at approximately:
- Lines 132-135 (first test: USE_WHISPER_CPP=true → setupIpcHandlers called with voiceHandler)
- Lines 255-258 (second test: USE_WHISPER_CPP=false → setupIpcHandlers called WITHOUT voiceHandler)
- Lines 377-380 (third test: VRAM detection throws → continues with base model fallback)

Do NOT change anything else in the file. No other mocks, no imports, no test logic.

This is per PATCH-01 (45-01-PLAN.md): index.ts calls setVoiceModeManager(voiceModeManager) at line 301; the mock must expose this export so Vitest does not throw.
  </action>

  <verify>
    <automated>cd apps/desktop && npx vitest run src/main/__tests__/index.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - Command exits with code 0
    - Output contains "3 passed" with no errors
    - No line matching "No 'setVoiceModeManager' export is defined" appears in output
    - grep "setVoiceModeManager: vi.fn()" apps/desktop/src/main/__tests__/index.test.ts returns exactly 3 matches
  </acceptance_criteria>

  <done>All 3 index.test.ts startup tests pass; no unhandled Vitest mock errors; full ptt-hotkey mock is complete in all three test blocks.</done>
</task>

</tasks>

<verification>
Run from apps/desktop directory:

1. npx vitest run src/main/__tests__/index.test.ts
   Expected: 3 passed, 0 errors, exit 0

2. npx vitest run src/main/__tests__/ptt-hotkey.test.ts
   Expected: 15 passed (no regression)

3. npx vitest run src/main/__tests__/index.main.test.ts
   Expected: 9 passed (no regression)
</verification>

<success_criteria>
- npx vitest run src/main/__tests__/index.test.ts exits 0 with 3 tests passed and 0 unhandled errors
- No regression in ptt-hotkey.test.ts (15 tests) or index.main.test.ts (9 tests)
- grep for setVoiceModeManager: vi.fn() in index.test.ts returns exactly 3 matches
</success_criteria>

<output>
After completion, create .planning/phases/45-voice-pipeline-bug-fixes/45-03-SUMMARY.md
</output>
