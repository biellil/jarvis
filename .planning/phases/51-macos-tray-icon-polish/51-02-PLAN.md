---
phase: 51-macos-tray-icon-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - apps/desktop/src/main/tray.ts
  - apps/desktop/src/main/__tests__/tray.platform.test.ts
autonomous: true
requirements:
  - MCOS-01
must_haves:
  truths:
    - "Em macOS (process.platform === 'darwin'), tray.ts carrega 'iconTemplate.png'"
    - "Em Windows (process.platform === 'win32'), tray.ts continua carregando 'icon-16x16.png'"
    - "Em Linux (process.platform === 'linux'), tray.ts continua carregando 'icon-16x16.png'"
    - "Selecao de icone e feita por platform check inline em createTray() — nao requer listener nem restart"
    - "Eletron auto-detecta '@2x.png' retina pelo naming convention — sem codigo extra"
  artifacts:
    - path: "apps/desktop/src/main/tray.ts"
      provides: "createTray() com selecao condicional de iconPath por process.platform"
      contains: "iconTemplate.png"
    - path: "apps/desktop/src/main/__tests__/tray.platform.test.ts"
      provides: "Testes platform-specific verificando o icon path por OS"
      contains: "iconTemplate.png"
  key_links:
    - from: "apps/desktop/src/main/tray.ts"
      to: "apps/desktop/resources/tray/iconTemplate.png"
      via: "path.join + new Tray(iconPath) quando process.platform === 'darwin'"
      pattern: "iconTemplate\\.png"
    - from: "apps/desktop/src/main/__tests__/tray.platform.test.ts"
      to: "tray.ts source"
      via: "fs.readFileSync + grep assertion"
      pattern: "iconTemplate\\.png"
---

<objective>
Wire o `tray.ts` para usar `iconTemplate.png` em macOS e manter `icon-16x16.png` em Windows/Linux. Adicionar testes de regressao em `tray.platform.test.ts` cobrindo os 3 casos de plataforma (per MCOS-01, D-04, D-05, D-06 + Success Criteria 1/2/3/4).

Purpose: Implementar o comportamento observavel — macOS adapta automaticamente ao tema (template image), Windows/Linux inalterados.
Output: tray.ts com selecao condicional + testes que provam o comportamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-macos-tray-icon-polish/51-CONTEXT.md
@.planning/phases/51-macos-tray-icon-polish/51-01-SUMMARY.md
@apps/desktop/src/main/tray.ts
@apps/desktop/src/main/__tests__/tray.platform.test.ts
@apps/desktop/src/main/__tests__/tray.test.ts

<interfaces>
<!-- Trecho relevante do tray.ts atual (linhas 52-55) — o executor modifica APENAS estas linhas. -->

```ts
export function createTray(mainWindow: BrowserWindow, voiceModeManager: VoiceModeManager): void {
  // D-05: Use 16x16 icon (Electron auto-selects 32x32 for high-DPI)
  const iconPath = path.join(__dirname, '../../resources/tray/icon-16x16.png');
  tray = new Tray(iconPath);
  ...
}
```

<!-- Padrao de platform check ja presente no mesmo arquivo (linha ~95): -->
```ts
if (process.platform === 'darwin' && ...) { ... }
```

<!-- Padrao de teste source-level ja usado em tray.platform.test.ts: -->
```ts
const traySource = fs.readFileSync(path.join(__dirname, '../tray.ts'), 'utf-8');
expect(traySource).toMatch(/icon-\d+x\d+\.png/);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar testes platform-specific para iconPath em tray.platform.test.ts</name>
  <files>apps/desktop/src/main/__tests__/tray.platform.test.ts</files>
  <read_first>
    - apps/desktop/src/main/__tests__/tray.platform.test.ts (arquivo a estender)
    - apps/desktop/src/main/__tests__/tray.test.ts (estilo: source-level assertions com readFileSync)
    - apps/desktop/src/main/tray.ts (estado atual antes da modificacao)
    - .planning/phases/51-macos-tray-icon-polish/51-CONTEXT.md (D-04, D-05)
  </read_first>
  <behavior>
    - Test 1: tray.ts contem a string 'iconTemplate.png' (assertiva de presenca do nome do asset macOS)
    - Test 2: tray.ts contem a string 'icon-16x16.png' (assertiva de preservacao do asset Windows/Linux — regressao Success Criteria #4)
    - Test 3: tray.ts contem a string "process.platform === 'darwin'" usada na selecao do iconPath (selecao por plataforma)
    - Test 4: o trecho entre createTray e new Tray( contem ambos 'iconTemplate.png' e 'icon-16x16.png' (selecao condicional inline, nao em outro lugar do arquivo)
    - Test 5: o trecho entre createTray e new Tray( contem o operador ternario `=== 'darwin' ?` OU um if statement com `'darwin'` (forma da decisao)
  </behavior>
  <action>
    Adicionar um novo `describe` block ao final de `apps/desktop/src/main/__tests__/tray.platform.test.ts` (antes do `});` final do file-level scope se houver, ou apos o describe existente — sao describes paralelos no top-level):

    ```ts
    describe('Phase 51 (MCOS-01): macOS template image selection', () => {
      it('D-04: tray.ts referencia iconTemplate.png para macOS', () => {
        expect(traySource).toContain('iconTemplate.png');
      });

      it('D-04: tray.ts continua referenciando icon-16x16.png (Windows/Linux fallback)', () => {
        expect(traySource).toContain('icon-16x16.png');
      });

      it('D-04: selecao usa process.platform === darwin', () => {
        expect(traySource).toContain("process.platform === 'darwin'");
      });

      it('D-04: selecao condicional ocorre dentro de createTray (entre assinatura e new Tray())', () => {
        const createTrayIdx = traySource.indexOf('export function createTray');
        const newTrayIdx = traySource.indexOf('new Tray(', createTrayIdx);
        expect(createTrayIdx).toBeGreaterThan(-1);
        expect(newTrayIdx).toBeGreaterThan(createTrayIdx);
        const block = traySource.slice(createTrayIdx, newTrayIdx);
        expect(block).toContain('iconTemplate.png');
        expect(block).toContain('icon-16x16.png');
      });

      it('D-04: forma da decisao e ternario ou if com darwin no bloco de selecao', () => {
        const createTrayIdx = traySource.indexOf('export function createTray');
        const newTrayIdx = traySource.indexOf('new Tray(', createTrayIdx);
        const block = traySource.slice(createTrayIdx, newTrayIdx);
        // Aceita: ternary `=== 'darwin' ?` OU if `=== 'darwin')`
        expect(block).toMatch(/=== 'darwin'\s*[?)]/);
      });
    });
    ```

    Rodar testes — devem FALHAR (RED) antes da Task 2:
    ```
    cd apps/desktop && npm test -- tray.platform.test.ts
    ```
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npx vitest run src/main/__tests__/tray.platform.test.ts --reporter=verbose 2&gt;&amp;1 | grep -E "Phase 51|MCOS-01|iconTemplate"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 51 (MCOS-01)" apps/desktop/src/main/__tests__/tray.platform.test.ts` retorna 1
    - `grep -c "iconTemplate.png" apps/desktop/src/main/__tests__/tray.platform.test.ts` retorna pelo menos 2 (Test 1 e Test 4)
    - `grep "process.platform === 'darwin'" apps/desktop/src/main/__tests__/tray.platform.test.ts` retorna match
    - Antes da Task 2 ser executada: `npx vitest run src/main/__tests__/tray.platform.test.ts` mostra os 5 novos testes em estado FAILING (RED) — assertivas sobre `iconTemplate.png` e selecao condicional ainda nao existem em tray.ts
  </acceptance_criteria>
  <done>
    5 novos testes adicionados ao describe "Phase 51 (MCOS-01)". Testes falham porque tray.ts ainda nao foi modificado — estado RED esperado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modificar tray.ts para selecao condicional de iconPath por plataforma</name>
  <files>apps/desktop/src/main/tray.ts</files>
  <read_first>
    - apps/desktop/src/main/tray.ts (linhas 52-55 — alvo da modificacao)
    - .planning/phases/51-macos-tray-icon-polish/51-CONTEXT.md (D-04 com snippet exato)
    - .planning/phases/51-macos-tray-icon-polish/51-01-SUMMARY.md (confirma que iconTemplate.png ja existe em resources/tray/)
  </read_first>
  <behavior>
    - GREEN: os 5 testes do Phase 51 (MCOS-01) describe block passam
    - Os testes existentes em tray.test.ts e tray.platform.test.ts continuam passando (zero regressao)
    - Em macOS, iconPath resolve para `.../resources/tray/iconTemplate.png`
    - Em Windows/Linux, iconPath resolve para `.../resources/tray/icon-16x16.png`
  </behavior>
  <action>
    Em `apps/desktop/src/main/tray.ts`, substituir as linhas 52-55:

    ANTES (atual):
    ```ts
    export function createTray(mainWindow: BrowserWindow, voiceModeManager: VoiceModeManager): void {
      // D-05: Use 16x16 icon (Electron auto-selects 32x32 for high-DPI)
      const iconPath = path.join(__dirname, '../../resources/tray/icon-16x16.png');
      tray = new Tray(iconPath);
    ```

    DEPOIS (modificacao exata per D-04):
    ```ts
    export function createTray(mainWindow: BrowserWindow, voiceModeManager: VoiceModeManager): void {
      // D-05: Use 16x16 icon (Electron auto-selects 32x32 for high-DPI)
      // Phase 51 (MCOS-01, D-04): em macOS usa template image (iconTemplate.png)
      // — Electron auto-inverte para preto/branco conforme tema do sistema.
      // Windows/Linux mantem icon-16x16.png (comportamento inalterado).
      const iconFile = process.platform === 'darwin' ? 'iconTemplate.png' : 'icon-16x16.png';
      const iconPath = path.join(__dirname, '../../resources/tray/', iconFile);
      tray = new Tray(iconPath);
    ```

    Nao mexer em mais nada no arquivo. Manter todos os outros comentarios e logica.

    Rodar testes — devem PASSAR (GREEN):
    ```
    cd apps/desktop && npx vitest run src/main/__tests__/tray.platform.test.ts src/main/__tests__/tray.test.ts
    ```
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; npx vitest run src/main/__tests__/tray.platform.test.ts src/main/__tests__/tray.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "iconTemplate.png" apps/desktop/src/main/tray.ts` retorna exatamente 1 match
    - `grep "icon-16x16.png" apps/desktop/src/main/tray.ts` retorna exatamente 1 match
    - `grep "process.platform === 'darwin' ? 'iconTemplate.png' : 'icon-16x16.png'" apps/desktop/src/main/tray.ts` retorna match (ternario exato per D-04)
    - `npx vitest run src/main/__tests__/tray.platform.test.ts` — todos os testes (existentes + 5 novos) passam GREEN
    - `npx vitest run src/main/__tests__/tray.test.ts` — todos os testes existentes passam (zero regressao; teste "uses 16x16 icon path" continua green pois `icon-16x16.png` ainda esta no source)
    - `npm run typecheck` (ou `tsc --noEmit` no apps/desktop) sem erros novos
  </acceptance_criteria>
  <done>
    tray.ts modificado em apenas 2 linhas (1 declaracao removida + 2 declaracoes adicionadas). Todos os testes GREEN. Comportamento observavel: macOS carrega iconTemplate.png, Windows/Linux carregam icon-16x16.png.
  </done>
</task>

</tasks>

<verification>
**Comandos automatizados:**
- `cd apps/desktop && npx vitest run src/main/__tests__/tray.platform.test.ts src/main/__tests__/tray.test.ts` — todos GREEN
- `grep "iconTemplate.png" apps/desktop/src/main/tray.ts` — 1 match
- `grep "icon-16x16.png" apps/desktop/src/main/tray.ts` — 1 match (regressao Windows/Linux preservada)

**Verificacao manual (per D-06, fora do escopo automatizavel):**
- Em maquina macOS: rodar JARVIS em modo claro -> icone aparece preto. Trocar para modo escuro nas System Settings -> icone vira branco instantaneamente sem restart. (Cobertura de Success Criteria 1, 2, 3.)
</verification>

<success_criteria>
1. Em macOS: iconPath resolvido termina em `iconTemplate.png` (Success Criteria 1, 2, 3 cobertos via template image API do macOS)
2. Em Windows/Linux: iconPath resolvido termina em `icon-16x16.png` (Success Criteria 4)
3. Selecao feita inline em `createTray()` via ternario com `process.platform === 'darwin'`
4. Todos os testes Vitest passam (existentes + 5 novos)
5. Zero codigo de runtime extra (sem listener de tema, sem reload de Tray) — macOS faz a inversao nativamente
</success_criteria>

<output>
After completion, create `.planning/phases/51-macos-tray-icon-polish/51-02-SUMMARY.md`
</output>
