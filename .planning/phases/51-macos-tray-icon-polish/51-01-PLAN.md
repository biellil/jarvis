---
phase: 51-macos-tray-icon-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/scripts/generate-tray-template.mjs
  - apps/desktop/resources/tray/iconTemplate.png
  - apps/desktop/resources/tray/iconTemplate@2x.png
  - apps/desktop/package.json
autonomous: true
requirements:
  - MCOS-01
must_haves:
  truths:
    - "Arquivo iconTemplate.png (16x16) existe em apps/desktop/resources/tray/"
    - "Arquivo iconTemplate@2x.png (32x32) existe em apps/desktop/resources/tray/"
    - "PNGs gerados sao black+alpha (RGB forcado para #000000, canal alpha preservado da silhueta original)"
    - "Script generate-tray-template.mjs e reproduzivel via 'node apps/desktop/scripts/generate-tray-template.mjs'"
  artifacts:
    - path: "apps/desktop/scripts/generate-tray-template.mjs"
      provides: "Script Node que gera os PNGs template a partir de icon-16x16.png e icon-32x32.png"
    - path: "apps/desktop/resources/tray/iconTemplate.png"
      provides: "Template image 16x16 black+alpha para tray macOS"
    - path: "apps/desktop/resources/tray/iconTemplate@2x.png"
      provides: "Template image 32x32 retina black+alpha para tray macOS"
  key_links:
    - from: "apps/desktop/scripts/generate-tray-template.mjs"
      to: "apps/desktop/resources/tray/icon-16x16.png"
      via: "sharp().composite() ou pixel manipulation lendo o PNG fonte"
      pattern: "icon-16x16\\.png"
    - from: "apps/desktop/scripts/generate-tray-template.mjs"
      to: "apps/desktop/resources/tray/iconTemplate.png"
      via: "sharp().toFile() escrevendo o output"
      pattern: "iconTemplate\\.png"
---

<objective>
Gerar os assets template PNG (`iconTemplate.png` 16x16 + `iconTemplate@2x.png` 32x32) a partir dos icones existentes da tray, em formato black+alpha, via script Node versionado e reproduzivel.

Purpose: macOS exige template images (RGB preto + canal alpha) com naming convention `*Template.png` para que o Tray adapte automaticamente o icone ao modo claro/escuro do sistema (per MCOS-01, D-01, D-02, D-03).
Output: 2 PNGs commitados em `apps/desktop/resources/tray/` + 1 script de geracao em `apps/desktop/scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-macos-tray-icon-polish/51-CONTEXT.md
@apps/desktop/resources/tray/icon-16x16.png
@apps/desktop/resources/tray/icon-32x32.png
@apps/desktop/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar script generate-tray-template.mjs e gerar os PNGs template</name>
  <files>apps/desktop/scripts/generate-tray-template.mjs, apps/desktop/resources/tray/iconTemplate.png, apps/desktop/resources/tray/iconTemplate@2x.png, apps/desktop/package.json</files>
  <read_first>
    - apps/desktop/resources/tray/icon-16x16.png (asset fonte base)
    - apps/desktop/resources/tray/icon-32x32.png (asset fonte retina)
    - apps/desktop/package.json (para adicionar sharp como devDependency e script npm)
    - apps/desktop/scripts/poc-wakeword-file-fetch.mjs (referencia de estilo de script Node existente no projeto)
    - .planning/phases/51-macos-tray-icon-polish/51-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    1. Instalar `sharp` como devDependency em `apps/desktop/`:
       ```
       cd apps/desktop && npm install --save-dev sharp
       ```
       (Per D-03 — Claude pode trocar por `jimp` apenas se `sharp` falhar no install; preferir sharp.)

    2. Adicionar npm script em `apps/desktop/package.json` na secao `scripts`:
       ```json
       "generate:tray-template": "node scripts/generate-tray-template.mjs"
       ```

    3. Criar `apps/desktop/scripts/generate-tray-template.mjs` com este conteudo (per D-03):
       ```js
       #!/usr/bin/env node
       /**
        * generate-tray-template.mjs — Phase 51 (MCOS-01, D-03)
        *
        * Gera apps/desktop/resources/tray/iconTemplate.png (16x16) e
        * iconTemplate@2x.png (32x32) a partir de icon-16x16.png e icon-32x32.png.
        *
        * Estrategia: preserva canal alpha do PNG fonte; forca RGB para #000000.
        * macOS auto-inverte para branco no dark mode via template image API.
        *
        * Uso: node apps/desktop/scripts/generate-tray-template.mjs
        */
       import sharp from 'sharp';
       import path from 'node:path';
       import { fileURLToPath } from 'node:url';

       const __dirname = path.dirname(fileURLToPath(import.meta.url));
       const TRAY_DIR = path.resolve(__dirname, '../resources/tray');

       const PAIRS = [
         { src: 'icon-16x16.png', dst: 'iconTemplate.png' },
         { src: 'icon-32x32.png', dst: 'iconTemplate@2x.png' },
       ];

       async function blackenPreservingAlpha(srcPath, dstPath) {
         const img = sharp(srcPath).ensureAlpha();
         const { data, info } = await img.raw().toBuffer({ resolveWithObject: true });
         // info.channels === 4 (RGBA). Forca R=G=B=0; preserva A.
         for (let i = 0; i < data.length; i += 4) {
           data[i] = 0;
           data[i + 1] = 0;
           data[i + 2] = 0;
           // data[i + 3] (alpha) inalterado
         }
         await sharp(data, {
           raw: { width: info.width, height: info.height, channels: 4 },
         })
           .png()
           .toFile(dstPath);
         console.log(`[generate-tray-template] ${path.basename(srcPath)} -> ${path.basename(dstPath)} (${info.width}x${info.height})`);
       }

       for (const { src, dst } of PAIRS) {
         await blackenPreservingAlpha(path.join(TRAY_DIR, src), path.join(TRAY_DIR, dst));
       }
       console.log('[generate-tray-template] done.');
       ```

    4. Executar o script para gerar os assets:
       ```
       cd apps/desktop && node scripts/generate-tray-template.mjs
       ```

    5. Verificar visualmente que os PNGs foram criados (ls + tamanho > 0). NAO commitar `node_modules`.

    Decisoes implementadas:
    - D-01: silhueta reutilizada (script le icon-16x16.png e icon-32x32.png existentes)
    - D-02: PNGs ficam em `resources/tray/` flat, sem subpasta `macos/`
    - D-03: script versionado em `apps/desktop/scripts/`, on-demand (nao prebuild), PNGs commitados
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; test -f resources/tray/iconTemplate.png &amp;&amp; test -f resources/tray/iconTemplate@2x.png &amp;&amp; test -f scripts/generate-tray-template.mjs &amp;&amp; node -e "const s=require('fs').statSync('resources/tray/iconTemplate.png'); if(s.size&lt;100) process.exit(1); const s2=require('fs').statSync('resources/tray/iconTemplate@2x.png'); if(s2.size&lt;100) process.exit(1); console.log('OK', s.size, s2.size);"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/resources/tray/iconTemplate.png` existe e tem tamanho > 100 bytes
    - `apps/desktop/resources/tray/iconTemplate@2x.png` existe e tem tamanho > 100 bytes
    - `apps/desktop/scripts/generate-tray-template.mjs` existe
    - `grep -E "iconTemplate\.png" apps/desktop/scripts/generate-tray-template.mjs` retorna match
    - `grep -E "iconTemplate@2x\.png" apps/desktop/scripts/generate-tray-template.mjs` retorna match
    - `grep "\"sharp\"" apps/desktop/package.json` retorna match em devDependencies
    - `grep "generate:tray-template" apps/desktop/package.json` retorna match em scripts
    - Re-executar `node apps/desktop/scripts/generate-tray-template.mjs` produz os mesmos arquivos sem erro (idempotente)
  </acceptance_criteria>
  <done>
    PNGs template gerados, commitados e reproduziveis via script. Pronto para serem referenciados por `tray.ts` no Plan 02.
  </done>
</task>

</tasks>

<verification>
- Arquivos existem: `iconTemplate.png`, `iconTemplate@2x.png`, `generate-tray-template.mjs`
- Script reproduz os PNGs sem erro
- `sharp` listada em devDependencies de `apps/desktop/package.json`
</verification>

<success_criteria>
1. Os 2 PNGs template existem em `apps/desktop/resources/tray/`
2. Script `generate-tray-template.mjs` esta versionado e funciona
3. `sharp` adicionada como devDependency
4. Nenhum codigo de runtime (tray.ts) modificado neste plano (isolamento — Plan 02 faz a wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/51-macos-tray-icon-polish/51-01-SUMMARY.md`
</output>
