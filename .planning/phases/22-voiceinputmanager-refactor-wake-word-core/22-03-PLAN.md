---
phase: 22-voiceinputmanager-refactor-wake-word-core
plan: 03
type: execute
wave: 2
depends_on: [22-01]
autonomous: true
requirements: [WAKE-09]
files_modified:
  - apps/desktop/resources/wakeword-models/.gitkeep
  - apps/desktop/resources/wakeword-models/LICENSE-models.txt
  - scripts/download-wakeword-models.sh
  - scripts/download-wakeword-models.mjs
  - .gitignore
  - apps/desktop/.env.example
  - .env.example
  - apps/desktop/package.json
  - apps/desktop/vitest.config.ts
  - .github/workflows/ci-lockfile-ban.yml
tags: [assets, download, licensing, env-vars, ci, wake-word]

must_haves:
  truths:
    - "Modelos ONNX NÃO são commitados no git — .gitignore bloqueia *.onnx em apps/desktop/resources/wakeword-models/"
    - "Script idempotente baixa os 4 modelos de URLs canônicas do HuggingFace com checksum verification antes de sobrescrever"
    - "Rodando 'pnpm install' aciona postinstall que executa o download script — primeiro setup automático"
    - "Env vars VITE_WAKE_WORD_ENABLED, VITE_WAKE_WORD_THRESHOLD, VITE_WAKE_WORD_VAD_TIMEOUT_MS, VITE_WAKE_WORD_MODEL aparecem em .env.example com defaults travados do CONTEXT.md"
    - "CI workflow (ou script equivalente) grep-bane porcupine/picovoice/bumblebee-hotword no pnpm-lock.yaml"
    - "LICENSE-models.txt documenta CC BY-NC-SA 4.0 dos modelos e a justificativa (assistente pessoal por PROJECT.md)"
  artifacts:
    - path: scripts/download-wakeword-models.mjs
      provides: "Node script idempotente que baixa 4 .onnx files, verifica SHA-256, e reporta skip/success/fail por arquivo"
      exports: []
      contains: "SHA-256"
    - path: apps/desktop/resources/wakeword-models/.gitkeep
      provides: "Placeholder para manter o diretório no git sem commitar binários"
    - path: apps/desktop/resources/wakeword-models/LICENSE-models.txt
      provides: "Licensing notice dos modelos openwakeword (CC BY-NC-SA 4.0) + rationale de conformidade"
      contains: "CC BY-NC-SA 4.0"
    - path: .gitignore
      provides: "Regra que ignora *.onnx em resources/wakeword-models/ (modelos nunca commitados)"
      contains: "wakeword-models/*.onnx"
    - path: apps/desktop/.env.example
      provides: "Defaults das env vars VITE_WAKE_WORD_* travadas no CONTEXT.md"
      contains: "VITE_WAKE_WORD_ENABLED=true"
  key_links:
    - from: "apps/desktop/package.json scripts.postinstall"
      to: "scripts/download-wakeword-models.mjs"
      via: "node script run automaticamente após install"
      pattern: "postinstall.*download-wakeword-models"
    - from: "scripts/download-wakeword-models.mjs"
      to: "apps/desktop/resources/wakeword-models/*.onnx"
      via: "https download + fs.writeFile + crypto.createHash('sha256')"
      pattern: "crypto\\.createHash"
---

<objective>
Preparar toda a infraestrutura de assets e configuração do wake word SEM tocar em código de runtime. Especificamente: criar o diretório `apps/desktop/resources/wakeword-models/` com `.gitkeep` + `LICENSE-models.txt`, implementar o script idempotente de download dos 4 modelos ONNX (com checksum verification), wirar no `postinstall` do desktop package, adicionar as env vars `VITE_WAKE_WORD_*` no `.env.example`, e colocar o CI grep-ban contra porcupine/picovoice/bumblebee-hotword.

Purpose: Modelos `.onnx` totalizam ~4MB e têm licença CC BY-NC-SA 4.0 — NÃO podem ser commitados no git (privacy + licensing + repo bloat). Esse plan roda em paralelo com Plan 02 (Wave 2) porque não toca em nenhum arquivo que Plan 02 toca: este plan mexe em `apps/desktop/resources/`, `scripts/`, `.env.example`, `.gitignore`, `.github/`, `vitest.config.ts` (global setup) e `apps/desktop/package.json` (apenas no campo `scripts`, não em `dependencies` — Plan 02 mexe em `dependencies`). Após os dois plans terminarem, Plan 04 pode integrar tudo.

Output:
- Dir `apps/desktop/resources/wakeword-models/` com `.gitkeep` e `LICENSE-models.txt`
- `scripts/download-wakeword-models.mjs` (Node, cross-platform — preferido sobre `.sh` para suporte a Windows dev box)
- `scripts/download-wakeword-models.sh` como thin wrapper opcional (`node scripts/download-wakeword-models.mjs`)
- `.gitignore` atualizado
- `apps/desktop/.env.example` e/ou `.env.example` raiz com defaults
- `apps/desktop/package.json` com `"postinstall": "node ../../scripts/download-wakeword-models.mjs"` (ou raiz do monorepo)
- `.github/workflows/ci-lockfile-ban.yml` (ou um script equivalente em `scripts/` + job no CI existente)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md
@apps/desktop/package.json
@.gitignore
@CLAUDE.md

<interfaces>
<!-- Defaults travados do CONTEXT.md para as env vars VITE_WAKE_WORD_* — executor deve usar EXATAMENTE esses valores. -->

# apps/desktop/.env.example (novo) — padrões do CONTEXT.md
VITE_WAKE_WORD_ENABLED=true             # Decisão CONTEXT.md: ligado por default
VITE_WAKE_WORD_THRESHOLD=0.5            # Decisão CONTEXT.md: 0.5 (sensível)
VITE_WAKE_WORD_VAD_TIMEOUT_MS=3000      # Decisão CONTEXT.md: 3s (mínimo de 3-5s de WAKE-06)
VITE_WAKE_WORD_MODEL=hey_jarvis_v0.1    # Modelo atual (configurável para trocar no futuro)

# Modelos openwakeword — baixados em apps/desktop/resources/wakeword-models/
# URLs canônicas (validar com curl -I na primeira execução do script):
#   https://huggingface.co/davidscripka/openwakeword/resolve/main/melspectrogram.onnx
#   https://huggingface.co/davidscripka/openwakeword/resolve/main/embedding_model.onnx
#   https://huggingface.co/davidscripka/openwakeword/resolve/main/silero_vad.onnx
#   https://huggingface.co/davidscripka/openwakeword/resolve/main/hey_jarvis_v0.1.onnx

# Fallback: GitHub releases de dscripka/openWakeWord
# se o HuggingFace retornar 404, script deve tentar:
#   https://github.com/dscripka/openWakeWord/releases/download/v0.5.1/{filename}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar resources/wakeword-models/ dir + .gitignore rule + LICENSE doc + env.example defaults</name>
  <read_first>
    - apps/desktop/package.json (ver estrutura de scripts)
    - .gitignore (raiz — verificar formato atual)
    - .env.example (raiz — se existir, ver convenções)
    - apps/desktop/.env.example (se existir — pode não existir ainda)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md §"Environment Variables (new)" — usar os 4 valores TRAVADOS (VITE_WAKE_WORD_ENABLED=true, THRESHOLD=0.5, VAD_TIMEOUT_MS=3000, MODEL=hey_jarvis_v0.1)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Assets" (licenças e URLs)
  </read_first>
  <action>
    1. Criar diretório `apps/desktop/resources/wakeword-models/` (se não existir).

    2. Criar `apps/desktop/resources/wakeword-models/.gitkeep` (arquivo vazio — só pra o dir existir no git).

    3. Criar `apps/desktop/resources/wakeword-models/LICENSE-models.txt` com o seguinte conteúdo literal:
    ```
    Wake Word Models — Licensing & Attribution
    ===========================================

    Os 4 modelos ONNX neste diretório NÃO são parte do código-fonte deste
    repositório. Eles são baixados sob demanda via
    scripts/download-wakeword-models.mjs a partir de:

      https://huggingface.co/davidscripka/openwakeword

    Arquivos:
      - melspectrogram.onnx
      - embedding_model.onnx
      - silero_vad.onnx
      - hey_jarvis_v0.1.onnx

    Licenças:
      - openwakeword CÓDIGO:    Apache License 2.0
      - openwakeword MODELOS:   Creative Commons BY-NC-SA 4.0
      - Silero VAD:             MIT (bundled via openwakeword releases)

    Aplicabilidade no JARVIS
    ------------------------

    JARVIS é declarado explicitamente em PROJECT.md como "assistente pessoal
    inteligente para uso próprio" — uso não comercial, pessoal, sem
    distribuição comercial. A licença CC BY-NC-SA 4.0 é COMPATÍVEL com esse
    uso.

    Caso o projeto mude para uso comercial no futuro, reavaliar:
      (a) treinar modelo custom "jarvis" standalone (planejado para v1.5+),
          removendo dependência dos modelos CC BY-NC-SA, OU
      (b) licenciar o modelo diretamente com os autores.

    Attribution
    -----------

    openwakeword: https://github.com/dscripka/openWakeWord
    davidscripka (David Scripka) — modelos pré-treinados
    Silero Team — silero_vad.onnx
    ```

    4. Atualizar `.gitignore` (raiz do monorepo): adicionar seção (se ainda não existir):
    ```
    # Wake word models — baixados via scripts/download-wakeword-models.mjs
    # (licença CC BY-NC-SA 4.0, não podem ser commitados)
    apps/desktop/resources/wakeword-models/*.onnx
    !apps/desktop/resources/wakeword-models/.gitkeep
    !apps/desktop/resources/wakeword-models/LICENSE-models.txt
    ```

    5. Criar/atualizar `apps/desktop/.env.example` adicionando as 4 variáveis VITE_WAKE_WORD_*. Se o arquivo não existir ainda em apps/desktop/, criar com essas 4 linhas no topo:
    ```
    # ============================================
    # Wake Word (Phase 22 — v1.4)
    # ============================================
    # Prefixo VITE_ é obrigatório — engine roda no renderer e consome via
    # import.meta.env. Defaults travados em .planning/phases/22-.../22-CONTEXT.md
    VITE_WAKE_WORD_ENABLED=true
    VITE_WAKE_WORD_THRESHOLD=0.5
    VITE_WAKE_WORD_VAD_TIMEOUT_MS=3000
    VITE_WAKE_WORD_MODEL=hey_jarvis_v0.1
    ```
    Se o arquivo já existir, inserir no fim (após as vars existentes).

    6. Atualizar `.env.example` na raiz (se existir) com as mesmas variáveis — ou, se o projeto só tiver `.env` na raiz (sem `.env.example` raiz), criar apenas o de `apps/desktop/`. **Investigar**: se há `electron.vite.config.ts` fazendo `envDir` apontando para raiz do monorepo, as VITE_* vars vão ser lidas de lá. Verificar antes — se for esse caso, colocar as vars no `.env.example` raiz.

    Commit: `📝 chore(22-03): dir resources/wakeword-models (.gitkeep + LICENSE) + .gitignore + .env.example com defaults WAKE travados`
  </action>
  <verify>
    <automated>ls apps/desktop/resources/wakeword-models/; grep "wakeword-models" .gitignore; grep "VITE_WAKE_WORD_ENABLED=true" apps/desktop/.env.example .env.example 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test -d apps/desktop/resources/wakeword-models && echo OK` retorna OK
    - `test -f apps/desktop/resources/wakeword-models/.gitkeep && echo OK` retorna OK
    - `test -f apps/desktop/resources/wakeword-models/LICENSE-models.txt && echo OK` retorna OK
    - `grep "CC BY-NC-SA 4.0" apps/desktop/resources/wakeword-models/LICENSE-models.txt` retorna pelo menos 1 linha
    - `grep "wakeword-models/\*.onnx" .gitignore` retorna 1 linha
    - `grep "VITE_WAKE_WORD_ENABLED=true" apps/desktop/.env.example` retorna 1 linha (ou o equivalente em .env.example raiz)
    - `grep "VITE_WAKE_WORD_THRESHOLD=0.5" apps/desktop/.env.example` retorna 1 linha
    - `grep "VITE_WAKE_WORD_VAD_TIMEOUT_MS=3000" apps/desktop/.env.example` retorna 1 linha
    - `grep "VITE_WAKE_WORD_MODEL=hey_jarvis_v0.1" apps/desktop/.env.example` retorna 1 linha
    - `git status --porcelain apps/desktop/resources/wakeword-models/*.onnx 2>/dev/null` retorna vazio (nenhum .onnx commitado)
  </acceptance_criteria>
  <done>Dir de modelos existe com .gitkeep + LICENSE, gitignore bloqueia .onnx, env.example tem os 4 defaults travados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar download-wakeword-models.mjs (idempotente, com checksum) + wire postinstall + smoke test do script</name>
  <read_first>
    - apps/desktop/package.json (para ver estrutura do scripts field atual)
    - package.json (raiz do monorepo — se existir, ver se postinstall global)
    - pnpm-workspace.yaml (confirmar monorepo layout)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Assets" URLs canônicas + §"Environment Availability"
  </read_first>
  <behavior>
    O script deve:
    1. Ler `EXPECTED_MODELS` — array de 4 objetos `{ name, url, fallbackUrl, sha256?, sizeApprox }`.
    2. Para cada modelo:
       - Verificar se `apps/desktop/resources/wakeword-models/<name>` já existe.
       - Se existe E tem checksum registrado: recomputar SHA-256 e comparar. Se bate → SKIP (idempotente). Se não bate → LOG warning + redownload.
       - Se existe mas não tem checksum esperado conhecido: SKIP (confia no disk — primeira rodada apenas baixa e registra).
       - Se não existe: download via https → buffer → compute sha256 → write para arquivo destino. Se download falhar, tentar `fallbackUrl`.
    3. Log estruturado: `[wakeword-models] mel: SKIP (checksum OK)` / `[wakeword-models] hey_jarvis_v0.1: DOWNLOADED (82 KB, sha256=abc...)` / `[wakeword-models] vad: FAIL (HTTP 404 on primary + fallback)`.
    4. Exit code 0 se todos 4 OK (download ou skip). Exit 1 se qualquer falhar — MAS com flag `--soft` (default para postinstall) exit 0 apenas com warning, porque NÃO queremos quebrar `pnpm install` offline.
    5. Flag `--force` força redownload mesmo se já existirem.
    6. Flag `--check` só verifica presença + checksum, não baixa.
    7. Flag `--verbose` imprime URLs sendo tentadas.
  </behavior>
  <action>
    1. Criar `scripts/download-wakeword-models.mjs` (Node nativo, sem deps externas — usar `node:https`, `node:fs`, `node:crypto`, `node:path`, `node:url`):

    ```js
    #!/usr/bin/env node
    /**
     * Download wake word models idempotente + checksum verification.
     *
     * Usage:
     *   node scripts/download-wakeword-models.mjs              # soft mode (postinstall)
     *   node scripts/download-wakeword-models.mjs --force      # force redownload
     *   node scripts/download-wakeword-models.mjs --check      # check-only (CI)
     *   node scripts/download-wakeword-models.mjs --verbose
     *
     * Source: Phase 22 Plan 03 — modelos NÃO commitados (CC BY-NC-SA 4.0 + privacy).
     */
    import fs from 'node:fs';
    import path from 'node:path';
    import https from 'node:https';
    import crypto from 'node:crypto';
    import { fileURLToPath } from 'node:url';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    const TARGET_DIR = path.resolve(__dirname, '../apps/desktop/resources/wakeword-models');

    // Checksums LEFT EMPTY na primeira execução — o script vai registrar no
    // stdout e o maintainer deve colar os valores aqui no primeiro PR. Após
    // isso, todo download futuro é verificado.
    const EXPECTED_MODELS = [
      {
        name: 'melspectrogram.onnx',
        url: 'https://huggingface.co/davidscripka/openwakeword/resolve/main/melspectrogram.onnx',
        fallbackUrl: 'https://github.com/dscripka/openWakeWord/releases/download/v0.5.1/melspectrogram.onnx',
        sha256: null,  // TODO: colar após primeira execução
      },
      {
        name: 'embedding_model.onnx',
        url: 'https://huggingface.co/davidscripka/openwakeword/resolve/main/embedding_model.onnx',
        fallbackUrl: 'https://github.com/dscripka/openWakeWord/releases/download/v0.5.1/embedding_model.onnx',
        sha256: null,
      },
      {
        name: 'silero_vad.onnx',
        url: 'https://huggingface.co/davidscripka/openwakeword/resolve/main/silero_vad.onnx',
        fallbackUrl: 'https://github.com/dscripka/openWakeWord/releases/download/v0.5.1/silero_vad.onnx',
        sha256: null,
      },
      {
        name: 'hey_jarvis_v0.1.onnx',
        url: 'https://huggingface.co/davidscripka/openwakeword/resolve/main/hey_jarvis_v0.1.onnx',
        fallbackUrl: 'https://github.com/dscripka/openWakeWord/releases/download/v0.5.1/hey_jarvis_v0.1.onnx',
        sha256: null,
      },
    ];

    const args = new Set(process.argv.slice(2));
    const FORCE = args.has('--force');
    const CHECK_ONLY = args.has('--check');
    const VERBOSE = args.has('--verbose');
    const SOFT = !CHECK_ONLY; // default soft mode

    function log(msg) { console.log(`[wakeword-models] ${msg}`); }
    function verbose(msg) { if (VERBOSE) log(msg); }

    function sha256OfBuffer(buf) {
      return crypto.createHash('sha256').update(buf).digest('hex');
    }

    function downloadToBuffer(url, redirectsLeft = 5) {
      return new Promise((resolve, reject) => {
        verbose(`GET ${url}`);
        https.get(url, (res) => {
          if (res.statusCode >= 300 && res.statusCode < 400 && res.headers.location && redirectsLeft > 0) {
            return resolve(downloadToBuffer(res.headers.location, redirectsLeft - 1));
          }
          if (res.statusCode !== 200) {
            return reject(new Error(`HTTP ${res.statusCode} on ${url}`));
          }
          const chunks = [];
          res.on('data', (c) => chunks.push(c));
          res.on('end', () => resolve(Buffer.concat(chunks)));
          res.on('error', reject);
        }).on('error', reject);
      });
    }

    async function downloadModel(model) {
      try {
        return await downloadToBuffer(model.url);
      } catch (primaryErr) {
        log(`${model.name}: primary URL failed (${primaryErr.message}), trying fallback`);
        return await downloadToBuffer(model.fallbackUrl);
      }
    }

    async function processModel(model) {
      const target = path.join(TARGET_DIR, model.name);
      const exists = fs.existsSync(target);

      if (exists && !FORCE) {
        if (model.sha256) {
          const actual = sha256OfBuffer(fs.readFileSync(target));
          if (actual === model.sha256) {
            log(`${model.name}: SKIP (checksum OK)`);
            return { status: 'skip' };
          } else {
            log(`${model.name}: WARN checksum mismatch (expected ${model.sha256}, got ${actual}) — redownloading`);
          }
        } else {
          log(`${model.name}: SKIP (exists, no expected checksum yet)`);
          return { status: 'skip-no-checksum' };
        }
      }

      if (CHECK_ONLY) {
        if (!exists) {
          log(`${model.name}: MISSING (check-only mode)`);
          return { status: 'missing' };
        }
        return { status: 'present' };
      }

      const buf = await downloadModel(model);
      const actual = sha256OfBuffer(buf);
      if (model.sha256 && actual !== model.sha256) {
        throw new Error(`${model.name}: downloaded sha256 ${actual} does not match expected ${model.sha256}`);
      }
      fs.mkdirSync(TARGET_DIR, { recursive: true });
      fs.writeFileSync(target, buf);
      log(`${model.name}: DOWNLOADED (${buf.length} bytes, sha256=${actual})`);
      if (!model.sha256) {
        log(`${model.name}: (register this checksum in EXPECTED_MODELS: '${actual}')`);
      }
      return { status: 'downloaded', sha256: actual };
    }

    async function main() {
      fs.mkdirSync(TARGET_DIR, { recursive: true });
      const results = [];
      let anyFailed = false;
      for (const model of EXPECTED_MODELS) {
        try {
          const r = await processModel(model);
          results.push({ name: model.name, ...r });
        } catch (err) {
          anyFailed = true;
          log(`${model.name}: FAIL ${err.message}`);
          results.push({ name: model.name, status: 'fail', error: err.message });
        }
      }
      if (anyFailed) {
        if (SOFT) {
          log('⚠ soft mode: exiting 0 despite failures — app may fall back to PTT-only');
          process.exit(0);
        } else {
          process.exit(1);
        }
      }
      log('all models OK');
    }

    main().catch((err) => {
      log(`fatal: ${err.message}`);
      process.exit(SOFT ? 0 : 1);
    });
    ```

    2. Criar `scripts/download-wakeword-models.sh` como thin wrapper:
    ```bash
    #!/usr/bin/env bash
    # Thin wrapper — delega para o script Node cross-platform.
    set -euo pipefail
    exec node "$(dirname "$0")/download-wakeword-models.mjs" "$@"
    ```
    Tornar executável.

    3. Atualizar `apps/desktop/package.json` — adicionar `"postinstall": "node ../../scripts/download-wakeword-models.mjs"` ao campo `scripts`. **Atenção**: o postinstall roda DEPOIS do `pnpm add` do Plan 02, então ordem não importa.

    4. **Smoke test do script** (NÃO é vitest — é execução direta):
       - Rodar `node scripts/download-wakeword-models.mjs --check`. Exit 0 esperado com 4 linhas MISSING (os modelos ainda não foram baixados).
       - Rodar `node scripts/download-wakeword-models.mjs` (soft mode, sem flags). Se online, deve baixar os 4 e reportar DOWNLOADED. Se offline, deve reportar FAIL + exit 0 (soft mode).
       - Após o download bem-sucedido, rodar de novo `node scripts/download-wakeword-models.mjs --check` — deve reportar 4 PRESENT (ou pelo menos 4 skip/present, não MISSING).
       - Verificar que os 4 arquivos existem em `apps/desktop/resources/wakeword-models/` com tamanhos não-zero.
       - Copiar os SHA-256 impressos no stdout e registrar no `EXPECTED_MODELS` array no próprio script (cola inline). Commitar o arquivo com os checksums preenchidos.

    5. Adicionar o dir do node_modules ao `.gitignore` apenas se necessário (provavelmente já está).

    Commit: `✨ feat(22-03): script idempotente de download dos 4 modelos wake word com checksum + postinstall hook`
  </action>
  <verify>
    <automated>node scripts/download-wakeword-models.mjs --check 2>&1 | tail -10; ls -la apps/desktop/resources/wakeword-models/*.onnx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/download-wakeword-models.mjs && echo OK` retorna OK
    - `grep "crypto.createHash('sha256')" scripts/download-wakeword-models.mjs` retorna pelo menos 1 linha
    - `grep "huggingface.co/davidscripka/openwakeword" scripts/download-wakeword-models.mjs` retorna pelo menos 4 linhas (uma por modelo)
    - `grep "github.com/dscripka/openWakeWord/releases" scripts/download-wakeword-models.mjs` retorna pelo menos 4 linhas (fallbacks)
    - `grep "postinstall" apps/desktop/package.json` retorna 1 linha contendo `download-wakeword-models`
    - `node scripts/download-wakeword-models.mjs --check` exit 0 (mesmo que imprima MISSING)
    - Se online: após rodar sem flags, `ls apps/desktop/resources/wakeword-models/*.onnx | wc -l` retorna `4`
    - Se online: cada `.onnx` tem tamanho > 10 bytes (`find apps/desktop/resources/wakeword-models -name "*.onnx" -size +10c | wc -l` = 4)
    - `git status --porcelain apps/desktop/resources/wakeword-models/*.onnx 2>/dev/null` retorna vazio (gitignored)
    - `grep -c "sha256: '[a-f0-9]\{64\}'" scripts/download-wakeword-models.mjs` retorna `4` APÓS a primeira execução bem-sucedida (checksums preenchidos — OU, se ambiente offline, pode ficar como `null` e registrar no SUMMARY)
  </acceptance_criteria>
  <done>Script de download cross-platform implementado e testado (ao menos em modo --check). Postinstall wireado. Modelos baixados (se online) com checksums registrados.</done>
</task>

<task type="auto">
  <name>Task 3: CI grep-ban (porcupine/picovoice/bumblebee) e script de validação local</name>
  <read_first>
    - .github/workflows/ (listar workflows existentes para entender o padrão do projeto)
    - apps/desktop/package.json (ver scripts existentes)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Phase Requirements → Test Map" linha WAKE-09 CI grep ban
    - CLAUDE.md §Git commit format
    - pnpm-lock.yaml (confirmar localização — raiz do monorepo)
  </read_first>
  <action>
    1. Criar `scripts/verify-no-banned-wake-word-libs.mjs`:
    ```js
    #!/usr/bin/env node
    /**
     * Ban a libs proibidas de wake word do lockfile (WAKE-09).
     * Phase 22 decision: apenas onnxruntime-web + openwakeword ONNX models.
     */
    import fs from 'node:fs';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));
    const LOCKFILE = path.resolve(__dirname, '../pnpm-lock.yaml');

    const BANNED = [
      '@picovoice/porcupine',
      'porcupine-node',
      'bumblebee-hotword',
      'snowboy',
      'vosk',
    ];

    if (!fs.existsSync(LOCKFILE)) {
      console.error(`[ban-check] pnpm-lock.yaml not found at ${LOCKFILE}`);
      process.exit(1);
    }

    const content = fs.readFileSync(LOCKFILE, 'utf8');
    const hits = [];
    for (const lib of BANNED) {
      const regex = new RegExp(lib.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'i');
      if (regex.test(content)) {
        hits.push(lib);
      }
    }

    if (hits.length > 0) {
      console.error(`[ban-check] ❌ FAILED — banned libs in pnpm-lock.yaml: ${hits.join(', ')}`);
      console.error('[ban-check] These libraries are prohibited by Phase 22 (privacy / licensing).');
      console.error('[ban-check] See .planning/phases/22-.../22-CONTEXT.md §Locked by Research.');
      process.exit(1);
    }

    console.log('[ban-check] ✅ no banned wake word libs in pnpm-lock.yaml');
    process.exit(0);
    ```

    2. Adicionar script ao `apps/desktop/package.json` scripts field:
    `"verify:wake-word-bans": "node ../../scripts/verify-no-banned-wake-word-libs.mjs"`

    3. Criar `.github/workflows/ci-lockfile-ban.yml`:
    ```yaml
    name: CI — Lockfile Bans
    on:
      pull_request:
        paths:
          - 'pnpm-lock.yaml'
          - 'apps/desktop/package.json'
          - 'scripts/verify-no-banned-wake-word-libs.mjs'
      push:
        branches: [main, master]
    jobs:
      wake-word-ban-check:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '22'
          - name: Verify no banned wake word libs
            run: node scripts/verify-no-banned-wake-word-libs.mjs
    ```

    4. Se `.github/workflows/` já tiver outros workflows, inspecionar um (para confirmar a versão de `actions/checkout` e `setup-node` usados) e alinhar com o padrão existente.

    5. Rodar `node scripts/verify-no-banned-wake-word-libs.mjs` localmente — deve retornar exit 0 e log de sucesso. (Porque Plan 02 só adicionou onnxruntime-web, nenhum dos banned libs.)

    6. Rodar `pnpm --filter @jarvis/desktop run verify:wake-word-bans` — deve funcionar como script de workspace.

    Commit: `🤖 ci(22-03): grep-ban de porcupine/picovoice/bumblebee-hotword no pnpm-lock.yaml`
  </action>
  <verify>
    <automated>node scripts/verify-no-banned-wake-word-libs.mjs; test -f .github/workflows/ci-lockfile-ban.yml && echo WORKFLOW_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/verify-no-banned-wake-word-libs.mjs && echo OK` retorna OK
    - `grep "porcupine" scripts/verify-no-banned-wake-word-libs.mjs` retorna pelo menos 1 linha
    - `grep "bumblebee-hotword" scripts/verify-no-banned-wake-word-libs.mjs` retorna pelo menos 1 linha
    - `grep "snowboy" scripts/verify-no-banned-wake-word-libs.mjs` retorna pelo menos 1 linha
    - `test -f .github/workflows/ci-lockfile-ban.yml && echo OK` retorna OK
    - `grep "verify-no-banned-wake-word-libs" .github/workflows/ci-lockfile-ban.yml` retorna pelo menos 1 linha
    - `node scripts/verify-no-banned-wake-word-libs.mjs` exit code 0
    - `grep "verify:wake-word-bans" apps/desktop/package.json` retorna 1 linha
    - `pnpm --filter @jarvis/desktop run verify:wake-word-bans` exit 0
  </acceptance_criteria>
  <done>CI workflow + script local prontos. Rodam verdes contra o lockfile atual. Qualquer PR futuro que introduza porcupine/picovoice/bumblebee/snowboy/vosk será bloqueado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 03)

| Boundary | Description |
|----------|-------------|
| external → file system | `scripts/download-wakeword-models.mjs` baixa via HTTPS do HuggingFace/GitHub e escreve em `apps/desktop/resources/wakeword-models/` |
| CI → lockfile | workflow valida `pnpm-lock.yaml` em cada PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | Download de modelo `.onnx` de URL comprometida (MITM, typosquatting) | mitigate | SHA-256 checksum verification obrigatório após primeira execução registrar os hashes inline. URLs HTTPS (TLS native). Fallback URL também HTTPS e do mesmo autor. Se checksum quebra, script imprime warning + redownload (não silencia). |
| T-22-03-02 | Supply Chain (Tampering) | Dev adiciona por engano `@picovoice/porcupine-node` ou `bumblebee-hotword` ao package.json | mitigate | CI workflow grep-bane o lockfile. Script local disponível para pre-commit manual. WAKE-09 + CONTEXT.md Locked Decisions enforcement mecânico. |
| T-22-03-03 | Information Disclosure | Script de download loga a URL + SHA completos no stdout (aparecem em CI logs) | accept | URLs são públicas (HuggingFace open model hub). Hashes são de artifacts públicos. Zero segredo em logs. |
| T-22-03-04 | Denial of Service | Script de download falha em ambiente offline ou trava o `pnpm install` | mitigate | Soft mode (default para postinstall): qualquer falha de download resulta em exit 0 com warning. App carrega mas degrada para PTT-only (WAKE-08 path já planejado no Plan 02). Dev pode rodar `--force` depois quando online. |
| T-22-03-05 | Information Disclosure / Licensing | Modelos CC BY-NC-SA 4.0 commitados acidentalmente no repo | mitigate | `.gitignore` bloqueia `*.onnx` no dir, `.gitkeep` + `LICENSE-models.txt` mantêm dir estruturado. Script nunca mexe em nada fora de `apps/desktop/resources/wakeword-models/`. |
</threat_model>

<verification>
## Phase-Level Checks

- [ ] `ls apps/desktop/resources/wakeword-models/` mostra `.gitkeep`, `LICENSE-models.txt`, e (se online) 4 `.onnx`
- [ ] `git status apps/desktop/resources/wakeword-models/` mostra apenas `.gitkeep` e `LICENSE-models.txt` como tracked (nunca .onnx)
- [ ] `pnpm install` na raiz do monorepo aciona o postinstall sem quebrar (soft mode)
- [ ] `node scripts/verify-no-banned-wake-word-libs.mjs` exit 0
- [ ] `pnpm --filter @jarvis/desktop test --run` continua verde (Task 3 não tocou em código de teste)
- [ ] Commits seguem formato pt-BR
</verification>

<success_criteria>
1. Dir `apps/desktop/resources/wakeword-models/` existe com `.gitkeep` + `LICENSE-models.txt`
2. `.gitignore` bloqueia `*.onnx` naquele dir
3. `apps/desktop/.env.example` tem as 4 variáveis `VITE_WAKE_WORD_*` com defaults travados (`true`, `0.5`, `3000`, `hey_jarvis_v0.1`)
4. `scripts/download-wakeword-models.mjs` existe, é idempotente, suporta `--check`/`--force`/`--verbose`, valida SHA-256
5. `postinstall` do desktop package aciona o download em modo soft
6. CI workflow `ci-lockfile-ban.yml` bane porcupine/picovoice/bumblebee/snowboy/vosk
7. Script verifier local passa verde no estado atual do repo
8. Desktop test suite continua verde (zero regressão)
</success_criteria>

<output>
After completion, create `.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-03-SUMMARY.md` with:
- Lista de arquivos criados (dir, script, CI workflow)
- Se os 4 modelos foram baixados com sucesso (online) ou pulados (offline)
- SHA-256 registrados (se aplicável)
- Resultado do `verify-no-banned-wake-word-libs.mjs`
- Próximo plan: 22-04 (integração live useWakeWord + packaging)
</output>
