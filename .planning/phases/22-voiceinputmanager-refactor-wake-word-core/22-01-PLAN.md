---
phase: 22-voiceinputmanager-refactor-wake-word-core
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WAKE-07]
files_modified:
  - apps/desktop/src/renderer/src/voice/voiceInputManager.ts
  - apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts
  - apps/desktop/src/main/ptt-hotkey.ts
  - apps/desktop/src/main/__tests__/ptt-hotkey.test.ts
  - apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx
  - apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/shared/ipc-types.ts
tags: [refactor, mic-ownership, ptt, wake-word, voice-input-manager]

must_haves:
  truths:
    - "Um único módulo decide quem detém o microfone a qualquer momento (PTT ou WakeWord), nunca os dois"
    - "Pressionar PTT hotkey enquanto wake word estivesse ativo preempta o wake word (PTT sempre ganha)"
    - "Tentativa de adquirir wake word enquanto PTT está ativo é rejeitada"
    - "O refactor não quebra o comportamento atual do PTT existente (toggle start/stop, tray reconfig, unregister on quit)"
  artifacts:
    - path: apps/desktop/src/renderer/src/voice/voiceInputManager.ts
      provides: "Singleton VoiceInputManager no renderer com acquire/release/getCurrentSource/subscribe"
      exports: [voiceInputManager, VoiceInputManager, InputSource, VoiceInputGrant]
    - path: apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts
      provides: "State machine table tests cobrindo 9 cenários de arbitragem PTT x wakeword"
      contains: "describe('voiceInputManager'"
    - path: apps/desktop/src/main/ptt-hotkey.ts
      provides: "PTT hotkey refatorado — sem `let isRecording` module-local, delega estado via IPC 'ptt:action' para o renderer (que consulta o VoiceInputManager)"
      contains: "ptt:action"
    - path: apps/desktop/src/main/__tests__/ptt-hotkey.test.ts
      provides: "Test do refactor do ptt-hotkey (mocka globalShortcut + BrowserWindow, valida toggle via state externo e cleanup no unregister)"
      contains: "describe('registerPttHotkey'"
    - path: apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx
      provides: "Test existente atualizado para o novo contrato ptt:action 'toggle' (se houver assertions sobre o payload antigo)"
      contains: "ChatInput"
  key_links:
    - from: "apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx (handler do IPC 'ptt:action')"
      to: "voiceInputManager.acquire('ptt') / release('ptt')"
      via: "chamada direta ao singleton antes de audioRecorder.startRecording / stopRecording"
      pattern: "voiceInputManager\\.acquire\\('ptt'\\)"
    - from: "apps/desktop/src/renderer/src/voice/voiceInputManager.ts"
      to: "listeners subscritos via subscribe() recebem notificação de mudança de source"
      via: "observer pattern in-process (in-renderer)"
      pattern: "subscribe\\(listener"
---

<objective>
Extrair ownership de microfone de `apps/desktop/src/main/ptt-hotkey.ts` para um singleton `VoiceInputManager` no renderer que arbitra `source: 'ptt' | 'wakeword' | null` com política "PTT sempre ganha". **Este é o primeiro commit da Phase 22 e NÃO contém nenhuma linha de código de wake word** — é a mitigação obrigatória de PITFALL #3 (double MediaRecorder race condition) e precondição absoluta antes de qualquer outro plan.

Purpose: Sem este refactor, adicionar wake word em cima do `let isRecording = false;` module-local do `ptt-hotkey.ts` garante race condition em produção — dois `MediaRecorder` competem pelo mesmo `MediaStream`, um mata tracks do outro, backend recebe áudio truncado. O `VoiceInputManager` estabelece um single-writer para "quem está gravando agora" antes de qualquer segundo ator existir.

Output:
- `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` — singleton contract
- `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` — 9 cenários de tabela
- `apps/desktop/src/main/ptt-hotkey.ts` refatorado sem estado de gravação module-local (remove `let isRecording` e as 3 regiões de callsite `ptt:action` que alternam 'start'/'stop')
- `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` — novo test do refactor
- `apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` — handler do `ptt:action` consulta o manager antes de start/stop
- `apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx` — test existente atualizado caso contenha assertions sobre o payload antigo `'start' | 'stop'`
- Zero arquivos novos dentro de `src/renderer/src/voice/wakeWord/` — wake word está **fora** deste plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md
@apps/desktop/src/main/ptt-hotkey.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/renderer/hooks/useAudioRecorder.ts
@apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx
@apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx
@apps/desktop/src/preload/index.ts
@apps/desktop/src/shared/ipc-types.ts
@CLAUDE.md

<interfaces>
<!-- Contratos-chave que este plano define ou consome. Executor não precisa explorar para descobrir. -->

// NOVO — apps/desktop/src/renderer/src/voice/voiceInputManager.ts
// Derivado de 22-RESEARCH.md Pattern 1 (lines 218-267) + WAKE-07.
export type InputSource = 'ptt' | 'wakeword' | null;

export interface VoiceInputGrant {
  source: 'ptt' | 'wakeword';
  releasedPreviousSource: InputSource;
  startedAt: number;
}

export interface VoiceInputManager {
  /**
   * Tenta adquirir o mic para um source.
   * - PTT preempta wakeword: se currentSource==='wakeword' e caller==='ptt', notifica listeners (release automático do wakeword) e concede 'ptt'.
   * - WakeWord é rejeitado se currentSource === 'ptt': retorna { error: 'BUSY' }.
   * - Mesma source tentando adquirir 2x seguidas é idempotente: retorna grant existente, NÃO preempta a si mesma.
   */
  acquire(source: 'ptt' | 'wakeword'): VoiceInputGrant | { error: 'BUSY' };

  /**
   * Libera o mic. No-op se source !== currentSource (evita PTT matar wakeword por acidente).
   * Dispara listeners com null.
   */
  release(source: 'ptt' | 'wakeword'): void;

  /** Estado atual, para observers. */
  getCurrentSource(): InputSource;

  /** Subscribe para transitions. Retorna unsubscribe. */
  subscribe(listener: (source: InputSource) => void): () => void;
}

export declare const voiceInputManager: VoiceInputManager;

// EXISTENTE — apps/desktop/src/main/ptt-hotkey.ts (atual — a ser refatorado)
export function registerPttHotkey(mainWindow: BrowserWindow): boolean;
export function changePttHotkey(accelerator: string, mainWindow: BrowserWindow): boolean;
export function unregisterPttHotkey(): void;
// Estado atual (verificado via grep em 2026-04-11):
//   - linha 18: `let isRecording = false;` — ESTA É A LINHA QUE DEVE SER REMOVIDA.
//   - linhas 33/35/40: toggle dentro do callback de `registerPttHotkey`, emitindo 'stop' (L36) e 'start' (L41).
//   - linhas 76/77/81: toggle dentro do callback re-registrado em `changePttHotkey` (sucesso), emitindo 'stop' (L78) e 'start' (L82).
//   - linhas 96/97/100: toggle dentro do callback FALLBACK re-registrado em `changePttHotkey` (re-register do accelerator antigo), emitindo 'stop' (L98) e 'start' (L101).
//   - linha 72/123: `isRecording = false;` em pontos de cleanup de `changePttHotkey` e `unregisterPttHotkey`.
// Total: 3 pares de `ptt:action` 'start'/'stop' distribuídos em 3 regiões de callback distintas. Refactor DEVE cobrir TODAS as 3.

// EXISTENTE — apps/desktop/src/renderer/hooks/useAudioRecorder.ts
export function useAudioRecorder(): {
  isRecording: boolean;
  error: string | null;
  startRecording: () => Promise<void>;
  stopRecording: () => Promise<Uint8Array | null>;
};

// EXISTENTE — apps/desktop/src/shared/ipc-types.ts (linha 78)
export type PttAction = 'start' | 'stop';
// Este tipo deve virar `export type PttAction = 'toggle';` (string literal fixa).
// Atualizar qualquer consumer do tipo (preload + ChatInput handler).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — criar tests failing do VoiceInputManager e do ptt-hotkey refactor</name>
  <read_first>
    - apps/desktop/src/main/ptt-hotkey.ts (arquivo atual, 127 linhas, com `let isRecording = false;` na linha 18 e TRÊS regiões de callback emitindo `ptt:action` 'start'/'stop': linhas 36/41 em `registerPttHotkey`, linhas 78/82 em `changePttHotkey` success branch, linhas 98/101 em `changePttHotkey` fallback re-register branch)
    - apps/desktop/src/renderer/src/voice/__tests__/handleAudioResponse.test.ts (exemplos de vitest + mock patterns no projeto, se existir) — caso não exista, usar padrão de qualquer test em apps/desktop/src/renderer/__tests__/
    - apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx (test existente do ChatInput — confirmar se há alguma assertion sobre payload `ptt:action` 'start'|'stop'; se sim, precisará ser atualizada na Task 3)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1: VoiceInputManager Singleton" e §"Validation Architecture → Phase Requirements → Test Map" linha WAKE-07
    - apps/desktop/vitest.config.ts (para confirmar include paths de tests)
    - apps/desktop/package.json (confirmar comando `pnpm --filter @jarvis/desktop test`)
  </read_first>
  <behavior>
    voiceInputManager.test.ts — tabela de 10 cenários:
    1. Initial state: getCurrentSource() === null
    2. acquire('ptt') com null → grant com source='ptt', releasedPreviousSource=null, getCurrentSource()==='ptt'
    3. acquire('wakeword') com null → grant com source='wakeword', getCurrentSource()==='wakeword'
    4. acquire('ptt') idempotente: chamando 2x seguidas retorna grants consistentes, subscribe listener chamado só 1x
    5. acquire('wakeword') idempotente: idem
    6. PTT preempta wakeword: pre='wakeword', acquire('ptt') → grant.source='ptt', releasedPreviousSource='wakeword', listener chamado com 'ptt'
    7. WakeWord rejeitado quando PTT ativo: pre='ptt', acquire('wakeword') → { error: 'BUSY' }, getCurrentSource() continua 'ptt'
    8. release('ptt') quando current='ptt' → getCurrentSource()===null, listener chamado com null
    9. release('ptt') quando current='wakeword' → NO-OP, getCurrentSource() continua 'wakeword', listener NÃO chamado
    10. subscribe retorna unsubscribe — chamar unsubscribe silencia notificações subsequentes

    ptt-hotkey.test.ts — describe('registerPttHotkey'):
    1. Dado mock do `globalShortcut.register` (retorna true) e `BrowserWindow.webContents.send` spy, registerPttHotkey(win) retorna true e registra o accelerator default de getPttHotkey()
    2. Callback do globalShortcut dispara `mainWindow.webContents.send('ptt:action', 'toggle')` em TODAS as invocações (nunca mais `'start'` ou `'stop'`)
    3. O arquivo `ptt-hotkey.ts` NÃO contém mais `let isRecording` em escopo de módulo — asserção via `fs.readFileSync` + regex negativo `expect(source).not.toMatch(/^let isRecording/m)`
    4. O arquivo `ptt-hotkey.ts` NÃO contém mais NENHUMA referência a `isRecording` — `expect(source).not.toMatch(/isRecording/)` (cobre as 12+ referências atuais espalhadas pelas 3 regiões de callback)
    5. unregisterPttHotkey() chama `globalShortcut.unregister` com o accelerator registrado

    ptt-hotkey.test.ts — describe('changePttHotkey'):
    6. Success branch: `changePttHotkey('CmdOrCtrl+Alt+Space', win)` com globalShortcut.register retornando true — callback NOVO emite `webContents.send('ptt:action', 'toggle')` quando disparado (região 2 — linhas 78/82 atuais, agora unificadas)
    7. Fallback branch: primeiro `register` falha, fallback re-register do accelerator antigo sucede — callback do fallback também emite `'ptt:action', 'toggle'` (região 3 — linhas 98/101 atuais, agora unificadas). Configurar globalShortcut.register para retornar false na PRIMEIRA call e true na SEGUNDA.
  </behavior>
  <action>
    Criar os 2 arquivos de teste COMO FAILING TESTS (stubs que quando rodados reportam que `voiceInputManager` não existe ou que `ptt-hotkey.ts` ainda contém `let isRecording`).

    1. Criar `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` com:
       - `import { describe, it, expect, beforeEach, vi } from 'vitest';`
       - `import { voiceInputManager } from '../voiceInputManager';` ← ESTE IMPORT VAI FALHAR (arquivo ainda não existe) — é o red do Wave 0
       - 10 testes (cenários 1-10 acima) com assertions concretas.
       - Cenário 6: depois de `acquire('ptt')` sobre pre='wakeword', `expect(listener).toHaveBeenCalledWith('ptt')` — travar em UMA sequência (notificação única com 'ptt').
       - Cenário 10: `const unsub = voiceInputManager.subscribe(listener); unsub(); voiceInputManager.acquire('ptt'); expect(listener).not.toHaveBeenCalled();`

    2. Criar `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` com:
       - `vi.mock('electron', ...)` mockando `globalShortcut.register`, `globalShortcut.unregister`, `BrowserWindow`
       - `vi.mock('../store', () => ({ getPttHotkey: () => 'CmdOrCtrl+Space', setPttHotkey: vi.fn() }))`
       - Describe `registerPttHotkey`:
         - it: `retorna true quando globalShortcut.register retorna true`
         - it: `invoca webContents.send('ptt:action', 'toggle') na primeira call do callback`
         - it: `invoca webContents.send('ptt:action', 'toggle') na segunda call do callback (toggle resolvido no renderer via VoiceInputManager)`
         - it: `arquivo ptt-hotkey.ts não contém mais "let isRecording" em escopo de módulo` — `fs.readFileSync(path.resolve(__dirname, '../ptt-hotkey.ts'), 'utf8')` + `expect(src).not.toMatch(/^let isRecording/m)` (vai FALHAR inicialmente).
         - it: `arquivo ptt-hotkey.ts não contém NENHUMA referência a isRecording` — `expect(src).not.toMatch(/isRecording/)` (cobre as 3 regiões — vai FALHAR inicialmente).
       - Describe `changePttHotkey`:
         - it: `success branch emite 'ptt:action', 'toggle' no callback novo`
         - it: `fallback branch (primeiro register falha, fallback re-register sucede) emite 'ptt:action', 'toggle' no callback do fallback` — configurar mock `globalShortcut.register` para false na primeira call e true na segunda.
       - Describe `unregisterPttHotkey`: chama `globalShortcut.unregister` com o accelerator registrado

    3. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts src/main/__tests__/ptt-hotkey.test.ts` — DEVE falhar.

    4. NÃO implementar `voiceInputManager.ts` nem modificar `ptt-hotkey.ts` nesta task — isso é Task 2/3.

    Commit: `✅ test(22-01): cria Wave 0 tests failing para voiceInputManager + refactor ptt-hotkey`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts src/main/__tests__/ptt-hotkey.test.ts 2>&1 | grep -E "FAIL|Cannot find module|let isRecording|isRecording"</automated>
  </verify>
  <acceptance_criteria>
    - arquivo `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` existe e contém `describe('voiceInputManager'` e pelo menos 10 `it(` blocks
    - arquivo `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` existe e contém `describe('registerPttHotkey'` e `describe('changePttHotkey'`
    - `grep -c "toggle" apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` retorna pelo menos 3 (cobrindo as 3 regiões de callback)
    - ambos os testes FALHAM quando rodados
    - `grep "from '../voiceInputManager'" apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` retorna 1 linha
    - `grep "fs.readFileSync" apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` retorna pelo menos 1 linha
    - nenhuma edição em `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` (não deve existir ainda)
    - `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `1` (ainda presente)
  </acceptance_criteria>
  <done>Wave 0 failing tests criados e committados. Suite reporta falhas esperadas. Nenhuma linha de produção modificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar VoiceInputManager singleton no renderer e green dos tests</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts (criado na Task 1 — é o contrato de entrada)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1: VoiceInputManager Singleton"
    - apps/desktop/src/renderer/src/voice/handleAudioResponse.ts (padrão de módulo TS no domínio voice/)
    - CLAUDE.md §"Git Commit Guidelines"
  </read_first>
  <action>
    Criar `apps/desktop/src/renderer/src/voice/voiceInputManager.ts`:

    1. Types exportados (copiar VERBATIM do `<interfaces>` block):
       - `export type InputSource = 'ptt' | 'wakeword' | null;`
       - `export interface VoiceInputGrant { source: 'ptt' | 'wakeword'; releasedPreviousSource: InputSource; startedAt: number; }`
       - `export interface VoiceInputManager { acquire / release / getCurrentSource / subscribe }`

    2. Implementação closure-based singleton:
       - `let currentSource: InputSource = null;`
       - `let grantStartedAt = 0;`
       - `const listeners = new Set<(source: InputSource) => void>();`
       - Helper privado `notify(src)` → `listeners.forEach(l => l(src));`

       acquire(source):
       - Se `currentSource === source` → retorna grant existente SEM notificar (idempotência).
       - Se `source === 'wakeword' && currentSource === 'ptt'` → retorna `{ error: 'BUSY' as const }`.
       - Se `source === 'ptt' && currentSource === 'wakeword'` (preempção): atualiza state, notifica com 'ptt', retorna grant com `releasedPreviousSource='wakeword'`.
       - Se `currentSource === null`: atualiza state, notifica com source, retorna grant.

       release(source):
       - Se `source !== currentSource` → NO-OP.
       - Se `source === currentSource` → seta null, notifica com null.

       getCurrentSource() → `return currentSource;`
       subscribe(listener) → `listeners.add(listener); return () => listeners.delete(listener);`

    3. Export final:
       ```ts
       export const voiceInputManager: VoiceInputManager = { acquire, release, getCurrentSource, subscribe };
       export function __resetVoiceInputManagerForTests(): void {
         currentSource = null;
         grantStartedAt = 0;
         listeners.clear();
       }
       ```

    4. No test file, trocar `beforeEach` para usar `__resetVoiceInputManagerForTests()`.

    5. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts`. Todos os 10 cenários verdes.

    Commit: `✨ feat(22-01): VoiceInputManager singleton com política PTT-preempts-wakeword`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` existe
    - `grep "export const voiceInputManager" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna 1 linha
    - `grep "export type InputSource = 'ptt' | 'wakeword' | null" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna 1 linha
    - `grep "__resetVoiceInputManagerForTests" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna 1 linha
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts` exit code 0
    - `grep "wakeWord" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna vazio
    - `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` ainda retorna `1` (intocado)
  </acceptance_criteria>
  <done>voiceInputManager singleton implementado, 10 cenários de test verdes, nenhum código de wake word introduzido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refatorar ptt-hotkey.ts (remove isRecording + 3 regiões de callback) + wire no ChatInput via VoiceInputManager</name>
  <read_first>
    - apps/desktop/src/main/ptt-hotkey.ts (127 linhas — o alvo do refactor). ATENÇÃO: TRÊS regiões de callback distintas emitem `ptt:action` 'start'/'stop' e TODAS precisam virar 'toggle':
        - Região 1 — `registerPttHotkey`, callback inicial (linhas 33-42 atuais): checa `isRecording`, envia 'stop' (L36) ou 'start' (L41), alterna `isRecording`.
        - Região 2 — `changePttHotkey` success branch, callback do novo register (linhas 75-83 atuais): mesmo padrão, 'stop' (L78) ou 'start' (L82).
        - Região 3 — `changePttHotkey` fallback branch, callback do re-register do accelerator antigo (linhas 93-102 atuais): mesmo padrão, 'stop' (L98) ou 'start' (L101).
      O refactor DEVE remover `let isRecording` + 6 atribuições/leituras + 6 sends distribuídos nas 3 regiões, substituindo cada uma por um único `webContents.send('ptt:action', 'toggle')` por região.
    - apps/desktop/src/main/__tests__/ptt-hotkey.test.ts (Task 1 — contrato, cobre as 3 regiões)
    - apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx (CAMINHO REAL do handler atual — NÃO `src/renderer/src/chat/ChatInput.tsx`)
    - apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx (test existente — se houver assertion sobre payload 'start'|'stop' ou mock de `onPttAction` entregando `'start'|'stop'`, atualizar para `'toggle'`)
    - apps/desktop/src/preload/index.ts (onde `window.jarvis.onPttAction` é exposto)
    - apps/desktop/src/shared/ipc-types.ts (linha 78: `export type PttAction = 'start' | 'stop';` — DEVE virar `'toggle'`)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1 Implementation notes"
  </read_first>
  <action>
    REFACTOR em três sub-passos atomizados, sem introduzir qualquer código de wake word:

    **Sub-passo A — Refactor `apps/desktop/src/main/ptt-hotkey.ts` (as 3 regiões de callback):**

    1. Remover `let isRecording = false;` (linha 18 atual).
    2. Remover TODAS as atribuições e leituras de `isRecording`:
       - Linhas 33/35/40 em `registerPttHotkey` (região 1)
       - Linha 72 em `changePttHotkey` (cleanup antes do novo register)
       - Linhas 76/77/81 em `changePttHotkey` success branch (região 2)
       - Linhas 96/97/100 em `changePttHotkey` fallback branch (região 3)
       - Linha 123 em `unregisterPttHotkey` (cleanup final)
       Verificar com `grep -c "isRecording" apps/desktop/src/main/ptt-hotkey.ts` = `0`.
    3. Substituir cada uma das 3 regiões `if (isRecording) send('stop') else send('start')` por UM único send sem alternância:
       - Região 1: callback vira `() => mainWindow.webContents.send('ptt:action', 'toggle');`
       - Região 2: callback novo também vira `() => mainWindow.webContents.send('ptt:action', 'toggle');`
       - Região 3: callback do fallback também vira `() => mainWindow.webContents.send('ptt:action', 'toggle');`
    4. Manter exports inalterados.
    5. Adicionar comentário no topo: `// Phase 22: ownership de mic foi extraída para VoiceInputManager (renderer). Este módulo só emite o toggle event, renderer decide start/stop.`

    **Sub-passo B — Atualizar tipos + preload + handler no renderer:**

    1. `apps/desktop/src/shared/ipc-types.ts` (linha 78): `export type PttAction = 'start' | 'stop';` → `export type PttAction = 'toggle';`
    2. `apps/desktop/src/preload/index.ts`: se `onPttAction` for tipado com `'start' | 'stop'`, atualizar para `PttAction` importado do shared.
    3. `apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` — LER primeiro. Substituir a lógica do listener `ptt:action`:
       - Importar `voiceInputManager` de `@/voice/voiceInputManager` (alias `@` = `src/renderer/src`).
       - Handler novo consulta o singleton em vez de branch pelo payload:
         ```ts
         const handlePttToggle = async () => {
           if (voiceInputManager.getCurrentSource() === 'ptt') {
             voiceInputManager.release('ptt');
             await handleStopRecording();
           } else {
             const grant = voiceInputManager.acquire('ptt');
             if ('error' in grant) {
               console.warn('[ChatInput] PTT acquire BUSY');
               return;
             }
             await handleStartRecording();
           }
         };
         ```
       - Substituir o registro do `onPttAction` listener para chamar `handlePttToggle` sem olhar o payload.

    **Sub-passo C — Atualizar tests existentes + green:**

    1. Atualizar `ptt-hotkey.test.ts` da Task 1:
       - Trocar `'start'` / `'stop'` por `'toggle'` em TODAS as asserções de `webContents.send`.
       - Manter `not.toMatch(/^let isRecording/m)` e `not.toMatch(/isRecording/)` — agora devem PASSAR.
    2. Ler `apps/desktop/src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx`. SE contiver referência a `'start'`/`'stop'` como payload de `ptt:action` ou mock de `onPttAction` entregando essas strings, atualizar para `'toggle'`. SE apenas testar o botão UI do chat (toggle visibility) sem tocar em PTT payload, deixar intacto e registrar no commit message.
    3. Rodar `pnpm --filter @jarvis/desktop test --run src/main/__tests__/ptt-hotkey.test.ts src/renderer/src/voice/__tests__/voiceInputManager.test.ts src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx` → todos verdes.
    4. Rodar full suite `pnpm --filter @jarvis/desktop test --run` — zero regressão.

    Commit: `♻️ refactor(22-01): remove isRecording module-local de ptt-hotkey (3 regiões) e delega toggle ao VoiceInputManager no renderer`
  </action>
  <verify>
    <automated>grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts; grep -c "isRecording" apps/desktop/src/main/ptt-hotkey.ts; grep "ptt:action" apps/desktop/src/main/ptt-hotkey.ts | grep -c "toggle"; pnpm --filter @jarvis/desktop test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `0`
    - `grep -c "isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `0` (cobre as 3 regiões + os 2 cleanups — zero referência residual)
    - `grep "ptt:action" apps/desktop/src/main/ptt-hotkey.ts | grep -c "toggle"` retorna `>= 3` (uma por região de callback: registerPttHotkey + changePttHotkey success + changePttHotkey fallback)
    - `grep "ptt:action" apps/desktop/src/main/ptt-hotkey.ts | grep -c "'start'\|'stop'"` retorna `0` (zero payload antigo remanescente)
    - `grep "voiceInputManager" apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` retorna pelo menos 1 linha
    - `grep "acquire('ptt')" apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` retorna pelo menos 1 linha
    - `grep "release('ptt')" apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` retorna pelo menos 1 linha
    - `grep "PttAction = 'toggle'" apps/desktop/src/shared/ipc-types.ts` retorna 1 linha
    - `grep "'start' | 'stop'" apps/desktop/src/shared/ipc-types.ts` retorna `0`
    - `pnpm --filter @jarvis/desktop test --run src/main/__tests__/ptt-hotkey.test.ts` exit 0
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts` exit 0
    - `pnpm --filter @jarvis/desktop test --run src/renderer/components/ChatInput/__tests__/ChatInput.test.tsx` exit 0
    - `pnpm --filter @jarvis/desktop test --run` full suite exit 0
    - `grep -r "wakeWord\|onnxruntime\|openwakeword" apps/desktop/src/main/ptt-hotkey.ts apps/desktop/src/renderer/src/voice/voiceInputManager.ts apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx` retorna vazio
  </acceptance_criteria>
  <done>ptt-hotkey.ts sem estado module-local, as 3 regiões de callback emitem 'toggle', ChatInput consulta VoiceInputManager, full desktop suite verde, nenhum código de wake word introduzido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 01)

| Boundary | Description |
|----------|-------------|
| main→renderer IPC | `ptt:action` event flui do main (globalShortcut callback) para o renderer — payload era `'start'\|'stop'`, agora vira `'toggle'` (string literal fixa, não user-controlled) |
| renderer in-process | `voiceInputManager` singleton vive inteiramente no renderer; state nunca cruza IPC boundary neste plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | `ptt:action` IPC event | accept | Payload é literal string `'toggle'` emitido apenas pelo main em resposta a globalShortcut. Não há input de usuário injetado no payload. |
| T-22-01-02 | Elevation of Privilege | voiceInputManager state | mitigate | Manager vive só no renderer (no IPC → no cross-process escalation). Não aceita args de user input — `acquire`/`release` recebem string literal `'ptt'\|'wakeword'` validada por TypeScript types. |
| T-22-01-03 | Denial of Service | Idempotência de acquire | mitigate | Cenário 4-5 do test cobre: chamadas repetidas de `acquire(same)` são NO-OP, não vazam memória nem disparam listeners redundantes. |
| T-22-01-04 | Race Condition (data integrity) | ptt-hotkey.ts old `let isRecording` | mitigate (core) | **Este plano é a mitigação** de PITFALL #3. Removendo o state module-local e centralizando em `voiceInputManager`, eliminamos double-writer — base obrigatória para Plan 04 poder adicionar wake word sem race. |
| T-22-01-05 | Information Disclosure | subscribe listeners | accept | Listeners recebem só `InputSource` (enum de 3 valores) — nenhum áudio, nenhum PII. Baixo valor de target. |
</threat_model>

<verification>
## Phase-Level Checks

- [ ] `pnpm --filter @jarvis/desktop test --run` full suite verde
- [ ] `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` === 0
- [ ] `grep -c "isRecording" apps/desktop/src/main/ptt-hotkey.ts` === 0 (zero referências residuais nas 3 regiões de callback + cleanups)
- [ ] `grep "ptt:action" apps/desktop/src/main/ptt-hotkey.ts | grep -c "toggle"` >= 3 (uma emissão por região de callback)
- [ ] `grep -r "wakeWord\|onnxruntime" apps/desktop/src/renderer/src/voice/voiceInputManager.ts apps/desktop/src/main/ptt-hotkey.ts` vazio
- [ ] PTT hotkey ainda funciona em `pnpm dev` (smoke manual opcional — não bloqueante porque suite cobre)
- [ ] Commits seguem formato `♻️ refactor(22-01):` / `✨ feat(22-01):` / `✅ test(22-01):` em pt-BR
</verification>

<success_criteria>
1. Arquivo `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` existe com o contrato exato do `<interfaces>` block
2. 10 cenários de `voiceInputManager.test.ts` verdes
3. 7+ cenários de `ptt-hotkey.test.ts` verdes (incluindo os 2 regex negativos de `isRecording` e cobertura das 3 regiões de callback)
4. `ptt-hotkey.ts` não tem `isRecording` e emite `'ptt:action', 'toggle'` nas 3 regiões
5. `ChatInput.tsx` (caminho real `apps/desktop/src/renderer/components/ChatInput/ChatInput.tsx`) consulta `voiceInputManager.acquire('ptt')` antes de `startRecording`
6. Zero arquivos dentro de `apps/desktop/src/renderer/src/voice/wakeWord/` — wake word vem nos próximos plans
7. Full desktop test suite verde (nenhuma regressão)
</success_criteria>

<output>
After completion, create `.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-01-SUMMARY.md` with:
- What was refactored (voiceInputManager contract, ptt-hotkey toggle externalization cobrindo as 3 regiões de callback)
- Test coverage (10 cenários VoiceInputManager + 7+ cenários ptt-hotkey)
- Confirmation that wake word code is ABSENT (grep evidence)
- Next plan: 22-02 (Wake Word Core modules)
</output>
