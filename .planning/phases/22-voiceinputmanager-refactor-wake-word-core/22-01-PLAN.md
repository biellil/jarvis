---
phase: 22-voiceinputmanager-refactor-wake-word-core
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WAKE-07]
files_modified:
  - apps/desktop/src/renderer/src/voice/voiceInputManager.ts
  - apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts
  - apps/desktop/src/main/ptt-hotkey.ts
  - apps/desktop/src/main/__tests__/ptt-hotkey.test.ts
  - apps/desktop/src/renderer/src/chat/ChatInput.tsx
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/shared/ipc-types.ts
tags: [refactor, mic-ownership, ptt, wake-word, voice-input-manager]

must_haves:
  truths:
    - "Um único módulo decide quem detém o microfone a qualquer momento (PTT ou WakeWord), nunca os dois"
    - "Pressionar PTT hotkey enquanto wake word estivesse ativo preempta o wake word (PTT sempre ganha)"
    - "Tentativa de adquirir wake word enquanto PTT está ativo é rejeitada"
    - "O refactor não quebra o comportamento atual do PTT existente (toggle start/stop, tray reconfig, unregister on quit)"
  artifacts:
    - path: apps/desktop/src/renderer/src/voice/voiceInputManager.ts
      provides: "Singleton VoiceInputManager no renderer com acquire/release/getCurrentSource/subscribe"
      exports: [voiceInputManager, VoiceInputManager, InputSource, VoiceInputGrant]
    - path: apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts
      provides: "State machine table tests cobrindo 9 cenários de arbitragem PTT x wakeword"
      contains: "describe('voiceInputManager'"
    - path: apps/desktop/src/main/ptt-hotkey.ts
      provides: "PTT hotkey refatorado — sem `let isRecording` module-local, delega estado via IPC 'ptt:action' para o renderer (que consulta o VoiceInputManager)"
      contains: "ptt:action"
    - path: apps/desktop/src/main/__tests__/ptt-hotkey.test.ts
      provides: "Test do refactor do ptt-hotkey (mocka globalShortcut + BrowserWindow, valida toggle via state externo e cleanup no unregister)"
      contains: "describe('registerPttHotkey'"
  key_links:
    - from: "apps/desktop/src/renderer/src/chat/ChatInput.tsx (handler do IPC 'ptt:action')"
      to: "voiceInputManager.acquire('ptt') / release('ptt')"
      via: "chamada direta ao singleton antes de audioRecorder.startRecording / stopRecording"
      pattern: "voiceInputManager\\.acquire\\('ptt'\\)"
    - from: "apps/desktop/src/renderer/src/voice/voiceInputManager.ts"
      to: "listeners subscritos via subscribe() recebem notificação de mudança de source"
      via: "observer pattern in-process (in-renderer)"
      pattern: "subscribe\\(listener"
---

<objective>
Extrair ownership de microfone de `apps/desktop/src/main/ptt-hotkey.ts` para um singleton `VoiceInputManager` no renderer que arbitra `source: 'ptt' | 'wakeword' | null` com política "PTT sempre ganha". **Este é o primeiro commit da Phase 22 e NÃO contém nenhuma linha de código de wake word** — é a mitigação obrigatória de PITFALL #3 (double MediaRecorder race condition) e precondição absoluta antes de qualquer outro plan.

Purpose: Sem este refactor, adicionar wake word em cima do `let isRecording = false;` module-local do `ptt-hotkey.ts` garante race condition em produção — dois `MediaRecorder` competem pelo mesmo `MediaStream`, um mata tracks do outro, backend recebe áudio truncado. O `VoiceInputManager` estabelece um single-writer para "quem está gravando agora" antes de qualquer segundo ator existir.

Output:
- `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` — singleton contract
- `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` — 9 cenários de tabela
- `apps/desktop/src/main/ptt-hotkey.ts` refatorado sem estado de gravação module-local
- `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` — novo test do refactor
- `apps/desktop/src/renderer/src/chat/ChatInput.tsx` — handler do `ptt:action` consulta o manager antes de start/stop
- Zero arquivos novos dentro de `src/renderer/src/voice/wakeWord/` — wake word está **fora** deste plan
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md
@apps/desktop/src/main/ptt-hotkey.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/renderer/hooks/useAudioRecorder.ts
@apps/desktop/src/renderer/src/chat/ChatInput.tsx
@apps/desktop/src/preload/index.ts
@apps/desktop/src/shared/ipc-types.ts
@CLAUDE.md

<interfaces>
<!-- Contratos-chave que este plano define ou consome. Executor não precisa explorar para descobrir. -->

// NOVO — apps/desktop/src/renderer/src/voice/voiceInputManager.ts
// Derivado de 22-RESEARCH.md Pattern 1 (lines 218-267) + WAKE-07.
export type InputSource = 'ptt' | 'wakeword' | null;

export interface VoiceInputGrant {
  source: 'ptt' | 'wakeword';
  releasedPreviousSource: InputSource;
  startedAt: number;
}

export interface VoiceInputManager {
  /**
   * Tenta adquirir o mic para um source.
   * - PTT preempta wakeword: se currentSource==='wakeword' e caller==='ptt', notifica listeners (release automático do wakeword) e concede 'ptt'.
   * - WakeWord é rejeitado se currentSource === 'ptt': retorna { error: 'BUSY' }.
   * - Mesma source tentando adquirir 2x seguidas é idempotente: retorna grant existente, NÃO preempta a si mesma.
   */
  acquire(source: 'ptt' | 'wakeword'): VoiceInputGrant | { error: 'BUSY' };

  /**
   * Libera o mic. No-op se source !== currentSource (evita PTT matar wakeword por acidente).
   * Dispara listeners com null.
   */
  release(source: 'ptt' | 'wakeword'): void;

  /** Estado atual, para observers. */
  getCurrentSource(): InputSource;

  /** Subscribe para transitions. Retorna unsubscribe. */
  subscribe(listener: (source: InputSource) => void): () => void;
}

export declare const voiceInputManager: VoiceInputManager;

// EXISTENTE — apps/desktop/src/main/ptt-hotkey.ts (atual — a ser refatorado)
export function registerPttHotkey(mainWindow: BrowserWindow): boolean;
export function changePttHotkey(accelerator: string, mainWindow: BrowserWindow): boolean;
export function unregisterPttHotkey(): void;
// Estado atual: `let isRecording = false;` module-local na linha 18 — ESTA É A LINHA QUE DEVE SER REMOVIDA.

// EXISTENTE — apps/desktop/src/renderer/hooks/useAudioRecorder.ts
export function useAudioRecorder(): {
  isRecording: boolean;
  error: string | null;
  startRecording: () => Promise<void>;
  stopRecording: () => Promise<Uint8Array | null>;
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — criar tests failing do VoiceInputManager e do ptt-hotkey refactor</name>
  <read_first>
    - apps/desktop/src/main/ptt-hotkey.ts (arquivo atual, 127 linhas, com `let isRecording = false;` na linha 18)
    - apps/desktop/src/renderer/src/voice/__tests__/handleAudioResponse.test.ts (exemplos de vitest + mock patterns no projeto, se existir) — caso não exista, usar padrão de qualquer test em apps/desktop/src/renderer/__tests__/
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1: VoiceInputManager Singleton" e §"Validation Architecture → Phase Requirements → Test Map" linha WAKE-07
    - apps/desktop/vitest.config.ts (para confirmar include paths de tests)
    - apps/desktop/package.json (confirmar comando `pnpm --filter @jarvis/desktop test`)
  </read_first>
  <behavior>
    voiceInputManager.test.ts — tabela de 9 cenários (describe.each):
    1. Initial state: getCurrentSource() === null
    2. acquire('ptt') com null → grant com source='ptt', releasedPreviousSource=null, getCurrentSource()==='ptt'
    3. acquire('wakeword') com null → grant com source='wakeword', getCurrentSource()==='wakeword'
    4. acquire('ptt') idempotente: chamando 2x seguidas retorna grants consistentes, subscribe listener chamado só 1x
    5. acquire('wakeword') idempotente: idem
    6. PTT preempta wakeword: pre='wakeword', acquire('ptt') → grant.source='ptt', releasedPreviousSource='wakeword', listener chamado com 'ptt' (e antes com null ou só 'ptt' direto — documentar escolha)
    7. WakeWord rejeitado quando PTT ativo: pre='ptt', acquire('wakeword') → { error: 'BUSY' }, getCurrentSource() continua 'ptt'
    8. release('ptt') quando current='ptt' → getCurrentSource()===null, listener chamado com null
    9. release('ptt') quando current='wakeword' → NO-OP, getCurrentSource() continua 'wakeword', listener NÃO chamado
    10. subscribe retorna unsubscribe — chamar unsubscribe silencia notificações subsequentes

    ptt-hotkey.test.ts — describe('registerPttHotkey'):
    1. Dado mock do `globalShortcut.register` (retorna true) e `BrowserWindow.webContents.send` spy, registerPttHotkey(win) retorna true e registra o accelerator default de getPttHotkey()
    2. Callback do globalShortcut dispara `mainWindow.webContents.send('ptt:action', 'start')` na PRIMEIRA invocação e `('ptt:action', 'stop')` na SEGUNDA (toggle externalizado — ver assertion abaixo)
    3. O arquivo `ptt-hotkey.ts` NÃO contém mais `let isRecording` em escopo de módulo — asserção via `fs.readFileSync` + regex negativo: `expect(source).not.toMatch(/^let isRecording/m)` (test estrutural — válido porque é refactor spec)
    4. unregisterPttHotkey() chama `globalShortcut.unregister` com o accelerator registrado
  </behavior>
  <action>
    Criar os 2 arquivos de teste COMO FAILING TESTS (stubs que quando rodados reportam que `voiceInputManager` não existe ou que `ptt-hotkey.ts` ainda contém `let isRecording`).

    1. Criar `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` com:
       - `import { describe, it, expect, beforeEach, vi } from 'vitest';`
       - `import { voiceInputManager } from '../voiceInputManager';` ← ESTE IMPORT VAI FALHAR (arquivo ainda não existe) — é o red do Wave 0
       - 10 testes (cenários 1-10 acima) com assertions concretas. Para cada cenário usar `beforeEach(() => voiceInputManager.release('ptt'); voiceInputManager.release('wakeword'));` para garantir estado limpo.
       - Cenário 6: depois de `acquire('ptt')` sobre pre='wakeword', `expect(listener).toHaveBeenCalledWith('ptt')` — aceitar duas sequências possíveis: chamada única direta para 'ptt' OU null→'ptt'. Documentar no test o que foi escolhido e travar em uma sequência.
       - Cenário 10: `const unsub = voiceInputManager.subscribe(listener); unsub(); voiceInputManager.acquire('ptt'); expect(listener).not.toHaveBeenCalled();`

    2. Criar `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` com:
       - `vi.mock('electron', ...)` mockando `globalShortcut.register`, `globalShortcut.unregister`, `BrowserWindow`
       - `vi.mock('../store', () => ({ getPttHotkey: () => 'CmdOrCtrl+Space', setPttHotkey: vi.fn() }))`
       - Describe `registerPttHotkey`:
         - it: `retorna true quando globalShortcut.register retorna true`
         - it: `invoca webContents.send('ptt:action', 'start') na primeira call do callback`
         - it: `invoca webContents.send('ptt:action', 'stop') na segunda call do callback (toggle resolvido externalmente pelo renderer, module não carrega mais let isRecording)`
         - it: `arquivo ptt-hotkey.ts não contém mais "let isRecording" em escopo de módulo` — ler o source do arquivo via `fs.readFileSync(path.resolve(__dirname, '../ptt-hotkey.ts'), 'utf8')` e `expect(src).not.toMatch(/^let isRecording/m)` (regex negativo em multiline mode). Este test vai FALHAR inicialmente porque a linha 18 do arquivo atual ainda tem `let isRecording = false;`.
       - Describe `unregisterPttHotkey`: chama `globalShortcut.unregister` com o accelerator registrado

    3. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts src/main/__tests__/ptt-hotkey.test.ts` — DEVE falhar com erros de import / assertion (essa falha é o sinal Wave 0 correto).

    4. NÃO implementar `voiceInputManager.ts` nem modificar `ptt-hotkey.ts` nesta task — isso é Task 2. Esta task deixa a suíte vermelha de propósito.

    Commit: `✅ test(22-01): cria Wave 0 tests failing para voiceInputManager + refactor ptt-hotkey`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts src/main/__tests__/ptt-hotkey.test.ts 2>&1 | grep -E "FAIL|Cannot find module|let isRecording"</automated>
  </verify>
  <acceptance_criteria>
    - arquivo `apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` existe e contém `describe('voiceInputManager'` e pelo menos 10 `it(` blocks
    - arquivo `apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` existe e contém `describe('registerPttHotkey'`
    - ambos os testes FALHAM quando rodados (erro esperado: Cannot find module '../voiceInputManager' E/OU regex match de `let isRecording`)
    - `grep "from '../voiceInputManager'" apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts` retorna 1 linha
    - `grep "fs.readFileSync" apps/desktop/src/main/__tests__/ptt-hotkey.test.ts` retorna pelo menos 1 linha (test estrutural do arquivo)
    - nenhuma edição em `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` (arquivo ainda não deve existir) — verificar com `test ! -f apps/desktop/src/renderer/src/voice/voiceInputManager.ts && echo NOT_YET || echo EXISTS` deve retornar NOT_YET
    - nenhuma edição em `apps/desktop/src/main/ptt-hotkey.ts` — `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` deve retornar `1` (ainda presente na linha 18)
  </acceptance_criteria>
  <done>Wave 0 failing tests criados e committados. Suite reporta falhas esperadas (import missing + regex match). Nenhuma linha de produção modificada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar VoiceInputManager singleton no renderer e green dos tests</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts (criado na Task 1 — é o contrato de entrada)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1: VoiceInputManager Singleton" (lines 218-267) — contract completo
    - apps/desktop/src/renderer/src/voice/handleAudioResponse.ts (padrão de módulo TS no domínio voice/)
    - CLAUDE.md §"Git Commit Guidelines" (formato Conventional Commits + emoji em pt-BR)
  </read_first>
  <action>
    Criar `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` com:

    1. Types exportados (copiar VERBATIM do `<interfaces>` block acima):
       - `export type InputSource = 'ptt' | 'wakeword' | null;`
       - `export interface VoiceInputGrant { source: 'ptt' | 'wakeword'; releasedPreviousSource: InputSource; startedAt: number; }`
       - `export interface VoiceInputManager { acquire / release / getCurrentSource / subscribe }`

    2. Implementação in-module (closure-based singleton, não class — para facilitar reset em tests):
       - `let currentSource: InputSource = null;`
       - `let grantStartedAt = 0;`
       - `const listeners = new Set<(source: InputSource) => void>();`
       - Helper privado `notify(src: InputSource)` → `listeners.forEach(l => l(src));`

       acquire(source):
       - Se `currentSource === source` → retorna `{ source, releasedPreviousSource: source, startedAt: grantStartedAt }` SEM notificar (idempotência, cenário 4-5).
       - Se `source === 'wakeword' && currentSource === 'ptt'` → retorna `{ error: 'BUSY' as const }`.
       - Se `source === 'ptt' && currentSource === 'wakeword'` (preempção PTT): `const previous = currentSource; currentSource = 'ptt'; grantStartedAt = Date.now(); notify('ptt'); return { source: 'ptt', releasedPreviousSource: previous, startedAt: grantStartedAt };` (notifica UMA vez com 'ptt', listeners do wakeword inferem release pelo source !== 'wakeword').
       - Se `currentSource === null`: `currentSource = source; grantStartedAt = Date.now(); notify(source); return { source, releasedPreviousSource: null, startedAt: grantStartedAt };`

       release(source):
       - Se `source !== currentSource` → NO-OP (não notifica — cenário 9).
       - Se `source === currentSource` → `currentSource = null; grantStartedAt = 0; notify(null);`

       getCurrentSource() → `return currentSource;`
       subscribe(listener) → `listeners.add(listener); return () => listeners.delete(listener);`

    3. Export final:
       ```ts
       export const voiceInputManager: VoiceInputManager = { acquire, release, getCurrentSource, subscribe };
       ```
       E um helper para tests:
       ```ts
       /** Reset module state. ONLY for tests. */
       export function __resetVoiceInputManagerForTests(): void {
         currentSource = null;
         grantStartedAt = 0;
         listeners.clear();
       }
       ```

    4. No test file `voiceInputManager.test.ts`, trocar o `beforeEach` que chama `release('ptt'); release('wakeword')` por `beforeEach(() => __resetVoiceInputManagerForTests())` — garantindo estado limpo entre testes.

    5. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts`. Todos os 10 cenários devem passar verde.

    Commit: `✨ feat(22-01): VoiceInputManager singleton com política PTT-preempts-wakeword`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - arquivo `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` existe
    - `grep "export const voiceInputManager" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna exatamente 1 linha
    - `grep "export type InputSource = 'ptt' | 'wakeword' | null" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna 1 linha
    - `grep "__resetVoiceInputManagerForTests" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` retorna 1 linha
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts` exit code 0 com pelo menos 10 testes passando
    - o módulo NÃO importa nada de `./wakeWord/` (ainda não existe) — `grep "wakeWord" apps/desktop/src/renderer/src/voice/voiceInputManager.ts` deve retornar vazio
    - arquivo `ptt-hotkey.ts` ainda NÃO foi modificado nesta task — `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `1`
  </acceptance_criteria>
  <done>voiceInputManager singleton implementado, 10 cenários de test verdes, nenhum código de wake word introduzido, ptt-hotkey ainda intocado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refatorar ptt-hotkey.ts (remove isRecording module-local) + wire no ChatInput via VoiceInputManager</name>
  <read_first>
    - apps/desktop/src/main/ptt-hotkey.ts (127 linhas — o alvo do refactor)
    - apps/desktop/src/main/__tests__/ptt-hotkey.test.ts (criado na Task 1 — contrato de saída)
    - apps/desktop/src/renderer/src/chat/ChatInput.tsx (handler atual do IPC 'ptt:action' — onde vamos inserir a consulta ao VoiceInputManager)
    - apps/desktop/src/preload/index.ts (onde `window.jarvis.onPttAction` é exposto, se já existir; caso contrário ver `src/shared/ipc-types.ts`)
    - apps/desktop/src/shared/ipc-types.ts
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 1 Implementation notes" (ponto 2 — IPC flow via 'ptt:action')
  </read_first>
  <action>
    REFACTOR em dois sub-passos atomizados, sem introduzir qualquer código de wake word:

    **Sub-passo A — Refactor `apps/desktop/src/main/ptt-hotkey.ts`:**

    1. Remover a linha `let isRecording = false;` (linha 18 atual).
    2. Remover também o `isRecording = false;` dentro de `unregisterPttHotkey` e dentro de `changePttHotkey`.
    3. Trocar o callback de `globalShortcut.register`: em vez de alternar localmente, agora envia UM ÚNICO evento `'ptt:action'` sem payload de start/stop — o renderer resolve o toggle consultando o VoiceInputManager e sabe se deve começar (`getCurrentSource() !== 'ptt'`) ou parar (`getCurrentSource() === 'ptt'`).
    4. Nova assinatura do evento IPC: `mainWindow.webContents.send('ptt:action', 'toggle')` (string literal fixa). A decisão start/stop vira responsabilidade do renderer.
    5. `changePttHotkey` adota o mesmo padrão (callback com `send('ptt:action', 'toggle')`).
    6. Manter exports inalterados: `registerPttHotkey`, `changePttHotkey`, `unregisterPttHotkey` — nenhuma mudança de signature pública.
    7. Adicionar comentário no topo do arquivo explicando: `// Phase 22: ownership de mic foi extraída para VoiceInputManager (renderer). Este módulo só emite o toggle event, renderer decide start/stop.`

    **Sub-passo B — Atualizar handler no renderer (`apps/desktop/src/renderer/src/chat/ChatInput.tsx`):**

    1. LER primeiro o arquivo ChatInput.tsx e localizar o handler atual de `ptt:action` (seja via `useEffect` + `window.jarvis.onPttAction` ou via `window.electron.ipcRenderer.on('ptt:action', ...)`).
    2. O handler precisa suportar tanto o payload ANTIGO (`'start' | 'stop'`) quanto o NOVO (`'toggle'`) — porque o preload pode ainda estar exposto como `(action: 'start' | 'stop') => void`. Solução:
       - Alterar o handler para ignorar o payload e sempre consultar `voiceInputManager.getCurrentSource()`.
       - Se `getCurrentSource() === 'ptt'`: release + stopRecording (lado de stop).
       - Caso contrário: `const grant = voiceInputManager.acquire('ptt'); if ('error' in grant) return; startRecording();` (lado de start).
    3. Atualizar `src/shared/ipc-types.ts` para trocar o tipo do payload de `'ptt:action'` de `'start' | 'stop'` para `'toggle'` (ou string literal `'toggle'`). Atualizar `src/preload/index.ts` coerentemente se o tipo for re-exportado.

    **Sub-passo C — Tests ficam verdes:**

    1. Atualizar o test `ptt-hotkey.test.ts` da Task 1:
       - Asserção `webContents.send('ptt:action', 'start')` / `'stop'` → `webContents.send('ptt:action', 'toggle')` nas DUAS invocações (primeira e segunda call).
       - Manter a asserção do regex `not.toMatch(/^let isRecording/m)` — agora deve PASSAR.
    2. Rodar `pnpm --filter @jarvis/desktop test --run src/main/__tests__/ptt-hotkey.test.ts src/renderer/src/voice/__tests__/voiceInputManager.test.ts` → ambos verdes.
    3. Rodar `pnpm --filter @jarvis/desktop test --run` (full desktop suite) — confirmar que nada mais quebrou. Se algum test de ChatInput antigo testava o payload 'start'/'stop', ajustar para 'toggle'.

    Commit: `♻️ refactor(22-01): remove isRecording module-local de ptt-hotkey e delega toggle ao VoiceInputManager no renderer`
  </action>
  <verify>
    <automated>grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts; pnpm --filter @jarvis/desktop test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `0`
    - `grep -c "isRecording" apps/desktop/src/main/ptt-hotkey.ts` retorna `0` (nenhuma referência residual)
    - `grep "ptt:action" apps/desktop/src/main/ptt-hotkey.ts | grep -c "toggle"` retorna pelo menos `2` (uma em registerPttHotkey, uma em changePttHotkey)
    - `grep "voiceInputManager" apps/desktop/src/renderer/src/chat/ChatInput.tsx` retorna pelo menos 1 linha (import ou uso)
    - `grep "acquire('ptt')" apps/desktop/src/renderer/src/chat/ChatInput.tsx` retorna pelo menos 1 linha
    - `grep "release('ptt')" apps/desktop/src/renderer/src/chat/ChatInput.tsx` retorna pelo menos 1 linha
    - `pnpm --filter @jarvis/desktop test --run src/main/__tests__/ptt-hotkey.test.ts` exit code 0
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/__tests__/voiceInputManager.test.ts` exit code 0 (ainda)
    - `pnpm --filter @jarvis/desktop test --run` full suite exit code 0 (zero regressão)
    - `grep -r "wakeWord\|onnxruntime\|openwakeword" apps/desktop/src/main/ptt-hotkey.ts apps/desktop/src/renderer/src/voice/voiceInputManager.ts apps/desktop/src/renderer/src/chat/ChatInput.tsx` retorna vazio (zero contaminação de wake word neste plan)
  </acceptance_criteria>
  <done>ptt-hotkey.ts não tem mais estado module-local, ChatInput consulta VoiceInputManager para toggle, full desktop suite verde, nenhum código de wake word introduzido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 01)

| Boundary | Description |
|----------|-------------|
| main→renderer IPC | `ptt:action` event flui do main (globalShortcut callback) para o renderer — payload era `'start'|'stop'`, agora vira `'toggle'` (string literal fixa, não user-controlled) |
| renderer in-process | `voiceInputManager` singleton vive inteiramente no renderer; state nunca cruza IPC boundary neste plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | `ptt:action` IPC event | accept | Payload é literal string `'toggle'` emitido apenas pelo main em resposta a globalShortcut. Não há input de usuário injetado no payload. |
| T-22-01-02 | Elevation of Privilege | voiceInputManager state | mitigate | Manager vive só no renderer (no IPC → no cross-process escalation). Não aceita args de user input — `acquire`/`release` recebem string literal `'ptt'|'wakeword'` validada por TypeScript types. |
| T-22-01-03 | Denial of Service | Idempotência de acquire | mitigate | Cenário 4-5 do test cobre: chamadas repetidas de `acquire(same)` são NO-OP, não vazam memória nem disparam listeners redundantes. |
| T-22-01-04 | Race Condition (data integrity) | ptt-hotkey.ts old `let isRecording` | mitigate (core) | **Este plano é a mitigação** de PITFALL #3. Removendo o state module-local e centralizando em `voiceInputManager`, eliminamos double-writer — base obrigatória para Plan 04 poder adicionar wake word sem race. |
| T-22-01-05 | Information Disclosure | subscribe listeners | accept | Listeners recebem só `InputSource` (enum de 3 valores) — nenhum áudio, nenhum PII. Baixo valor de target. |
</threat_model>

<verification>
## Phase-Level Checks

- [ ] `pnpm --filter @jarvis/desktop test --run` full suite verde
- [ ] `grep -c "let isRecording" apps/desktop/src/main/ptt-hotkey.ts` === 0
- [ ] `grep -r "wakeWord\|onnxruntime" apps/desktop/src/renderer/src/voice/voiceInputManager.ts apps/desktop/src/main/ptt-hotkey.ts` vazio
- [ ] PTT hotkey ainda funciona em `pnpm dev` (smoke manual opcional — não bloqueante porque suite cobre)
- [ ] Commits seguem formato `♻️ refactor(22-01):` / `✨ feat(22-01):` / `✅ test(22-01):` em pt-BR
</verification>

<success_criteria>
1. Arquivo `apps/desktop/src/renderer/src/voice/voiceInputManager.ts` existe com o contrato exato do `<interfaces>` block
2. 10 cenários de `voiceInputManager.test.ts` verdes
3. 4 cenários de `ptt-hotkey.test.ts` verdes (incluindo o regex negativo de `let isRecording`)
4. `ptt-hotkey.ts` não tem `let isRecording` e emite `'ptt:action', 'toggle'`
5. `ChatInput.tsx` consulta `voiceInputManager.acquire('ptt')` antes de `startRecording`
6. Zero arquivos dentro de `apps/desktop/src/renderer/src/voice/wakeWord/` (dir não existe) — wake word vem nos próximos plans
7. Full desktop test suite verde (nenhuma regressão)
</success_criteria>

<output>
After completion, create `.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-01-SUMMARY.md` with:
- What was refactored (voiceInputManager contract, ptt-hotkey toggle externalization)
- Test coverage (10 + 4 cenários)
- Confirmation that wake word code is ABSENT (grep evidence)
- Next plan: 22-02 (Wake Word Core modules)
</output>
