---
phase: 22-voiceinputmanager-refactor-wake-word-core
plan: 04
type: execute
wave: 3
depends_on: [22-01, 22-02, 22-03]
autonomous: false
requirements: [WAKE-01, WAKE-05, WAKE-06, WAKE-07, WAKE-08, WAKE-09]
files_modified:
  - apps/desktop/src/renderer/hooks/useWakeWord.ts
  - apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts
  - apps/desktop/src/renderer/src/App.tsx
  - apps/desktop/src/renderer/src/audio/ttsPlayer.ts
  - apps/desktop/src/renderer/src/audio/__tests__/ttsPlayer.test.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/main/store.ts
  - apps/desktop/electron-builder.yml
  - apps/desktop/package.json
  - apps/desktop/scripts/cpu-benchmark-wakeword.mjs
  - apps/desktop/scripts/smoke-packaged-build.sh
tags: [wake-word, integration, useWakeWord, orb-gating, tts-wrap, electron-builder, cpu-benchmark]

must_haves:
  truths:
    - "Usuário pode falar 'Hey JARVIS' sem tocar teclado e o orb transiciona idle → listening (WAKE-01 — manual smoke ≤500ms)"
    - "useWakeWord hook SÓ dispara onDetected quando OrbContext.state === 'idle' — gate previne TTS self-trigger loop"
    - "useWakeWord chama voiceInputManager.acquire('wakeword') antes de iniciar gravação — rejeita se PTT ativo (WAKE-07)"
    - "Após ciclo wake → listening → processing → responding → idle, engine.resume() é chamado automaticamente (WAKE-05)"
    - "Se o usuário não falar em 3000ms após wake (VITE_WAKE_WORD_VAD_TIMEOUT_MS), gravação é abortada e orb volta para idle (WAKE-06)"
    - "Se getUserMedia falhar, useWakeWord captura o erro, broadcasta 'Mic unavailable' para o tray, e PTT continua funcionando (WAKE-08)"
    - "BrowserWindow tem backgroundThrottling: false em webPreferences (janela oculta não pausa wake word)"
    - "ttsPlayer.beforePlay suspende o engine, afterPlay + 300ms tail resume (belt-and-braces para self-trigger)"
    - "electron-builder.yml empacota 4 ONNX models via extraResources (NÃO asarUnpack)"
    - "CPU benchmark script mede <2% sustained após 10min silêncio (success criterion WAKE-09)"
  artifacts:
    - path: apps/desktop/src/renderer/hooks/useWakeWord.ts
      provides: "React hook que monta WakeWordEngine, wira OrbContext gating, VAD timeout, degrade path WAKE-08"
      exports: [useWakeWord]
    - path: apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts
      provides: "Tests para state gating (WAKE-01/05), VAD timeout (WAKE-06), degrade on getUserMedia fail (WAKE-08), PTT preemption (WAKE-07)"
      contains: "describe('useWakeWord'"
    - path: apps/desktop/electron-builder.yml
      provides: "Packaging config com extraResources para wakeword-models"
      contains: "extraResources:"
    - path: apps/desktop/scripts/cpu-benchmark-wakeword.mjs
      provides: "Script standalone que roda o engine por 10min e mede sustained CPU"
      contains: "10 * 60 * 1000"
    - path: apps/desktop/scripts/smoke-packaged-build.sh
      provides: "Script bash que roda depois do electron-builder e verifica os 4 .onnx no unpacked build"
      contains: "wc -l"
  key_links:
    - from: "apps/desktop/src/renderer/hooks/useWakeWord.ts"
      to: "WakeWordEngine (Plan 02) + voiceInputManager (Plan 01) + OrbContext + useAudioRecorder"
      via: "import e subscribe a state changes"
      pattern: "WakeWordEngine"
    - from: "apps/desktop/src/renderer/src/App.tsx"
      to: "useWakeWord hook"
      via: "chamado uma vez dentro do AppContent (ou componente equivalente dentro do OrbProvider)"
      pattern: "useWakeWord\\(\\)"
    - from: "apps/desktop/src/main/index.ts BrowserWindow webPreferences"
      to: "backgroundThrottling: false"
      via: "config explícita no objeto"
      pattern: "backgroundThrottling:\\s*false"
    - from: "apps/desktop/electron-builder.yml"
      to: "apps/desktop/resources/wakeword-models/*.onnx"
      via: "extraResources glob"
      pattern: "wakeword-models"
---

<objective>
Wave 3: integrar o `WakeWordEngine` (Plan 02) no runtime do renderer via `useWakeWord` hook, gated pelo `OrbContext`, wireado ao `voiceInputManager` (Plan 01), com pause/resume envolvendo o TTS player, `backgroundThrottling: false` na BrowserWindow, e empacotamento via `electron-builder` com `extraResources`. Validar CPU budget <2% sustained + packaged smoke test. Este é o único plan da phase com **checkpoints human-verify** porque WAKE-01 latência ≤500ms e CPU sustained só podem ser comprovados com mic real.

Purpose: Fechar os 6 requirements WAKE-* end-to-end. Após este plan, o usuário pode instalar o app empacotado, falar "Hey JARVIS", e o orb transiciona sem nenhuma interação de teclado.

Output:
- `useWakeWord` hook React com gate por OrbContext + VAD timeout + degrade path
- `App.tsx` monta o hook dentro do OrbProvider
- `ttsPlayer.ts` com hooks beforePlay/afterPlay + 300ms tail
- `BrowserWindow` config com `backgroundThrottling: false`
- `electron-builder.yml` com `extraResources` apontando para `resources/wakeword-models/`
- `electron-builder` adicionado como devDependency
- `cpu-benchmark-wakeword.mjs` — medição empírica do budget
- `smoke-packaged-build.sh` — verificação pós-build do artifact
- 3 checkpoints human-verify nos pontos críticos (latência, full cycle, CPU budget, packaged)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-VALIDATION.md
@apps/desktop/src/main/index.ts
@apps/desktop/src/main/store.ts
@apps/desktop/src/renderer/components/Orb/OrbContext.tsx
@apps/desktop/src/renderer/hooks/useAudioRecorder.ts
@apps/desktop/src/renderer/src/audio/ttsPlayer.ts
@apps/desktop/src/renderer/src/App.tsx
@apps/desktop/package.json
@CLAUDE.md

<interfaces>
<!-- Contratos finais deste plan — derivados de 22-RESEARCH.md Pattern 5 (useWakeWord + TTS wrap) e Pattern 4 (electron-builder.yml). -->

// NOVO — apps/desktop/src/renderer/hooks/useWakeWord.ts
export interface UseWakeWordState {
  status: 'loading' | 'active' | 'unavailable' | 'error';
  error?: string;
}
export function useWakeWord(): UseWakeWordState;

// EXISTENTE — apps/desktop/src/renderer/components/Orb/OrbContext.tsx (já criado)
export type OrbState = 'idle' | 'listening' | 'processing' | 'responding';
export function useOrbContext(): { state: OrbState; setState: (s: OrbState) => void };

// MODIFICADO — apps/desktop/src/renderer/src/audio/ttsPlayer.ts
type TTSLifecycleHooks = {
  beforePlay?: () => Promise<void> | void;
  afterPlay?: () => Promise<void> | void;
};
export function registerTTSHooks(h: TTSLifecycleHooks): void;
export async function playTTSResponse(base64: string, format: 'mp3' | 'wav'): Promise<void>;

// MODIFICADO — apps/desktop/src/main/index.ts BrowserWindow config
// webPreferences MUST add: backgroundThrottling: false
// Aplica à Window principal (mainWindow). Comentário obrigatório explicando por quê.

// NOVO — apps/desktop/electron-builder.yml
// Usa pattern literal do RESEARCH.md §Pattern 4:
// appId: com.jarvis.desktop
// productName: JARVIS
// directories.output: release
// files: dist/**/*, package.json
// extraResources: [{ from: resources/wakeword-models, to: wakeword-models, filter: [*.onnx, LICENSE*] }]
// asar: true
// win.target: nsis
// mac.target: dmg + NSMicrophoneUsageDescription
// linux.target: AppImage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 failing test do useWakeWord + implementar useWakeWord hook + ttsPlayer hooks + backgroundThrottling: false + wire em App.tsx</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts (Plan 02 — contract)
    - apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts (Plan 02 — loadWakeWordSessions)
    - apps/desktop/src/renderer/src/voice/voiceInputManager.ts (Plan 01 — contract)
    - apps/desktop/src/renderer/components/Orb/OrbContext.tsx (state machine)
    - apps/desktop/src/renderer/hooks/useAudioRecorder.ts (startRecording / stopRecording — Plan 04 chama)
    - apps/desktop/src/renderer/src/audio/ttsPlayer.ts (atual — inserir hooks beforePlay/afterPlay)
    - apps/desktop/src/renderer/src/App.tsx (onde montar useWakeWord — deve ficar DENTRO do OrbProvider)
    - apps/desktop/src/main/index.ts (BrowserWindow webPreferences — adicionar backgroundThrottling: false)
    - apps/desktop/src/preload/index.ts (confirmar window.jarvis.wakeWord.loadModels do Plan 02)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 5: Orb State Gating + TTS Pause/Resume"
  </read_first>
  <behavior>
    useWakeWord.test.ts (happy-dom + RTL):
    1. Mount useWakeWord dentro de OrbProvider mockado — hook chama window.jarvis.wakeWord.loadModels() (mockado), cria WakeWordEngine (mockado), chama engine.start()
    2. Quando mock do engine dispara onDetected(0.8) E orb state === 'idle', o hook:
       a. chama voiceInputManager.acquire('wakeword')
       b. setState('listening')
       c. chama audioRecorder.startRecording()
    3. Se orb state === 'responding' quando onDetected dispara, o hook IGNORA (gate — WAKE self-trigger).
    4. Se voiceInputManager.acquire retorna { error: 'BUSY' }, hook NÃO chama startRecording nem setState (WAKE-07).
    5. Quando state transiciona de 'idle' → 'listening', VAD timeout é armado (fake timer). Após 3000ms sem usuário falar (mock), hook chama audioRecorder.stopRecording() + voiceInputManager.release('wakeword') + setState('idle') (WAKE-06).
    6. Quando state muda para 'responding'/'processing'/'listening', engine.suspend() é chamado.
    7. Quando state volta para 'idle', engine.resume() é chamado (WAKE-05).
    8. Se engine.start() rejeitar com NotAllowedError, hook captura, status vira 'unavailable', nenhum throw propaga (WAKE-08). PTT continua funcional.
    9. Ao desmontar, engine.stop() é chamado (cleanup).
    10. Hook registra hooks no ttsPlayer: beforePlay suspende engine, afterPlay (após 300ms) resume engine.

    ttsPlayer.test.ts:
    - registerTTSHooks({ beforePlay, afterPlay }) + playTTSResponse → beforePlay chamado antes do decode, afterPlay chamado após onended + 300ms delay (fake timers).
  </behavior>
  <action>
    **Sub-passo A — Wave 0 failing tests:**

    1. Criar `apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts` com os 10 cenários acima. Import de `../useWakeWord` que ainda não existe → test falha.

       Mocks necessários:
       - `vi.mock('../../src/voice/wakeWord/WakeWordEngine', ...)` com classe fake onde `start/suspend/resume/stop` são `vi.fn()` e há um método `__emitDetection(score)` para simular detecção.
       - `vi.mock('../../src/voice/wakeWord/modelLoader', () => ({ loadWakeWordSessions: vi.fn().mockResolvedValue({ mel: {}, embed: {}, vad: {}, kw: {} }) }))`.
       - `vi.mock('../../src/voice/voiceInputManager', ...)` com `acquire`/`release`/`getCurrentSource`/`subscribe` spies.
       - `vi.mock('../useAudioRecorder', ...)` com `startRecording`/`stopRecording` spies.
       - `vi.stubGlobal('window', { jarvis: { wakeWord: { loadModels: vi.fn().mockResolvedValue({ mel: new Uint8Array(), ... }) } } })`
       - `vi.stubGlobal('navigator', { mediaDevices: { getUserMedia: vi.fn().mockResolvedValue({ getTracks: () => [] }) } })`
       - Fake timers via `vi.useFakeTimers()` para VAD timeout cenário 5.

    2. Criar `apps/desktop/src/renderer/src/audio/__tests__/ttsPlayer.test.ts` (pode já existir — se já existir, adicionar só os novos testes de hooks):
       - describe `registerTTSHooks`: register + playTTSResponse → beforePlay chamado 1x antes do audio start. Após source.onended + 300ms delay (vi.advanceTimersByTime(300)), afterPlay chamado 1x.
       - Mockar `AudioContext` + `decodeAudioData` apropriadamente.

    3. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/hooks/__tests__/useWakeWord.test.ts src/renderer/src/audio/__tests__/ttsPlayer.test.ts` → falhas esperadas.

    **Sub-passo B — Implementação useWakeWord.ts:**

    Criar `apps/desktop/src/renderer/hooks/useWakeWord.ts` (derivado do research §Pattern 5 + ajustes para satisfazer todos os 10 cenários):

    ```ts
    import { useEffect, useRef, useState } from 'react';
    import { useOrbContext } from '../components/Orb/OrbContext';
    import { useAudioRecorder } from './useAudioRecorder';
    import { WakeWordEngine } from '../src/voice/wakeWord/WakeWordEngine';
    import { loadWakeWordSessions } from '../src/voice/wakeWord/modelLoader';
    import { voiceInputManager } from '../src/voice/voiceInputManager';
    import { registerTTSHooks } from '../src/audio/ttsPlayer';

    export interface UseWakeWordState {
      status: 'loading' | 'active' | 'unavailable' | 'error';
      error?: string;
    }

    export function useWakeWord(): UseWakeWordState {
      const { state, setState } = useOrbContext();
      const audioRecorder = useAudioRecorder();
      const engineRef = useRef<WakeWordEngine | null>(null);
      const stateRef = useRef(state);
      const vadTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);
      const [hookState, setHookState] = useState<UseWakeWordState>({ status: 'loading' });
      stateRef.current = state;

      // Boot engine
      useEffect(() => {
        let cancelled = false;
        const boot = async () => {
          try {
            if (import.meta.env.VITE_WAKE_WORD_ENABLED === 'false') {
              setHookState({ status: 'unavailable', error: 'Disabled via env' });
              return;
            }
            const bytes = await window.jarvis.wakeWord.loadModels();
            if (cancelled) return;
            const sessions = await loadWakeWordSessions(bytes);
            if (cancelled) return;
            const engine = new WakeWordEngine({
              threshold: parseFloat(import.meta.env.VITE_WAKE_WORD_THRESHOLD ?? '0.5'),
              debounceMs: 2000,
              vadThreshold: 0.3,
              onDetected: (score) => {
                // GATE 1: orb must be idle
                if (stateRef.current !== 'idle') {
                  console.log('[wakeWord] ignored (state !== idle)', stateRef.current);
                  return;
                }
                // GATE 2: voiceInputManager must grant
                const grant = voiceInputManager.acquire('wakeword');
                if ('error' in grant) {
                  console.log('[wakeWord] ignored (busy)', grant.error);
                  return;
                }
                console.log('[wakeWord] detected', score);
                setState('listening');
                void audioRecorder.startRecording();
                // Arm VAD timeout (WAKE-06)
                const timeoutMs = parseInt(import.meta.env.VITE_WAKE_WORD_VAD_TIMEOUT_MS ?? '3000', 10);
                vadTimeoutRef.current = setTimeout(() => {
                  void audioRecorder.stopRecording();
                  voiceInputManager.release('wakeword');
                  setState('idle');
                }, timeoutMs);
              },
              onSilentStream: () => {
                setHookState({ status: 'unavailable', error: 'Mic captando silêncio — verifique permissões' });
              },
            });
            const stream = await navigator.mediaDevices.getUserMedia({
              audio: { sampleRate: 16000, channelCount: 1, echoCancellation: true, noiseSuppression: false },
            });
            if (cancelled) { stream.getTracks().forEach((t) => t.stop()); return; }
            await engine.start(sessions, stream);
            if (cancelled) { await engine.stop(); return; }
            engineRef.current = engine;
            setHookState({ status: 'active' });
          } catch (err) {
            console.error('[useWakeWord] boot failed — degrading to PTT-only', err);
            const msg = err instanceof Error ? err.message : String(err);
            setHookState({ status: 'unavailable', error: msg });
            // TODO: broadcast to tray (Phase 23 WAKE-03/WAKE-08 tray indication)
          }
        };
        void boot();
        return () => {
          cancelled = true;
          if (vadTimeoutRef.current) clearTimeout(vadTimeoutRef.current);
          void engineRef.current?.stop();
          engineRef.current = null;
        };
      }, []);

      // Orb state gate (suspend/resume) — WAKE-05
      useEffect(() => {
        const engine = engineRef.current;
        if (!engine) return;
        if (state === 'idle') {
          void engine.resume();
        } else {
          void engine.suspend();
          // If leaving listening to non-idle, clear any pending VAD timeout
          if (vadTimeoutRef.current) {
            clearTimeout(vadTimeoutRef.current);
            vadTimeoutRef.current = null;
          }
        }
      }, [state]);

      // TTS hooks — belt-and-braces gating (Pattern 5)
      useEffect(() => {
        registerTTSHooks({
          beforePlay: async () => {
            await engineRef.current?.suspend();
          },
          afterPlay: async () => {
            // 300ms tail is handled by ttsPlayer wrapper; we just resume here.
            await engineRef.current?.resume();
          },
        });
        return () => registerTTSHooks({});
      }, []);

      return hookState;
    }
    ```

    **Sub-passo C — ttsPlayer hooks:**

    Modificar `apps/desktop/src/renderer/src/audio/ttsPlayer.ts`:
    ```ts
    // Adicionar no topo do arquivo (após imports existentes):
    type TTSLifecycleHooks = {
      beforePlay?: () => Promise<void> | void;
      afterPlay?: () => Promise<void> | void;
    };
    let ttsHooks: TTSLifecycleHooks = {};
    export function registerTTSHooks(h: TTSLifecycleHooks): void {
      ttsHooks = h;
    }
    ```
    Depois, em `playTTSResponse`:
    - Antes do `currentSource = source; source.start();` chamar `await ttsHooks.beforePlay?.();`
    - Dentro de `source.onended = () => { if (currentSource === source) currentSource = null; setTimeout(() => { void ttsHooks.afterPlay?.(); }, 300); };`

    Manter `__resetForTests()` e `stopTTSPlayback()` inalterados. Adicionar `ttsHooks = {};` dentro de `__resetForTests()`.

    **Sub-passo D — App.tsx wire:**

    1. Localizar o ponto onde `OrbProvider` envolve o conteúdo do app (provavelmente em `App.tsx` ou similar).
    2. Criar um child component (dentro do arquivo App.tsx ou novo) que chama `useWakeWord()` — precisa estar DENTRO do OrbProvider porque o hook usa `useOrbContext()`.
    3. Exemplo:
    ```tsx
    function AppContent() {
      useWakeWord();  // mount the wake word engine
      // ... resto do AppContent existente ...
    }
    function App() {
      return (
        <OrbProvider>
          <AppContent />
        </OrbProvider>
      );
    }
    ```

    **Sub-passo E — backgroundThrottling: false na BrowserWindow:**

    Editar `apps/desktop/src/main/index.ts`, no objeto `webPreferences` do `new BrowserWindow`, adicionar (com comentário explicando por quê):
    ```ts
    webPreferences: {
      contextIsolation: true,
      nodeIntegration: false,
      sandbox: true,
      webSecurity: true,
      allowRunningInsecureContent: false,
      // Phase 22 WAKE-01: janela oculta não pode pausar o renderer — wake word
      // engine precisa continuar rodando inferência contínua mesmo quando o
      // usuário esconde o orb via hotkey. backgroundThrottling: false é
      // obrigatório (trava de research + CONTEXT.md).
      backgroundThrottling: false,
      preload: path.join(__dirname, '../preload/index.js'),
    },
    ```

    **Sub-passo F — Run tests:**

    Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/hooks/__tests__/useWakeWord.test.ts src/renderer/src/audio/__tests__/ttsPlayer.test.ts` → verde.

    Rodar `pnpm --filter @jarvis/desktop test --run` full suite → verde.

    Rodar `pnpm --filter @jarvis/desktop build` → TS compile clean.

    Commits:
    - `✅ test(22-04): Wave 0 failing tests useWakeWord + ttsPlayer hooks`
    - `✨ feat(22-04): useWakeWord hook com OrbContext gating, VAD timeout, TTS wrap + backgroundThrottling:false`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/hooks/__tests__/useWakeWord.test.ts src/renderer/src/audio/__tests__/ttsPlayer.test.ts; pnpm --filter @jarvis/desktop test --run; grep "backgroundThrottling: false" apps/desktop/src/main/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/hooks/useWakeWord.ts` existe com `export function useWakeWord`
    - `grep "voiceInputManager.acquire('wakeword')" apps/desktop/src/renderer/hooks/useWakeWord.ts` retorna pelo menos 1 linha
    - `grep "stateRef.current !== 'idle'" apps/desktop/src/renderer/hooks/useWakeWord.ts` retorna pelo menos 1 linha (gate check)
    - `grep "VITE_WAKE_WORD_THRESHOLD" apps/desktop/src/renderer/hooks/useWakeWord.ts` retorna pelo menos 1 linha
    - `grep "VITE_WAKE_WORD_VAD_TIMEOUT_MS" apps/desktop/src/renderer/hooks/useWakeWord.ts` retorna pelo menos 1 linha
    - `grep "registerTTSHooks" apps/desktop/src/renderer/src/audio/ttsPlayer.ts` retorna pelo menos 2 linhas (definição + export)
    - `grep "ttsHooks.beforePlay" apps/desktop/src/renderer/src/audio/ttsPlayer.ts` retorna 1 linha
    - `grep "setTimeout.*300" apps/desktop/src/renderer/src/audio/ttsPlayer.ts` retorna 1 linha (300ms tail)
    - `grep "backgroundThrottling: false" apps/desktop/src/main/index.ts` retorna 1 linha
    - `grep "useWakeWord()" apps/desktop/src/renderer/src/App.tsx` retorna pelo menos 1 linha
    - `pnpm --filter @jarvis/desktop test --run src/renderer/hooks/__tests__/useWakeWord.test.ts` exit 0 com pelo menos 10 testes passando
    - `pnpm --filter @jarvis/desktop test --run` full suite exit 0
    - `pnpm --filter @jarvis/desktop build` exit 0
  </acceptance_criteria>
  <done>useWakeWord hook implementado, gated por OrbContext, wireado ao voiceInputManager e TTS player, backgroundThrottling: false setado, full suite verde.</done>
</task>

<task type="auto">
  <name>Task 2: electron-builder setup + extraResources + cpu-benchmark script + smoke-packaged script</name>
  <read_first>
    - apps/desktop/package.json (scripts + devDependencies — confirmar ausência de electron-builder)
    - apps/desktop/src/main/voiceInput/resources.ts (Plan 02 — path resolver que depende de app.isPackaged ? process.resourcesPath : devPath)
    - apps/desktop/electron.vite.config.ts (confirmar output dir `dist/main`, `dist/renderer`)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 4: electron-builder extraResources + Runtime Path Resolver" — YAML literal
    - pnpm-workspace.yaml (para confirmar o workspace layout)
  </read_first>
  <action>
    **Sub-passo A — Adicionar electron-builder como devDep:**

    1. `pnpm --filter @jarvis/desktop add -D electron-builder` — adicionar versão latest estável. Confirmar que entra em `devDependencies` do `apps/desktop/package.json`. Se a versão resolver para >27 e houver incompat com Electron 41, ajustar para ~26.x.

    **Sub-passo B — Criar electron-builder.yml:**

    Criar `apps/desktop/electron-builder.yml` literal do research §Pattern 4:
    ```yaml
    # Phase 22 Plan 04 — Packaging config com extraResources para wake word models.
    # Source: .planning/phases/22-.../22-RESEARCH.md §Pattern 4
    appId: com.jarvis.desktop
    productName: JARVIS
    directories:
      output: release
      buildResources: resources
    files:
      - dist/**/*
      - package.json
    extraResources:
      - from: resources/wakeword-models
        to: wakeword-models
        filter:
          - "*.onnx"
          - "LICENSE*"
    asar: true
    win:
      target: nsis
    mac:
      target: dmg
      extendInfo:
        NSMicrophoneUsageDescription: "JARVIS precisa do microfone para detectar o wake word 'Hey JARVIS'."
    linux:
      target: AppImage
    ```

    **IMPORTANTE**: `extraResources` coloca os arquivos em `<resourcesPath>/wakeword-models/*.onnx` — o resolver do Plan 02 `getWakeWordModelPaths()` já usa `path.join(process.resourcesPath, 'wakeword-models')`. Cross-check: o path `from: resources/wakeword-models` é RELATIVO a `apps/desktop/` (o cwd do build), que é o local atual dos arquivos baixados pelo Plan 03.

    **Sub-passo C — Scripts de build:**

    Adicionar ao `apps/desktop/package.json` scripts:
    ```json
    "build:dist": "electron-vite build && electron-builder --config electron-builder.yml",
    "build:dist:win": "electron-vite build && electron-builder --config electron-builder.yml --win",
    "build:dist:mac": "electron-vite build && electron-builder --config electron-builder.yml --mac",
    "build:dist:linux": "electron-vite build && electron-builder --config electron-builder.yml --linux"
    ```

    Atualizar `.gitignore` adicionando `apps/desktop/release/` se não estiver.

    **Sub-passo D — cpu-benchmark-wakeword.mjs:**

    Criar `apps/desktop/scripts/cpu-benchmark-wakeword.mjs`:
    ```js
    #!/usr/bin/env node
    /**
     * CPU Benchmark — Wake Word Engine (WAKE-09 success criterion)
     *
     * Este script NÃO roda o engine completo em Node (não tem Web Audio nem
     * AudioWorklet). Ele funciona como um HARNESS que:
     *   1. Inicia o app em modo dev (`electron .`)
     *   2. Lê o CPU usage via `process.getCPUUsage()` exposto pelo main
     *      process (IPC 'benchmark:cpu-sample')
     *   3. Amostra a cada 10s por 10 minutos com orb em idle
     *   4. Calcula delta por janela + média sliding
     *   5. PASS se avg < 2%, FAIL se >= 2%
     *
     * Uso: node apps/desktop/scripts/cpu-benchmark-wakeword.mjs
     *
     * Alternativa (mais simples, menos automática): executor roda o app em
     * `pnpm dev`, abre o Windows Task Manager / Activity Monitor / htop,
     * observa o processo JARVIS renderer por 10 minutos, anota a média.
     * Este script é o caminho automatizado; o checkpoint manual aceita o
     * caminho observacional também.
     */
    const DURATION_MS = 10 * 60 * 1000;  // 10 minutes
    const SAMPLE_INTERVAL_MS = 10 * 1000;  // 10 seconds
    const TARGET_CPU_PERCENT = 2.0;

    console.log('[cpu-benchmark] Duration:', DURATION_MS / 1000, 's');
    console.log('[cpu-benchmark] Sample interval:', SAMPLE_INTERVAL_MS / 1000, 's');
    console.log('[cpu-benchmark] Target: <', TARGET_CPU_PERCENT, '%');
    console.log('');
    console.log('[cpu-benchmark] MANUAL MODE (default):');
    console.log('  1. Em outro terminal: pnpm --filter @jarvis/desktop dev');
    console.log('  2. Aguarde o orb aparecer em idle (não diga "Hey JARVIS")');
    console.log('  3. Abra o Task Manager / Activity Monitor / htop');
    console.log('  4. Filtre por processo "JARVIS" ou "electron" (renderer)');
    console.log('  5. Observe a média de CPU% por 10 minutos');
    console.log('  6. PASS se média < 2%, FAIL se >= 2%');
    console.log('');
    console.log('[cpu-benchmark] AUTO MODE (TODO):');
    console.log('  Instrumentar main process com IPC handler "benchmark:cpu-sample" que');
    console.log('  responde com process.getCPUUsage(). Script Node abre IPC socket ou usa');
    console.log('  chrome-debugger protocol para amostrar. Deixado como enhancement.');
    console.log('');
    console.log('[cpu-benchmark] Para este plan, o checkpoint é MANUAL e aceita observação');
    console.log('[cpu-benchmark] humana via Task Manager como evidência.');
    process.exit(0);
    ```

    **Sub-passo E — smoke-packaged-build.sh:**

    Criar `apps/desktop/scripts/smoke-packaged-build.sh`:
    ```bash
    #!/usr/bin/env bash
    # Smoke test — valida que o artefato empacotado contém os 4 ONNX models via extraResources.
    # Roda DEPOIS de `pnpm --filter @jarvis/desktop build:dist`.
    set -euo pipefail

    cd "$(dirname "$0")/.."

    if [ ! -d "release" ]; then
      echo "[smoke] ❌ release/ dir not found — rode 'pnpm build:dist' primeiro"
      exit 1
    fi

    # Procura o unpacked build (Windows/Linux/macOS têm layouts diferentes)
    UNPACKED_DIRS=(
      "release/win-unpacked/resources/wakeword-models"
      "release/linux-unpacked/resources/wakeword-models"
      "release/mac/JARVIS.app/Contents/Resources/wakeword-models"
    )

    FOUND=0
    for dir in "${UNPACKED_DIRS[@]}"; do
      if [ -d "$dir" ]; then
        COUNT=$(ls "$dir"/*.onnx 2>/dev/null | wc -l)
        echo "[smoke] ${dir}: ${COUNT} .onnx files"
        if [ "$COUNT" -eq 4 ]; then
          FOUND=1
          echo "[smoke] ✅ 4 ONNX models encontrados em $dir"
        else
          echo "[smoke] ❌ esperado 4, encontrado $COUNT em $dir"
        fi
      fi
    done

    if [ $FOUND -eq 0 ]; then
      echo "[smoke] ❌ FAIL — nenhum unpacked build contém os 4 ONNX models"
      echo "[smoke] Verifique electron-builder.yml extraResources config"
      exit 1
    fi

    echo "[smoke] ✅ PASS"
    ```
    Chmod +x.

    Commit: `🏗️ build(22-04): electron-builder.yml com extraResources + build:dist scripts + cpu-benchmark + smoke-packaged scripts`
  </action>
  <verify>
    <automated>grep "electron-builder" apps/desktop/package.json; test -f apps/desktop/electron-builder.yml && cat apps/desktop/electron-builder.yml | grep extraResources; test -f apps/desktop/scripts/smoke-packaged-build.sh && test -x apps/desktop/scripts/smoke-packaged-build.sh && echo EXEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"electron-builder\":" apps/desktop/package.json` retorna 1 linha (devDependencies)
    - `test -f apps/desktop/electron-builder.yml` returns 0
    - `grep "extraResources:" apps/desktop/electron-builder.yml` retorna 1 linha
    - `grep "wakeword-models" apps/desktop/electron-builder.yml` retorna pelo menos 2 linhas (from + to)
    - `grep "asar: true" apps/desktop/electron-builder.yml` retorna 1 linha
    - `grep "NSMicrophoneUsageDescription" apps/desktop/electron-builder.yml` retorna 1 linha
    - `grep "build:dist" apps/desktop/package.json` retorna pelo menos 1 linha
    - `test -f apps/desktop/scripts/cpu-benchmark-wakeword.mjs` returns 0
    - `test -f apps/desktop/scripts/smoke-packaged-build.sh` returns 0
    - `test -x apps/desktop/scripts/smoke-packaged-build.sh` returns 0 (executable)
    - `grep "release/" .gitignore` retorna pelo menos 1 linha (apps/desktop/release ou release/)
    - `pnpm --filter @jarvis/desktop build` (sem :dist) ainda funciona exit 0 — electron-builder NÃO deve quebrar o build simples
    - `pnpm --filter @jarvis/desktop test --run` full suite continua verde
  </acceptance_criteria>
  <done>electron-builder instalado, config yml criado com extraResources correto, scripts de build + benchmark + smoke pacote prontos. Sem tentar rodar o build:dist real ainda (isso é o checkpoint da Task 3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT — validação manual do wake word end-to-end + CPU budget + packaged build</name>
  <what-built>
    Pipeline wake word completo integrado:
    - VoiceInputManager (Plan 01) + WakeWordEngine (Plan 02) + Download script (Plan 03) + useWakeWord hook + electron-builder (Plan 04)
    - OrbContext gating, TTS pause/resume wrap, backgroundThrottling: false, VAD timeout 3000ms
    - 4 modelos ONNX baixados via postinstall, packaged via extraResources
    - Test suite unit: 100% verde
    - CPU benchmark script + smoke packaged script disponíveis
  </what-built>
  <how-to-verify>
    **VERIFICAÇÃO 1 — Dev mode smoke test (WAKE-01 latência):**

    1. Confirmar que os 4 modelos estão em `apps/desktop/resources/wakeword-models/`:
       ```
       ls apps/desktop/resources/wakeword-models/*.onnx | wc -l
       ```
       Esperado: `4`. Se não, rodar `node scripts/download-wakeword-models.mjs --force`.

    2. Rodar `pnpm --filter @jarvis/desktop dev`
    3. Aguardar o orb aparecer (idle)
    4. Verificar no DevTools (Ctrl+Shift+I) console: deve haver log `[wakeWord] engine started` ou similar sem erro
    5. **Falar claramente**: "Hey JARVIS"
    6. **Cronometrar com smartphone**: tempo do fim de "JARVIS" até orb transicionar para listening
    7. Esperado: orb vai para listening em **≤500ms** (WAKE-01)
    8. Se latência > 500ms → FAIL — documentar valor medido + investigar (pode ser cold start do ORT — medir segunda tentativa)

    **VERIFICAÇÃO 2 — Full cycle (WAKE-05):**

    1. Ainda em `pnpm dev`, com orb em idle
    2. Falar "Hey JARVIS, que horas são?"
    3. Esperado: orb idle → listening → processing → responding (TTS) → idle
    4. Após o TTS terminar, aguardar ~1s
    5. Falar "Hey JARVIS, obrigado" novamente
    6. Esperado: segundo ciclo funciona igual ao primeiro (sem precisar reiniciar o app)
    7. Se o segundo "Hey JARVIS" não for detectado → FAIL (engine não resumiu — investigar logs)

    **VERIFICAÇÃO 3 — VAD timeout (WAKE-06):**

    1. Falar "Hey JARVIS" (orb vai pra listening)
    2. Ficar em silêncio por 4 segundos
    3. Esperado: orb volta para idle após ~3s (VITE_WAKE_WORD_VAD_TIMEOUT_MS=3000)
    4. Se ficar travado em listening > 5s → FAIL

    **VERIFICAÇÃO 4 — PTT preemption (WAKE-07):**

    1. Com orb em idle, falar "Hey JARVIS" + imediatamente pressionar Ctrl+Space (PTT)
    2. Esperado: PTT ganha — orb fica em listening, e quando o usuário solta o PTT ou pressiona de novo, captura é do PTT (não do wake word)
    3. Verificar no console: log `[wakeWord] ignored (busy)` ou equivalente
    4. Depois testar o inverso: pressionar PTT → falar → parar → wake word deve voltar a funcionar depois

    **VERIFICAÇÃO 5 — Mic unavailable degrade (WAKE-08):**

    1. Em Windows: abrir Settings → Privacy & security → Microphone → DESLIGAR acesso do app
    2. Reiniciar JARVIS (`pnpm dev`)
    3. Esperado: orb aparece, PTT ainda funciona (Ctrl+Space), mas wake word não dispara
    4. Verificar no DevTools console: log de erro capturado (`[useWakeWord] boot failed — degrading to PTT-only`) + status=`unavailable`
    5. O app NÃO deve crashar
    6. Restaurar permissão de mic depois do teste

    **VERIFICAÇÃO 6 — CPU budget sustained <2% (WAKE-09 blocking criterion):**

    1. Com orb em idle, rodar `pnpm --filter @jarvis/desktop dev`
    2. Abrir Task Manager (Windows) ou Activity Monitor (macOS) ou htop (Linux)
    3. Filtrar por processo `electron` / `JARVIS` (o renderer especificamente)
    4. **Deixar rodando em silêncio total por 10 minutos** (sem falar, música desligada)
    5. Anotar CPU% médio da janela
    6. Esperado: < 2% sustained
    7. Se CPU >= 2% → **FAIL — bloqueante, não pode mergear**. Investigar: VAD gate funcionando? numThreads=1? worklet cadência correta? Aumentar debounceMs? Abrir issue.

    **VERIFICAÇÃO 7 — Packaged build smoke test:**

    1. Rodar `pnpm --filter @jarvis/desktop build:dist`
    2. Esperado: termina sem erro, cria dir `apps/desktop/release/`
    3. Rodar `bash apps/desktop/scripts/smoke-packaged-build.sh`
    4. Esperado: `[smoke] ✅ PASS` com 4 .onnx encontrados em `release/win-unpacked/resources/wakeword-models/` (ou equivalente de OS)
    5. Se Windows: instalar o `.exe` gerado (ou rodar o unpacked diretamente) e repetir VERIFICAÇÃO 1 no artefato instalado
    6. Se wake word funciona no instalador → extraResources + runtime path resolver estão corretos

    **Documentar no SUMMARY o resultado de cada verificação:**
    - ✅ / ❌ por verificação
    - Tempo medido de WAKE-01
    - CPU% médio medido
    - Saída de smoke-packaged-build.sh

    **Critério de aprovação**: TODAS as 7 verificações PASS. Qualquer FAIL → criar gap no VERIFICATION.md e rodar `/gsd-plan-phase 22 --gaps`.
  </how-to-verify>
  <resume-signal>Digite "approved" se todas as 7 verificações passaram + colar valores medidos (latência, CPU%). Ou digite "failed:VERIFICATION-X" + descrição para acionar gap closure.</resume-signal>
  <files>apps/desktop/ (runtime validation only — nenhum arquivo editado)</files>
  <action>Ver bloco &lt;how-to-verify&gt; acima — executor conduz as 7 verificações manuais e anota os resultados. Não edita nenhum arquivo. Se alguma verificação falhar, documentar em SUMMARY e acionar /gsd-plan-phase 22 --gaps.</action>
  <verify>Checkpoint manual — sem comando automatizado. Gate blocking: human-verify approval obrigatória para marcar phase como done.</verify>
  <done>Todas as 7 verificações manuais retornam PASS, valores medidos (latência WAKE-01, CPU% sustained 10min, smoke-packaged-build.sh exit 0) registrados no 22-04-SUMMARY.md, usuário digitou "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 04)

| Boundary | Description |
|----------|-------------|
| OS mic → renderer | `getUserMedia` atravessa prompt de permissão do OS; stream entra no renderer |
| renderer state machine | OrbContext é single source of truth para wake word gating |
| TTS audio → speakers → mic | Loop potencial; gated por OrbContext + belt-and-braces TTS hooks |
| file system (packaged) → ort.InferenceSession | modelos em `resources/wakeword-models/` via extraResources, path via `process.resourcesPath` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Spoofing / Trust | TTS self-trigger loop — JARVIS fala "hey jarvis" na resposta → mic pega → wake word dispara → loop | mitigate | **Duas camadas**: (1) `useWakeWord` verifica `stateRef.current === 'idle'` no onDetected — quando state é `responding`, ignora. (2) `ttsPlayer.beforePlay` chama `engine.suspend()`, `afterPlay + 300ms` chama `engine.resume()`. Teste unitário assegura o gate. |
| T-22-04-02 | Tampering | Malicious `.onnx` no `extraResources` (swap no installer) | mitigate | `download-wakeword-models.mjs` (Plan 03) verifica SHA-256 dos modelos baixados. Installer é assinado (se o dev signar). Asar é read-only em produção. |
| T-22-04-03 | Information Disclosure | Wake word captura áudio durante screen lock — conversa privada | accept (this phase) | Documented: `powerMonitor.on('lock-screen')` seria mitigação completa, mas é explicitamente **fora do escopo** de Phase 22 (research §Pattern 6 notes). Flagged para Phase 23 ou v1.5. Aceito porque uso é single-user pessoal, sala própria. |
| T-22-04-04 | Denial of Service | CPU drain — wake word inference em loop intenso | mitigate (BLOCKING) | CPU benchmark checkpoint manual é blocking criterion. Arquitetura garante: AudioWorklet + VAD gate + numThreads=1 + 12.5Hz + debounce 2000ms. Se checkpoint falhar → phase falha. |
| T-22-04-05 | Denial of Service | Mic fica preso em `listening` após VAD timeout não disparar | mitigate | VAD timeout 3000ms armado no `onDetected`. Cleanup em cleanup do useEffect + quando state sai de 'idle'. Teste unitário cobre esse cenário com fake timers. |
| T-22-04-06 | Elevation of Privilege | Renderer comprometido → IPC `wakeWord:load-models` → lê arquivos arbitrários | mitigate | Handler no main (Plan 02) NÃO aceita args do renderer — path é hardcoded via `getWakeWordModelPaths()`. T-22-02-01 coverage. |
| T-22-04-07 | Information Disclosure | Mic always-on captura áudio que NUNCA deve sair do device | mitigate | WAKE-09 enforcement: (1) Plan 02 invariant test `expect(fetch).not.toHaveBeenCalled()` em WakeWordEngine; (2) Plan 03 CI grep-ban; (3) onnxruntime-web roda só em WASM local, zero network. |
| T-22-04-08 | Trust | Usuário não vê indicação visual que wake word está ativo — percepção de surveillance | accept (this phase) | Visual indication (orb distinction idle-active vs paused) é **WAKE-04 → Phase 23**. Fora de escopo aqui. Mitigação temporária: CHECKPOINT manual documenta que o dev sabe quando o wake word está ativo via console logs. |
</threat_model>

<verification>
## Phase-Level Checks

- [ ] Full desktop test suite verde
- [ ] `pnpm --filter @jarvis/desktop build` exit 0
- [ ] `pnpm --filter @jarvis/desktop build:dist` roda sem erro (gera release/)
- [ ] `bash apps/desktop/scripts/smoke-packaged-build.sh` exit 0
- [ ] 7 verificações manuais do checkpoint todas PASS
- [ ] `grep "backgroundThrottling: false" apps/desktop/src/main/index.ts` === 1
- [ ] `grep "extraResources:" apps/desktop/electron-builder.yml` === 1
- [ ] `grep "wakeword-models" apps/desktop/electron-builder.yml` >= 2
- [ ] Commits seguem formato pt-BR + emoji
</verification>

<success_criteria>
1. `useWakeWord` hook existe, é montado dentro do OrbProvider em App.tsx
2. Gate por `stateRef.current === 'idle'` funcionando (testado + verificado manual)
3. VAD timeout 3000ms funcional (WAKE-06)
4. PTT preempção via voiceInputManager (WAKE-07)
5. Degrade graceful em getUserMedia fail (WAKE-08) — PTT continua funcionando
6. `backgroundThrottling: false` na BrowserWindow (WAKE-01 com janela oculta)
7. ttsPlayer hooks beforePlay/afterPlay + 300ms tail (anti self-trigger)
8. `electron-builder.yml` com `extraResources` → wake word models no artefato
9. `build:dist` script funciona
10. CPU sustained <2% em 10min silêncio (WAKE-09 — checkpoint manual)
11. Latência "Hey JARVIS" → listening ≤500ms (WAKE-01 — checkpoint manual)
12. Full cycle retoma automaticamente (WAKE-05 — checkpoint manual)
13. Packaged `.exe` contém os 4 .onnx e detecta wake word no primeiro launch (pitfall #5 — checkpoint manual)
14. Desktop test suite verde ao fim do plan
</success_criteria>

<output>
After completion, create `.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-04-SUMMARY.md` with:
- Componentes integrados (useWakeWord, ttsPlayer hooks, backgroundThrottling, electron-builder)
- Resultado de cada verificação manual do checkpoint (latência medida, CPU% medido)
- Saída do smoke-packaged-build.sh
- Se houve gaps, qual foi o erro + próximo passo
- Próximo: Phase 23 (Orb UX Polish — WAKE-02, WAKE-03, WAKE-04, ORB-POL-01, ORB-POL-02) — agora com callback `onDetected()` real disponível
</output>
