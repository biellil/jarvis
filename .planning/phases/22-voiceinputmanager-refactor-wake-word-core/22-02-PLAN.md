---
phase: 22-voiceinputmanager-refactor-wake-word-core
plan: 02
type: execute
wave: 2
depends_on: [22-01]
autonomous: true
requirements: [WAKE-05, WAKE-06, WAKE-08, WAKE-09]
files_modified:
  - apps/desktop/package.json
  - apps/desktop/src/renderer/public/wakeWordWorklet.js
  - apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts
  - apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts
  - apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts
  - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts
  - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts
  - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts
  - apps/desktop/src/main/voiceInput/resources.ts
  - apps/desktop/src/main/ipc/wakeWord.ts
  - apps/desktop/src/main/ipc/index.ts
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/scripts/poc-wakeword-file-fetch.mjs
tags: [wake-word, onnx, audio-worklet, inference, rms-guard, poc]

must_haves:
  truths:
    - "onnxruntime-web@1.24.3 está instalado como dependência de apps/desktop e ort.env.wasm.numThreads === 1 em todos os code paths"
    - "AudioWorklet processor roda em apps/desktop/src/renderer/public/wakeWordWorklet.js (servido como asset pelo electron-vite em dev e produção) em formato JS puro sem imports ESM"
    - "WakeWordEngine expõe start(modelPaths)/suspend()/resume()/stop() e um onDetected(score) callback com threshold configurável"
    - "RmsZeroGuard detecta stream silenciado (sumSq < 1e-8) por 63 frames consecutivos e dispara callback exatamente uma vez"
    - "modelLoader carrega os 4 modelos ONNX via IPC read-and-transfer (main le bytes, preload passa via ipc, renderer cria ort.InferenceSession a partir de Uint8Array) — validado contra A4 via POC"
    - "Nenhum fetch para URLs fora de file:// ou blob:// pode vir do módulo wakeWord (invariant de test)"
    - "registerWakeWordIpc() é chamado a partir de apps/desktop/src/main/ipc/index.ts (setupIpcHandlers), o entry point REAL de IPC do projeto — NÃO existe apps/desktop/src/main/ipc.ts"
  artifacts:
    - path: apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts
      provides: "Classe WakeWordEngine orquestrando mel → embedding → VAD gate → classifier pipeline"
      exports: [WakeWordEngine, WakeWordEngineOptions]
    - path: apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts
      provides: "loadAll() que recebe Uint8Arrays via IPC e cria InferenceSessions em paralelo"
      exports: [loadWakeWordSessions, WakeWordSessions]
    - path: apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts
      provides: "Sliding window RMS trip detector para detecção de mic silencioso (PITFALL #7)"
      exports: [RmsZeroGuard, RmsZeroGuardOptions]
    - path: apps/desktop/src/renderer/public/wakeWordWorklet.js
      provides: "AudioWorkletProcessor registrado como 'wake-word-chunker', emite frames de 1280 samples via port.postMessage com transferable buffer"
      contains: "registerProcessor('wake-word-chunker'"
    - path: apps/desktop/src/main/voiceInput/resources.ts
      provides: "getWakeWordModelBytes() — le os 4 arquivos .onnx via fs.readFile e retorna {mel, embed, vad, kw}: Uint8Array"
      exports: [getWakeWordModelPaths, loadWakeWordModelBytes]
    - path: apps/desktop/src/main/ipc/wakeWord.ts
      provides: "Exporta registerWakeWordIpc() que registra o handler 'wakeWord:load-models' no main — é chamado por apps/desktop/src/main/ipc/index.ts"
      contains: "ipcMain.handle('wakeWord:load-models'"
    - path: apps/desktop/src/main/ipc/index.ts
      provides: "Entry point existente de IPC — EDITADO para importar e chamar registerWakeWordIpc() dentro de setupIpcHandlers, junto dos handlers existentes (chat, hotkey)"
      contains: "registerWakeWordIpc"
    - path: apps/desktop/scripts/poc-wakeword-file-fetch.mjs
      provides: "POC script que valida A4 — se ort.InferenceSession.create('file://...') funciona direto no Electron renderer"
      contains: "file://"
  key_links:
    - from: "apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts"
      to: "onnxruntime-web InferenceSession API"
      via: "import * as ort from 'onnxruntime-web'"
      pattern: "import \\* as ort from 'onnxruntime-web'"
    - from: "apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts"
      to: "AudioWorklet via '/wakeWordWorklet.js'"
      via: "audioContext.audioWorklet.addModule('/wakeWordWorklet.js')"
      pattern: "'/wakeWordWorklet\\.js'"
    - from: "apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts"
      to: "IPC 'wakeWord:load-models'"
      via: "window.jarvis.wakeWord.loadModels()"
      pattern: "wakeWord:load-models"
    - from: "apps/desktop/src/main/ipc/wakeWord.ts"
      to: "fs.readFile em apps/desktop/resources/wakeword-models/*.onnx"
      via: "getWakeWordModelPaths() + fs.promises.readFile"
      pattern: "fs\\.promises\\.readFile"
    - from: "apps/desktop/src/main/ipc/index.ts (setupIpcHandlers)"
      to: "registerWakeWordIpc() de ./wakeWord"
      via: "import { registerWakeWordIpc } from './wakeWord'; registerWakeWordIpc();"
      pattern: "registerWakeWordIpc"
---

<objective>
Implementar os módulos core da detecção wake word (Bloco 2 do RESEARCH.md) — tudo **sem integração live com OrbContext nem electron-builder**, que são Plan 04. Adicionar `onnxruntime-web@1.24.3` como dependência, criar WakeWordEngine / modelLoader / RmsZeroGuard / AudioWorklet processor, montar a ponte IPC que lê os 4 modelos ONNX no main e transfere Uint8Arrays para o renderer (mitigação A4 confirmada por POC), e cobrir cada módulo com failing tests → green tests.

Purpose: Este plan entrega o motor de inferência como uma caixa preta testável com áudio sintético. Plan 04 depois liga no `useWakeWord` hook + OrbContext + TTS wrap e roda o CPU benchmark. Ao fim deste plan, `new WakeWordEngine({...}).start(modelPaths)` funciona tecnicamente mas ainda não é montado pelo App — a classe fica exportada pronta para Plan 04 consumir.

Output:
- `onnxruntime-web@1.24.3` em `apps/desktop/package.json` dependencies
- 4 novos arquivos de código em `apps/desktop/src/renderer/src/voice/wakeWord/`
- 1 worklet `.js` puro em `apps/desktop/src/renderer/public/`
- 3 novos arquivos de teste cobrindo pipeline, loader e guard
- 2 novos arquivos no main process (`resources.ts` + `ipc/wakeWord.ts`) para load-and-transfer
- `apps/desktop/src/main/ipc/index.ts` editado (entry point real do IPC) — adicionar import e call de `registerWakeWordIpc()` dentro de `setupIpcHandlers`
- POC script validando A4 (fetch file:// viability)
- preload expondo `window.jarvis.wakeWord.loadModels()` + tipos em ipc-types.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-CONTEXT.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-VALIDATION.md
@apps/desktop/package.json
@apps/desktop/electron.vite.config.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/main/ipc/index.ts
@apps/desktop/src/main/ipc/chat.ts
@apps/desktop/src/main/ipc/hotkey.ts
@apps/desktop/src/preload/index.ts
@apps/desktop/src/shared/ipc-types.ts
@CLAUDE.md

<interfaces>
<!-- Contratos exatos — copiar verbatim. Derivado de 22-RESEARCH.md Patterns 2-4-6. -->

// NOVO — apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts
import * as ort from 'onnxruntime-web';

export interface WakeWordSessions {
  mel: ort.InferenceSession;
  embed: ort.InferenceSession;
  vad: ort.InferenceSession;
  kw: ort.InferenceSession;
}

export interface WakeWordModelBytes {
  mel: Uint8Array;
  embed: Uint8Array;
  vad: Uint8Array;
  kw: Uint8Array;
}

export async function loadWakeWordSessions(bytes: WakeWordModelBytes): Promise<WakeWordSessions>;

// NOVO — apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts
export interface RmsZeroGuardOptions {
  windowFrames: number;   // default 63 (~5s @ 12.5Hz)
  epsilon: number;        // default 1e-8
  onSilentStream: () => void;
}

export class RmsZeroGuard {
  constructor(opts: RmsZeroGuardOptions);
  observe(chunk: Float32Array): void;
  reset(): void;
}

// NOVO — apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts
import * as ort from 'onnxruntime-web';
ort.env.wasm.numThreads = 1;
ort.env.wasm.simd = true;

export interface WakeWordEngineOptions {
  threshold: number;        // from env VITE_WAKE_WORD_THRESHOLD, default 0.5
  debounceMs: number;       // default 2000
  vadThreshold: number;     // default 0.3
  rmsGuardWindowFrames?: number;
  onDetected: (score: number) => void;
  onSilentStream?: () => void;  // WAKE-08 degrade path
}

export class WakeWordEngine {
  constructor(opts: WakeWordEngineOptions);
  start(sessions: WakeWordSessions, stream: MediaStream): Promise<void>;
  suspend(): Promise<void>;
  resume(): Promise<void>;
  stop(): Promise<void>;
}

// NOVO — apps/desktop/src/main/voiceInput/resources.ts
export function getWakeWordModelPaths(): { mel: string; embed: string; vad: string; kw: string };
export async function loadWakeWordModelBytes(): Promise<{
  mel: Uint8Array; embed: Uint8Array; vad: Uint8Array; kw: Uint8Array;
}>;

// NOVO — apps/desktop/src/main/ipc/wakeWord.ts
export function registerWakeWordIpc(): void;  // registra 'wakeWord:load-models' handler

// EXISTENTE — apps/desktop/src/main/ipc/index.ts (entry REAL do IPC, confirmado via ls em 2026-04-11):
//   export function setupIpcHandlers(chatDeps: ChatHandlerDeps): void {
//     setupChatHandlers(chatDeps);
//     setupHotkeyHandlers();
//     // ← ADICIONAR: registerWakeWordIpc();
//   }
// NÃO existe apps/desktop/src/main/ipc.ts — qualquer menção ao path antigo é obsoleta.

// NOVO — apps/desktop/src/preload/index.ts additions
// window.jarvis.wakeWord = {
//   loadModels: () => Promise<{ mel, embed, vad, kw: Uint8Array }>
// }

// NOVO — apps/desktop/src/renderer/public/wakeWordWorklet.js
// registerProcessor('wake-word-chunker', class extends AudioWorkletProcessor { ... })
// Emits 1280-sample Float32 frames via port.postMessage(buffer, [buffer]).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add onnxruntime-web dep, Wave 0 failing tests para 3 módulos, POC A4 (file:// fetch viability)</name>
  <read_first>
    - apps/desktop/package.json (confirmar ausência de onnxruntime-web)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Installation" (comandos pnpm exatos), §"Assumptions Log" A4 (file:// fetch risk), §"Pattern 6: RMS Zero Guard" (window frames + epsilon)
    - apps/desktop/src/renderer/src/voice/__tests__/voiceInputManager.test.ts (padrão de test adotado no Plan 01 — vitest + happy-dom)
    - apps/desktop/vitest.config.ts
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <behavior>
    rmsZeroGuard.test.ts:
    - feedar 62 frames zero → callback NÃO chamado
    - feedar 63 frames zero → callback chamado EXATAMENTE 1 vez
    - feedar 70 frames zero após trip → callback ainda chamado só 1 vez (tripped idempotente)
    - feedar 50 frames zero, depois 1 frame não-zero (contendo 0.5), depois mais 63 zeros → callback chamado 1 vez (streak resetou no frame não-zero)
    - reset() limpa o state: depois de reset + 63 zeros, callback chamado de novo

    modelLoader.test.ts:
    - loadWakeWordSessions({mel, embed, vad, kw: uint8arrays}) cria 4 InferenceSessions em paralelo (mockar `ort.InferenceSession.create`) → retorna objeto com as 4 sessions
    - se qualquer um dos 4 create rejeitar, a função rejeita com o erro original e não vaza sessions (cleanup de sessions já criadas)
    - ordem de criação não é importante (Promise.all) — spy de ort.InferenceSession.create é chamado 4x

    WakeWordEngine.test.ts (stub completo — vai ficar red até Task 3):
    - constructor com opts mínimos (threshold: 0.5, debounceMs: 2000, vadThreshold: 0.3, onDetected spy)
    - stub mock de sessions retornando tensores de shapes controlados
    - start(sessions, fakeStream) não lança e atribui audioContext/worklet (happy-dom suporta AudioContext mock)
    - dois cenários de detection: score >= threshold dispara onDetected; score < threshold não dispara
    - debounce: dois scores >= threshold dentro de 2000ms → onDetected chamado 1x só
    - VAD gate: vadScore < 0.3 por 12 frames → classifier NÃO é chamado (spy assertion em sessions.kw.run)
    - invariant fetch: mock global.fetch, assertar que nenhum call fetch foi feito para URL não-file:// durante processChunk

    poc-wakeword-file-fetch.mjs (NÃO é vitest — é um script standalone que o executor roda manualmente para validar A4):
    - script tenta `ort.InferenceSession.create('file:///absolute/path/to/dummy.onnx')` e loga success OU o erro exato. Gera uma decisão: "use direct file:// URL" ou "use IPC read-and-transfer".
  </behavior>
  <action>
    1. `pnpm --filter @jarvis/desktop add onnxruntime-web@1.24.3` — confirmar que aparece em dependencies (NÃO devDependencies) de `apps/desktop/package.json` com versão EXATA `1.24.3`.

    2. Criar dir `apps/desktop/src/renderer/src/voice/wakeWord/` e `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/`.

    3. Criar `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts` com TODOS os cenários acima. Import de `../rmsZeroGuard` que ainda não existe → tests falham no import.

    4. Criar `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts` com:
       ```ts
       import { describe, it, expect, vi, beforeEach } from 'vitest';
       import { loadWakeWordSessions } from '../modelLoader';
       vi.mock('onnxruntime-web', () => ({
         InferenceSession: { create: vi.fn().mockResolvedValue({ __mock: 'session' }) },
         env: { wasm: { numThreads: 1, simd: true } },
         Tensor: vi.fn(),
       }));
       ```
       Testar: 4 create chamadas em paralelo, error propagation.

    5. Criar `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts` com:
       - Mock completo de `onnxruntime-web`: `InferenceSession.create` retorna objeto fake com `run: vi.fn()`.
       - Mock de `AudioContext`, `AudioWorkletNode`, `MediaStream` (happy-dom pode não ter — stub via `vi.stubGlobal`).
       - Mock de `global.fetch = vi.fn()` → asserção no fim: `expect(fetch).not.toHaveBeenCalledWith(expect.stringMatching(/^https?:/))` (invariant #4 do research, WAKE-09).
       - Cenários listados em behavior.

    6. Criar `apps/desktop/scripts/poc-wakeword-file-fetch.mjs`:
       ```js
       #!/usr/bin/env node
       // POC — valida Assumption A4: se Electron renderer consegue fetch('file:///...') do modelo ONNX.
       // Uso: node apps/desktop/scripts/poc-wakeword-file-fetch.mjs /path/to/model.onnx
       // Se rodar em ambiente Node (fora do renderer), ele só imprime o resultado esperado como orientação.
       // Para POC real: executor deve colar este snippet no DevTools console do renderer em `pnpm dev` e relatar o resultado.
       //
       // Snippet para console do renderer:
       // ```
       // (async () => {
       //   const ort = await import('onnxruntime-web');
       //   try {
       //     const s = await ort.InferenceSession.create('file:///C:/path/to/hey_jarvis_v0.1.onnx');
       //     console.log('A4_RESULT: file_url_works');
       //   } catch (e) {
       //     console.log('A4_RESULT: file_url_blocked', e.message);
       //   }
       // })();
       // ```
       //
       // Decisão:
       // - A4_RESULT=file_url_works → Task 2 usa direct URL no modelLoader
       // - A4_RESULT=file_url_blocked → Task 2 usa IPC read-and-transfer (approach 1 do research)
       console.log('[POC A4] Cole o snippet acima no DevTools do Electron em pnpm dev e relate o resultado.');
       ```
       Este arquivo fica como documentação do POC — não precisa rodar em CI. O executor deve chamá-lo e registrar a decisão no SUMMARY.

    7. Rodar `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/`. Deve falhar com Cannot find module em todos os 3 arquivos.

    **Decisão de A4**: Para este plano, vamos **assumir approach 1 (IPC read-and-transfer)** por default (mais robusto, garantido pelo research §Pattern 4 "Gotcha descoberto"). Task 2 implementa via IPC. Se o executor rodar o POC e confirmar que file:// funciona direto, ele pode adicionar uma nota no SUMMARY mas a arquitetura já está commitada para IPC. Trade-off aceito por segurança.

    Commit: `✅ test(22-02): Wave 0 failing tests para rmsZeroGuard, modelLoader, WakeWordEngine + onnxruntime-web@1.24.3 dep + POC A4 stub`
  </action>
  <verify>
    <automated>grep -c "\"onnxruntime-web\": \"1.24.3\"" apps/desktop/package.json; pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/ 2>&1 | grep -E "FAIL|Cannot find module"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\"onnxruntime-web\": \"1.24.3\"" apps/desktop/package.json` retorna 1 linha (dependencies, NÃO devDependencies)
    - arquivo `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts` existe com `describe('RmsZeroGuard'`
    - arquivo `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts` existe com `describe('loadWakeWordSessions'`
    - arquivo `apps/desktop/src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts` existe com `describe('WakeWordEngine'`
    - arquivo `apps/desktop/scripts/poc-wakeword-file-fetch.mjs` existe contendo a string `A4_RESULT`
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/` exit code não-zero (tests falham por Cannot find module) — comportamento esperado Wave 0
    - nenhum arquivo de produção em `src/renderer/src/voice/wakeWord/*.ts` (só __tests__) — `ls apps/desktop/src/renderer/src/voice/wakeWord/*.ts 2>/dev/null | wc -l` retorna 0
    - `grep "onnxruntime-web" apps/desktop/pnpm-lock.yaml apps/desktop/../../pnpm-lock.yaml 2>/dev/null` retorna pelo menos 1 match
  </acceptance_criteria>
  <done>Dep adicionada, 3 test files failing (red Wave 0), POC A4 script documentado. Zero código de produção no wakeWord/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar rmsZeroGuard, modelLoader, AudioWorklet processor, main-side IPC read-and-transfer + preload bridge</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts (Task 1)
    - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts (Task 1)
    - apps/desktop/src/preload/index.ts (padrão de contextBridge.exposeInMainWorld — para estender window.jarvis)
    - apps/desktop/src/shared/ipc-types.ts (onde adicionar os novos channel constants + types)
    - apps/desktop/src/main/ipc/index.ts (ENTRY REAL DO IPC — define `setupIpcHandlers(chatDeps)` e chama `setupChatHandlers` + `setupHotkeyHandlers`. É AQUI que `registerWakeWordIpc()` precisa ser adicionado. NÃO existe `apps/desktop/src/main/ipc.ts` — confirmado via ls.)
    - apps/desktop/src/main/ipc/chat.ts (referência do padrão `setupChatHandlers(deps)` — mostra como o projeto estrutura handlers por domínio)
    - apps/desktop/src/main/ipc/hotkey.ts (referência do padrão `setupHotkeyHandlers()` — o nosso `registerWakeWordIpc()` segue o mesmo shape: função sem args que chama `ipcMain.handle(...)` internamente)
    - apps/desktop/src/main/index.ts (confirmar de onde `setupIpcHandlers` é chamado)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 4: electron-builder extraResources + Runtime Path Resolver" (código de resources.ts) e §"Pattern 3: AudioWorklet Asset Serving"
    - apps/desktop/electron.vite.config.ts (confirmar que renderer.root === 'src/renderer' e que public/ é suportado)
  </read_first>
  <action>
    **Sub-passo A — rmsZeroGuard.ts (implementação direta):**

    Criar `apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts`. Implementação literal do research §Pattern 6:
    ```ts
    export interface RmsZeroGuardOptions {
      windowFrames: number;  // default 63
      epsilon: number;       // default 1e-8
      onSilentStream: () => void;
    }

    export class RmsZeroGuard {
      private zeroStreakFrames = 0;
      private tripped = false;
      constructor(private opts: RmsZeroGuardOptions) {}

      observe(chunk: Float32Array): void {
        if (this.tripped) return;
        let sumSq = 0;
        for (let i = 0; i < chunk.length; i++) sumSq += chunk[i] * chunk[i];
        const rms = Math.sqrt(sumSq / chunk.length);
        if (rms < this.opts.epsilon) {
          this.zeroStreakFrames++;
          if (this.zeroStreakFrames >= this.opts.windowFrames) {
            this.tripped = true;
            this.opts.onSilentStream();
          }
        } else {
          this.zeroStreakFrames = 0;
        }
      }

      reset(): void {
        this.zeroStreakFrames = 0;
        this.tripped = false;
      }
    }
    ```
    Rodar `pnpm --filter @jarvis/desktop test --run rmsZeroGuard.test.ts` → deve ficar verde.

    **Sub-passo B — modelLoader.ts + IPC read-and-transfer:**

    1. Criar `apps/desktop/src/main/voiceInput/resources.ts`:
    ```ts
    import { app } from 'electron';
    import path from 'node:path';
    import { fileURLToPath } from 'node:url';
    import fs from 'node:fs/promises';

    const __dirname = path.dirname(fileURLToPath(import.meta.url));

    export function getWakeWordModelPaths() {
      const base = app.isPackaged
        ? path.join(process.resourcesPath, 'wakeword-models')
        : path.join(__dirname, '../../../resources/wakeword-models');
      return {
        mel: path.join(base, 'melspectrogram.onnx'),
        embed: path.join(base, 'embedding_model.onnx'),
        vad: path.join(base, 'silero_vad.onnx'),
        kw: path.join(base, 'hey_jarvis_v0.1.onnx'),
      };
    }

    export async function loadWakeWordModelBytes() {
      const paths = getWakeWordModelPaths();
      const [mel, embed, vad, kw] = await Promise.all([
        fs.readFile(paths.mel),
        fs.readFile(paths.embed),
        fs.readFile(paths.vad),
        fs.readFile(paths.kw),
      ]);
      return {
        mel: new Uint8Array(mel),
        embed: new Uint8Array(embed),
        vad: new Uint8Array(vad),
        kw: new Uint8Array(kw),
      };
    }
    ```

    2. Criar `apps/desktop/src/main/ipc/wakeWord.ts`:
    ```ts
    import { ipcMain } from 'electron';
    import { loadWakeWordModelBytes } from '../voiceInput/resources';

    /**
     * Registra o handler 'wakeWord:load-models'. Deve ser chamado UMA vez
     * durante o boot, a partir de apps/desktop/src/main/ipc/index.ts
     * (setupIpcHandlers), junto dos demais registradores de handler do projeto.
     *
     * SECURITY: o handler NÃO aceita nenhum argumento do renderer — paths são
     * hardcoded via getWakeWordModelPaths() baseado em app.isPackaged. Isso
     * mitiga T-22-02-01 (path traversal via IPC).
     */
    export function registerWakeWordIpc(): void {
      ipcMain.handle('wakeWord:load-models', async () => {
        return await loadWakeWordModelBytes();
      });
    }
    ```

    3. **EDITAR `apps/desktop/src/main/ipc/index.ts`** (entry point REAL do IPC — confirmado via ls; NÃO existe `apps/desktop/src/main/ipc.ts`). O arquivo atual é exatamente:
       ```ts
       /**
        * IPC Handler Registry
        * D-01: Aggregates all feature handlers
        */
       import { setupChatHandlers, type ChatHandlerDeps } from './chat';
       import { setupHotkeyHandlers } from './hotkey';

       export function setupIpcHandlers(chatDeps: ChatHandlerDeps): void {
         setupChatHandlers(chatDeps);
         setupHotkeyHandlers();
       }
       ```
       Alterar para:
       ```ts
       /**
        * IPC Handler Registry
        * D-01: Aggregates all feature handlers
        * Phase 22 Plan 02: + wake word load-models handler
        */
       import { setupChatHandlers, type ChatHandlerDeps } from './chat';
       import { setupHotkeyHandlers } from './hotkey';
       import { registerWakeWordIpc } from './wakeWord';

       export function setupIpcHandlers(chatDeps: ChatHandlerDeps): void {
         setupChatHandlers(chatDeps);
         setupHotkeyHandlers();
         registerWakeWordIpc();
       }
       ```
       Confirmar via `grep "registerWakeWordIpc" apps/desktop/src/main/ipc/index.ts` que retorna 2 linhas (import + call).

    4. Atualizar `apps/desktop/src/preload/index.ts`:
    ```ts
    // dentro do contextBridge.exposeInMainWorld('jarvis', { ... existing ..., wakeWord: { loadModels: () => ipcRenderer.invoke('wakeWord:load-models') } })
    ```

    5. Atualizar `apps/desktop/src/shared/ipc-types.ts`: adicionar tipo `WakeWordApi { loadModels: () => Promise<WakeWordModelBytes> }` e `WakeWordModelBytes` type (mel/embed/vad/kw Uint8Array). Atualizar a declaração de `window.jarvis`.

    6. Criar `apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts`:
    ```ts
    import * as ort from 'onnxruntime-web';
    ort.env.wasm.numThreads = 1;
    ort.env.wasm.simd = true;

    export interface WakeWordSessions {
      mel: ort.InferenceSession;
      embed: ort.InferenceSession;
      vad: ort.InferenceSession;
      kw: ort.InferenceSession;
    }

    export interface WakeWordModelBytes {
      mel: Uint8Array;
      embed: Uint8Array;
      vad: Uint8Array;
      kw: Uint8Array;
    }

    export async function loadWakeWordSessions(bytes: WakeWordModelBytes): Promise<WakeWordSessions> {
      const created: Partial<WakeWordSessions> = {};
      try {
        const [mel, embed, vad, kw] = await Promise.all([
          ort.InferenceSession.create(bytes.mel),
          ort.InferenceSession.create(bytes.embed),
          ort.InferenceSession.create(bytes.vad),
          ort.InferenceSession.create(bytes.kw),
        ]);
        return { mel, embed, vad, kw };
      } catch (err) {
        // Cleanup any session that may have been created successfully
        for (const k of Object.keys(created)) {
          try { await (created as any)[k]?.release?.(); } catch {}
        }
        throw err;
      }
    }
    ```
    Rodar `pnpm --filter @jarvis/desktop test --run modelLoader.test.ts` → deve ficar verde.

    **Sub-passo C — AudioWorklet processor em public/:**

    1. Criar `apps/desktop/src/renderer/public/` dir se não existir (confirmar se electron-vite copia de `src/renderer/public/` — ver electron.vite.config.ts `root: 'src/renderer'`).

    2. Criar `apps/desktop/src/renderer/public/wakeWordWorklet.js` (arquivo `.js` NATIVO, sem TypeScript, sem import ESM):
    ```javascript
    // AudioWorkletGlobalScope — no ESM imports allowed.
    // Source: 22-RESEARCH.md Pattern 3 + openwakeword_wasm reference.
    class WakeWordChunker extends AudioWorkletProcessor {
      constructor() {
        super();
        this.buffer = new Float32Array(1280);
        this.bufferIndex = 0;
      }
      process(inputs) {
        const input = inputs[0];
        if (!input || input.length === 0) return true;
        const channel = input[0];
        if (!channel) return true;
        for (let i = 0; i < channel.length; i++) {
          this.buffer[this.bufferIndex++] = channel[i];
          if (this.bufferIndex === 1280) {
            const frame = this.buffer.slice();
            this.port.postMessage(frame.buffer, [frame.buffer]);
            this.bufferIndex = 0;
          }
        }
        return true;
      }
    }
    registerProcessor('wake-word-chunker', WakeWordChunker);
    ```

    3. Verificar via `pnpm --filter @jarvis/desktop build` (electron-vite build) que `dist/renderer/wakeWordWorklet.js` é gerado após o build — se NÃO for gerado, adicionar nota no SUMMARY e ajustar `electron.vite.config.ts` (provavelmente via `publicDir` option no renderer block).

    Commit: `✨ feat(22-02): rmsZeroGuard + modelLoader + AudioWorklet processor + IPC bridge wakeWord:load-models registrado em ipc/index.ts`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts; pnpm --filter @jarvis/desktop build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts` existe com `export class RmsZeroGuard`
    - `apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts` existe com `export async function loadWakeWordSessions`
    - `grep "ort.env.wasm.numThreads = 1" apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts` retorna 1 linha
    - `apps/desktop/src/main/voiceInput/resources.ts` existe com `export function getWakeWordModelPaths` e `export async function loadWakeWordModelBytes`
    - `apps/desktop/src/main/ipc/wakeWord.ts` existe com `ipcMain.handle('wakeWord:load-models'` e `export function registerWakeWordIpc`
    - `grep "registerWakeWordIpc" apps/desktop/src/main/ipc/index.ts | wc -l` retorna pelo menos `2` (import + call no setupIpcHandlers)
    - `test -f apps/desktop/src/main/ipc.ts && echo FAIL || echo OK` retorna OK (o path antigo NUNCA é criado — confirma que estamos usando o entry real)
    - `apps/desktop/src/renderer/public/wakeWordWorklet.js` existe, é `.js` puro (sem `import` nem `export` nem `:` de type annotation) e contém `registerProcessor('wake-word-chunker'`
    - `grep "wakeWord" apps/desktop/src/preload/index.ts` retorna pelo menos 1 linha (exposição via contextBridge)
    - `grep "WakeWordModelBytes\|wakeWord:load-models" apps/desktop/src/shared/ipc-types.ts` retorna pelo menos 1 linha
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/rmsZeroGuard.test.ts` exit 0
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/modelLoader.test.ts` exit 0
    - `pnpm --filter @jarvis/desktop build` exit 0 (TypeScript compila, sem erros no main/preload/renderer)
    - Após build: `test -f apps/desktop/dist/renderer/wakeWordWorklet.js` retorna 0 (arquivo copiado pelo Vite)
    - WakeWordEngine.test.ts ainda falhando (será green na Task 3)
  </acceptance_criteria>
  <done>rmsZeroGuard + modelLoader verdes, AudioWorklet JS em public/, IPC bridge wakeWord:load-models registrado via apps/desktop/src/main/ipc/index.ts (entry real), preload exposto. Build TS passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar WakeWordEngine orquestrador (pipeline mel → embed → VAD gate → classifier) e green do WakeWordEngine.test</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts (Task 1 — contrato)
    - apps/desktop/src/renderer/src/voice/wakeWord/modelLoader.ts (Task 2 — WakeWordSessions type)
    - apps/desktop/src/renderer/src/voice/wakeWord/rmsZeroGuard.ts (Task 2)
    - .planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-RESEARCH.md §"Pattern 2: AudioWorklet + ONNX Runtime Web no Renderer" — code sketch linhas 294-392, §"Pattern 6: RMS Zero Guard" integration point, §"Pitfall 3" (CPU budget — VAD gate obrigatório), §"Assumptions Log" A3 (shape do classifier input)
  </read_first>
  <action>
    Criar `apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts`:

    ```ts
    import * as ort from 'onnxruntime-web';
    import type { WakeWordSessions } from './modelLoader';
    import { RmsZeroGuard } from './rmsZeroGuard';

    // Critical: single-thread to avoid crossOriginIsolated requirement.
    ort.env.wasm.numThreads = 1;
    ort.env.wasm.simd = true;

    export interface WakeWordEngineOptions {
      threshold: number;
      debounceMs: number;
      vadThreshold: number;
      rmsGuardWindowFrames?: number;
      onDetected: (score: number) => void;
      onSilentStream?: () => void;
    }

    const EMBEDDING_RING_SIZE = 76;  // A3 — openwakeword context window

    export class WakeWordEngine {
      private sessions: WakeWordSessions | null = null;
      private audioContext: AudioContext | null = null;
      private stream: MediaStream | null = null;
      private workletNode: AudioWorkletNode | null = null;
      private sourceNode: MediaStreamAudioSourceNode | null = null;
      private embeddingRing: Float32Array[] = [];
      private lastDetectionAt = 0;
      private vadHangover = 0;
      private rmsGuard: RmsZeroGuard;
      private processing = false;

      constructor(private opts: WakeWordEngineOptions) {
        this.rmsGuard = new RmsZeroGuard({
          windowFrames: opts.rmsGuardWindowFrames ?? 63,
          epsilon: 1e-8,
          onSilentStream: () => opts.onSilentStream?.(),
        });
      }

      async start(sessions: WakeWordSessions, stream: MediaStream): Promise<void> {
        this.sessions = sessions;
        this.stream = stream;
        this.audioContext = new AudioContext({ sampleRate: 16000 });
        await this.audioContext.audioWorklet.addModule('/wakeWordWorklet.js');
        this.sourceNode = this.audioContext.createMediaStreamSource(stream);
        this.workletNode = new AudioWorkletNode(this.audioContext, 'wake-word-chunker');
        this.workletNode.port.onmessage = (e: MessageEvent) => {
          const chunk = new Float32Array(e.data);
          void this.processChunk(chunk);
        };
        this.sourceNode.connect(this.workletNode);
        // Do NOT connect worklet to destination (echoes to speakers).
      }

      private async processChunk(chunk: Float32Array): Promise<void> {
        if (!this.sessions || this.processing) return;
        this.processing = true;
        try {
          this.rmsGuard.observe(chunk);

          // 1. Mel spectrogram
          const melInput = new ort.Tensor('float32', chunk, [1, chunk.length]);
          const melOut = await this.sessions.mel.run({ input: melInput });
          const melTensor = melOut[Object.keys(melOut)[0]];

          // 2. Embedding
          const embedOut = await this.sessions.embed.run({ input: melTensor });
          const embedTensor = embedOut[Object.keys(embedOut)[0]];
          const embedding = embedTensor.data as Float32Array;
          this.embeddingRing.push(embedding);
          if (this.embeddingRing.length > EMBEDDING_RING_SIZE) this.embeddingRing.shift();
          if (this.embeddingRing.length < EMBEDDING_RING_SIZE) return;  // cold start

          // 3. VAD gate — critical CPU optimization (PITFALL #4)
          const vadOut = await this.sessions.vad.run({ input: embedTensor });
          const vadTensor = vadOut[Object.keys(vadOut)[0]];
          const vadScore = (vadTensor.data as Float32Array)[0];
          if (vadScore < this.opts.vadThreshold) {
            if (this.vadHangover > 0) this.vadHangover--;
            if (this.vadHangover === 0) return;  // skip classifier
          } else {
            this.vadHangover = 12;
          }

          // 4. Keyword classifier
          const embedDim = embedding.length;
          const flattened = new Float32Array(EMBEDDING_RING_SIZE * embedDim);
          for (let i = 0; i < EMBEDDING_RING_SIZE; i++) {
            flattened.set(this.embeddingRing[i], i * embedDim);
          }
          const kwInput = new ort.Tensor('float32', flattened, [1, EMBEDDING_RING_SIZE, embedDim]);
          const kwOut = await this.sessions.kw.run({ input: kwInput });
          const kwTensor = kwOut[Object.keys(kwOut)[0]];
          const score = (kwTensor.data as Float32Array)[0];

          // 5. Debounce + threshold
          const now = Date.now();
          if (score >= this.opts.threshold && now - this.lastDetectionAt > this.opts.debounceMs) {
            this.lastDetectionAt = now;
            this.opts.onDetected(score);
          }
        } finally {
          this.processing = false;
        }
      }

      async suspend(): Promise<void> {
        await this.audioContext?.suspend();
      }
      async resume(): Promise<void> {
        await this.audioContext?.resume();
      }
      async stop(): Promise<void> {
        try { this.sourceNode?.disconnect(); } catch {}
        try { this.workletNode?.disconnect(); } catch {}
        this.stream?.getTracks().forEach((t) => t.stop());
        await this.audioContext?.close();
        this.sessions = null;
        this.audioContext = null;
        this.stream = null;
        this.workletNode = null;
        this.sourceNode = null;
        this.embeddingRing = [];
        this.vadHangover = 0;
        this.lastDetectionAt = 0;
        this.rmsGuard.reset();
      }
    }
    ```

    Depois ajustar `WakeWordEngine.test.ts` (Task 1) para usar a nova assinatura `start(sessions, stream)` em vez de `start(modelPaths)` — o engine agora recebe sessions pré-carregadas + stream, desacoplando carregamento de mic acquisition. Isso é intencional: Plan 04 vai fazer loader + stream em `useWakeWord`.

    No test, mockar:
    - `AudioContext` global via `vi.stubGlobal('AudioContext', class { audioWorklet = { addModule: vi.fn() }; createMediaStreamSource = vi.fn(); suspend = vi.fn(); resume = vi.fn(); close = vi.fn(); })`.
    - `AudioWorkletNode` global → classe fake com `port = { onmessage: null }`, `disconnect: vi.fn()`.
    - `ort.Tensor` vi.fn construtor.
    - `sessions.mel.run` / `embed.run` / `vad.run` / `kw.run` retornam Promises com objetos `{ output: { data: new Float32Array([...]) } }`.

    Cenários:
    1. start(sessions, fakeStream) completa sem throw e atribui workletNode.
    2. Simular 76 chunks para encher ring; chunk 77 com kw.run retornando Float32Array([0.8]) (acima do threshold 0.5) → onDetected chamado 1x com 0.8.
    3. Segundo chunk com score 0.9 ainda dentro do debounceMs 2000ms → onDetected NÃO chamado de novo.
    4. VAD gate: vad.run retorna 0.1 (< 0.3) por 12+ frames → sessions.kw.run NÃO é chamado nessa janela.
    5. Invariant `fetch`: mockar `global.fetch = vi.fn()` antes de criar engine; depois de rodar 100 chunks, assertar `expect(fetch).not.toHaveBeenCalled()` (ou `not.toHaveBeenCalledWith(expect.stringMatching(/^https?:/))`).
    6. stop() limpa sessions, chama close no audioContext, para os tracks.
    7. onSilentStream callback dispara depois de feeds de 63 chunks zero (integração com RmsZeroGuard).

    Rodar: `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts` → verde.

    Rodar full suite: `pnpm --filter @jarvis/desktop test --run` → zero regressão.

    Commit: `✨ feat(22-02): WakeWordEngine orquestrando mel → embed → VAD gate → classifier com debounce + RmsZeroGuard`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/; pnpm --filter @jarvis/desktop test --run</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` existe com `export class WakeWordEngine`
    - `grep "ort.env.wasm.numThreads = 1" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna 1 linha
    - `grep "vadHangover" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna pelo menos 2 linhas (gate lógica presente)
    - `grep "audioWorklet.addModule('/wakeWordWorklet.js')" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna 1 linha
    - `grep "debounceMs" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna pelo menos 1 linha
    - `grep "rmsGuard" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna pelo menos 2 linhas (integração presente)
    - `pnpm --filter @jarvis/desktop test --run src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts` exit 0 com pelo menos 7 testes passando
    - `pnpm --filter @jarvis/desktop test --run` full suite exit 0 (nenhuma regressão)
    - `pnpm --filter @jarvis/desktop build` exit 0 (TypeScript compila)
    - invariant fetch: `grep "expect(fetch).not.toHaveBeenCalled" apps/desktop/src/renderer/src/voice/wakeWord/__tests__/WakeWordEngine.test.ts` retorna pelo menos 1 linha
    - `grep "import.*MediaRecorder" apps/desktop/src/renderer/src/voice/wakeWord/WakeWordEngine.ts` retorna vazio (invariant #3 — wake word NUNCA usa MediaRecorder)
    - WakeWordEngine NÃO é importado em nenhum arquivo de `apps/desktop/src/renderer/src/` fora de `wakeWord/` — `grep -r "WakeWordEngine" apps/desktop/src/renderer/ --include="*.ts" --include="*.tsx" | grep -v "wakeWord/" | grep -v "__tests__"` retorna vazio (integração é Plan 04)
  </acceptance_criteria>
  <done>WakeWordEngine implementado, testado com áudio sintético mockado, zero integração live (isso é Plan 04), full suite verde, CPU budget assumption validada via VAD gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Plan 02)

| Boundary | Description |
|----------|-------------|
| main→renderer IPC | `wakeWord:load-models` handler retorna Uint8Arrays dos 4 modelos ONNX — paths hardcoded, NUNCA aceita args do renderer |
| file system → main | main process lê `apps/desktop/resources/wakeword-models/*.onnx` via `fs.readFile` — path resolvido via `app.isPackaged` check |
| renderer → WASM runtime | onnxruntime-web roda em WASM dentro do Chromium, single-threaded |
| AudioWorklet boundary | Worklet roda em thread isolada, só comunica via `port.postMessage` (Transferable buffer, zero-copy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Elevation of Privilege / Information Disclosure | `wakeWord:load-models` IPC handler | mitigate | Handler NÃO aceita argumentos do renderer — paths são hardcoded via `getWakeWordModelPaths()` baseado em `app.isPackaged`. Renderer comprometido não consegue ler arquivos arbitrários. Asserção: `grep "ipcMain.handle.*wakeWord:load-models" apps/desktop/src/main/ipc/wakeWord.ts` + confirmar que handler ignora `event` args. Registro do handler atravessa `setupIpcHandlers` em `apps/desktop/src/main/ipc/index.ts` (entry real). |
| T-22-02-02 | Tampering | Malicious `.onnx` file swap → onnxruntime bug → arbitrary code execution | mitigate | Modelos vêm via `extraResources` (Plan 04) — bundled no installer read-only. Plan 03 implementa `download-wakeword-models.sh` com checksum verification. Runtime: `ort.env.wasm.numThreads = 1` reduz superfície de ataque (sem shared memory concerns). |
| T-22-02-03 | Information Disclosure | Wake word capture leaked to cloud | mitigate | Invariant #4 testado: `expect(fetch).not.toHaveBeenCalled()` no WakeWordEngine.test.ts. CI grep ban (Plan 03) previne porcupine/picovoice/bumblebee regression. Nenhum código em wakeWord/ pode importar libs network. |
| T-22-02-04 | Denial of Service | Inference CPU drain (PITFALL #4) | mitigate | VAD gate obrigatório (`vadHangover` skip classifier em silêncio). `numThreads: 1`. Cadência 12.5Hz via worklet (1280 samples @ 16kHz). Benchmark empírico roda em Plan 04. Este plan garante a ARQUITETURA correta. |
| T-22-02-05 | Tampering | `rms` zero guard bypass → usuário acha que wake word funciona mas mic está silenciado | mitigate | `RmsZeroGuard.observe` chamado em todo chunk antes do mel inference. Callback dispara exatamente 1 vez por test. Plan 04 fia no callback para degrade path WAKE-08. |
| T-22-02-06 | Information Disclosure | IPC Uint8Array transfer da memória do main process | accept | Buffer dos 4 modelos (~4MB total) atravessa IPC 1x no boot. Renderer já tem acesso a tudo que precisa (são assets públicos do app). Zero segredo exposto. |
</threat_model>

<verification>
## Phase-Level Checks

- [ ] `pnpm --filter @jarvis/desktop test --run` full suite verde (3 novos test files + existentes)
- [ ] `pnpm --filter @jarvis/desktop build` exit 0 (TS compile clean)
- [ ] `test -f apps/desktop/dist/renderer/wakeWordWorklet.js` após build
- [ ] `grep -r "MediaRecorder" apps/desktop/src/renderer/src/voice/wakeWord/` vazio (invariant #3)
- [ ] `grep -r "fetch.*https" apps/desktop/src/renderer/src/voice/wakeWord/` vazio (WAKE-09)
- [ ] `grep "numThreads = 1" apps/desktop/src/renderer/src/voice/wakeWord/` retorna pelo menos 2 matches (modelLoader + WakeWordEngine)
- [ ] `grep "registerWakeWordIpc" apps/desktop/src/main/ipc/index.ts | wc -l` >= 2 (import + call dentro de setupIpcHandlers)
- [ ] `test ! -f apps/desktop/src/main/ipc.ts` (o path antigo NUNCA é criado — entry real é ipc/index.ts)
- [ ] POC A4 resultado registrado em SUMMARY (file:// works OR IPC fallback)
- [ ] preload expõe `window.jarvis.wakeWord.loadModels` (manual check via DevTools em `pnpm dev`)
- [ ] Commits seguem formato pt-BR + emoji
</verification>

<success_criteria>
1. `onnxruntime-web@1.24.3` em dependencies (não devDependencies)
2. 3 novos módulos (rmsZeroGuard, modelLoader, WakeWordEngine) implementados e testados
3. AudioWorklet JS puro em `public/wakeWordWorklet.js`, copiado para `dist/renderer/` pelo build
4. Main-side IPC `wakeWord:load-models` registrado via `registerWakeWordIpc()` em `apps/desktop/src/main/ipc/wakeWord.ts`, com path hardcoded (não aceita args do renderer)
5. `apps/desktop/src/main/ipc/index.ts` (entry real de `setupIpcHandlers`) importa e chama `registerWakeWordIpc()` junto dos outros handlers
6. Preload expõe `window.jarvis.wakeWord.loadModels`
7. WakeWordEngine tem VAD gate funcional (classifier é skipado em silêncio — assertado no test)
8. WakeWordEngine NÃO é montado pelo App ainda (integração = Plan 04)
9. Nenhum fetch externo do módulo wakeWord (invariant testado)
10. POC A4 documentado no SUMMARY
11. Full test suite do desktop verde
</success_criteria>

<output>
After completion, create `.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-02-SUMMARY.md` with:
- Módulos criados + cobertura de teste
- Resultado do POC A4 (file:// url works? OR IPC fallback usado?)
- Confirmação de que WakeWordEngine NÃO é importado fora de `src/renderer/src/voice/wakeWord/`
- Confirmação de que `registerWakeWordIpc()` foi adicionado em `apps/desktop/src/main/ipc/index.ts` (NÃO no `ipc.ts` obsoleto)
- Próximo plan: 22-03 (download script + assets setup) e 22-04 (integração live)
</output>
