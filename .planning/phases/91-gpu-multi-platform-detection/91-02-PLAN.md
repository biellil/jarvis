---
phase: 91-gpu-multi-platform-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 91-01
files_modified:
  - apps/desktop-py/src/jarvis_desktop/device_detect.py
  - apps/desktop-py/src/jarvis_desktop/config.py
  - apps/desktop-py/tests/test_device_detect.py
autonomous: true
requirements:
  - GPU-01
  - GPU-02
  - GPU-04
  - GPU-05
must_haves:
  truths:
    - "device_detect.detect() retorna (device_str, backend_str) tuple correto para o OS atual"
    - "Nenhum device é retornado sem passar pelo allocation test torch.zeros(1, device=...)"
    - "Falha na allocation test faz fallback silencioso para próximo device na cascade"
    - "JarvisConfig tem campo gpu.amd_backend com default 'directml'"
    - "Testes unitários verificam a cascade com mocks de torch"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/device_detect.py"
      provides: "Factory centralizada de detecção de device"
      exports: ["detect", "DeviceResult", "get_fallback_chain"]
    - path: "apps/desktop-py/tests/test_device_detect.py"
      provides: "Testes unitários da cascade de detecção"
      min_lines: 80
    - path: "apps/desktop-py/src/jarvis_desktop/config.py"
      provides: "Campo gpu.amd_backend no JarvisConfig"
      contains: "amd_backend"
  key_links:
    - from: "device_detect.detect()"
      to: "torch.zeros(1, device=...)"
      via: "_allocation_test() interno"
      pattern: "torch\\.zeros.*device"
    - from: "stt.py (Plan 03)"
      to: "device_detect.detect()"
      via: "import direto — device_detect é fonte única de verdade"
      pattern: "from jarvis_desktop.device_detect import detect"
---

<objective>
Criar `device_detect.py` como factory centralizada de detecção de GPU — substitui a lógica local fragmentada em `stt.py` e `tts.py` com uma única fonte de verdade. Adiciona campo de config `gpu.amd_backend` em `JarvisConfig`.

Purpose: GPU-01 (cascade multi-OS), GPU-02 (allocation test anti-false-positive), GPU-04 (MPS), GPU-05 (Vulkan detection-only).
Output: `device_detect.py` + testes + campo de config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md
@.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md
@apps/desktop-py/src/jarvis_desktop/config.py
@apps/desktop-py/src/jarvis_desktop/stt.py
@apps/desktop-py/src/jarvis_desktop/tts.py

<interfaces>
<!-- Existing patterns that device_detect.py must follow -->

From stt.py (existing detection to replace/consolidate):
```python
def _detect_device() -> str:
    # Returns "cuda" or "cpu"
    # Cascade: CUDA → ROCm → MPS → CPU

def _detect_amd_windows() -> bool:
    # Uses subprocess wmic on Windows
    # Returns True if AMD/Radeon GPU found
```

From tts.py (existing detection to replace/consolidate):
```python
def _detect_chatterbox_device() -> list:
    # Returns list of candidates: ["cuda", "cpu"] etc.
    # Cascade: CUDA → MPS → DirectML → CPU
```

From config.py (pattern for new config fields):
```python
class JarvisConfig(BaseModel):
    # All fields use Field(default=..., description="...")
    # New fields added at bottom, never redefine existing ones
    stt_language: str = Field(default="pt", description="...")
```

Singleton + threading.Lock pattern (used in stt.py, tts.py, speaker.py):
```python
_lock = threading.Lock()
_cached: Optional[T] = None

def get_thing() -> T:
    global _cached
    with _lock:
        if _cached is None:
            _cached = _compute()
        return _cached
```

Lazy import pattern (used throughout):
```python
try:
    import torch
    if torch.cuda.is_available():
        candidates.append("cuda")
except (ImportError, Exception):
    pass
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar device_detect.py com cascade e allocation test</name>
  <files>
    apps/desktop-py/src/jarvis_desktop/device_detect.py
    apps/desktop-py/tests/test_device_detect.py
  </files>
  <read_first>
    - .planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md (fallback_strategy — GPU ou CPU-ONLY)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-01..D-10 completos)
    - apps/desktop-py/src/jarvis_desktop/stt.py (linhas 65-150 — _detect_device, _detect_amd_windows)
    - apps/desktop-py/src/jarvis_desktop/tts.py (linhas 537-582 — _detect_chatterbox_device)
    - apps/desktop-py/src/jarvis_desktop/config.py (padrão JarvisConfig e Field)
  </read_first>
  <behavior>
    - Test 1: detect() com CUDA disponível retorna DeviceResult(device="cuda", backend="cuda")
    - Test 2: detect() com CUDA indisponível + MPS disponível retorna DeviceResult(device="mps", backend="mps")
    - Test 3: detect() com CUDA indisponível + MPS indisponível + DirectML disponível retorna DeviceResult(device="directml", backend="directml")
    - Test 4: detect() sem nenhuma GPU retorna DeviceResult(device="cpu", backend="cpu")
    - Test 5: allocation test falha (torch.zeros lança RuntimeError) → device ignorado, próximo da cascade testado
    - Test 6: get_fallback_chain() retorna lista com todos os devices avaliados e seus status (ok/failed/skipped)
    - Test 7: config com amd_backend="rocm" em Windows com HIP SDK → ROCm Windows entra na cascade antes de DirectML
    - Test 8: Vulkan aparece em get_fallback_chain() como "vulkan (detection-only)" quando ctypes encontra vulkan-1.dll/libvulkan.so, mas NUNCA entra no device retornado por detect()
  </behavior>
  <action>
**Read 91-P1-VALIDATION.md first** to determine fallback_strategy (GPU or CPU-ONLY) before writing.

Create `apps/desktop-py/src/jarvis_desktop/device_detect.py`:

```python
"""JARVIS device detection factory — Phase 91.

Single source of truth for GPU/device selection across all subsystems
(STT, TTS Chatterbox, TTS Kokoro). Replaces fragmented detection in
stt.py (_detect_device, _detect_amd_windows) and tts.py (_detect_chatterbox_device).

Public API:
  detect(config) -> DeviceResult          — returns best validated device
  get_fallback_chain(config) -> list[ChainEntry]  — full cascade evaluated

Cascade order (D-02 from 91-CONTEXT.md):
  CUDA → ROCm Linux → MPS (macOS) → DirectML (Windows AMD, if amd_backend="directml")
       → ROCm Windows (if amd_backend="rocm" + HIP SDK) → CPU

Vulkan: detection-only. Exposed in get_fallback_chain() as status string.
Never routed to any subsystem in Phase 91 (D-07).

GPU P-2 safeguard: every candidate passes _allocation_test() before commit.
torch.zeros(1, device=device_str) — RuntimeError = false positive → skip.
"""
from __future__ import annotations

import threading
from dataclasses import dataclass, field
from typing import TYPE_CHECKING, Optional

from loguru import logger

if TYPE_CHECKING:
    from jarvis_desktop.config import JarvisConfig

# ---------------------------------------------------------------------------
# Public types
# ---------------------------------------------------------------------------

@dataclass
class DeviceResult:
    """Result of device detection."""
    device: str          # "cuda" | "mps" | "directml" | "cpu"
    backend: str         # same as device; future: "rocm", "metal" for display
    vram_mb: int = 0     # 0 if unknown or CPU
    driver_info: str = ""  # Optional driver version string for --verbose

@dataclass
class ChainEntry:
    """One entry in the fallback chain evaluation log."""
    device: str
    status: str   # "selected" | "ok" | "failed" | "skipped" | "detection-only"
    reason: str   # Human-readable explanation

# ---------------------------------------------------------------------------
# Module-level cache (D-10: detect on startup, in-memory only, no disk cache)
# ---------------------------------------------------------------------------

_cached_result: Optional[DeviceResult] = None
_cached_chain: Optional[list] = None
_lock = threading.Lock()


# ---------------------------------------------------------------------------
# Public API
# ---------------------------------------------------------------------------

def detect(config: "JarvisConfig") -> DeviceResult:
    """Return best validated compute device.

    First call runs the full cascade + allocation tests.
    Subsequent calls return the cached result (D-10: ~50ms, not hot path).
    Thread-safe.

    Args:
        config: JarvisConfig — reads config.gpu_amd_backend for AMD strategy

    Returns:
        DeviceResult with validated device string ready for use in torch/ctranslate2
    """
    global _cached_result, _cached_chain
    with _lock:
        if _cached_result is None:
            _cached_result, _cached_chain = _run_cascade(config)
        return _cached_result


def get_fallback_chain(config: "JarvisConfig") -> list[ChainEntry]:
    """Return full fallback chain evaluated during last detect() call.

    Triggers detect() if not yet called. Used by jd validate-gpu (GPU-08).

    Args:
        config: JarvisConfig

    Returns:
        List of ChainEntry, one per candidate evaluated (including detection-only)
    """
    global _cached_result, _cached_chain
    with _lock:
        if _cached_chain is None:
            _cached_result, _cached_chain = _run_cascade(config)
        return list(_cached_chain)


def reset_cache() -> None:
    """Clear detection cache. Use in tests only."""
    global _cached_result, _cached_chain
    with _lock:
        _cached_result = None
        _cached_chain = None


# ---------------------------------------------------------------------------
# Internal cascade
# ---------------------------------------------------------------------------

def _run_cascade(config: "JarvisConfig") -> tuple[DeviceResult, list[ChainEntry]]:
    """Run full device cascade. Returns (selected, full_chain_log).

    Cascade order (D-02):
      1. CUDA (NVIDIA, or ROCm Linux via PyTorch ROCm build)
      2. ROCm Linux (explicit /opt/rocm check — for ctranslate2 ROCm wheel)
      3. MPS (macOS Apple Silicon — D-04)
      4. DirectML (Windows AMD/Intel, if amd_backend="directml" and torch_directml installed)
      5. ROCm Windows (if amd_backend="rocm" and HIP SDK via torch ROCm wheel)
      6. [Vulkan detection-only — never selected, D-07]
      7. CPU (always available)
    """
    chain: list[ChainEntry] = []
    amd_backend = getattr(config, "gpu_amd_backend", "directml")

    # 1. CUDA (covers NVIDIA and ROCm Linux via torch+rocm build)
    if _try_cuda(chain):
        return DeviceResult(device="cuda", backend="cuda", vram_mb=_query_vram_mb()), chain

    # 2. MPS (macOS Apple Silicon — D-04)
    if _try_mps(chain):
        return DeviceResult(device="mps", backend="mps"), chain

    # 3. DirectML (Windows AMD/Intel, default path — D-01)
    if amd_backend == "directml":
        if _try_directml(chain):
            return DeviceResult(device="directml", backend="directml"), chain
    else:
        chain.append(ChainEntry("directml", "skipped", f"amd_backend='{amd_backend}' — DirectML opt-out"))

    # 4. ROCm Windows (opt-in via amd_backend="rocm" — D-03)
    if amd_backend == "rocm":
        if _try_cuda(chain, label="rocm-windows"):  # ROCm Windows also reports via torch.cuda
            return DeviceResult(device="cuda", backend="rocm"), chain

    # 5. Vulkan — detection-only (D-07)
    _detect_vulkan(chain)

    # 6. CPU — always available
    chain.append(ChainEntry("cpu", "selected", "Universal fallback"))
    return DeviceResult(device="cpu", backend="cpu"), chain


def _allocation_test(device_str: str) -> bool:
    """GPU P-2: verify device is actually usable by allocating a 1-element tensor.

    Returns True if allocation succeeds, False on any error.
    This catches false positives (e.g., RDNA1 with HIP SDK, Intel Mac with MPS stub).
    """
    try:
        import torch
        t = torch.zeros(1, device=device_str)
        del t
        return True
    except Exception as exc:
        logger.debug(f"[device_detect] allocation test failed for {device_str!r}: {exc}")
        return False


def _try_cuda(chain: list, label: str = "cuda") -> bool:
    """Check CUDA availability and run allocation test."""
    try:
        import torch
        if not torch.cuda.is_available():
            chain.append(ChainEntry(label, "skipped", "torch.cuda.is_available() = False"))
            return False
        if _allocation_test("cuda"):
            chain.append(ChainEntry(label, "selected", f"torch.cuda OK — device 0"))
            return True
        else:
            chain.append(ChainEntry(label, "failed", "allocation test failed (false positive)"))
            return False
    except ImportError:
        chain.append(ChainEntry(label, "skipped", "torch not installed"))
        return False
    except Exception as exc:
        chain.append(ChainEntry(label, "failed", str(exc)))
        return False


def _try_mps(chain: list) -> bool:
    """Check Apple MPS availability and run allocation test (D-04)."""
    try:
        import torch
        if not (hasattr(torch.backends, "mps")
                and torch.backends.mps.is_available()
                and torch.backends.mps.is_built()):
            chain.append(ChainEntry("mps", "skipped", "MPS not available or not built"))
            return False
        if _allocation_test("mps"):
            chain.append(ChainEntry("mps", "selected", "Apple Metal MPS OK"))
            return True
        else:
            chain.append(ChainEntry("mps", "failed", "allocation test failed (Intel Mac false positive)"))
            return False
    except ImportError:
        chain.append(ChainEntry("mps", "skipped", "torch not installed"))
        return False
    except Exception as exc:
        chain.append(ChainEntry("mps", "failed", str(exc)))
        return False


def _try_directml(chain: list) -> bool:
    """Check torch-directml availability and run allocation test."""
    try:
        import torch_directml  # type: ignore[import-not-found]
        if torch_directml.device_count() == 0:
            chain.append(ChainEntry("directml", "skipped", "torch_directml.device_count() = 0"))
            return False
        dml_device = torch_directml.device(0)
        # Allocation test: create tensor on the dml device object
        try:
            import torch
            t = torch.zeros(1, device=dml_device)
            del t
            chain.append(ChainEntry("directml", "selected", "torch-directml allocation OK"))
            return True
        except Exception as exc:
            chain.append(ChainEntry("directml", "failed", f"allocation test failed: {exc}"))
            return False
    except ImportError:
        chain.append(ChainEntry("directml", "skipped", "torch-directml not installed"))
        return False
    except Exception as exc:
        chain.append(ChainEntry("directml", "failed", str(exc)))
        return False


def _detect_vulkan(chain: list) -> None:
    """Detection-only Vulkan check (D-07). Never selected as active device."""
    import ctypes
    import platform
    lib_name = "vulkan-1.dll" if platform.system() == "Windows" else "libvulkan.so.1"
    try:
        ctypes.cdll.LoadLibrary(lib_name)
        chain.append(ChainEntry("vulkan", "detection-only",
                                f"{lib_name} found — routing not enabled in Phase 91"))
    except OSError:
        chain.append(ChainEntry("vulkan", "skipped", f"{lib_name} not found"))


def _query_vram_mb() -> int:
    """Return total VRAM in MB for device 0. Returns 0 if unknown."""
    try:
        import torch
        if torch.cuda.is_available():
            props = torch.cuda.get_device_properties(0)
            return props.total_memory // (1024 ** 2)
    except Exception:
        pass
    return 0
```

Then create `apps/desktop-py/tests/test_device_detect.py` with tests covering all 8 behaviors listed in `<behavior>`. Use `unittest.mock.patch` to mock torch availability without requiring real GPU hardware. Call `device_detect.reset_cache()` in setUp/tearDown of each test.

Key mock targets:
- `jarvis_desktop.device_detect.torch.cuda.is_available` → True/False
- `jarvis_desktop.device_detect.torch.backends.mps.is_available` → True/False
- `jarvis_desktop.device_detect.torch.zeros` → success or raises RuntimeError (allocation test mock)
- `jarvis_desktop.device_detect.torch_directml.device_count` → 0 or 1

For the config, create a minimal mock config:
```python
from types import SimpleNamespace
config = SimpleNamespace(gpu_amd_backend="directml")
```
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -m pytest tests/test_device_detect.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop-py/src/jarvis_desktop/device_detect.py` exists
    - File exports `detect`, `DeviceResult`, `get_fallback_chain`, `reset_cache`, `ChainEntry`
    - `device_detect.py` contains `torch.zeros` (allocation test — GPU P-2)
    - `device_detect.py` contains `threading.Lock` (thread-safe singleton)
    - `device_detect.py` contains `"directml"` and `"mps"` and `"vulkan"` (all cascade branches)
    - `device_detect.py` contains `"detection-only"` (Vulkan D-07)
    - `tests/test_device_detect.py` exists with at least 8 test functions
    - `pytest tests/test_device_detect.py` exits 0 (all tests pass)
  </acceptance_criteria>
  <done>device_detect.py criado com cascade completa D-02, allocation test P-2, e todos os testes passando.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar campo gpu_amd_backend em JarvisConfig</name>
  <files>apps/desktop-py/src/jarvis_desktop/config.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/config.py (linha 131 em diante — final dos campos existentes)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-01: nome do campo, D-02: cascade)
  </read_first>
  <action>
Add a new field at the bottom of JarvisConfig (after `stt_language` field, before the closing class body). Follow the exact Field pattern used by all other fields:

```python
    # Phase 91: GPU device detection (GPU-01, D-01)
    gpu_amd_backend: str = Field(
        default="directml",
        description=(
            "Estratégia AMD Windows: 'directml' = torch-directml (zero friction, default); "
            "'rocm' = torch+rocm7.2.1 (opt-in, requer HIP SDK + wheel ROCm). "
            "Irrelevante em Linux/macOS. D-01 from 91-CONTEXT.md."
        ),
    )
```

Do NOT change any other part of config.py. The field name MUST be `gpu_amd_backend` (not `gpu.amd_backend` — Pydantic BaseModel uses flat field names, not nested dot notation).
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "
from jarvis_desktop.config import JarvisConfig
c = JarvisConfig()
assert c.gpu_amd_backend == 'directml', f'Expected directml, got {c.gpu_amd_backend}'
assert hasattr(c, 'gpu_amd_backend'), 'Field missing'
print('gpu_amd_backend field OK, default=directml')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop-py/src/jarvis_desktop/config.py` contains `gpu_amd_backend`
    - `config.py` contains `default="directml"`
    - `JarvisConfig()` instantiates without error (python -c check exits 0)
    - `c.gpu_amd_backend == "directml"` (default value correct)
    - No existing field names or defaults changed
  </acceptance_criteria>
  <done>Campo gpu_amd_backend adicionado em JarvisConfig com default "directml".</done>
</task>

</tasks>

<verification>
- `pytest apps/desktop-py/tests/test_device_detect.py` exits 0
- `python -c "from jarvis_desktop.config import JarvisConfig; c = JarvisConfig(); print(c.gpu_amd_backend)"` prints `directml`
- `python -c "from jarvis_desktop.device_detect import detect, get_fallback_chain, DeviceResult, ChainEntry"` imports without error
- `grep -n "torch.zeros" apps/desktop-py/src/jarvis_desktop/device_detect.py` finds at least 1 match (allocation test present)
</verification>

<success_criteria>
device_detect.py é a fonte única de verdade de detecção de device. Cascade implementa D-02 completo. Allocation test P-2 obrigatório. Testes unitários passam sem hardware real. JarvisConfig tem gpu_amd_backend com default "directml".
</success_criteria>

<output>
After completion, create `.planning/phases/91-gpu-multi-platform-detection/91-02-SUMMARY.md`
</output>
