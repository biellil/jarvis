---
phase: 91-gpu-multi-platform-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md
autonomous: false
requirements:
  - GPU-03
must_haves:
  truths:
    - "Um documento escrito confirma se torch==2.9.1+rocm7.2.1 é compatível com Chatterbox API"
    - "A estratégia de fallback está documentada antes de qualquer código de feature ser escrito"
    - "Os Plans 02-04 têm o plano de fallback que devem seguir (Chatterbox GPU ou CPU-only)"
  artifacts:
    - path: ".planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md"
      provides: "Resultado do gate de validação P-1 com go/no-go e fallback strategy"
      contains: "## Resultado"
  key_links:
    - from: "91-P1-VALIDATION.md"
      to: "91-02-PLAN.md"
      via: "fallback_strategy field read by Plan 02 executor"
      pattern: "fallback_strategy:"
---

<objective>
Validar em isolamento se `torch==2.9.1+rocm7.2.1` é compatível com a API do Chatterbox TTS antes de qualquer código de feature ser escrito.

Purpose: D-04 — nenhum código de feature é escrito antes do go/no-go. O plano de fallback (D-05/D-06) fica registrado antes do Plan 02 começar.
Output: `.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md` com resultado e fallback strategy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testar compatibilidade torch 2.9.1+rocm7.2.1 com Chatterbox</name>
  <files>.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md</files>
  <read_first>
    - .planning/STATE.md (seção "Pitfalls conhecidos (v3.6 entrada)" — GPU P-1 descrição exata)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-04, D-05, D-06)
    - apps/desktop-py/pyproject.toml (versões atuais de torch/chatterbox no extra [chatterbox])
  </read_first>
  <action>
Execute these compatibility checks in a temporary isolated venv. Record ALL outputs verbatim.

**Step 1 — Create isolation venv:**
```
python -m venv /tmp/jarvis-p1-test
/tmp/jarvis-p1-test/Scripts/activate  # Windows
# or: source /tmp/jarvis-p1-test/bin/activate  # Linux/macOS
```

**Step 2 — Install torch 2.9.1+rocm7.2.1:**
```
pip install torch==2.9.1+rocm7.2.1 --extra-index-url https://download.pytorch.org/whl/rocm7.2.1
```
Record: exit code, any error messages.

**Step 3 — Install Chatterbox with --no-deps (as in production):**
```
pip install chatterbox-tts==0.1.7 --no-deps
```
Record: exit code, any error messages.

**Step 4 — Test API compatibility:**
```python
# test_p1.py
import torch
print(f"torch version: {torch.__version__}")

from chatterbox.mtl_tts import ChatterboxMultilingualTTS
print("Import OK")

# Test instantiation (don't actually load model — just check API signatures)
import inspect
sig = inspect.signature(ChatterboxMultilingualTTS.__init__)
print(f"__init__ params: {list(sig.parameters.keys())}")

# Check for breaking API changes vs torch 2.6.0
# Known pattern: _warmup_chatterbox() calls engine.to(device) and engine.generate(text, language_id="pt")
gen_sig = inspect.signature(ChatterboxMultilingualTTS.generate)
print(f"generate params: {list(gen_sig.parameters.keys())}")
print("API check OK")
```
Record full stdout/stderr.

**Step 5 — If API check passed, test actual device allocation:**
```python
# Only if torch ROCm wheel installed successfully
import torch
device = torch.device("cuda")  # ROCm uses "cuda" string
t = torch.zeros(1, device=device)
print(f"Allocation test: {t.device}")
```
Record: success or error.

Write 91-P1-VALIDATION.md with this exact structure:

```markdown
# Phase 91 — P-1 Validation Gate

**Date:** {today}
**Torch version tested:** {version}
**Chatterbox version:** 0.1.7

## Resultado

**Status:** GO | NO-GO

**go/no-go reason:** {one sentence}

## fallback_strategy: GPU | CPU-ONLY

(GPU = Chatterbox goes to GPU via ROCm — D-06 applies)
(CPU-ONLY = Chatterbox stays on CPU, Whisper+Kokoro get GPU — D-05 applies)

## Test Outputs

### Step 2 — torch install
{verbatim output}

### Step 3 — chatterbox install
{verbatim output}

### Step 4 — API compatibility
{verbatim output}

### Step 5 — Allocation test
{verbatim output or "Skipped — Step 4 failed"}

## Notes for Plan 02 Executor

{Any breaking API changes, version mismatches, or warnings that Plan 02 must account for}
```

If `torch==2.9.1+rocm7.2.1` wheel is not available on this machine (wheel not found for OS), document that explicitly and set fallback_strategy to CPU-ONLY as conservative default. The Plans 02-04 must work regardless of which strategy is chosen — they branch on `fallback_strategy`.
  </action>
  <verify>
    <automated>python -c "
import pathlib
p = pathlib.Path('.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md')
content = p.read_text()
assert '## Resultado' in content, 'Missing Resultado section'
assert 'fallback_strategy:' in content, 'Missing fallback_strategy field'
assert 'Status:' in content, 'Missing Status field'
print('91-P1-VALIDATION.md structure OK')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md` exists
    - File contains `## Resultado` section
    - File contains `fallback_strategy: GPU` or `fallback_strategy: CPU-ONLY`
    - File contains `Status: GO` or `Status: NO-GO`
    - File contains verbatim output from at least Steps 2 and 3
  </acceptance_criteria>
  <done>Documento de validação P-1 escrito com go/no-go e fallback_strategy explícita para os Plans 02-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Revisar resultado do gate P-1</name>
  <files>.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md</files>
  <what-built>Documento 91-P1-VALIDATION.md com resultado do teste de compat torch 2.9.1 + Chatterbox.</what-built>
  <action>Human review checkpoint — verificar conteúdo de 91-P1-VALIDATION.md e confirmar fallback_strategy antes de prosseguir.</action>
  <how-to-verify>
    1. Leia `.planning/phases/91-gpu-multi-platform-detection/91-P1-VALIDATION.md`
    2. Confirme que o resultado está claro: GO (Chatterbox vai para GPU) ou NO-GO (Chatterbox fica em CPU)
    3. Confirme que `fallback_strategy` está definida (GPU ou CPU-ONLY)
    4. Se o resultado mudar a estratégia planejada (ex: NO-GO inesperado), ajuste antes de continuar
  </how-to-verify>
  <verify>91-P1-VALIDATION.md reviewed and fallback_strategy confirmed by user</verify>
  <done>Usuário aprovou o resultado do gate P-1 e a fallback_strategy está registrada.</done>
  <resume-signal>Digite "aprovado" ou descreva problemas encontrados</resume-signal>
</task>

</tasks>

<verification>
- 91-P1-VALIDATION.md existe com estrutura completa
- fallback_strategy está definida (GPU ou CPU-ONLY) — Plans 02-04 vão ler esse valor
- Resultado do gate está aprovado pelo usuário antes dos plans de feature começarem
</verification>

<success_criteria>
O gate P-1 está concluído quando: (1) o documento de validação existe com go/no-go explícito, (2) a fallback_strategy está registrada para Plans 02-04 seguirem, (3) o usuário aprovou o resultado.
</success_criteria>

<output>
After completion, create `.planning/phases/91-gpu-multi-platform-detection/91-01-SUMMARY.md`
</output>
