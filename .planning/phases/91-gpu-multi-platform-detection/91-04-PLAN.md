---
phase: 91-gpu-multi-platform-detection
plan: 04
type: execute
wave: 3
depends_on:
  - 91-02
files_modified:
  - apps/desktop-py/src/jarvis_desktop/__main__.py
  - apps/desktop-py/pyproject.toml
  - apps/desktop-py/README.md
autonomous: true
requirements:
  - GPU-08
  - GPU-09
must_haves:
  truths:
    - "jd validate-gpu imprime device detectado, fallback chain completo e status de compat por subsistema"
    - "jd validate-gpu --json retorna JSON estruturado (para scripting/health-check)"
    - "pyproject.toml tem os 4 extras: [nvidia-gpu], [amd-gpu-windows], [apple-silicon], [vulkan]"
    - "apps/desktop-py/README.md documenta os 4 extras com comandos de instalação"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/__main__.py"
      provides: "Subcomando validate-gpu em _entry()"
      contains: "validate-gpu"
    - path: "apps/desktop-py/pyproject.toml"
      provides: "4 extras de GPU"
      contains: "nvidia-gpu"
    - path: "apps/desktop-py/README.md"
      provides: "Documentação de instalação com extras GPU"
      contains: "## Instalação"
  key_links:
    - from: "__main__.py::_entry()"
      to: "device_detect.detect() + get_fallback_chain()"
      via: "validate-gpu subcommand handler"
      pattern: "validate-gpu"
    - from: "pyproject.toml extras"
      to: "README.md install section"
      via: "documentação dos mesmos nomes de extras"
      pattern: "nvidia-gpu|amd-gpu-windows|apple-silicon|vulkan"
---

<objective>
Implementar o subcomando `jd validate-gpu` na CLI existente e adicionar os extras de GPU em `pyproject.toml` com documentação em README.

Purpose: GPU-08 (CLI que mostra device + fallback chain) e GPU-09 (extras documentados para instalação por plataforma).
Output: `jd validate-gpu` executável, 4 extras em pyproject.toml, README.md com seção de instalação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md
@.planning/phases/91-gpu-multi-platform-detection/91-02-SUMMARY.md
@apps/desktop-py/src/jarvis_desktop/__main__.py
@apps/desktop-py/pyproject.toml

<interfaces>
<!-- API de device_detect.py usada neste plan -->
```python
from jarvis_desktop.device_detect import detect, get_fallback_chain, DeviceResult, ChainEntry

result: DeviceResult = detect(config)
# result.device   = "cuda" | "mps" | "directml" | "cpu"
# result.backend  = same, or "rocm" for display
# result.vram_mb  = int (0 if unknown)

chain: list[ChainEntry] = get_fallback_chain(config)
# chain[i].device  = device name
# chain[i].status  = "selected" | "ok" | "failed" | "skipped" | "detection-only"
# chain[i].reason  = human-readable string
```

<!-- Ponto de extensão em __main__.py -->
```python
def _entry() -> None:
    import sys
    if len(sys.argv) > 1 and sys.argv[1] == "setup":
        from jarvis_desktop.setup_wizard import run_setup
        run_setup()
    else:
        main()
# Adicionar: elif sys.argv[1] == "validate-gpu":
```

<!-- Extras existentes em pyproject.toml (para referência de formato) -->
```toml
[project.optional-dependencies]
dev = ["pytest>=8.0.0", ...]
voice-cloning = ["librosa>=0.10.0"]
chatterbox = ["chatterbox-tts==0.1.7", "torch==2.6.0", ...]
speaker = ["webrtcvad-wheels==2.0.14", "resemblyzer==0.1.4"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar subcomando validate-gpu em __main__.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/__main__.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/__main__.py (completo — ver _entry() e main())
    - apps/desktop-py/src/jarvis_desktop/device_detect.py (API: detect, get_fallback_chain, DeviceResult, ChainEntry)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-08, D-09, D-10)
  </read_first>
  <action>
    Adicionar a função `_cmd_validate_gpu()` e o branch `validate-gpu` em `_entry()`.

    **1. Criar a função `_cmd_validate_gpu(args: list[str]) -> None` em __main__.py:**

    ```python
    def _cmd_validate_gpu(args: list[str]) -> None:
        """jd validate-gpu — mostra device detectado, fallback chain e compat por subsistema.

        Flags:
          --verbose : expande para diagnóstico completo (driver info, todas GPUs enumeradas)
          --json    : saída JSON estruturada para scripting/health-check (D-09)
        """
        import json as _json
        from jarvis_desktop.config import load_config
        from jarvis_desktop.device_detect import detect, get_fallback_chain, reset_cache

        verbose = "--verbose" in args
        as_json = "--json" in args

        config = load_config()
        # Force fresh detection (in case called standalone; no cached result from main())
        reset_cache()

        result = detect(config)
        chain = get_fallback_chain(config)

        if as_json:
            data = {
                "device": result.device,
                "backend": result.backend,
                "vram_mb": result.vram_mb,
                "driver_info": result.driver_info,
                "fallback_chain": [
                    {"device": e.device, "status": e.status, "reason": e.reason}
                    for e in chain
                ],
                "subsystems": {
                    "whisper_faster": result.device,
                    "kokoro": result.device,
                    "chatterbox": result.device,  # may differ if P-1 NO-GO; update after Plan 03
                },
            }
            print(_json.dumps(data, ensure_ascii=False, indent=2))
            return

        # Rich table output (PT-BR — D-10)
        from rich.console import Console
        from rich.table import Table
        from rich.panel import Panel

        console = Console()

        # Header
        vram_str = f" — {result.vram_mb} MB VRAM" if result.vram_mb > 0 else ""
        console.print(Panel(
            f"[bold cyan]Device selecionado:[/bold cyan] [bold green]{result.device}[/bold green]{vram_str}",
            title="[bold]JARVIS — Detecção de GPU[/bold]",
            expand=False,
        ))

        # Fallback chain table
        table = Table(title="Fallback Chain Avaliada", show_header=True, header_style="bold magenta")
        table.add_column("Device", style="cyan", width=14)
        table.add_column("Status", width=16)
        table.add_column("Motivo")

        status_styles = {
            "selected": "[bold green]selected[/bold green]",
            "ok": "[green]ok[/green]",
            "failed": "[red]failed[/red]",
            "skipped": "[yellow]skipped[/yellow]",
            "detection-only": "[blue]detection-only[/blue]",
        }

        for entry in chain:
            styled_status = status_styles.get(entry.status, entry.status)
            table.add_row(entry.device, styled_status, entry.reason)

        console.print(table)

        # Subsystem compat
        console.print("\n[bold]Compatibilidade por subsistema:[/bold]")
        subsystem_device = result.device
        console.print(f"  Whisper (faster-whisper) : [green]{subsystem_device}[/green]")
        console.print(f"  Kokoro TTS               : [green]{subsystem_device}[/green]")
        console.print(f"  Chatterbox TTS           : [green]{subsystem_device}[/green]")

        if verbose:
            console.print("\n[bold]Diagnóstico detalhado:[/bold]")
            console.print(f"  gpu_amd_backend config   : {config.gpu_amd_backend}")
            if result.driver_info:
                console.print(f"  Driver info              : {result.driver_info}")
    ```

    **2. Atualizar `_entry()` para reconhecer o subcomando:**

    ANTES:
    ```python
    def _entry() -> None:
        import sys
        if len(sys.argv) > 1 and sys.argv[1] == "setup":
            from jarvis_desktop.setup_wizard import run_setup
            run_setup()
        else:
            main()
    ```

    DEPOIS:
    ```python
    def _entry() -> None:
        import sys
        args = sys.argv[1:]
        if args and args[0] == "setup":
            from jarvis_desktop.setup_wizard import run_setup
            run_setup()
        elif args and args[0] == "validate-gpu":
            _cmd_validate_gpu(args[1:])
        else:
            main()
    ```
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "
from jarvis_desktop.__main__ import _cmd_validate_gpu, _entry
print('_cmd_validate_gpu importa OK')
import inspect
sig = inspect.signature(_cmd_validate_gpu)
print(f'signature: {sig}')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `__main__.py` contains `def _cmd_validate_gpu(`
    - `__main__.py` contains `"validate-gpu"` (subcommand routing)
    - `__main__.py` contains `"--json"` (JSON flag handling)
    - `__main__.py` contains `get_fallback_chain` (chain display)
    - `python -c "from jarvis_desktop.__main__ import _cmd_validate_gpu"` exits 0
    - `__main__.py` does NOT contain the old single-arg pattern `sys.argv[1] == "setup"` (updated to use `args[0]`)
  </acceptance_criteria>
  <done>_cmd_validate_gpu() implementado com saída rich table (default) e --json. _entry() rota para o subcomando quando sys.argv[1] == "validate-gpu".</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar extras GPU em pyproject.toml e criar README com documentação</name>
  <files>
    apps/desktop-py/pyproject.toml
    apps/desktop-py/README.md
  </files>
  <read_first>
    - apps/desktop-py/pyproject.toml (extras existentes: chatterbox, speaker, voice-cloning, dev)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-01, D-03 — wheels ROCm/DirectML)
    - .planning/ROADMAP.md (Phase 91 success criteria #5)
  </read_first>
  <action>
    **1. Adicionar 4 extras em pyproject.toml, após o extra `speaker`:

    ```toml
    nvidia-gpu = [
        # Phase 91 — NVIDIA GPU aceleração (CUDA)
        # Substitui torch 2.6.0 CPU-only por build CUDA 12.4
        # NOTA: mutuamente exclusivo com torch 2.6.0 do extra [chatterbox]
        # Instalar ANTES do [chatterbox] ou usar: pip install ".[nvidia-gpu,chatterbox]"
        "torch==2.6.0+cu124; sys_platform != 'darwin'",
        "torchaudio==2.6.0+cu124; sys_platform != 'darwin'",
    ]
    amd-gpu-windows = [
        # Phase 91 — AMD GPU no Windows via DirectML (D-01, default AMD path)
        # torch-directml já está no [chatterbox] extra como dep opcional.
        # Este extra torna explícito o install para usuários AMD Windows sem Chatterbox.
        "torch-directml==0.2.5.dev240914; sys_platform == 'win32'",
    ]
    apple-silicon = [
        # Phase 91 — Apple Silicon (M1/M2/M3) via Metal MPS (D-04)
        # PyTorch padrão já inclui MPS backend no wheel macOS arm64.
        # Este extra documenta que nenhum install adicional é necessário.
        # "torch" já está nas deps base — MPS habilitado automaticamente no macOS arm64.
    ]
    vulkan = [
        # Phase 91 — Vulkan detection (D-07)
        # ctranslate2[vulkan] tem wheel conflict com ctranslate2[cuda] — DEFERRED para v3.7+
        # Whisper.cpp com Vulkan backend: instalar whisper-cpp-bindings separado
        # Este extra reservado para quando o conflito for resolvido upstream
    ]
    ```

    **2. Criar `apps/desktop-py/README.md`** com seção de instalação documentando os extras.
    O README deve ter as seguintes seções:

    ```markdown
    # JARVIS Desktop — Python Client

    Cliente Python do JARVIS: pipeline de voz (STT → LLM → TTS) com detecção automática de GPU.

    ## Requisitos

    - Python 3.12+
    - [uv](https://docs.astral.sh/uv/) (gerenciador de pacotes recomendado)

    ## Instalação

    ### Instalação base (CPU)

    ```bash
    cd apps/desktop-py
    uv sync
    ```

    ### Extras por plataforma GPU

    | Extra | Plataforma | Quando usar |
    |-------|------------|-------------|
    | `[nvidia-gpu]` | Linux / Windows | NVIDIA GPU com CUDA 12.4+ |
    | `[amd-gpu-windows]` | Windows | AMD GPU (RDNA2+) via DirectML |
    | `[apple-silicon]` | macOS (M1/M2/M3) | Metal MPS — nenhum extra necessário* |
    | `[vulkan]` | Reservado | Pendente resolução de conflito upstream |

    *MPS está incluído no PyTorch padrão para macOS arm64.

    #### NVIDIA GPU (Linux / Windows)

    ```bash
    uv sync --extra nvidia-gpu --extra chatterbox
    ```

    #### AMD GPU Windows (DirectML — default, zero friction)

    ```bash
    uv sync --extra amd-gpu-windows --extra chatterbox
    ```

    #### AMD GPU Windows (ROCm — opt-in, requer HIP SDK)

    ```bash
    # Instalar torch ROCm wheel manualmente (mutuamente exclusivo com DirectML):
    pip install torch==2.9.1+rocm7.2.1 --extra-index-url https://download.pytorch.org/whl/rocm7.2.1
    uv sync --extra chatterbox
    # Configurar: editar ~/.jarvis/config.json → "gpu_amd_backend": "rocm"
    ```

    #### Apple Silicon (macOS M1/M2/M3)

    ```bash
    uv sync --extra apple-silicon
    # MPS habilitado automaticamente — nenhuma configuração adicional
    ```

    ## TTS Neural (Chatterbox)

    Para ativar Chatterbox TTS (voz emocional multilingual):

    ```bash
    uv sync --extra chatterbox
    ```

    ## Diagnóstico de GPU

    ```bash
    jd validate-gpu              # Mostra device detectado + fallback chain
    jd validate-gpu --verbose    # Diagnóstico completo com versão de driver
    jd validate-gpu --json       # JSON estruturado para scripting
    ```

    ## Uso

    ```bash
    jd                 # Inicia cliente JARVIS (chat loop)
    jd setup           # Assistente de configuração inicial
    jd validate-gpu    # Diagnóstico de GPU
    ```
    ```

    Ensure the README uses proper markdown (no HTML tags). The install section is what GPU-09 requires to be documented.
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "
import tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
extras = data.get('project', {}).get('optional-dependencies', {})
required = ['nvidia-gpu', 'amd-gpu-windows', 'apple-silicon', 'vulkan']
for name in required:
    assert name in extras, f'Missing extra: {name}'
    print(f'Extra [{name}]: OK')
print('All 4 GPU extras present')
"
</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `nvidia-gpu` in `[project.optional-dependencies]`
    - `pyproject.toml` contains `amd-gpu-windows` in `[project.optional-dependencies]`
    - `pyproject.toml` contains `apple-silicon` in `[project.optional-dependencies]`
    - `pyproject.toml` contains `vulkan` in `[project.optional-dependencies]`
    - `python -c "import tomllib; ..."` check (above) exits 0
    - `apps/desktop-py/README.md` exists
    - `README.md` contains `## Instalação`
    - `README.md` contains `nvidia-gpu` and `amd-gpu-windows` and `apple-silicon` and `vulkan`
    - `README.md` contains `jd validate-gpu` (CLI documented)
  </acceptance_criteria>
  <done>4 extras GPU em pyproject.toml. README.md criado com seção de instalação documentando todos os extras e os comandos jd validate-gpu.</done>
</task>

</tasks>

<verification>
- `jd validate-gpu --json` executa sem erro e imprime JSON com campos "device", "backend", "fallback_chain" (requer GPU ou CPU fallback)
- `python -c "import tomllib; ..."` verifica os 4 extras presentes em pyproject.toml (exits 0)
- `apps/desktop-py/README.md` existe com seção ## Instalação listando os 4 extras
- `grep -n "validate-gpu" apps/desktop-py/src/jarvis_desktop/__main__.py` retorna pelo menos 2 matches (routing + function)
</verification>

<success_criteria>
`jd validate-gpu` funciona como comando CLI standalone com saída rich (default) e --json. pyproject.toml tem os 4 extras GPU. README documenta instalação por plataforma. Todos os requisitos GPU-08 e GPU-09 atendidos.
</success_criteria>

<output>
After completion, create `.planning/phases/91-gpu-multi-platform-detection/91-04-SUMMARY.md`
</output>
