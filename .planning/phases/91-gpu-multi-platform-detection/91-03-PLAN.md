---
phase: 91-gpu-multi-platform-detection
plan: 03
type: execute
wave: 3
depends_on:
  - 91-02
files_modified:
  - apps/desktop-py/src/jarvis_desktop/stt.py
  - apps/desktop-py/src/jarvis_desktop/tts.py
  - apps/desktop-py/tests/test_stt.py
  - apps/desktop-py/tests/test_tts.py
autonomous: true
requirements:
  - GPU-06
  - GPU-07
must_haves:
  truths:
    - "stt.py não tem mais _detect_device() local — usa device_detect.detect() para obter device"
    - "tts.py não tem mais _detect_chatterbox_device() local — usa device_detect.detect() para obter device"
    - "Falha de init em qualquer subsistema cai para CPU sem crash (fallback automático)"
    - "Testes de stt e tts passam com mocks que incluem device_detect"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/stt.py"
      provides: "STT init usando device_detect.detect()"
      contains: "from jarvis_desktop.device_detect import detect"
    - path: "apps/desktop-py/src/jarvis_desktop/tts.py"
      provides: "TTS Chatterbox warmup usando device_detect.detect()"
      contains: "from jarvis_desktop.device_detect import detect"
  key_links:
    - from: "stt.py::init_stt()"
      to: "device_detect.detect()"
      via: "substituição de _detect_device() call"
      pattern: "device_detect\\.detect|from jarvis_desktop\\.device_detect"
    - from: "tts.py::_start_chatterbox_warmup()"
      to: "device_detect.detect()"
      via: "substituição de _detect_chatterbox_device() call"
      pattern: "device_detect\\.detect|from jarvis_desktop\\.device_detect"
---

<objective>
Fazer stt.py e tts.py consumirem `device_detect.detect()` ao invés de executar lógica de detecção própria. Remove código duplicado e estabelece fonte única de verdade para device selection (GPU-06, GPU-07).

Purpose: GPU-06 e GPU-07 — subsistemas não devem ter lógica de detecção local. Toda decisão de device passa por device_detect.
Output: stt.py e tts.py com chamadas a device_detect.detect(); funções locais de detecção removidas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md
@.planning/phases/91-gpu-multi-platform-detection/91-02-SUMMARY.md
@apps/desktop-py/src/jarvis_desktop/device_detect.py

<interfaces>
<!-- API de device_detect.py (criado em Plan 02) -->

```python
from jarvis_desktop.device_detect import detect, DeviceResult

# detect() retorna DeviceResult(device="cuda"|"mps"|"directml"|"cpu", backend=..., vram_mb=int)
result: DeviceResult = detect(config)
device_str: str = result.device  # string pronta para usar em WhisperModel(device=device_str)
vram_mb: int = result.vram_mb    # 0 se CPU ou desconhecido
```

<!-- Funções locais que serão REMOVIDAS de stt.py -->
```python
# stt.py — REMOVER estas funções (D-06 — Plan 02 as substitui):
def _detect_device() -> str: ...          # linha ~65
def _detect_amd_windows() -> bool: ...    # linha ~114
```

<!-- Função local que será REMOVIDA de tts.py -->
```python
# tts.py — REMOVER esta função (D-07):
def _detect_chatterbox_device() -> list: ...  # linha ~537
```

<!-- Padrão de uso em stt.py::init_stt() — substituição -->
# ANTES (remover):
device = _detect_device()
# DEPOIS (adicionar):
from jarvis_desktop.device_detect import detect as _detect_device_factory
_dd_result = _detect_device_factory(config)
device = _dd_result.device

<!-- Padrão de uso em tts.py::_start_chatterbox_warmup() — substituição -->
# ANTES (remover):
candidates = _detect_chatterbox_device()
device = candidates[0]  # or first available
# DEPOIS:
from jarvis_desktop.device_detect import detect as _detect_device_factory
_dd_result = _detect_device_factory(config)
device = _dd_result.device
# A cascade device_detect já inclui "cpu" como último fallback — o mesmo que _detect_chatterbox_device fazia
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar stt.py — substituir _detect_device() por device_detect.detect()</name>
  <files>
    apps/desktop-py/src/jarvis_desktop/stt.py
    apps/desktop-py/tests/test_stt.py
  </files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/stt.py (completo — linhas 1-531)
    - apps/desktop-py/src/jarvis_desktop/device_detect.py (API: detect, DeviceResult)
    - apps/desktop-py/tests/test_stt.py (testes existentes que devem continuar passando)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-06)
  </read_first>
  <behavior>
    - Test existing: test_init_whisper_model_loads_successfully continua passando com mock atualizado
    - Test existing: test_init_whisper_model_with_invalid_size continua passando
    - Test new: test_stt_uses_device_detect — init_stt() com mocked device_detect.detect() retorna DeviceResult(device="cuda"); verify WhisperModel chamado com device="cuda"
    - Test new: test_stt_cpu_fallback_on_device_detect_fail — se device_detect.detect() retorna "cuda" mas WhisperModel(device="cuda") lança Exception, init_stt() faz retry com "cpu"
  </behavior>
  <action>
    Em `stt.py`, realizar estas mudanças cirúrgicas:

    **1. Remover as funções `_detect_device()` (linhas ~65-111) e `_detect_amd_windows()` (linhas ~114-134).**
    Não remover docstring do módulo — atualizar a seção "Public API" para refletir que as funções foram removidas e substituídas por device_detect.

    **2. Na função `init_stt()`, substituir o call de `_detect_device()`:**

    ANTES (localizar e remover):
    ```python
    device = _detect_device()
    ```

    DEPOIS (inserir no mesmo lugar):
    ```python
    from jarvis_desktop.device_detect import detect as _device_factory
    _device_result = _device_factory(config)
    device = _device_result.device
    # vram_mb agora vem do device_detect (elimina _query_vram_mb() call duplicado)
    if not config.whisper_model_locked:
        vram_mb = _device_result.vram_mb
        model_size = _select_model_for_device(device, vram_mb)
    else:
        model_size = config.whisper_model
    ```

    **3. Na seção que resolve o backend h98 em `init_stt()`**, remover o import e uso de `_detect_amd_windows`:
    ANTES:
    ```python
    if resolved_backend == "auto":
        resolved_backend = "whisper_cpp" if _detect_amd_windows() else "faster_whisper"
    ```
    DEPOIS: determinar AMD Windows através de device_detect — usar `_device_result.backend == "directml"` ou verificar platform + device_detect diretamente.

    Concretamente, substituir por:
    ```python
    if resolved_backend == "auto":
        import platform as _platform
        if _platform.system() == "Windows":
            # Checa se AMD/Radeon GPU via wmic (mantém lógica funcional de h98)
            # mas usa device_detect para consistency — se DirectML detectado, usa whisper.cpp
            _temp_result = _device_factory(config)
            resolved_backend = "whisper_cpp" if _temp_result.backend == "directml" else "faster_whisper"
        else:
            resolved_backend = "faster_whisper"
    ```

    **4. Remover o call a `_query_vram_mb()` que estava depois do `_detect_device()` call** (já incorporado ao device_detect via vram_mb):
    ```python
    # REMOVER esta linha (vram_mb já vem do device_detect):
    vram_mb = _query_vram_mb() if device == "cuda" else 0
    ```

    **5. Manter `_query_vram_mb()` no arquivo** — ainda é usada internamente por outras paths ou pode ser removida se não há mais referências. Verificar com grep antes de remover.

    **6. Atualizar `__main__.py` reference:** `__main__.py` linha ~63 chama `_detect_amd_windows` diretamente de stt. Após remover de stt.py, essa linha vai quebrar. Substituir em `__main__.py`:
    ANTES:
    ```python
    from jarvis_desktop.stt import _detect_amd_windows
    _resolved_backend = config.stt_backend
    if _resolved_backend == "auto":
        _resolved_backend = "whisper_cpp" if _detect_amd_windows() else "faster_whisper"
    ```
    DEPOIS:
    ```python
    import platform as _platform
    _resolved_backend = config.stt_backend
    if _resolved_backend == "auto":
        if _platform.system() == "Windows":
            from jarvis_desktop.device_detect import detect as _dd
            _resolved_backend = "whisper_cpp" if _dd(config).backend == "directml" else "faster_whisper"
        else:
            _resolved_backend = "faster_whisper"
    ```

    **7. Atualizar test_stt.py:**
    - Mock existente `mock_whisper_model` (no conftest.py) deve continuar funcionando
    - Adicionar mock de `jarvis_desktop.device_detect.detect` para os novos testes
    - Os 4 tests listados em `<behavior>` devem ser criados/atualizados
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -m pytest tests/test_stt.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `stt.py` does NOT contain `def _detect_device(` (function removed)
    - `stt.py` does NOT contain `def _detect_amd_windows(` (function removed)
    - `stt.py` contains `from jarvis_desktop.device_detect import` (new import present)
    - `__main__.py` does NOT contain `from jarvis_desktop.stt import _detect_amd_windows` (reference removed)
    - `pytest tests/test_stt.py` exits 0 (all tests pass including new ones)
    - `python -c "from jarvis_desktop.stt import init_stt"` imports without error
  </acceptance_criteria>
  <done>stt.py delega device selection para device_detect.detect(). _detect_device() e _detect_amd_windows() removidos. __main__.py atualizado. Todos os testes passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar tts.py — substituir _detect_chatterbox_device() por device_detect.detect()</name>
  <files>
    apps/desktop-py/src/jarvis_desktop/tts.py
    apps/desktop-py/tests/test_tts.py
  </files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/tts.py (linhas 537-660 — _detect_chatterbox_device e _start_chatterbox_warmup)
    - apps/desktop-py/src/jarvis_desktop/device_detect.py (API: detect, DeviceResult)
    - apps/desktop-py/tests/test_tts.py (testes existentes que devem continuar passando)
    - .planning/phases/91-gpu-multi-platform-detection/91-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <behavior>
    - Test existing: todos os testes de tts.py existentes continuam passando
    - Test new: test_tts_chatterbox_uses_device_detect — _start_chatterbox_warmup() chama device_detect.detect() (não _detect_chatterbox_device)
    - Test new: test_tts_chatterbox_cpu_fallback — se device_detect retorna "cuda" mas _create_chatterbox_engine lança RuntimeError, warmup retenta com "cpu" e marca _chatterbox_available=True
  </behavior>
  <action>
    Em `tts.py`, realizar estas mudanças cirúrgicas:

    **1. Remover a função `_detect_chatterbox_device()` (linhas ~537-581).**

    **2. Em `_start_chatterbox_warmup()`, localizar onde `_detect_chatterbox_device()` é chamado** e substituir:

    ANTES (padrão aproximado na _warmup_worker interna):
    ```python
    candidates = _detect_chatterbox_device()
    for device in candidates:
        try:
            engine = _create_chatterbox_engine(config, device)
            ...
            break
        except (ImportError, RuntimeError):
            continue
    ```

    DEPOIS:
    ```python
    from jarvis_desktop.device_detect import detect as _device_factory
    _dd_result = _device_factory(config)
    # device_detect já retorna o melhor device com allocation test — tentar esse primeiro
    # Se falhar (ex: Chatterbox não suporta este device), retry com CPU (D-05)
    primary_device = _dd_result.device
    devices_to_try = [primary_device] if primary_device != "cpu" else []
    devices_to_try.append("cpu")

    for device in devices_to_try:
        try:
            engine = _create_chatterbox_engine(config, device)
            # smoke test: gerar texto curto
            _run_warmup_generate(engine)
            # success
            _chatterbox_device_selected = device  # store locally for _jd_validate info
            break
        except (ImportError, RuntimeError, Exception):
            logger.warning(f"[TTS] Chatterbox falhou em {device!r} — tentando próximo")
            continue
    ```

    Note: the exact structure depends on how `_start_chatterbox_warmup` is currently implemented. Read the full function before making changes. The key invariant: wherever `_detect_chatterbox_device()` was called, replace with `detect(config).device`.

    **3. Update the `_chatterbox_device` global:** Instead of being set by the old detection function, it is now set from `device_detect.detect(config).device` at warmup time.

    **4. Atualizar test_tts.py:**
    - Manter todos os mocks existentes
    - Adicionar mock de `jarvis_desktop.device_detect.detect` nos novos testes
    - Os 2 novos tests listados em `<behavior>` devem ser adicionados
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -m pytest tests/test_tts.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `tts.py` does NOT contain `def _detect_chatterbox_device(` (function removed)
    - `tts.py` contains `from jarvis_desktop.device_detect import` (new import present)
    - `pytest tests/test_tts.py` exits 0 (all tests pass including new ones)
    - `python -c "from jarvis_desktop.tts import init_tts"` imports without error
  </acceptance_criteria>
  <done>tts.py delega device selection para device_detect.detect(). _detect_chatterbox_device() removido. CPU fallback preservado. Testes passam.</done>
</task>

</tasks>

<verification>
- `grep -rn "_detect_device\|_detect_amd_windows\|_detect_chatterbox_device" apps/desktop-py/src/` finds zero matches (all local detection functions removed from stt.py/tts.py)
- `grep -rn "from jarvis_desktop.device_detect import" apps/desktop-py/src/jarvis_desktop/stt.py apps/desktop-py/src/jarvis_desktop/tts.py` finds matches in both files
- `cd apps/desktop-py && python -m pytest tests/test_stt.py tests/test_tts.py -v` exits 0
</verification>

<success_criteria>
Única fonte de verdade para device selection: device_detect.detect(). Funções locais de detecção removidas de stt.py e tts.py. Todos os testes de STT e TTS passam com mocks atualizados.
</success_criteria>

<output>
After completion, create `.planning/phases/91-gpu-multi-platform-detection/91-03-SUMMARY.md`
</output>
