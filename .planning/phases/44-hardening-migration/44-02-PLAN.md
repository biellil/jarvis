---
phase: 44-hardening-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/scripts/soak-test.ts
  - README.md
autonomous: true
requirements:
  - VHARD-01

must_haves:
  truths:
    - "Script soak-test.ts existe em apps/desktop/scripts/ e executa sem erro de sintaxe TypeScript"
    - "Script mede process.memoryUsage().heapUsed E rss (não só heap — Pitfall 2 do RESEARCH.md)"
    - "Baseline é capturado aos t=30s (após warm-up), amostragem a cada 30 minutos"
    - "Ao final (t=8h ou Ctrl+C), script imprime delta heap em MB e resultado PASS/FAIL (delta <10MB = PASS)"
    - "README.md documenta o comando de execução: node --expose-gc apps/desktop/scripts/soak-test.ts"
  artifacts:
    - path: "apps/desktop/scripts/soak-test.ts"
      provides: "Soak test script standalone — mede heap e RSS ao longo de 8h"
      contains: "process.memoryUsage"
    - path: "README.md"
      provides: "Instrução de execução do soak test (D-10)"
      contains: "soak-test"
  key_links:
    - from: "soak-test.ts"
      to: "process.memoryUsage().heapUsed"
      via: "Node.js stdlib"
      pattern: "memoryUsage"
    - from: "soak-test.ts"
      to: "console.log PASS/FAIL"
      via: "delta calculation"
      pattern: "PASS|FAIL"
---

<objective>
Script standalone de soak test para validar estabilidade de heap em Always-Listening mode ao longo de 8 horas. Script mede heapUsed e RSS no processo main, captura baseline em t=30s, amostra a cada 30 minutos, e reporta delta PASS/FAIL ao final.

Purpose: VHARD-01 (D-07 a D-10) — detectar vazamentos de memória no AlwaysListeningEngine antes de um release de produção. Não faz parte do CI (muito lento); executado manualmente antes de cada release.

Output:
- apps/desktop/scripts/soak-test.ts: script standalone completo
- README.md: seção "Soak Test" com comando de execução (D-10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-hardening-migration/44-CONTEXT.md
@.planning/phases/44-hardening-migration/44-RESEARCH.md
@README.md
</context>

<interfaces>
<!-- Referência da API Node.js usada no soak test -->

```typescript
// process.memoryUsage() retorna:
interface MemoryUsage {
  rss: number;       // Resident Set Size (total process memory, inclui native)
  heapTotal: number; // Total JS heap alocado
  heapUsed: number;  // JS heap em uso (métrica primária D-08)
  external: number;  // C++ objects linked to JS objects
  arrayBuffers: number;
}

// global.gc() disponível apenas com --expose-gc flag
// Verificar antes de chamar: if (global.gc) global.gc();
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Script soak-test.ts standalone</name>
  <files>
    apps/desktop/scripts/soak-test.ts
  </files>

  <read_first>
    - .planning/phases/44-hardening-migration/44-CONTEXT.md (D-07 a D-10 — requisitos do soak test)
    - .planning/phases/44-hardening-migration/44-RESEARCH.md (Pattern 4 — exemplo de código; Pitfall 2 — medir RSS também; Pitfall 5 — GC non-determinism)
    - apps/desktop/scripts/ (ver arquivos existentes para contexto do diretório)
  </read_first>

  <action>
    Criar `apps/desktop/scripts/soak-test.ts` como script Node.js standalone (não é test vitest).

    O script faz o seguinte:
    1. Imprime header com timestamp de início
    2. Aguarda 30 segundos (warm-up) antes de capturar baseline
    3. Captura baseline: `{ time: Date.now(), heapUsed, rss }` de `process.memoryUsage()`
    4. Configura setInterval de 30 minutos para amostrar heap + rss
    5. Em cada amostra: opcionalmente força GC se `global.gc` disponível, lê memória, imprime linha de log
    6. Configura SIGINT handler para permitir Ctrl+C com relatório gracioso
    7. Configura timeout de 8 horas (28800000ms) para finalização automática
    8. Ao finalizar: calcula delta heap e delta RSS, imprime relatório, exit 0 se PASS, exit 1 se FAIL

    Código completo a implementar:

    ```typescript
    #!/usr/bin/env node
    /**
     * JARVIS Soak Test — Phase 44 (VHARD-01, D-07 to D-10)
     *
     * Valida estabilidade de heap em Always-Listening mode ao longo de 8 horas.
     * Mede process.memoryUsage().heapUsed (JS heap) E rss (processo completo).
     *
     * Execução: node --expose-gc apps/desktop/scripts/soak-test.ts
     *
     * PASS criterion: delta heapUsed < 10MB em 8h (D-09)
     * WARNING: Soak test dura 8 horas. Não rodar em CI. Usar antes de release.
     *
     * Pitfall 2 (RESEARCH.md): heapUsed estável com RSS crescendo = native leak.
     * Por isso medimos AMBOS. RSS growth >50MB também é sinalizado como WARNING.
     */

    const BASELINE_DELAY_MS = 30_000;      // 30s warm-up antes do baseline (D-09)
    const SAMPLE_INTERVAL_MS = 30 * 60 * 1000; // 30 min entre amostras (D-09 Claude's Discretion)
    const SOAK_DURATION_MS = 8 * 60 * 60 * 1000; // 8 horas total (D-09)
    const HEAP_DELTA_FAIL_THRESHOLD_MB = 10;  // >10MB = FAIL (D-09)
    const RSS_DELTA_WARN_THRESHOLD_MB = 50;   // >50MB RSS delta = WARNING (Pitfall 2)

    interface MemSample {
      time: number;
      heapUsed: number;
      rss: number;
    }

    let baseline: MemSample | null = null;
    const samples: MemSample[] = [];
    let intervalId: ReturnType<typeof setInterval> | null = null;
    let soakTimeoutId: ReturnType<typeof setTimeout> | null = null;

    function mb(bytes: number): string {
      return (bytes / 1024 / 1024).toFixed(2);
    }

    function sample(): MemSample {
      // D-09: força GC antes de medir se disponível (--expose-gc flag)
      // Pitfall 5 (RESEARCH.md): GC não-determinístico sem essa flag
      if (typeof (global as unknown as { gc?: () => void }).gc === 'function') {
        (global as unknown as { gc: () => void }).gc();
      }
      const mem = process.memoryUsage();
      return { time: Date.now(), heapUsed: mem.heapUsed, rss: mem.rss };
    }

    function logSample(s: MemSample, label: string): void {
      const elapsed = baseline ? ((s.time - baseline.time) / 1000 / 60).toFixed(1) : '0.0';
      console.log(
        `[${new Date(s.time).toISOString()}] ${label} | heap: ${mb(s.heapUsed)} MB | rss: ${mb(s.rss)} MB | t+${elapsed}min`,
      );
    }

    function report(reason: 'timeout' | 'sigint'): void {
      console.log('\n========================================');
      console.log(`JARVIS Soak Test — ${reason === 'timeout' ? '8h complete' : 'interrupted'}`);
      console.log('========================================');

      if (!baseline || samples.length === 0) {
        console.log('No samples collected — test did not run long enough for baseline.');
        process.exit(1);
      }

      const last = samples[samples.length - 1]!;
      const heapDeltaBytes = last.heapUsed - baseline.heapUsed;
      const rssDeltaBytes = last.rss - baseline.rss;
      const heapDeltaMb = heapDeltaBytes / 1024 / 1024;
      const rssDeltaMb = rssDeltaBytes / 1024 / 1024;

      console.log(`Baseline (t+30s):  heap=${mb(baseline.heapUsed)} MB  rss=${mb(baseline.rss)} MB`);
      console.log(`Final    (t+${((last.time - baseline.time) / 1000 / 60 / 60).toFixed(1)}h): heap=${mb(last.heapUsed)} MB  rss=${mb(last.rss)} MB`);
      console.log(`Delta heap: ${heapDeltaMb >= 0 ? '+' : ''}${heapDeltaMb.toFixed(2)} MB`);
      console.log(`Delta rss:  ${rssDeltaMb >= 0 ? '+' : ''}${rssDeltaMb.toFixed(2)} MB`);
      console.log(`Samples collected: ${samples.length}`);
      console.log('');

      const heapPass = heapDeltaMb < HEAP_DELTA_FAIL_THRESHOLD_MB;
      const rssWarn = rssDeltaMb > RSS_DELTA_WARN_THRESHOLD_MB;

      if (rssWarn) {
        console.warn(`WARNING: RSS delta ${rssDeltaMb.toFixed(2)} MB > ${RSS_DELTA_WARN_THRESHOLD_MB} MB — possível native memory leak (Whisper.cpp, ONNX, áudio)`);
      }

      if (heapPass) {
        console.log(`PASS — heap delta ${heapDeltaMb.toFixed(2)} MB < ${HEAP_DELTA_FAIL_THRESHOLD_MB} MB threshold`);
        process.exit(0);
      } else {
        console.error(`FAIL — heap delta ${heapDeltaMb.toFixed(2)} MB >= ${HEAP_DELTA_FAIL_THRESHOLD_MB} MB threshold`);
        process.exit(1);
      }
    }

    function cleanup(reason: 'timeout' | 'sigint'): void {
      if (intervalId) clearInterval(intervalId);
      if (soakTimeoutId) clearTimeout(soakTimeoutId);
      report(reason);
    }

    // Graceful shutdown on Ctrl+C
    process.on('SIGINT', () => {
      console.log('\n[soak-test] Interrupted — generating partial report...');
      cleanup('sigint');
    });

    console.log('========================================');
    console.log('JARVIS Soak Test — Phase 44 (VHARD-01)');
    console.log('========================================');
    console.log(`Start time: ${new Date().toISOString()}`);
    console.log(`Baseline delay: ${BASELINE_DELAY_MS / 1000}s warm-up`);
    console.log(`Sample interval: ${SAMPLE_INTERVAL_MS / 1000 / 60} min`);
    console.log(`Total duration: ${SOAK_DURATION_MS / 1000 / 60 / 60}h`);
    console.log(`PASS threshold: heap delta < ${HEAP_DELTA_FAIL_THRESHOLD_MB} MB`);
    console.log(`GC available: ${typeof (global as unknown as { gc?: () => void }).gc === 'function' ? 'YES (--expose-gc)' : 'NO (use node --expose-gc for better accuracy)'}`);
    console.log('');
    console.log(`Waiting ${BASELINE_DELAY_MS / 1000}s for warm-up...`);

    setTimeout(() => {
      baseline = sample();
      logSample(baseline, 'BASELINE');
      console.log(`Sampling every ${SAMPLE_INTERVAL_MS / 1000 / 60} min for ${SOAK_DURATION_MS / 1000 / 60 / 60}h...`);

      intervalId = setInterval(() => {
        const s = sample();
        samples.push(s);
        logSample(s, `SAMPLE[${samples.length}]`);
      }, SAMPLE_INTERVAL_MS);

      soakTimeoutId = setTimeout(() => {
        cleanup('timeout');
      }, SOAK_DURATION_MS);
    }, BASELINE_DELAY_MS);
    ```

    IMPORTANTE: Este é um script Node.js puro executado diretamente (não via vitest, não via electron). Não requer nenhum import de electron ou do projeto. Usa apenas `process` e `global` do Node.js stdlib. TypeScript é compilado via `ts-node` ou `tsx` antes de executar.
  </action>

  <verify>
    <automated>cd /root/jarvis && npx tsx apps/desktop/scripts/soak-test.ts --help 2>&1 | head -5 || node -e "require('typescript'); console.log('ts ok')" 2>/dev/null; ls apps/desktop/scripts/soak-test.ts && echo "FILE_EXISTS"</automated>
  </verify>

  <acceptance_criteria>
    - apps/desktop/scripts/soak-test.ts existe (criado)
    - apps/desktop/scripts/soak-test.ts contém `process.memoryUsage`
    - apps/desktop/scripts/soak-test.ts contém `heapUsed`
    - apps/desktop/scripts/soak-test.ts contém `rss`
    - apps/desktop/scripts/soak-test.ts contém `BASELINE_DELAY_MS` com valor `30_000`
    - apps/desktop/scripts/soak-test.ts contém `SAMPLE_INTERVAL_MS` com valor `30 * 60 * 1000`
    - apps/desktop/scripts/soak-test.ts contém `SOAK_DURATION_MS` com valor `8 * 60 * 60 * 1000`
    - apps/desktop/scripts/soak-test.ts contém `HEAP_DELTA_FAIL_THRESHOLD_MB` com valor `10`
    - apps/desktop/scripts/soak-test.ts contém `PASS` e `FAIL` strings em console.log/error
    - apps/desktop/scripts/soak-test.ts contém `process.exit(0)` e `process.exit(1)`
    - apps/desktop/scripts/soak-test.ts contém comentário `--expose-gc`
    - `ls apps/desktop/scripts/soak-test.ts` retorna 0 (arquivo existe)
  </acceptance_criteria>
  <done>soak-test.ts criado com: warm-up de 30s, baseline capturado, amostragem a cada 30min por 8h, GC opcional via --expose-gc, delta heapUsed e RSS calculados, PASS/FAIL printado, process.exit(0/1) correto.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar soak test no README.md (D-10)</name>
  <files>
    README.md
  </files>

  <read_first>
    - README.md (ver estrutura atual, seções existentes, onde inserir seção de soak test)
    - .planning/phases/44-hardening-migration/44-CONTEXT.md (D-10: "documentado no README como run before release")
  </read_first>

  <action>
    Localizar uma seção adequada no README.md (ex.: após "Stack" ou criar seção "Release Checklist" / "Validação de Release"). Adicionar subsection de soak test:

    ```markdown
    ## Soak Test (Validação de Release)

    Antes de cada release, execute o soak test de 8 horas para validar que o modo Always-Listening não vaza memória:

    ```bash
    # Requer --expose-gc para GC determinístico (recomendado)
    node --expose-gc apps/desktop/scripts/soak-test.ts

    # Sem --expose-gc (GC natural, menos preciso)
    npx tsx apps/desktop/scripts/soak-test.ts
    ```

    **Critério de PASS:** delta de `heapUsed` < 10 MB em 8 horas (após warm-up de 30s).

    O script mede tanto `heapUsed` (JS heap) quanto `rss` (memória total do processo, inclui Whisper/ONNX). Se RSs crescer >50MB com heap estável, investigar vazamentos em módulos nativos.

    > **Nota:** O soak test dura 8 horas reais. Não é executado no CI — apenas manualmente antes de releases que alterem o modo Always-Listening.
    ```

    Inserir esta seção em local adequado no README.md existente. Se o README.md for muito básico/curto, adicionar ao final. Não remover conteúdo existente.
  </action>

  <verify>
    <automated>grep -c "soak-test\|soak test\|Soak Test" /root/jarvis/README.md && echo "soak test documented"</automated>
  </verify>

  <acceptance_criteria>
    - README.md contém `soak-test.ts`
    - README.md contém `--expose-gc`
    - README.md contém `heapUsed`
    - README.md contém `10 MB` ou `10MB` (threshold documentado)
    - README.md contém texto indicando execução manual antes de release (não CI)
    - `grep -c "soak" /root/jarvis/README.md` retorna >= 1
  </acceptance_criteria>
  <done>README.md contém seção de soak test com: comando de execução (node --expose-gc), critério de PASS (delta <10MB), explicação de rss vs heapUsed, nota de que não vai no CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| soak-test.ts → process | Script standalone — acesso total ao processo Node.js; sem entrada de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Information Disclosure | soak-test.ts stdout | accept | Script imprime métricas de heap/RSS para stdout — sem PII, sem dados de usuário. Risco inexistente para aplicação local. |
| T-44-07 | Denial of Service | soak-test.ts — 8h run | accept | Script projetado para rodar 8h por design (D-09). Handler SIGINT permite abortar. Não impacta app de produção. |
</threat_model>

<verification>
Verificações após plano 02:

1. `ls apps/desktop/scripts/soak-test.ts` — arquivo existe
2. `grep "heapUsed\|rss\|PASS\|FAIL" apps/desktop/scripts/soak-test.ts | wc -l` — >= 4 matches
3. `grep "soak-test" README.md | wc -l` — >= 1 match
4. Inspeção visual: constantes BASELINE_DELAY_MS=30s, SAMPLE_INTERVAL_MS=30min, SOAK_DURATION_MS=8h, HEAP_DELTA_FAIL_THRESHOLD_MB=10
5. `cd /root/jarvis/apps/desktop && npm test -- --run` — full suite verde (script não é test, não afeta suite)
</verification>

<success_criteria>
- apps/desktop/scripts/soak-test.ts existe com todos os parâmetros D-07/D-08/D-09 implementados
- Script mede heapUsed E rss (Pitfall 2 RESEARCH.md)
- Baseline em t=30s, amostras a cada 30min, duração 8h
- PASS (exit 0) se delta <10MB, FAIL (exit 1) se delta >=10MB
- GC opcional via global.gc() quando --expose-gc disponível
- SIGINT handler para relatório gracioso em caso de Ctrl+C
- README.md documenta comando e critério (D-10)
</success_criteria>

<output>
Após completar, criar `.planning/phases/44-hardening-migration/44-02-SUMMARY.md` com:
- Localização do soak-test.ts e parâmetros implementados
- Comando de execução documentado
- Métricas coletadas (heapUsed, rss)
- Critério PASS/FAIL
</output>
