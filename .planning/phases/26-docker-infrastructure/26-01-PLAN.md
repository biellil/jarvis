---
phase: 26-docker-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - apps/backend-ts/src/config.ts
  - apps/backend-ts/src/memory/vectors.ts
  - .env.example
autonomous: true
requirements:
  - DOCK-06
  - DOCK-07

must_haves:
  truths:
    - "ChromaDB roda como serviço dedicado no docker-compose com nome 'chromadb'"
    - "Backend-ts conecta ao ChromaDB via rede Docker interna usando a variável CHROMA_HOST"
    - "ChromaDB tem volume persistente — dados sobrevivem a docker compose down"
    - "Nenhuma referência a localhost:8000 hardcoded no código de produção"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Serviço chromadb com volume persistente"
      contains: "chromadb:"
    - path: "apps/backend-ts/src/config.ts"
      provides: "chromaHost e chromaPort lidos de env vars"
      exports: ["config.chromaHost", "config.chromaPort"]
    - path: "apps/backend-ts/src/memory/vectors.ts"
      provides: "MemoryVectors usa config.chromaHost ao invés de 'localhost'"
      contains: "config.chromaHost"
    - path: ".env.example"
      provides: "Documentação das vars CHROMA_HOST e CHROMA_PORT"
      contains: "CHROMA_HOST="
  key_links:
    - from: "docker-compose.yml"
      to: "chromadb service"
      via: "depends_on + healthcheck"
      pattern: "chromadb:"
    - from: "apps/backend-ts/src/memory/vectors.ts"
      to: "config.chromaHost"
      via: "import { config }"
      pattern: "config\\.chromaHost"
---

<objective>
Adicionar ChromaDB como serviço dedicado no docker-compose e corrigir o ChromaConnectionError que ocorre porque o cliente JS tenta conectar em `localhost` dentro do container — que não roteia para nada.

Purpose: DOCK-06 e DOCK-07 — ChromaDB persistente em Docker + backend-ts conectando via rede interna Docker.
Output: docker-compose.yml com serviço `chromadb`, volume `chroma-data`, e backend-ts usando `CHROMA_HOST=chromadb` em produção.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Tipos e contratos relevantes extraídos do codebase -->

De apps/backend-ts/src/memory/vectors.ts:
```typescript
export interface MemoryVectorsOptions {
  host?: string;   // default: 'localhost'  ← precisa mudar para ler de config
  port?: number;   // default: 8000
  ssl?: boolean;
}

export class MemoryVectors {
  constructor(options: MemoryVectorsOptions = {}) {
    this.host = options.host ?? 'localhost';  // BUG: hardcoded localhost
    this.port = options.port ?? 8000;
  }
}
```

De apps/backend-ts/src/config.ts (atual):
```typescript
export const config = {
  backendPort: parseInt(process.env.BACKEND_TS_PORT ?? "8001", 10),
  nodeEnv: process.env.NODE_ENV ?? "development",
} as const;
```

De apps/backend-ts/src/memory/manager.ts:
```typescript
export class MemoryManager {
  constructor(opts: MemoryManagerOptions = {}) {
    this.vectors = new MemoryVectors(opts.vectorsOptions ?? {});
    // opts.vectorsOptions não é passado em index.ts — usa defaults (localhost:8000)
  }
}
```

De apps/backend-ts/src/index.ts:
```typescript
const memory = new MemoryManager();  // sem vectorsOptions — usa host default
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar ChromaDB ao docker-compose com volume persistente</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (ler o estado atual completo antes de qualquer edição)
  </read_first>
  <action>
Editar docker-compose.yml para adicionar:

1. Novo serviço `chromadb`:
```yaml
  chromadb:
    image: chromadb/chroma:1.0.12
    container_name: jarvis-chromadb
    expose:
      - "8000"
    networks:
      - jarvis-net
    volumes:
      - chroma-data:/chroma/chroma
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/chroma/chroma
      - ANONYMIZED_TELEMETRY=FALSE
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v2/heartbeat"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
```

2. Adicionar dependência em `backend-ts`:
```yaml
    depends_on:
      chromadb:
        condition: service_healthy
```
(adicionar ao bloco `depends_on` existente do backend-ts, ou criar novo `depends_on` se não existir)

3. Adicionar variável de ambiente em `backend-ts`:
```yaml
    environment:
      - BACKEND_TS_PORT=8001
      - NODE_ENV=production
      - CHROMA_HOST=chromadb
      - CHROMA_PORT=8000
```

4. Adicionar volume `chroma-data` na seção `volumes`:
```yaml
volumes:
  whisper-models:
  chroma-data:
```

5. Adicionar dependência em `gateway` também para chromadb (via backend-ts já cobre indiretamente, mas manter depends_on para backend-ts que já existe).

O serviço gateway NÃO precisa conectar diretamente ao chromadb — apenas backend-ts.
  </action>
  <verify>
    <automated>grep -n "chromadb:" /c/jarvis/docker-compose.yml && grep -n "chroma-data:" /c/jarvis/docker-compose.yml && grep -n "CHROMA_HOST=chromadb" /c/jarvis/docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contém `chromadb:` como serviço (grep confirma)
    - docker-compose.yml contém `chroma-data:` na seção volumes
    - docker-compose.yml contém `CHROMA_HOST=chromadb` no env de backend-ts
    - docker-compose.yml contém `IS_PERSISTENT=TRUE` no env do serviço chromadb
    - docker-compose.yml contém healthcheck para chromadb com `/api/v2/heartbeat`
    - backend-ts tem `depends_on: chromadb: condition: service_healthy`
  </acceptance_criteria>
  <done>ChromaDB declarado como serviço com volume persistente; backend-ts depende dele via healthcheck.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expor CHROMA_HOST/PORT em config.ts e corrigir MemoryVectors + MemoryManager</name>
  <files>apps/backend-ts/src/config.ts, apps/backend-ts/src/memory/vectors.ts, apps/backend-ts/src/memory/manager.ts, apps/backend-ts/src/index.ts, .env.example</files>
  <read_first>
    - apps/backend-ts/src/config.ts (ler estado atual)
    - apps/backend-ts/src/memory/vectors.ts (ler integralmente — entender MemoryVectorsOptions e constructor)
    - apps/backend-ts/src/memory/manager.ts (ler integralmente — entender como MemoryVectors é instanciado)
    - apps/backend-ts/src/index.ts (verificar como MemoryManager é instanciado — sem vectorsOptions passados)
    - .env.example (ler integralmente para adicionar vars no lugar certo)
  </read_first>
  <behavior>
    - config.chromaHost retorna process.env.CHROMA_HOST ?? 'localhost'
    - config.chromaPort retorna parseInt(process.env.CHROMA_PORT ?? '8000', 10)
    - MemoryVectors construtor usa config.chromaHost como default ao invés de 'localhost' hardcoded
    - MemoryManager passa vectorsOptions com host e port de config quando não fornecido pelo caller
    - Em produção Docker (CHROMA_HOST=chromadb), client conecta em http://chromadb:8000
    - Em dev local (sem CHROMA_HOST), client conecta em http://localhost:8000 (comportamento preservado)
  </behavior>
  <action>
**1. Atualizar apps/backend-ts/src/config.ts:**

```typescript
export const config = {
  backendPort: parseInt(process.env.BACKEND_TS_PORT ?? "8001", 10),
  nodeEnv: process.env.NODE_ENV ?? "development",
  chromaHost: process.env.CHROMA_HOST ?? "localhost",
  chromaPort: parseInt(process.env.CHROMA_PORT ?? "8000", 10),
} as const;
```

**2. Atualizar apps/backend-ts/src/memory/vectors.ts:**

No constructor de MemoryVectors, o import de config não existe ainda. Adicionar no topo:
```typescript
import { config } from '../config.js';
```

Alterar o constructor para usar os defaults de config:
```typescript
constructor(options: MemoryVectorsOptions = {}) {
  this.host = options.host ?? config.chromaHost;
  this.port = options.port ?? config.chromaPort;
  this.ssl = options.ssl ?? false;
}
```

Não alterar mais nada em vectors.ts — apenas o constructor default.

**3. Verificar apps/backend-ts/src/memory/manager.ts:**

`new MemoryManager()` em index.ts não passa `vectorsOptions`. Manager chama `new MemoryVectors(opts.vectorsOptions ?? {})`. Com `{}` vazio, o constructor de MemoryVectors agora vai usar `config.chromaHost` como default. Nenhuma mudança necessária em manager.ts ou index.ts — o fix em vectors.ts já propaga.

**4. Atualizar .env.example — adicionar na seção Memory:**

Localizar a linha `CHROMA_PATH=data/chroma` e adicionar abaixo dela:
```
# Para Docker: CHROMA_HOST=chromadb (nome do serviço docker-compose)
# Para dev local: deixar em branco (default: localhost)
CHROMA_HOST=
CHROMA_PORT=8000
```
  </action>
  <verify>
    <automated>cd /c/jarvis/apps/backend-ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/backend-ts/src/config.ts contém `chromaHost: process.env.CHROMA_HOST ?? "localhost"`
    - apps/backend-ts/src/config.ts contém `chromaPort: parseInt(process.env.CHROMA_PORT ?? "8000", 10)`
    - apps/backend-ts/src/memory/vectors.ts contém `import { config } from '../config.js'`
    - apps/backend-ts/src/memory/vectors.ts NÃO contém `?? 'localhost'` hardcoded no constructor (foi substituído por `config.chromaHost`)
    - apps/backend-ts/src/memory/vectors.ts contém `options.host ?? config.chromaHost`
    - .env.example contém `CHROMA_HOST=`
    - .env.example contém `CHROMA_PORT=8000`
    - `npx tsc --noEmit` passa sem erros de tipo relacionados às mudanças
  </acceptance_criteria>
  <done>Backend-ts lê CHROMA_HOST/PORT de env vars; em Docker conecta em chromadb:8000; em dev usa localhost:8000.</done>
</task>

</tasks>

<verification>
Após completar ambas as tasks:
1. `grep -n "chromadb:" docker-compose.yml` — deve encontrar o serviço
2. `grep -n "CHROMA_HOST=chromadb" docker-compose.yml` — deve encontrar no env de backend-ts
3. `grep -n "chroma-data:" docker-compose.yml` — deve encontrar na seção volumes
4. `grep -n "config.chromaHost" apps/backend-ts/src/memory/vectors.ts` — deve encontrar no constructor
5. `grep -n "CHROMA_HOST=" .env.example` — deve encontrar a var documentada
6. `cd apps/backend-ts && npx tsc --noEmit` — zero erros de tipo
</verification>

<success_criteria>
- ChromaDB declarado em docker-compose.yml como serviço separado com volume `chroma-data` persistente (DOCK-06)
- Backend-ts usa CHROMA_HOST env var para conectar ao ChromaDB — zero hardcode de localhost (DOCK-07)
- `docker compose up` subirá chromadb ANTES de backend-ts (healthcheck gate) — ChromaConnectionError eliminado
</success_criteria>

<output>
Após completion, criar `.planning/phases/26-docker-infrastructure/26-01-SUMMARY.md`
</output>
