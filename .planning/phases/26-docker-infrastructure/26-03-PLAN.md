---
phase: 26-docker-infrastructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
gap_closure: true
requirements:
  - DOCK-08
  - DOCK-09
must_haves:
  truths:
    - "docker build do backend-ts baixa o modelo whisper base durante a build, não em runtime"
    - "docker compose up em máquina limpa não aciona download de modelo whisper em runtime"
  artifacts:
    - path: "docker-compose.yml"
      provides: "WHISPER_MODEL env var alinhada com modelo pre-baixado"
      contains: "WHISPER_MODEL=base"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile.backend-ts"
      via: "WHISPER_MODEL env var matching pre-downloaded model"
      pattern: "WHISPER_MODEL=base"
---

<objective>
Corrigir mismatch entre modelo whisper pre-baixado (base) no Dockerfile e variável WHISPER_MODEL no docker-compose.yml.

Purpose: Eliminar download em runtime do modelo "small" que não existe na imagem — o Dockerfile downloads ggml-base.bin mas docker-compose.yml pede WHISPER_MODEL=small, causando fallback para autoDownloadModelName em runtime e violando DOCK-08/DOCK-09.

Output: docker-compose.yml com WHISPER_MODEL=base alinhado ao modelo pre-baixado, satisfazendo DOCK-08 e DOCK-09 completamente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-docker-infrastructure/26-VERIFICATION.md
@.planning/phases/26-docker-infrastructure/26-02-SUMMARY.md
@docker-compose.yml
@Dockerfile.backend-ts
</context>

<gap_context>
## Verification Gap

**Truth failed:** "docker build do backend-ts baixa o modelo whisper base durante build, não em runtime"

**Status:** PARTIAL

**Reason:** Dockerfile.backend-ts contém curl para baixar ggml-base.bin (linha 61), mas docker-compose.yml tem WHISPER_MODEL=small (linha 57) enquanto o Dockerfile downloads base — mismatch causará fallback para download em runtime se o runtime resolver o modelo 'small' e não encontrar.

**Artifacts with issues:**
- docker-compose.yml linha 57: WHISPER_MODEL=small não corresponde ao modelo pre-baixado (base)

**Missing items:**
- Corrigir docker-compose.yml para WHISPER_MODEL=base para corresponder ao modelo pre-baixado

**Impact:**
- DOCK-08 partially satisfied (download happens, validation works, but wrong model)
- DOCK-09 blocked (docker compose up will trigger runtime download on first transcription)

**Root cause:** 26-02-SUMMARY.md linha 156 documenta commit dc1822c que supostamente corrigiu isto para base, mas o arquivo atual ainda mostra small — ou commit não persistiu ou SUMMARY documentou intenção e não realidade.
</gap_context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir WHISPER_MODEL para base no docker-compose.yml</name>

  <read_first>
    - docker-compose.yml (verificar estado atual da linha 57)
    - Dockerfile.backend-ts (confirmar qual modelo está sendo baixado nas linhas 60-67)
    - .planning/phases/26-docker-infrastructure/26-VERIFICATION.md (gap completo)
  </read_first>

  <files>docker-compose.yml</files>

  <action>
Alterar linha 57 do docker-compose.yml de:
```yaml
      - WHISPER_MODEL=small
```

Para:
```yaml
      - WHISPER_MODEL=base
```

**Contexto:** O Dockerfile.backend-ts linha 61 executa:
```dockerfile
curl -L -o node_modules/nodejs-whisper/cpp/whisper.cpp/models/ggml-base.bin \
    https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
```

Este comando baixa o modelo BASE (~142 MB), não small. Em runtime, LocalSTTProvider (apps/backend-ts/src/voice/stt/local.ts) lê a env var WHISPER_MODEL e tenta carregar esse modelo específico via nodejs-whisper. Se o modelo não for encontrado em node_modules/nodejs-whisper/cpp/whisper.cpp/models/, o nodejs-whisper aciona autoDownloadModelName como fallback, causando download em runtime.

**Verificação pré-commit:**
```bash
grep "WHISPER_MODEL=base" docker-compose.yml
```
Deve retornar linha 57 com valor "base" (não "small").

**Razão da falha original:** 26-02-SUMMARY.md documenta commit dc1822c que alegadamente fixou isto, mas verificação mostra que docker-compose.yml ainda contém "small" — possível que commit tenha sido revertido ou SUMMARY documentou intenção mas mudança não persistiu.
  </action>

  <verify>
    <automated>grep "WHISPER_MODEL=base" docker-compose.yml</automated>
  </verify>

  <acceptance_criteria>
    - docker-compose.yml linha 57 contém "WHISPER_MODEL=base" (não "small")
    - grep "WHISPER_MODEL=small" docker-compose.yml retorna exit code 1 (não encontrado)
    - Dockerfile.backend-ts linhas 60-67 ainda contém curl download de ggml-base.bin (não modificado)
    - Commit message referencia gap closure: "fix(26-03): alinha WHISPER_MODEL=base com modelo pre-baixado (gap closure DOCK-08/09)"
  </acceptance_criteria>

  <done>
docker-compose.yml linha 57 atualizada para WHISPER_MODEL=base, alinhada com ggml-base.bin baixado no Dockerfile, eliminando mismatch que causava runtime download.
  </done>
</task>

</tasks>

<verification>
**Automated checks:**
```bash
# Verify WHISPER_MODEL=base is set
grep "WHISPER_MODEL=base" docker-compose.yml

# Verify small is no longer present
! grep "WHISPER_MODEL=small" docker-compose.yml

# Verify Dockerfile still downloads base model
grep "ggml-base.bin" Dockerfile.backend-ts
```

**Gap closure validation:**
- Truth "docker build baixa modelo whisper base durante build, não em runtime" now FULLY verified (model downloaded matches model requested)
- Truth "docker compose up sem downloads em runtime" now achievable (no mismatch triggers autoDownload)
- DOCK-08 fully satisfied (correct model pre-downloaded and validated)
- DOCK-09 fully satisfied (docker compose up uses pre-downloaded model without network calls)
</verification>

<success_criteria>
**Phase goal restored:**
- docker compose up em máquina limpa sobe ambiente completo sem downloads em runtime ✓
- Modelo STT whisper base pré-baixado na imagem corresponde ao modelo requisitado em runtime ✓
- LocalSTTProvider carrega ggml-base.bin do volume sem acionar autoDownloadModelName ✓

**Gap closed:**
- docker-compose.yml WHISPER_MODEL env var alinhada com Dockerfile modelo pre-baixado
- Zero runtime downloads em primeira transcrição após docker compose up
- DOCK-08 e DOCK-09 requirements completamente satisfeitos

**Observable evidence:**
```bash
# Build and verify no download in runtime
docker compose build --no-cache
docker compose up -d
docker compose logs backend-ts | grep -i "download\|whisper"
# Expected: No "Downloading" messages, only "Loading model: base" or similar
```
</success_criteria>

<output>
After completion, create `.planning/phases/26-docker-infrastructure/26-03-SUMMARY.md`
</output>
