---
phase: 56-always-listening-soak-test
plan: 03
type: execute
wave: 3
depends_on:
  - 56-01
  - 56-02
files_modified: []
autonomous: false
requirements:
  - QA-01

must_haves:
  truths:
    - "1-minute quick soak test runs successfully and produces a PASS HTML report"
    - "GET /internal/diagnostics responds with all 4 metric fields while JARVIS is running in Always-Listening mode"
    - "HTML report file is generated and contains Chart.js charts with all 4 metrics"
  artifacts:
    - path: "soak-report-*.html"
      provides: "Generated report from 1-minute quick test"
      contains: "PASS"
  key_links:
    - from: apps/desktop/scripts/soak-test.ts
      to: "http://localhost:3000/internal/diagnostics"
      via: "HTTP GET fetch with AbortSignal.timeout(10_000)"
---

<objective>
Human-verified smoke test of the complete Phase 56 soak test infrastructure before the 8-hour run.

The user starts JARVIS in Always-Listening mode, then runs the 1-minute quick soak test to confirm:
1. `GET /internal/diagnostics` returns valid JSON with all 4 fields
2. The soak script polls, samples, and generates a PASS HTML report
3. The HTML report opens in browser and shows Chart.js charts

Purpose: Catch wiring issues (e.g., diagnostics server not starting, port conflicts, audioContextCount always 0) before committing 8 hours.
Output: Confirmed working pipeline; user ready to run full 8h soak.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-always-listening-soak-test/56-CONTEXT.md
@.planning/phases/56-always-listening-soak-test/56-01-SUMMARY.md
@.planning/phases/56-always-listening-soak-test/56-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Smoke-test the full diagnostics + soak pipeline (1-minute run)</name>
  <what-built>
    Plan 01: diagnostics instrumentation (GET /internal/diagnostics, perf_hooks event loop, AudioContext IPC bridge)
    Plan 02: refactored soak-test.ts with HTTP polling, QA-01 thresholds, HTML report, --duration flag
  </what-built>
  <how-to-verify>
    Step 1 — Start JARVIS in Always-Listening mode:
    - Run `pnpm dev` (or `pnpm start`) from the monorepo root
    - In the JARVIS tray menu, switch to Always-Listening mode
    - Wait ~10 seconds for the mode to stabilize

    Step 2 — Verify the diagnostics endpoint directly:
    - Open a terminal and run:
      `curl http://localhost:3000/internal/diagnostics`
    - Expected response (JSON with all 4 fields):
      `{"heapUsed":...,"rss":...,"eventLoopP99Ms":...,"audioContextCount":...,"timestamp":"..."}`
    - Confirm: all 4 fields present; eventLoopP99Ms is a positive number (not 0 or NaN); audioContextCount is 0 or 1

    Step 3 — Run the 1-minute quick soak test:
    - In a separate terminal (keep JARVIS running):
      `node --expose-gc apps/desktop/scripts/soak-test.ts --duration 60000`
    - Expected output: baseline log line → SAMPLE[1] log line (after ~10s) → "PASS" printed
    - Expected: `soak-report-*.html` file created in current directory

    Step 4 — Open the HTML report:
    - Open the generated `soak-report-*.html` in a browser
    - Confirm: 3 Chart.js charts are visible (not blank)
    - Confirm: "PASS" badge is green at the top of the report
    - Confirm: sample table shows at least 1 sample row beyond baseline
    - Confirm: all 4 metric cards show values (not NaN or 0 for everything)

    Step 5 — audioContextCount validation:
    - Confirm: audioContextCount shows 0 (no audio played yet) or 1 (audio has played)
    - If value is 2+: this is a FAIL — AudioContext singleton is leaking

    Step 6 (optional) — Start the 8-hour soak:
    - Once Step 3-5 pass, you can start the full 8h test:
      `node --expose-gc apps/desktop/scripts/soak-test.ts`
    - Leave JARVIS running in Always-Listening mode for 8 hours
    - Report will be generated at the end
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass.
    If issues found, describe what failed (e.g., "diagnostics endpoint 404", "HTML report blank charts", "audioContextCount = 2").
  </resume-signal>
</task>

</tasks>

<verification>
This plan's verification IS the checkpoint task above. Automated verification:
- `curl http://localhost:3000/internal/diagnostics` returns JSON with 4 fields (requires JARVIS running)
- `node --expose-gc apps/desktop/scripts/soak-test.ts --duration 60000` exits 0
- `soak-report-*.html` file exists after the run
</verification>

<success_criteria>
- 1-minute soak test exits with code 0 (PASS)
- HTML report generated with visible Chart.js charts
- audioContextCount = 0 or 1 (never > 1) throughout the quick test
- eventLoopP99Ms is a real value (> 0, < 50ms expected on a healthy system)
- User confirms pipeline ready for 8h full soak run
</success_criteria>

<output>
After completion, create `.planning/phases/56-always-listening-soak-test/56-03-SUMMARY.md`
</output>
