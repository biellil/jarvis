---
phase: 72-python-infrastructure-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - .env.example
autonomous: true
requirements:
  - PYSETUP-02
  - PYSETUP-03

must_haves:
  truths:
    - "User can run `pnpm dev:desktop-py` from repo root to start the Python client"
    - ".gitignore includes venv/ so local Python venvs are never committed"
    - ".env.example documents GATEWAY_URL so developers know the required variable"
  artifacts:
    - path: "package.json"
      provides: "Root workspace script dev:desktop-py"
      contains: "dev:desktop-py"
    - path: ".gitignore"
      provides: "venv/ exclusion for Python venv created by uv"
      contains: "venv/"
    - path: ".env.example"
      provides: "GATEWAY_URL documented for Python client"
      contains: "GATEWAY_URL"
  key_links:
    - from: "package.json dev:desktop-py"
      to: "apps/desktop-py/package.json dev script"
      via: "pnpm --filter @jarvis/desktop-py dev"
      pattern: "filter.*@jarvis/desktop-py"
---

<objective>
Wire the Python client into the monorepo: add the `dev:desktop-py` script to root package.json, add `venv/` to root .gitignore, and document `GATEWAY_URL` in .env.example.

Purpose: Without this plan, `pnpm dev:desktop-py` fails (unknown script) and `venv/` would be committed. This is pure plumbing — no Python code, only config file changes.
Output: Root package.json with dev:desktop-py script, .gitignore with venv/, .env.example with GATEWAY_URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/72-python-infrastructure-setup/72-CONTEXT.md

<interfaces>
<!-- Root package.json current scripts — add dev:desktop-py after dev:desktop -->
<!-- Current scripts in root package.json:
  "dev": "pnpm run --parallel --stream --filter \"@jarvis/*\" dev",
  "dev:backend": "pnpm --filter @jarvis/backend-ts dev",
  "dev:gateway": "pnpm --filter @jarvis/gateway dev",
  "dev:desktop": "pnpm --filter @jarvis/desktop dev",
  ...
-->
<!-- Add: "dev:desktop-py": "pnpm --filter @jarvis/desktop-py dev" -->

<!-- Current .gitignore Python section:
  # Python cache
  __pycache__/
  *.py[cod]
  *.egg-info/
  # Python virtualenv
  .venv/
-->
<!-- Need to add: venv/ (without the dot) — uv may create either .venv/ or venv/ -->

<!-- Current .env.example sections — add GATEWAY_URL under Gateway section:
  # ============================================
  # Gateway (Express)
  # ============================================
  GATEWAY_PORT=3000
  BACKEND_TS_URL=http://localhost:8001
  (add here) GATEWAY_URL=http://localhost:3000
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev:desktop-py script to root package.json</name>
  <files>package.json</files>

  <read_first>
    - C:\Users\biel1\OneDrive\Documentos\GitHub\jarvis\package.json — MUST read current content before editing to avoid overwriting existing scripts
    - C:\Users\biel1\OneDrive\Documentos\GitHub\jarvis\.planning\phases\72-python-infrastructure-setup\72-CONTEXT.md — decisions D-05, D-06 (exact script name and filter)
  </read_first>

  <action>
Edit root package.json. Add EXACTLY this script after the `"dev:desktop"` line (per D-05):

```json
"dev:desktop-py": "pnpm --filter @jarvis/desktop-py dev",
```

The resulting scripts object must include ALL existing scripts plus the new one:
```json
"scripts": {
  "dev": "pnpm run --parallel --stream --filter \"@jarvis/*\" dev",
  "dev:backend": "pnpm --filter @jarvis/backend-ts dev",
  "dev:gateway": "pnpm --filter @jarvis/gateway dev",
  "dev:desktop": "pnpm --filter @jarvis/desktop dev",
  "dev:desktop-py": "pnpm --filter @jarvis/desktop-py dev",
  "build": "pnpm run --recursive --filter \"@jarvis/*\" build",
  "dist": "node scripts/preflight-dist.mjs auto && pnpm -F @jarvis/desktop build:dist",
  "dist:win": "node scripts/preflight-dist.mjs win && pnpm -F @jarvis/desktop build:dist:win",
  "dist:mac": "node scripts/preflight-dist.mjs mac && pnpm -F @jarvis/desktop build:dist:mac",
  "dist:linux": "node scripts/preflight-dist.mjs linux && pnpm -F @jarvis/desktop build:dist:linux",
  "test": "pnpm run --recursive --filter \"@jarvis/*\" test --run",
  "typecheck": "pnpm run --recursive --filter \"@jarvis/*\" --if-present typecheck",
  "e2e": "pnpm --filter @jarvis/backend-ts e2e"
}
```

Do NOT change any other section of package.json (devDependencies, optionalDependencies, pnpm, engines).
  </action>

  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.scripts['dev:desktop-py'])"</automated>
  </verify>

  <acceptance_criteria>
    - package.json contains `"dev:desktop-py": "pnpm --filter @jarvis/desktop-py dev"`
    - All pre-existing scripts remain unchanged (dev, dev:backend, dev:gateway, dev:desktop, build, dist, dist:win, dist:mac, dist:linux, test, typecheck, e2e)
    - `node -e "const p=require('./package.json'); console.log(p.scripts['dev:desktop-py'])"` outputs `pnpm --filter @jarvis/desktop-py dev`
  </acceptance_criteria>

  <done>Root package.json has dev:desktop-py script. Once Plan 01 creates apps/desktop-py/package.json, the filter @jarvis/desktop-py will resolve and `pnpm dev:desktop-py` will work.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore with venv/ and add GATEWAY_URL to .env.example</name>
  <files>.gitignore, .env.example</files>

  <read_first>
    - C:\Users\biel1\OneDrive\Documentos\GitHub\jarvis\.gitignore — MUST read current content before editing to avoid duplicating existing entries
    - C:\Users\biel1\OneDrive\Documentos\GitHub\jarvis\.env.example — MUST read current content before editing to avoid breaking existing vars
    - C:\Users\biel1\OneDrive\Documentos\GitHub\jarvis\.planning\phases\72-python-infrastructure-setup\72-CONTEXT.md — decisions D-13, D-14
  </read_first>

  <action>
**Step 1 — Update .gitignore:**

The current .gitignore has `.venv/` under `# Python virtualenv` but is missing `venv/` (without dot). uv creates `.venv/` by default but may also use `venv/` depending on config. Add `venv/` immediately after `.venv/`.

Find this block:
```
# Python virtualenv
.venv/
```

Replace with:
```
# Python virtualenv
.venv/
venv/
```

Do NOT add `.env` — it is already at the top of the file.
Do NOT add `.pytest_cache/` — it is already present.
Do NOT add `__pycache__/` — it is already present.

**Step 2 — Update .env.example:**

Find the Gateway section:
```
# ============================================
# Gateway (Express)
# ============================================
GATEWAY_PORT=3000
BACKEND_TS_URL=http://localhost:8001
```

Add `GATEWAY_URL` immediately after `BACKEND_TS_URL=http://localhost:8001`:
```
# ============================================
# Gateway (Express)
# ============================================
GATEWAY_PORT=3000
BACKEND_TS_URL=http://localhost:8001
GATEWAY_URL=http://localhost:3000
```

The value `http://localhost:3000` matches GATEWAY_PORT=3000 in the same file.
Do NOT add `GATEWAY_URL` in any other section. Do NOT modify any other variable.
  </action>

  <verify>
    <automated>grep -n "^venv/$" /c/Users/biel1/OneDrive/Documentos/GitHub/jarvis/.gitignore && grep -n "^GATEWAY_URL=" /c/Users/biel1/OneDrive/Documentos/GitHub/jarvis/.env.example && echo "both checks OK"</automated>
  </verify>

  <acceptance_criteria>
    - .gitignore contains a line with exactly `venv/` (no leading dot, no trailing space)
    - .gitignore still contains `.venv/` (existing entry preserved)
    - .gitignore still contains `.env` (existing entry preserved — no secrets committed)
    - .env.example contains a line matching `GATEWAY_URL=http://localhost:3000`
    - .env.example still contains `GATEWAY_PORT=3000` (existing entry preserved)
    - .env.example still contains all pre-existing variables (LLM_PROVIDER, LM_STUDIO_URL, etc.)
    - `grep "^venv/$" .gitignore` exits 0
    - `grep "^GATEWAY_URL=" .env.example` exits 0
  </acceptance_criteria>

  <done>.gitignore covers both venv/ and .venv/ so Python virtual environments are never committed. .env.example documents GATEWAY_URL so developers know to set it when creating their .env.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `node -e "const p=require('./package.json'); console.log(p.scripts['dev:desktop-py'])"` — outputs `pnpm --filter @jarvis/desktop-py dev`
2. `grep "^venv/$" .gitignore` — exits 0 (match found)
3. `grep "^GATEWAY_URL=" .env.example` — exits 0 (match found)
4. `node -e "require('./package.json')"` — exits 0 (valid JSON, no syntax errors)
</verification>

<success_criteria>
- pnpm dev:desktop-py is wired in root workspace (resolves to @jarvis/desktop-py after Plan 01 creates the app)
- venv/ is gitignored alongside .venv/ — no Python venvs will be committed
- GATEWAY_URL is documented in .env.example for Python client developers
</success_criteria>

<output>
After completion, create `.planning/phases/72-python-infrastructure-setup/72-02-SUMMARY.md`
</output>
