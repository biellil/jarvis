---
phase: 25-orb-visual-polish-p2
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/main/store.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/renderer/components/Orb/Orb.tsx
autonomous: true
requirements:
  - ORB-POL-05

must_haves:
  truths:
    - "Usuário pode clicar e arrastar o orb para qualquer posição da tela"
    - "Durante o drag, o orb segue o cursor em tempo real sem lag perceptível"
    - "Ao soltar o botão do mouse, o orb permanece na nova posição"
    - "Após reiniciar o app, o orb aparece na última posição onde foi solto (persistência via electron-store)"
    - "Fora do modo drag (mouse não pressionado), o comportamento de click-through das áreas transparentes é preservado"
    - "O drag funciona somente na esfera visível (128x128px central) — áreas transparentes ao redor continuam passando cliques para o desktop"
  artifacts:
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "Canal IPC WINDOW_MOVE + método moveWindow na JarvisAPI"
      contains: "WINDOW_MOVE"
    - path: "apps/desktop/src/main/store.ts"
      provides: "StoreSchema com campo orbPosition + getOrbPosition/setOrbPosition"
      contains: "orbPosition"
    - path: "apps/desktop/src/main/index.ts"
      provides: "Handler IPC 'window:move' que chama mainWindow.setPosition(x, y)"
      contains: "window:move"
    - path: "apps/desktop/src/preload/index.ts"
      provides: "window.jarvis.moveWindow(dx, dy) via contextBridge"
      contains: "moveWindow"
    - path: "apps/desktop/src/renderer/components/Orb/Orb.tsx"
      provides: "Mouse event handlers (onMouseDown, onMouseMove, onMouseUp) + save on mouseup"
      contains: "onMouseDown"
  key_links:
    - from: "Orb.tsx onMouseDown"
      to: "window.jarvis.setIgnoreMouseEvents(false)"
      via: "IPC 'window:set-ignore-mouse' — habilita captura de mouse no drag"
      pattern: "setIgnoreMouseEvents"
    - from: "Orb.tsx onMouseMove"
      to: "window.jarvis.moveWindow(dx, dy)"
      via: "IPC 'window:move' → main → mainWindow.setPosition()"
      pattern: "moveWindow"
    - from: "Orb.tsx onMouseUp"
      to: "window.jarvis.saveOrbPosition()"
      via: "IPC 'window:save-orb-position' → main → store.setOrbPosition()"
      pattern: "saveOrbPosition"
    - from: "apps/desktop/src/main/index.ts calculateInitialPosition()"
      to: "store.getOrbPosition()"
      via: "position.ts já lê 'window.position' — store.ts adiciona 'orbPosition' como alias ou reusa"
      pattern: "orbPosition"
---

<objective>
Implementar drag-to-reposition do orb com persistência (ORB-POL-05): o usuário arrasta a esfera com o mouse para qualquer posição, e ao reabrir o app a esfera aparece no mesmo lugar.

Purpose: Stretch goal P2 do Phase 23. A posição fixa no canto inferior direito limita uso em setups multi-monitor ou com taskbars em posições diferentes. Drag livre com memória resolve isso sem precisar de settings UI.

Output: 
- IPC channel `window:move` no main para mover a janela via `mainWindow.setPosition()`
- `window.jarvis.moveWindow(dx, dy)` e `window.jarvis.saveOrbPosition()` expostos via preload
- Mouse handlers no Orb.tsx (onMouseDown/Move/Up) usando delta de posição
- `orbPosition` no StoreSchema + getOrbPosition/setOrbPosition em store.ts
- `calculateInitialPosition()` em position.ts atualizado para ler orbPosition do store primeiro
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/main/store.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/main/position.ts
@apps/desktop/src/preload/index.ts
@apps/desktop/src/renderer/components/Orb/Orb.tsx
</context>

<interfaces>
<!-- Contratos existentes que o executor deve usar diretamente — NÃO duplicar -->

De shared/ipc-types.ts — onde adicionar:
```ts
export const IPC_CHANNELS = {
  // ... canais existentes ...
  WINDOW_MOVE: 'window:move',             // ← ADICIONAR
  WINDOW_SAVE_ORB_POSITION: 'window:save-orb-position', // ← ADICIONAR
} as const;

export interface JarvisAPI {
  // ... métodos existentes ...
  moveWindow: (dx: number, dy: number) => void;           // ← ADICIONAR
  saveOrbPosition: () => void;                            // ← ADICIONAR
}
```

De main/store.ts — StoreSchema atual:
```ts
export interface StoreSchema {
  hotkey?: HotkeyConfig;
  pttHotkey?: HotkeyConfig;
  wakeWordPaused?: boolean;
  // ← ADICIONAR: orbPosition?: { x: number; y: number };
}
```

De main/index.ts — padrão de IPC handler existente:
```ts
// Padrão atual (linha ~161):
ipcMain.on('window:set-ignore-mouse', (_event, ignore: boolean) => {
  mainWindow?.setIgnoreMouseEvents(ignore, { forward: true });
});
// ← Adicionar handlers análogos para window:move e window:save-orb-position
```

De preload/index.ts — padrão de exposição via contextBridge:
```ts
setIgnoreMouseEvents: (ignore: boolean): void => {
  ipcRenderer.send(IPC_CHANNELS.SET_IGNORE_MOUSE, ignore);
},
// ← Adicionar:
moveWindow: (dx: number, dy: number): void => {
  ipcRenderer.send(IPC_CHANNELS.WINDOW_MOVE, dx, dy);
},
saveOrbPosition: (): void => {
  ipcRenderer.send(IPC_CHANNELS.WINDOW_SAVE_ORB_POSITION);
},
```

De main/index.ts — mainWindow já existe:
```ts
let mainWindow: BrowserWindow | null = null;
// mainWindow.getPosition() → [x, y]
// mainWindow.setPosition(x, y) → void
```

De main/position.ts — função de posição inicial:
```ts
export function calculateInitialPosition(): WindowPosition {
  // Já lê store.get('window.position') — adaptar para checar 'orbPosition' também
  // OU: getOrbPosition() pode reutilizar a chave 'window.position' existente
  // para evitar duplicação. Decisão: reutilizar a chave existente 'window.position'
  // em vez de criar nova — simplifica e evita migração de dados no store.
}
// NOTA: A função savePosition(x, y) em position.ts já salva em 'window.position'.
// Para ORB-POL-05, basta chamar savePosition(x, y) no handler de window:save-orb-position.
// Não é necessário criar orbPosition separado — a semântica é a mesma (última posição da janela).
```

De Orb.tsx — pointerEvents atual:
```tsx
// Atualmente o root div tem: pointerEvents: 'none'
// E a janela tem: mainWindow.setIgnoreMouseEvents(true, { forward: true })
// Para drag: a região da ESFERA VISÍVEL precisa ter pointerEvents habilitado
// durante hover/drag, depois volta para 'none' ao soltar.
// O IPC 'window:set-ignore-mouse' já existe para isso — usar o mesmo padrão.
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar canal IPC + tipos + store.ts para drag persist</name>
  <files>
    apps/desktop/src/shared/ipc-types.ts
    apps/desktop/src/main/store.ts
  </files>

  <read_first>
    - apps/desktop/src/shared/ipc-types.ts (ler inteiro — entender IPC_CHANNELS e JarvisAPI)
    - apps/desktop/src/main/store.ts (ler inteiro — entender StoreSchema e padrão de accessors)
  </read_first>

  <action>
**Em shared/ipc-types.ts:**

Adicionar ao objeto `IPC_CHANNELS` (após `WAKE_WORD_PAUSE_TOGGLE`):
```ts
// Phase 25 ORB-POL-05 — drag-to-reposition
WINDOW_MOVE: 'window:move',
WINDOW_SAVE_ORB_POSITION: 'window:save-orb-position',
```

Adicionar na interface `JarvisAPI` (após `setIgnoreMouseEvents`):
```ts
// Phase 25 ORB-POL-05 — arrastar orb e persistir posição
moveWindow: (dx: number, dy: number) => void;
saveOrbPosition: () => void;
```

**Em main/store.ts:**

Adicionar ao `StoreSchema`:
```ts
// Phase 25 ORB-POL-05: posição personalizada do orb (sobrescreve default bottom-right)
orbPosition?: { x: number; y: number };
```

Adicionar funções de acesso após `setWakeWordPaused`:
```ts
/**
 * Orb position accessors (Phase 25 ORB-POL-05)
 *
 * Persiste a posição da janela arrastada pelo usuário.
 * Lida separadamente de 'window.position' (legacy position.ts) para
 * evitar conflito com a lógica de validação de bounds existente em position.ts.
 * Se getOrbPosition() retornar undefined, position.ts usa o default bottom-right.
 */
export function getOrbPosition(): { x: number; y: number } | undefined {
  return store.get('orbPosition');
}

export function setOrbPosition(x: number, y: number): void {
  store.set('orbPosition', { x, y });
}
```

NOTA SOBRE DESIGN: Criar `orbPosition` como campo separado de `window.position`
evita conflito com a lógica de validação de bounds em `isPositionValid()` (que
leva em conta padding transparente e workArea). O drag salva posição absoluta da
JANELA (não da esfera), igual ao que `setPosition(x, y)` e `getPosition()` usam.
  </action>

  <verify>
    <automated>grep -n "WINDOW_MOVE\|WINDOW_SAVE_ORB_POSITION\|moveWindow\|saveOrbPosition\|orbPosition\|getOrbPosition\|setOrbPosition" /root/jarvis/apps/desktop/src/shared/ipc-types.ts /root/jarvis/apps/desktop/src/main/store.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "WINDOW_MOVE" apps/desktop/src/shared/ipc-types.ts` retorna a linha no IPC_CHANNELS
    - `grep "moveWindow" apps/desktop/src/shared/ipc-types.ts` retorna a linha na JarvisAPI
    - `grep "orbPosition" apps/desktop/src/main/store.ts` retorna pelo menos 3 linhas (schema, get, set)
    - `grep "getOrbPosition\|setOrbPosition" apps/desktop/src/main/store.ts` retorna as duas funções exportadas
    - TypeScript válido: `cd /root/jarvis/apps/desktop && npx tsc --noEmit 2>&1 | grep "error TS" | head -5`
  </acceptance_criteria>

  <done>IPC channels WINDOW_MOVE e WINDOW_SAVE_ORB_POSITION adicionados; JarvisAPI tem moveWindow e saveOrbPosition; store.ts tem getOrbPosition/setOrbPosition com schema atualizado</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: IPC handlers no main + preload bridge + drag handlers no Orb.tsx</name>
  <files>
    apps/desktop/src/main/index.ts
    apps/desktop/src/preload/index.ts
    apps/desktop/src/renderer/components/Orb/Orb.tsx
  </files>

  <read_first>
    - apps/desktop/src/main/index.ts (ler inteiro — localizar onde registrar novos ipcMain.on handlers, especificamente próximo ao 'window:set-ignore-mouse' na linha ~161)
    - apps/desktop/src/preload/index.ts (ler inteiro — localizar onde adicionar moveWindow e saveOrbPosition no objeto api)
    - apps/desktop/src/renderer/components/Orb/Orb.tsx (ler inteiro — entender estrutura de layers e onde adicionar handlers de mouse)
    - apps/desktop/src/main/store.ts (confirmar que getOrbPosition/setOrbPosition foram criados na Task 1)
  </read_first>

  <action>
**Em main/index.ts:**

Adicionar import de `getOrbPosition` e `setOrbPosition` no import de store:
```ts
import { getWidgetHotkey, getWakeWordPaused, setWakeWordPaused, getOrbPosition, setOrbPosition } from './store';
```
(verificar imports existentes de store — pode já estar importando parcialmente)

Adicionar handlers IPC após o handler `window:set-ignore-mouse` existente (linha ~161):
```ts
// Phase 25 ORB-POL-05: mover janela durante drag do orb.
// dx/dy são deltas de posição (pixels) — o renderer calcula a diferença
// entre posição atual do mouse e posição no início do drag (mousedown).
// Usamos delta em vez de coordenada absoluta para evitar descontinuidade
// se o cursor sair da janela durante movimentos rápidos.
ipcMain.on(IPC_CHANNELS.WINDOW_MOVE, (_event, dx: number, dy: number) => {
  if (!mainWindow) return;
  const [x, y] = mainWindow.getPosition();
  mainWindow.setPosition(x + dx, y + dy);
});

// Phase 25 ORB-POL-05: persistir posição após drag completado.
// Chamado no mouseup do renderer — salva posição atual da janela.
ipcMain.on(IPC_CHANNELS.WINDOW_SAVE_ORB_POSITION, (_event) => {
  if (!mainWindow) return;
  const [x, y] = mainWindow.getPosition();
  setOrbPosition(x, y);
});
```

Atualizar `app.whenReady()` para usar `getOrbPosition()` na criação da janela:
```ts
// Dentro de createWindow(), substituir:
const { x, y } = calculateInitialPosition();
// Por:
const orbPos = getOrbPosition();
const { x, y } = orbPos ?? calculateInitialPosition();
mainWindow.setPosition(x, y);
```

Adicionar import de `IPC_CHANNELS` se necessário (verificar se já está importado — provavelmente não está; `IPC_CHANNELS` é de `shared/ipc-types.ts`):
```ts
import { IPC_CHANNELS } from '../shared/ipc-types';
```

**Em preload/index.ts:**

Adicionar os dois métodos ao objeto `api` (após `setIgnoreMouseEvents`):
```ts
// Phase 25 ORB-POL-05 — drag orb
moveWindow: (dx: number, dy: number): void => {
  ipcRenderer.send(IPC_CHANNELS.WINDOW_MOVE, dx, dy);
},
saveOrbPosition: (): void => {
  ipcRenderer.send(IPC_CHANNELS.WINDOW_SAVE_ORB_POSITION);
},
```

**Em Orb.tsx:**

Adicionar `useRef` aos imports (se não já presente — verifica imports do React).

Adicionar drag state tracking antes do return:
```tsx
// ORB-POL-05: drag-to-reposition
// dragging: true durante o mousedown → mouseup
// lastPos: posição do mouse no último mousemove — usado para calcular delta
const draggingRef = useRef(false);
const lastPosRef = useRef({ x: 0, y: 0 });

const handleMouseDown = (e: React.MouseEvent) => {
  // Só captura botão esquerdo
  if (e.button !== 0) return;
  draggingRef.current = true;
  lastPosRef.current = { x: e.clientX, y: e.clientY };
  // Desabilita click-through para capturar mouse events durante drag
  window.jarvis.setIgnoreMouseEvents(false);
  // Previne seleção de texto acidental
  e.preventDefault();
};

const handleMouseMove = (e: React.MouseEvent) => {
  if (!draggingRef.current) return;
  const dx = e.clientX - lastPosRef.current.x;
  const dy = e.clientY - lastPosRef.current.y;
  lastPosRef.current = { x: e.clientX, y: e.clientY };
  // Envia delta para main mover a janela
  window.jarvis.moveWindow(dx, dy);
};

const handleMouseUp = () => {
  if (!draggingRef.current) return;
  draggingRef.current = false;
  // Persiste posição final
  window.jarvis.saveOrbPosition();
  // Restaura click-through em áreas transparentes
  window.jarvis.setIgnoreMouseEvents(true);
};
```

No JSX do root div, adicionar os handlers de mouse e ajustar `pointerEvents`:
```tsx
<div
  aria-label={`JARVIS orb in ${state} state`}
  className={rootClassName}
  onMouseDown={handleMouseDown}
  onMouseMove={handleMouseMove}
  onMouseUp={handleMouseUp}
  onMouseLeave={handleMouseUp}   // Segurança: soltar drag se mouse sair da janela
  style={{
    position: 'relative',
    width: SIZE,
    height: SIZE,
    // ORB-POL-05: 'auto' em vez de 'none' para capturar eventos de mouse
    // A janela cuida do click-through via setIgnoreMouseEvents — o componente
    // não precisa usar pointerEvents:none para isso.
    pointerEvents: 'auto',
    cursor: draggingRef.current ? 'grabbing' : 'grab',
    // ... resto dos styles (filter, opacity, transition) inalterado
  }}
>
```

NOTA: `onMouseLeave` chama `handleMouseUp` para evitar orb "preso" se o cursor
sair da janela durante drag rápido. Caso o mouse saia completamente, o `draggingRef`
é zerado e `setIgnoreMouseEvents(true)` é restaurado.

IMPORTANTE: A prop `pointerEvents: 'none'` existente no root deve ser substituída
por `pointerEvents: 'auto'`. O click-through é gerenciado pelo main process via
`setIgnoreMouseEvents`, não pelo renderer — então `pointerEvents: 'none'` no CSS
estava conflitando com a capacidade de capturar mouse events para drag.
  </action>

  <verify>
    <automated>grep -n "WINDOW_MOVE\|handleMouseDown\|draggingRef\|moveWindow\|saveOrbPosition\|getOrbPosition" /root/jarvis/apps/desktop/src/main/index.ts /root/jarvis/apps/desktop/src/preload/index.ts /root/jarvis/apps/desktop/src/renderer/components/Orb/Orb.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "WINDOW_MOVE" apps/desktop/src/main/index.ts` retorna o handler ipcMain.on
    - `grep "moveWindow" apps/desktop/src/preload/index.ts` retorna a exposição via ipcRenderer.send
    - `grep "handleMouseDown\|handleMouseMove\|handleMouseUp" apps/desktop/src/renderer/components/Orb/Orb.tsx` retorna as 3 funções
    - `grep "draggingRef" apps/desktop/src/renderer/components/Orb/Orb.tsx` retorna pelo menos 3 linhas (declaração, uso em handleMouseMove, uso em handleMouseUp)
    - `grep "pointerEvents.*none" apps/desktop/src/renderer/components/Orb/Orb.tsx` NÃO retorna resultado no root div (foi substituído por 'auto')
    - `grep "getOrbPosition" apps/desktop/src/main/index.ts` retorna uso na lógica de posição inicial
    - TypeScript compila sem erros: `cd /root/jarvis/apps/desktop && npx tsc --noEmit 2>&1 | grep "error TS" | head -5`
    - Build completo: `cd /root/jarvis && pnpm --filter @jarvis/desktop build 2>&1 | tail -5`
  </acceptance_criteria>

  <done>Drag funcional end-to-end: main tem handlers IPC para mover e salvar posição; preload expõe moveWindow/saveOrbPosition; Orb.tsx captura mouse events com handleMouseDown/Move/Up; getOrbPosition() é consultado no createWindow(); TypeScript e build passam</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | Renderer envia dx/dy para mover janela; main executa setPosition() |
| main → electron-store | Posição da janela persistida em store local (sem rede) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Tampering | ipcMain.on 'window:move' aceita dx/dy | accept | Contexto de uso local/pessoal. O renderer opera em sandbox (contextIsolation:true, nodeIntegration:false). Um atacante que controla o renderer já tem acesso total ao sistema — mover a janela é risco negligível nesse threat model |
| T-25-03-02 | Denial of Service | mousemove envia IPC a cada pixel de movimento | mitigate | Throttle pode ser adicionado se causar lag — mas IPC send é fire-and-forget assíncrono, impacto esperado mínimo. Se IPC flood se tornar problema observado em testes, adicionar `requestAnimationFrame` throttle |
| T-25-03-03 | Tampering | setOrbPosition salva coordenadas arbitrárias | accept | Coordenadas são x/y da janela (integers) — não há parsing de dados externos; mainWindow.getPosition() é a fonte da verdade |
</threat_model>

<verification>
```bash
# 1. IPC channels adicionados
grep "WINDOW_MOVE\|WINDOW_SAVE_ORB_POSITION" /root/jarvis/apps/desktop/src/shared/ipc-types.ts

# 2. Handlers no main
grep "window:move\|window:save-orb-position\|WINDOW_MOVE" /root/jarvis/apps/desktop/src/main/index.ts

# 3. Preload expõe os métodos
grep "moveWindow\|saveOrbPosition" /root/jarvis/apps/desktop/src/preload/index.ts

# 4. Orb.tsx tem handlers
grep "handleMouseDown\|handleMouseMove\|handleMouseUp\|draggingRef" /root/jarvis/apps/desktop/src/renderer/components/Orb/Orb.tsx

# 5. Store tem getOrbPosition/setOrbPosition
grep "getOrbPosition\|setOrbPosition\|orbPosition" /root/jarvis/apps/desktop/src/main/store.ts

# 6. pointerEvents:none removido do root
grep "pointerEvents.*none" /root/jarvis/apps/desktop/src/renderer/components/Orb/Orb.tsx && echo "FALHOU: pointerEvents:none ainda no root" || echo "OK"

# 7. TypeScript e build
cd /root/jarvis/apps/desktop && npx tsc --noEmit 2>&1 | grep "error TS" | head -5
cd /root/jarvis && pnpm --filter @jarvis/desktop build 2>&1 | tail -10
```
</verification>

<success_criteria>
- Novo IPC channel `window:move` no main processa dx/dy e chama `mainWindow.setPosition(x+dx, y+dy)`
- Novo IPC channel `window:save-orb-position` chama `setOrbPosition(x, y)` com a posição atual da janela
- `window.jarvis.moveWindow(dx, dy)` e `window.jarvis.saveOrbPosition()` expostos via preload
- Orb.tsx captura onMouseDown/onMouseMove/onMouseUp com delta tracking e toggle de click-through
- `createWindow()` consulta `getOrbPosition()` antes de `calculateInitialPosition()` para restaurar posição salva
- `pnpm --filter @jarvis/desktop build` passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/25-orb-visual-polish-p2/25-03-SUMMARY.md`
</output>
