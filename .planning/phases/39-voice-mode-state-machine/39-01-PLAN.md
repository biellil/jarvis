---
phase: 39-voice-mode-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/main/store.ts
autonomous: true
requirements:
  - VMODE-02
  - VMODE-03

must_haves:
  truths:
    - "VoiceMode type ('wake-word' | 'always-listening' | 'ptt-only') existe e é exportado de ipc-types.ts"
    - "VoiceModeChangeEvent type com campos {oldMode, newMode, reason, timestamp} existe em ipc-types.ts"
    - "store.ts StoreSchema contém campo voiceMode?: VoiceMode"
    - "getVoiceMode() retorna 'wake-word' quando campo ausente no electron-store (migração v1.8 silenciosa, D-07)"
    - "setVoiceMode() persiste no campo 'voiceMode' do store"
  artifacts:
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "VoiceMode union type, VoiceModeChangeEvent interface"
      contains: "export type VoiceMode"
    - path: "apps/desktop/src/main/store.ts"
      provides: "voiceMode persistence accessors"
      contains: "voiceMode?: VoiceMode"
  key_links:
    - from: "apps/desktop/src/main/store.ts"
      to: "apps/desktop/src/shared/ipc-types.ts"
      via: "import { VoiceMode } from '../../shared/ipc-types.js'"
      pattern: "import.*VoiceMode.*ipc-types"
---

<objective>
Criar os tipos compartilhados para Voice Mode e estender o electron-store com persistência do modo ativo. Este plano entrega a fundação de tipos que o Plan 02 (VoiceModeManager) consumirá.

Purpose: Definir contratos antes da implementação — Plan 02 importa esses tipos diretamente, sem precisar explorar o codebase.
Output: VoiceMode type + VoiceModeChangeEvent em ipc-types.ts; StoreSchema estendido + accessors getVoiceMode/setVoiceMode em store.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-voice-mode-state-machine/39-CONTEXT.md

@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/main/store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar VoiceMode types em ipc-types.ts</name>
  <files>apps/desktop/src/shared/ipc-types.ts</files>

  <read_first>
    - apps/desktop/src/shared/ipc-types.ts — estado atual completo; novos exports devem seguir o padrão de seções existente
  </read_first>

  <behavior>
    - VoiceMode type exportado: union literal 'wake-word' | 'always-listening' | 'ptt-only'
    - VoiceModeChangeEvent interface exportada com: oldMode: VoiceMode, newMode: VoiceMode, reason: 'user' | 'migration' | 'system', timestamp: number
    - IPC_CHANNELS estendido com VOICE_MODE_CHANGE: 'voiceMode:change'
  </behavior>

  <action>
Ler apps/desktop/src/shared/ipc-types.ts na íntegra. Adicionar uma nova seção após "// ============================================" dos tipos PTT Event Types (linha ~127). A seção deve seguir o mesmo padrão de comentário de seção existente.

Inserir antes da seção "// Channel Names":

```typescript
// ============================================
// Voice Mode Types — Phase 39 (VMODE-01, VMODE-03)
// ============================================

/**
 * VoiceMode — os 3 modos de captura mutuamente exclusivos.
 * Persiste via electron-store (campo 'voiceMode').
 * Default implícito: 'wake-word' (D-07: migração v1.8 silenciosa).
 */
export type VoiceMode = 'wake-word' | 'always-listening' | 'ptt-only';

/**
 * VoiceModeChangeEvent — payload do EventEmitter 'voiceMode:change'.
 * D-05: payload rich para suportar audit log futuro e UX condicional.
 * D-08: reason='migration' NUNCA emitido no startup — só em trocas reais.
 */
export interface VoiceModeChangeEvent {
  oldMode: VoiceMode;
  newMode: VoiceMode;
  /** 'user' = troca manual via tray/API; 'system' = mudança programática interna */
  reason: 'user' | 'system';
  /** Unix timestamp ms — Date.now() */
  timestamp: number;
}
```

No objeto IPC_CHANNELS (const), adicionar após a linha `SETTINGS_CLOSE`:
```typescript
  // Phase 39 — voice mode change broadcast (main → renderer)
  VOICE_MODE_CHANGE: 'voiceMode:change',
```

Não criar arquivos novos — todos os tipos vivem em ipc-types.ts (D-06).
Não remover ou modificar nenhum tipo existente.
  </action>

  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "export type VoiceMode" apps/desktop/src/shared/ipc-types.ts retorna match
    - grep "export interface VoiceModeChangeEvent" apps/desktop/src/shared/ipc-types.ts retorna match
    - grep "oldMode: VoiceMode" apps/desktop/src/shared/ipc-types.ts retorna match
    - grep "reason: 'user' | 'system'" apps/desktop/src/shared/ipc-types.ts retorna match
    - grep "VOICE_MODE_CHANGE" apps/desktop/src/shared/ipc-types.ts retorna match
    - npx tsc --noEmit exits 0 (sem erros de tipo)
  </acceptance_criteria>

  <done>VoiceMode e VoiceModeChangeEvent exportados de ipc-types.ts; IPC_CHANNELS contém VOICE_MODE_CHANGE; zero erros de TypeScript.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender store.ts com voiceMode accessor (D-07)</name>
  <files>apps/desktop/src/main/store.ts, apps/desktop/src/main/__tests__/store.test.ts</files>

  <read_first>
    - apps/desktop/src/main/store.ts — estado atual completo; seguir padrão de accessors existentes
    - apps/desktop/src/main/__tests__/store.test.ts — padrão de mock electron-store com __resetStore; novos testes devem seguir o mesmo describe/it pattern
    - apps/desktop/src/shared/ipc-types.ts — após Task 1, contém VoiceMode type a ser importado
  </read_first>

  <behavior>
    - Test 1: getVoiceMode() retorna 'wake-word' quando campo 'voiceMode' ausente no store
    - Test 2: setVoiceMode('always-listening') → getVoiceMode() retorna 'always-listening'
    - Test 3: setVoiceMode persiste como valor direto na chave 'voiceMode' (não aninhado em objeto)
    - Test 4: getVoiceMode() com valor inválido no store (ex: 'invalid-mode') retorna 'wake-word' (T-01 mitigação)
  </behavior>

  <action>
Ler store.ts na íntegra. Fazer as seguintes modificações:

1. **Adicionar import** no topo do arquivo, após `import Store from 'electron-store'`:
```typescript
import type { VoiceMode } from '../../shared/ipc-types.js';
```

2. **Estender StoreSchema** adicionando o campo após whisperModelOverride:
```typescript
  // Phase 39 — Voice Mode State Machine (VMODE-02)
  voiceMode?: VoiceMode;
```

3. **Adicionar constante default** após DEFAULT_WAKE_WORD_PAUSED:
```typescript
const DEFAULT_VOICE_MODE: VoiceMode = 'wake-word'; // D-07: default para migração v1.8 silenciosa
```

4. **Adicionar accessors** no final do arquivo, antes de `export default store`:
```typescript
/**
 * Voice Mode accessors (Phase 39 — VMODE-02)
 *
 * D-07: Migração v1.8 → v1.9 via default implícito no read.
 * store.get('voiceMode') undefined → retorna 'wake-word'. Próxima escrita persiste.
 * T-01: Valor inválido no store JSON é tratado como undefined → retorna default.
 */
export function getVoiceMode(): VoiceMode {
  const value = store.get('voiceMode');
  const validModes: VoiceMode[] = ['wake-word', 'always-listening', 'ptt-only'];
  if (value !== undefined && validModes.includes(value as VoiceMode)) {
    return value as VoiceMode;
  }
  return DEFAULT_VOICE_MODE;
}

export function setVoiceMode(mode: VoiceMode): void {
  store.set('voiceMode', mode);
}
```

**Testes** — adicionar novo describe block ao final de apps/desktop/src/main/__tests__/store.test.ts:
```typescript
// ============================================================
// Phase 39 Voice Mode — store.ts voiceMode accessors
// ============================================================

import { getVoiceMode, setVoiceMode } from '../store';

describe('store.ts — Voice Mode accessors (Phase 39)', () => {
  beforeEach(() => {
    (Store as any).__resetStore();
  });

  it("getVoiceMode() returns 'wake-word' when not set (D-07 migration default)", () => {
    expect(getVoiceMode()).toBe('wake-word');
  });

  it("setVoiceMode('always-listening') → getVoiceMode() returns 'always-listening'", () => {
    setVoiceMode('always-listening');
    expect(getVoiceMode()).toBe('always-listening');
  });

  it("setVoiceMode('ptt-only') → getVoiceMode() returns 'ptt-only'", () => {
    setVoiceMode('ptt-only');
    expect(getVoiceMode()).toBe('ptt-only');
  });

  it("setVoiceMode writes value directly to store key 'voiceMode'", () => {
    setVoiceMode('always-listening');
    const backing = (Store as any).__getBackingStore();
    expect(backing).toHaveProperty('voiceMode', 'always-listening');
  });

  it("getVoiceMode() returns 'wake-word' when store contains invalid value (T-01 mitigation)", () => {
    // Simula edição manual do JSON do electron-store
    const backing = (Store as any).__getBackingStore();
    backing['voiceMode'] = 'invalid-mode';
    expect(getVoiceMode()).toBe('wake-word');
  });
});
```

Atenção: o import de getVoiceMode/setVoiceMode deve ser adicionado ao import existente de '../store' no topo do arquivo de testes, não criar um import duplicado.
  </action>

  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run src/main/__tests__/store.test.ts 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep "voiceMode\?: VoiceMode" apps/desktop/src/main/store.ts retorna match
    - grep "export function getVoiceMode" apps/desktop/src/main/store.ts retorna match
    - grep "export function setVoiceMode" apps/desktop/src/main/store.ts retorna match
    - grep "import type { VoiceMode }" apps/desktop/src/main/store.ts retorna match
    - npx vitest run src/main/__tests__/store.test.ts — todos os describes passam (incluindo o novo "Phase 39 Voice Mode")
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>StoreSchema tem voiceMode? campo; getVoiceMode/setVoiceMode exportados com default 'wake-word' e validação de enum; 5 novos testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron-store JSON file → main process | Arquivo editável pelo usuário fora do app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | store.ts getVoiceMode() | mitigate | Validação de enum no read: valores fora de `['wake-word', 'always-listening', 'ptt-only']` são tratados como undefined e retornam default 'wake-word'. Implementado em getVoiceMode() via `validModes.includes()`. |
| T-39-02 | Information Disclosure | ipc-types.ts VoiceModeChangeEvent | accept | Payload não contém PII — apenas enum de modo, razão da troca e timestamp. Nenhum dado sensível exposto. |
</threat_model>

<verification>
Após execução do plan:
1. `grep "export type VoiceMode" apps/desktop/src/shared/ipc-types.ts` — match
2. `grep "export interface VoiceModeChangeEvent" apps/desktop/src/shared/ipc-types.ts` — match
3. `grep "voiceMode\?: VoiceMode" apps/desktop/src/main/store.ts` — match
4. `cd apps/desktop && npx vitest run src/main/__tests__/store.test.ts` — todos os testes passam
5. `cd apps/desktop && npx tsc --noEmit` — 0 erros
</verification>

<success_criteria>
- VoiceMode type ('wake-word' | 'always-listening' | 'ptt-only') exportado de ipc-types.ts
- VoiceModeChangeEvent interface exportada com campos {oldMode, newMode, reason, timestamp}
- IPC_CHANNELS.VOICE_MODE_CHANGE = 'voiceMode:change' registrado
- StoreSchema contém voiceMode?: VoiceMode
- getVoiceMode() retorna 'wake-word' quando campo ausente ou inválido (D-07 + T-01 mitigação)
- setVoiceMode() persiste no campo 'voiceMode'
- 5 testes novos passando em store.test.ts
- Zero erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/39-voice-mode-state-machine/39-01-SUMMARY.md` com:
- Arquivos modificados e o que foi adicionado em cada um
- Tipos exportados (assinaturas completas)
- Decisões implementadas (D-05, D-06, D-07, T-01)
- Resultado dos testes
</output>
