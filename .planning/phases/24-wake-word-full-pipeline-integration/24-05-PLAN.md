---
phase: 24-wake-word-full-pipeline-integration
plan: 05
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
  - 24-03
  - 24-04
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md
autonomous: false
requirements:
  - WAKE-05
  - WAKE-06
  - WAKE-10
  - WAKE-11
  - WAKE-12
  - WAKE-13
tags:
  - requirements
  - uat
  - documentation
  - milestone-closeout

must_haves:
  truths:
    - "REQUIREMENTS.md has WAKE-10, WAKE-11, WAKE-12, WAKE-13 defined under v1.4 Wake Word section"
    - "Traceability table in REQUIREMENTS.md maps each new REQ to Phase 24 + plan IDs"
    - "24-UAT.md exists with a human-runnable end-to-end checklist matching Success Criteria in ROADMAP Phase 24"
    - "24-UAT.md includes the 3 manual behaviors from 24-VALIDATION.md Manual-Only table (E2E mic, TTS degrade, backend down)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "WAKE-10..WAKE-13 requirement definitions + traceability map"
      contains: "WAKE-10"
    - path: .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md
      provides: "Human UAT checklist for Phase 24 E2E sign-off"
      contains: "Hey JARVIS"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/phases/24-wake-word-full-pipeline-integration/
      via: "Traceability table — Phase 24 Plan column"
---

<objective>
Fechar formalmente a Phase 24 antes do gate de execução final. Duas entregas documentais + um checkpoint humano:

1. **REQUIREMENTS.md update** — Adicionar WAKE-10, WAKE-11, WAKE-12, WAKE-13 (propostos pela research §"Phase Requirements") à seção v1.4 Wake Word. Atualizar a tabela Traceability com mapping para Phase 24 + plan IDs.

2. **24-UAT.md** — Checklist humano end-to-end cobrindo os Success Criteria da ROADMAP Phase 24 + as 3 entradas Manual-Only da VALIDATION.md (E2E com mic real, TTS degrade, backend down).

3. **Human verification checkpoint** — Usuário executa o UAT com microfone real, assina cada item ou reporta gap. Esta plan é `autonomous: false` porque termina num checkpoint obrigatório — não há commits de código aqui, apenas docs + sign-off.

Purpose: A Phase 24 existe para fechar o loop wake word → STT → LLM → TTS → idle. Sem sign-off humano, `/gsd-verify-work` não pode declarar a phase completa (é integração com hardware real — unit tests provam contratos, não provam que o mic captura voz real em pt-BR).

Output:
- `.planning/REQUIREMENTS.md` — 4 novos requirements + traceability rows
- `.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` — checklist
- Human sign-off messages in checkpoint resume
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/24-wake-word-full-pipeline-integration/24-CONTEXT.md
@.planning/phases/24-wake-word-full-pipeline-integration/24-RESEARCH.md
@.planning/phases/24-wake-word-full-pipeline-integration/24-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update REQUIREMENTS.md with WAKE-10..WAKE-13 + traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — see exact format of existing WAKE-01..WAKE-09 entries and the traceability table)
    - .planning/phases/24-wake-word-full-pipeline-integration/24-RESEARCH.md lines 68-80 (proposed requirements list WAKE-10..WAKE-13)
    - .planning/phases/24-wake-word-full-pipeline-integration/24-CONTEXT.md §decisions D-04, D-06, D-07, D-08 (the decisions these REQs formalize)
  </read_first>
  <action>
    **Step 1 — Add WAKE-10..WAKE-13 to the v1.4 Wake Word P1 section:**

    Open `.planning/REQUIREMENTS.md`. Find the block `### Wake Word (WAKE)` → `**P1 — must ship:**`. After the `WAKE-09` entry, insert:

    ```
    - [ ] **WAKE-10** — Se o TTS falhar ao tocar a resposta (audioBase64 vazio, player crash, codec error), o texto da resposta do LLM permanece visível no chat e o orb retorna ao idle normalmente (D-06 — degrade gracioso via `handleAudioResponse.addAgentMessage` antes de `playTTS`)
    - [ ] **WAKE-11** — Hard errors (backend down, LLM timeout, mic muted mid-recording, silent stream 6s, VAD lib crash) disparam toast pt-BR visível via `ChatContext.setToast` e o orb retorna ao idle sem travar (D-08)
    - [ ] **WAKE-12** — Usuário pode escolher `TTS_PROVIDER=murf` no `.env` e ouvir respostas em voz masculina pt-BR via Murf.ai, com fallback para LocalTTSProvider se `MURF_API_KEY` ausente (D-04 + D-05)
    - [ ] **WAKE-13** — Fluxo PTT (`ChatInput.tsx`) e fluxo wake word (`useWakeWord.ts`) consomem a mesma função compartilhada `sendAudioAndHandle` — sem duplicação de lógica de state/toast/handleAudioResponse (D-07)
    ```

    **Step 2 — Update the Traceability table:**

    Find the `## Traceability (v1.4)` section. Update the `Phase 22 total` / `Phase 23 total` summary line to add Phase 24. Then update the table rows — change `WAKE-05` and `WAKE-06` from `Phase 22` to `Phase 24` (per the research finding that these requirements are actually unsatisfied until Phase 24 wires the loop). Mark them as `TBD → Phase 24`.

    Add new rows for WAKE-10..WAKE-13:
    ```
    | WAKE-05 | P1 | Phase 24 | 24-02, 24-04 |
    | WAKE-06 | P1 | Phase 24 | 24-04 |
    | WAKE-10 | P1 | Phase 24 | 24-02 |
    | WAKE-11 | P1 | Phase 24 | 24-02, 24-04 |
    | WAKE-12 | P1 | Phase 24 | 24-01 |
    | WAKE-13 | P1 | Phase 24 | 24-02, 24-03, 24-04 |
    ```

    Update the coverage header: `Coverage: **11/11 P1 requirements mapped** + 3 P2 stretch bundled in Phase 23.` → `Coverage: **15/15 P1 requirements mapped** (4 new from Phase 24) + 3 P2 stretch bundled in Phase 23.`

    Update the totals footer:
    ```
    **Phase 22 total:** 4 P1 (WAKE-01, 07, 08, 09) — wake word core + VoiceInputManager refactor prerequisito
    **Phase 23 total:** 5 P1 (WAKE-02, 03, 04, ORB-POL-01, 02) + 3 P2 stretch (ORB-POL-03, 04, 05) — visual polish + UX
    **Phase 24 total:** 6 P1 (WAKE-05, 06, 10, 11, 12, 13) — integração full pipeline + Murf + VAD real + shared helper
    ```

    Note: WAKE-05 and WAKE-06 were originally mapped to Phase 22 but Phase 22 SUMMARY shows they were effectively bypassed (vadTimeoutMs fixed, no return-trip to backend). This correction is honest — moves them to where they are actually satisfied.

    **Step 3 — Do NOT touch:** The ORB-POL entries, validated requirements from v1.0-v1.3, or the Out of Scope list.
  </action>
  <acceptance_criteria>
    - `grep -q 'WAKE-10' .planning/REQUIREMENTS.md` → matches
    - `grep -q 'WAKE-11' .planning/REQUIREMENTS.md` → matches
    - `grep -q 'WAKE-12' .planning/REQUIREMENTS.md` → matches
    - `grep -q 'WAKE-13' .planning/REQUIREMENTS.md` → matches
    - `grep -q 'Murf' .planning/REQUIREMENTS.md` → matches (WAKE-12 description)
    - `grep -q 'sendAudioAndHandle' .planning/REQUIREMENTS.md` → matches (WAKE-13 description)
    - `grep -q '| WAKE-10 | P1 | Phase 24' .planning/REQUIREMENTS.md` → matches
    - `grep -q '| WAKE-05 | P1 | Phase 24' .planning/REQUIREMENTS.md` → matches (reassigned)
    - `grep -q '15/15 P1 requirements mapped' .planning/REQUIREMENTS.md` → matches
    - `grep -q 'Phase 24 total' .planning/REQUIREMENTS.md` → matches
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'WAKE-1[0-3]' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    REQUIREMENTS.md has 4 new Phase 24 requirements defined + traceability table accurately reflects which phase satisfies each WAKE-* requirement. Coverage counter bumped from 11/11 to 15/15.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create 24-UAT.md human verification checklist</name>
  <files>.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 24" Success Criteria (the 5 items — orient the UAT around them)
    - .planning/phases/24-wake-word-full-pipeline-integration/24-VALIDATION.md §Manual-Only Verifications (the 3 documented manual tests)
    - .planning/phases/24-wake-word-full-pipeline-integration/24-CONTEXT.md §D-08 (the 5 error scenarios in the toast table)
  </read_first>
  <action>
    Create `.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` with this structure:

    ```markdown
    ---
    phase: 24
    slug: wake-word-full-pipeline-integration
    type: uat
    created: 2026-04-11
    status: pending
    approver: user
    ---

    # Phase 24 — User Acceptance Test

    **Goal:** Provar end-to-end que o loop wake word → STT → LLM → TTS → idle fecha corretamente em ambiente real, com microfone físico, voz humana em pt-BR, e Murf.ai como TTS provider.

    **Pré-requisitos:**

    - [ ] Backend-ts rodando com `TTS_PROVIDER=murf` e `MURF_API_KEY` válido no `.env`
    - [ ] Gateway (Express) rodando em :3000
    - [ ] LM Studio rodando em localhost:1234 com modelo carregado (qualquer modelo pt-BR capaz)
    - [ ] Desktop app rodando via `pnpm --filter @jarvis/desktop dev`
    - [ ] Microfone físico conectado e testado via OS settings
    - [ ] `wakeWordPaused === false` no store (tray menu "Resume listening" se necessário)
    - [ ] Orb visível em estado `idle` com wake word ATIVO (glow normal, não dimmed)

    ---

    ## Success Criteria (do ROADMAP §Phase 24)

    ### SC-1: Happy path — wake word → pergunta → resposta TTS

    **Steps:**
    1. Com orb em idle, dizer em voz normal: **"Hey JARVIS, que horas são?"**
    2. Observar orb: wake burst (amber ring, ~350ms)
    3. Observar orb transicionar para `listening` (laranja pulsante)
    4. Esperar o VAD Silero detectar fim da fala (~500ms após terminar a pergunta)
    5. Observar orb transicionar para `processing`
    6. Observar orb transicionar para `responding`
    7. Ouvir resposta em pt-BR via voz Heitor (Murf)
    8. Observar orb retornar a `idle` após o áudio terminar

    **Acceptance:**
    - [ ] Wake burst visível
    - [ ] Listening → processing transition automático (sem apertar nada)
    - [ ] Resposta audível em pt-BR com voz masculina Murf
    - [ ] Orb retorna a idle sem intervenção
    - [ ] Latência total ≤ 5s percebidos entre fim da pergunta e início da resposta audível
    - [ ] Texto da resposta aparece no chat/SpeechBubble (pode ser antes ou durante o TTS)

    ### SC-2: Loop automático retoma sem ação — WAKE-05

    **Steps:**
    1. Após SC-1 terminar e orb estar em `idle`, aguardar 2s
    2. Sem tocar em nada, dizer novamente: **"Hey JARVIS, obrigado"**
    3. Observar fluxo idêntico ao SC-1

    **Acceptance:**
    - [ ] Wake word dispara na segunda invocação sem configuração adicional
    - [ ] Ciclo completo funciona idêntico ao SC-1

    ### SC-3: VAD real substitui timeout fixo — WAKE-06

    **Steps:**
    1. Com orb em idle, dizer **"Hey JARVIS"** e então parar de falar imediatamente (diga só o wake word sem pergunta)
    2. Observar orb ir para `listening`
    3. Após silêncio de ~500ms, VAD deve detectar fim da fala OU disparar fallback de 6s

    **Acceptance:**
    - [ ] Se VAD detectou (provável): orb vai para processing + recebe alguma resposta do LLM (pode ser "Como posso ajudar?" ou similar)
    - [ ] Se fallback 6s disparou: toast amarelo "Não ouvi nada. Diga Hey JARVIS de novo." aparece e orb volta a idle
    - [ ] Em nenhum cenário o orb fica travado em `listening`

    ### SC-4: Error recovery — toast pt-BR + orb idle

    **Cenário A — Backend down (D-08):**
    1. Matar o processo backend-ts (Ctrl+C no terminal do backend)
    2. Dizer **"Hey JARVIS, teste"**
    3. Observar:
       - [ ] Orb pisca red (ou similar) ao receber erro
       - [ ] Toast pt-BR aparece: "JARVIS offline. Verifique o backend." (ou mensagem similar mapeada por `mapErrorCode`)
       - [ ] Orb retorna a `idle`
    4. Reiniciar o backend para continuar o UAT

    **Cenário B — TTS failure degrade (D-06):**
    1. Temporariamente setar `MURF_API_KEY=invalid_key_for_uat` no `.env` e reiniciar o backend
       (isso força Murf a retornar 401 → fallback para LocalTTSProvider → texto ainda funciona mas TTS pode ser ruim ou falhar)
    2. Dizer **"Hey JARVIS, como você está?"**
    3. Observar:
       - [ ] Texto da resposta aparece no chat (addAgentMessage) — texto NÃO é perdido
       - [ ] Audio pode ou não tocar — aceitável qualquer dos dois
       - [ ] Orb retorna a idle
    4. Restaurar `MURF_API_KEY` válido

    ### SC-5: Shared pipeline parity — WAKE-13

    **Steps:**
    1. Com orb em idle, apertar PTT (`Ctrl+Space`)
    2. Dizer **"Teste de PTT"**
    3. Apertar PTT novamente para parar
    4. Observar fluxo: listening → processing → responding → idle com resposta TTS

    **Acceptance:**
    - [ ] PTT ainda funciona end-to-end após o refactor da Plan 03
    - [ ] Resposta aparece no chat e TTS toca
    - [ ] Nenhum erro visível no console que não estava presente antes da Phase 24

    ---

    ## Manual-Only Verifications (de 24-VALIDATION.md)

    - [ ] **E2E com mic real em pt-BR** — coberto por SC-1 e SC-2 acima
    - [ ] **Degradação gracioso TTS → texto** — coberto por SC-4 Cenário B
    - [ ] **Error recovery backend down** — coberto por SC-4 Cenário A

    ---

    ## A6 Runtime Assumption Check (Research)

    **What to verify:** `MicVAD.new({ getStream })` reuses the existing MediaStream without triggering a second `getUserMedia` prompt.

    **Steps:**
    1. Clean-start the app (kill + `pnpm --filter @jarvis/desktop dev`)
    2. Grant mic permission when prompted
    3. Count the number of permission prompts that appear

    **Acceptance:**
    - [ ] Exactly ONE mic permission prompt appears (not two)
    - [ ] If two appear, create a gap-fix ticket to pass a dedicated stream in `getStream` rather than reusing

    ---

    ## Decisions Sign-Off

    Confirm each decision from CONTEXT.md was implemented:
    - [ ] D-01: `@ricky0123/vad-web@0.0.30` installed (check `apps/desktop/package.json`)
    - [ ] D-02: Library defaults used (A1 resolution — no explicit threshold/timing override)
    - [ ] D-03: 6s max fallback via `VITE_WAKE_WORD_MAX_RECORDING_MS=6000`
    - [ ] D-04: `TTS_PROVIDER=murf` works in the factory
    - [ ] D-05: Default voice is `pt-BR-heitor`
    - [ ] D-06: TTS failure keeps agent text visible
    - [ ] D-07: `sendAudioAndHandle` pure function exists and is consumed by both PTT and wake word
    - [ ] D-08: Hard errors show pt-BR toast via `ChatContext.setToast`
    - [ ] D-09: 60s internal timeout reused (no external AbortController per A5)
    - [ ] D-10: No barge-in, no multi-turn, no partial streaming (scope locked)

    ---

    ## Final Sign-Off

    **Tester:** _________________________
    **Date:** _________________________
    **Status:** ⬜ All criteria passed / ⬜ Gaps found (list below)

    **Gaps found (if any):**
    1. ...
    2. ...

    **Next action:** Run `/gsd-verify-work 24` (if all passed) OR `/gsd-plan-phase 24 --gaps` (if gaps).
    ```
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → exists
    - `grep -q 'Hey JARVIS' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'SC-1' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'SC-5' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches (all 5 criteria)
    - `grep -q 'TTS_PROVIDER=murf' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'MURF_API_KEY' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'Não ouvi nada' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'JARVIS offline' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'A6 Runtime Assumption' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -q 'Decisions Sign-Off' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → matches
    - `grep -cE 'D-0[1-9]|D-10' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` returns ≥ 10 (all 10 decisions checked)
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md && grep -c 'SC-[1-5]' .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md</automated>
  </verify>
  <done>
    UAT checklist exists, covers all 5 ROADMAP Success Criteria, all 3 Manual-Only Verifications, A6 runtime check, and all 10 decisions from CONTEXT.md.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UAT sign-off</name>
  <files>.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md</files>
  <read_first>
    - .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md (the checklist created in Task 2 — executor presents it to the user)
    - .planning/phases/24-wake-word-full-pipeline-integration/24-VALIDATION.md §Manual-Only Verifications
  </read_first>
  <what-built>
    Phase 24 completa as plans 01-04:
    - MurfTTSProvider implementado + factory estendido (Plan 01)
    - sendAudioAndHandle pure function com testes (Plan 02)
    - ChatInput migrado para shared pipeline (Plan 03)
    - useWakeWord integrado com MicVAD + encodeFloat32ToWav + 6s fallback, byte-discard gap closed (Plan 04)
    - REQUIREMENTS.md + 24-UAT.md atualizados (Plan 05 Tasks 1-2)

    O loop wake word → STT → LLM → TTS → idle está wired end-to-end. Unit tests verificam os contratos mas não provam funcionamento com hardware real — por isso este checkpoint é bloqueante.
  </what-built>
  <how-to-verify>
    Executar o checklist completo em `.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md`:

    1. Configurar `.env` com `TTS_PROVIDER=murf` + `MURF_API_KEY` válido
    2. Rodar backend: `pnpm --filter @jarvis/backend-ts dev`
    3. Rodar gateway: `pnpm --filter @jarvis/gateway dev`
    4. Rodar LM Studio com modelo pt-BR carregado
    5. Rodar desktop: `pnpm --filter @jarvis/desktop dev`
    6. Executar SC-1 até SC-5 em ordem, marcando checkboxes no UAT
    7. Executar as 3 Manual-Only Verifications (já cobertas por SC-1/SC-4)
    8. Validar A6 (apenas UM prompt de mic)
    9. Conferir sign-off das 10 decisões D-01..D-10
    10. Preencher o bloco "Final Sign-Off" ao final do UAT.md
  </how-to-verify>
  <action>
    **CHECKPOINT (blocking) — this task is not auto-executable.**

    The executor presents `.planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` to the user, waits for the user to manually run the checklist against the running app (real mic, real Murf, real LLM), and resumes only after the user reports the result.

    **Executor script:**
    1. Open and display the content of `24-UAT.md`
    2. Remind the user of the pre-requisites block
    3. Do NOT run any code, do NOT commit, do NOT proceed — wait for the user's sign-off message
    4. When the user responds with `approved` or a `gaps:` block, write the result to `24-05-SUMMARY.md` (per `<output>`)
    5. If approved → direct user to `/gsd-verify-work 24`
    6. If gaps → direct user to `/gsd-plan-phase 24 --gaps`

    **Do NOT make assumptions about the sign-off** — if the user does not respond within the execution window, leave the checkpoint pending. The phase remains in `autonomous: false` state until resolved.

    This task has NO code modifications. The `<files>` entry above refers to the UAT.md the executor reads and displays, not files it edits.
  </action>
  <resume-signal>
    Digitar "approved" se todos os itens do UAT passaram, ou listar gaps no formato:
    ```
    gaps:
    - SC-X item Y: <o que falhou>
    - ...
    ```
    Se gaps encontrados, próxima ação é `/gsd-plan-phase 24 --gaps`. Se approved, próxima ação é `/gsd-verify-work 24`.
  </resume-signal>
  <verify>
    <automated>test -f .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md</automated>
  </verify>
  <done>
    Human sign-off collected (text signal "approved" or "gaps: ..."). SUMMARY.md written with the final status. No code changes produced by this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is documentation-only (no code changes outside .md files). No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-40 | Repudiation | Phase declared complete without real-world verification | mitigate | This plan is explicitly `autonomous: false` with a blocking human checkpoint. `/gsd-verify-work 24` cannot run until the checkpoint signal arrives. |
| T-24-41 | Information Disclosure | UAT instructions ask user to set `MURF_API_KEY=invalid_key_for_uat` temporarily (D-06 test) | accept | Instructions explicitly say "temporarily" and "restore after". No real key is exposed. User can skip SC-4 Cenário B if they don't want to modify their key. |
| T-24-42 | Tampering | Traceability table changes (WAKE-05/06 reassigned from Phase 22 to Phase 24) might be seen as rewriting history | mitigate | Plan 05 Task 1 rationale documents the reassignment honestly: "Phase 22 SUMMARY shows they were effectively bypassed." This is a correction, not a backdate. |

**Blocking threats:** T-24-40 is the entire point of the checkpoint — handled by `autonomous: false`.
</threat_model>

<verification>
- `test -f .planning/phases/24-wake-word-full-pipeline-integration/24-UAT.md` → exists
- `grep -qc 'WAKE-1[0-3]' .planning/REQUIREMENTS.md` → 4 matches minimum
- Human sign-off collected in checkpoint resume-signal
</verification>

<success_criteria>
- REQUIREMENTS.md reflects the 4 new Phase 24 requirements and correct traceability
- UAT checklist exists and is complete (all 5 SCs, all 3 manual-only, A6 check, 10 decisions)
- Human has executed the UAT with real mic + real Murf + real LLM
- Sign-off completed (either approved or gaps listed)
</success_criteria>

<output>
After the human sign-off, create `.planning/phases/24-wake-word-full-pipeline-integration/24-05-SUMMARY.md` including:
- Final sign-off status (approved or gaps)
- If gaps: the gap list ready for `/gsd-plan-phase 24 --gaps` consumption
- If approved: confirmation that `/gsd-verify-work 24` is the next step
- Confirmation of A6 mic prompt count (1 or 2)
- Any subjective notes from the tester (voice quality of Heitor, latency perception, etc.)
</output>
