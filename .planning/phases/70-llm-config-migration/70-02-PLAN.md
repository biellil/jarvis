---
phase: 70
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/routes/reload-llm.ts
  - apps/backend-ts/src/routes/mcp-client.ts
  - apps/backend-ts/src/app.ts
  - apps/backend-ts/src/llm/config.ts
  - .env.example
autonomous: true
requirements:
  - SIMP-04
  - SIMP-01
user_setup: []
must_haves:
  truths:
    - "Backend não expõe mais POST /internal/reload-llm (rota deletada)"
    - "Backend não expõe mais /internal/mcp-client/{reload,status} (rota deletada, D-20 default)"
    - "app.ts não importa nem chama createReloadLlmRouter nem createMcpClientRouter"
    - ".env.example contém USE_LM_STUDIO_STREAMING_EVENTS=false posicionado abaixo de LM_STUDIO_MODEL com comentário (D-08)"
    - ".env.example contém GEMINI_API_KEY= (verificado ausente atualmente; adicionar junto com USE_LM_STUDIO_STREAMING_EVENTS)"
    - "Zod schema para USE_LM_STUDIO_STREAMING_EVENTS no llm/config.ts trata literal 'false' como false (corrigido com z.preprocess se z.coerce.boolean() for ingênuo)"
  artifacts:
    - path: apps/backend-ts/src/app.ts
      provides: Express app sem reload-llm router e sem mcp-client router
    - path: .env.example
      provides: Template canônico com USE_LM_STUDIO_STREAMING_EVENTS e GEMINI_API_KEY
      contains: "USE_LM_STUDIO_STREAMING_EVENTS=false"
    - path: apps/backend-ts/src/llm/config.ts
      provides: Zod schema correto para boolean coerce (preprocess explícito se necessário)
      contains: "USE_LM_STUDIO_STREAMING_EVENTS"
  key_links:
    - from: apps/backend-ts/src/app.ts
      to: nada (routes deletadas)
      via: ausência de import/use
      pattern: "createReloadLlmRouter|createMcpClientRouter"
---

<objective>
**Backend cleanup + `.env.example` canonização** — implementa parte de SIMP-04 (deleta o caminho IPC paralelo via HTTP) e parte de SIMP-01 (template `.env.example` reflete o novo contrato que a Settings UI deixa de oferecer).

Tarefas:
1. Deletar `apps/backend-ts/src/routes/reload-llm.ts` inteiro (D-10) + remover import/use em `app.ts`.
2. Deletar `apps/backend-ts/src/routes/mcp-client.ts` inteiro (D-20 default — Claude's discretion confirmou: sem callers após P70) + remover import/use em `app.ts`.
3. Adicionar `USE_LM_STUDIO_STREAMING_EVENTS=false` e `GEMINI_API_KEY=` em `.env.example` (D-08) — verificado por scout: `GEMINI_API_KEY` está ausente, `USE_LM_STUDIO_STREAMING_EVENTS` também.
4. Verificar comportamento de `z.coerce.boolean()` em zod 4.3.6 quando recebe literal `"false"` (Open Question 1 do RESEARCH.md). Se ingênuo (`"false"` → `true`), patchar `apps/backend-ts/src/llm/config.ts:23` para `z.preprocess(v => typeof v === 'string' ? v.toLowerCase() === 'true' : v, z.boolean()).default(false)` — RESEARCH.md flagged como CRITICAL.

**Purpose:** Sem caller via IPC após P70, o endpoint HTTP é dead code. Manter aumenta surface area de bugs e packaging. `.env.example` é o ÚNICO contrato de configuração do usuário pós-P70 — precisa estar completo. Zod coerce bug latente bloquearia a feature de streaming events silenciosamente.

**Output:** 2 arquivos deletados (`reload-llm.ts`, `mcp-client.ts`); 3 arquivos modificados (`app.ts`, `.env.example`, `llm/config.ts`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-llm-config-migration/70-CONTEXT.md
@.planning/phases/70-llm-config-migration/70-RESEARCH.md
@.planning/phases/70-llm-config-migration/70-VALIDATION.md

<interfaces>
<!-- Contratos atuais a serem demolidos -->

From apps/backend-ts/src/app.ts (referência §6, §40 e §7, §45 conforme research):
```typescript
// REMOVE imports + app.use:
import { createReloadLlmRouter } from './routes/reload-llm.js';   // REMOVE
import { createMcpClientRouter } from './routes/mcp-client.js';   // REMOVE
// ...
app.use('/internal/reload-llm', createReloadLlmRouter(...));      // REMOVE
app.use('/internal/mcp-client', createMcpClientRouter(...));      // REMOVE
```

From apps/backend-ts/src/llm/config.ts:23 (Zod schema atual — possível bug):
```typescript
USE_LM_STUDIO_STREAMING_EVENTS: z.coerce.boolean().default(false),
```
Em Zod 3.x e early 4.x este coerce trata QUALQUER string non-empty como `true` (incluindo `"false"`). Open Question 1 do RESEARCH.md exige verificação antes de commit.

From .env.example (estrutura atual, scout confirmou):
```
# ============================================
# LLM
# ============================================
LLM_PROVIDER=lmstudio
LLM_MODEL=

LM_STUDIO_URL=http://localhost:1234/v1
LM_STUDIO_MODEL=
# ← INSERIR USE_LM_STUDIO_STREAMING_EVENTS aqui (D-08)
# ← INSERIR GEMINI_API_KEY aqui (research detectou ausência)

OPENAI_API_KEY=

ANTHROPIC_API_KEY=
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deletar reload-llm.ts e mcp-client.ts; remover imports/use de app.ts (D-10, D-20)</name>

  <files>
    apps/backend-ts/src/routes/reload-llm.ts (DELETE)
    apps/backend-ts/src/routes/mcp-client.ts (DELETE)
    apps/backend-ts/src/app.ts (MODIFY)
  </files>

  <read_first>
    - apps/backend-ts/src/app.ts (arquivo inteiro — identificar linhas exatas de import e app.use)
    - apps/backend-ts/src/routes/reload-llm.ts (confirmar arquivo existe e entender se há tests acoplados)
    - apps/backend-ts/src/routes/mcp-client.ts (confirmar arquivo existe)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-10 e D-20)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — confirmação que ambas rotas não têm callers)
  </read_first>

  <action>
    1. **Deletar arquivos:**
       ```bash
       rm apps/backend-ts/src/routes/reload-llm.ts
       rm apps/backend-ts/src/routes/mcp-client.ts
       ```
       Antes do delete, fazer `grep -rn "from.*routes/reload-llm\|from.*routes/mcp-client\|createReloadLlmRouter\|createMcpClientRouter" apps/backend-ts/src/` para confirmar único caller é `app.ts`. Se algum outro caller aparecer (improvável conforme RESEARCH.md Integration Map), parar e reportar ao orchestrator.

    2. **Editar `apps/backend-ts/src/app.ts`:**
       - Remover linha de import `import { createReloadLlmRouter } from './routes/reload-llm.js';` (ou `.ts` se assim escrito; preservar extensão exata do projeto).
       - Remover linha de import `import { createMcpClientRouter } from './routes/mcp-client.js';`
       - Remover bloco `app.use('/internal/reload-llm', createReloadLlmRouter(...));` (provavelmente uma única linha, mas pode incluir setup adicional — strip o `app.use` completo).
       - Remover bloco `app.use('/internal/mcp-client', createMcpClientRouter(...));`
       - **NÃO** tocar em outros `app.use` (ex: env-watcher de MCP-CLI vive em `src/index.ts`, não em `app.ts` — preservado por D-09).

    3. **Verificar tests do backend não quebram:**
       ```bash
       pnpm --filter @jarvis/backend-ts test:run
       ```
       Se algum test referencia `/internal/reload-llm` ou `/internal/mcp-client`, esse test é dead — deletar o test também (ou removê-lo via grep e o agent decide localmente; grep `grep -rn "reload-llm\|mcp-client/reload\|mcp-client/status" apps/backend-ts/src/` para detectar).

    4. **Verificar typecheck do backend:**
       ```bash
       pnpm --filter @jarvis/backend-ts typecheck
       ```
       Deve passar limpo.
  </action>

  <verify>
    <automated>! test -f apps/backend-ts/src/routes/reload-llm.ts && ! test -f apps/backend-ts/src/routes/mcp-client.ts && ! grep -E "createReloadLlmRouter|createMcpClientRouter|routes/reload-llm|routes/mcp-client" apps/backend-ts/src/app.ts && pnpm --filter @jarvis/backend-ts typecheck && pnpm --filter @jarvis/backend-ts test:run</automated>
  </verify>

  <acceptance_criteria>
    - `! test -f apps/backend-ts/src/routes/reload-llm.ts` (arquivo deletado)
    - `! test -f apps/backend-ts/src/routes/mcp-client.ts` (arquivo deletado)
    - `! grep "createReloadLlmRouter" apps/backend-ts/src/app.ts` (zero matches)
    - `! grep "createMcpClientRouter" apps/backend-ts/src/app.ts` (zero matches)
    - `! grep "routes/reload-llm" apps/backend-ts/src/app.ts` (zero matches)
    - `! grep "routes/mcp-client" apps/backend-ts/src/app.ts` (zero matches)
    - `pnpm --filter @jarvis/backend-ts typecheck` exits 0
    - `pnpm --filter @jarvis/backend-ts test:run` exits 0 (ou apenas após deletar tests órfãos — agent reporta o que removeu)
    - Sanity: `grep -rn "reload-llm\|mcp-client/reload\|mcp-client/status" apps/backend-ts/src/` retorna 0 matches (sem callers órfãos)
  </acceptance_criteria>

  <done>
    `reload-llm.ts` e `mcp-client.ts` (em `apps/backend-ts/src/routes/`) deletados. `app.ts` sem imports nem `app.use` para essas rotas. Backend typecheck verde e tests passando (eventuais tests órfãos limpos no processo). D-10 e D-20 cumpridos.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verificar/corrigir z.coerce.boolean() em llm/config.ts (Open Question 1 — CRITICAL)</name>

  <files>
    apps/backend-ts/src/llm/config.ts
    apps/backend-ts/src/llm/__tests__/config.test.ts (CREATE OR MODIFY se já existir)
  </files>

  <read_first>
    - apps/backend-ts/src/llm/config.ts (arquivo inteiro — entender Zod schema e onde está USE_LM_STUDIO_STREAMING_EVENTS, linha ~23)
    - apps/backend-ts/package.json (confirmar versão de zod — esperado 4.3.6)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Pitfall 7 + Open Question 1 + Assumption A1 — CRITICAL flag)
    - apps/backend-ts/src/llm/__tests__/config.test.ts (se existir — entender pattern; senão, criar do zero)
  </read_first>

  <behavior>
    Test cases (em vitest):
    1. `parses USE_LM_STUDIO_STREAMING_EVENTS=true as true` — env `{ USE_LM_STUDIO_STREAMING_EVENTS: 'true' }` → resultado tem `useLmStudioStreamingEvents` (ou name equivalente do field) === true
    2. `parses USE_LM_STUDIO_STREAMING_EVENTS=false as false (NOT true)` — env `{ USE_LM_STUDIO_STREAMING_EVENTS: 'false' }` → resultado === false. **Este é o teste crítico** — se atualmente passa para true, ele FALHA e indica que o coerce está ingênuo.
    3. `defaults to false when key missing` — env sem a key → resultado === false
    4. `accepts boolean true literal (non-string)` — env `{ USE_LM_STUDIO_STREAMING_EVENTS: true }` → true (se aplicável a interface de loadConfig)

    Se test #2 falhar contra a implementação atual:
    - Substituir o schema line 23 (ou onde estiver) por:
      ```typescript
      USE_LM_STUDIO_STREAMING_EVENTS: z.preprocess(
        (v) => typeof v === 'string' ? v.toLowerCase() === 'true' : v,
        z.boolean()
      ).default(false),
      ```
    - Re-run test, deve passar.

    Se test #2 PASSA já contra a implementação atual (zod 4.3.6 corrigiu o coerce), deixar `config.ts` inalterado. Mas manter o test como guardrail.
  </behavior>

  <action>
    1. **Localizar o schema atual:**
       ```bash
       grep -n "USE_LM_STUDIO_STREAMING_EVENTS" apps/backend-ts/src/llm/config.ts
       ```
       Esperado: linha ~23 com `z.coerce.boolean().default(false)` (ou similar).

    2. **Verificar se existe `apps/backend-ts/src/llm/__tests__/config.test.ts`:**
       - Se sim, abrir e localizar bloco de testes relacionados a `USE_LM_STUDIO_STREAMING_EVENTS`. Se não cobrir os 3 cenários da behavior, adicionar.
       - Se não, criar `apps/backend-ts/src/llm/__tests__/config.test.ts` com vitest `describe('loadConfig USE_LM_STUDIO_STREAMING_EVENTS', () => { ... })` cobrindo os 4 cenários da behavior section.

    3. **Rodar tests:**
       ```bash
       pnpm --filter @jarvis/backend-ts test:run src/llm/__tests__/config.test.ts
       ```
       - Se #2 PASSA com schema atual: nenhuma mudança em `config.ts`. Documentar no SUMMARY que zod 4.3.6 trata `"false"` corretamente. Commit só com o test guardrail.
       - Se #2 FALHA: patchar `config.ts` linha 23 para usar `z.preprocess` conforme behavior. Re-run, confirmar pass. Commit com patch + test.

    4. **Validar TypeScript:**
       ```bash
       pnpm --filter @jarvis/backend-ts typecheck
       ```

    **IMPORTANTE:** Mesmo se zod 4.3.6 já está correto e nenhum patch é necessário em `config.ts`, **o test ainda deve existir** — serve como guardrail contra regressão se a versão de zod for downgradada ou substituída.
  </action>

  <verify>
    <automated>pnpm --filter @jarvis/backend-ts test:run src/llm/__tests__/config.test.ts && pnpm --filter @jarvis/backend-ts typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `test -f apps/backend-ts/src/llm/__tests__/config.test.ts` retorna 0
    - `grep -c "USE_LM_STUDIO_STREAMING_EVENTS" apps/backend-ts/src/llm/__tests__/config.test.ts` retorna `>=3` (cobertura dos cenários)
    - Test crítico passa: `pnpm --filter @jarvis/backend-ts test:run src/llm/__tests__/config.test.ts -- -t "false as false"` retorna pass (ou nome equivalente do `it`)
    - `pnpm --filter @jarvis/backend-ts typecheck` exits 0
    - Se patch aplicado: `grep "z.preprocess" apps/backend-ts/src/llm/config.ts` retorna `>=1`. Se NÃO aplicado: `grep "z.coerce.boolean" apps/backend-ts/src/llm/config.ts` retorna `>=1`. SUMMARY documenta qual caso.
  </acceptance_criteria>

  <done>
    Test guardrail existe e passa para os 4 cenários (true, "false" as false, missing default, boolean literal). Se zod 4.3.6 era ingênuo, `config.ts` patchado com `z.preprocess`. Open Question 1 do RESEARCH.md resolvida com evidência (test green). SIMP-04 fica protegida contra silenciosamente sempre-ligar o streaming events.
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar .env.example com USE_LM_STUDIO_STREAMING_EVENTS=false e GEMINI_API_KEY (D-08)</name>

  <files>.env.example</files>

  <read_first>
    - .env.example (arquivo inteiro — confirmar ordem atual: LM_STUDIO_URL, LM_STUDIO_MODEL, depois OPENAI_API_KEY)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-08 posicionamento)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Example 2 — diff exato)
  </read_first>

  <action>
    Editar `.env.example` na seção `# LLM`:

    **Atualmente** (verificado por scout):
    ```
    # LM Studio (used when LLM_PROVIDER=lmstudio)
    # For Docker: use http://host.docker.internal:1234/v1 (Linux requires extra_hosts: host-gateway)
    LM_STUDIO_URL=http://localhost:1234/v1
    LM_STUDIO_MODEL=

    # OpenAI (used when LLM_PROVIDER=openai)
    OPENAI_API_KEY=

    # Anthropic / Claude (used when LLM_PROVIDER=anthropic)
    ANTHROPIC_API_KEY=
    ```

    **Após edit (inserir 2 entradas com comentários, abaixo de LM_STUDIO_MODEL):**
    ```
    # LM Studio (used when LLM_PROVIDER=lmstudio)
    # For Docker: use http://host.docker.internal:1234/v1 (Linux requires extra_hosts: host-gateway)
    LM_STUDIO_URL=http://localhost:1234/v1
    LM_STUDIO_MODEL=
    # true = usa SSE nativo /api/v1/chat do LM Studio (mais rápido)
    # false = fallback OpenAI-compat (default seguro)
    USE_LM_STUDIO_STREAMING_EVENTS=false

    # Google Gemini (used when LLM_PROVIDER=gemini)
    GEMINI_API_KEY=

    # OpenAI (used when LLM_PROVIDER=openai)
    OPENAI_API_KEY=

    # Anthropic / Claude (used when LLM_PROVIDER=anthropic)
    ANTHROPIC_API_KEY=
    ```

    **Atenção:**
    1. NÃO mexer nas linhas existentes (preservar comments, blank lines, ordem).
    2. Inserir EXATAMENTE 2 linhas de comment + 1 linha de valor para `USE_LM_STUDIO_STREAMING_EVENTS`, conforme RESEARCH.md Example 2.
    3. Inserir 1 linha de comment + 1 linha de valor para `GEMINI_API_KEY` (research detectou ausência — `LLM_PROVIDER=gemini` é caso suportado pelo backend mas estava sem placeholder em `.env.example`).
    4. Manter `LLM_PROVIDER` provider list comment atualizado (atual: `# Provider: lmstudio | openai | anthropic` — deve virar `# Provider: lmstudio | openai | anthropic | gemini` para refletir o 4o provider de P57). Verificar se já está atualizado; se sim, no-op.

    Sequência exata recomendada (block atomic via overwrite com Write tool):
    - Use Read para pegar o arquivo inteiro.
    - Use Edit (ou Write se preservação for problemática) para inserir os blocos novos.
  </action>

  <verify>
    <automated>grep "USE_LM_STUDIO_STREAMING_EVENTS=false" .env.example && grep "^GEMINI_API_KEY=" .env.example && grep "# true = usa SSE nativo" .env.example && grep "lmstudio | openai | anthropic | gemini" .env.example</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "USE_LM_STUDIO_STREAMING_EVENTS=false" .env.example` retorna 1
    - `grep -c "^GEMINI_API_KEY=" .env.example` retorna 1
    - `grep "# true = usa SSE nativo" .env.example` retorna 1 match (comment D-08)
    - `grep "# false = fallback OpenAI-compat" .env.example` retorna 1 match
    - Posicionamento: linha de `USE_LM_STUDIO_STREAMING_EVENTS` deve aparecer DEPOIS de `LM_STUDIO_MODEL=` e ANTES de `OPENAI_API_KEY=`. Validar com `awk '/^LM_STUDIO_MODEL=/{a=NR} /^USE_LM_STUDIO_STREAMING_EVENTS=/{b=NR} /^OPENAI_API_KEY=/{c=NR} END{exit (a<b && b<c)?0:1}' .env.example`.
    - Provider list atualizado: `grep "lmstudio | openai | anthropic | gemini" .env.example` retorna `>=1` (se já estava atualizado, no-op confirmado; se não, edit aplicado).
    - Arquivo continua válido (parseável): `node -e "require('dotenv').config({path:'.env.example'})"` exits 0 (smoke).
  </acceptance_criteria>

  <done>
    `.env.example` ganha `USE_LM_STUDIO_STREAMING_EVENTS=false` com 2 linhas de comment (D-08) posicionado abaixo de `LM_STUDIO_MODEL=`; ganha `GEMINI_API_KEY=` na section LLM; LLM_PROVIDER comment list inclui `gemini`. Arquivo permanece sintaticamente válido (`dotenv.config` parseia).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP `/internal/*` | Express server interno escuta em `127.0.0.1:PORT` — antes acessível pelo Electron via fetch; pós-P70 sem callers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-05 | Elevation of Privilege | Endpoint HTTP `/internal/reload-llm` aceitava JSON com API keys plaintext — surface area indevida agora que UI sai | mitigate | Delete completo do route file + app.use (D-10). Zero surface area após task 1. |
| T-70-06 | Tampering | `USE_LM_STUDIO_STREAMING_EVENTS=false` silenciosamente sempre-on se zod coerce ingênuo | mitigate | Task 2 — test guardrail + patch via `z.preprocess` se necessário. Open Question 1 resolvida com evidência. |
| T-70-07 | Information Disclosure | API keys em `.env.example` (placeholder vazio) — não é threat real porque arquivo é template público; nenhum valor sensível | accept | Template fica em git. Apenas placeholders `=` sem valores. |
</threat_model>

<verification>
- Routes deletadas: `! test -f` em ambos retorna sucesso.
- `app.ts` limpo: `grep` por símbolos retorna 0.
- Zod boolean comportamento confirmado por test guardrail.
- `.env.example` reflete o novo contrato com posicionamento correto.
</verification>

<success_criteria>
1. SIMP-04 fortalecida — caminho HTTP de override dead, backend só lê `.env`.
2. SIMP-01 protegido — `.env.example` é a documentação canônica do que o usuário configura agora que a UI não oferece nada LLM.
3. Zod coerce boolean validado contra Open Question 1 — test guardrail previne regressão.
4. D-20 cumprido — `mcp-client.ts` route deletado (default Claude's discretion).
</success_criteria>

<output>
After completion, create `.planning/phases/70-llm-config-migration/70-02-SUMMARY.md`.
</output>
