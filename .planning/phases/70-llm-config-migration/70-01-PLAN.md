---
phase: 70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/migrations/llm-config.ts
  - apps/desktop/src/main/migrations/llm-config-runner.ts
  - apps/desktop/src/main/migrations/__tests__/llm-config.test.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/main/store.ts
autonomous: true
requirements:
  - SIMP-03
  - SIMP-04
user_setup: []
must_haves:
  truths:
    - "Pure function migrateLlmConfigToEnv() é testada com >=9 cenários cobrindo D-02, D-03, D-06, D-07"
    - "Boot do Electron chama runLlmConfigMigration() ANTES de process.loadEnvFile() em index.ts"
    - "Após migração bem-sucedida, electron-store não contém mais as 6 chaves LLM (batch atomic delete)"
    - "Se .env é ausente, migração é no-op silencioso com console.warn (D-05)"
    - "Schema interface entries + 10 accessors LLM removidos de store.ts (~120 linhas)"
  artifacts:
    - path: apps/desktop/src/main/migrations/llm-config.ts
      provides: Pure migration function (no I/O) com parser dotenv.parse + upsert line-based preservando comments
      exports: [migrateLlmConfigToEnv, LLM_STORE_KEYS, LlmStoreSnapshot, MigrationResult]
    - path: apps/desktop/src/main/migrations/llm-config-runner.ts
      provides: I/O wrapper que lê .env + store, chama pure function, escreve atomic, faz batch cleanup
      exports: [runLlmConfigMigration]
    - path: apps/desktop/src/main/migrations/__tests__/llm-config.test.ts
      provides: Vitest com 9 cenários do test matrix (RESEARCH.md Example 3)
      contains: "describe('migrateLlmConfigToEnv'"
    - path: apps/desktop/src/main/index.ts
      provides: Hook D-04 — runLlmConfigMigration(envPath) antes de process.loadEnvFile(envPath)
      contains: "runLlmConfigMigration"
    - path: apps/desktop/src/main/store.ts
      provides: Schema sem entries LLM + sem accessors LLM
      min_lines_removed: 100
  key_links:
    - from: apps/desktop/src/main/index.ts
      to: apps/desktop/src/main/migrations/llm-config-runner.ts
      via: import + call antes de process.loadEnvFile
      pattern: "runLlmConfigMigration\\(envPath\\)"
    - from: apps/desktop/src/main/migrations/llm-config-runner.ts
      to: apps/desktop/src/main/store.ts
      via: store.get() para snapshot + store.store = {...} para batch delete
      pattern: "store\\.get\\('llmProvider'\\)"
    - from: apps/desktop/src/main/migrations/llm-config.ts
      to: dotenv (transitively via backend-ts no monorepo)
      via: parse() import
      pattern: "from 'dotenv'"
---

<objective>
**Núcleo da migração LLM electron-store → `.env`** — implementa SIMP-03 (migração automática) e cobre a parte de SIMP-04 referente à remoção de schema/accessors do `store.ts`.

Cria pure function testável `migrateLlmConfigToEnv()` que recebe `(envContent, storeSnapshot)` e devolve `{ newEnvContent, keysToDelete, migratedKeys, skippedKeys }` preservando comments e blank lines do `.env` original (rejeita `envfile`/`dotenv-stringify` que destroem comments). Side-effect wrapper `runLlmConfigMigration()` lê snapshot do `store`, faz I/O com atomic write (temp + rename) e cleanup batch atômico via `store.store = next`. Hook em `apps/desktop/src/main/index.ts` ANTES de `process.loadEnvFile` (D-04). Strip de ~120 linhas do `store.ts` removendo schema entries + 10 accessors LLM.

**Purpose:** Privacy-first — API keys saem de electron-store JSON plaintext (`644`) para `.env` controlado por OS file perms (`600`). Estado único de source-of-truth para LLM config = `.env`. SIMP-03 cumprida sem intervenção manual do usuário.

**Output:** 3 novos arquivos (pure function + runner + test); 2 arquivos modificados (`index.ts` ganha hook, `store.ts` perde ~120 linhas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-llm-config-migration/70-CONTEXT.md
@.planning/phases/70-llm-config-migration/70-RESEARCH.md
@.planning/phases/70-llm-config-migration/70-VALIDATION.md

<interfaces>
<!-- Contratos extraídos do código atual. Executor usa diretamente sem explorar codebase. -->

From apps/desktop/src/main/store.ts (linhas relevantes):
```typescript
// Schema entries a REMOVER (linhas 49, 51, 59, 61, 62, 63):
interface StoreSchema {
  // ... outras keys preservadas (whisper, kokoro, ptt, etc.)
  lmStudioUrl?: string;                          // §49 — REMOVE
  llmProvider?: LlmProvider;                     // §51 — REMOVE (e remove import LlmProvider se órfão)
  streamingLMStudioEventsEnabled?: boolean;      // §59 — REMOVE
  geminiApiKey?: { key: string };                // §61 — REMOVE
  openaiApiKey?: { key: string };                // §62 — REMOVE
  anthropicApiKey?: { key: string };             // §63 — REMOVE
}

// Accessors a REMOVER (10 funções, ~120 linhas):
// §292-298: getLmStudioUrl, setLmStudioUrl
// §305-317: getLlmProvider, setLlmProvider
// §375-381: getStreamingLMStudioEventsEnabled, setStreamingLMStudioEventsEnabled
// §390-395: getGeminiApiKey, setGeminiApiKey
// §399-403: getOpenaiApiKey, setOpenaiApiKey
// §407-411: getAnthropicApiKey, setAnthropicApiKey

// Default export `store` permanece intacto. Migration reads via store.get(key) direto.
```

From apps/desktop/src/main/index.ts (linhas 12-19, hook D-04):
```typescript
import path from 'node:path';
import process from 'node:process';

const envPath = path.resolve(import.meta.dirname ?? __dirname, '../../../../.env');

// EXISTING (NÃO TOCAR estrutura — só adicionar chamada nova antes):
try {
  process.loadEnvFile(envPath);
} catch {
  // .env é opcional — loadBackendConfig fail-fast em vars obrigatórias.
}
```

From apps/backend-ts/src/mcp/client/env-diff.ts (referência de uso de dotenv.parse):
```typescript
import { parse } from 'dotenv';
const parsed = parse(content); // Record<string, string>
```

From apps/backend-ts/src/llm/config.ts (Zod schema — NÃO MODIFICAR, referência canônica):
```typescript
// Já lê: LLM_PROVIDER, LM_STUDIO_URL, OPENAI_API_KEY, ANTHROPIC_API_KEY, GEMINI_API_KEY, USE_LM_STUDIO_STREAMING_EVENTS
// SIMP-04 satisfeito do lado backend — Phase 70 não toca este arquivo.
```

Reference test pattern (P68): apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts — pure-function test sem mocks de FS.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar pure function migrateLlmConfigToEnv() + runner + 9 cenários de test (Wave 0 + W1)</name>

  <files>
    apps/desktop/src/main/migrations/llm-config.ts (CREATE)
    apps/desktop/src/main/migrations/llm-config-runner.ts (CREATE)
    apps/desktop/src/main/migrations/__tests__/llm-config.test.ts (CREATE)
  </files>

  <read_first>
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Architecture Pattern 1, Pattern 2, Pattern 3, Example 3 test matrix, Pitfall 1-7)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-01 a D-07, D-08)
    - apps/backend-ts/src/mcp/client/env-diff.ts (referência de uso do `dotenv.parse`)
    - apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts (padrão de pure-function test em vitest no desktop)
    - apps/desktop/src/main/store.ts §49-65 e §292-411 (shapes de valores que serão lidos pelo runner)
  </read_first>

  <behavior>
    Pure function tests (9 cenários OBRIGATÓRIOS, todos derivados de RESEARCH.md Example 3):
    1. `no-op when store is empty` — snapshot vazio → migratedKeys=[], keysToDelete=[], newEnvContent inalterado
    2. `writes all keys when .env is empty` — `.env`=`''` + snapshot com llmProvider/openaiApiKey → escreve ambos
    3. `preserves .env when it has non-empty value (D-02)` — `.env` já tem `LLM_PROVIDER=lmstudio`, snapshot tem `llmProvider:'openai'` → newEnvContent inalterado, skippedKeys=['LLM_PROVIDER'], keysToDelete=['llmProvider'] (D-06 sempre apaga)
    4. `overwrites .env when key exists but is empty (D-02)` — `.env` tem `LLM_PROVIDER=` (vazio), snapshot tem `llmProvider:'openai'` → newEnvContent passa a `LLM_PROVIDER=openai\n`
    5. `preserves comments and blank lines` — input `# LLM section\nLLM_PROVIDER=\n\n# Other\nFOO=bar\n` + snapshot llmProvider:'openai' → output preserva `# LLM section` e `# Other` e a blank line
    6. `serializes boolean as "true"/"false" literal (D-07)` — `streamingLMStudioEventsEnabled:true` → `USE_LM_STUDIO_STREAMING_EVENTS=true`; mesmo com `false` literal
    7. `unwraps {key: string} for API keys` — `geminiApiKey:{key:'AIzaSy-test'}` → `GEMINI_API_KEY=AIzaSy-test`
    8. `idempotent — second run no-op after store cleanup` — `.env` cheio + snapshot vazio → migratedKeys=[], keysToDelete=[], newEnvContent inalterado
    9. `mixed: some keys win in .env, some win in store` — caso composto (RESEARCH.md Example 3 último teste)

    Pure function MUST:
    - Importar `parse as parseDotenv` de `dotenv` (já no monorepo via backend-ts via pnpm hoisting; sem mudança em package.json)
    - Exportar `LLM_STORE_KEYS` (tuple as const) com 6 valores: llmProvider, lmStudioUrl, geminiApiKey, openaiApiKey, anthropicApiKey, streamingLMStudioEventsEnabled
    - Exportar `LlmStoreSnapshot` interface com os 6 fields opcionais (geminiApiKey/openaiApiKey/anthropicApiKey como `{ key: string }`; streamingLMStudioEventsEnabled como `boolean`; resto como string scalar)
    - Exportar `MigrationResult` interface `{ newEnvContent: string; keysToDelete: (typeof LLM_STORE_KEYS)[number][]; migratedKeys: string[]; skippedKeys: string[] }`
    - Função `migrateLlmConfigToEnv(envContent, snapshot)` itera LLM_STORE_KEYS, extrai valor via switch (`extractStoreValue`), compara com `parseDotenv(envContent)[envKey]`, decide via D-02 (`.env` non-empty → skip+delete; `.env` empty/missing → write+delete), aplica `upsertEnvKey` mantendo content base
    - `extractStoreValue` faz unwrap específico por shape: API keys retornam `snap.xxxApiKey?.key` (or undefined se vazio); boolean vira `'true'`/`'false'` literal; resto retorna scalar `string | undefined` (filtra strings vazias para undefined)
    - `upsertEnvKey(content, key, value)`: regex `new RegExp('^' + escapeRegex(key) + '=.*$', 'm')` — se match, replace única linha; se miss, append com `\n` separator se content não termina com `\n`
    - `serializeEnvValue(value)`: bare se `!/[\s"'#\n]/.test(value)`; senão double-quoted com escape `\\`, `\"`, `\n`
    - `escapeRegex` standard `s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')`

    Map STORE_TO_ENV (private const):
    - llmProvider → LLM_PROVIDER
    - lmStudioUrl → LM_STUDIO_URL
    - geminiApiKey → GEMINI_API_KEY
    - openaiApiKey → OPENAI_API_KEY
    - anthropicApiKey → ANTHROPIC_API_KEY
    - streamingLMStudioEventsEnabled → USE_LM_STUDIO_STREAMING_EVENTS

    Runner (`llm-config-runner.ts`) — side-effect wrapper:
    - Import `existsSync, readFileSync, writeFileSync, renameSync` de `node:fs`
    - Import `migrateLlmConfigToEnv, LLM_STORE_KEYS, type LlmStoreSnapshot` de `./llm-config.js`
    - Import default `store` de `../store.js`
    - Função `runLlmConfigMigration(envPath: string): void`:
      1. Se `!existsSync(envPath)`: `console.warn('[migration] .env not found at ' + envPath + ', skipping')`; return (D-05)
      2. Montar snapshot lendo `store.get('llmProvider')`, `store.get('lmStudioUrl')`, etc. com type assertions (`as` para casting). NÃO usar accessors — chamar `store.get` direto porque accessors serão deletados.
      3. Se nenhuma key do snapshot definida: return (idempotência D-03)
      4. `readFileSync(envPath, 'utf-8')` → conteúdo
      5. Chamar pure function → resultado
      6. Se `result.migratedKeys.length > 0`: atomic write — `tmpPath = envPath + '.tmp-' + process.pid`; `writeFileSync(tmpPath, ...)`; `renameSync(tmpPath, envPath)` (Pattern 2 — RESEARCH.md)
      7. Se `result.keysToDelete.length > 0`: batch atomic delete — `const current = { ...store.store } as Record<string, unknown>; for (const k of result.keysToDelete) delete current[k]; store.store = current as never;` (Pattern 3)
      8. Log informativo se algo aconteceu: `console.log('[migration] LLM config: migrated N key(s) to .env [KEY1, KEY2]; M already present [KEYX]; X store key(s) cleaned')`

    Test file structure:
    - Vitest `import { describe, it, expect } from 'vitest'`
    - `import { migrateLlmConfigToEnv } from '../llm-config.js'` (use `.js` extension — projeto usa NodeNext/ESM)
    - 9 `it(...)` blocks com nomes idênticos aos da behavior list
    - NÃO mockar FS — pure function não toca FS, só strings
  </behavior>

  <action>
    1. **Criar `apps/desktop/src/main/migrations/llm-config.ts`** exatamente com a estrutura de RESEARCH.md §Architecture Pattern 1 (linhas 170-307 do RESEARCH.md):
       - Imports: `import { parse as parseDotenv } from 'dotenv';`
       - Export `LLM_STORE_KEYS = ['llmProvider', 'lmStudioUrl', 'geminiApiKey', 'openaiApiKey', 'anthropicApiKey', 'streamingLMStudioEventsEnabled'] as const;`
       - Type `LlmProvider` localmente: `type LlmProvider = 'lmstudio' | 'openai' | 'anthropic' | 'gemini';` (não importar de ipc-types — Plan 03 vai deletar; manter migração standalone)
       - Export `LlmStoreSnapshot` interface
       - Const privada `STORE_TO_ENV: Record<typeof LLM_STORE_KEYS[number], string>` com o mapping acima
       - Export `MigrationResult` interface
       - Export `migrateLlmConfigToEnv(envContent, snapshot)` — implementação loop conforme behavior
       - Funções privadas: `extractStoreValue`, `upsertEnvKey`, `escapeRegex`, `serializeEnvValue` conforme RESEARCH.md Pattern 1

    2. **Criar `apps/desktop/src/main/migrations/llm-config-runner.ts`** com `runLlmConfigMigration(envPath)` exatamente conforme RESEARCH.md linhas 312-363 (incluindo logs informativos D-19 Claude's discretion + Pattern 3 batch delete).

    3. **Criar `apps/desktop/src/main/migrations/__tests__/llm-config.test.ts`** com os 9 `it(...)` cenários listados na behavior section. Usar literais exatos de input/output conforme RESEARCH.md Example 3 (linhas 524-604). Cada teste é um assert único + comentário curto referenciando a decisão (D-02, D-06, D-07).

    4. **Rodar test scoped**: `pnpm --filter @jarvis/desktop test:run src/main/migrations/__tests__/llm-config.test.ts`. Confirmar todos 9 passes.

    5. **NÃO** adicionar `dotenv` ao `apps/desktop/package.json` — pnpm hoisting do monorepo já expõe (verificado em RESEARCH.md). Se import falhar com "Cannot find module 'dotenv'", então adicionar `"dotenv": "^17.4.2"` em `apps/desktop/package.json` dependencies e re-run `pnpm install` da raiz.

    **NÃO** implementar hook em index.ts neste task (vem no Task 2). **NÃO** deletar accessors do store.ts ainda (vem no Task 3 do mesmo plan).
  </action>

  <verify>
    <automated>pnpm --filter @jarvis/desktop test:run src/main/migrations/__tests__/llm-config.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `test -f apps/desktop/src/main/migrations/llm-config.ts` retorna 0
    - `test -f apps/desktop/src/main/migrations/llm-config-runner.ts` retorna 0
    - `test -f apps/desktop/src/main/migrations/__tests__/llm-config.test.ts` retorna 0
    - `grep -c "^  it(" apps/desktop/src/main/migrations/__tests__/llm-config.test.ts` retorna `>=9`
    - `grep "export function migrateLlmConfigToEnv" apps/desktop/src/main/migrations/llm-config.ts` retorna 1 match
    - `grep "export function runLlmConfigMigration" apps/desktop/src/main/migrations/llm-config-runner.ts` retorna 1 match
    - `grep "store.store = current" apps/desktop/src/main/migrations/llm-config-runner.ts` retorna 1 match (batch delete pattern)
    - `grep "renameSync" apps/desktop/src/main/migrations/llm-config-runner.ts` retorna `>=1` (atomic write)
    - `pnpm --filter @jarvis/desktop test:run src/main/migrations/__tests__/llm-config.test.ts` exits 0 com 9 passes
  </acceptance_criteria>

  <done>
    Pure function `migrateLlmConfigToEnv` + runner `runLlmConfigMigration` criados em `apps/desktop/src/main/migrations/`. 9 cenários de teste passando (no-op store vazio, write em .env vazio, .env vence non-empty, .env overwrite quando vazio, comments preservados, boolean literal, API key unwrap, idempotência segunda run, mixed scenario). Sem touch em index.ts ou store.ts ainda.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plugar runLlmConfigMigration() no boot do Electron antes de process.loadEnvFile (D-04)</name>

  <files>apps/desktop/src/main/index.ts</files>

  <read_first>
    - apps/desktop/src/main/index.ts (linhas 1-25 — entender hook point atual)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-04 sequência migrate → loadEnvFile → spawn)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Example 1 — Hook point exato linha 478-500)
    - apps/desktop/src/main/migrations/llm-config-runner.ts (criado no Task 1 — confirmar nome do export)
  </read_first>

  <action>
    Em `apps/desktop/src/main/index.ts`:

    1. Adicionar import logo após os imports `node:*` existentes (provavelmente após linha do `import process`):
       ```typescript
       import { runLlmConfigMigration } from './migrations/llm-config-runner.js';
       ```

    2. Antes do bloco `try { process.loadEnvFile(envPath) } catch { ... }` existente (em torno da linha 16), inserir:
       ```typescript
       // Phase 70 (SIMP-03): migrate legacy electron-store LLM config to .env BEFORE loadEnvFile.
       // Idempotent — no-op after first successful run (D-03/D-06).
       try {
         runLlmConfigMigration(envPath);
       } catch (err) {
         console.error('[migration] LLM config migration failed (non-fatal):', err);
         // Non-fatal — boot continua; backend ainda lê .env como está.
       }
       ```

    3. **NÃO** alterar o `try { process.loadEnvFile(envPath) } catch { ... }` existente — apenas adicionar bloco novo ANTES dele.

    4. **NÃO** alterar a definição de `envPath` (linha 15 atual — `path.resolve(import.meta.dirname ?? __dirname, '../../../../.env')`); a localização do `.env` permanece no monorepo root por D-01.

    5. Rodar typecheck do workspace para garantir que o import resolve e tipos batem: `pnpm --filter @jarvis/desktop typecheck`.

    6. Smoke test manual (logado mas não bloqueante para CI): `pnpm dev` deve bootar sem erro e logar `[migration]` no console se store tem keys legadas, ou silenciar se não tem.
  </action>

  <verify>
    <automated>grep -B1 -A3 "runLlmConfigMigration" apps/desktop/src/main/index.ts && pnpm --filter @jarvis/desktop typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep "runLlmConfigMigration" apps/desktop/src/main/index.ts` retorna `>=2` matches (1 import + 1 chamada)
    - `grep "from './migrations/llm-config-runner.js'" apps/desktop/src/main/index.ts` retorna 1 match
    - Ordem correta: linha do `runLlmConfigMigration(envPath)` aparece ANTES da linha `process.loadEnvFile(envPath)` em index.ts. Validar via `awk '/runLlmConfigMigration\(envPath\)/{a=NR} /process\.loadEnvFile\(envPath\)/{b=NR} END{exit (a<b)?0:1}' apps/desktop/src/main/index.ts`
    - `pnpm --filter @jarvis/desktop typecheck` exits 0
  </acceptance_criteria>

  <done>
    Hook D-04 instalado: `runLlmConfigMigration(envPath)` chamado em `apps/desktop/src/main/index.ts` ANTES de `process.loadEnvFile(envPath)`, dentro de try/catch que loga erro sem crashar o boot. Typecheck verde. `envPath` inalterado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip schema + accessors LLM de store.ts (~120 linhas, D-17)</name>

  <files>apps/desktop/src/main/store.ts</files>

  <read_first>
    - apps/desktop/src/main/store.ts (arquivo inteiro — entender shape do schema, accessors, default export)
    - .planning/phases/70-llm-config-migration/70-CONTEXT.md (D-17 listagem exata dos accessors a remover)
    - .planning/phases/70-llm-config-migration/70-RESEARCH.md (Integration Map — store.ts mutate)
  </read_first>

  <action>
    Strip cirúrgico em `apps/desktop/src/main/store.ts`:

    **Schema interface (`StoreSchema` ou equivalente)** — remover 6 fields:
    - `lmStudioUrl?: string;` (§49)
    - `llmProvider?: LlmProvider;` (§51)
    - `streamingLMStudioEventsEnabled?: boolean;` (§59)
    - `geminiApiKey?: { key: string };` (§61)
    - `openaiApiKey?: { key: string };` (§62)
    - `anthropicApiKey?: { key: string };` (§63)

    **JSON Schema entries (se existirem em `schemaShape` ou similar)** — remover as 6 keys correspondentes.

    **Import órfão** — após remover `llmProvider?: LlmProvider`, verificar se `LlmProvider` type ainda é referenciado em store.ts:
    - `grep -c "LlmProvider" apps/desktop/src/main/store.ts` — se 0 referências sobreviventes, remover a linha de import `import type { LlmProvider } from '...ipc-types.js'` (ou equivalente).
    - Se ainda referenciado por outro field não-LLM, manter import. (Esperado: zero referências após strip.)

    **Accessors** — remover 10 funções (presumir blocos de getters/setters individuais):
    - §292-298: `getLmStudioUrl`, `setLmStudioUrl`
    - §305-317: `getLlmProvider`, `setLlmProvider`
    - §375-381: `getStreamingLMStudioEventsEnabled`, `setStreamingLMStudioEventsEnabled`
    - §390-395: `getGeminiApiKey`, `setGeminiApiKey`
    - §399-403: `getOpenaiApiKey`, `setOpenaiApiKey`
    - §407-411: `getAnthropicApiKey`, `setAnthropicApiKey`

    Deletar cada função inteira (assinatura + corpo + export se houver `export function`/`export const`). NÃO substituir por stubs.

    **Default export `store`** permanece intacto — `runLlmConfigMigration` (Task 2) lê via `store.get(key)` direto.

    **Outros accessors não-LLM (whisper, kokoro, ptt, etc.)** — NÃO TOCAR. Strip é cirúrgico.

    **Validação:**
    1. `pnpm --filter @jarvis/desktop typecheck` deve passar — se quebrar, indica que algum código consumidor desses accessors não foi atualizado (Plan 03 esperado, mas validar). Se quebrar em arquivo que NÃO está em Plan 03 (`apps/desktop/src/main/ipc/settings.ts`, `apps/desktop/src/preload/settings.ts`, `apps/desktop/src/renderer/...`), isso é blocker — relatar ao orchestrator.
    2. Como Plan 03 ainda não rodou, é esperado que `apps/desktop/src/main/ipc/settings.ts` quebre referenciando accessors removidos. Solução: Wave dependency — Plan 03 (wave 2) deve completar strip dos consumidores. **Em vez disso, este task NÃO faz typecheck full ainda — só verifica que store.ts compila isoladamente:** `pnpm --filter @jarvis/desktop typecheck` é executado em Plan 03 final task.

    Para verificação isolada deste task, usar grep apenas (acceptance_criteria abaixo).
  </action>

  <verify>
    <automated>! grep -E "lmStudioUrl\?|llmProvider\?|streamingLMStudioEventsEnabled\?|geminiApiKey\?|openaiApiKey\?|anthropicApiKey\?" apps/desktop/src/main/store.ts && ! grep -E "getLmStudioUrl|setLmStudioUrl|getLlmProvider|setLlmProvider|getGeminiApiKey|setGeminiApiKey|getOpenaiApiKey|setOpenaiApiKey|getAnthropicApiKey|setAnthropicApiKey|getStreamingLMStudioEventsEnabled|setStreamingLMStudioEventsEnabled" apps/desktop/src/main/store.ts</automated>
  </verify>

  <acceptance_criteria>
    - Schema entries removidos: `! grep -E "lmStudioUrl\?|llmProvider\?|streamingLMStudioEventsEnabled\?|geminiApiKey\?|openaiApiKey\?|anthropicApiKey\?" apps/desktop/src/main/store.ts` retorna 0 matches (exit 0 = success of `!`)
    - Accessors removidos: `! grep -E "getLmStudioUrl|setLmStudioUrl|getLlmProvider|setLlmProvider|getGeminiApiKey|setGeminiApiKey|getOpenaiApiKey|setOpenaiApiKey|getAnthropicApiKey|setAnthropicApiKey|getStreamingLMStudioEventsEnabled|setStreamingLMStudioEventsEnabled" apps/desktop/src/main/store.ts` retorna 0 matches
    - Diff de tamanho: `git diff apps/desktop/src/main/store.ts | grep -c "^-"` retorna `>=80` (heurística: ~120 linhas removidas, conservador 80)
    - Default export `store` preservado: `grep "export default store" apps/desktop/src/main/store.ts` retorna 1 match (ou padrão equivalente do projeto)
    - Outros accessors preservados (sanity): `grep -E "getWhisper|getKokoro|getPtt" apps/desktop/src/main/store.ts` retorna `>=1` (não tudo foi varrido por engano)
    - `runLlmConfigMigration` continua funcionando sozinho — `pnpm --filter @jarvis/desktop test:run src/main/migrations/__tests__/llm-config.test.ts` ainda exits 0 (pure function não depende de accessors)
  </acceptance_criteria>

  <done>
    `store.ts` reduzido em ~120 linhas: schema interface sem 6 entries LLM + 10 accessor funções removidas. Default export `store` intacto. Migration runner (Task 2) opera via `store.get(key)` direto. `typecheck` full do workspace é validado em Plan 03 Task final (espera-se quebra residual em `ipc/settings.ts` até Plan 03 stripar consumers).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron-store JSON → `.env` file | Migração lê valores plaintext do JSON e escreve em arquivo plaintext novo; ambos no diretório `userData` ou monorepo (já controlados pelo usuário) |
| `process.env` ← `.env` file | `process.loadEnvFile` lê `.env` modificado pela migração; spawn do backend-ts herda |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | API keys em `.env` plaintext | accept | Design decision: `.env` (`~600`) é mais privado que `electron-store/config.json` (`644`). Documentado em CONTEXT.md "Privacy > UX rica". |
| T-70-02 | Tampering | Corrupção do `.env` em crash mid-write | mitigate | Pattern 2 — atomic write via temp file (`${envPath}.tmp-${process.pid}`) + `fs.renameSync`. POSIX rename atomic same-FS; Windows best-effort via MoveFileEx. Runner em `llm-config-runner.ts`. |
| T-70-03 | Information Disclosure | `.env.tmp` órfão se rename falhar deixa keys plaintext em arquivo sem proteção | mitigate | Best-effort: wrap rename em try/catch; em falha, tentar `unlinkSync(tmpPath)` e logar erro. Aceitar perda da migração nesta run (D-03 idempotência roda na próxima). Patch a aplicar no runner. |
| T-70-04 | Tampering | electron-store `delete` parcial deixa keys órfãs (3 de 6 deletadas) | mitigate | Pattern 3 — batch atomic delete via `store.store = next`. 1 write atômico em vez de 6. Implementado no runner. |
</threat_model>

<verification>
- Pure function 9 cenários passam isoladamente (`pnpm --filter @jarvis/desktop test:run src/main/migrations/`).
- Hook em `index.ts` aparece ANTES de `process.loadEnvFile(envPath)` (ordering grep).
- `store.ts` perde 6 schema entries + 10 accessors via grep negativos.
- Workspace typecheck rodará no Plan 03 final task (após consumers de accessors stripped).
</verification>

<success_criteria>
1. SIMP-03 implementada — primeira boot com store legado executa `migrateLlmConfigToEnv`, escreve `.env`, faz batch cleanup do store. Smoke manual: `cat ~/.config/jarvis-desktop/config.json` antes/depois mostra remoção das 6 keys.
2. SIMP-04 parcial — `store.ts` não expõe mais accessors LLM; consumers (settings.ts) serão estripados em Plan 03.
3. Idempotência D-03 verificada — segunda boot é no-op (`migratedKeys=[]`).
4. D-02 .env-wins verificado — teste #3 e #9 do test matrix.
5. Atomic write verificado — runner usa `tmpPath + renameSync` (grep positivo).
</success_criteria>

<output>
After completion, create `.planning/phases/70-llm-config-migration/70-01-SUMMARY.md`.
</output>
