---
phase: 48-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - apps/desktop/src/renderer/src/components/ui/button.tsx
  - apps/desktop/src/renderer/src/components/ui/input.tsx
  - apps/desktop/src/renderer/src/components/ui/select.tsx
  - apps/desktop/src/renderer/src/components/ui/slider.tsx
  - apps/desktop/src/renderer/src/components/ui/label.tsx
autonomous: true
requirements: [REDESIGN-03]

must_haves:
  truths:
    - "Button renders with primary/secondary/ghost variants and md size by default; primary uses cyan-500 bg and shows accent focus ring on :focus-visible"
    - "Input renders with default/error variants and shows accent ring on focus, destructive ring on error+focus"
    - "Select trigger looks like Input; opens a slate-900 popover panel with check icon on selected item"
    - "Slider track uses surface token, range uses accent (cyan-500), thumb has 20px circle with focus-visible ring — visually replicates the legacy VAD slider"
    - "Label is a Radix-based <label> with text-sm font-medium fg-muted"
    - "Every primitive renders only token-derived colors (passes hardcoded-hex grep — no bg-gray, text-white, raw #hex, or rgba color literals except documented exceptions)"
  artifacts:
    - path: "apps/desktop/src/renderer/src/components/ui/button.tsx"
      provides: "Button primitive with variants (primary, secondary, ghost, destructive) + sizes (sm, md, lg)"
      exports: ["Button", "buttonVariants"]
    - path: "apps/desktop/src/renderer/src/components/ui/input.tsx"
      provides: "Input primitive with default/error variants"
      exports: ["Input"]
    - path: "apps/desktop/src/renderer/src/components/ui/select.tsx"
      provides: "Radix Select wrapper (Trigger, Content, Item, Value, etc.)"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue"]
    - path: "apps/desktop/src/renderer/src/components/ui/slider.tsx"
      provides: "Radix Slider wrapper with VAD-compatible visual contract"
      exports: ["Slider"]
    - path: "apps/desktop/src/renderer/src/components/ui/label.tsx"
      provides: "Radix Label wrapper"
      exports: ["Label"]
  key_links:
    - from: "primitives"
      to: "lib/cn.ts"
      via: "import { cn } from"
      pattern: "from ['\"].*lib/cn['\"]"
    - from: "primitives"
      to: "Tailwind tokens (bg-surface, text-fg-muted, ring-accent-ring, etc.)"
      via: "className using token-derived utilities"
      pattern: "bg-surface|text-fg|ring-accent|border-border"
---

<objective>
Install and customize the five shadcn-based primitives that compose the bulk of the Settings UI: Button, Input, Select, Slider, Label. Each primitive's variants and states match UI-SPEC §"Component Inventory" exactly.

Purpose: Phase 49 needs `<Button variant="primary">Save</Button>`, `<Slider>` (replacing VAD `<input type=range>`), `<Select>` (replacing Whisper `<select>`), and `<Input>` (TTS API key) — all with the design contract baked in, not configured at call sites.
Output: Five tsx files under `components/ui/` consuming Tailwind v4 tokens from Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-design-system-foundation/48-UI-SPEC.md
@apps/desktop/components.json
@apps/desktop/src/renderer/src/lib/cn.ts
@apps/desktop/src/renderer/src/styles/globals.css

<interfaces>
Plan 01 provides:
- `cn()` from `apps/desktop/src/renderer/src/lib/cn.ts`
- Tokens via Tailwind v4 @theme: `bg-bg`, `bg-surface`, `bg-surface-hover`, `border-border`, `border-border-hover`, `bg-accent`, `text-accent`, `bg-accent-soft`, `ring-accent-ring`, `bg-destructive`, `bg-success`, `text-fg`, `text-fg-muted`, `text-fg-subtle`, `text-fg-disabled`, `rounded-sm/md/lg/xl/full`, `shadow-xs/sm/md`, `shadow-glow-accent`
- Radix peers installed: `@radix-ui/react-label`, `@radix-ui/react-select`, `@radix-ui/react-slider`, `@radix-ui/react-slot`
- `class-variance-authority` and `lucide-react` available

Existing legacy VAD slider visual (replicate in Slider primitive):
```css
/* From Phase 40 — REMOVED in Plan 01, semantics MUST be preserved */
thumb: 20px circle, bg cyan-500, box-shadow 0 0 8px rgba(6,182,212,0.4)
thumb hover: box-shadow 0 0 12px rgba(6,182,212,0.6)
focus: outline 2px solid rgba(6,182,212,0.5), outline-offset 2px
```

Import path style for primitives (used inside the package):
- `import { cn } from '../../lib/cn'` (relative — Vite alias `@/` may not be set up; verify before assuming `@/` works)
- Verify by reading `apps/desktop/electron.vite.config.ts` (or vite.config) for `resolve.alias` BEFORE writing imports. If no `@/` alias exists, use relative paths.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install shadcn Button + Input + Label, customize variants per UI-SPEC</name>
  <read_first>
    - apps/desktop/components.json
    - apps/desktop/src/renderer/src/lib/cn.ts
    - .planning/phases/48-design-system-foundation/48-UI-SPEC.md §"1. Button", §"2. Input", §"5. Label"
    - apps/desktop/electron.vite.config.ts (or similar — to confirm whether `@/` alias is set; if not, use relative imports)
  </read_first>
  <files>
    - apps/desktop/src/renderer/src/components/ui/button.tsx
    - apps/desktop/src/renderer/src/components/ui/input.tsx
    - apps/desktop/src/renderer/src/components/ui/label.tsx
  </files>
  <action>
    1. Run `cd apps/desktop && pnpm dlx shadcn@latest add button input label --yes` to scaffold the three files into `src/renderer/src/components/ui/`. If the CLI prompts about overwrites, accept. If it complains about Tailwind v4 vs v3 detection, fall back to writing the files manually (template below).

       **File naming convention:** shadcn CLI defaults to lowercase filenames (`button.tsx`, `input.tsx`, `select.tsx`, `slider.tsx`, `label.tsx`). KEEP this convention — do not rename to PascalCase. The Plan 03 barrel `index.ts` re-exports the symbols (`Button`, `Input`, etc.) so consumers still import PascalCase identifiers per UI-SPEC.

    2. **Customize `button.tsx`** to match UI-SPEC §"1. Button" exactly. Use `cva` for variants. Variants:
       - `primary` (default): `bg-accent text-fg shadow-xs hover:bg-cyan-400 active:bg-cyan-600 disabled:bg-accent/40 disabled:text-fg/60 disabled:cursor-not-allowed`
       - `secondary`: `bg-surface text-fg-muted border border-border hover:bg-surface-hover hover:border-border-hover hover:text-fg active:border-white/20 disabled:bg-surface/50 disabled:text-fg-disabled disabled:border-white/5 disabled:cursor-not-allowed`
       - `ghost`: `bg-transparent text-fg-muted hover:bg-white/5 hover:text-fg active:bg-white/10 disabled:text-fg-disabled disabled:cursor-not-allowed`
       - `destructive`: `bg-destructive text-fg hover:bg-rose-400 disabled:bg-destructive/40 disabled:text-fg/60`

       Sizes (height + padding from UI-SPEC §"1. Button"):
       - `sm`: `h-7 px-md text-sm font-medium` (28px)
       - `md` (default): `h-9 px-base text-sm font-medium` (36px)
       - `lg`: `h-11 px-lg text-sm font-medium` (44px)

       Base classes for ALL variants (focus-visible per UI-SPEC §"Interaction Contracts"):
       `inline-flex items-center justify-center gap-sm rounded-md transition-colors duration-base ease-standard outline-none focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-bg disabled:pointer-events-none`

       Default: `variant="primary"` `size="md"`. Use `@radix-ui/react-slot` for `asChild` prop (shadcn standard).

       Export both `Button` (FC) and `buttonVariants` (cva instance) so other primitives can compose.

    3. **Customize `input.tsx`** to match UI-SPEC §"2. Input":
       - Variants: `default` and `error`. Sizes: `sm` (h-8) and `md` (h-9, default).
       - Base: `flex w-full rounded-md bg-surface border border-border text-fg placeholder:text-fg-subtle text-sm px-md py-sm transition-colors duration-base ease-standard outline-none hover:bg-surface-hover hover:border-border-hover focus-visible:bg-surface-hover focus-visible:border-accent-ring focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-0 disabled:bg-surface/50 disabled:border-white/5 disabled:text-fg-disabled disabled:cursor-not-allowed`
       - Error variant adds: `aria-[invalid=true]:border-destructive focus-visible:aria-[invalid=true]:ring-[rgba(244,63,94,0.30)] focus-visible:aria-[invalid=true]:border-destructive`
       - Forward ref. Default `type="text"`.

    4. **Customize `label.tsx`** to match UI-SPEC §"5. Label":
       - Wrap `@radix-ui/react-label`'s Root.
       - Classes: `text-sm font-medium text-fg-muted peer-disabled:cursor-not-allowed peer-disabled:text-fg-disabled`
       - Forward ref.

    5. **NEVER use `text-white`, `bg-gray-800`, `bg-black/95` or any other hardcoded color** — every color must come from the @theme tokens. Verify with grep before commit.

    6. CLAUDE.md commit convention applies: when creating the commit (orchestrator handles), use `✨ feat(48-02): add Button/Input/Label primitives`.
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; node -e "const fs=require('fs'); const files=['src/renderer/src/components/ui/button.tsx','src/renderer/src/components/ui/input.tsx','src/renderer/src/components/ui/label.tsx']; files.forEach(f=>{ if(!fs.existsSync(f)){console.error('MISSING',f);process.exit(1);} const s=fs.readFileSync(f,'utf8'); if(!s.includes('cn(')){console.error(f,'no cn() use');process.exit(1);} const banned=[/#[0-9a-fA-F]{3,8}/,/rgba?\(\s*\d+\s*,\s*\d+\s*,\s*\d+/,/bg-gray-\d+/,/text-white/,/bg-black/,/bg-slate-\d+/]; banned.forEach(b=>{ if((b instanceof RegExp ? b.test(s) : s.includes(b))){console.error(f,'contains banned color pattern',String(b));process.exit(1);} }); }); const btn=fs.readFileSync(files[0],'utf8'); ['primary','secondary','ghost','destructive','sm','md','lg','focus-visible:ring','accent-ring'].forEach(t=>{ if(!btn.includes(t)){console.error('button missing',t);process.exit(1);} }); console.log('Task1 OK');" &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/button.tsx` exists, exports `Button` and `buttonVariants`, contains strings: `"primary"`, `"secondary"`, `"ghost"`, `"destructive"`, `"sm"`, `"md"`, `"lg"`, `focus-visible:ring`, `accent-ring`
    - `components/ui/input.tsx` exists, exports `Input`, contains `"error"` variant, references `border-accent-ring` or `ring-accent-ring`
    - `components/ui/label.tsx` exists, exports `Label`, imports `@radix-ui/react-label`
    - NONE of the three files contains `bg-gray-`, `text-white`, `bg-black`, `#FFFFFF`, or `rgba(6, 182, 212` (must use tokens via Tailwind utilities like `text-fg`, `bg-accent`)
    - Each file imports `cn` from the lib/cn helper
    - `pnpm exec tsc --noEmit -p tsconfig.json` shows no type errors in these three files
  </acceptance_criteria>
  <done>Button/Input/Label match UI-SPEC state matrices, all colors via tokens, TypeScript compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install shadcn Select + Slider, customize per UI-SPEC (preserve VAD slider visual)</name>
  <read_first>
    - .planning/phases/48-design-system-foundation/48-UI-SPEC.md §"3. Select", §"4. Slider"
    - apps/desktop/src/renderer/src/styles/globals.css (confirm Plan 01 removed legacy slider CSS)
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx lines 175-192 (existing VAD slider markup — Phase 49 will swap it; we must preserve its aria-valuetext semantics)
  </read_first>
  <files>
    - apps/desktop/src/renderer/src/components/ui/select.tsx
    - apps/desktop/src/renderer/src/components/ui/slider.tsx
  </files>
  <action>
    1. Run `cd apps/desktop && pnpm dlx shadcn@latest add select slider --yes` to scaffold both files.

    2. **Customize `select.tsx`** to match UI-SPEC §"3. Select":
       - Trigger styling: same visual contract as Input (`flex h-9 w-full items-center justify-between rounded-md bg-surface border border-border text-sm text-fg px-md py-sm transition-colors duration-base outline-none hover:bg-surface-hover hover:border-border-hover focus-visible:border-accent-ring focus-visible:ring-2 focus-visible:ring-accent-ring data-[state=open]:border-accent-ring`)
       - Right-aligned chevron: `<ChevronDown />` from `lucide-react`, size 16, `text-fg-subtle`
       - Content panel: `bg-slate-900 border border-white/10 rounded-lg shadow-md max-h-80 overflow-y-auto` (note: `bg-slate-900` is the ONLY exception to the no-hardcoded-color rule because UI-SPEC §"3. Select" Content panel says "full opacity, not translucent" — this differs from the `--color-surface` token which is 70% alpha. Document with a comment: `/* UI-SPEC §3 — Content panel uses full-opacity slate-900 for readability, not the translucent --color-surface token */`)
       - Item base: `relative flex items-center px-md py-sm text-sm text-fg-muted rounded-sm cursor-default outline-none data-[highlighted]:bg-surface-hover data-[highlighted]:text-fg`
       - Selected item indicator: `<Check />` from `lucide-react`, size 14, `text-accent`, right-aligned via `<SelectPrimitive.ItemIndicator>`
       - Export the standard shadcn surface: `Select`, `SelectTrigger`, `SelectValue`, `SelectContent`, `SelectItem`, `SelectGroup`, `SelectLabel`, `SelectSeparator`.

    3. **Customize `slider.tsx`** to match UI-SPEC §"4. Slider" — this REPLACES the legacy VAD slider visual (D-03):
       - Root: `relative flex w-full touch-none select-none items-center`
       - Track: `relative h-1.5 w-full grow overflow-hidden rounded-full bg-surface` (h-1.5 = 6px per UI-SPEC)
       - Range: `absolute h-full bg-accent rounded-full`
       - Thumb: `block h-5 w-5 rounded-full bg-accent border-2 border-white/20 shadow-[0_0_8px_rgba(6,182,212,0.4)] transition-shadow duration-base outline-none hover:shadow-[0_0_12px_rgba(6,182,212,0.6)] focus-visible:ring-2 focus-visible:ring-accent-ring focus-visible:ring-offset-2 focus-visible:ring-offset-bg active:scale-105 active:shadow-[0_0_16px_rgba(6,182,212,0.7)] disabled:bg-surface-hover disabled:shadow-none cursor-pointer`
         - The `rgba(6,182,212,...)` arbitrary values are PRESERVED from the legacy VAD slider visual contract — UI-SPEC §"4. Slider" explicitly says "shadow-[0 0 8px rgba(6,182,212,0.4)] (continuity with current VAD slider)". This is the documented exception to the no-hardcoded-color rule.
       - Coarse-pointer hit target: wrap thumb with media-query CSS using Tailwind arbitrary variant `[@media(pointer:coarse)]:before:content-[''] [@media(pointer:coarse)]:before:absolute [@media(pointer:coarse)]:before:inset-[-12px]` (44px hit zone — UI-SPEC §"4. Slider" hit-target rule).
       - Forward ref. Use `@radix-ui/react-slider`. Default exports `Slider` only.

    4. Run `pnpm exec tsc --noEmit -p tsconfig.json` and `pnpm build` to confirm no type or build errors.
  </action>
  <verify>
    <automated>cd apps/desktop &amp;&amp; node -e "const fs=require('fs'); const sel=fs.readFileSync('src/renderer/src/components/ui/select.tsx','utf8'); const sld=fs.readFileSync('src/renderer/src/components/ui/slider.tsx','utf8'); ['SelectTrigger','SelectContent','SelectItem','ChevronDown','Check','data-[state=open]:border-accent-ring','bg-slate-900'].forEach(t=>{if(!sel.includes(t)){console.error('select missing',t);process.exit(1);}}); ['bg-accent','rounded-full','rgba(6,182,212,0.4)','focus-visible:ring-accent-ring','active:scale-105','@radix-ui/react-slider'].forEach(t=>{if(!sld.includes(t)){console.error('slider missing',t);process.exit(1);}}); // Allowlist: select.tsx may contain bg-slate-900 ONLY (full-opacity panel exception); slider.tsx may contain rgba(6,182,212 ONLY (legacy VAD glow exception)
const banSel=[/bg-gray-\d+/,/text-white/,/bg-black/,/#[0-9a-fA-F]{3,8}/,/bg-slate-(?!900)\d+/,/rgba?\(\s*\d+\s*,/];
const banSld=[/bg-gray-\d+/,/text-white/,/bg-black/,/bg-slate-\d+/,/#[0-9a-fA-F]{3,8}/];
banSel.forEach(b=>{if(b.test(sel)){console.error('select.tsx banned pattern',String(b));process.exit(1);}});
banSld.forEach(b=>{if(b.test(sld)){console.error('slider.tsx banned pattern',String(b));process.exit(1);}});
// Slider may contain rgba(6,182,212 — verify it is the ONLY rgba present
const sldRgbas=(sld.match(/rgba?\([^)]*\)/g)||[]).filter(r=>!r.startsWith('rgba(6,182,212') && !r.startsWith('rgba(6, 182, 212'));
if(sldRgbas.length){console.error('slider.tsx has non-allowlisted rgba:',sldRgbas);process.exit(1);} console.log('Task2 OK');" &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/select.tsx` exports `Select`, `SelectTrigger`, `SelectContent`, `SelectItem`, `SelectValue`
    - `select.tsx` contains `ChevronDown`, `Check` (from `lucide-react`), `data-[state=open]:border-accent-ring`, `bg-slate-900` (documented full-opacity exception)
    - `components/ui/slider.tsx` imports `@radix-ui/react-slider`
    - `slider.tsx` contains `bg-accent` for Range, `rgba(6,182,212,0.4)` for thumb shadow (legacy VAD continuity), `focus-visible:ring-accent-ring`, `active:scale-105`
    - Neither file contains `bg-gray-` or `text-white` (must use tokens)
    - `pnpm exec tsc --noEmit -p tsconfig.json` passes
    - `pnpm build` succeeds
  </acceptance_criteria>
  <done>Select + Slider primitives ready; legacy VAD visual preserved in Slider; tokens consumed end-to-end.</done>
</task>

</tasks>

<verification>
- All 5 primitive files exist under `apps/desktop/src/renderer/src/components/ui/`
- `pnpm exec tsc --noEmit -p tsconfig.json` passes
- `pnpm build` succeeds
- Grep confirms no banned hardcoded colors (`bg-gray-`, `text-white`, `bg-black`, `#FFFFFF`) except documented exceptions (`bg-slate-900` in Select Content, `rgba(6,182,212,...)` in Slider thumb shadows)
</verification>

<success_criteria>
1. Button has 4 variants × 3 sizes, all states match UI-SPEC §"1. Button" matrix
2. Input has default + error variants with focus and invalid states
3. Select trigger looks like Input; content panel is opaque slate-900 with check indicator on selected
4. Slider visually replicates legacy VAD slider (cyan thumb + glow shadows)
5. Label uses Radix and applies fg-muted styling
6. Every primitive consumes design tokens from Plan 01's @theme block
</success_criteria>

<output>
After completion, create `.planning/phases/48-design-system-foundation/48-02-SUMMARY.md` with:
- File-by-file variant inventory (Button: 4 variants × 3 sizes, etc.)
- Documented exceptions (bg-slate-900 in Select, rgba shadow in Slider)
- TypeScript + build status
- Confirmation barrel index will be added by Plan 03
</output>
