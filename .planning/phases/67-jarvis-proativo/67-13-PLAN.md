---
phase: 67-jarvis-proativo
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx
  - apps/desktop/src/renderer/src/App.tsx
autonomous: true
requirements: [PROACT-02]
gap_closure: true

must_haves:
  truths:
    - "ProactiveEventBubble e renderizado no DOM quando um proactive:event chega via IPC"
    - "Eventos proativos aparecem como um painel/strip visivel sobre o Orb no widget"
    - "Bubbles podem ser dispensadas individualmente via botao X"
    - "ChatContext.messages com role='proactive' sao consumidos por um componente de lista"
  artifacts:
    - path: "apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx"
      provides: "Componente que itera messages e renderiza ProactiveEventBubble para role=proactive"
      exports: ["ProactiveMessageList"]
      contains: "ProactiveEventBubble"
    - path: "apps/desktop/src/renderer/src/App.tsx"
      provides: "App.tsx renderizando ProactiveMessageList acima do Orb"
      contains: "ProactiveMessageList"
  key_links:
    - from: "apps/desktop/src/renderer/src/App.tsx"
      to: "apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx"
      via: "JSX <ProactiveMessageList />"
      pattern: "ProactiveMessageList"
    - from: "apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx"
      to: "apps/desktop/src/renderer/src/chat/ChatContext.tsx"
      via: "useChat().messages.filter(role=proactive)"
      pattern: "useChat"
    - from: "apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx"
      to: "apps/desktop/src/renderer/src/chat/ProactiveEventBubble.tsx"
      via: "JSX <ProactiveEventBubble event={msg.proactiveEvent} />"
      pattern: "ProactiveEventBubble"
---

<objective>
Criar ProactiveMessageList e integrar em App.tsx para que bubbles proativas sejam renderizadas no widget.

Purpose: ProactiveEventBubble.tsx existe e esta correto, ChatContext armazena mensagens com role='proactive', mas nenhum componente as renderiza. App.tsx so exibe <Orb/>. Este plano cria um componente minimo ProactiveMessageList que consome useChat().messages, filtra por role='proactive', e renderiza cada um como ProactiveEventBubble num painel/strip posicionado acima do Orb. Escopo intencionalmenteminimo — sem scroll history completo, sem animacoes, sem paginacao. Apenas visibilidade das notificacoes proativas recentes.

Output: ProactiveMessageList.tsx + integracao no App.tsx; eventos proativos visiveis no widget ao chegar via IPC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-jarvis-proativo/67-09-SUMMARY.md
@.planning/phases/67-jarvis-proativo/67-UI-SPEC.md

<interfaces>
<!-- ChatContext API (apps/desktop/src/renderer/src/chat/ChatContext.tsx): -->
<!--   export type ChatRole = 'human' | 'agent' | 'proactive'; -->
<!--   export interface ChatMessage { -->
<!--     id: string; -->
<!--     role: ChatRole; -->
<!--     text: string; -->
<!--     taskId?: string; -->
<!--     proactiveEvent?: ProactiveEvent;  // populated when role === 'proactive' -->
<!--   } -->
<!--   export function useChat(): ChatContextValue -->
<!--     ChatContextValue.messages: ChatMessage[]  // todos os tipos de mensagem -->

<!-- ProactiveEventBubble (apps/desktop/src/renderer/src/chat/ProactiveEventBubble.tsx): -->
<!--   interface ProactiveEventBubbleProps { -->
<!--     event: ProactiveEvent; -->
<!--     onDismiss?: () => void;   // callback para remover o bubble da lista -->
<!--     onSnooze?: (minutes: number) => void; // reservado, nao usado ainda -->
<!--   } -->
<!--   export function ProactiveEventBubble({ event, onDismiss }: ProactiveEventBubbleProps) -->
<!--   Renderiza: role="article", className="flex gap-sm p-base rounded-lg bg-surface border-l-4 {stripeColor}" -->

<!-- App.tsx atual (apps/desktop/src/renderer/src/App.tsx): -->
<!--   function AppContent() — componente interno com acesso ao ChatProvider -->
<!--   return ( -->
<!--     <div className="h-screen w-screen flex items-center justify-center" ...> -->
<!--       {wakeFeaturesEnabled && <WakeWordFeatures ... />} -->
<!--       <div className="app-container"> -->
<!--         <div style={{ WebkitAppRegion: 'drag' }} ...> -->
<!--           <Orb /> -->
<!--         </div> -->
<!--       </div> -->
<!--       {toast && <Toast ... />} -->
<!--       {pendingAction && <ActionConfirmationToast ... />} -->
<!--     </div> -->
<!--   ); -->

<!-- ProactiveEvent union (apps/desktop/src/shared/ipc-types.ts): -->
<!--   type ProactiveEvent = ReminderEvent | FolderEvent | DailySummaryEvent -->
<!--   interface ReminderEvent { kind: 'reminder'; message: string; dueAt: number; ... } -->
<!--   interface FolderEvent { kind: 'folder_event'; files: [{name,path}]; folderPath: string } -->
<!--   interface DailySummaryEvent { kind: 'daily_summary'; text: string; generatedAt: number } -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar ProactiveMessageList.tsx</name>
  <files>apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx, apps/desktop/src/renderer/src/chat/__tests__/ProactiveMessageList.test.tsx</files>
  <read_first>
    - apps/desktop/src/renderer/src/chat/ProactiveEventBubble.tsx (componente que sera usado internamente)
    - apps/desktop/src/renderer/src/chat/ChatContext.tsx (linhas 1-80 — ChatMessage type, useChat hook)
    - apps/desktop/src/renderer/src/settings/__tests__/ProactiveSection.test.tsx (padrao de testes desta fase para copiar setup de vitest/happy-dom)
  </read_first>
  <behavior>
    - Test 1: renderiza null quando messages esta vazio (nao monta DOM)
    - Test 2: renderiza null quando ha messages mas nenhuma com role='proactive'
    - Test 3: renderiza 1 ProactiveEventBubble quando ha 1 message com role='proactive' e proactiveEvent preenchido
    - Test 4: renderiza 2 ProactiveEventBubble quando ha 2 messages proativas (ambas visiveis)
    - Test 5: chamada a onDismiss de um bubble remove-o da lista (nao chama funcao externa — gerencia state interno)
  </behavior>
  <action>
Criar apps/desktop/src/renderer/src/chat/__tests__/ProactiveMessageList.test.tsx com 5 testes (RED phase). Usar @vitest-environment happy-dom. Mock do useChat: vi.mock('../ChatContext', ...).

Criar apps/desktop/src/renderer/src/chat/ProactiveMessageList.tsx (GREEN phase):

```typescript
/**
 * ProactiveMessageList — Phase 67 Gap 3 (PROACT-02)
 *
 * Renderiza bubbles proativas a partir do ChatContext.messages.
 * Filtra role='proactive', limita aos 5 mais recentes para evitar overflow visual.
 * Dismiss e gerenciado por estado interno (ids dispensados) — nao remove do ChatContext
 * pois ChatContext nao tem removeMessage; o evento permanece na historia para eventual
 * MessageList completo.
 *
 * Posicionamento: absoluto no widget, acima do Orb, max-h limitado.
 */
import { useState } from 'react';
import { useChat } from './ChatContext';
import { ProactiveEventBubble } from './ProactiveEventBubble';

const MAX_VISIBLE = 5;

export function ProactiveMessageList() {
  const { messages } = useChat();
  const [dismissedIds, setDismissedIds] = useState<Set<string>>(new Set());

  const proactiveMessages = messages
    .filter((m) => m.role === 'proactive' && m.proactiveEvent && !dismissedIds.has(m.id))
    .slice(-MAX_VISIBLE); // Keep only the 5 most recent

  if (proactiveMessages.length === 0) return null;

  const handleDismiss = (id: string) => {
    setDismissedIds((prev) => new Set([...prev, id]));
  };

  return (
    <div
      className="flex flex-col gap-xs w-full max-h-64 overflow-y-auto"
      aria-label="Notificacoes proativas recentes"
      aria-live="polite"
    >
      {proactiveMessages.map((msg) => (
        <ProactiveEventBubble
          key={msg.id}
          event={msg.proactiveEvent!}
          onDismiss={() => handleDismiss(msg.id)}
        />
      ))}
    </div>
  );
}
```

Commits:
1. RED: "✅ test(67-13): add failing tests for ProactiveMessageList"
2. GREEN: "✨ feat(67-13): criar ProactiveMessageList — renderiza bubbles proativas do ChatContext"
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run ProactiveMessageList 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 5/5 testes passando no suite ProactiveMessageList
    - ProactiveMessageList exportada como named export
    - Componente importa useChat e ProactiveEventBubble (grep confirma)
    - Filtra por role='proactive' (grep "role.*proactive\|proactive.*role" no arquivo)
  </acceptance_criteria>
  <done>ProactiveMessageList.tsx existe, exporta o componente, 5 testes passando (null quando vazio, filtra role, renderiza bubbles, dismiss funcional).</done>
</task>

<task type="auto">
  <name>Task 2: Integrar ProactiveMessageList no App.tsx</name>
  <files>apps/desktop/src/renderer/src/App.tsx</files>
  <read_first>
    - apps/desktop/src/renderer/src/App.tsx (completo — ler antes de editar, especialmente o return de AppContent linhas 296-335)
  </read_first>
  <action>
No arquivo apps/desktop/src/renderer/src/App.tsx:

1. Adicionar import no topo (junto com os outros imports do chat):
```typescript
import { ProactiveMessageList } from './chat/ProactiveMessageList';
```

2. No return de AppContent, adicionar <ProactiveMessageList /> ANTES da div app-container (entre WakeWordFeatures e o Orb), dentro de um wrapper posicionado para aparecer como strip/painel acima do orb:

```typescript
return (
  <div
    className="h-screen w-screen flex items-center justify-center"
    style={{ background: 'transparent' } as React.CSSProperties}
    onMouseEnter={() => window.jarvis.setIgnoreMouseEvents?.(false)}
    onMouseLeave={() => window.jarvis.setIgnoreMouseEvents?.(true)}
  >
    {wakeFeaturesEnabled && (
      <WakeWordFeatures multiTurnEnabled={multiTurnEnabled} windowMs={windowMs} />
    )}
    {/* Phase 67 Gap 3 (PROACT-02): proactive event bubbles strip above the Orb */}
    <div className="absolute top-2 left-2 right-2 z-10">
      <ProactiveMessageList />
    </div>
    <div className="app-container">
      <div
        style={{ WebkitAppRegion: 'drag', cursor: 'grab' } as React.CSSProperties}
        onMouseDown={(e) => { (e.currentTarget as HTMLElement).style.cursor = 'grabbing'; }}
        onMouseUp={(e) => { (e.currentTarget as HTMLElement).style.cursor = 'grab'; }}
      >
        <Orb />
      </div>
    </div>
    {toast && (
      <Toast ... />
    )}
    {pendingAction && (
      <ActionConfirmationToast ... />
    )}
  </div>
);
```

3. Remover o TODO comment sobre MessageList (linhas 282-284), pois o gap foi fechado.

Commit atomico: "✨ feat(67-13): integrar ProactiveMessageList no App.tsx — PROACT-02 fechado"
  </action>
  <verify>
    <automated>grep -n "ProactiveMessageList" /root/jarvis/apps/desktop/src/renderer/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep retorna ao menos 2 linhas: 1 import + 1 uso no JSX
    - grep "Phase 67 TODO" apps/desktop/src/renderer/src/App.tsx retorna 0 (TODO removido)
    - grep "ProactiveMessageList" App.tsx mostra uso dentro do return do JSX
    - cd apps/desktop && npx tsc --noEmit retorna 0 erros
  </acceptance_criteria>
  <done>App.tsx importa e renderiza <ProactiveMessageList /> num div absolute posicionado acima do Orb. TODO comment removido. TypeScript sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC→ChatContext→ProactiveMessageList | Dados de ProactiveEvent vem do main process via IPC; renderizados via JSX (auto-escape) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-13-01 | Information Disclosure | ProactiveEventBubble content | accept | T-67-05 (Phase 67-09) ja mitiga XSS: JSX React auto-escapa strings, sem dangerouslySetInnerHTML. ProactiveMessageList herda a mesma protecao. |
| T-67-13-02 | Denial of Service | MAX_VISIBLE limit | mitigate | Limitar a 5 mensagens mais recentes via .slice(-MAX_VISIBLE) — evita overflow visual com muitos eventos acumulados |
</threat_model>

<verification>
Apos execucao:

```bash
# Testes unitarios
cd apps/desktop && npx vitest run ProactiveMessageList
# Deve mostrar: 5/5 PASS

# Verifica integracao no App.tsx
grep "ProactiveMessageList" apps/desktop/src/renderer/src/App.tsx
# Deve retornar: import linha + JSX linha

# Verifica TODO removido
grep "Phase 67 TODO" apps/desktop/src/renderer/src/App.tsx
# Deve retornar vazio

# Type-check completo
cd apps/desktop && npx tsc --noEmit 2>&1 | grep -i error | head -10
# Deve retornar vazio
```
</verification>

<success_criteria>
- ProactiveMessageList.tsx criado e exportado
- 5 testes passando (null vazio, filtra role, renderiza bubbles, dismiss funcional)
- App.tsx renderiza <ProactiveMessageList /> no JSX (strip acima do Orb)
- TODO comment de Phase 67 removido
- TypeScript sem erros
- PROACT-02 fechado: bubble proativo visivel no widget
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/67-jarvis-proativo/67-13-SUMMARY.md`
</output>
