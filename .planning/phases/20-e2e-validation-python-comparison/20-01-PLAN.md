---
phase: 20-e2e-validation-python-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/src/middleware/backendRouter.ts
  - apps/gateway/src/routes/chat.ts
  - apps/gateway/src/app.ts
autonomous: true
requirements:
  - VAL-07
must_haves:
  truths:
    - "Request com header 'X-Backend-Version: ts' chega ao backend TypeScript (port 8001)"
    - "Request sem o header chega ao backend Python (port 8000) — comportamento padrão inalterado"
    - "Os três endpoints /chat, /chat/stream e /chat/audio respeitam o feature flag"
  artifacts:
    - path: "apps/gateway/src/middleware/backendRouter.ts"
      provides: "Função resolveUpstreamUrl(req) que lê o header X-Backend-Version"
      exports: ["resolveUpstreamUrl"]
    - path: "apps/gateway/src/routes/chat.ts"
      provides: "Rotas chat modificadas usando resolveUpstreamUrl em vez de config.fastapiUrl"
      contains: "resolveUpstreamUrl"
    - path: "apps/gateway/src/app.ts"
      provides: "App Express com import de backendRouter (se necessário)"
  key_links:
    - from: "apps/gateway/src/routes/chat.ts"
      to: "apps/gateway/src/middleware/backendRouter.ts"
      via: "import { resolveUpstreamUrl }"
      pattern: "resolveUpstreamUrl"
    - from: "apps/gateway/src/middleware/backendRouter.ts"
      to: "apps/gateway/src/config.ts"
      via: "import { config }"
      pattern: "config\\.backendTsUrl|config\\.fastapiUrl"
---

<objective>
Implementar o gateway feature flag que roteia requests para o backend TypeScript quando o header `X-Backend-Version: ts` estiver presente.

Purpose: Permite validação controlada do backend TypeScript sem alterar o comportamento padrão (Python continua sendo o default). Necessário para que o E2E script possa apontar tráfego para o TS backend via gateway.

Output: Middleware `backendRouter.ts` com função `resolveUpstreamUrl`, e as três rotas de chat modificadas para usá-la.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-e2e-validation-python-comparison/20-CONTEXT.md

<interfaces>
<!-- Contratos relevantes. Executor usa diretamente sem explorar o codebase. -->

De apps/gateway/src/config.ts:
```typescript
export const config = {
  fastapiUrl: process.env.FASTAPI_URL ?? "http://localhost:8000",
  backendTsUrl: process.env.BACKEND_TS_URL ?? "http://localhost:8001",
  gatewayPort: parseInt(process.env.GATEWAY_PORT ?? "3000", 10),
  apiKey: process.env.JARVIS_API_KEY as string | undefined,
} as const;
```

De apps/gateway/src/routes/chat.ts (estado atual — 3 rotas):
- `chatRouter.post("/chat", ...)` — usa `${config.fastapiUrl}/chat` (linha 19)
- `chatRouter.get("/chat/stream", ...)` — usa `${config.fastapiUrl}/chat/stream?message=...` (linha 67)
- `chatRouter.post("/chat/audio", ...)` — usa `${config.backendTsUrl}/chat/audio` (linha 127)

De apps/gateway/src/app.ts (estado atual):
```typescript
import express from "express";
import { chatRouter } from "./routes/chat.js";
import { healthRouter } from "./routes/health.js";
import { toolCallsRouter } from "./routes/tool-calls.js";
import { errorHandler } from "./middleware/errorHandler.js";

export function createApp() {
  const app = express();
  app.use(express.json());
  app.use("/api", chatRouter);
  app.use("/api", healthRouter);
  app.use("/api", toolCallsRouter);
  app.use(errorHandler);
  return app;
}
```

Middleware existente em apps/gateway/src/middleware/:
- `errorHandler.ts` — já existe
- `validate.ts` — já existe
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar backendRouter.ts com resolveUpstreamUrl</name>
  <files>apps/gateway/src/middleware/backendRouter.ts</files>

  <read_first>
    - apps/gateway/src/config.ts (valores de fastapiUrl e backendTsUrl)
    - apps/gateway/src/middleware/errorHandler.ts (padrão de middleware existente)
  </read_first>

  <action>
Criar o arquivo `apps/gateway/src/middleware/backendRouter.ts` com a função utilitária `resolveUpstreamUrl`:

```typescript
import { type Request } from 'express';
import { config } from '../config.js';

/**
 * Resolve o upstream URL com base no header X-Backend-Version.
 *
 * - Header "X-Backend-Version: ts" → config.backendTsUrl (port 8001)
 * - Ausente ou qualquer outro valor → config.fastapiUrl (port 8000, default)
 *
 * Usado pelos route handlers de chat para implementar o feature flag VAL-07.
 * Não altera o comportamento padrão — Python continua sendo o default.
 */
export function resolveUpstreamUrl(req: Request): string {
  const version = req.headers['x-backend-version'];
  if (version === 'ts') {
    return config.backendTsUrl;
  }
  return config.fastapiUrl;
}
```

Não é necessário adicionar o import em app.ts — a função é importada diretamente pelos route handlers, não registrada como app.use() middleware.
  </action>

  <verify>
    <automated>grep -n "resolveUpstreamUrl\|x-backend-version\|backendTsUrl\|fastapiUrl" /root/jarvis/apps/gateway/src/middleware/backendRouter.ts</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `apps/gateway/src/middleware/backendRouter.ts` existe
    - Contém `export function resolveUpstreamUrl(req: Request): string`
    - Contém `req.headers['x-backend-version']`
    - Contém `config.backendTsUrl` no branch `version === 'ts'`
    - Contém `config.fastapiUrl` como fallback default
    - Importa `config` de `'../config.js'`
    - Importa `type Request` de `'express'`
  </acceptance_criteria>

  <done>backendRouter.ts existe com função resolveUpstreamUrl que lê o header e retorna a URL correta</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar rotas chat.ts para usar resolveUpstreamUrl</name>
  <files>apps/gateway/src/routes/chat.ts</files>

  <read_first>
    - apps/gateway/src/routes/chat.ts (estado ATUAL completo — necessário para saber cada linha a modificar)
    - apps/gateway/src/middleware/backendRouter.ts (função criada na Task 1)
  </read_first>

  <action>
Modificar `apps/gateway/src/routes/chat.ts` para importar `resolveUpstreamUrl` e usá-la nos três handlers:

**1. Adicionar import no topo (após os imports existentes):**
```typescript
import { resolveUpstreamUrl } from '../middleware/backendRouter.js';
```

**2. Modificar POST /chat (linha ~19):**
```typescript
// ANTES:
const upstream = await fetch(`${config.fastapiUrl}/chat`, {
// DEPOIS:
const upstream = await fetch(`${resolveUpstreamUrl(req)}/chat`, {
```

**3. Modificar GET /chat/stream (linha ~67):**
```typescript
// ANTES:
const upstream = await fetch(
  `${config.fastapiUrl}/chat/stream?message=${encodeURIComponent(message)}`,
// DEPOIS:
const upstream = await fetch(
  `${resolveUpstreamUrl(req)}/chat/stream?message=${encodeURIComponent(message)}`,
```

**4. Modificar POST /chat/audio (linha ~127):**
```typescript
// ANTES:
const upstream = await fetch(`${config.backendTsUrl}/chat/audio`, {
// DEPOIS:
const upstream = await fetch(`${resolveUpstreamUrl(req)}/chat/audio`, {
```

**Importante:** A rota `/chat/audio` atualmente usa `config.backendTsUrl` hardcoded (porque o audio endpoint só existe no TS backend por design). Com a mudança, o comportamento padrão passa a ser `fastapiUrl` para audio também, quando o header `X-Backend-Version: ts` estiver ausente. Isso está correto per VAL-07: sem o header, tudo vai para fastapiUrl (Python default). O caller que quiser usar o endpoint de audio do TS deve enviar o header.

Não remover o import de `config` — ainda é usado em `config.apiKey` na mesma rota.
  </action>

  <verify>
    <automated>grep -n "resolveUpstreamUrl\|backendRouter" /root/jarvis/apps/gateway/src/routes/chat.ts</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `apps/gateway/src/routes/chat.ts` contém `import { resolveUpstreamUrl } from '../middleware/backendRouter.js'`
    - As três ocorrências de `config.fastapiUrl` e `config.backendTsUrl` nas chamadas fetch foram substituídas por `resolveUpstreamUrl(req)`
    - O import de `config` permanece (ainda usado para `config.apiKey`)
    - Executar `cd /root/jarvis && pnpm --filter @jarvis/gateway typecheck` ou `pnpm --filter @jarvis/gateway build` sem erros de tipo
    - `grep "config\.fastapiUrl\|config\.backendTsUrl" apps/gateway/src/routes/chat.ts` retorna apenas linhas de comentário, se houver, ou nenhuma linha de código ativo
  </acceptance_criteria>

  <done>Os três handlers de chat usam resolveUpstreamUrl(req) em vez de config.fastapiUrl/backendTsUrl hardcoded. TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente→gateway | Header X-Backend-Version: ts é controlado pelo caller |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | backendRouter.ts | accept | Header apenas roteia entre dois backends internos (ambos localhost). Não há dados sensíveis no roteamento. Uso local, sem auth threat. |
| T-20-02 | Elevation of Privilege | chat.ts header routing | accept | Backend TS está em localhost:8001 — não acessível externamente. O header não concede permissões adicionais, apenas escolhe implementação. |
</threat_model>

<verification>
Após completar as duas tasks, verificar que:
1. `grep -rn "resolveUpstreamUrl" /root/jarvis/apps/gateway/src/` mostra ocorrências em `backendRouter.ts` (definição) e `routes/chat.ts` (3 usos)
2. `cd /root/jarvis && pnpm --filter @jarvis/gateway build` passa sem erros de TypeScript
3. `grep "config\.fastapiUrl\|config\.backendTsUrl" /root/jarvis/apps/gateway/src/routes/chat.ts` — não deve retornar linhas de código ativo (apenas comentários se houver)
</verification>

<success_criteria>
- backendRouter.ts criado com resolveUpstreamUrl exportado
- chat.ts modificado com os 3 handlers usando resolveUpstreamUrl(req)
- Gateway compila sem erros
- Comportamento padrão (sem header) permanece inalterado: vai para fastapiUrl (Python port 8000)
- Com header X-Backend-Version: ts: vai para backendTsUrl (TypeScript port 8001)
</success_criteria>

<output>
Após completar, criar `.planning/phases/20-e2e-validation-python-comparison/20-01-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
