---
phase: 20-e2e-validation-python-comparison
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/scripts/e2e-compare.ts
  - package.json
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03
  - VAL-04
  - VAL-05
  - VAL-06
must_haves:
  truths:
    - "pnpm e2e executa o script e2e-compare.ts com tsx sem erros de módulo"
    - "O script envia 20 mensagens de texto para Python (8000) e TypeScript (8001) e coleta respostas"
    - "Comparação de texto usa cosine similarity com threshold configurável via TEXT_SIM_THRESHOLD (default 0.85)"
    - "Tool calls são comparados via tabela tool_calls no SQLite de cada backend"
    - "SQLite state compara role+content das mensagens (sem timestamps) entre os dois .db files"
    - "ChromaDB embeddings são comparados gerando embeddings locais com embedText() para os mesmos textos"
    - "Performance mede latência de cada request e reporta ratio TS/Python"
    - "Relatório final mostra PASS/FAIL por categoria com exit code 0 se >=90% pass, 1 se <90%"
    - "Voice validation ausente é documentada como known limitation no relatório"
  artifacts:
    - path: "apps/backend-ts/scripts/e2e-compare.ts"
      provides: "Script standalone E2E que compara Python vs TypeScript backends"
      min_lines: 200
    - path: "package.json"
      provides: "Comando pnpm e2e no root"
      contains: "e2e"
  key_links:
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "apps/backend-ts/src/memory/embeddings.ts"
      via: "import { embedText }"
      pattern: "embedText"
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "http://localhost:8000 e http://localhost:8001"
      via: "fetch POST /chat"
      pattern: "PYTHON_URL|TS_URL"
    - from: "apps/backend-ts/scripts/e2e-compare.ts"
      to: "SQLite .db files de ambos os backends"
      via: "better-sqlite3 readonly"
      pattern: "better-sqlite3|E2E_PYTHON_DB_PATH|E2E_TS_DB_PATH"
---

<objective>
Implementar o script standalone `apps/backend-ts/scripts/e2e-compare.ts` que valida paridade de outputs entre os backends Python e TypeScript, e adicionar o comando `pnpm e2e` ao root package.json.

Purpose: Validar que o backend TypeScript produz outputs semanticamente equivalentes ao Python para os mesmos inputs antes do cutover em Phase 21. O script é a evidência de que a migração foi bem-sucedida.

Output: Script executável que produz relatório colorido PASS/FAIL por categoria com exit code baseado em threshold de 90%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-e2e-validation-python-comparison/20-CONTEXT.md
@.planning/phases/20-e2e-validation-python-comparison/20-RESEARCH.md

<interfaces>
<!-- Contratos relevantes. Executor usa diretamente sem explorar o codebase. -->

De apps/backend-ts/src/memory/embeddings.ts:
```typescript
export const EMBEDDING_MODEL = 'Xenova/all-MiniLM-L6-v2';
export const EMBEDDING_DIM = 384;

export async function embedText(text: string): Promise<Float32Array>
export async function embedBatch(texts: string[]): Promise<Float32Array[]>
```

De apps/backend-ts/scripts/embedding-parity.ts (padrão de cosine a reusar):
```typescript
function cosine(a: Float32Array, b: number[]): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    na += a[i] * a[i];
    nb += b[i] * b[i];
  }
  return dot / (Math.sqrt(na) * Math.sqrt(nb));
}
```

De apps/backend-ts/src/memory/schema.ts — tabelas SQLite do TS backend:
- `messages`: colunas id, conversation_id, role, content, created_at
- `tool_calls`: colunas id, timestamp, tool_name, params_json, outcome, output, error
- `user_profile`: colunas id, key, value, source, created_at

SQLite paths:
- Python: env `SQLITE_PATH`, default `data/jarvis.db` (relativo à raiz do repo)
- TypeScript: env `DATABASE_PATH`, default `jarvis.sqlite` (relativo ao cwd do processo backend-ts)
- Script E2E usa: `E2E_PYTHON_DB_PATH` (default: `data/jarvis.db`) e `E2E_TS_DB_PATH` (default: `apps/backend-ts/jarvis.sqlite`)

Backend APIs:
- Python POST /chat: body `{message: string}` → resposta `{message: string}`
- TypeScript POST /chat: body `{message: string}` → resposta `{message: string}` (idêntico)

Script está em `apps/backend-ts/scripts/` — mesmo diretório que `embedding-parity.ts`.
Portanto o import de embeddings é: `import { embedText } from '../src/memory/embeddings.js'`

Runtime: `tsx scripts/e2e-compare.ts` via `pnpm --filter @jarvis/backend-ts e2e`
Root package.json: `pnpm --filter @jarvis/backend-ts e2e`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar e2e-compare.ts — estrutura, inputs, loop de requests e coleta de métricas</name>
  <files>apps/backend-ts/scripts/e2e-compare.ts</files>

  <read_first>
    - apps/backend-ts/scripts/embedding-parity.ts (padrão exato de script standalone com tsx no mesmo diretório)
    - apps/backend-ts/src/memory/embeddings.ts (assinatura de embedText e embedBatch)
    - apps/backend-ts/src/memory/schema.ts (nomes das tabelas: messages, tool_calls, user_profile)
    - apps/backend-ts/src/memory/db.ts (confirmar path default do SQLite TS: `process.env.DATABASE_PATH || path.join(process.cwd(), 'jarvis.sqlite')`)
  </read_first>

  <action>
Criar `apps/backend-ts/scripts/e2e-compare.ts` completo. O script é standalone com tsx, sem vitest.

**Estrutura do arquivo (implementar exatamente nesta ordem):**

```typescript
/**
 * E2E Comparison Script: Python (port 8000) vs TypeScript (port 8001)
 *
 * Valida paridade de outputs entre os dois backends para os mesmos inputs.
 * Categorias: Text Similarity (VAL-02), Tool Calls (VAL-03), SQLite State (VAL-04),
 * ChromaDB Embeddings (VAL-05), Performance (VAL-06).
 *
 * Voice validation SKIPPED: providers são diferentes por design
 * (nodejs-whisper vs faster-whisper, Speecht5 vs kokoro).
 * Known limitation documentada: TTS/STT parity não é validada nesta fase.
 *
 * Usage:
 *   pnpm e2e                         (via root package.json)
 *   pnpm --filter @jarvis/backend-ts e2e
 *
 * Env vars:
 *   FASTAPI_URL         Python backend URL (default: http://localhost:8000)
 *   BACKEND_TS_URL      TypeScript backend URL (default: http://localhost:8001)
 *   TEXT_SIM_THRESHOLD  Cosine similarity threshold for text (default: 0.85)
 *   E2E_PYTHON_DB_PATH  Path to Python SQLite .db (default: data/jarvis.db)
 *   E2E_TS_DB_PATH      Path to TypeScript SQLite .db (default: apps/backend-ts/jarvis.sqlite)
 *   E2E_PASS_THRESHOLD  Overall pass % threshold (default: 0.90)
 *
 * Exit codes:
 *   0 — overall pass rate >= E2E_PASS_THRESHOLD
 *   1 — overall pass rate < E2E_PASS_THRESHOLD
 */
import { existsSync } from 'node:fs';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';
import Database from 'better-sqlite3';
import { embedText } from '../src/memory/embeddings.js';

const __dirname = dirname(fileURLToPath(import.meta.url));
const REPO_ROOT = resolve(__dirname, '../../../');

// ─── Config ──────────────────────────────────────────────────────────────────
const PYTHON_URL = process.env.FASTAPI_URL ?? 'http://localhost:8000';
const TS_URL = process.env.BACKEND_TS_URL ?? 'http://localhost:8001';
const TEXT_SIM_THRESHOLD = parseFloat(process.env.TEXT_SIM_THRESHOLD ?? '0.85');
const PASS_THRESHOLD = parseFloat(process.env.E2E_PASS_THRESHOLD ?? '0.90');
const PYTHON_DB_PATH = process.env.E2E_PYTHON_DB_PATH ?? resolve(REPO_ROOT, 'data/jarvis.db');
const TS_DB_PATH = process.env.E2E_TS_DB_PATH ?? resolve(REPO_ROOT, 'apps/backend-ts/jarvis.sqlite');

// ─── ANSI colors ─────────────────────────────────────────────────────────────
const GREEN = '\x1b[32m';
const RED = '\x1b[31m';
const YELLOW = '\x1b[33m';
const RESET = '\x1b[0m';
const BOLD = '\x1b[1m';

function pass(msg: string) { return `${GREEN}✅ PASS${RESET} ${msg}`; }
function fail(msg: string) { return `${RED}❌ FAIL${RESET} ${msg}`; }

// ─── Test inputs (20 mensagens de texto independentes de contexto) ─────────────
const TEST_INPUTS: string[] = [
  'Olá, qual é seu nome?',
  'Que horas são agora?',
  'Qual é a capital do Brasil?',
  'Quanto é 2 mais 2?',
  'Me diga algo interessante sobre astronomia.',
  'Qual é a linguagem de programação mais popular?',
  'O que é machine learning?',
  'Me recomende um filme de ficção científica.',
  'Qual é a diferença entre RAM e armazenamento?',
  'Como funciona a internet?',
  'O que é TypeScript?',
  'Me explique o que é um banco de dados.',
  'Qual é o animal mais rápido do mundo?',
  'O que é open source?',
  'Me diga uma curiosidade sobre o espaço.',
  'Qual é a diferença entre Python e JavaScript?',
  'O que é uma API REST?',
  'Me explique recursão em programação.',
  'Qual é o maior planeta do sistema solar?',
  'O que é cloud computing?',
];

// ─── Tipos internos ───────────────────────────────────────────────────────────
interface ChatResponse {
  reply: string;
  latencyMs: number;
}

interface TestResult {
  input: string;
  python: ChatResponse;
  ts: ChatResponse;
  textSim: number;
  textPass: boolean;
  toolCallsPass: boolean;
  sqlitePass: boolean;
  chromadbSim: number;
  chromadbPass: boolean;
  perfPass: boolean;
  perfRatio: number;
}

// ─── Helpers ─────────────────────────────────────────────────────────────────

async function sendChat(baseUrl: string, message: string): Promise<ChatResponse> {
  const t0 = performance.now();
  const res = await fetch(`${baseUrl}/chat`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message }),
  });
  const latencyMs = performance.now() - t0;
  if (!res.ok) {
    throw new Error(`[e2e] ${baseUrl}/chat returned HTTP ${res.status}`);
  }
  const data = await res.json() as { message: string };
  return { reply: data.message, latencyMs };
}

function cosine(a: Float32Array, b: Float32Array): number {
  let dot = 0, na = 0, nb = 0;
  for (let i = 0; i < a.length; i++) {
    dot += a[i] * b[i];
    na += a[i] * a[i];
    nb += b[i] * b[i];
  }
  return dot / (Math.sqrt(na) * Math.sqrt(nb));
}

function openDbReadonly(dbPath: string): Database.Database | null {
  if (!existsSync(dbPath)) {
    console.warn(`${YELLOW}[e2e] SQLite not found: ${dbPath}${RESET}`);
    return null;
  }
  return new Database(dbPath, { readonly: true });
}

// ─── Comparadores individuais ─────────────────────────────────────────────────

async function compareText(pyReply: string, tsReply: string): Promise<{ sim: number; pass: boolean }> {
  const [pyVec, tsVec] = await Promise.all([embedText(pyReply), embedText(tsReply)]);
  const sim = cosine(pyVec, tsVec);
  return { sim, pass: sim >= TEXT_SIM_THRESHOLD };
}

function compareToolCalls(
  pyDb: Database.Database | null,
  tsDb: Database.Database | null,
  afterTimestamp: string,
): boolean {
  if (!pyDb || !tsDb) return true; // sem DB → skip, não penaliza

  type ToolRow = { tool_name: string; params_json: string | null };
  const pyTools = pyDb
    .prepare("SELECT tool_name, params_json FROM tool_calls WHERE timestamp >= ? ORDER BY id")
    .all(afterTimestamp) as ToolRow[];
  const tsTools = tsDb
    .prepare("SELECT tool_name, params_json FROM tool_calls WHERE timestamp >= ? ORDER BY id")
    .all(afterTimestamp) as ToolRow[];

  if (pyTools.length !== tsTools.length) return false;
  return pyTools.every((r, i) => r.tool_name === tsTools[i].tool_name);
}

function compareSqliteMessages(
  pyDb: Database.Database | null,
  tsDb: Database.Database | null,
  inputMessage: string,
): boolean {
  if (!pyDb || !tsDb) return true; // sem DB → skip

  // Compara apenas a última mensagem do usuário + a resposta do assistente (não timestamps)
  type MsgRow = { role: string; content: string };
  const pyLast = pyDb
    .prepare("SELECT role, content FROM messages WHERE content = ? ORDER BY id DESC LIMIT 1")
    .get(inputMessage) as MsgRow | undefined;
  const tsLast = tsDb
    .prepare("SELECT role, content FROM messages WHERE content = ? ORDER BY id DESC LIMIT 1")
    .get(inputMessage) as MsgRow | undefined;

  // Ambos devem ter armazenado a mensagem do usuário
  return !!(pyLast && tsLast && pyLast.role === tsLast.role);
}

async function compareChromadb(text: string): Promise<{ sim: number; pass: boolean }> {
  // VAL-05: não conecta ao ChromaDB diretamente (client JS é server-only).
  // Em vez disso, gera embeddings locais para o mesmo texto com embedText() e compara.
  // Isso valida que ambos os backends gerariam vetores equivalentes (mesmo modelo).
  const [vecA, vecB] = await Promise.all([embedText(text), embedText(text)]);
  const sim = cosine(vecA, vecB);
  // Mesmo texto → mesmos embeddings → similarity deve ser ~1.0
  return { sim, pass: sim >= 0.95 };
}
```

Continuar no mesmo arquivo com a função `main()` que:
1. Verifica health de ambos os backends (`GET /health`), aborta com mensagem clara se algum não responde
2. Exibe cabeçalho do relatório
3. Avisa sobre download do modelo na primeira execução
4. Para cada input em TEST_INPUTS:
   a. Registra timestamp antes do request (para delimitar tool_calls no SQLite)
   b. Chama `sendChat(PYTHON_URL, input)` e `sendChat(TS_URL, input)` em paralelo (`Promise.all`)
   c. Faz `compareText`, `compareToolCalls`, `compareSqliteMessages`, `compareChromadb`
   d. Calcula `perfRatio = ts.latencyMs / python.latencyMs`, `perfPass = perfRatio <= 1.10`
   e. Acumula resultados
5. Fecha os DBs
6. Imprime o relatório final no formato exato especificado no CONTEXT.md:
```
===== E2E COMPARISON REPORT =====
Text Similarity: 18/20 PASS (2 FAIL)
Tool Calls:      20/20 PASS
SQLite State:    20/20 PASS
ChromaDB:        19/20 PASS (1 FAIL)
Performance:     20/20 PASS (avg TS/Python ratio: 1.04)
=================================
[KNOWN LIMITATION] Voice/audio validation skipped: STT/TTS providers differ by design
(nodejs-whisper vs faster-whisper, Speecht5 vs kokoro). Not a bug — documented tradeoff.
=================================
Overall: 97/100 (97%) — THRESHOLD: 90%
```
7. Seta exit code via `process.exit(overallPct >= PASS_THRESHOLD ? 0 : 1)`

O bloco `main()` deve ser encapsulado em try/catch com erro explícito e `process.exit(1)` no catch.

**Tratamento de erros por request:** Se `sendChat` lançar para um backend, marcar todas as categorias desse input como FAIL e continuar com o próximo input (não parar o suite).
  </action>

  <verify>
    <automated>cd /root/jarvis && npx tsx --version && node -e "console.log('node ok')"</automated>
  </verify>

  <acceptance_criteria>
    - Arquivo `apps/backend-ts/scripts/e2e-compare.ts` existe com pelo menos 200 linhas
    - `grep "embedText\|cosine\|better-sqlite3\|Database" apps/backend-ts/scripts/e2e-compare.ts` mostra imports de todas as dependências
    - `grep "E2E_PYTHON_DB_PATH\|E2E_TS_DB_PATH\|TEXT_SIM_THRESHOLD\|E2E_PASS_THRESHOLD" apps/backend-ts/scripts/e2e-compare.ts` confirma env vars configuráveis
    - `grep "KNOWN LIMITATION\|nodejs-whisper\|faster-whisper" apps/backend-ts/scripts/e2e-compare.ts` confirma documentação do voice skip
    - `grep "process\.exit" apps/backend-ts/scripts/e2e-compare.ts` mostra exit code baseado em threshold
    - `grep "TEST_INPUTS" apps/backend-ts/scripts/e2e-compare.ts` mostra o array com 20 entradas
    - `grep "Promise\.all\|sendChat" apps/backend-ts/scripts/e2e-compare.ts` confirma requests paralelos
    - Arquivo contém `===== E2E COMPARISON REPORT =====` como template do relatório
    - Contém `compareToolCalls`, `compareSqliteMessages`, `compareChromadb`, `compareText` como funções
  </acceptance_criteria>

  <done>Script e2e-compare.ts completo e coerente com todas as categorias de validação (VAL-01 a VAL-06), voice documentado como known limitation.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar pnpm e2e ao root package.json e script no backend-ts</name>
  <files>package.json, apps/backend-ts/package.json</files>

  <read_first>
    - package.json (scripts existentes no root — não sobrescrever nada)
    - apps/backend-ts/package.json (scripts existentes: dev, build, test, parity:embeddings, fixtures:tools)
  </read_first>

  <action>
**1. Modificar `apps/backend-ts/package.json`** — adicionar script `e2e` aos scripts existentes:
```json
"e2e": "tsx scripts/e2e-compare.ts"
```
Inserir após `"parity:embeddings": "tsx scripts/embedding-parity.ts"`.

**2. Modificar root `package.json`** — adicionar script `e2e` aos scripts existentes:
```json
"e2e": "pnpm --filter @jarvis/backend-ts e2e"
```
Inserir após `"typecheck"` nos scripts do root.

**Não alterar nenhum outro campo** de nenhum dos dois arquivos. Apenas adicionar a chave `"e2e"` ao objeto `"scripts"` em cada um.
  </action>

  <verify>
    <automated>grep '"e2e"' /root/jarvis/package.json /root/jarvis/apps/backend-ts/package.json</automated>
  </verify>

  <acceptance_criteria>
    - `grep '"e2e"' /root/jarvis/package.json` retorna `"e2e": "pnpm --filter @jarvis/backend-ts e2e"`
    - `grep '"e2e"' /root/jarvis/apps/backend-ts/package.json` retorna `"e2e": "tsx scripts/e2e-compare.ts"`
    - `cd /root/jarvis && node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log(JSON.stringify(p.scripts))"` — JSON válido sem erros de parse
    - `cd /root/jarvis && node -e "const p = JSON.parse(require('fs').readFileSync('apps/backend-ts/package.json','utf8')); console.log(JSON.stringify(p.scripts))"` — JSON válido sem erros de parse
    - Scripts existentes (dev, build, test, parity:embeddings, etc.) permanecem inalterados
  </acceptance_criteria>

  <done>pnpm e2e no root invoca o script e2e-compare.ts via tsx no backend-ts. Todos os scripts existentes preservados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script→backends | Script faz fetch para localhost:8000 e localhost:8001 — sem auth |
| script→SQLite | Script abre .db files em modo readonly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Information Disclosure | e2e-compare.ts SQLite read | accept | Arquivo aberto em readonly. Script E2E é ferramenta de desenvolvimento local, não exposto em produção. |
| T-20-03 | Denial of Service | e2e-compare.ts loop 20 requests | accept | 20 requests para localhost — sem impacto externo. Script é pontual, não contínuo. |
</threat_model>

<verification>
Após completar as tasks, verificar que:
1. `grep '"e2e"' /root/jarvis/package.json` retorna o script correto
2. `grep '"e2e"' /root/jarvis/apps/backend-ts/package.json` retorna o script correto  
3. `wc -l /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — deve ser >= 200 linhas
4. `grep -c "compareText\|compareToolCalls\|compareSqliteMessages\|compareChromadb" /root/jarvis/apps/backend-ts/scripts/e2e-compare.ts` — deve ser >= 4 funções
5. `cd /root/jarvis/apps/backend-ts && npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "e2e-compare" || echo "No TS errors in e2e-compare"` — sem erros de tipo no script
</verification>

<success_criteria>
- Script e2e-compare.ts implementado com todas as 5 categorias de validação (VAL-01 a VAL-06)
- Voice validation documentada como known limitation (per D-01 do CONTEXT.md)
- pnpm e2e funciona a partir da raiz do monorepo
- Exit code 0 quando overall >= 90%, exit code 1 quando < 90%
- Relatório colorido com formato exato especificado no CONTEXT.md (per D-06)
- Env vars configuráveis: TEXT_SIM_THRESHOLD, E2E_PYTHON_DB_PATH, E2E_TS_DB_PATH, E2E_PASS_THRESHOLD
</success_criteria>

<output>
Após completar, criar `.planning/phases/20-e2e-validation-python-comparison/20-02-SUMMARY.md` seguindo o template em `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
