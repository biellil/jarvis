---
phase: 19_5-voice-electron
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/preload/index.ts
autonomous: true
requirements: [VOICE-TS-04]
must_haves:
  truths:
    - "SendAudioResponse carrega transcription, message, audioBase64, audioFormat, sttProvider, ttsProvider em success"
    - "SendAudioResponse em falha carrega {code, message} estruturado"
    - "JarvisAPI.sendAudio expõe o shape novo via contextBridge"
  artifacts:
    - path: apps/desktop/src/shared/ipc-types.ts
      provides: "SendAudioData, SendAudioError, SendAudioResponse novos"
    - path: apps/desktop/src/preload/index.ts
      provides: "sendAudio tipado com response novo"
  key_links:
    - from: apps/desktop/src/preload/index.ts
      to: apps/desktop/src/shared/ipc-types.ts
      via: "import type SendAudioResponse"
      pattern: "SendAudioResponse"
---

<objective>
Atualizar `SendAudioResponse` em `apps/desktop/src/shared/ipc-types.ts` pro shape novo definido no CONTEXT e garantir que o preload (`apps/desktop/src/preload/index.ts`) continua expondo `sendAudio` corretamente com o tipo atualizado.

Purpose: O shape novo é breaking change — renderer, main handler e preload precisam compartilhar a mesma definição. Este plano cria o contrato que os outros planos da wave consomem.
Output: Types atualizados + preload validado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19_5-voice-electron/19_5-CONTEXT.md
@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/preload/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar SendAudioData/Error/Response em ipc-types.ts</name>
  <files>apps/desktop/src/shared/ipc-types.ts</files>
  <action>
    1. Substituir o bloco `SendAudioData` / `SendAudioResponse` por:
    ```typescript
    export interface SendAudioData {
      transcription: string;
      message: string;
      audioBase64: string;
      audioFormat: 'mp3' | 'wav';
      sttProvider: string;
      ttsProvider: string;
    }

    export interface SendAudioError {
      code: string;
      message: string;
    }

    export type SendAudioResponse =
      | { success: true; data: SendAudioData }
      | { success: false; error: SendAudioError };
    ```
    2. O `IpcResult<T>` genérico continua sendo usado pelo SendTextResponse — NÃO mexer. `SendAudioResponse` vira uma union discriminada própria porque o tipo de `error` mudou de `string` pra objeto.
    3. Atualizar `JarvisAPI.sendAudio` pro retorno novo: `sendAudio: (audioBuffer: Uint8Array) => Promise<SendAudioResponse>` (assinatura não muda — só o type alias por baixo).
    4. Nenhuma mudança em `IPC_CHANNELS` ou `PttAction`.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit 2>&1 | grep -E "ipc-types|preload/index" || echo "no ipc-types/preload errors"</automated>
  </verify>
  <done>
    Types exportados conforme spec, `tsc --noEmit` sem erros nos arquivos de tipos/preload (erros em chat.ts / renderer são corrigidos em outros planos da fase).
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirmar preload expõe sendAudio tipado</name>
  <files>apps/desktop/src/preload/index.ts</files>
  <action>
    1. Reler `apps/desktop/src/preload/index.ts`. O import `SendAudioResponse` já existe — garantir que continua importado.
    2. A implementação de `sendAudio` não muda (continua `ipcRenderer.invoke(IPC_CHANNELS.CHAT_SEND_AUDIO, Buffer.from(audioBuffer))`).
    3. Remover comentário desatualizado mencionando "WAV conversion" (agora é webm puro): trocar por `// Phase 19.5: WebM/Opus bytes pro gateway /api/chat/audio`.
    4. NÃO expor novos canais/métodos.
  </action>
  <verify>
    <automated>cd apps/desktop && pnpm tsc --noEmit src/preload/index.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    Preload compila sem erros de tipo referentes a SendAudioResponse; comentário atualizado.
  </done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` reporta erros SOMENTE em arquivos tocados por outros planos (chat.ts handler, App.tsx) — não em ipc-types.ts nem preload/index.ts
- `grep -n "reply" apps/desktop/src/shared/ipc-types.ts` não retorna referência em SendAudioData
</verification>

<success_criteria>
Shape novo de SendAudioResponse disponível pros demais planos; preload tipado corretamente; comentário histórico atualizado.
</success_criteria>

<output>
Criar `.planning/phases/19_5-voice-electron/19_5-02-SUMMARY.md`
</output>
