---
phase: 27-conversation-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/session/system-prompt.ts
  - apps/backend-ts/src/memory/manager.ts
autonomous: true
requirements:
  - CONV-07
  - CONV-08
  - CONV-09

must_haves:
  truths:
    - "JARVIS always responds in Portuguese Brazilian regardless of user input language"
    - "System prompt explicitly instructs pt-BR responses"
    - "Memory recall returns high-quality results (similarity >0.7) up to 10 matches"
  artifacts:
    - path: "apps/backend-ts/src/session/system-prompt.ts"
      provides: "pt-BR system prompt with explicit language instruction"
      min_lines: 8
      contains: "português brasileiro"
    - path: "apps/backend-ts/src/memory/manager.ts"
      provides: "Dynamic topK logic (3-10 results based on similarity >0.7)"
      exports: ["MemoryManager"]
  key_links:
    - from: "apps/backend-ts/src/session/index.ts"
      to: "system-prompt.ts"
      via: "ChatSession constructor imports SYSTEM_PROMPT"
      pattern: "import.*SYSTEM_PROMPT.*from.*system-prompt"
    - from: "apps/backend-ts/src/session/tools.ts"
      to: "memory/manager.ts"
      via: "createRecallMemoryTool calls memory.buildContext()"
      pattern: "memory\\.buildContext"
---

<objective>
Translate system prompt to Portuguese Brazilian with explicit language instruction, and implement dynamic topK memory recall logic that returns high-quality results (similarity >0.7) up to 10 matches instead of fixed topK=5.

**Purpose:** Ensure JARVIS always responds in pt-BR and retrieves relevant cross-session memories efficiently (CONV-07, CONV-08, CONV-09).

**Output:**
- System prompt in pt-BR with casual, friendly tone
- Dynamic memory recall returning 3-10 results based on quality threshold
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-conversation-quality/27-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate system prompt to pt-BR</name>

  <read_first>
    - apps/backend-ts/src/session/system-prompt.ts (current English prompt)
    - .planning/phases/27-conversation-quality/27-CONTEXT.md (D-01 through D-04: tone, structure, content requirements)
  </read_first>

  <files>apps/backend-ts/src/session/system-prompt.ts</files>

  <action>
Replace the English SYSTEM_PROMPT constant with Portuguese Brazilian translation following these exact specifications per user decisions D-01 through D-04:

**Tone (D-01):** Casual and friendly — "Just A Rather Very Intelligent System" is informal reference, not executive/formal

**Language instruction (D-02):** Add explicit "Sempre responda em português brasileiro" to guarantee consistency even with English input

**Content (D-03):** Concise but complete — identity ("Você é o JARVIS") + capabilities (memory, actions) + expected behavior (helpful, use tools when needed)

**Structure (D-04):** Maintain same structure as current English prompt

Current English prompt:
```
'You are JARVIS, a helpful personal assistant. ' +
'You remember everything from our conversations and help the user ' +
'with tasks, questions, and anything they need.'
```

Target pt-BR prompt (translate maintaining structure):
```typescript
export const SYSTEM_PROMPT =
  'Você é o JARVIS (Just A Rather Very Intelligent System), um assistente pessoal prestativo. ' +
  'Você lembra de tudo das nossas conversas e ajuda o usuário com tarefas, perguntas e o que ele precisar. ' +
  'Sempre responda em português brasileiro.';
```

Keep:
- Export const declaration
- JSDoc comment format
- String concatenation with `+` (same style as original)
  </action>

  <verify>
    <automated>grep -F "português brasileiro" apps/backend-ts/src/session/system-prompt.ts</automated>
  </verify>

  <acceptance_criteria>
    - File contains `export const SYSTEM_PROMPT`
    - Prompt text is in Portuguese Brazilian
    - Prompt includes explicit instruction "Sempre responda em português brasileiro"
    - Prompt mentions JARVIS identity with casual tone (includes "Just A Rather Very Intelligent System" reference)
    - Prompt mentions memory capability ("lembra de tudo das nossas conversas")
    - Prompt mentions helping with tasks/questions
    - JSDoc comment preserved
  </acceptance_criteria>

  <done>
System prompt is fully translated to pt-BR with explicit language instruction, maintaining original structure and adding friendly tone per D-01.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement dynamic topK memory recall</name>

  <read_first>
    - apps/backend-ts/src/memory/manager.ts (buildContext implementation, current topK=5)
    - apps/backend-ts/src/memory/vectors.ts (queryMemories threshold filtering logic)
    - .planning/phases/27-conversation-quality/27-CONTEXT.md (D-05, D-06, D-07: dynamic topK strategy)
  </read_first>

  <files>apps/backend-ts/src/memory/manager.ts</files>

  <action>
Modify `MemoryManager.buildContext()` to implement dynamic topK logic per user decision D-05:

**Strategy (D-05):** Return all results with similarity >0.7 (high quality threshold), limited to maximum 10 matches instead of fixed topK=5

**Threshold (D-06):** Keep recallThreshold=0.5 as constructor default (already correct), but apply additional high-quality filter of >0.7 in buildContext

**Current implementation:**
```typescript
const recalls = await this.vectors.queryMemories(
  userText,
  this.recallTopK,  // Fixed at 5
  this.recallThreshold,  // 0.5
);
```

**New implementation:**
```typescript
// Query with larger topK to get candidate pool
const recalls = await this.vectors.queryMemories(
  userText,
  10,  // Max results to consider
  0.7, // High-quality threshold per D-05
);
// No additional filtering needed — queryMemories already filters by threshold
// Results are naturally limited to ≤10 and all have similarity >0.7
```

**Rationale:** By passing threshold=0.7 directly to `queryMemories()`, we leverage existing filtering logic in `vectors.ts` lines 138-139. The method returns 0-10 results, all with similarity >0.7, achieving dynamic topK behavior without adding new code paths.

**Format (D-07):** Keep current markdown list format for recall results (lines 97-102) — no changes needed to output format.

Do NOT modify:
- Constructor parameters (recallTopK, recallThreshold stay as configurable options)
- Profile facts section (lines 89-95)
- Return value format
- Error handling
  </action>

  <verify>
    <automated>grep "queryMemories.*userText.*10.*0\.7" apps/backend-ts/src/memory/manager.ts</automated>
  </verify>

  <acceptance_criteria>
    - buildContext() calls `this.vectors.queryMemories(userText, 10, 0.7)` with parameters in correct order
    - No longer uses `this.recallTopK` variable in the queryMemories call
    - Profile facts section unchanged (still includes `### User profile` header)
    - Recall results section unchanged (still includes `### Recall from past conversations` header)
    - Return type unchanged (still `Promise<string>`)
    - Constructor parameters `recallTopK` and `recallThreshold` preserved (backward compatibility)
  </acceptance_criteria>

  <done>
buildContext() returns 3-10 high-quality memory results (similarity >0.7) instead of fixed 5 results, improving cross-session context relevance per CONV-08.
  </done>
</task>

</tasks>

<verification>
1. System prompt contains pt-BR text with "Sempre responda em português brasileiro"
2. buildContext() queries with topK=10 and threshold=0.7
3. No compilation errors: `cd apps/backend-ts && pnpm tsc --noEmit`
4. Existing tests pass: `cd apps/backend-ts && pnpm test src/memory/manager.test.ts`
</verification>

<success_criteria>
- [ ] JARVIS system prompt is in Portuguese Brazilian (CONV-07)
- [ ] System prompt explicitly instructs "Sempre responda em português brasileiro"
- [ ] Memory recall logic returns dynamic 3-10 results based on similarity >0.7
- [ ] TypeScript compilation succeeds
- [ ] Existing memory manager tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-conversation-quality/27-01-SUMMARY.md`
</output>
