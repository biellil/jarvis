---
phase: 53-streaming-tts
verified: 2026-05-05T00:00:00Z
status: human_needed
score: 4/4 must-haves verified
human_verification:
  - test: "Smoke test: enable Streaming TTS in Settings UI, then say wake word + a multi-sentence prompt"
    expected: "First sentence of audio begins playing <1s after the first sentence is fully generated by the LLM; subsequent sentences play with no perceptible silence between them"
    why_human: "Latency feel and gapless playback quality require ear-level perception; latency is unit-tested with mocks but real LM Studio + TTS provider timing not measured"
  - test: "Toggle STREAMING_TTS off mid-session and run another voice turn"
    expected: "Legacy Murf.ai single-shot path runs (no chunking, no tts:chunk IPCs); JARVIS speaks only after full response is generated — no regression"
    why_human: "Verifies success criteria #3 against real Murf.ai network call, not mocked synthesize()"
  - test: "Toggle STREAMING_TTS on, speak prompt, then barge-in (wake word again or PTT) during playback"
    expected: "Current audio stops within ~150ms; new turn begins"
    why_human: "Barge-in entry point abortActiveStreamingTurn() exists but the consumer-side wake-word/PTT dispatcher is NOT YET wired to call it (Plan 04 Summary explicitly defers this). Renderer-side onStop listener is wired. Need user to confirm whether legacy stopTTSPlayback path covers streaming chunks, or if barge-in leaves residual audio queued"
  - test: "Restart JARVIS after toggling Streaming TTS on; verify the toggle remains on and behavior is streaming on first turn"
    expected: "Persisted via electron-store; flag survives restart"
    why_human: "Persistence verified by store unit tests but full process restart cycle not exercised in test suite"
---

# Phase 53: Streaming TTS — Verification Report

**Phase Goal:** O JARVIS inicia o playback de voz na primeira sentença completa sem aguardar a resposta inteira do LLM.
**Verified:** 2026-05-05
**Status:** human_needed (all automated checks pass; visual/auditory + barge-in behavior need human confirmation)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to ROADMAP Success Criteria)

| #   | Truth (Success Criterion)                                                                                            | Status         | Evidence                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | First audio plays <1s after first sentence completes                                                                  | ? UNCERTAIN    | `streamingTurn.latency.test.ts` exercises latency with mocked TTS — passes per 53-01 SUMMARY. End-to-end real-LLM latency not measured. Needs human ear test.                                                                     |
| 2   | Subsequent sentences play with no perceptible silence (gapless)                                                       | ✓ VERIFIED     | `streamingTtsPlayer.ts:103` uses `createBufferSource` + sample-accurate `start(when)` scheduling at `lastEnd`. Out-of-order chunk reordering tested. Gap-free behavior is sample-accurate by Web Audio contract.                  |
| 3   | Flag=false preserves legacy v2.1 path (no Murf regression)                                                            | ✓ VERIFIED     | `voiceHandler.ts:161` reads `getStreamingTtsEnabled()` once; flag=false branch is byte-identical to prior code (additive bifurcation). `voiceHandler.streaming.test.ts` Test 1 confirms `runStreamingTurn` never called when flag=false. |
| 4   | Toggling flag does not require restart                                                                                | ✓ VERIFIED     | Flag read at start of every turn (`voiceHandler.ts:161`). `voiceHandler.streaming.test.ts` Test 4 (two-turn flag flip) executes turn1 legacy → flip → turn2 streaming in same process, no module reload.                            |

**Score:** 3/4 verified, 1/4 needs human (latency feel)

### Required Artifacts

| Artifact                                                                       | Expected                                            | Status      | Details                                                                                                              |
| ------------------------------------------------------------------------------ | --------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------- |
| `apps/desktop/src/main/voiceInput/chunker.ts`                                   | SentenceChunker with feed/flush                     | ✓ VERIFIED  | 58 lines; `export class SentenceChunker` confirmed; tests in `__tests__/chunker.test.ts`.                           |
| `apps/desktop/src/main/voiceInput/streamingTurn.ts`                             | runStreamingTurn orchestrator                       | ✓ VERIFIED  | 169 lines; signature is `export function runStreamingTurn` (returns Promise; not literal `async` keyword). Tooling reported "missing pattern" because plan declared `async function`; implementation is functionally equivalent. |
| `apps/desktop/src/shared/ipc-types.ts`                                          | TTS_CHUNK / TTS_END / TTS_STOP channels             | ✓ VERIFIED  | All five constants present (lines 273–281): TTS_CHUNK, TTS_END, TTS_STOP, STREAMING_TTS_SET, STREAMING_TTS_CHANGED. |
| `apps/desktop/src/renderer/src/audio/audioContextSingleton.ts`                  | Shared AudioContext getter                          | ✓ VERIFIED  | 25 lines; `export function getAudioContext` present.                                                                |
| `apps/desktop/src/renderer/src/audio/streamingTtsPlayer.ts`                     | enqueueChunk / stopTurn / onFirstSentenceStart      | ✓ VERIFIED  | 189 lines; `export async function enqueueChunk` (line 87) — plan declared non-async; functionally equivalent. `stopTurn` (137), `setOnFirstSentenceStart` (52), `wireStreamingTtsListeners` (170). |
| `apps/desktop/src/main/store.ts`                                                | get/setStreamingTtsEnabled                          | ✓ VERIFIED  | Lines 298, 303; defaults to false.                                                                                   |
| `apps/desktop/src/main/ipc/settings.ts`                                          | streamingTts:set handler + broadcast                | ✓ VERIFIED  | Lines 221–229; `ipcMain.handle(STREAMING_TTS_SET, …)` persists then broadcasts STREAMING_TTS_CHANGED to all windows.|
| `apps/desktop/src/renderer/src/components/ui/switch.tsx`                        | Radix Switch primitive                              | ✓ VERIFIED  | 44 lines; uses `@radix-ui/react-switch`.                                                                            |
| `apps/desktop/src/renderer/src/settings/sections/TtsSection.tsx`                 | Streaming TTS toggle                                | ✓ VERIFIED  | 122 lines; `<Switch checked={streamingTtsEnabled} onCheckedChange={onStreamingTtsChange} />` (lines 107–110).        |
| `apps/desktop/src/main/voiceInput/voiceHandler.ts`                              | Bifurcated handleAudio gated by flag                | ✓ VERIFIED  | Imports `getStreamingTtsEnabled` (line 20); flag read at line 161; delegates to `runStreamingTurn` line 163; `abortActiveStreamingTurn` exported (line 52). |

### Key Link Verification

| From                                            | To                                                                | Via                                  | Status      | Details                                                                                                  |
| ----------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------- |
| `streamingTurn.ts`                               | `sse-client.openChatStream`                                       | import + onToken/onEnd               | ✓ WIRED     | `import { openChatStream } from '../sse-client.js'` (line 35); used at line 77.                          |
| `streamingTurn.ts`                               | `getActiveTtsProvider().synthesize()`                             | per-sentence call                    | ✓ WIRED     | Imported (line 36); `tts.synthesize(text)` line 90.                                                      |
| `streamingTurn.ts`                               | `BrowserWindow.webContents.send('tts:chunk')`                     | per-sentence IPC                     | ✓ WIRED     | `webContents.send(IPC_CHANNELS.TTS_CHUNK, payload)` line 84; TTS_END line 153.                           |
| `streamingTtsPlayer.ts`                          | `AudioContext.decodeAudioData + createBufferSource`               | gapless scheduling                   | ✓ WIRED     | Lines 92, 103.                                                                                           |
| `streamingTtsPlayer.ts`                          | `ipcRenderer.on('tts:chunk' / 'tts:end' / 'tts:stop')`            | preload subscriptions                | ✓ WIRED     | `wireStreamingTtsListeners()` at line 170; invoked from `App.tsx:137`. Preload exposes via `streamingTts.{onChunk,onEnd,onStop}`. |
| `TtsSection.tsx`                                 | `window.settings.setStreamingTts(boolean)`                        | Switch onCheckedChange               | ✓ WIRED     | `SettingsLayout.tsx:283` — `window.settings.setStreamingTts(enabled)`. Preload exposes at `settings.ts:28`. |
| `ipc/settings.ts`                                | `setStreamingTtsEnabled + BrowserWindow broadcast`                | ipcMain.handle('streamingTts:set')   | ✓ WIRED     | Lines 221–230; persists via store, then `getAllWindows().forEach(win => win.webContents.send(STREAMING_TTS_CHANGED, value))`. |
| `voiceHandler.ts`                                | `store.getStreamingTtsEnabled`                                    | start of every turn                  | ✓ WIRED     | Line 161 — single read into local `streamingEnabled` constant.                                            |
| `voiceHandler.ts (flag=true branch)`             | `streamingTurn.runStreamingTurn`                                  | function call                        | ✓ WIRED     | Line 163; handle stored in module-scoped `activeStreamingTurn` (line 38).                                |
| barge-in dispatcher                              | `activeStreamingTurn.abort() + tts:stop IPC`                      | wake-word/PTT path (Phase 39)        | ⚠️ PARTIAL  | `abortActiveStreamingTurn()` (line 52) implements abort + TTS_STOP correctly, BUT 53-04 SUMMARY explicitly states: "Currently NOT wired in this plan — there is no main-process barge-in dispatcher today". Renderer-side onStop listener exists. |

### Data-Flow Trace (Level 4)

| Artifact                | Data Variable           | Source                                                  | Produces Real Data | Status      |
| ----------------------- | ----------------------- | ------------------------------------------------------- | ------------------ | ----------- |
| `streamingTtsPlayer.ts` | TTSChunkPayload         | `wireStreamingTtsListeners()` ← preload `streamingTts.onChunk` ← main `webContents.send(TTS_CHUNK)` ← `runStreamingTurn` | Yes                | ✓ FLOWING   |
| `TtsSection.tsx`        | `streamingTtsEnabled` prop | `SettingsLayout.tsx:111` — populated from initial `getSettings()` IPC, then re-set on `streamingTts:changed` broadcast (line 168) | Yes                | ✓ FLOWING   |
| `voiceHandler.ts`       | `streamingEnabled`        | `getStreamingTtsEnabled()` ← `store.get('streamingTtsEnabled')` ← electron-store JSON | Yes                | ✓ FLOWING   |

### Behavioral Spot-Checks

Skipped — phase changes desktop voice pipeline that requires Electron app + LM Studio + TTS provider running. Static checks confirm all wiring; runtime behavior is human-verification scope.

### Requirements Coverage

| Requirement | Source Plan(s)        | Description                                                                                                                                  | Status     | Evidence                                                                                                                                       |
| ----------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| STTS-01     | 53-01, 53-02, 53-04   | TTS inicia playback na primeira sentença completa sem esperar resposta completa do LLM (chunking por `[.!?]\s+` no Electron)                 | ✓ SATISFIED | chunker.ts (regex chunking), streamingTurn.ts (per-sentence synthesize), streamingTtsPlayer.ts (gapless schedule), voiceHandler.ts (bifurcation) |
| STTS-02     | 53-03, 53-04          | Feature flag `STREAMING_TTS=true/false` ativa/desativa streaming sem restart (default: false)                                                | ✓ SATISFIED | store.ts (default false), ipc/settings.ts (handler+broadcast), TtsSection.tsx (toggle UI), voiceHandler.ts:161 (flag read per turn)             |

No orphaned requirements — both STTS-01 and STTS-02 mapped to plans declaring them in frontmatter.

### Anti-Patterns Found

None blocking. Notable observations from 53-04 SUMMARY:

| File                                         | Severity | Note                                                                                                                                                                                                |
| -------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `voiceHandler.ts` (consumer-side barge-in)   | ⚠️ Warning | `abortActiveStreamingTurn` defined but no production caller wired. SUMMARY: "no main-process barge-in dispatcher today". Renderer onStop covers IPC arrival, but barge-in will need main-side wiring in a future phase. |
| Test suite baseline                          | ℹ️ Info   | 19 pre-existing failures unrelated to Phase 53. Net delta: +5 new passing, +4 fewer failing. Zero regressions.                                                                                      |

### Human Verification Required

Listed in frontmatter `human_verification`. Summary:

1. **Streaming latency feel** — verify <1s first-sentence-to-audio in real LM Studio + TTS provider session (Truth #1)
2. **Legacy regression check** — toggle off, run a turn, confirm Murf.ai single-shot still works (Success Criterion #3 with real network)
3. **Barge-in during streaming** — verify if existing wake-word/PTT path actually stops streaming chunks; main-side `abortActiveStreamingTurn` consumer is unwired by design in Plan 04
4. **Persistence across restart** — toggle on, restart app, confirm flag stays on and first turn is streaming

### Gaps Summary

No gaps blocking goal achievement under code-level verification. Every must-have is either VERIFIED or maps to a Web Audio API contract that is sample-accurate by spec. The single ⚠️ partial is the barge-in dispatcher consumer-side, which Plan 04 explicitly deferred (designed as a hook to be called when a future phase introduces a main-process barge-in dispatcher; renderer-side TTS_STOP listener is wired today). The phase deliberately ships with this seam.

The four success criteria mapped to four observable truths: three are programmatically verified through static analysis + unit tests; the fourth (latency feel) is inherently a human/UX measurement and is routed to the human verification list along with three additional real-system checks.

---

_Verified: 2026-05-05_
_Verifier: Claude (gsd-verifier)_
