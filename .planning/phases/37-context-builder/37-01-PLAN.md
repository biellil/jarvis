---
phase: 37-context-builder
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/test/memory/manager-context.test.ts
  - apps/backend-ts/src/memory/manager.ts
autonomous: true
requirements:
  - MCTX-01
  - MCTX-02
  - MCTX-03
  - MCTX-04

must_haves:
  truths:
    - "buildContext() chama queryMemoriesByType() 3 vezes (semantic, episodic, procedural) sem threshold"
    - "As 3 queries executam em paralelo via Promise.all() — latência total < 200ms mesmo com 80ms de delay por coleção"
    - "O contexto retornado segue a ordem: Perfil do usuário → rolling summary (quando presente) → Memórias semânticas → Memórias episódicas → Memórias procedurais"
    - "Seções vazias (array vazio retornado por queryMemoriesByType) são omitidas do output"
    - "buildContext(userText) sem rollingSum funciona idêntico ao comportamento anterior — backward compatible"
  artifacts:
    - path: "apps/backend-ts/test/memory/manager-context.test.ts"
      provides: "Suite de testes RED→GREEN para MCTX-01, MCTX-02, MCTX-03, MCTX-04"
      exports: []
    - path: "apps/backend-ts/src/memory/manager.ts"
      provides: "buildContext() refatorado com Promise.all, headers pt-BR, rollingSum opcional"
      exports: ["MemoryManager", "MemoryManagerOptions"]
  key_links:
    - from: "apps/backend-ts/src/memory/manager.ts:buildContext()"
      to: "apps/backend-ts/src/memory/vectors.ts:queryMemoriesByType()"
      via: "Promise.all de 3 chamadas paralelas"
      pattern: "Promise\\.all\\(\\["
    - from: "apps/backend-ts/src/memory/manager.ts:buildContext()"
      to: "apps/backend-ts/src/memory/store.ts:getProfileFacts()"
      via: "chamada sequencial antes do Promise.all"
      pattern: "getProfileFacts"
---

<objective>
Refatorar buildContext() no MemoryManager usando ciclo TDD RED→GREEN:

1. RED: Criar manager-context.test.ts com testes que cobrem MCTX-01 a MCTX-04 — todos falham contra a implementação atual (que usa queryMemories() sequencial com threshold 0.7)
2. GREEN: Refatorar buildContext() em manager.ts para passar todos os testes — três queryMemoriesByType() em paralelo, headers em pt-BR, rollingSum opcional, seções vazias omitidas

Purpose: Substituir a chamada única legada (queryMemories, threshold 0.7) por recuperação tipada paralela, entregando contexto tiered em formato pt-BR para o LLM.

Output:
- apps/backend-ts/test/memory/manager-context.test.ts (novo arquivo de testes)
- apps/backend-ts/src/memory/manager.ts (buildContext() refatorado)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-context-builder/37-CONTEXT.md
@.planning/phases/37-context-builder/37-RESEARCH.md
@.planning/phases/37-context-builder/37-VALIDATION.md

<!-- Arquivos a modificar/criar — leia antes de qualquer edição -->
@apps/backend-ts/src/memory/manager.ts
@apps/backend-ts/src/memory/vectors.ts
@apps/backend-ts/test/memory/manager-typed.test.ts
@apps/backend-ts/test/memory/vectors-typed.test.ts
</context>

<interfaces>
<!-- Contratos existentes que o executor precisa — extraídos do codebase -->

De apps/backend-ts/src/memory/vectors.ts:
```typescript
export interface QueryResult {
  id: string;
  document: string;
  similarity: number; // 1 - cosine_distance, em [0, 1]
  metadata?: Record<string, unknown>;
}

// Método a ser chamado 3x em paralelo em buildContext():
async queryMemoriesByType(
  userText: string,
  type: 'semantic' | 'episodic' | 'procedural',
  topK = 5,
): Promise<QueryResult[]>
// Comportamento: retorna [] em erro, NUNCA lança exceção
// Sem threshold — retorna sempre top-K resultados por score de distância

// Método legado (NÃO usar em buildContext() após refatoração):
async queryMemories(queryText: string, nResults = 5, threshold?: number): Promise<QueryResult[]>
```

De apps/backend-ts/src/memory/store.ts (via manager.ts):
```typescript
export interface ProfileFact { key: string; value: string; source: string }

// this.store.getProfileFacts() — síncrono, retorna ProfileFact[], pode retornar []
```

De apps/backend-ts/src/memory/manager.ts (estado atual que SERÁ refatorado):
```typescript
// ANTES (legado — a ser substituído):
async buildContext(userText: string): Promise<string> {
  const facts = this.store.getProfileFacts();
  const recalls = await this.vectors.queryMemories(userText, 10, 0.7); // REMOVER
  // ... formata com '### User profile' e '### Recall from past conversations' (REMOVER headers em inglês)
}

// DEPOIS (nova assinatura com D-05):
async buildContext(userText: string, rollingSum?: string): Promise<string>
```

Padrão de mock já estabelecido em manager-typed.test.ts:
```typescript
vi.mock('../../src/memory/vectors.js', () => {
  class MockMemoryVectors {
    addTypedMemory = vi.fn().mockResolvedValue(undefined);
    addMemory = vi.fn().mockResolvedValue(undefined);
    queryMemories = vi.fn().mockResolvedValue([]);
    // NOVO: adicionar queryMemoriesByType ao mock neste plano
  }
  return { MemoryVectors: MockMemoryVectors };
});
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Criar manager-context.test.ts com testes falhando</name>
  <files>apps/backend-ts/test/memory/manager-context.test.ts</files>

  <read_first>
    - apps/backend-ts/test/memory/manager-typed.test.ts (padrão de mock de MemoryStore e MemoryVectors)
    - apps/backend-ts/test/memory/vectors-typed.test.ts (padrão de mock com delay para latência)
    - apps/backend-ts/src/memory/manager.ts (assinatura atual de buildContext — o alvo da refatoração)
    - apps/backend-ts/src/memory/vectors.ts (assinatura de queryMemoriesByType)
  </read_first>

  <behavior>
    Criar novo arquivo de teste que cobre os 4 requisitos. Todos os testes DEVEM falhar contra a implementação atual antes de qualquer mudança em manager.ts:

    **MCTX-02 — top-5 sem threshold:**
    - `it('chama queryMemoriesByType 3 vezes — semantic, episodic, procedural')`: verificar que queryMemoriesByType é chamado exatamente 3 vezes com tipos corretos e topK=5. Verificar que queryMemories (legado) NÃO é chamado.

    **MCTX-03 — paralelismo < 200ms:**
    - `it('executa as 3 queries em paralelo — completa em < 200ms com delay de 80ms por query')`: cada mock de queryMemoriesByType adiciona 80ms via `await new Promise(r => setTimeout(r, 80))`. Medir `Date.now()` antes e depois. Assert `elapsed < 200` (paralelo = ~80ms; sequencial = ~240ms — só passa se paralelo).

    **MCTX-01 — ordem tiered e headers pt-BR:**
    - `it('retorna seções na ordem: Perfil → Semântica → Episódica → Procedural')`: mock retorna facts=['key:val'], semantic=['fato1'], episodic=['evento1'], procedural=['how-to1']. Assert que o contexto contém os 4 headers `### Perfil do usuário`, `### Memórias semânticas`, `### Memórias episódicas`, `### Memórias procedurais` nessa ordem (verificar indexOf de cada header < indexOf do próximo).
    - `it('omite seção quando queryMemoriesByType retorna array vazio')`: mock semantic retorna `[]`, outros retornam docs. Assert que `### Memórias semânticas` NÃO aparece no output.

    **MCTX-04 — backward compatibility e rollingSum:**
    - `it('buildContext(userText) sem rollingSum funciona sem erro')`: chamar `buildContext('query')` sem segundo argumento — resolve sem erro.
    - `it('insere rollingSum entre Perfil e Memórias semânticas quando passado')`: chamar `buildContext('query', 'resumo da conversa')`. Assert que o output contém `'resumo da conversa'` e que aparece APÓS `### Perfil do usuário` e ANTES de `### Memórias semânticas`.
  </behavior>

  <action>
    Criar `apps/backend-ts/test/memory/manager-context.test.ts` com o seguinte conteúdo exato:

    **Imports e mocks:**
    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { MemoryManager } from '../../src/memory/manager.js';
    ```

    **Mock de MemoryStore** (copiar padrão de manager-typed.test.ts):
    ```typescript
    vi.mock('../../src/memory/store.js', () => {
      class MockMemoryStore {
        saveTypedMemory = vi.fn();
        startConversation = vi.fn().mockReturnValue(1);
        endConversation = vi.fn();
        saveMessages = vi.fn();
        getProfileFacts = vi.fn().mockReturnValue([{ key: 'nome', value: 'Biel', source: 'explicit' }]);
        upsertProfile = vi.fn();
        close = vi.fn();
      }
      return { MemoryStore: MockMemoryStore };
    });
    ```

    **Mock de MemoryVectors** — incluir `queryMemoriesByType` com mock simples (sem delay no mock global):
    ```typescript
    vi.mock('../../src/memory/vectors.js', () => {
      class MockMemoryVectors {
        addTypedMemory = vi.fn().mockResolvedValue(undefined);
        addMemory = vi.fn().mockResolvedValue(undefined);
        queryMemories = vi.fn().mockResolvedValue([]);
        queryMemoriesByType = vi.fn().mockResolvedValue([]);
      }
      return { MemoryVectors: MockMemoryVectors };
    });
    ```

    **Mock config:**
    ```typescript
    vi.mock('../../src/config.js', () => ({
      config: { chromaHost: 'localhost', chromaPort: 8000 },
    }));

    vi.mock('../../src/memory/profile.js', () => ({
      isExplicitProfileCommand: vi.fn().mockReturnValue(false),
      extractProfileFacts: vi.fn().mockResolvedValue({}),
    }));
    ```

    **Helper:**
    ```typescript
    function makeManager(): MemoryManager {
      return new MemoryManager({});
    }
    ```

    **Suite MCTX-02:**
    ```typescript
    describe('buildContext() — MCTX-02: top-k=5 sem threshold', () => {
      beforeEach(() => { vi.clearAllMocks(); });

      it('chama queryMemoriesByType 3 vezes com os tipos corretos e topK=5', async () => {
        const mgr = makeManager();
        await mgr.buildContext('teste de busca');

        const spy = mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>;
        expect(spy).toHaveBeenCalledTimes(3);
        expect(spy).toHaveBeenCalledWith('teste de busca', 'semantic', 5);
        expect(spy).toHaveBeenCalledWith('teste de busca', 'episodic', 5);
        expect(spy).toHaveBeenCalledWith('teste de busca', 'procedural', 5);
      });

      it('NÃO chama queryMemories (legado com threshold)', async () => {
        const mgr = makeManager();
        await mgr.buildContext('teste de busca');

        const legacySpy = mgr.vectors.queryMemories as ReturnType<typeof vi.fn>;
        expect(legacySpy).not.toHaveBeenCalled();
      });
    });
    ```

    **Suite MCTX-03 (teste de latência — delay 80ms):**
    ```typescript
    describe('buildContext() — MCTX-03: queries paralelas < 200ms', () => {
      it('completa em < 200ms com 80ms de delay por coleção (paralelo = ~80ms, sequencial = ~240ms)', async () => {
        const mgr = makeManager();

        // Substituir mock para adicionar 80ms de delay real por chamada
        (mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>).mockImplementation(
          async () => {
            await new Promise<void>((r) => setTimeout(r, 80));
            return [];
          },
        );

        const start = Date.now();
        await mgr.buildContext('latência test');
        const elapsed = Date.now() - start;

        // Paralelo: ~80ms. Sequencial: ~240ms. Margem: 199ms.
        expect(elapsed).toBeLessThan(200);
      });
    });
    ```

    **Suite MCTX-01 (ordem e headers pt-BR):**
    ```typescript
    describe('buildContext() — MCTX-01: ordem tiered e headers pt-BR', () => {
      beforeEach(() => { vi.clearAllMocks(); });

      it('retorna seções na ordem: Perfil → Semântica → Episódica → Procedural', async () => {
        const mgr = makeManager();

        (mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>).mockImplementation(
          async (_: string, type: string) => {
            if (type === 'semantic') return [{ id: 's1', document: 'fato semântico', similarity: 0.9 }];
            if (type === 'episodic') return [{ id: 'e1', document: 'evento episódico', similarity: 0.8 }];
            if (type === 'procedural') return [{ id: 'p1', document: 'how-to procedural', similarity: 0.7 }];
            return [];
          },
        );

        const ctx = await mgr.buildContext('teste');

        const iPerfil = ctx.indexOf('### Perfil do usuário');
        const iSem = ctx.indexOf('### Memórias semânticas');
        const iEpi = ctx.indexOf('### Memórias episódicas');
        const iPro = ctx.indexOf('### Memórias procedurais');

        expect(iPerfil).toBeGreaterThanOrEqual(0);
        expect(iSem).toBeGreaterThan(iPerfil);
        expect(iEpi).toBeGreaterThan(iSem);
        expect(iPro).toBeGreaterThan(iEpi);
      });

      it('omite seção quando queryMemoriesByType retorna [] para aquele tipo', async () => {
        const mgr = makeManager();

        (mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>).mockImplementation(
          async (_: string, type: string) => {
            if (type === 'semantic') return []; // vazio — deve ser omitido
            if (type === 'episodic') return [{ id: 'e1', document: 'evento', similarity: 0.8 }];
            return [];
          },
        );

        const ctx = await mgr.buildContext('teste');

        expect(ctx).not.toContain('### Memórias semânticas');
        expect(ctx).toContain('### Memórias episódicas');
      });

      it('retorna string vazia quando perfil e todas as typed collections estão vazias', async () => {
        const mgr = makeManager();
        (mgr.store.getProfileFacts as ReturnType<typeof vi.fn>).mockReturnValue([]);
        (mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>).mockResolvedValue([]);

        const ctx = await mgr.buildContext('vazio');
        expect(ctx).toBe('');
      });
    });
    ```

    **Suite MCTX-04 (backward compat e rollingSum):**
    ```typescript
    describe('buildContext() — MCTX-04: backward compatibility e rollingSum', () => {
      beforeEach(() => { vi.clearAllMocks(); });

      it('buildContext(userText) sem rollingSum resolve sem erro (call site legado)', async () => {
        const mgr = makeManager();
        await expect(mgr.buildContext('query legada')).resolves.not.toThrow();
      });

      it('insere rollingSum entre Perfil e Memórias semânticas', async () => {
        const mgr = makeManager();

        (mgr.vectors.queryMemoriesByType as ReturnType<typeof vi.fn>).mockImplementation(
          async (_: string, type: string) => {
            if (type === 'semantic') return [{ id: 's1', document: 'fato', similarity: 0.9 }];
            return [];
          },
        );

        const rolling = 'Resumo: falamos sobre bugs ontem.';
        const ctx = await mgr.buildContext('teste', rolling);

        const iPerfil = ctx.indexOf('### Perfil do usuário');
        const iRolling = ctx.indexOf(rolling);
        const iSem = ctx.indexOf('### Memórias semânticas');

        expect(iRolling).toBeGreaterThan(iPerfil);
        expect(iSem).toBeGreaterThan(iRolling);
      });
    });
    ```

    **Executar testes após criar o arquivo — TODOS devem FALHAR (RED):**
    ```bash
    cd apps/backend-ts && npx vitest run test/memory/manager-context.test.ts 2>&1 | tail -30
    ```

    Confirmar que os testes falham antes de prosseguir para Task 2.
  </action>

  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts 2>&1 | grep -E "(FAIL|pass|fail|×|✓)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - `test/memory/manager-context.test.ts` existe em disco
    - `grep -c "it('" apps/backend-ts/test/memory/manager-context.test.ts` retorna 8 ou mais (8 casos de teste)
    - `grep "queryMemoriesByType" apps/backend-ts/test/memory/manager-context.test.ts` retorna pelo menos 5 linhas
    - `grep "< 200" apps/backend-ts/test/memory/manager-context.test.ts` encontra o assert de latência
    - `grep "### Perfil do usuário" apps/backend-ts/test/memory/manager-context.test.ts` encontra o assert de header pt-BR
    - `grep "### Memórias semânticas" apps/backend-ts/test/memory/manager-context.test.ts` encontra o assert de header pt-BR
    - `grep "rollingSum" apps/backend-ts/test/memory/manager-context.test.ts` retorna pelo menos 3 linhas
    - Ao executar `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts`, os testes FALHAM (estado RED) — a implementação atual ainda não satisfaz os novos contratos
  </acceptance_criteria>

  <done>Arquivo manager-context.test.ts criado com 8+ testes. Todos falham contra a implementação atual de buildContext() (RED confirmado).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Refatorar buildContext() em manager.ts</name>
  <files>apps/backend-ts/src/memory/manager.ts</files>

  <read_first>
    - apps/backend-ts/src/memory/manager.ts (estado atual COMPLETO — ler antes de editar)
    - apps/backend-ts/test/memory/manager-context.test.ts (testes criados em Task 1 — são o contrato)
    - apps/backend-ts/src/memory/vectors.ts (assinatura de queryMemoriesByType — confirmar parâmetros)
  </read_first>

  <behavior>
    Ao finalizar, todos os 8+ testes de manager-context.test.ts devem passar (GREEN):
    - queryMemoriesByType chamado 3x com (userText, 'semantic'|'episodic'|'procedural', 5)
    - queryMemories legado NÃO chamado
    - Promise.all executa as 3 queries em paralelo (latência ~80ms com mock de 80ms/query)
    - Output segue a ordem: Perfil → rollingSum (opcional) → Semântica → Episódica → Procedural
    - Seções com array vazio são omitidas do output
    - buildContext(userText) sem rollingSum funciona (backward compatible)
    - buildContext(userText, rollingSum) insere o rolling summary na posição correta
  </behavior>

  <action>
    Editar SOMENTE o método buildContext() e os campos privados relacionados ao threshold em manager.ts. NÃO alterar nenhum outro método.

    **Remover:**
    - `private readonly recallThreshold: number;` — campo não mais usado
    - `this.recallThreshold = opts.recallThreshold ?? 0.5;` — no constructor
    - `recallThreshold?` do MemoryManagerOptions (ou manter como deprecated — OK manter interface para não quebrar call sites, mas o campo pode ficar sem efeito)

    **Substituir buildContext() completo pelo seguinte:**

    ```typescript
    /**
     * Assemble a context block with tiered memory retrieval (Phase 37 — MCTX-01 to MCTX-04).
     *
     * Order: Perfil do usuário → rolling summary (optional) → Memórias semânticas →
     *        Memórias episódicas → Memórias procedurais
     *
     * Queries 3 typed ChromaDB collections in parallel (Promise.all).
     * No similarity threshold — always returns top-5 per type (D-06).
     * Sections with no results are omitted from output (D-01).
     *
     * @param userText - Query text used for semantic retrieval
     * @param rollingSum - Optional rolling summary string (Phase 38 will provide this).
     *                     Appears between Perfil and typed memories when provided.
     * @returns Context string ready for system prompt, or '' if everything is empty.
     */
    async buildContext(userText: string, rollingSum?: string): Promise<string> {
      const facts = this.store.getProfileFacts();

      // Parallel queries for all 3 typed collections (MCTX-03 — D-07)
      const [semantic, episodic, procedural] = await Promise.all([
        this.vectors.queryMemoriesByType(userText, 'semantic', 5),
        this.vectors.queryMemoriesByType(userText, 'episodic', 5),
        this.vectors.queryMemoriesByType(userText, 'procedural', 5),
      ]);

      const parts: string[] = [];

      // Section 1: Perfil do usuário (D-03, D-04)
      if (facts.length > 0) {
        const lines = ['### Perfil do usuário'];
        for (const f of facts) {
          lines.push(`- ${f.key}: ${f.value}`);
        }
        parts.push(lines.join('\n'));
      }

      // Section 2: Rolling summary (Phase 38 interface — D-05)
      if (rollingSum) {
        parts.push(rollingSum);
      }

      // Section 3: Memórias semânticas (D-02, D-01)
      if (semantic.length > 0) {
        parts.push(this.formatMemoriesSection('### Memórias semânticas', semantic));
      }

      // Section 4: Memórias episódicas (D-02, D-01)
      if (episodic.length > 0) {
        parts.push(this.formatMemoriesSection('### Memórias episódicas', episodic));
      }

      // Section 5: Memórias procedurais (D-02, D-01)
      if (procedural.length > 0) {
        parts.push(this.formatMemoriesSection('### Memórias procedurais', procedural));
      }

      return parts.join('\n\n');
    }

    /** Format a typed memory section with header and bullet list. */
    private formatMemoriesSection(
      header: string,
      memories: import('./vectors.js').QueryResult[],
    ): string {
      const lines = [header];
      for (const m of memories) {
        lines.push(`- "${m.document}"`);
      }
      return lines.join('\n');
    }
    ```

    **Após editar, executar os testes (GREEN esperado):**
    ```bash
    cd apps/backend-ts && npx vitest run test/memory/manager-context.test.ts 2>&1 | tail -30
    ```

    Se algum teste falhar, investigar e corrigir antes de prosseguir.

    **Commit RED→GREEN:**
    ```
    ✅ test(37-P01): add RED tests for buildContext() tiered retrieval
    ♻️ refactor(37-P01): implement buildContext() with parallel typed queries
    ```
    (Dois commits separados — primeiro o arquivo de teste, depois a implementação)
  </action>

  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - `grep "Promise.all" apps/backend-ts/src/memory/manager.ts` retorna exatamente 1 linha dentro de buildContext()
    - `grep "queryMemoriesByType" apps/backend-ts/src/memory/manager.ts` retorna 3 linhas (semantic, episodic, procedural)
    - `grep "queryMemories(" apps/backend-ts/src/memory/manager.ts` — se retornar resultado, deve ser APENAS dentro de outros métodos, nunca dentro de buildContext()
    - `grep "rollingSum" apps/backend-ts/src/memory/manager.ts` retorna pelo menos 2 linhas (parâmetro + uso)
    - `grep "### Perfil do usuário" apps/backend-ts/src/memory/manager.ts` retorna 1 linha (header pt-BR)
    - `grep "### Memórias semânticas" apps/backend-ts/src/memory/manager.ts` retorna 1 linha
    - `grep "recallThreshold" apps/backend-ts/src/memory/manager.ts` — o campo privado foi removido do constructor (pode manter no MemoryManagerOptions como deprecated OK)
    - `grep "formatMemoriesSection" apps/backend-ts/src/memory/manager.ts` retorna 2+ linhas (definição + 3 chamadas)
    - Todos os testes do arquivo manager-context.test.ts passam (GREEN): `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts` — 0 falhas
    - Suite anterior de manager-typed.test.ts continua verde: `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-typed.test.ts` — 0 falhas (não quebramos nada)
  </acceptance_criteria>

  <done>buildContext() refatorado com Promise.all, headers pt-BR, rollingSum opcional. Todos os testes de manager-context.test.ts passam. Suite manager-typed.test.ts continua verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userText → embedding layer | O texto do usuário entra como parâmetro em queryMemoriesByType(), que o passa para embedText() — tratado como string opaca, sem execução de código |
| context string → LLM | O contexto montado por buildContext() é enviado ao LLM como dados, não como instruções de sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | buildContext() — userText → embedding | accept | embedText() via ONNX/Transformers.js trata o input como string opaca e gera vetor — não executa código arbitrário. Injeção de prompt no texto não afeta o embedding layer. |
| T-37-02 | Tampering | LLM prompt injection via context output | accept | Context é bare strings (não system directives). Headers como `### Memórias semânticas` são dados, não comandos. Pattern atual é seguro — não incluir user text raw em headers. |
| T-37-03 | Denial | buildContext() — Promise.all() com 3 queries | mitigate | queryMemoriesByType() já retorna [] em erro (Phase 36) — uma coleção lenta/indisponível não bloqueia o Promise.all completo. Timeout implícito via ChromaDB client. |
| T-37-04 | Disclosure | Context string — resultado combinado de 3 coleções | accept | Memórias são do próprio usuário em sistema single-user local. Não há risco de vazamento entre usuários. buildContext() retorna apenas dados já persistidos pelo próprio usuário. |
| T-37-05 | Denial | Unbounded context size | mitigate | top-k=5 por tipo + perfil + rollingSum = máximo ~20 itens. Output é bounded — nunca cresce sem controle. |
</threat_model>

<verification>
Ao final do plano 01, verificar:

1. `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts` — todos os testes passam (GREEN)
2. `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-typed.test.ts` — continua verde
3. `grep "Promise.all" apps/backend-ts/src/memory/manager.ts` — 1 ocorrência em buildContext()
4. `grep "queryMemoriesByType" apps/backend-ts/src/memory/manager.ts` — 3 ocorrências (semantic, episodic, procedural)
5. `grep "0.7" apps/backend-ts/src/memory/manager.ts` — 0 ocorrências (threshold removido)
</verification>

<success_criteria>
- manager-context.test.ts existe com 8+ casos cobrindo MCTX-01 a MCTX-04
- buildContext() em manager.ts usa Promise.all com 3 chamadas a queryMemoriesByType()
- Threshold 0.7 removido de buildContext()
- Headers em português: `### Perfil do usuário`, `### Memórias semânticas`, `### Memórias episódicas`, `### Memórias procedurais`
- Assinatura `buildContext(userText: string, rollingSum?: string): Promise<string>` — parâmetro opcional
- Seções vazias omitidas do output
- Todos os testes do plano passam (0 falhas)
- Suite manager-typed.test.ts continua verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/37-context-builder/37-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Resultado dos testes (count pass/fail)
- Decisões de implementação tomadas (nomes de helpers, etc.)
- Status de cada requisito MCTX-01 a MCTX-04
</output>
