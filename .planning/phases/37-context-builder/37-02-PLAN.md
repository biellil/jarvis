---
phase: 37-context-builder
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01-PLAN.md
files_modified:
  - apps/backend-ts/src/memory/manager.ts
autonomous: false
requirements:
  - MCTX-04

must_haves:
  truths:
    - "createRecallMemoryTool() em tools.ts chama buildContext(query) sem segundo argumento e o resultado é uma string não-vazia com os novos headers pt-BR"
    - "A suite completa de testes do backend passa sem nenhuma falha após a refatoração de buildContext()"
    - "Nenhum arquivo fora de manager.ts foi modificado"
  artifacts:
    - path: "apps/backend-ts/src/memory/manager.ts"
      provides: "buildContext() finalizado com backward compatibility verificada"
      exports: ["MemoryManager", "MemoryManagerOptions"]
  key_links:
    - from: "apps/backend-ts/src/session/tools.ts:createRecallMemoryTool()"
      to: "apps/backend-ts/src/memory/manager.ts:buildContext()"
      via: "await memory.buildContext(query) — sem segundo argumento"
      pattern: "buildContext\\(query\\)"
---

<objective>
Verificar que a refatoração de buildContext() de Phase 37-01 não quebrou nenhum call site existente. Executar a suite completa de testes e confirmar backward compatibility.

Purpose: MCTX-04 exige que streaming SSE, voice handler e CLI continuem funcionando sem nenhuma modificação. Este plano valida esse contrato.

Output: Suite completa de testes verde + checkpoint de verificação humana.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-context-builder/37-CONTEXT.md
@.planning/phases/37-context-builder/37-01-SUMMARY.md

<!-- Call sites que não devem ser modificados -->
@apps/backend-ts/src/session/tools.ts
@apps/backend-ts/src/memory/manager.ts
</context>

<interfaces>
<!-- Call site em produção — deve continuar funcionando SEM alteração -->

De apps/backend-ts/src/session/tools.ts:
```typescript
export function createRecallMemoryTool(memory: MemoryManager) {
  return tool(
    async ({ query }: { query: string }): Promise<string> => {
      try {
        const ctx = await memory.buildContext(query); // sem rollingSum — must still work
        if (ctx === '') {
          return EMPTY_FALLBACK;
        }
        return ctx;
      } catch (exc) {
        return `Erro ao buscar memórias: ${(exc as Error).message}`;
      }
    },
    // ...
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Executar suite completa e verificar ausência de regressões</name>
  <files>apps/backend-ts/src/memory/manager.ts</files>

  <read_first>
    - apps/backend-ts/src/memory/manager.ts (verificar estado atual pós-37-01 — confirmar refatoração está aplicada)
    - apps/backend-ts/src/session/tools.ts (call site que não deve ter sido alterado)
    - apps/backend-ts/test/session/tool-dispatch.test.ts (testes de ferramentas do agente)
    - apps/backend-ts/test/memory/manager-typed.test.ts (verificar se MockMemoryVectors inclui queryMemoriesByType)
  </read_first>

  <action>
    Executar a suite completa de testes do backend para detectar qualquer regressão:

    ```bash
    cd /root/jarvis/apps/backend-ts && npx vitest run 2>&1
    ```

    **Se algum teste falhar:**

    1. Identificar qual arquivo de teste falhou e qual assertion quebrou
    2. Verificar se o mock de MemoryVectors em outros testes inclui `queryMemoriesByType` — se não incluir, adicionar ao mock da classe:
       ```typescript
       queryMemoriesByType = vi.fn().mockResolvedValue([]);
       ```
    3. Verificar se algum teste usava os headers em inglês (`### User profile`, `### Recall from past conversations`) — se sim, atualizar para os novos headers pt-BR (`### Perfil do usuário`) NOS TESTES (NÃO no código de produção)
    4. NÃO alterar manager.ts — apenas corrigir testes que dependiam de comportamento do legado

    **Verificar especificamente:**
    - `test/session/tool-dispatch.test.ts` — inclui mock de memory.buildContext?
    - `test/session/chat-session-tools.test.ts` — usa MemoryManager diretamente?
    - `test/memory/manager-typed.test.ts` — o mock de MemoryVectors inclui queryMemoriesByType?

    Se o mock em manager-typed.test.ts não incluir `queryMemoriesByType`, adicionar a linha:
    ```typescript
    queryMemoriesByType = vi.fn().mockResolvedValue([]);
    ```
    dentro do `class MockMemoryVectors` nesse arquivo.

    **Após correções (se necessárias), executar novamente:**
    ```bash
    cd /root/jarvis/apps/backend-ts && npx vitest run 2>&1 | tail -20
    ```

    Confirmar que a saída mostra 0 falhas antes de prosseguir para o checkpoint.
  </action>

  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npx vitest run 2>&1 | grep -E "(Tests|pass|fail|FAIL|error)" | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `cd /root/jarvis/apps/backend-ts && npx vitest run 2>&1 | grep "fail"` — retorna linha dizendo "0 failed"
    - `cd /root/jarvis/apps/backend-ts && npx vitest run test/memory/manager-context.test.ts 2>&1 | grep "fail"` — 0 failed
    - `grep "buildContext" apps/backend-ts/src/session/tools.ts` — arquivo NÃO foi modificado (exatamente como antes)
    - `grep -n "queryMemories(" apps/backend-ts/src/memory/manager.ts` — se retornar resultado dentro de buildContext(), é FALHA (queryMemories legado não deve aparecer em buildContext)
    - `grep "this\.recallThreshold" apps/backend-ts/src/memory/manager.ts` — retorna vazio (campo privado removido)
  </acceptance_criteria>

  <done>Suite completa passa sem falhas. Call sites existentes não foram modificados. Manager.ts tem buildContext() refatorado e funcionando.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificação humana — implementação de buildContext() tiered</name>
  <files>apps/backend-ts/src/memory/manager.ts</files>

  <read_first>
    - apps/backend-ts/src/memory/manager.ts (confirmar implementação final)
  </read_first>

  <what-built>
    buildContext() refatorado para:
    - 3 queries paralelas via Promise.all() para coleções typed (semantic, episodic, procedural)
    - Headers em pt-BR: `### Perfil do usuário`, `### Memórias semânticas`, `### Memórias episódicas`, `### Memórias procedurais`
    - Parâmetro opcional `rollingSum?` para Phase 38
    - Threshold 0.7 removido — top-5 incondicional por tipo
    - Seções vazias omitidas
    - Backward compatible com todos os call sites
  </what-built>

  <how-to-verify>
    **Verificação por inspeção de código (rápida):**
    1. Abrir `apps/backend-ts/src/memory/manager.ts`
    2. Confirmar que buildContext() contém `Promise.all([`
    3. Confirmar que os 3 headers em pt-BR estão presentes: `### Perfil do usuário`, `### Memórias semânticas`, `### Memórias episódicas`, `### Memórias procedurais`
    4. Confirmar que `0.7` não aparece dentro de buildContext()
    5. Confirmar que a assinatura é `buildContext(userText: string, rollingSum?: string): Promise<string>`
    6. Confirmar que tools.ts NÃO foi alterado: `grep "buildContext(query)" apps/backend-ts/src/session/tools.ts`
  </how-to-verify>

  <action>
    Ponto de verificação — nenhuma ação automática. O executor deve apresentar ao usuário o resumo do que foi implementado e aguardar aprovação antes de criar o SUMMARY.md final.
  </action>

  <verify>
    <automated>grep -n "Promise.all\|rollingSum\|### Perfil do usuário\|### Memórias semânticas" /root/jarvis/apps/backend-ts/src/memory/manager.ts</automated>
  </verify>

  <done>Usuário aprovou a implementação. Phase 37 completa.</done>

  <acceptance_criteria>
    - `grep "Promise.all" apps/backend-ts/src/memory/manager.ts` retorna 1 linha em buildContext()
    - `grep "rollingSum" apps/backend-ts/src/memory/manager.ts` retorna pelo menos 2 linhas
    - `grep "### Perfil do usuário" apps/backend-ts/src/memory/manager.ts` retorna 1 linha
    - `grep "### Memórias semânticas" apps/backend-ts/src/memory/manager.ts` retorna 1 linha
    - Usuário confirma aprovação via "aprovado"
  </acceptance_criteria>

  <resume-signal>Digite "aprovado" se a implementação está correta, ou descreva qualquer problema encontrado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suite de testes → manager.ts | Testes validam o contrato público de buildContext() — mocks garantem isolamento de ChromaDB real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06 | Tampering | test mock coverage — MockMemoryVectors sem queryMemoriesByType | mitigate | Verificar que todos os mocks de MemoryVectors em outros arquivos de teste incluem `queryMemoriesByType = vi.fn().mockResolvedValue([])` — evita "method not found" silencioso |
| T-37-07 | Denial | Suite de testes — regressão silenciosa em call sites | mitigate | Executar suite completa (npx vitest run) não apenas manager-context.test.ts. Verificar 0 falhas em todos os arquivos de teste. |
</threat_model>

<verification>
Suite completa de testes verde:
```bash
cd /root/jarvis/apps/backend-ts && npx vitest run 2>&1 | tail -5
```

Backward compatibility confirmada:
```bash
grep "buildContext" /root/jarvis/apps/backend-ts/src/session/tools.ts
# Deve mostrar: await memory.buildContext(query) — sem segundo argumento
```

Implementação verificada:
```bash
grep -n "Promise.all\|queryMemoriesByType\|rollingSum\|### Perfil" /root/jarvis/apps/backend-ts/src/memory/manager.ts
```
</verification>

<success_criteria>
- Suite completa `npx vitest run` passa com 0 falhas
- tools.ts não foi modificado
- buildContext() tem `Promise.all([` na implementação
- Parâmetro `rollingSum?` presente e opcional
- Headers em pt-BR no output
- Phase 37 pronta para ser marcada como completa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/37-context-builder/37-02-SUMMARY.md` com:
- Resultado final da suite completa (N testes, 0 falhas)
- Arquivos corrigidos (se algum mock precisou de update)
- Confirmação de backward compatibility
- Status final MCTX-01 a MCTX-04 (todos completos)
</output>
