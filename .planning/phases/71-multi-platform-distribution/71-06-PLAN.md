---
phase: 71
plan: 06
type: execute
wave: 3
depends_on:
  - 71-01
  - 71-02
  - 71-03
  - 71-04
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-05
tags:
  - docs
  - readme
  - i18n-pt-br

must_haves:
  truths:
    - "README.md contém seção '## Build & Install' antes de '## Problemas comuns'"
    - "Seção tem 6 sub-blocos: Build local, Onde os binários ficam, Windows install, macOS install, Linux install, .env config"
    - "SmartScreen workflow documentado exatamente: 'More info' → 'Run anyway'"
    - "Gatekeeper workflow Sequoia 15+ documentado: System Settings → Privacy & Security → Open Anyway"
    - "Linux AppImage instructions cover libfuse2t64 + --no-sandbox para Ubuntu 24.04"
    - "Seção 'Pré-requisitos' tem bullet 'wine + mono-devel' para cross-build Win em Linux"
    - "Termos técnicos (NSIS, AppImage, Gatekeeper, SmartScreen, FUSE) ficam em inglês — texto narrativo em pt-BR"
  artifacts:
    - path: "README.md"
      provides: "Build & Install section (DIST-05 acceptance criterion)"
      contains: "## Build & Install"
  key_links:
    - from: "README.md §Build & Install"
      to: "pnpm dist:* commands"
      via: "Documented command list per platform"
      pattern: "pnpm dist"
    - from: "README.md §Build & Install"
      to: "userData/.env paths per OS"
      via: "Table mapping Windows/macOS/Linux paths"
      pattern: "%APPDATA%|Library/Application Support|.config/JARVIS"
---

<objective>
Documentar build local (`pnpm dist:*`) e instalação per OS (Windows NSIS+portable, macOS DMG, Linux AppImage) no README.md em pt-BR, com workflows precisos para SmartScreen e Gatekeeper bypass (research §State of the Art + §Pitfalls), tratativa para Ubuntu 24.04 (libfuse2t64 + --no-sandbox), e localização do `.env` em userData per OS.

Purpose: DIST-05 é o último success criterion do roadmap — sem README atualizado, user não tem instruções para usar os binários gerados pelos Plans 04 + 05. Também serve como cheat sheet para o próprio user em uso futuro (não-Claude readers).

Output: 1 nova seção `## Build & Install` no README + 1 bullet adicional em `## Pré-requisitos`. Sem screenshots (D-19). pt-BR com termos técnicos em inglês.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/71-multi-platform-distribution/71-CONTEXT.md
@.planning/phases/71-multi-platform-distribution/71-RESEARCH.md
@.planning/phases/71-multi-platform-distribution/71-04-SUMMARY.md
@.planning/phases/71-multi-platform-distribution/71-05-SUMMARY.md

<interfaces>
<!-- README current structure (heading positions confirmed): -->

Heading map in README.md (current state, 307 lines):
- Line 1: "# JARVIS — Just A Rather Very Intelligent System"
- Line 9: "## Arquitetura"
- Line 30: "## Stack"
- Line 45: "## Pré-requisitos" ← INSERT bullet "wine + mono-devel" here (D-20)
- Line 56: "## Platform Support"
- Line 85: "## Setup inicial"
- Line 117: "## Rodando em dev"
- Line 161: "## Validação rápida (sem abrir Electron)"
- Line 176: "## Estrutura do projeto"
- Line 224: "## Problemas comuns" ← INSERT new section "## Build & Install" BEFORE this line
- Line 253: "## Desenvolvimento"
- Line 285: "## Soak Test (Validação de Release)"
- Line 305: "## Licença"

Existing "## Pré-requisitos" structure (lines 45-54):
```
## Pré-requisitos

- **Node 22+** (LTS)
- **pnpm 9+**
- **LM Studio** rodando com um modelo carregado (ou API key de OpenAI/Anthropic)
- **ffmpeg** (pro pipeline de voz — `apt install ffmpeg` / `brew install ffmpeg` / choco windows)
- **ChromaDB** (opcional, só pra memory + voice — `pip install chromadb`)
- Linux (Mac/Windows funcionam mas alguns componentes — como PC tools executor — são Linux-only por ora)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar bullet "wine + mono-devel" em §Pré-requisitos (D-20)</name>
  <files>
    README.md
  </files>
  <read_first>
    - README.md §"Pré-requisitos" (lines 45-54)
    - .planning/phases/71-multi-platform-distribution/71-CONTEXT.md D-20 (Pré-requisitos update)
  </read_first>
  <action>
    Find the existing "## Pré-requisitos" bullet list (lines 47-53) and INSERT one new bullet IMMEDIATELY AFTER the line `- Linux (Mac/Windows funcionam mas alguns componentes — como PC tools executor — são Linux-only por ora)`. The new bullet:

    ```
    - **Para gerar binários Windows em Linux:** `wine` + `mono-devel` + `libfuse2t64` (Ubuntu 24.04 Noble: habilitar i386 antes via `sudo dpkg --add-architecture i386 && sudo apt update`). Veja §Build & Install para instalação completa.
    ```

    Use the Edit tool with anchor on the previous line for unambiguous match.

    After this task, the Pré-requisitos section has 7 bullets (was 6), with the new bullet linking forward to the new Build & Install section.
  </action>
  <verify>
    <automated>grep -q "wine.*mono-devel.*libfuse2t64" README.md && grep -B 2 "wine.*mono-devel" README.md | grep -q "Linux-only"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "Para gerar binários Windows em Linux" README.md` returns 0
    - `grep -q "wine.*mono-devel" README.md` returns 0
    - `grep -q "libfuse2t64" README.md` returns 0
    - `grep -q "dpkg --add-architecture i386" README.md` returns 0
    - The new bullet is between the existing "Linux (Mac/Windows funcionam..." bullet and the start of section "## Platform Support" (verify via `awk '/^## Pré-requisitos/,/^## Platform Support/' README.md` shows expected ordering)
  </acceptance_criteria>
  <done>§Pré-requisitos has one new bullet covering cross-build deps. Forward-link to Build & Install present.</done>
</task>

<task type="auto">
  <name>Task 2: Inserir nova seção "## Build & Install" antes de "## Problemas comuns" (D-18, D-19)</name>
  <files>
    README.md
  </files>
  <read_first>
    - README.md (full current state)
    - .planning/phases/71-multi-platform-distribution/71-RESEARCH.md §"Code Examples — README §Build & Install (D-18)" (full skeleton — copy verbatim with minor adjustments)
    - .planning/phases/71-multi-platform-distribution/71-CONTEXT.md D-18, D-19 (section structure, no screenshots, pt-BR with English technical terms)
    - .planning/phases/71-multi-platform-distribution/71-RESEARCH.md §"Common Pitfalls" #2, #3, #5 (Linux libfuse2t64, --no-sandbox, macOS Sequoia 15+)
    - .planning/phases/71-multi-platform-distribution/71-04-SUMMARY.md (if exists — Linux build smoke notes)
    - .planning/phases/71-multi-platform-distribution/71-05-SUMMARY.md (if exists — Windows build + UAT notes)
  </read_first>
  <action>
    Insert a new section IMMEDIATELY BEFORE the line `## Problemas comuns` (currently at line 224 — but line numbers may shift after Task 1). Use anchor `## Problemas comuns` for unambiguous Edit.

    The new section content (verbatim from RESEARCH §Code Examples — README skeleton, with light adjustments to use placeholders for version like `<version>` so the doc doesn't go stale, and to include all 3 pitfall workflows):

    ```markdown
    ## Build & Install

    Como gerar e instalar binários distribuíveis do JARVIS em Windows, macOS e Linux. Distribuição usa `electron-builder` e roda local (sem CI/GitHub Actions em v3.1).

    ### Build local

    Gera binários para uma plataforma específica a partir do monorepo:

    ```bash
    pnpm dist          # gera para o OS atual (Linux=AppImage, macOS=DMG, Windows=NSIS+portable)
    pnpm dist:linux    # Linux AppImage (requer host Linux)
    pnpm dist:win      # Windows NSIS installer + portable (Linux host com wine, ou Windows host)
    pnpm dist:mac      # macOS DMG universal arm64+x64 (requer host macOS 12+)
    ```

    Cada comando roda um pre-flight script (`scripts/preflight-dist.mjs`) que valida deps antes de chamar `electron-builder`.

    **Pré-requisito para cross-build Windows em Linux (Ubuntu 24.04 Noble):**

    ```bash
    sudo dpkg --add-architecture i386
    sudo apt update
    sudo apt install -y wine wine32 wine64 mono-devel libfuse2t64
    wine --version    # esperado: wine-9.x
    ```

    > `mono-devel` é **opcional** — NSIS funciona sem (NSIS só precisa de wine). `mono-devel` só é exigido para o target `Squirrel.Windows` (não usado por JARVIS). `libfuse2t64` é necessário para rodar AppImages no próprio host de dev.

    > macOS DMG só pode ser buildado em macOS 12+ (limitação Apple — Codesign tooling é Mac-only). Em Linux/Windows host, `pnpm dist:mac` falha-fast com mensagem clara.

    ### Onde os binários ficam

    Após `pnpm dist:*`, artifacts vão para `apps/desktop/release-v2/`:

    | Target | Arquivo (padrão) |
    |--------|------------------|
    | Windows NSIS installer | `JARVIS Setup <version>.exe` |
    | Windows portable | `JARVIS <version>.exe` |
    | macOS DMG universal | `JARVIS-<version>-universal.dmg` |
    | Linux AppImage | `JARVIS-<version>.AppImage` |

    ### Instalação no Windows

    **NSIS installer (`JARVIS Setup <version>.exe`):**

    1. Duplo-clique em `JARVIS Setup <version>.exe`.
    2. **SmartScreen warning** (esperado — binário não é code-signed em v3.1):
       - Windows 11 24H2 mostra "Microsoft Defender SmartScreen prevented an unrecognized app from starting"
       - Clique em **More info**
       - Clique em **Run anyway**
    3. Instalador roda automático (one-click). JARVIS instala em `%APPDATA%\Local\Programs\JARVIS\` (per-user, sem prompt admin) e cria atalhos no Menu Iniciar e Área de Trabalho.
    4. **Desinstalação:** Painel de Controle → Apps & Features → "JARVIS" → Uninstall.

    **Portable (`JARVIS <version>.exe`):**

    1. Duplo-clique no `.exe` portable (qualquer pasta, USB, pendrive — não precisa instalar).
    2. Mesmo warning SmartScreen — "More info" → "Run anyway".
    3. JARVIS executa diretamente, sem instalação e sem privilégios admin. Settings persistem em `%APPDATA%\JARVIS\` (mesmo caminho da versão instalada — portable e installed compartilham config).

    ### Instalação no macOS

    1. Duplo-clique em `JARVIS-<version>-universal.dmg` para montar.
    2. Arraste `JARVIS.app` para `/Applications`.
    3. **Gatekeeper bloqueia primeira execução** (binário não-signed):
       - **macOS 15 Sequoia ou mais novo:** após a primeira tentativa falhar com "JARVIS cannot be opened because Apple cannot check it for malicious software", vá em **System Settings → Privacy & Security**, role até o fim da página, clique no botão **Open Anyway** que aparece para JARVIS, confirme no diálogo seguinte.
       - **macOS 12-14 (Monterey / Ventura / Sonoma):** right-click em `JARVIS.app` no Finder → **Open** → **Open** no diálogo de confirmação.
    4. Primeira execução pede permissão de microfone — conceda em **System Settings → Privacy & Security → Microphone**.

    ### Instalação no Linux

    ```bash
    chmod +x JARVIS-<version>.AppImage
    ./JARVIS-<version>.AppImage --no-sandbox
    ```

    **A flag `--no-sandbox` é necessária em Ubuntu 24.04+ (AppArmor 4.0 hardening).** Em distros mais antigas (Ubuntu 22.04 LTS, Fedora 38) a flag é opcional — o AppImage roda sem ela. Detalhes em [pitfall AppArmor user namespaces](https://tamim.blog/post/fix-appimage-sandbox-issues-ubuntu-24-04/).

    **Ubuntu 24.04 Noble — erro `dlopen(): libfuse.so.2`:**

    ```bash
    sudo apt install libfuse2t64
    ```

    Alternativa sem instalar nada: `./JARVIS-<version>.AppImage --appimage-extract-and-run` (extrai squashfs para `/tmp/.mount_xxx`, dispensa FUSE).

    ### Configuração pós-install (`.env`)

    Após primeira execução, JARVIS cria automaticamente um `.env` baseado em `.env.example`. Local por OS:

    | OS | Caminho do `.env` |
    |----|-------------------|
    | Windows | `%APPDATA%\JARVIS\.env` |
    | macOS | `~/Library/Application Support/JARVIS/.env` |
    | Linux | `~/.config/JARVIS/.env` (respeita `$XDG_CONFIG_HOME`) |

    Em Linux/macOS, o arquivo `.env` é criado com permissão `0o600` (owner-only — outros usuários do sistema não podem ler API keys).

    **Atalho:** click no ícone do JARVIS na bandeja do sistema (tray) e selecione **"Abrir .env"** — abre o file manager do OS na pasta correta com o `.env` selecionado.

    Edite `LLM_PROVIDER`, `OPENAI_API_KEY`, `LM_STUDIO_URL`, etc., e **reinicie o JARVIS** para aplicar mudanças (v3.1 não tem hot-reload — restart é literal).

    ### Limitações conhecidas (v3.1)

    - Binários **não são code-signed** — daí SmartScreen warning em Windows e Gatekeeper bypass em macOS. Code signing foi deferido para v3.2 (DIST-FUT-02).
    - **Não há auto-update** — atualização requer rebuild manual + reinstall. Deferido para v3.2 (DIST-FUT-01).
    - **macOS DMG só builda em macOS 12+** — limitação Apple. Linux/Windows host falha-fast com mensagem clara.
    - **Bundle size grande (~2-2.5 GB)** — JARVIS embute Whisper base + medium models para STT offline funcionar sem download adicional. Pode crescer no futuro com mais models on-demand.
    ```

    **Important:** the section uses **literal backticks** for code blocks. In Markdown source these are 3 backticks before and after each block. Do NOT escape them.

    After this task, `## Build & Install` is at the position previously occupied by `## Problemas comuns`, and `## Problemas comuns` shifts down. Section order is now:
    - ... (existing sections through Estrutura do projeto)
    - `## Build & Install` (NEW)
    - `## Problemas comuns`
    - ... (rest unchanged)
  </action>
  <verify>
    <automated>grep -q "^## Build & Install" README.md && grep -q "SmartScreen" README.md && grep -q "Gatekeeper" README.md && grep -q "libfuse2t64" README.md && grep -q "More info.*Run anyway" README.md && grep -q "System Settings.*Privacy.*Security.*Open Anyway" README.md && grep -q "%APPDATA%\\\\JARVIS\\\\\\.env" README.md && grep -q "~/.config/JARVIS/.env" README.md && [ $(awk '/^## Build & Install/{print NR; exit}' README.md) -lt $(awk '/^## Problemas comuns/{print NR; exit}' README.md) ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^## Build & Install" README.md` returns 0
    - `grep -q "SmartScreen" README.md` returns 0
    - `grep -q "More info" README.md` returns 0 AND `grep -q "Run anyway" README.md` returns 0 (exact SmartScreen workflow per Plan 71-05 Task 3 wording)
    - `grep -q "Gatekeeper" README.md` returns 0
    - `grep -q "Open Anyway" README.md` returns 0 (Sequoia 15+ workflow)
    - `grep -q "right-click" README.md` returns 0 (macOS 12-14 alternative)
    - `grep -q "libfuse2t64" README.md` returns 0
    - `grep -q -- "--no-sandbox" README.md` returns 0
    - `grep -q -- "--appimage-extract-and-run" README.md` returns 0
    - `grep -q "%APPDATA%" README.md` returns 0 (Windows .env path)
    - `grep -q "Library/Application Support/JARVIS" README.md` returns 0 (macOS .env path)
    - `grep -q "~/.config/JARVIS" README.md` returns 0 (Linux .env path)
    - `grep -q "Abrir .env" README.md` returns 0 (tray menu reference)
    - `grep -q "0o600" README.md` returns 0 (POSIX permissions documentation — references Plan 71-01 D-08)
    - `grep -q "DIST-FUT-01" README.md` returns 0 OR `grep -q "auto-update" README.md` returns 0 (limitations callout)
    - `grep -q "DIST-FUT-02" README.md` returns 0 OR `grep -q "code-signed" README.md` returns 0
    - Order check: line# of "## Build & Install" < line# of "## Problemas comuns" (validated by awk in automated verify)
    - NO screenshots referenced (`grep -c "\\!\\[" README.md` — count should be unchanged from pre-Plan-06)
    - Pré-requisitos bullet from Task 1 still present
  </acceptance_criteria>
  <done>README has new "## Build & Install" section placed before "## Problemas comuns" with all 6 sub-sections (Build local, Onde, Windows, macOS, Linux, .env config + Limitações). All SmartScreen/Gatekeeper/libfuse2t64/--no-sandbox workflows documented. Termos técnicos em inglês, narrativa em pt-BR.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README documentation ↔ user trust | User follows README to bypass SmartScreen / Gatekeeper. Documentation accuracy affects user's security decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Repudiation | Unsigned installer executes (SmartScreen/Gatekeeper warnings) | accept (with documentation) | README explicitly warns user that binaries are unsigned and walks through the exact bypass workflow. User makes informed opt-in. DIST-FUT-02 (code signing) deferred to v3.2. |
| T-71-04 | Tampering | AppImage `--no-sandbox` flag bypasses Chrome sandbox on Ubuntu 24.04 | accept (with documentation) | README documents the flag's necessity and provides safer alternatives (apparmor profile, --appimage-extract-and-run). User opt-in. |
| T-71-14 | Information Disclosure | README accidentally leaks internal paths/secrets | accept | Manual review — README contains no sensitive paths beyond %APPDATA%, ~/Library, ~/.config which are public OS conventions. No API keys, no internal URLs. |

</threat_model>

<verification>
**Wave 3 (Plan 71-06):**

1. Task 1 acceptance criteria all pass (wine + mono-devel + libfuse2t64 bullet added)
2. Task 2 acceptance criteria all pass (Build & Install section with 6 sub-blocks)
3. `awk '/^## /{print}' README.md` shows section order:
   - `## Pré-requisitos` (modified)
   - ...
   - `## Build & Install` (NEW)
   - `## Problemas comuns`
4. `markdownlint` or similar lint, if configured, passes — but not required (no markdownlint config in repo currently).
5. Manual smoke read: section reads coherently in pt-BR, technical terms in English (NSIS, SmartScreen, Gatekeeper, AppImage, FUSE, ASAR, ACL).

**DIST-05 success criterion met:** README documents build (`pnpm dist`) and install per platform + SmartScreen/Gatekeeper warnings + .env config.
</verification>

<success_criteria>
- README.md has new "## Build & Install" section between "## Estrutura do projeto" and "## Problemas comuns"
- Section has 6 sub-sections (Build local, Onde os binários ficam, Windows, macOS, Linux, .env config) plus Limitações conhecidas
- SmartScreen workflow: "More info" → "Run anyway" (exact wording from research)
- Gatekeeper Sequoia 15+ workflow: System Settings → Privacy & Security → Open Anyway
- Gatekeeper Monterey/Ventura/Sonoma workflow: right-click → Open → Open
- Ubuntu 24.04 quirks: libfuse2t64 + --no-sandbox documented
- AppImage extract-and-run fallback documented
- `.env` paths per OS in a table (Windows, macOS, Linux)
- Tray "Abrir .env" mention
- 0o600 permission hardening callout for Linux/macOS
- Limitations section flags unsigned binaries + no auto-update + bundle size
- §Pré-requisitos has new bullet linking to §Build & Install
- All technical terms in English (NSIS, AppImage, Gatekeeper, SmartScreen, FUSE)
- No screenshots
- DIST-05 success criterion met
</success_criteria>

<output>
After completion, create `.planning/phases/71-multi-platform-distribution/71-06-SUMMARY.md` covering:
- DIST-05 status: completed (README updated with full Build & Install section)
- Decisions exercised: D-18 (section structure, position before Problemas comuns), D-19 (pt-BR with English technical terms, no screenshots), D-20 (Pré-requisitos bullet)
- Threats addressed: T-71-03 documented + accepted; T-71-04 documented + accepted; T-71-14 reviewed
- Files modified: README.md (1 bullet added to §Pré-requisitos + new section before §Problemas comuns)
- Cross-references baked in: Plan 71-05 SmartScreen exact wording, Plan 71-04 --no-sandbox necessity, Plan 71-01 chmod 0o600 hardening
- Phase 71 completion checklist: DIST-01 (P05), DIST-02 (P05), DIST-03 (config + README, build deferred to user's Mac), DIST-04 (P04 + this README), DIST-05 (this plan)
- Suggested next step for v3.2 plant-seed: add screenshots if README is shared publicly; add markdownlint config; document GitHub Releases manual upload workflow
</output>
