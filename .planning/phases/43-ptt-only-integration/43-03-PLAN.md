---
phase: 43
plan: 03
type: execute
wave: 2
depends_on: [43-01, 43-02]
files_modified:
  - apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts
  - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts
  - apps/desktop/src/shared/ipc-types.ts
autonomous: true
requirements:
  - VPTT-03
must_haves:
  truths:
    - "AlwaysListeningStrategy expõe método público forceFlush() que envia IPC `always-listening:force-flush` ao renderer quando status === 'capturing' AND inFlight === false"
    - "forceFlush() é no-op silencioso (zero side-effect, zero log) quando status === 'idle' ou status === 'processing' ou inFlight === true"
    - "AlwaysListeningStrategy.start() subscreve pttHotkeyEmitter 'toggle' com referência estável `private boundOnPttToggle` que invoca this.forceFlush() (D-02)"
    - "AlwaysListeningStrategy.stop() desinscreve pttHotkeyEmitter 'toggle' usando a mesma referência — listenerCount após stop === 0"
    - "Constante IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH adicionada com value 'always-listening:force-flush' (main → renderer)"
    - "Cleanup ordem em stop(): primeiro unsubscribe pttHotkeyEmitter, depois unsubscribe ALWAYS_LISTENING_UTTERANCE — ambos com referência estável"
    - "Tests existentes em alwaysListening.test.ts (Phase 40) continuam verdes — só adicionamos describes, nada removido"
    - "VPTT-03 main-side está completo; renderer-side wiring (hook React subscribindo ao IPC) é declarado out-of-scope com note no SUMMARY"
  artifacts:
    - path: "apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts"
      provides: "forceFlush() método + listener pttHotkeyEmitter 'toggle' no lifecycle"
      contains: "forceFlush\\(\\): void"
      min_lines: 220
    - path: "apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts"
      provides: "Testes para forceFlush() comportamento por estado + listener subscribe/unsubscribe"
      contains: "describe.*VPTT-03"
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "Constante IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH"
      contains: "ALWAYS_LISTENING_FORCE_FLUSH"
  key_links:
    - from: "apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts"
      to: "apps/desktop/src/main/ptt-hotkey.ts (Plan 02)"
      via: "import { pttHotkeyEmitter } + pttHotkeyEmitter.on('toggle', this.boundOnPttToggle)"
      pattern: "pttHotkeyEmitter\\.on\\('toggle'"
    - from: "apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (forceFlush)"
      to: "renderer (futuro hook useAlwaysListening)"
      via: "this.deps.mainWindow.webContents.send(IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH)"
      pattern: "ALWAYS_LISTENING_FORCE_FLUSH"
---

<objective>
Implementar VPTT-03 (AL → PTT hotkey override) main-side: estender `AlwaysListeningStrategy` com método `forceFlush()` + listener no `pttHotkeyEmitter` (Plan 02 Task 1) que invoca o force-flush. Comportamento por estado (D-02): `capturing` + samples > 0 → envia IPC, demais estados → no-op silencioso. Detecção de "0 samples" fica no renderer (engine tem visibilidade do ring buffer).

Purpose: Permitir que usuário em modo Always-Listening pressione a hotkey PTT para fechar imediatamente o utterance — sem esperar VAD silence threshold. Usado para ditado curto onde o usuário sabe que terminou. Comportamento DEFENSIVO: nada de no-op verbose, nada de log para "tecla quebrou" — silêncio absoluto fora de capturing.
Output: AlwaysListeningStrategy modificada (não substituída) + IPC channel novo + testes que validam comportamento por estado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-ptt-only-integration/43-CONTEXT.md
@.planning/phases/43-ptt-only-integration/43-RESEARCH.md
@.planning/phases/43-ptt-only-integration/43-01-PLAN.md
@.planning/phases/43-ptt-only-integration/43-02-PLAN.md
@apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts
@apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts
@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/main/ptt-hotkey.ts

<interfaces>
<!-- Existing AlwaysListeningStrategy contract — extending without breaking -->

From apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts:71-189:
```typescript
export class AlwaysListeningStrategy implements VoiceCaptureStrategy {
  private status: 'idle' | 'capturing' | 'processing' = 'idle';
  private inFlight = false;  // true durante processUtterance (entre 'capturing' bursts)
  private utteranceListener: ((event, payload) => void) | null = null;

  constructor(deps: AlwaysListeningStrategyDeps) {}
  async start(): Promise<void>;  // registra utteranceListener + envia ALWAYS_LISTENING_START
  private async processUtterance(payload): Promise<void>;
  async stop(): Promise<void>;   // remove listener + envia ALWAYS_LISTENING_STOP
  async dispose(): Promise<void>;  // = stop()
  getStatus(): 'idle' | 'capturing' | 'processing';
}
```

From apps/desktop/src/shared/ipc-types.ts:208-218 (existing channels):
```typescript
ALWAYS_LISTENING_START: 'always-listening:start',
ALWAYS_LISTENING_STOP: 'always-listening:stop',
ALWAYS_LISTENING_UTTERANCE: 'always-listening:utterance',
ALWAYS_LISTENING_VAD_THRESHOLD: 'always-listening:vad-threshold',
VOICE_MODE_DEGRADED: 'voiceMode:degraded',
VOICE_MODE_SWITCH_RESULT: 'voice-mode:switch-result',
```

From apps/desktop/src/main/ptt-hotkey.ts (Plan 02 Task 1):
```typescript
export const pttHotkeyEmitter: EventEmitter;  // emits 'toggle' on each hotkey press
export function __resetPttHotkeyEmitterForTests(): void;
import { type PttAction } from '../shared/ipc-types';  // = 'toggle'
```

D-02 behavior table (CONTEXT.md):
- status === 'capturing' AND samples > 0 → fecha utterance agora (envia IPC)
- status === 'capturing' AND samples === 0 → no-op silencioso (renderer decide)
- status === 'idle' → no-op silencioso
- status === 'processing' → no-op silencioso (já enviou, esperando STT)

OQ-1 RESOLVED (Plan 01): VPTT-03 main-side é o escopo. Renderer hook (`useAlwaysListening`) que escuta `ALWAYS_LISTENING_FORCE_FLUSH` é declarado out-of-scope — documentar no SUMMARY como follow-up.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH em ipc-types.ts</name>
  <read_first>
    - apps/desktop/src/shared/ipc-types.ts (linhas 190-225 — IPC_CHANNELS object)
  </read_first>
  <behavior>
    - IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH definido com value 'always-listening:force-flush'
    - Posicionado próximo aos outros canais ALWAYS_LISTENING_* (após ALWAYS_LISTENING_VAD_THRESHOLD linha 216)
    - Comment JSDoc inline explicando direção (main → renderer) e propósito (VPTT-03)
    - Tipos derivados (IpcChannel union) absorvem automaticamente — sem mudança extra
  </behavior>
  <action>
    Modificar `apps/desktop/src/shared/ipc-types.ts` linha 217 (entre ALWAYS_LISTENING_VAD_THRESHOLD e VOICE_MODE_DEGRADED), ADICIONANDO:

    ```typescript
        /** Settings → main → renderer: reconfigure VAD silence threshold em tempo real (UI-SPEC: real-time apply) */
        ALWAYS_LISTENING_VAD_THRESHOLD: 'always-listening:vad-threshold',
        /**
         * Phase 43 (VPTT-03): main → renderer — força fim imediato do utterance VAD.
         * Disparado quando user pressiona hotkey PTT em modo Always-Listening.
         * Renderer (hook futuro `useAlwaysListening`) decide se há samples > 0
         * antes de fechar a janela — strategy main-side só comanda.
         * Comportamento por estado (D-02): no-op silencioso em idle/processing/0-samples.
         */
        ALWAYS_LISTENING_FORCE_FLUSH: 'always-listening:force-flush',
        /** main → tray (Phase 41 consumer): always-listening falhou, modo degradado */
        VOICE_MODE_DEGRADED: 'voiceMode:degraded',
    ```

    NÃO renomear, mover ou remover canais existentes. NÃO mudar a ordem dos outros canais.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && grep -n "ALWAYS_LISTENING_FORCE_FLUSH" src/shared/ipc-types.ts && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ALWAYS_LISTENING_FORCE_FLUSH: 'always-listening:force-flush'" apps/desktop/src/shared/ipc-types.ts` retorna 1
    - `grep -c "VPTT-03" apps/desktop/src/shared/ipc-types.ts` retorna >= 1 (comment de origem)
    - `pnpm tsc --noEmit -p tsconfig.node.json` sem erros (não há regressão de tipo)
    - Outros canais (ALWAYS_LISTENING_START, STOP, UTTERANCE, VAD_THRESHOLD, VOICE_MODE_DEGRADED, VOICE_MODE_SWITCH_RESULT) continuam exatamente como estavam — `grep -c "ALWAYS_LISTENING_" apps/desktop/src/shared/ipc-types.ts` retorna >= 5
  </acceptance_criteria>
  <done>Canal IPC adicionado, comment inline, sem regressão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender AlwaysListeningStrategy com forceFlush() + listener pttHotkeyEmitter</name>
  <read_first>
    - apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (arquivo completo)
    - apps/desktop/src/main/ptt-hotkey.ts (após Plan 02 Task 1 — confirmar pttHotkeyEmitter exportado)
    - apps/desktop/src/shared/ipc-types.ts (após Task 1 — confirmar canal disponível)
  </read_first>
  <behavior>
    - Novo field `private boundOnPttToggle: ((action: PttAction) => void) | null = null` para referência estável
    - Em start() (após linha 99 onde já registra utteranceListener), adiciona o listener pttHotkeyEmitter ANTES de mudar status
    - forceFlush() é PUBLIC void method (não Promise — síncrono e fire-and-forget)
    - forceFlush() guard early-return: status !== 'capturing' OU inFlight === true → return imediato sem log
    - forceFlush() depois do guard: webContents.send(ALWAYS_LISTENING_FORCE_FLUSH) com isDestroyed() check
    - Em stop(), remove pttHotkeyEmitter listener ANTES de remover utteranceListener (ordem: pttToggle primeiro, utterance depois)
    - listenerCount('toggle') após stop() === 0 — independente do utteranceListener path
    - Idempotência preservada: stop() duplo não vaza
  </behavior>
  <action>
    Modificar `apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts`:

    **1. Imports (após linha 27 ou linha 36)**, adicionar:
    ```typescript
    import { pttHotkeyEmitter } from '../../ptt-hotkey.js';
    import type { PttAction } from '../../../shared/ipc-types.js';
    ```

    Confirmar que `IPC_CHANNELS` já é importado (linha 30) — sim, está. Não precisa mudar imports IPC_CHANNELS.

    **2. Class fields (após linha 76, após `private utteranceListener` declaration)**, adicionar:
    ```typescript
      /**
       * Phase 43 VPTT-03 — listener estável para o bus pttHotkeyEmitter.
       * Quando user pressiona hotkey PTT em modo Always-Listening, this.forceFlush()
       * é invocado. Referência estável é necessária para off() funcionar (T-43-LEAK).
       */
      private boundOnPttToggle: ((action: PttAction) => void) | null = null;
    ```

    **3. start() — após o registro do utteranceListener (após linha 99 `ipcMain.on(IPC_CHANNELS.ALWAYS_LISTENING_UTTERANCE, this.utteranceListener);`)**, adicionar:
    ```typescript

        // Phase 43 VPTT-03 — subscribe pttHotkeyEmitter 'toggle' para force-flush.
        // Stable reference (T-43-LEAK). Listener é arrow function que delega
        // para this.forceFlush() — comportamento por estado em D-02.
        this.boundOnPttToggle = (_action: PttAction) => {
          this.forceFlush();
        };
        pttHotkeyEmitter.on('toggle', this.boundOnPttToggle);
    ```

    Esse bloco fica ENTRE o `ipcMain.on` (linha 99) e o `const vadThresholdMs` (linha 102).

    **4. Adicionar método público forceFlush() — entre processUtterance() (termina linha 153) e stop() (começa linha 159)**:
    ```typescript

      /**
       * forceFlush — VPTT-03 (D-02): fecha utterance imediatamente via IPC.
       *
       * Invocado por:
       *   - pttHotkeyEmitter 'toggle' callback (subscribed em start())
       *   - testes diretos (public para verificação por estado)
       *
       * Comportamento por estado (D-02 — comportamento DEFENSIVO, sem logs):
       *   - status === 'capturing' AND inFlight === false → envia IPC ao renderer
       *   - status === 'idle' → no-op silencioso (return)
       *   - status === 'processing' → no-op silencioso (return)
       *   - inFlight === true → no-op silencioso (utterance já em STT)
       *
       * Detecção de "0 samples" (D-02 último caso) é responsabilidade do
       * renderer — engine tem visibilidade do ring buffer + VAD state.
       * Strategy main-side só comanda; engine decide aplicar ou descartar.
       *
       * @see CONTEXT.md D-02 (force-flush behavior table)
       * @see RESEARCH.md Pattern 3 (VPTT-03 architecture)
       */
      forceFlush(): void {
        if (this.status !== 'capturing') {
          return; // idle / processing → no-op silencioso (D-02)
        }
        if (this.inFlight) {
          return; // utterance já sendo enviada — duplicação inútil (D-02)
        }

        if (!this.deps.mainWindow.isDestroyed()) {
          this.deps.mainWindow.webContents.send(IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH);
        }
      }
    ```

    **5. stop() — modificar para remover boundOnPttToggle ANTES do utteranceListener.**
    Atualmente stop() (linhas 159-179):
    ```typescript
    async stop(): Promise<void> {
      if (this.status === 'idle' && !this.utteranceListener) {
        return;
      }

      this.status = 'idle';

      if (this.utteranceListener) {
        ipcMain.off(IPC_CHANNELS.ALWAYS_LISTENING_UTTERANCE, this.utteranceListener);
        this.utteranceListener = null;
      }

      if (!this.deps.mainWindow.isDestroyed()) {
        this.deps.mainWindow.webContents.send(IPC_CHANNELS.ALWAYS_LISTENING_STOP);
      }
    }
    ```

    Mudar para:
    ```typescript
    async stop(): Promise<void> {
      if (this.status === 'idle' && !this.utteranceListener && !this.boundOnPttToggle) {
        return;
      }

      this.status = 'idle';

      // Phase 43 VPTT-03: unsubscribe pttHotkeyEmitter ANTES do utteranceListener.
      // Ordem importa porque um force-flush concurrent que dispare entre o
      // status='idle' acima e o off() abaixo já é guard'd pelo if status check
      // em forceFlush() — mas mantemos ordem cleanup explícita por clareza.
      if (this.boundOnPttToggle) {
        pttHotkeyEmitter.off('toggle', this.boundOnPttToggle);
        this.boundOnPttToggle = null;
      }

      if (this.utteranceListener) {
        ipcMain.off(IPC_CHANNELS.ALWAYS_LISTENING_UTTERANCE, this.utteranceListener);
        this.utteranceListener = null;
      }

      if (!this.deps.mainWindow.isDestroyed()) {
        this.deps.mainWindow.webContents.send(IPC_CHANNELS.ALWAYS_LISTENING_STOP);
      }
    }
    ```

    NÃO modificar dispose() (linhas 181-184) — ele chama stop() que já cobre tudo.
    NÃO modificar processUtterance() — sua lógica de inFlight é o que protege forceFlush.
    NÃO modificar createAlwaysListeningFactory ou scheduleModelPreDownload — independentes.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E "alwaysListening|pttOnly|voiceMode/index" | head -10 && pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "forceFlush(): void" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna 1
    - `grep -c "boundOnPttToggle" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna >= 5 (declaração + assign + on + off + null cleanup)
    - `grep -c "pttHotkeyEmitter.on('toggle'" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna 1
    - `grep -c "pttHotkeyEmitter.off('toggle'" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna 1
    - `grep -c "IPC_CHANNELS.ALWAYS_LISTENING_FORCE_FLUSH" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna 1
    - `grep -c "import { pttHotkeyEmitter }" apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts` retorna 1
    - `pnpm tsc --noEmit -p tsconfig.node.json` sem erros em alwaysListening.ts
    - Suite alwaysListening.test.ts EXISTENTE continua verde — `pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage` exits 0 (testes Phase 40 não regrediram)
  </acceptance_criteria>
  <done>forceFlush() exposto, listener subscribed/unsubscribed com referência estável, suite Phase 40 verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar testes VPTT-03 em alwaysListening.test.ts</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts (estrutura completa — observar mocks, vi.hoisted, beforeEach pattern)
    - apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (após Task 2 — comportamento real)
    - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts (Plan 01)
  </read_first>
  <behavior>
    - Novo describe 'VPTT-03 — force-flush behavior' adicionado ao final do arquivo (não substituir nada)
    - Testes cobrem: forceFlush em capturing → IPC enviado; forceFlush em idle → no-op; forceFlush em processing → no-op; forceFlush durante inFlight → no-op
    - Subscribe/unsubscribe lifecycle: start() adiciona listener (count === 1), stop() remove (count === 0), idempotência verificada
    - 5-cycle leak guard específico para VPTT-03 listener
    - Mock de `../../ptt-hotkey.js` substitui o pttHotkeyEmitter por um EventEmitter local controlado pelos testes
    - Suite Phase 40 (testes existentes) continua passando — nenhum describe pré-existente alterado
  </behavior>
  <action>
    Modificar `apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts`:

    **1. Adicionar mock de `../../ptt-hotkey.js`** próximo aos outros mocks (após o mock de `../../voiceInput/voiceHandler.js`, antes do `import { AlwaysListeningStrategy }`). O exato local fica entre `vi.mock('../../voiceInput/voiceHandler.js', ...)` e os imports do código produção:

    ```typescript
    // Phase 43 — mock pttHotkeyEmitter para testes VPTT-03.
    // EventEmitter local controlado pelos testes; reset em beforeEach.
    import { EventEmitter } from 'events';
    const { mockPttHotkeyEmitter } = vi.hoisted(() => ({
      mockPttHotkeyEmitter: new EventEmitter(),
    }));
    mockPttHotkeyEmitter.setMaxListeners(20);

    vi.mock('../../ptt-hotkey.js', () => ({
      pttHotkeyEmitter: mockPttHotkeyEmitter,
      __resetPttHotkeyEmitterForTests: () => {
        mockPttHotkeyEmitter.removeAllListeners();
      },
    }));
    ```

    **2. Adicionar novo describe ao final do arquivo** (depois do último `});` de fechamento do describe top-level, ou aninhado dentro dele se a estrutura for um único describe wrapper — analisar o arquivo e decidir consistentemente). Conteúdo:

    ```typescript

    // ============================================================
    // Phase 43 — VPTT-03 force-flush + pttHotkeyEmitter listener
    // ============================================================
    describe('VPTT-03 — force-flush behavior (Phase 43)', () => {
      beforeEach(() => {
        mockPttHotkeyEmitter.removeAllListeners();
      });

      describe('forceFlush() comportamento por estado (D-02)', () => {
        it('status="idle" → no-op silencioso (zero webContents.send chamadas)', async () => {
          const deps = makeStrategyDeps();
          const strategy = new AlwaysListeningStrategy(deps);
          // Sem start(), status === 'idle'
          strategy.forceFlush();
          // Verifica que NÃO foi chamado com FORCE_FLUSH (outros sends como
          // start/stop também são zero porque não start() foi chamado).
          const sendMock = deps.mainWindow.webContents.send as ReturnType<typeof vi.fn>;
          const forceFlushCalls = sendMock.mock.calls.filter(
            (args: unknown[]) => args[0] === 'always-listening:force-flush'
          );
          expect(forceFlushCalls).toHaveLength(0);
        });

        it('status="capturing" + inFlight=false → envia IPC always-listening:force-flush', async () => {
          const deps = makeStrategyDeps();
          const strategy = new AlwaysListeningStrategy(deps);
          await strategy.start(); // status -> 'capturing'
          const sendMock = deps.mainWindow.webContents.send as ReturnType<typeof vi.fn>;
          sendMock.mockClear(); // limpa o send do START

          strategy.forceFlush();

          expect(sendMock).toHaveBeenCalledWith('always-listening:force-flush');
        });

        it('status="capturing" + inFlight=true → no-op silencioso', async () => {
          const deps = makeStrategyDeps();
          const strategy = new AlwaysListeningStrategy(deps);
          await strategy.start();
          // Força inFlight=true via reflexão — utterance entra em processUtterance
          // e fica pendente. Não precisamos invocar processUtterance real;
          // basta setar a flag.
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (strategy as any).inFlight = true;
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          (strategy as any).status = 'processing';

          const sendMock = deps.mainWindow.webContents.send as ReturnType<typeof vi.fn>;
          sendMock.mockClear();

          strategy.forceFlush();

          const forceFlushCalls = sendMock.mock.calls.filter(
            (args: unknown[]) => args[0] === 'always-listening:force-flush'
          );
          expect(forceFlushCalls).toHaveLength(0);
        });

        it('mainWindow.isDestroyed() === true → no-op (sem crash)', async () => {
          const deps = makeStrategyDeps();
          (deps.mainWindow as unknown as { isDestroyed: () => boolean }).isDestroyed = () => true;
          const strategy = new AlwaysListeningStrategy(deps);
          await strategy.start();
          // Sem throw
          expect(() => strategy.forceFlush()).not.toThrow();
        });
      });

      describe('pttHotkeyEmitter listener lifecycle (T-43-LEAK)', () => {
        it('start() subscreve pttHotkeyEmitter "toggle" → listenerCount === 1', async () => {
          const strategy = new AlwaysListeningStrategy(makeStrategyDeps());
          expect(mockPttHotkeyEmitter.listenerCount('toggle')).toBe(0);
          await strategy.start();
          expect(mockPttHotkeyEmitter.listenerCount('toggle')).toBe(1);
        });

        it('stop() remove listener → listenerCount === 0', async () => {
          const strategy = new AlwaysListeningStrategy(makeStrategyDeps());
          await strategy.start();
          await strategy.stop();
          expect(mockPttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('dispose() chama stop() → listenerCount === 0', async () => {
          const strategy = new AlwaysListeningStrategy(makeStrategyDeps());
          await strategy.start();
          await strategy.dispose();
          expect(mockPttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('5 cycles de start/stop → listenerCount === 0 ao final', async () => {
          const strategy = new AlwaysListeningStrategy(makeStrategyDeps());
          for (let i = 0; i < 5; i++) {
            await strategy.start();
            await strategy.stop();
          }
          expect(mockPttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('emit "toggle" no pttHotkeyEmitter durante capturing dispara forceFlush IPC', async () => {
          const deps = makeStrategyDeps();
          const strategy = new AlwaysListeningStrategy(deps);
          await strategy.start();
          const sendMock = deps.mainWindow.webContents.send as ReturnType<typeof vi.fn>;
          sendMock.mockClear();

          mockPttHotkeyEmitter.emit('toggle', 'toggle');

          expect(sendMock).toHaveBeenCalledWith('always-listening:force-flush');
        });

        it('emit "toggle" após stop() é no-op (listener já removido)', async () => {
          const deps = makeStrategyDeps();
          const strategy = new AlwaysListeningStrategy(deps);
          await strategy.start();
          await strategy.stop();
          const sendMock = deps.mainWindow.webContents.send as ReturnType<typeof vi.fn>;
          sendMock.mockClear();

          mockPttHotkeyEmitter.emit('toggle', 'toggle');

          const forceFlushCalls = sendMock.mock.calls.filter(
            (args: unknown[]) => args[0] === 'always-listening:force-flush'
          );
          expect(forceFlushCalls).toHaveLength(0);
        });
      });
    });
    ```

    **IMPORTANTE:** o helper `makeStrategyDeps()` provavelmente já existe no arquivo (Phase 40) — REUSE-O. Se não existir com esse nome exato, procure pela função que constrói deps com `mainWindow`, `voiceHandlerDeps`, `onDegraded` (linhas iniciais do describe do arquivo). Não criar helper novo se já houver.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe.*VPTT-03" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` retorna >= 1
    - `grep -c "mockPttHotkeyEmitter" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` retorna >= 6
    - `grep -c "force-flush" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` retorna >= 5
    - `grep -c "vi.mock.*ptt-hotkey" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` retorna 1
    - `pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage` retorna exit 0 com TODOS os testes verdes (Phase 40 originais + novos VPTT-03 totalizando >= 18 + 10)
    - Nenhum it() pré-existente da Phase 40 foi modificado/removido (`grep "it(" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts | wc -l` aumentou em pelo menos 8 vs versão original)
  </acceptance_criteria>
  <done>Suite alwaysListening.test.ts com testes VPTT-03 verde, sem regressão dos testes Phase 40.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pttHotkeyEmitter (main bus) ↔ AlwaysListeningStrategy | strategy subscreve/desinscreve com referência estável; emit cross-strategy possível mas independente |
| AlwaysListeningStrategy.forceFlush() ↔ renderer (futuro) | webContents.send fire-and-forget; renderer hook não existe ainda (out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-FF-LEAK | DoS | boundOnPttToggle listener vida do objeto | mitigate | Stable callback ref + cleanup em stop() ANTES de utteranceListener (ordem explícita). 5-cycle leak test + listenerCount assertions enforce. |
| T-43-FF-RACE | DoS | forceFlush durante mid-VAD-window | mitigate | Guard duplo: `status !== 'capturing'` AND `inFlight === true` no início do forceFlush. Síncrono — sem await entre check e send (nenhuma janela onde o estado mude entre os dois). |
| T-43-FF-NOOP-VERBOSE | Information Disclosure | logs em no-op states | mitigate | forceFlush() NÃO logga quando status !== capturing (evita spam de "PTT pressionada em idle" se user desperdiça). Comportamento DEFENSIVO — silêncio é a feature. |
| T-43-FF-IDLE-RENDERER | Information Disclosure | renderer recebe FORCE_FLUSH sem hook | accept | OQ-1 RESOLVED: renderer wiring out-of-scope. webContents.send é fire-and-forget; mensagem cai no vazio se hook não existe. SUMMARY documenta como follow-up. |
| T-43-FF-DESTROYED | Tampering | mainWindow destruída entre start e forceFlush | mitigate | `if (!this.deps.mainWindow.isDestroyed())` check antes de send (pattern já estabelecido em alwaysListening.ts:105). Test "isDestroyed === true → no throw" cobre. |
</threat_model>

<verification>
- Após Task 1: `grep "ALWAYS_LISTENING_FORCE_FLUSH" apps/desktop/src/shared/ipc-types.ts` retorna 1+ linhas
- Após Task 2: `pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage` continua verde com testes Phase 40 originais (regressão check antes de adicionar testes novos)
- Após Task 3: `pnpm vitest run src/main/__tests__/voiceMode/alwaysListening.test.ts --no-coverage` exits 0 com >= 28 testes passed (>= 18 Phase 40 + >= 10 novos VPTT-03)
- Geral: `pnpm vitest run src/main/__tests__/voiceMode --no-coverage` exits 0 (todos os 4 arquivos do diretório verdes)
- Geral: `pnpm tsc --noEmit -p tsconfig.node.json` sem erros
</verification>

<success_criteria>
- IPC channel ALWAYS_LISTENING_FORCE_FLUSH adicionado com value canonical 'always-listening:force-flush'
- AlwaysListeningStrategy expõe forceFlush() público com guard duplo (status + inFlight) e isDestroyed check
- Listener pttHotkeyEmitter 'toggle' subscribed em start() / unsubscribed em stop() com referência estável (boundOnPttToggle)
- Cleanup ordem: pttToggle PRIMEIRO, depois utteranceListener (clareza explícita)
- T-43-LEAK mitigado: 5 cycles deixam listenerCount === 0
- D-02 comportamento por estado: capturing+!inFlight → IPC; demais → no-op silencioso (sem log)
- Suite Phase 40 (alwaysListening.test.ts existing) sem regressão
- Suite VPTT-03 (10 novos testes) verde
</success_criteria>

<output>
After completion, create `.planning/phases/43-ptt-only-integration/43-03-SUMMARY.md` documenting:
- forceFlush() API e comportamento por estado (D-02 table)
- IPC channel adicionado (direção, propósito, consumer expectation)
- Follow-up note OBRIGATÓRIA: "VPTT-03 está completo main-side. Renderer hook (`useAlwaysListening`) que consome ALWAYS_LISTENING_FORCE_FLUSH precisa ser criado em quick task ou phase futura — sem isso, force-flush é fire-and-forget sem efeito visual ao usuário. Ver RESEARCH.md Pitfall 4."
- Comando de regression test rápida pra Plan 04 (verificar voiceMode/alwaysListening.test.ts continua verde)
</output>
