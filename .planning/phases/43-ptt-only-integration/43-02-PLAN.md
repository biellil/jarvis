---
phase: 43
plan: 02
type: execute
wave: 1
depends_on: [43-01]
files_modified:
  - apps/desktop/src/main/ptt-hotkey.ts
  - apps/desktop/src/main/voiceMode/strategies/pttOnly.ts
  - apps/desktop/src/main/voiceMode/index.ts
  - apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts
autonomous: true
requirements:
  - VPTT-01
  - VPTT-02
must_haves:
  truths:
    - "PttOnlyStrategy implementa VoiceCaptureStrategy (start/stop/dispose/getStatus) com lifecycle idempotente"
    - "ptt-hotkey.ts exporta pttHotkeyEmitter (Node EventEmitter) que emite 'toggle' a cada disparo da hotkey, EM ADIÇÃO ao webContents.send existente — renderer ChatInput.tsx não é afetado"
    - "PttOnlyStrategy.start() chama setWakeWordPaused(true) + broadcastPauseToggle(true) — wake word loop renderer suspendido (validado via setWakeWordPaused(true) + broadcastPauseToggle(true) chamados nos unit tests pttOnly.test.ts + UAT manual VALIDATION.md row 1; D-03 ownership preservada, hotkey continua sendo única, mas wake word não responde) (VPTT-01)"
    - "PttOnlyStrategy NÃO chama globalShortcut.register diretamente — D-03 mantém ptt-hotkey.ts como único registrador (VPTT-02 atendido por reuso transparente)"
    - "Listener leak guard: 5 cycles de start/stop deixam pttHotkeyEmitter.listenerCount('toggle') === 0"
    - "Stable callback reference: `private boundOnToggle: (() => void) | null` permite off() limpar exatamente o listener registrado em on()"
    - "createPttOnlyFactory(deps) retorna () => PttOnlyStrategy para wiring no main/index.ts (Wave 2)"
    - "VoiceModeManager continua passando suite voiceMode.test.ts inalterada — esta plan adiciona, não altera o setMode()"
  artifacts:
    - path: "apps/desktop/src/main/ptt-hotkey.ts"
      provides: "globalShortcut registrar único + pttHotkeyEmitter EventEmitter exportado + __resetPttHotkeyEmitterForTests helper"
      contains: "export const pttHotkeyEmitter"
    - path: "apps/desktop/src/main/voiceMode/strategies/pttOnly.ts"
      provides: "PttOnlyStrategy class + createPttOnlyFactory factory builder"
      contains: "export class PttOnlyStrategy"
      exports: ["PttOnlyStrategy", "createPttOnlyFactory", "PttOnlyStrategyDeps"]
    - path: "apps/desktop/src/main/voiceMode/index.ts"
      provides: "Re-export de PttOnlyStrategy + createPttOnlyFactory para callers (entry point + tests)"
      contains: "export { PttOnlyStrategy, createPttOnlyFactory }"
  key_links:
    - from: "apps/desktop/src/main/voiceMode/strategies/pttOnly.ts"
      to: "apps/desktop/src/main/ptt-hotkey.ts"
      via: "import { pttHotkeyEmitter, type PttAction }"
      pattern: "from ['\"]\\.\\./\\.\\./ptt-hotkey"
    - from: "apps/desktop/src/main/voiceMode/strategies/pttOnly.ts"
      to: "apps/desktop/src/main/store.ts + apps/desktop/src/main/ipc/settings.ts"
      via: "setWakeWordPaused + broadcastPauseToggle (reuso OQ-4)"
      pattern: "broadcastPauseToggle"
    - from: "apps/desktop/src/main/ptt-hotkey.ts (registerPttHotkey + changePttHotkey callback)"
      to: "pttHotkeyEmitter"
      via: "pttHotkeyEmitter.emit('toggle', 'toggle')"
      pattern: "pttHotkeyEmitter\\.emit\\('toggle'"
---

<objective>
Implementar a classe `PttOnlyStrategy` (VPTT-01) e estender `ptt-hotkey.ts` com um Node `EventEmitter` interno (`pttHotkeyEmitter`) — bus main-side que strategies consomem (D-03). VPTT-02 é satisfeito automaticamente: PttOnlyStrategy NÃO registra `globalShortcut`, apenas subscreve o emitter. Renderer (`ChatInput.tsx`) já consome `webContents.send('ptt:action', 'toggle')` — esse caminho fica intocado.

Purpose: Entregar o modo PTT-only funcional no main process — wake word silenciado (via reuso de `wakeWord:pause-toggle`), hotkey reusada (zero reconfig — VPTT-02), listeners limpos com referência estável (mitiga T-43-LEAK). Dual-cast (renderer + main bus) sem quebrar nada.
Output: PttOnlyStrategy class + factory builder + EventEmitter exportado de ptt-hotkey.ts + testes pttOnly.test.ts preenchidos com assertions reais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-ptt-only-integration/43-CONTEXT.md
@.planning/phases/43-ptt-only-integration/43-RESEARCH.md
@.planning/phases/43-ptt-only-integration/43-01-PLAN.md
@apps/desktop/src/main/voiceMode/index.ts
@apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts
@apps/desktop/src/main/ptt-hotkey.ts
@apps/desktop/src/main/store.ts
@apps/desktop/src/main/ipc/settings.ts
@apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts
@apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. -->

From apps/desktop/src/main/voiceMode/index.ts (interface to implement):
```typescript
export interface VoiceCaptureStrategy {
  start(): Promise<void>;
  stop(): Promise<void>;
  dispose(): Promise<void>;
  getStatus(): 'idle' | 'capturing' | 'processing';
}
```

From apps/desktop/src/main/ipc/settings.ts:131-135:
```typescript
export function broadcastPauseToggle(paused: boolean): void {
  BrowserWindow.getAllWindows().forEach((win) => {
    win.webContents.send(IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE, paused);
  });
}
```

From apps/desktop/src/main/store.ts:
```typescript
export function setWakeWordPaused(paused: boolean): void;
export function getWakeWordPaused(): boolean;
```

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export type PttAction = 'toggle';
```

From apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (pattern to mirror):
- Lifecycle: status state machine ('idle' | 'capturing' | 'processing')
- Idempotent dispose: dispose() chama stop() + cleanup
- Stable listener ref: `private utteranceListener: ((event, payload) => void) | null = null`
- Cleanup pattern em stop(): `if (this.utteranceListener) { ipcMain.off(...); this.utteranceListener = null; }`

From Plan 01 helpers (already exists):
```typescript
export function makeMockStrategy(status?: 'idle' | 'capturing' | 'processing'): MockStrategy;
export function makeFailingFactory(failuresBeforeSuccess: number, errorMessage?: string): MockedFunction<() => VoiceCaptureStrategy>;
export function makeSequentialFactory(strategies: MockStrategy[]): MockedFunction<() => VoiceCaptureStrategy>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender ptt-hotkey.ts com pttHotkeyEmitter (EventEmitter dual-cast)</name>
  <read_first>
    - apps/desktop/src/main/ptt-hotkey.ts (ler arquivo completo — linhas 1-107)
    - apps/desktop/src/main/__tests__/ptt-hotkey.test.ts (verificar se assertions vão regredir; se sim, adicionar assertions para o emitter)
    - apps/desktop/src/main/voiceMode/index.ts (linha 12 — pattern `import { EventEmitter } from 'events'`)
    - apps/desktop/src/main/voice/voiceInputManager.ts (se existir — pattern de `__resetXxxForTests` reset helper; ver linha ~125 do file mencionado em RESEARCH.md Pitfall 3)
  </read_first>
  <behavior>
    - registerPttHotkey continua disparando `mainWindow.webContents.send('ptt:action', 'toggle')` (renderer-facing, intocado)
    - registerPttHotkey ADICIONALMENTE faz `pttHotkeyEmitter.emit('toggle', 'toggle')` após o webContents.send (ordem: renderer primeiro, main bus depois — ChatInput tem prioridade)
    - changePttHotkey replica o mesmo dual-cast no novo callback (não pode esquecer — dual-cast em 1 lugar quebra atomicidade)
    - pttHotkeyEmitter.setMaxListeners(5) configurado na declaração
    - __resetPttHotkeyEmitterForTests() exportado para isolamento de tests (chamado em beforeEach do voiceMode.race.test.ts e pttOnly.test.ts)
    - Suite ptt-hotkey.test.ts existente continua verde (zero regressão)
  </behavior>
  <action>
    Modificar `apps/desktop/src/main/ptt-hotkey.ts` ADICIONANDO (não removendo nada):

    1. Após `import` linhas 18-19, adicionar:
       ```typescript
       import { EventEmitter } from 'events';
       import type { PttAction } from '../shared/ipc-types.js';
       ```

    2. Após linha 22 (`let currentPttHotkey: string | null = null;`), adicionar:
       ```typescript
       /**
        * pttHotkeyEmitter — bus interno main-side para fan-out do evento 'toggle'.
        *
        * Phase 43 (D-03): strategies (PttOnlyStrategy, AlwaysListeningStrategy)
        * subscrevem em start() via pttHotkeyEmitter.on('toggle', ...) e
        * desinscrevem em stop()/dispose(). WakeWordStrategy não subscreve.
        *
        * O `webContents.send('ptt:action', 'toggle')` ao renderer continua
        * disparando — ChatInput.tsx é o consumer principal (PTT mic ownership
        * via voiceInputManager.acquire('ptt')). Strategies main-side são
        * consumers ADICIONAIS sem interferir no path renderer.
        *
        * setMaxListeners(5): 3 strategies + folga para tests/dev — leak detector
        * via warning Node se passar de 5 (default 10 é frouxo demais para
        * detectar leaks em mode switch).
        */
       export const pttHotkeyEmitter = new EventEmitter();
       pttHotkeyEmitter.setMaxListeners(5);
       ```

    3. Dentro de `registerPttHotkey` (linhas 38-43), modificar o callback do globalShortcut.register para:
       ```typescript
       const success = globalShortcut.register(accelerator, () => {
         // Dual-cast (D-03):
         //   1. Renderer (ChatInput.tsx) — comportamento existente, intocado.
         mainWindow.webContents.send('ptt:action', 'toggle');
         //   2. Main strategies — bus interno (Phase 43).
         pttHotkeyEmitter.emit('toggle', 'toggle' as PttAction);
         console.log('[PTT] Toggle event sent (renderer + main bus)');
       });
       ```

    4. Dentro de `changePttHotkey` (linhas 70-73 e linhas 83-85), aplicar O MESMO dual-cast em AMBOS os callbacks (registro novo E restore após falha):
       ```typescript
       // Linha 70-73 (novo registro):
       const success = globalShortcut.register(accelerator, () => {
         mainWindow.webContents.send('ptt:action', 'toggle');
         pttHotkeyEmitter.emit('toggle', 'toggle' as PttAction);
         console.log('[PTT] Toggle event sent (renderer + main bus)');
       });

       // Linha 83-85 (restore após falha):
       const restored = globalShortcut.register(currentPttHotkey, () => {
         mainWindow.webContents.send('ptt:action', 'toggle');
         pttHotkeyEmitter.emit('toggle', 'toggle' as PttAction);
       });
       ```

    5. Após `unregisterPttHotkey()` (final do arquivo, após linha 107), adicionar:
       ```typescript
       /**
        * __resetPttHotkeyEmitterForTests — helper de isolamento de tests.
        *
        * pttHotkeyEmitter é module-scoped (singleton); vitest reseta vi.fn() entre
        * tests mas NÃO reseta module state. Race tests + pttOnly tests chamam
        * em beforeEach para garantir listenerCount começa em 0.
        *
        * @see RESEARCH.md Pitfall 3 (pttHotkeyEmitter global sobrevive entre tests)
        * @see voiceInputManager.ts pattern análogo em src/renderer
        */
       export function __resetPttHotkeyEmitterForTests(): void {
         pttHotkeyEmitter.removeAllListeners();
       }
       ```

    NÃO modificar nada além disso. NÃO trocar `mainWindow.webContents.send('ptt:action', 'toggle')` por outra forma. NÃO criar canal IPC novo. NÃO alterar ordem dos `console.log` existentes (testes podem grep).
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/ptt-hotkey.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const pttHotkeyEmitter" apps/desktop/src/main/ptt-hotkey.ts` retorna 1
    - `grep -c "pttHotkeyEmitter.emit('toggle'" apps/desktop/src/main/ptt-hotkey.ts` retorna >= 3 (registerPttHotkey + 2 callbacks em changePttHotkey)
    - `grep -c "export function __resetPttHotkeyEmitterForTests" apps/desktop/src/main/ptt-hotkey.ts` retorna 1
    - `grep -c "setMaxListeners(5)" apps/desktop/src/main/ptt-hotkey.ts` retorna 1
    - `grep -c "import { EventEmitter } from 'events'" apps/desktop/src/main/ptt-hotkey.ts` retorna 1
    - `grep "webContents.send.*'ptt:action'" apps/desktop/src/main/ptt-hotkey.ts | wc -l` retorna >= 3 (renderer-facing emit preservado em todas as 3 callbacks)
    - `pnpm vitest run src/main/__tests__/ptt-hotkey.test.ts --no-coverage` exits 0 (sem regressão)
    - `pnpm tsc --noEmit -p tsconfig.node.json` sem erros referenciando ptt-hotkey.ts
  </acceptance_criteria>
  <done>Dual-cast funcionando, helper de reset exportado, suite existente verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar PttOnlyStrategy + factory + re-export em voiceMode/index.ts</name>
  <read_first>
    - apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (template a espelhar — ler arquivo completo)
    - apps/desktop/src/main/ptt-hotkey.ts (após Task 1 — para confirmar pttHotkeyEmitter export disponível)
    - apps/desktop/src/main/store.ts (linhas 80-95 — setWakeWordPaused signature + behavior)
    - apps/desktop/src/main/ipc/settings.ts (linhas 127-135 — broadcastPauseToggle signature)
    - apps/desktop/src/main/voiceMode/index.ts (linhas 19-23 — pattern de re-export)
  </read_first>
  <behavior>
    - PttOnlyStrategy.start() em sequência: status check (idempotência) → setWakeWordPaused(true) → broadcastPauseToggle(true) → subscribe pttHotkeyEmitter 'toggle' com stable ref → status = 'capturing'
    - PttOnlyStrategy.stop() em sequência: status check (idempotência) → status = 'idle' → unsubscribe pttHotkeyEmitter (mesma ref) → setWakeWordPaused(false) → broadcastPauseToggle(false)
    - PttOnlyStrategy.dispose() chama stop() — idempotência canônica
    - getStatus() retorna campo private status fielmente
    - Listener é arrow function inline gravada em this.boundOnToggle ANTES do on() — referência estável é a única forma de off() funcionar
    - Listener pode ser no-op com console.debug (D-03 — renderer já consome `ptt:action` via webContents.send; main listener existe para satisfazer "listenerCount >= 1 durante PTT-only ativo" + futuras telemetrias)
    - createPttOnlyFactory(deps) retorna closure compatível com VoiceCaptureStrategy factory map
  </behavior>
  <action>
    Criar `apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` com EXATAMENTE este conteúdo:

    ```typescript
    /**
     * PttOnlyStrategy — coordinator main-side para o modo PTT-only.
     *
     * Phase 43 (VPTT-01, VPTT-02, D-01, D-03)
     *
     * Responsabilidades:
     *   - Suspender o wake word loop renderer-side via reuso do canal existente
     *     `wakeWord:pause-toggle` (broadcastPauseToggle de ipc/settings.ts) +
     *     persistência em electron-store via setWakeWordPaused (D-04 OQ-4 do
     *     research — zero IPC novo).
     *   - Subscrever o pttHotkeyEmitter 'toggle' bus (D-03) para satisfazer
     *     listenerCount("toggle") >= 1 durante PTT-only ativo. O renderer
     *     (ChatInput.tsx) é quem efetivamente lida com o mic via
     *     voiceInputManager.acquire('ptt') ao receber webContents.send do
     *     ptt-hotkey.ts.
     *   - Cleanup idempotente em stop()/dispose() — listener removido com
     *     mesma referência (T-43-LEAK).
     *
     * D-01 (toggle, não press-and-hold): a estratégia em si não muda; o
     * comportamento toggle é responsabilidade do callback em ptt-hotkey.ts +
     * voiceInputManager renderer. Strategy só silencia o wake word.
     *
     * D-03 (hotkey ownership): NÃO chama globalShortcut.register. ptt-hotkey.ts
     * é único registrador (registrado uma vez em main/index.ts whenReady).
     *
     * @see CONTEXT.md D-01 (toggle), D-03 (ownership), D-04 OQ-4 (reuso pause)
     * @see ptt-hotkey.ts pttHotkeyEmitter (Plan 02 Task 1)
     */
    import type { BrowserWindow } from 'electron';
    import type { VoiceCaptureStrategy } from '../index.js';
    import { pttHotkeyEmitter, type PttAction } from '../../ptt-hotkey.js';
    import { setWakeWordPaused } from '../../store.js';
    import { broadcastPauseToggle } from '../../ipc/settings.js';

    export interface PttOnlyStrategyDeps {
      /**
       * Janela principal — não usada diretamente pela strategy (broadcast de
       * pause-toggle alcança todas as windows via BrowserWindow.getAllWindows).
       * Mantida no shape para consistência com AlwaysListeningStrategyDeps e
       * para futura extensão sem quebra de contrato.
       */
      mainWindow: BrowserWindow;
    }

    export class PttOnlyStrategy implements VoiceCaptureStrategy {
      private status: 'idle' | 'capturing' | 'processing' = 'idle';

      /**
       * Stable reference para o listener — pttHotkeyEmitter.off() requer
       * a MESMA função (não nova arrow inline). Sem isso, listener vaza e
       * próximo mode switch acumula outro (T-43-LEAK).
       */
      private boundOnToggle: ((action: PttAction) => void) | null = null;

      constructor(private readonly deps: PttOnlyStrategyDeps) {}

      async start(): Promise<void> {
        if (this.status !== 'idle') {
          // Idempotência defensiva — start() duplo não duplica listener.
          return;
        }

        // VPTT-01: suspende wake word loop no renderer.
        // Reusa canal `wakeWord:pause-toggle` (Phase 23 D-04) — zero IPC novo.
        // useWakeWord.ts:397 consome onPauseToggle e suspende o engine.
        setWakeWordPaused(true);
        broadcastPauseToggle(true);

        // D-03: subscribe pttHotkeyEmitter 'toggle' com referência estável.
        // Listener é no-op com console.debug — renderer (ChatInput) é quem
        // resolve o mic via voiceInputManager.acquire('ptt') ao receber
        // webContents.send('ptt:action', 'toggle') do ptt-hotkey.ts.
        // Existência do listener no main bus garante observability + permite
        // futura telemetria sem mudar API.
        this.boundOnToggle = (_action: PttAction) => {
          console.debug('[PttOnlyStrategy] ptt:action toggle (renderer handles mic)');
        };
        pttHotkeyEmitter.on('toggle', this.boundOnToggle);

        this.status = 'capturing';
      }

      async stop(): Promise<void> {
        if (this.status === 'idle' && !this.boundOnToggle) {
          // Idempotência: stop() duplo não vaza, não chama broadcast de novo.
          return;
        }

        this.status = 'idle';

        // T-43-LEAK: remoção com mesma referência. Sem isso, próximo mode
        // switch acumula listener fantasma.
        if (this.boundOnToggle) {
          pttHotkeyEmitter.off('toggle', this.boundOnToggle);
          this.boundOnToggle = null;
        }

        // VPTT-01: re-ativa wake word loop no renderer.
        setWakeWordPaused(false);
        broadcastPauseToggle(false);
      }

      async dispose(): Promise<void> {
        await this.stop();
        // Strategy é descartada — VoiceModeManager não reusa esta instância
        // (D-04 lazy lifecycle).
      }

      getStatus(): 'idle' | 'capturing' | 'processing' {
        return this.status;
      }
    }

    /**
     * Helper para criar a factory de PTT-only com deps já fechadas.
     * Usado pelo entry point (main/index.ts) ao instanciar o VoiceModeManager
     * (Plan 04 wiring) — substitui o factory throw stub do voiceMode/index.ts.
     */
    export function createPttOnlyFactory(
      deps: PttOnlyStrategyDeps,
    ): () => PttOnlyStrategy {
      return () => new PttOnlyStrategy(deps);
    }
    ```

    Em seguida, modificar `apps/desktop/src/main/voiceMode/index.ts`:

    1. Após linhas 19-23 (re-exports de AlwaysListening), ADICIONAR:
       ```typescript
       // Phase 43 — PttOnlyStrategy + factory builder. Mesmo pattern do
       // createAlwaysListeningFactory para o entry point sobrescrever o stub.
       import { PttOnlyStrategy, createPttOnlyFactory } from './strategies/pttOnly.js';
       export { PttOnlyStrategy, createPttOnlyFactory };
       export type { PttOnlyStrategyDeps } from './strategies/pttOnly.js';
       ```

    NÃO modificar a factory map em VoiceModeManager construtor (linhas 113-122) — Wave 2 (Plan 04) é quem vai trocar o `throw new Error('PttOnlyStrategy not yet implemented (Phase 43)')` por wiring real (precisa também das deps que vivem em main/index.ts). Aqui Plan 02 só EXPÕE; Plan 04 conecta.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E "pttOnly|voiceMode/index" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` existe
    - `grep -c "export class PttOnlyStrategy" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "implements VoiceCaptureStrategy" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "export function createPttOnlyFactory" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "export interface PttOnlyStrategyDeps" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "boundOnToggle" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna >= 4 (declaração + 3 usages: assign, on, off)
    - `grep -c "pttHotkeyEmitter.on('toggle'" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "pttHotkeyEmitter.off('toggle'" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 1
    - `grep -c "setWakeWordPaused(true)\|setWakeWordPaused(false)" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 2
    - `grep -c "broadcastPauseToggle(true)\|broadcastPauseToggle(false)" apps/desktop/src/main/voiceMode/strategies/pttOnly.ts` retorna 2
    - `grep -v "// " apps/desktop/src/main/voiceMode/strategies/pttOnly.ts | grep -c "globalShortcut"` retorna 0 (D-03: NÃO registra hotkey)
    - `grep -c "export { PttOnlyStrategy, createPttOnlyFactory }" apps/desktop/src/main/voiceMode/index.ts` retorna 1
    - `pnpm tsc --noEmit -p tsconfig.node.json` sem erros em pttOnly.ts ou voiceMode/index.ts
  </acceptance_criteria>
  <done>PttOnlyStrategy implementa VoiceCaptureStrategy, factory exposta, re-export feito. Sem registro de globalShortcut.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preencher pttOnly.test.ts com assertions reais (substituir it.todo)</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts (estrutura criada em Plan 01)
    - apps/desktop/src/main/voiceMode/strategies/pttOnly.ts (após Task 2)
    - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts (linhas 33-100 — pattern de mocks)
    - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts
    - apps/desktop/src/main/ipc/settings.ts (broadcastPauseToggle implementação real para validar mock)
  </read_first>
  <behavior>
    - Cada it.todo() do Plan 01 vira it() concreto que testa o comportamento prometido
    - Mock de `../../ipc/settings.js` captura broadcastPauseToggle calls em array hoisted
    - Mock de `../../store.js` captura setWakeWordPaused calls
    - Listener leak guard: 5 cycles fecham sem deixar listener — assertion `pttHotkeyEmitter.listenerCount('toggle')` final === 0
    - Idempotência: 2x start, 2x stop verificáveis
    - Suite de pttOnly.test.ts roda verde após Task 2 + Task 3
  </behavior>
  <action>
    Reescrever `apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` substituindo os it.todo() por it() concretos. Estrutura final:

    ```typescript
    /**
     * pttOnly.test.ts — PttOnlyStrategy main coordinator tests
     *
     * Phase 43 — VPTT-01 (lifecycle + wake word silenciado), VPTT-02 (hotkey reuso)
     *
     * OQ-4 RESOLVED (Plan 01): wake word pause via canal `wakeWord:pause-toggle`
     * (broadcastPauseToggle de ipc/settings.ts) — NÃO criar canal `ptt-only:active`.
     * Renderer useWakeWord.ts:397 já consome.
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // Mock electron-store
    vi.mock('electron-store', () => {
      let mockStore: Record<string, unknown> = {};
      return {
        default: class Store {
          get(key: string) { return mockStore[key]; }
          set(key: string, value: unknown) { mockStore[key] = value; }
          static __resetStore() { mockStore = {}; }
        },
      };
    });

    // Mock electron — minimal
    vi.mock('electron', () => ({
      ipcMain: { on: vi.fn(), off: vi.fn(), once: vi.fn() },
      BrowserWindow: vi.fn(() => ({
        isDestroyed: () => false,
        webContents: { send: vi.fn() },
      })),
      globalShortcut: { register: vi.fn(() => true), unregister: vi.fn() },
    }));

    // Mock store + ipc/settings — capturamos chamadas de pause
    const { setWakeWordPausedMock, broadcastPauseToggleMock } = vi.hoisted(() => ({
      setWakeWordPausedMock: vi.fn(),
      broadcastPauseToggleMock: vi.fn(),
    }));

    vi.mock('../../store.js', () => ({
      setWakeWordPaused: setWakeWordPausedMock,
      getWakeWordPaused: vi.fn(() => false),
    }));

    vi.mock('../../ipc/settings.js', () => ({
      broadcastPauseToggle: broadcastPauseToggleMock,
    }));

    import { PttOnlyStrategy, createPttOnlyFactory } from '../../voiceMode/strategies/pttOnly';
    import { pttHotkeyEmitter, __resetPttHotkeyEmitterForTests } from '../../ptt-hotkey';
    import type { BrowserWindow } from 'electron';

    function makeMainWindow(): BrowserWindow {
      return {
        isDestroyed: () => false,
        webContents: { send: vi.fn() },
      } as unknown as BrowserWindow;
    }

    describe('PttOnlyStrategy (Phase 43, VPTT-01)', () => {
      beforeEach(() => {
        __resetPttHotkeyEmitterForTests();
        setWakeWordPausedMock.mockClear();
        broadcastPauseToggleMock.mockClear();
      });

      describe('VPTT-01 — lifecycle', () => {
        it('start() pausa wake word via setWakeWordPaused(true) + broadcastPauseToggle(true)', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          expect(setWakeWordPausedMock).toHaveBeenCalledWith(true);
          expect(broadcastPauseToggleMock).toHaveBeenCalledWith(true);
        });

        it('start() subscreve pttHotkeyEmitter "toggle" — listenerCount === 1', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(0);
          await strategy.start();
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(1);
        });

        it('start() é idempotente — start() duplicado mantém apenas 1 listener', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          await strategy.start(); // duplo
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(1);
          // setWakeWordPaused não é chamado de novo (start cedo via status check)
          expect(setWakeWordPausedMock).toHaveBeenCalledTimes(1);
        });

        it('start() seta status para "capturing"', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          expect(strategy.getStatus()).toBe('idle');
          await strategy.start();
          expect(strategy.getStatus()).toBe('capturing');
        });

        it('stop() remove pttHotkeyEmitter "toggle" listener', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(1);
          await strategy.stop();
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('stop() chama broadcastPauseToggle(false) + setWakeWordPaused(false) — wake word reativado', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          setWakeWordPausedMock.mockClear();
          broadcastPauseToggleMock.mockClear();
          await strategy.stop();
          expect(setWakeWordPausedMock).toHaveBeenCalledWith(false);
          expect(broadcastPauseToggleMock).toHaveBeenCalledWith(false);
        });

        it('stop() seta status para "idle"', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          expect(strategy.getStatus()).toBe('capturing');
          await strategy.stop();
          expect(strategy.getStatus()).toBe('idle');
        });

        it('dispose() é idempotente — chamada dupla não vaza, listenerCount permanece 0', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          await strategy.dispose();
          await strategy.dispose(); // duplo
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('dispose() chama stop() internamente — broadcastPauseToggle(false) é emitido', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          broadcastPauseToggleMock.mockClear();
          await strategy.dispose();
          expect(broadcastPauseToggleMock).toHaveBeenCalledWith(false);
        });
      });

      describe('VPTT-02 — reuso da hotkey v1.7 (D-03 ownership)', () => {
        it('PttOnlyStrategy NÃO chama globalShortcut.register em nenhum momento', async () => {
          const { globalShortcut } = await import('electron');
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          await strategy.start();
          await strategy.stop();
          await strategy.dispose();
          expect(globalShortcut.register).not.toHaveBeenCalled();
          expect(globalShortcut.unregister).not.toHaveBeenCalled();
        });
      });

      describe('Listener leak guard (T-43-LEAK)', () => {
        it('5 cycles de start/stop deixam pttHotkeyEmitter.listenerCount("toggle") === 0', async () => {
          const strategy = new PttOnlyStrategy({ mainWindow: makeMainWindow() });
          for (let i = 0; i < 5; i++) {
            await strategy.start();
            await strategy.stop();
          }
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('createPttOnlyFactory retorna () => PttOnlyStrategy compatível com VoiceCaptureStrategy', () => {
          const factory = createPttOnlyFactory({ mainWindow: makeMainWindow() });
          const instance = factory();
          expect(instance).toBeInstanceOf(PttOnlyStrategy);
          expect(typeof instance.start).toBe('function');
          expect(typeof instance.stop).toBe('function');
          expect(typeof instance.dispose).toBe('function');
          expect(typeof instance.getStatus).toBe('function');
        });
      });
    });
    ```

    NÃO adicionar testes além desses. NÃO testar implementação interna de pttHotkeyEmitter (Task 1 já cobre).
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna 0 (todos viraram it() reais)
    - `grep -cE "^\s*it\(" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 12
    - `grep -c "expect.*pttHotkeyEmitter.listenerCount" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 4 (assertions de leak guard em múltiplos cenários)
    - `grep -c "expect.*setWakeWordPausedMock" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 2
    - `grep -c "expect.*broadcastPauseToggleMock" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 3
    - `pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage` retorna exit 0 com TODOS os testes verdes (passed >= 12, failed === 0)
    - `pnpm vitest run src/main/__tests__/voiceMode.test.ts --no-coverage` continua verde (regressão check — voiceMode.test.ts deve manter 18/18 passed)
  </acceptance_criteria>
  <done>Todos os testes pttOnly verdes, 0 it.todo restante, suite voiceMode.test.ts não regrediu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ChatInput.tsx ↔ main ptt-hotkey | renderer recebe `webContents.send('ptt:action', 'toggle')` (caminho intocado) |
| main strategies ↔ pttHotkeyEmitter | strategies subscrevem bus interno main-side (D-03) — não cross-process |
| main PttOnlyStrategy ↔ renderer useWakeWord | reuso do canal `wakeWord:pause-toggle` (já existe desde Phase 23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-LEAK | DoS | PttOnlyStrategy.boundOnToggle + pttHotkeyEmitter | mitigate | Stable callback reference (`private boundOnToggle`) + idempotent stop()/dispose(). Test "5 cycles → listenerCount === 0" enforce. setMaxListeners(5) detecta leak via Node warning antes de crash. |
| T-43-DUAL-CAST | Spoofing | ptt-hotkey.ts dual-cast (renderer + main bus) | mitigate | webContents.send é fire-and-forget (best effort); pttHotkeyEmitter.emit é síncrono. Ordem definida (renderer primeiro). Sem retry, sem race entre os dois canais. Renderer e main bus são independent — falha em um não afeta o outro. |
| T-43-IDEMPOTENT | Tampering | start()/stop()/dispose() idempotência | mitigate | status check no início de cada método (`if status !== 'idle' return` no start, simétrico no stop). Listener nunca duplicado, broadcastPauseToggle nunca chamado 2x sem stop() entre. |
| T-43-D03-OWNERSHIP | Tampering | hotkey ownership | mitigate | PttOnlyStrategy NÃO importa globalShortcut, NÃO chama register/unregister. Test explícito `globalShortcut.register not.toHaveBeenCalled` valida. ptt-hotkey.ts continua único registrador. |
| T-43-WAKEWORD-DESYNC | Tampering | setWakeWordPaused state vs broadcastPauseToggle ordem | accept | setWakeWordPaused (electron-store) acontece ANTES de broadcastPauseToggle (renderer). Se app crashar entre os dois, restart lê store value já correto — sem desync. |
</threat_model>

<verification>
- Após Task 1: `pnpm vitest run src/main/__tests__/ptt-hotkey.test.ts --no-coverage` exits 0 (sem regressão)
- Após Task 2: `pnpm tsc --noEmit -p tsconfig.node.json` sem erros em pttOnly.ts ou voiceMode/index.ts
- Após Task 3: `pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage` exits 0 com >= 12 testes passed
- Geral: `pnpm vitest run src/main/__tests__/voiceMode --no-coverage` exits 0 (todos os tests do diretório voiceMode incluindo voiceMode.test.ts e voiceMode/alwaysListening.test.ts ainda verdes)
- Geral: nenhuma referência a `'ptt-only:active'` no código (verifica que NÃO criamos canal IPC novo — `grep -r "ptt-only:active" apps/desktop/src` retorna 0 linhas)
</verification>

<success_criteria>
- pttHotkeyEmitter exportado, dual-cast funcionando em registerPttHotkey + changePttHotkey
- __resetPttHotkeyEmitterForTests exportado
- PttOnlyStrategy implementa VoiceCaptureStrategy com idempotência completa (start/stop/dispose)
- Wake word silenciado via reuso de wakeWord:pause-toggle (D-04 OQ-4) — zero IPC novo
- D-03 ownership preservado: 0 chamadas a globalShortcut.register em PttOnlyStrategy
- T-43-LEAK mitigado: 5 cycles deixam listenerCount === 0
- VPTT-02 atendido por arquitetura: PttOnlyStrategy NÃO toca getPttHotkey/setPttHotkey
- Suite voiceMode (todos os 3 files) verde
</success_criteria>

<output>
After completion, create `.planning/phases/43-ptt-only-integration/43-02-SUMMARY.md` documenting:
- pttHotkeyEmitter API (eventos suportados, padrão de subscribe/unsubscribe)
- PttOnlyStrategy lifecycle behavior (estado em cada método)
- Reuso de wakeWord:pause-toggle (link com Phase 23 D-04, useWakeWord.ts:397)
- Wiring pendente: factory ainda lança em VoiceModeManager (Plan 04 conecta)
- Comando de regression test rápida pra Plans 03 e 04
</output>
