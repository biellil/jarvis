---
phase: 43
plan: 04
type: execute
wave: 3
depends_on: [43-01, 43-02, 43-03]
files_modified:
  - apps/desktop/src/main/voiceMode/index.ts
  - apps/desktop/src/main/tray.ts
  - apps/desktop/src/main/ipc/voiceMode.ts
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/main/__tests__/voiceMode.race.test.ts
  - apps/desktop/src/main/__tests__/voiceMode.test.ts
autonomous: true
requirements:
  - VPTT-01
  - VPTT-02
  - VPTT-03
must_haves:
  truths:
    - "VoiceModeManager.setMode() implementa D-04 plano B: em catch da factory nova, re-instancia a strategy antiga via factory dela e dá start(). Atomicidade preservada (transitioning=true durante todo o switch incluindo recovery)"
    - "Se recovery TAMBÉM falhar: log de erro + currentMode = null + activeStrategy = null. Sistema entra em estado degradado documentado."
    - "Tipo `currentMode` refatorado para `VoiceMode | null`. Tipo retorno de `getMode()` agora é `VoiceMode | null`."
    - "tray.ts handle currentMode === null sem crash — option.mode === null retorna false em todos os items (nenhum radio marcado), tooltip cai em fallback `?? 'Wake Word'`"
    - "ipc/voiceMode.ts handle currentMode === null sem crash — broadcastModeSwitch já aceita success:false sem newMode (tipo permite undefined)"
    - "main/index.ts wira createPttOnlyFactory({mainWindow}) no construtor do VoiceModeManager — modo PTT-only deixa de lançar erro stub"
    - "voiceMode.race.test.ts cenários 1, 2, 3, 3b passam com assertions reais (substituindo it.todo do Plan 01)"
    - "voiceMode.test.ts adiciona testes para D-04 plano B em paralelo aos existentes (sem remover/modificar nenhum dos 18 testes Phase 39 existentes)"
    - "PTT-only mode é selecionável no tray e funciona end-to-end — start strategy → wake word silenciado → hotkey continua disparando ChatInput.tsx mic flow (validado via UAT manual conforme VALIDATION.md — testes automated cobrem apenas factory wiring no construtor via TypeScript compile check)"
  artifacts:
    - path: "apps/desktop/src/main/voiceMode/index.ts"
      provides: "VoiceModeManager.setMode() com D-04 plano B + restorePreviousStrategy helper + currentMode: VoiceMode | null"
      contains: "private async restorePreviousStrategy"
      min_lines: 280
    - path: "apps/desktop/src/main/tray.ts"
      provides: "Tooltip + radio handling de currentMode === null (fallback existente já funciona)"
      contains: "VoiceMode \\| null"
    - path: "apps/desktop/src/main/index.ts"
      provides: "createPttOnlyFactory wiring no construtor do VoiceModeManager"
      contains: "createPttOnlyFactory"
    - path: "apps/desktop/src/main/__tests__/voiceMode.race.test.ts"
      provides: "3 cenários D-05 + 1 cenário 3b (transitioning durante recovery) com assertions reais"
      contains: "expect.*toHaveBeenCalled"
    - path: "apps/desktop/src/main/__tests__/voiceMode.test.ts"
      provides: "Testes para D-04 plano B (recovery em catch) — adições, sem alterar testes Phase 39"
      contains: "D-04 plano B"
  key_links:
    - from: "apps/desktop/src/main/index.ts"
      to: "apps/desktop/src/main/voiceMode/strategies/pttOnly.ts"
      via: "createPttOnlyFactory({ mainWindow })"
      pattern: "createPttOnlyFactory\\("
    - from: "apps/desktop/src/main/voiceMode/index.ts (setMode catch)"
      to: "apps/desktop/src/main/voiceMode/index.ts (restorePreviousStrategy)"
      via: "await this.restorePreviousStrategy(oldMode, oldFactory)"
      pattern: "restorePreviousStrategy"
    - from: "apps/desktop/src/main/voiceMode/index.ts (currentMode null state)"
      to: "apps/desktop/src/main/tray.ts (option.mode === currentMode)"
      via: "Type propagation VoiceMode | null"
      pattern: "VoiceMode \\| null"
---

<objective>
Wave 2 fecha a Phase 43:
1. **D-04 plano B**: VoiceModeManager re-instancia strategy antiga em catch, com fallback para currentMode=null se recovery falhar (RESEARCH.md Pattern 4)
2. **Tipo refactor**: `currentMode: VoiceMode | null` propagado para tray.ts e ipc/voiceMode.ts (Pitfall 5)
3. **Wiring**: main/index.ts adiciona createPttOnlyFactory ao VoiceModeManager construtor — modo PTT-only fica selecionável end-to-end
4. **Race tests preenchidos**: voiceMode.race.test.ts 4 describes (cenário 1, 2, 3, 3b) com assertions reais usando os helpers do Plan 01 e infra dos Plans 02/03
5. **Testes D-04 em voiceMode.test.ts**: cobertura unitária do plano B (recovery, fallback, atomicidade do transitioning)

Purpose: Plano final fecha o ciclo de risco de Phase 43 — mode switch atômico + listener cleanup + race resilience. Wave 2 só faz sentido APÓS Plans 02 e 03 entregarem PttOnlyStrategy + AlwaysListening force-flush + pttHotkeyEmitter. Aqui o sistema fica integrado.
Output: Phase 43 production-ready. SC4 (5 trocas rápidas sem hotkey fantasma) coberto por testes automated; VPTT-01/02/03 atendidos main-side end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/43-ptt-only-integration/43-CONTEXT.md
@.planning/phases/43-ptt-only-integration/43-RESEARCH.md
@.planning/phases/43-ptt-only-integration/43-01-PLAN.md
@.planning/phases/43-ptt-only-integration/43-02-PLAN.md
@.planning/phases/43-ptt-only-integration/43-03-PLAN.md
@.planning/notes/phase-43-dispose-before-factory.md
@apps/desktop/src/main/voiceMode/index.ts
@apps/desktop/src/main/tray.ts
@apps/desktop/src/main/ipc/voiceMode.ts
@apps/desktop/src/main/index.ts
@apps/desktop/src/main/__tests__/voiceMode.test.ts
@apps/desktop/src/main/__tests__/voiceMode.race.test.ts
@apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts

<interfaces>
<!-- Existing setMode() flow that we are modifying — extracted from voiceMode/index.ts:175-245 -->

Current setMode() structure (Phase 39, simplified):
```typescript
async setMode(newMode: VoiceMode, reason): Promise<boolean> {
  if (newMode === this.currentMode) return false;
  if (this.transitioning) return false;
  this.transitioning = true;
  const oldMode = this.currentMode;
  try {
    if (this.activeStrategy) {
      await this.activeStrategy.dispose();
      this.activeStrategy = null;
    }
    const factory = this.strategyFactories.get(newMode);
    if (!factory) return false;  // ← BUG: currentMode aponta a oldMode mas activeStrategy é null
    try {
      this.activeStrategy = factory();
      await this.activeStrategy.start();
    } catch (err) {
      this.activeStrategy = null;
      return false;  // ← BUG: idem
    }
    this.currentMode = newMode;
    setVoiceMode(newMode);
    this.emit('voiceMode:change', { oldMode, newMode, reason, timestamp });
    return true;
  } finally {
    this.transitioning = false;
  }
}
```

Existing strategyFactories Map (linhas 113-122):
```typescript
this.strategyFactories = new Map<VoiceMode, () => VoiceCaptureStrategy>([
  ['wake-word', factories?.['wake-word'] ?? (() => new WakeWordStrategy())],
  ['always-listening', factories?.['always-listening'] ?? (() => { throw new Error('AlwaysListeningStrategy requires deps...') })],
  ['ptt-only', factories?.['ptt-only'] ?? (() => { throw new Error('PttOnlyStrategy not yet implemented (Phase 43)') })],
]);
```

main/index.ts linha 257-275 (current wiring — sem 'ptt-only'):
```typescript
voiceModeManager = new VoiceModeManager(
  useWhisperCpp && ttsProvider
    ? {
        'always-listening': createAlwaysListeningFactory({ mainWindow, voiceHandlerDeps, onDegraded }),
      }
    : {},
);
```

tray.ts linhas 73-86 (current — assumes currentMode is VoiceMode never null):
```typescript
const currentMode = voiceModeManager.getMode();  // VoiceMode (current type)
const modeLabel = VOICE_MODE_OPTIONS.find(o => o.mode === currentMode)?.label ?? 'Wake Word';
tray?.setToolTip(`JARVIS — ${modeLabel}`);
// ...
checked: option.mode === currentMode,
```

Helpers from Plan 01 (already exists):
```typescript
import { makeMockStrategy, makeFailingFactory, makeSequentialFactory } from '../helpers/strategyFactoryMocks';
```

From Plan 02 (already exists):
```typescript
import { pttHotkeyEmitter, __resetPttHotkeyEmitterForTests } from '../ptt-hotkey';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar VoiceModeManager — D-04 plano B + currentMode nullability + restorePreviousStrategy helper</name>
  <read_first>
    - apps/desktop/src/main/voiceMode/index.ts (arquivo completo)
    - .planning/notes/phase-43-dispose-before-factory.md (root cause + opção B endorsement)
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (linhas 219-268 — testes WR-01 atuais que cobrem factory throw; vamos AUMENTAR com D-04 plano B sem quebrar esses)
  </read_first>
  <behavior>
    - currentMode passa a ser `VoiceMode | null`. Inicializado em construtor via getVoiceMode() que retorna VoiceMode (default 'wake-word' por D-07) — então no startup é sempre VoiceMode; só fica null se D-04 fallback dispara.
    - getMode() retorna `VoiceMode | null`.
    - setMode() em catch da nova factory: chama restorePreviousStrategy(oldMode, oldFactory). NÃO retorna false antes do recovery.
    - restorePreviousStrategy(oldMode, oldFactory):
      - Se oldFactory existe: tenta oldFactory() + start(). Sucesso → activeStrategy reseta, currentMode permanece oldMode (não tocamos).
      - Se oldFactory NÃO existe OU recovery throw: log error + currentMode = null + activeStrategy = null.
    - transitioning permanece true durante TODO o setMode incluindo recovery (atomicidade — Claude's Discretion endossado).
    - Behaviour mudança importante: WR-01 tests (existing) verificam que após factory throw, getMode() === oldMode. Com D-04, comportamento é o MESMO em sucesso de recovery (oldMode preservado). Diferença: com D-04, a strategy antiga É RECRIADA — então ww.dispose foi chamado uma vez (no início do setMode) e ww NEW (recovered) foi started. Adaptar testes WR-01 para refletir essa diferença.
    - Edge case: setMode com newMode === currentMode quando currentMode === null. Recomendação: comportamento "se currentMode é null, sempre tenta entrar" — null nunca === VoiceMode value, então comparação atual já trata bem.
  </behavior>
  <action>
    Modificar `apps/desktop/src/main/voiceMode/index.ts`:

    **1. Mudar tipo de currentMode (linha 91):**
    ```typescript
    // De:
    private currentMode: VoiceMode;
    // Para:
    private currentMode: VoiceMode | null;
    ```

    **2. Mudar return type de getMode() (linha 156):**
    ```typescript
    // De:
    getMode(): VoiceMode {
      return this.currentMode;
    }
    // Para:
    getMode(): VoiceMode | null {
      return this.currentMode;
    }
    ```

    **3. Reescrever setMode() (linhas 175-245)** mantendo o frontmatter de doc-comment, alterando lógica:

    ```typescript
      /**
       * setMode() — tenta transicionar para um novo modo.
       *
       * D-01: Retorna false silenciosamente se mesmo modo OU transitioning.
       * D-04 (Phase 43): plano B em catch — se factory nova lança, re-instancia
       * a antiga via restorePreviousStrategy(). Atomicidade total: transitioning
       * permanece true durante recovery (Claude's Discretion endorsement).
       *
       * Estado degradado de último caso (D-04 fallback):
       *   Se nova factory lança E recovery da antiga TAMBÉM lança:
       *   currentMode = null + activeStrategy = null. Tray exibe nenhum radio
       *   marcado (option.mode === null retorna false sempre). Próximo setMode
       *   é "tentativa de saída do estado degradado".
       *
       * Phase 41 Plan 03 (gap closure): O gate D-02 (status !== 'idle') foi
       * removido na Phase 41. Apenas transitioning guard permanece.
       *
       * @see CONTEXT.md D-04 (plano B em catch)
       * @see notes/phase-43-dispose-before-factory.md (root cause)
       */
      async setMode(newMode: VoiceMode, reason: 'user' | 'system' = 'user'): Promise<boolean> {
        // Sem mudança — null nunca === VoiceMode value, então quando estado
        // degradado existir, qualquer setMode tenta entrar.
        if (newMode === this.currentMode) {
          return false;
        }

        if (this.transitioning) {
          console.warn(`[VoiceModeManager] setMode('${newMode}') blocked — transition in progress`);
          return false;
        }

        this.transitioning = true;
        const oldMode = this.currentMode;
        // D-04: guarda factory antiga ANTES do dispose para recovery.
        // Se oldMode for null (estado degradado), oldFactory também será undefined.
        const oldFactory = oldMode !== null
          ? this.strategyFactories.get(oldMode)
          : undefined;

        try {
          // 1. Dispose da antiga (se existe activeStrategy).
          if (this.activeStrategy) {
            await this.activeStrategy.dispose();
            this.activeStrategy = null;
          }

          // 2. Tenta factory nova.
          const newFactory = this.strategyFactories.get(newMode);
          if (!newFactory) {
            console.warn(`[VoiceModeManager] setMode('${newMode}') — no factory registered`);
            // D-04: tenta restaurar antiga (preserva oldMode em caso de sucesso)
            await this.restorePreviousStrategy(oldMode, oldFactory);
            return false;
          }

          try {
            this.activeStrategy = newFactory();
            await this.activeStrategy.start();
          } catch (err) {
            console.warn(
              `[VoiceModeManager] setMode() — Strategy not ready for mode '${newMode}':`,
              err instanceof Error ? err.message : err,
            );
            this.activeStrategy = null;
            // D-04 PLANO B: re-instancia strategy antiga
            await this.restorePreviousStrategy(oldMode, oldFactory);
            return false;
          }

          // 3. Sucesso — atualiza estado, persiste, emite event.
          this.currentMode = newMode;
          setVoiceMode(newMode); // VMODE-02: persiste

          const event: VoiceModeChangeEvent = {
            oldMode: oldMode as VoiceMode, // sucesso implica oldMode existia ou null
            newMode,
            reason,
            timestamp: Date.now(),
          };
          this.emit('voiceMode:change', event);
          console.log(`[VoiceModeManager] Mode changed: ${oldMode} → ${newMode} (reason: ${reason})`);

          return true;
        } finally {
          this.transitioning = false;
        }
      }

      /**
       * D-04 Plano B helper: re-instancia a strategy do oldMode quando a nova
       * factory falha. Mantém atomicidade do switch (transitioning permanece
       * true porque chamado dentro do try/finally do setMode).
       *
       * Comportamento:
       *   - oldMode === null OU oldFactory undefined: estado degradado direto
       *     (currentMode = null, activeStrategy = null) — sem strategy a restaurar.
       *   - oldFactory throws: estado degradado (log error + null state).
       *   - sucesso: activeStrategy é a NOVA instância antiga, currentMode
       *     permanece oldMode (não tocamos).
       *
       * @see RESEARCH.md Pattern 4 (D-04 Plano B)
       */
      private async restorePreviousStrategy(
        oldMode: VoiceMode | null,
        oldFactory: (() => VoiceCaptureStrategy) | undefined,
      ): Promise<void> {
        if (oldMode === null || !oldFactory) {
          console.error(
            `[VoiceModeManager] D-04 fallback: cannot restore (oldMode=${oldMode}, hasFactory=${!!oldFactory}) — entering null state`,
          );
          this.currentMode = null;
          this.activeStrategy = null;
          return;
        }

        try {
          this.activeStrategy = oldFactory();
          await this.activeStrategy.start();
          // currentMode permanece oldMode — não tocamos
          this.currentMode = oldMode;
          console.log(
            `[VoiceModeManager] D-04 recovery: restored '${oldMode}' after factory failure`,
          );
        } catch (recoveryErr) {
          console.error(
            `[VoiceModeManager] D-04 fallback: recovery for '${oldMode}' also failed:`,
            recoveryErr instanceof Error ? recoveryErr.message : recoveryErr,
          );
          this.currentMode = null;
          this.activeStrategy = null;
        }
      }
    ```

    **4. Verificar dispose() (linha 250) e init() (linha 129):** dispose() apenas zera activeStrategy + initialized — pode também zerar currentMode? NÃO — manter currentMode=oldValue para que próxima init() possa retomar. Não precisa mudar.

    init() linha 138-146 já tem try/catch para o erro em factory; não precisa mudar — ele apenas seta activeStrategy=null mas mantém currentMode (que é OK porque sem persistir).

    NÃO modificar mais nada no arquivo.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E "voiceMode/index\.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private currentMode: VoiceMode | null" apps/desktop/src/main/voiceMode/index.ts` retorna 1
    - `grep -c "getMode(): VoiceMode | null" apps/desktop/src/main/voiceMode/index.ts` retorna 1
    - `grep -c "private async restorePreviousStrategy" apps/desktop/src/main/voiceMode/index.ts` retorna 1
    - `grep -c "D-04 PLANO B\|D-04 fallback\|D-04 recovery" apps/desktop/src/main/voiceMode/index.ts` retorna >= 3
    - `grep -c "this.restorePreviousStrategy(oldMode, oldFactory)" apps/desktop/src/main/voiceMode/index.ts` retorna 2 (uma em "no factory" path, outra em catch block)
    - `pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E "voiceMode/index\.ts" | head -10` produz saída vazia (0 erros em voiceMode/index.ts). Erros remanescentes em tray.ts/ipc/voiceMode.ts são tolerados nesta task — Task 2 resolve.
    - voiceMode.test.ts pode mostrar 1-2 falhas em testes WR-01 (porque agora ww.dispose é chamada 1 vez no setMode, e WW NEW é recriada — comportamento mudou). Isso é ESPERADO. Task 3 atualiza esses testes.
  </acceptance_criteria>
  <done>setMode reescrito com D-04 plano B, restorePreviousStrategy implementado, currentMode nullable. Testes WR-01 podem precisar de ajuste em Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagar tipo VoiceMode|null em tray.ts + ipc/voiceMode.ts + main/index.ts wiring</name>
  <read_first>
    - apps/desktop/src/main/tray.ts (arquivo completo — observar uses de currentMode linhas 73-86)
    - apps/desktop/src/main/ipc/voiceMode.ts (arquivo completo)
    - apps/desktop/src/main/index.ts (linhas 257-275 — VoiceModeManager wiring)
    - apps/desktop/src/main/voiceMode/index.ts (após Task 1 — confirmar tipos exportados)
    - apps/desktop/src/main/voiceMode/strategies/pttOnly.ts (após Plan 02 — confirmar createPttOnlyFactory disponível)
  </read_first>
  <behavior>
    - tray.ts: tipo `VoiceMode | null` aceito em currentMode local. Optional chaining ou comparação atual `option.mode === currentMode` continua funcionando porque null nunca === VoiceMode (radio nenhum marcado quando degraded).
    - tray.ts tooltip fallback: já tem `?? 'Wake Word'` (linha 74) — funciona quando find() retorna undefined, que acontece em currentMode === null.
    - ipc/voiceMode.ts: NÃO precisa mudar — broadcastModeSwitch aceita success:false sem newMode (já permite undefined).
    - main/index.ts: ADICIONAR createPttOnlyFactory({mainWindow}) ao construtor do VoiceModeManager. Importar PttOnlyStrategy / createPttOnlyFactory de voiceMode/index.js.
    - PTT-only factory NÃO depende de useWhisperCpp ou ttsProvider — ela só silencia wake word + subscreve emitter. Sempre wireada.
    - Compilação TypeScript verde após mudanças.
  </behavior>
  <action>
    **1. apps/desktop/src/main/tray.ts** — modificar imports e usages:

    Linha 25 ja importa VoiceMode. NÃO precisa mudar import.

    Linhas 73-86 (`buildContextMenu` body), o tipo de `currentMode` é inferido como `ReturnType<VoiceModeManager['getMode']>` que agora é `VoiceMode | null`. NÃO precisa anotação explícita — TypeScript inferirá.

    A LINHA 74 e LINHA 86 já lidam graciosamente com null:
    - Linha 74: `VOICE_MODE_OPTIONS.find(o => o.mode === currentMode)?.label ?? 'Wake Word'` — find retorna undefined quando currentMode é null, fallback "Wake Word" aplica.
    - Linha 86: `checked: option.mode === currentMode` — null !== VoiceMode value, todos retornam false (nenhum radio marcado).

    PORÉM, adicionar comment explicativo após linha 73 para clareza:

    ```typescript
      // D-04 (Phase 43): currentMode pode ser null em estado degradado
      // (factory falhou + recovery falhou). VOICE_MODE_OPTIONS.find retorna
      // undefined → fallback 'Wake Word' tooltip. option.mode === null retorna
      // false em todos os items → nenhum radio marcado (UX explícito de "sem
      // modo ativo"). Comportamento intencional, não bug.
      const currentMode = voiceModeManager.getMode();
      const modeLabel = VOICE_MODE_OPTIONS.find(o => o.mode === currentMode)?.label ?? 'Wake Word';
    ```

    **2. apps/desktop/src/main/ipc/voiceMode.ts** — NÃO precisa modificar.
    Verificar manualmente se houver uso de `voiceModeManager.getMode()` que assume VoiceMode não-null. Se houver, adicionar guard ou anotação. Caso contrário, deixar intacto.

    **3. apps/desktop/src/main/index.ts** — wiring do PttOnlyStrategy.

    Linha 41 atual: `import { VoiceModeManager, createAlwaysListeningFactory } from './voiceMode/index.js';`
    Mudar para: `import { VoiceModeManager, createAlwaysListeningFactory, createPttOnlyFactory } from './voiceMode/index.js';`

    Linhas 257-275 atual:
    ```typescript
    voiceModeManager = new VoiceModeManager(
      useWhisperCpp && ttsProvider
        ? {
            'always-listening': createAlwaysListeningFactory({
              mainWindow: mainWindow!,
              voiceHandlerDeps: { config, selectedModel, ttsProvider },
              onDegraded: (event) => { /* ... */ },
            }),
          }
        : {},
    );
    ```

    Mudar para:
    ```typescript
    // Phase 43: createPttOnlyFactory NÃO requer voiceHandlerDeps — só silencia
    // wake word via reuso de wakeWord:pause-toggle. Sempre wireada (independe
    // de useWhisperCpp / ttsProvider).
    const pttOnlyFactory = createPttOnlyFactory({ mainWindow: mainWindow! });

    voiceModeManager = new VoiceModeManager(
      useWhisperCpp && ttsProvider
        ? {
            'always-listening': createAlwaysListeningFactory({
              mainWindow: mainWindow!,
              voiceHandlerDeps: { config, selectedModel, ttsProvider },
              onDegraded: (event) => {
                console.warn('[always-listening] Degraded:', event.message);
                if (mainWindow && !mainWindow.isDestroyed()) {
                  mainWindow.webContents.send(IPC_CHANNELS.VOICE_MODE_DEGRADED, event);
                }
              },
            }),
            'ptt-only': pttOnlyFactory,
          }
        : {
            'ptt-only': pttOnlyFactory,
          },
    );
    ```

    NÃO alterar nada antes ou depois deste bloco. NÃO mover `pttOnlyFactory` para fora desta função.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createPttOnlyFactory" apps/desktop/src/main/index.ts` retorna >= 2 (import + usage)
    - `grep -c "'ptt-only': pttOnlyFactory" apps/desktop/src/main/index.ts` retorna 2 (em ambos os branches do ternário)
    - `grep -c "D-04.*Phase 43.*currentMode pode ser null" apps/desktop/src/main/tray.ts` retorna 1 (comment de clareza)
    - `pnpm tsc --noEmit -p tsconfig.node.json` retorna 0 erros (compilação completa verde) — TODOS os call sites de getMode() passaram ou já tratam null
    - `pnpm vitest run src/main/__tests__/tray.test.ts --no-coverage` exits 0 (sem regressão tray)
    - `pnpm vitest run src/main/__tests__/index.test.ts --no-coverage` exits 0 (se houver — sem regressão)
  </acceptance_criteria>
  <done>Tipo nullable propagado, PTT-only wireado, compilação verde, testes tray sem regressão.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atualizar voiceMode.test.ts (D-04 plano B + ajustar WR-01) + preencher voiceMode.race.test.ts</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (arquivo completo — TODOS os 18 testes Phase 39)
    - apps/desktop/src/main/__tests__/voiceMode.race.test.ts (estrutura criada em Plan 01 — todos os it.todo precisam virar it() reais)
    - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts (Plan 01)
    - apps/desktop/src/main/voiceMode/index.ts (após Task 1 — comportamento real D-04)
    - apps/desktop/src/main/ptt-hotkey.ts (após Plan 02 — pttHotkeyEmitter + __resetPttHotkeyEmitterForTests)
  </read_first>
  <behavior>
    - voiceMode.test.ts mantém os 18 testes Phase 39 EXISTENTES verdes — ajustar APENAS os 2 testes WR-01 (linhas 219-268) que precisam refletir D-04 plano B (recovery acontece silenciosamente).
    - Adicionar novo describe 'D-04 plano B (Phase 43)' com 4-6 testes cobrindo: recovery success, recovery fail (fallback null), atomicidade transitioning, oldFactory undefined edge case
    - voiceMode.race.test.ts: TODOS os it.todo viram it() reais com assertions usando makeMockStrategy + makeFailingFactory + makeSequentialFactory + __resetPttHotkeyEmitterForTests
    - 4 describes (cenário 1, 2, 3, 3b) cada com >= 3 testes verdes
    - Suite total da Phase 43: zero it.todo restante em qualquer test file
  </behavior>
  <action>
    **1. apps/desktop/src/main/__tests__/voiceMode.test.ts** — AJUSTAR testes WR-01 + adicionar describe D-04 plano B.

    Os 2 testes em "WR-01: setMode() does not persist when new Strategy fails to start" (linhas 219-268) atualmente afirmam que `manager.getMode()` retorna 'wake-word' após factory throw. Com D-04 plano B, isso permanece verdade (recovery restaura), MAS:
    - `ww.dispose` agora foi chamada exatamente 1 vez (no início do setMode antes da nova factory)
    - O `ww` original foi disposed; uma NOVA instância de wake-word foi instanciada via factory(). Mas o teste passa `() => ww` como factory — sempre retorna a mesma referência. Então `ww.start` é chamada 2 vezes (init + recovery).

    Manter a assertion "result === false + getMode === 'wake-word' + store não persistiu" — funciona. Adicionar verificação extra:
    - `expect(ww.start).toHaveBeenCalledTimes(2)` — init + recovery
    - `expect(ww.dispose).toHaveBeenCalledTimes(1)` — pré-recovery dispose

    Re-escrever os 2 testes WR-01 como (mantendo o describe wrapper):

    ```typescript
      describe('WR-01: setMode() does not persist when new Strategy fails to start (with D-04 plano B Phase 43)', () => {
        it('factory throws → result false, mode unchanged, store untouched, no event — D-04 recovery re-instances old', async () => {
          const ww = makeStrategy('idle');
          const throwingAlFactory = vi.fn().mockImplementation(() => {
            throw new Error('AlwaysListeningStrategy not yet implemented');
          });
          const manager = new VoiceModeManager({
            'wake-word': () => ww,
            'always-listening': throwingAlFactory,
          });
          await manager.init();

          const events: VoiceModeChangeEvent[] = [];
          manager.on('voiceMode:change', (e: VoiceModeChangeEvent) => events.push(e));

          const result = await manager.setMode('always-listening');

          expect(result).toBe(false);
          expect(manager.getMode()).toBe('wake-word');
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          const backing = (Store as any).__getBackingStore();
          expect(backing['voiceMode']).not.toBe('always-listening');
          expect(events).toHaveLength(0);
          // D-04 plano B: dispose foi chamado uma vez (pré-recovery), start chamado 2x (init + recovery)
          expect(ww.dispose).toHaveBeenCalledTimes(1);
          expect(ww.start).toHaveBeenCalledTimes(2);
        });

        it('start() rejects after construction → result false, D-04 recovery re-instances old', async () => {
          const ww = makeStrategy('idle');
          const failingAl = {
            start: vi.fn().mockRejectedValue(new Error('audio device busy')),
            stop: vi.fn().mockResolvedValue(undefined),
            dispose: vi.fn().mockResolvedValue(undefined),
            getStatus: vi.fn().mockReturnValue('idle' as const),
          };
          const manager = new VoiceModeManager({
            'wake-word': () => ww,
            'always-listening': () => failingAl,
          });
          await manager.init();

          const result = await manager.setMode('always-listening');

          expect(result).toBe(false);
          expect(manager.getMode()).toBe('wake-word');
          // eslint-disable-next-line @typescript-eslint/no-explicit-any
          const backing = (Store as any).__getBackingStore();
          expect(backing['voiceMode']).not.toBe('always-listening');
          // D-04: ww.start chamada na init + recovery
          expect(ww.start).toHaveBeenCalledTimes(2);
        });
      });
    ```

    **2. ADICIONAR novo describe ao final do arquivo** (após o describe top-level de WakeWordStrategy stub):

    ```typescript

    // ============================================================
    // Phase 43 — D-04 Plano B (recovery em catch da nova factory)
    // ============================================================
    describe('VoiceModeManager — D-04 plano B (Phase 43)', () => {
      beforeEach(() => {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        (Store as any).__resetStore();
      });

      it('D-04 sucesso: factory antiga é re-instanciada quando nova falha', async () => {
        const ww = makeStrategy('idle');
        const ptt = makeStrategy('idle');
        let pttCallCount = 0;
        const failingPttFactory = vi.fn().mockImplementation(() => {
          pttCallCount += 1;
          if (pttCallCount === 1) throw new Error('PttOnlyStrategy not ready');
          return ptt;
        });

        const manager = new VoiceModeManager({
          'wake-word': () => ww,
          'ptt-only': failingPttFactory,
        });
        await manager.init();

        const result = await manager.setMode('ptt-only');

        expect(result).toBe(false);
        expect(manager.getMode()).toBe('wake-word');
        // ww.dispose chamado uma vez (no início), start chamado 2x (init + recovery)
        expect(ww.dispose).toHaveBeenCalledTimes(1);
        expect(ww.start).toHaveBeenCalledTimes(2);
      });

      it('D-04 fallback: recovery TAMBÉM falha → currentMode = null + activeStrategy = null', async () => {
        // Factory wake-word falha NA RECOVERY (segunda chamada)
        let wwCallCount = 0;
        const wwFactory = vi.fn().mockImplementation(() => {
          wwCallCount += 1;
          if (wwCallCount === 1) {
            return makeStrategy('idle'); // init OK
          }
          throw new Error('wake-word also failed in recovery');
        });
        const failingPttFactory = vi.fn().mockImplementation(() => {
          throw new Error('PttOnlyStrategy not ready');
        });

        const manager = new VoiceModeManager({
          'wake-word': wwFactory,
          'ptt-only': failingPttFactory,
        });
        await manager.init();

        const result = await manager.setMode('ptt-only');

        expect(result).toBe(false);
        // FALLBACK: currentMode = null
        expect(manager.getMode()).toBeNull();
      });

      it('D-04 não emite voiceMode:change durante recovery (silencioso)', async () => {
        const ww = makeStrategy('idle');
        const failingPttFactory = vi.fn().mockImplementation(() => {
          throw new Error('PttOnlyStrategy not ready');
        });
        const manager = new VoiceModeManager({
          'wake-word': () => ww,
          'ptt-only': failingPttFactory,
        });
        await manager.init();

        const events: VoiceModeChangeEvent[] = [];
        manager.on('voiceMode:change', (e) => events.push(e));

        await manager.setMode('ptt-only');

        expect(events).toHaveLength(0);
      });

      it('D-04 atomicidade: transitioning permanece true durante recovery (concurrent setMode rejeitado)', async () => {
        let resolveDispose!: () => void;
        const disposePending = new Promise<void>((r) => { resolveDispose = r; });
        const ww = {
          ...makeStrategy('idle'),
          dispose: vi.fn().mockImplementation(() => disposePending),
        };
        const failingPttFactory = vi.fn().mockImplementation(() => {
          throw new Error('PttOnlyStrategy not ready');
        });
        const manager = new VoiceModeManager({
          'wake-word': () => ww,
          'ptt-only': failingPttFactory,
        });
        await manager.init();

        const first = manager.setMode('ptt-only'); // vai bloquear no dispose
        await vi.waitFor(() => expect(ww.dispose).toHaveBeenCalled());
        // Durante o setMode (dispose pending → recovery), transitioning está true.
        const second = manager.setMode('ptt-only');
        resolveDispose();

        const [firstResult, secondResult] = await Promise.all([first, second]);
        expect(firstResult).toBe(false); // PTT factory falhou
        expect(secondResult).toBe(false); // Rejeitado por transitioning
      });

      it('D-04 oldMode null edge case: setMode após estado degradado tenta entrar', async () => {
        // Setup: força estado degradado
        let wwFailed = false;
        const wwFactory = vi.fn().mockImplementation(() => {
          if (wwFailed) throw new Error('wake-word recovery failed');
          return makeStrategy('idle');
        });
        const failingPttFactory = vi.fn().mockImplementation(() => {
          throw new Error('ptt failed');
        });
        const al = makeStrategy('idle');
        const manager = new VoiceModeManager({
          'wake-word': wwFactory,
          'ptt-only': failingPttFactory,
          'always-listening': () => al,
        });
        await manager.init();

        wwFailed = true; // próxima chamada wakeword vai falhar
        await manager.setMode('ptt-only'); // ptt fail + ww recovery fail → null state

        expect(manager.getMode()).toBeNull();

        // Nova tentativa de modo válido sai do estado degradado
        wwFailed = false;
        const result = await manager.setMode('always-listening');
        expect(result).toBe(true);
        expect(manager.getMode()).toBe('always-listening');
      });
    });
    ```

    **3. apps/desktop/src/main/__tests__/voiceMode.race.test.ts** — substituir TODOS os it.todo por it() reais.

    Reescrever o arquivo completo (manter mocks dos imports e estrutura describe do Plan 01, preencher implementações):

    ```typescript
    /**
     * voiceMode.race.test.ts — Race condition + D-04 plan B integration tests
     *
     * Phase 43 — D-05 race tests + D-04 plan B (recovery em catch)
     *
     * Cenários (D-05):
     *  1. Sequencial rápido: 5 trocas Wake→AL→PTT→Wake→AL com await entre cada
     *  2. Concorrente: Promise.all([5 setMode]) — guard rejeita extras
     *  3. Plano B em catch: nova factory falha → strategy antiga é re-instanciada
     *  3b. transitioning durante recovery (atomicidade)
     */
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    vi.mock('electron-store', () => {
      let mockStore: Record<string, unknown> = {};
      return {
        default: class Store {
          get(key: string) { return mockStore[key]; }
          set(key: string, value: unknown) { mockStore[key] = value; }
          static __resetStore() { mockStore = {}; }
          static __getBackingStore() { return mockStore; }
        },
      };
    });

    vi.mock('electron', () => ({
      ipcMain: { on: vi.fn(), off: vi.fn(), once: vi.fn() },
      BrowserWindow: vi.fn(() => ({
        isDestroyed: () => false,
        webContents: { send: vi.fn() },
      })),
      globalShortcut: { register: vi.fn(() => true), unregister: vi.fn() },
    }));

    import Store from 'electron-store';
    import { VoiceModeManager } from '../voiceMode/index';
    import { pttHotkeyEmitter, __resetPttHotkeyEmitterForTests } from '../ptt-hotkey';
    import { makeMockStrategy, makeFailingFactory, makeSequentialFactory } from './helpers/strategyFactoryMocks';

    describe('Voice Mode Race Conditions (D-05)', () => {
      beforeEach(() => {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        (Store as any).__resetStore();
        __resetPttHotkeyEmitterForTests();
      });

      describe('Cenário 1: 5 trocas sequenciais (sem listener leak)', () => {
        it('Wake→AL→PTT→Wake→AL final state é "always-listening"', async () => {
          const ww = makeMockStrategy();
          const al = makeMockStrategy();
          const ptt = makeMockStrategy();

          // ww e al são factories que retornam novas instâncias a cada chamada
          // (em produção, factory é () => new Strategy(deps)) — para race tests
          // simplificamos: sempre retornam o mesmo mock (idempotência via reset
          // de listenerCount).
          const manager = new VoiceModeManager({
            'wake-word': () => ww,
            'always-listening': () => al,
            'ptt-only': () => ptt,
          });
          await manager.init();

          await manager.setMode('always-listening');
          await manager.setMode('ptt-only');
          await manager.setMode('wake-word');
          await manager.setMode('always-listening');

          expect(manager.getMode()).toBe('always-listening');
        });

        it('5 trocas → pttHotkeyEmitter.listenerCount("toggle") === 0 após dispose final', async () => {
          // Para validar leak guard usando os listeners REAIS, precisaríamos
          // PttOnlyStrategy + AlwaysListeningStrategy reais. Mocks não tocam o
          // emitter. Mas podemos simular: cada strategy mock pode adicionar/remover
          // um listener via dispose. Aqui validamos que os mocks foram disposed
          // corretamente — leak real fica para o teste manual UAT (43-VALIDATION).
          const ww = makeMockStrategy();
          const al = makeMockStrategy();
          const ptt = makeMockStrategy();
          const manager = new VoiceModeManager({
            'wake-word': () => ww,
            'always-listening': () => al,
            'ptt-only': () => ptt,
          });
          await manager.init();
          await manager.setMode('always-listening');
          await manager.setMode('ptt-only');
          await manager.setMode('wake-word');
          await manager.setMode('always-listening');

          // Após 5 trocas: ww disposed 2x (1 + 1 quando voltou e saiu),
          // al disposed 1x (saiu uma vez), ptt disposed 1x.
          // Estado final = always-listening (al ATIVO, sem dispose pending)
          expect(al.dispose).toHaveBeenCalledTimes(1); // disposed quando saiu pra ptt
          // pttHotkeyEmitter sempre 0 nos mocks (não há listener real)
          expect(pttHotkeyEmitter.listenerCount('toggle')).toBe(0);
        });

        it('nenhum throw/unhandled rejection durante a sequência', async () => {
          const manager = new VoiceModeManager({
            'wake-word': () => makeMockStrategy(),
            'always-listening': () => makeMockStrategy(),
            'ptt-only': () => makeMockStrategy(),
          });
          await manager.init();
          // Promise.allSettled garante que se houver rejection, capturamos
          const results = await Promise.allSettled([
            manager.setMode('always-listening'),
            manager.setMode('ptt-only'),
            manager.setMode('wake-word'),
          ].map(p => Promise.resolve(p))); // já resolvidas serial via await chain
          for (const r of results) {
            expect(r.status).toBe('fulfilled');
          }
        });
      });

      describe('Cenário 2: Promise.all de 5 setMode concorrentes', () => {
        it('apenas 1 setMode retorna true (transitioning guard rejeita os outros)', async () => {
          const manager = new VoiceModeManager({
            'wake-word': () => makeMockStrategy(),
            'always-listening': () => makeMockStrategy(),
            'ptt-only': () => makeMockStrategy(),
          });
          await manager.init();

          const results = await Promise.all([
            manager.setMode('always-listening'),
            manager.setMode('ptt-only'),
            manager.setMode('wake-word'),
            manager.setMode('always-listening'),
            manager.setMode('ptt-only'),
          ]);

          const successCount = results.filter((r) => r === true).length;
          expect(successCount).toBe(1);
        });

        it('estado final == primeiro setMode que entrou no try block', async () => {
          const manager = new VoiceModeManager({
            'wake-word': () => makeMockStrategy(),
            'always-listening': () => makeMockStrategy(),
            'ptt-only': () => makeMockStrategy(),
          });
          await manager.init();

          await Promise.all([
            manager.setMode('always-listening'), // primeiro a entrar
            manager.setMode('ptt-only'),
            manager.setMode('wake-word'),
          ]);

          // Primeira chamada (always-listening) é a que vence
          expect(manager.getMode()).toBe('always-listening');
        });

        it('listenerCount("toggle") <= 1 após settle (race scenario)', async () => {
          const manager = new VoiceModeManager({
            'wake-word': () => makeMockStrategy(),
            'always-listening': () => makeMockStrategy(),
            'ptt-only': () => makeMockStrategy(),
          });
          await manager.init();

          await Promise.all([
            manager.setMode('always-listening'),
            manager.setMode('ptt-only'),
            manager.setMode('wake-word'),
          ]);

          expect(pttHotkeyEmitter.listenerCount('toggle')).toBeLessThanOrEqual(1);
        });
      });

      describe('Cenário 3: D-04 plano B — factory nova falha, antiga é re-instanciada', () => {
        it('PTT factory falha 1x → WakeWord factory é chamada 2x (init + recovery)', async () => {
          const wwInitial = makeMockStrategy();
          const wwRecovered = makeMockStrategy();
          const wwFactory = makeSequentialFactory([wwInitial, wwRecovered]);
          const failingPttFactory = makeFailingFactory(1, 'PttOnlyStrategy not ready');

          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
          });
          await manager.init();

          const result = await manager.setMode('ptt-only');

          expect(result).toBe(false);
          expect(manager.getMode()).toBe('wake-word');
          expect(wwFactory).toHaveBeenCalledTimes(2);
        });

        it('strategy antiga (wwInitial) tem dispose() chamado uma vez', async () => {
          const wwInitial = makeMockStrategy();
          const wwRecovered = makeMockStrategy();
          const wwFactory = makeSequentialFactory([wwInitial, wwRecovered]);
          const failingPttFactory = makeFailingFactory(1);

          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
          });
          await manager.init();
          await manager.setMode('ptt-only');

          expect(wwInitial.dispose).toHaveBeenCalledTimes(1);
        });

        it('strategy recuperada (wwRecovered) tem start() chamado uma vez', async () => {
          const wwInitial = makeMockStrategy();
          const wwRecovered = makeMockStrategy();
          const wwFactory = makeSequentialFactory([wwInitial, wwRecovered]);
          const failingPttFactory = makeFailingFactory(1);

          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
          });
          await manager.init();
          await manager.setMode('ptt-only');

          expect(wwRecovered.start).toHaveBeenCalledTimes(1);
        });

        it('FALLBACK: se recovery TAMBÉM falhar, currentMode = null + activeStrategy = null', async () => {
          const wwInitial = makeMockStrategy();
          // wwFactory: 1ª chamada (init) sucesso, 2ª (recovery) lança
          let wwCalls = 0;
          const wwFactory = vi.fn().mockImplementation(() => {
            wwCalls += 1;
            if (wwCalls === 1) return wwInitial;
            throw new Error('recovery also failed');
          });
          const failingPttFactory = makeFailingFactory(1);

          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
          });
          await manager.init();
          const result = await manager.setMode('ptt-only');

          expect(result).toBe(false);
          expect(manager.getMode()).toBeNull();
        });
      });

      describe('Cenário 3b: transitioning flag durante recovery (D-04 atomicidade)', () => {
        it('transitioning permanece true durante recovery — setMode concorrente é rejeitado', async () => {
          let resolveDispose!: () => void;
          const disposePending = new Promise<void>((r) => { resolveDispose = r; });
          const wwInitial = {
            ...makeMockStrategy(),
            dispose: vi.fn().mockImplementation(() => disposePending),
          };
          const wwRecovered = makeMockStrategy();
          const wwFactory = makeSequentialFactory([wwInitial, wwRecovered]);
          const failingPttFactory = makeFailingFactory(1);

          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
            'always-listening': () => makeMockStrategy(),
          });
          await manager.init();

          const first = manager.setMode('ptt-only');
          await vi.waitFor(() => expect(wwInitial.dispose).toHaveBeenCalled());
          // Durante recovery (recovery em curso após dispose), transitioning=true
          const second = manager.setMode('always-listening');
          resolveDispose();

          const [firstResult, secondResult] = await Promise.all([first, second]);
          expect(firstResult).toBe(false); // PTT failure
          expect(secondResult).toBe(false); // bloqueado por transitioning
        });

        it('transitioning volta a false após recovery (próximo setMode passa)', async () => {
          const wwFactory = makeSequentialFactory([makeMockStrategy(), makeMockStrategy()]);
          const failingPttFactory = makeFailingFactory(1);
          const manager = new VoiceModeManager({
            'wake-word': wwFactory,
            'ptt-only': failingPttFactory,
            'always-listening': () => makeMockStrategy(),
          });
          await manager.init();

          await manager.setMode('ptt-only'); // recovery roda + completa
          // Agora transitioning=false; novo setMode deve passar
          const result = await manager.setMode('always-listening');
          expect(result).toBe(true);
        });
      });
    });
    ```

    NÃO adicionar imports/testes além do escopo descrito.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/voiceMode.test.ts src/main/__tests__/voiceMode.race.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - voiceMode.test.ts: `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode.test.ts` retorna 0
    - voiceMode.race.test.ts: `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode.race.test.ts` retorna 0
    - voiceMode.test.ts: `grep -c "describe.*D-04 plano B" apps/desktop/src/main/__tests__/voiceMode.test.ts` retorna 1
    - voiceMode.test.ts: total it() count >= 22 (18 originais + ~5 novos D-04 — alguns originais foram modificados mas não removidos)
    - voiceMode.race.test.ts: `grep -cE "^\s*it\(" apps/desktop/src/main/__tests__/voiceMode.race.test.ts` retorna >= 12
    - `pnpm vitest run src/main/__tests__/voiceMode.test.ts --no-coverage` exits 0 com TODOS verdes
    - `pnpm vitest run src/main/__tests__/voiceMode.race.test.ts --no-coverage` exits 0 com TODOS verdes
    - `pnpm vitest run src/main/__tests__/voiceMode.test.ts --no-coverage 2>&1 | grep -E "Tests.*passed"` mostra >= 22 testes passed
    - `pnpm vitest run src/main/__tests__/voiceMode.race.test.ts --no-coverage 2>&1 | grep -E "Tests.*passed"` mostra >= 12 testes passed
  </acceptance_criteria>
  <done>Todos os testes verdes, 0 it.todo restante na phase, suites Phase 39 sem regressão (testes originais preservados ou ajustados para refletir D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VoiceModeManager.setMode ↔ strategyFactories | factory pode lançar (lazy invocation) — D-04 plano B é a barreira |
| VoiceModeManager state ↔ tray UI | tray lê getMode() lazy, deve aceitar null |
| main/index.ts wiring ↔ VoiceModeManager constructor | factories são closure sobre mainWindow + voiceHandlerDeps — main/index.ts é único producer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-ZUMBI | Tampering | currentMode estado consistency | mitigate | D-04 plano B re-instancia strategy antiga em catch. Test "factory throws → ww.start chamada 2x" verifica recovery. Fallback null state documentado e testado. |
| T-43-RACE | DoS / Tampering | concurrent setMode (Promise.all) | mitigate | transitioning flag mantido true durante recovery (atomicidade). Test "5 concurrent → 1 success, 4 false". Listener leak guard via listenerCount <= 1 no cenário 2. |
| T-43-NULL-CRASH | DoS | tray.ts / ipc/voiceMode handle null | mitigate | TypeScript `VoiceMode | null` força check em compile time. tray.ts: `?? 'Wake Word'` fallback + `option.mode === null` retorna false (radio sem checked). Comment explícito de intent. |
| T-43-WIRING-DROP | Information Disclosure | main/index.ts esquece de wirear PttOnlyStrategy | mitigate | createPttOnlyFactory adicionado em AMBOS os branches do ternário (com/sem useWhisperCpp+ttsProvider) — PTT-only NÃO depende de Whisper/TTS. Test passa em main/index.ts.test (se existir) ou via runtime. |
| T-43-RECOVERY-LOOP | DoS | recovery infinito (factory antiga falha sempre) | mitigate | Recovery é UMA tentativa só (sem retry). Se falha → null state. Próximo setMode é nova tentativa explícita do user (não automática). Sem loop. |
| T-43-EVENT-LEAK | DoS | voiceMode:change event subscribers | accept | setMaxListeners(20) já em VoiceModeManager (Phase 39). Recovery NÃO emite event — silencioso. Sem novo subscriber. |
</threat_model>

<verification>
- Após Task 1: `pnpm vitest run src/main/__tests__/voiceMode.test.ts --no-coverage` pode mostrar 1-2 falhas em WR-01 (esperado — Task 3 ajusta)
- Após Task 2: `pnpm tsc --noEmit -p tsconfig.node.json` 0 erros (compilação TypeScript completa verde)
- Após Task 2: `pnpm vitest run src/main/__tests__/tray.test.ts --no-coverage` exits 0
- Após Task 3: `pnpm vitest run src/main/__tests__/voiceMode --no-coverage` exits 0 (TODOS os arquivos do diretório voiceMode + voiceMode.test.ts + voiceMode.race.test.ts verdes)
- Geral final: `pnpm --filter @jarvis/desktop vitest run --no-coverage` exits 0 (full suite verde)
- Geral final: `grep -r "it.todo" apps/desktop/src/main/__tests__/voiceMode*.test.ts apps/desktop/src/main/__tests__/voiceMode/*.test.ts | wc -l` retorna 0 (zero todos pendentes)
</verification>

<success_criteria>
- D-04 plano B implementado: setMode em catch re-instancia strategy antiga; fallback null state se recovery falha
- Tipo `VoiceMode | null` propagado: voiceMode/index.ts (currentMode + getMode) + tray.ts (handles null gracefully via fallback existente)
- main/index.ts wira createPttOnlyFactory em AMBOS os branches do ternário
- voiceMode.test.ts: 18 testes Phase 39 + ~5 novos D-04 = 22+ verdes (WR-01 ajustados para refletir recovery)
- voiceMode.race.test.ts: 4 describes (cenário 1, 2, 3, 3b), 12+ testes verdes
- Zero it.todo() restante em qualquer test file da phase
- Full vitest suite verde
- TypeScript sem erros
- SC4 do roadmap (5 trocas rápidas sem hotkey fantasma) coberto por testes automated (cenário 1 + 2)
- VPTT-01/02/03 atendidos main-side end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/43-ptt-only-integration/43-04-SUMMARY.md` documenting:
- D-04 plano B fluxo (diagrama do try/catch/recovery)
- API mudança: getMode() agora retorna `VoiceMode | null` — callers consumindo devem tratar null
- main/index.ts wiring final do PttOnlyStrategy
- Coverage matrix VPTT-01/02/03 → testes (qual test cobre qual requirement)
- Follow-up notes consolidadas (de Plans 02 + 03 + 04):
  - VPTT-03 main-side completo; renderer hook useAlwaysListening fica como quick task / phase futura
  - PTT-only end-to-end válido em UAT real (manual): tray click → wake word silencia → hotkey continua disparando ChatInput.tsx mic flow
  - Estado degradado (currentMode === null) só atinge user em cenário extremo (ambas factories falham); UX é nenhum radio marcado + tooltip "JARVIS — Wake Word" (fallback default).
- Comando de full regression antes de `/gsd-verify-work`
</output>
