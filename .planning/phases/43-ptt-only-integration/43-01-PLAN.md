---
phase: 43
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts
  - apps/desktop/src/main/__tests__/voiceMode.race.test.ts
  - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts
autonomous: true
requirements:
  - VPTT-01
  - VPTT-02
  - VPTT-03
must_haves:
  truths:
    - "Test files pttOnly.test.ts e voiceMode.race.test.ts existem e contêm describe blocks com it.todo() ou stubs vazios para todos os requisitos VPTT-01/VPTT-02/VPTT-03 + D-04 + D-05 (3 cenários)"
    - "Helper strategyFactoryMocks.ts exporta makeMockStrategy() reutilizável compatível com VoiceCaptureStrategy interface (start/stop/dispose/getStatus)"
    - "Helper expõe makeFailingFactory(callsToFail) e makeSequentialFactory([s1, s2]) usados pelos testes de D-04 (cenário 3)"
    - "Comando `pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts src/main/__tests__/voiceMode.race.test.ts --no-coverage` executa sem erros de import"
    - "Open questions A2 (renderer wiring) e A5 (reuso wakeWord:pause-toggle) são respondidas inline no header dos test files via comments JSDoc"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts"
      provides: "Test stubs para PttOnlyStrategy lifecycle (VPTT-01, VPTT-02)"
      contains: "describe('PttOnlyStrategy"
    - path: "apps/desktop/src/main/__tests__/voiceMode.race.test.ts"
      provides: "Test stubs para D-05 (3 cenários) + D-04 plano B"
      contains: "describe('Voice Mode Race Conditions"
    - path: "apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts"
      provides: "Helpers compartilhados — makeMockStrategy, makeFailingFactory, makeSequentialFactory"
      exports: ["makeMockStrategy", "makeFailingFactory", "makeSequentialFactory"]
  key_links:
    - from: "apps/desktop/src/main/__tests__/voiceMode.race.test.ts"
      to: "apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts"
      via: "import { makeMockStrategy, makeFailingFactory } from './helpers/strategyFactoryMocks'"
      pattern: "from ['\"]\\.\\./helpers/strategyFactoryMocks"
    - from: "apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts"
      to: "apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts"
      via: "import { makeMockStrategy } from '../helpers/strategyFactoryMocks'"
      pattern: "from ['\"]\\.\\./\\.\\./helpers/strategyFactoryMocks"
---

<objective>
Wave 0 do Phase 43 — criar a infraestrutura de testes (Nyquist) ANTES da implementação. Garante que cada commit subsequente das Waves 1 e 2 já tem comando `<automated>` que pode rodar verde, falhar com clareza, ou regredir. Também fecha as 4 Open Questions do RESEARCH inline para os executores das próximas waves não precisarem revisitar.

Purpose: Plano 43-01 é o gate da Phase 43 — sem ele, todas as outras tarefas teriam `<automated>MISSING</automated>` e o sampling rate (per-task / per-wave) ficaria sem efeito. Também responde às assumptions A2/A5 (Open Questions 1 e 4 do RESEARCH).
Output: 3 arquivos de teste-stub funcionais (compilam, importam corretamente) + decisões arquiteturais resolvidas inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-ptt-only-integration/43-CONTEXT.md
@.planning/phases/43-ptt-only-integration/43-RESEARCH.md
@.planning/phases/43-ptt-only-integration/43-VALIDATION.md
@apps/desktop/src/main/voiceMode/index.ts
@apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts
@apps/desktop/src/main/__tests__/voiceMode.test.ts
@apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts

<interfaces>
<!-- Contracts the executor will consume directly. No exploration needed. -->

From apps/desktop/src/main/voiceMode/index.ts:
```typescript
export interface VoiceCaptureStrategy {
  start(): Promise<void>;
  stop(): Promise<void>;
  dispose(): Promise<void>;
  getStatus(): 'idle' | 'capturing' | 'processing';
}
```

From apps/desktop/src/shared/ipc-types.ts:
```typescript
export type VoiceMode = 'wake-word' | 'always-listening' | 'ptt-only';
export type PttAction = 'toggle';
export const IPC_CHANNELS = { WAKE_WORD_PAUSE_TOGGLE: 'wakeWord:pause-toggle', /* ... */ } as const;
```

Existing electron-store mock pattern (apps/desktop/src/main/__tests__/voiceMode.test.ts:13-23):
```typescript
vi.mock('electron-store', () => {
  let mockStore: Record<string, unknown> = {};
  return {
    default: class Store {
      get(key: string) { return mockStore[key]; }
      set(key: string, value: unknown) { mockStore[key] = value; }
      static __resetStore() { mockStore = {}; }
      static __getBackingStore() { return mockStore; }
    },
  };
});
```

Existing electron mock pattern (apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts:33-62):
```typescript
const { ipcListeners } = vi.hoisted(() => ({ ipcListeners: [] as Array<{channel: string; handler: Function}> }));
vi.mock('electron', () => ({
  ipcMain: {
    on: vi.fn((channel, handler) => { ipcListeners.push({ channel, handler }); }),
    off: vi.fn((channel, handler) => { /* remove from array */ }),
    once: vi.fn(),
  },
  BrowserWindow: vi.fn(() => ({ isDestroyed: () => false, webContents: { send: vi.fn() } })),
  globalShortcut: { register: vi.fn(() => true), unregister: vi.fn() },
}));
```
</interfaces>

<open_questions_resolution>
<!-- Decisões inline (de RESEARCH.md "## Open Questions") que executores das Waves 1/2 herdam -->

**OQ-1 (renderer wiring para ALWAYS_LISTENING_FORCE_FLUSH):**
- DECISÃO: VPTT-03 main-side é o escopo desta phase. Renderer-side wiring (hook React `useAlwaysListening` consumindo IPC `ALWAYS_LISTENING_FORCE_FLUSH`) é declarado fora de scope e fica documentado no SUMMARY do plan 43-03 como "follow-up note: VPTT-03 verde no main; renderer wiring requer hook bridge separado".
- TESTE escreve assertion sobre o IPC `webContents.send('always-listening:force-flush')` — não sobre comportamento renderer.

**OQ-2 (API exata force-flush em vad-web):**
- FORA DE ESCOPO desta phase. Strategy main-side só comanda via IPC; rendering-side é problema do hook futuro.

**OQ-3 (currentMode === null observável pela tray/orb):**
- DECISÃO: tray.ts já lida graciosamente com null (`option.mode === currentMode` retorna false → nenhum radio marcado). Não adicionar toast `voice-mode:degraded` agora — apenas log de erro no main. Tooltip cai em fallback "JARVIS — Wake Word" por causa do `?? 'Wake Word'` em tray.ts:74.

**OQ-4 (reuso de `wakeWord:pause-toggle` para pausa do wake word em PTT-only):**
- DECISÃO: REUSAR o canal existente. `PttOnlyStrategy.start()` chama `setWakeWordPaused(true)` + `broadcastPauseToggle(true)` (de `ipc/settings.ts`). `PttOnlyStrategy.stop()` chama `setWakeWordPaused(false)` + `broadcastPauseToggle(false)`. Zero IPC novo. NÃO criar canal `ptt-only:active`.
- Justificativa: o renderer hook `useWakeWord.ts:397-403` já consome `onPauseToggle(paused)` e suspende o engine — funciona out-of-the-box para PTT-only.
</open_questions_resolution>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar helper compartilhado strategyFactoryMocks.ts</name>
  <read_first>
    - apps/desktop/src/main/voiceMode/index.ts (ler interface VoiceCaptureStrategy linhas 34-43)
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (ler makeStrategy helper linhas 30-37 — pattern a generalizar)
    - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts (ler vi.hoisted patterns linhas 33-62)
  </read_first>
  <action>
    Criar diretório `apps/desktop/src/main/__tests__/helpers/` e criar o arquivo `strategyFactoryMocks.ts` com EXATAMENTE estas exports:

    ```typescript
    /**
     * strategyFactoryMocks.ts — test doubles compartilhados para VoiceCaptureStrategy
     *
     * Phase 43 Plan 01 (Wave 0): centraliza mocks usados por voiceMode.test.ts,
     * voiceMode.race.test.ts e voiceMode/pttOnly.test.ts. Evita duplicação de
     * boilerplate `vi.fn().mockResolvedValue(undefined)` em cada test file.
     *
     * Pattern source: apps/desktop/src/main/__tests__/voiceMode.test.ts:30-37
     * (helper `makeStrategy` original) — esta versão estende com factories.
     */
    import { vi, type MockedFunction } from 'vitest';
    import type { VoiceCaptureStrategy } from '../../voiceMode/index.js';

    export interface MockStrategy {
      start: MockedFunction<() => Promise<void>>;
      stop: MockedFunction<() => Promise<void>>;
      dispose: MockedFunction<() => Promise<void>>;
      getStatus: MockedFunction<() => 'idle' | 'capturing' | 'processing'>;
    }

    /**
     * makeMockStrategy — cria uma strategy mock que satisfaz VoiceCaptureStrategy.
     *
     * @param status valor inicial retornado por getStatus() (default: 'idle')
     */
    export function makeMockStrategy(
      status: 'idle' | 'capturing' | 'processing' = 'idle',
    ): MockStrategy {
      return {
        start: vi.fn().mockResolvedValue(undefined),
        stop: vi.fn().mockResolvedValue(undefined),
        dispose: vi.fn().mockResolvedValue(undefined),
        getStatus: vi.fn().mockReturnValue(status),
      };
    }

    /**
     * makeFailingFactory — factory que lança Error nas primeiras N chamadas e
     * depois retorna strategies normais (D-04 plano B test cenário 3).
     *
     * @param failuresBeforeSuccess número de chamadas que devem lançar antes da factory passar a retornar mocks
     * @param errorMessage mensagem do Error (default: 'Strategy factory failure')
     */
    export function makeFailingFactory(
      failuresBeforeSuccess: number,
      errorMessage = 'Strategy factory failure',
    ): MockedFunction<() => VoiceCaptureStrategy> {
      let callCount = 0;
      return vi.fn().mockImplementation(() => {
        callCount += 1;
        if (callCount <= failuresBeforeSuccess) {
          throw new Error(errorMessage);
        }
        return makeMockStrategy();
      }) as MockedFunction<() => VoiceCaptureStrategy>;
    }

    /**
     * makeSequentialFactory — factory que retorna strategies em sequência
     * pré-determinada (1ª chamada → strategies[0], 2ª → strategies[1] ...).
     * Útil para testes de recovery (D-04 cenário 3) onde queremos distinguir
     * entre instância "init" e instância "recovered".
     */
    export function makeSequentialFactory(
      strategies: MockStrategy[],
    ): MockedFunction<() => VoiceCaptureStrategy> {
      let callCount = 0;
      return vi.fn().mockImplementation(() => {
        const s = strategies[callCount];
        callCount += 1;
        if (!s) throw new Error(`makeSequentialFactory: no strategy for call ${callCount}`);
        return s;
      }) as MockedFunction<() => VoiceCaptureStrategy>;
    }
    ```

    NÃO acrescentar imports ou helpers fora desta lista — outros plans são quem vão consumir, e exports adicionais são red flag de over-engineering.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm tsc --noEmit -p tsconfig.node.json 2>&1 | grep -E "strategyFactoryMocks|helpers/" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts` existe
    - `grep -c "export function makeMockStrategy" apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts` retorna 1
    - `grep -c "export function makeFailingFactory" apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts` retorna 1
    - `grep -c "export function makeSequentialFactory" apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts` retorna 1
    - `grep -c "import type { VoiceCaptureStrategy }" apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts` retorna 1
    - Compilação TypeScript não emite erros para este arquivo (`pnpm tsc --noEmit` sem erros referenciando `helpers/strategyFactoryMocks.ts`)
  </acceptance_criteria>
  <done>3 helpers exportados, tipados, compiláveis. Sem implementações dummy alémo strategyFactoryMocks.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Criar test stub pttOnly.test.ts (VPTT-01, VPTT-02)</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts (ler estrutura completa do arquivo, sobretudo vi.hoisted/vi.mock electron linhas 33-62 + describe blocks)
    - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts (criado em Task 1)
    - apps/desktop/src/main/voiceMode/strategies/alwaysListening.ts (pattern de Strategy real — espelho da PttOnlyStrategy)
  </read_first>
  <action>
    Criar `apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` com a seguinte estrutura COMPLETA. Usa `it.todo()` para os comportamentos que Plan 02 vai implementar — isto é a anti-shallow guarantee: todo requisito tem placeholder testável.

    ```typescript
    /**
     * pttOnly.test.ts — PttOnlyStrategy main coordinator tests
     *
     * Phase 43 — PTT-only + Integration (VPTT-01, VPTT-02)
     *
     * Wave 0 (Plan 43-01): este arquivo é stub — todos os it() são it.todo() até
     * Plan 43-02 implementar PttOnlyStrategy. Os describe blocks já estão na
     * forma final para evitar revisão de estrutura na Wave 1.
     *
     * OQ-4 RESOLVED (Plan 01): wake word pause é feito via canal existente
     * `wakeWord:pause-toggle` (broadcastPauseToggle de ipc/settings.ts) — NÃO
     * criar canal `ptt-only:active`. Renderer useWakeWord.ts:397 já consome.
     */
    import { describe, it, vi, beforeEach } from 'vitest';

    // Mock electron — capturamos webContents.send + ipcMain methods
    interface IpcListenerEntry { channel: string; handler: (...args: unknown[]) => unknown; }
    const { ipcListeners, sendCalls } = vi.hoisted(() => ({
      ipcListeners: [] as IpcListenerEntry[],
      sendCalls: [] as Array<{ channel: string; args: unknown[] }>,
    }));

    vi.mock('electron', () => ({
      ipcMain: {
        on: vi.fn((channel: string, handler: (...args: unknown[]) => unknown) => {
          ipcListeners.push({ channel, handler });
        }),
        off: vi.fn((channel: string, handler: (...args: unknown[]) => unknown) => {
          const idx = ipcListeners.findIndex(
            (l) => l.channel === channel && l.handler === handler,
          );
          if (idx >= 0) ipcListeners.splice(idx, 1);
        }),
      },
      BrowserWindow: vi.fn(() => ({
        isDestroyed: () => false,
        webContents: { send: vi.fn((channel: string, ...args: unknown[]) => {
          sendCalls.push({ channel, args });
        }) },
      })),
      globalShortcut: { register: vi.fn(() => true), unregister: vi.fn() },
    }));

    // Mock electron-store
    vi.mock('electron-store', () => {
      let mockStore: Record<string, unknown> = {};
      return {
        default: class Store {
          get(key: string) { return mockStore[key]; }
          set(key: string, value: unknown) { mockStore[key] = value; }
          static __resetStore() { mockStore = {}; }
        },
      };
    });

    describe('PttOnlyStrategy (Phase 43, VPTT-01)', () => {
      beforeEach(() => {
        ipcListeners.length = 0;
        sendCalls.length = 0;
      });

      describe('VPTT-01 — lifecycle', () => {
        it.todo('start() pausa wake word via broadcastPauseToggle(true)');
        it.todo('start() subscribe pttHotkeyEmitter "toggle" listener');
        it.todo('start() é idempotente — chamada dupla não duplica listener');
        it.todo('start() seta status para "capturing"');
        it.todo('stop() remove pttHotkeyEmitter "toggle" listener (mesma referência)');
        it.todo('stop() chama broadcastPauseToggle(false) para reativar wake word');
        it.todo('stop() seta status para "idle"');
        it.todo('dispose() é idempotente — chamada dupla não vaza');
        it.todo('dispose() chama stop() internamente (cleanup canônico)');
      });

      describe('VPTT-01 — wake word silenciado durante PTT-only', () => {
        it.todo('webContents.send é invocado com "wakeWord:pause-toggle" e payload true em start()');
        it.todo('webContents.send é invocado com "wakeWord:pause-toggle" e payload false em stop()');
      });

      describe('VPTT-02 — reuso da hotkey v1.7', () => {
        it.todo('PttOnlyStrategy NÃO chama globalShortcut.register (D-03)');
        it.todo('PttOnlyStrategy NÃO chama setPttHotkey/getPttHotkey diretamente');
      });

      describe('Listener leak guard (T-43-LEAK)', () => {
        it.todo('5 cycles de start/stop deixam pttHotkeyEmitter.listenerCount("toggle") === 0');
      });
    });
    ```

    NÃO escrever assertions concretas dos it.todo() — Plan 02 fará isso ao implementar PttOnlyStrategy. O contrato deste plano é APENAS a estrutura.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage 2>&1 | grep -E "todo|test files"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` existe
    - `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 13 (mínimo dos comportamentos listados)
    - `grep -c "describe" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna >= 4 (4 grupos: lifecycle, pause-toggle, VPTT-02, leak guard)
    - `grep -c "vi.mock('electron'" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna 1
    - `grep -c "OQ-4 RESOLVED" apps/desktop/src/main/__tests__/voiceMode/pttOnly.test.ts` retorna 1 (decisão arquitetural inline)
    - `pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage` exits 0 com saída marcando os it.todo() como skipped/pending (vitest considera todo como passed)
  </acceptance_criteria>
  <done>Test file roda verde por causa dos it.todo(), describe blocks cobrem 100% de VPTT-01 + VPTT-02 + T-43-LEAK.</done>
</task>

<task type="auto">
  <name>Task 3: Criar test stub voiceMode.race.test.ts (D-05 + D-04 plano B)</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (ler todo o arquivo — pattern de teste do VoiceModeManager + race condition guard linhas 272-305)
    - apps/desktop/src/main/__tests__/helpers/strategyFactoryMocks.ts (criado em Task 1)
    - apps/desktop/src/main/voiceMode/index.ts (ler VoiceModeManager.setMode signature)
  </read_first>
  <action>
    Criar `apps/desktop/src/main/__tests__/voiceMode.race.test.ts` com a estrutura COMPLETA abaixo. Esses testes são integration-level — Plan 04 vai preencher as assertions reais ao implementar D-04 plano B. Por enquanto, todos os testes são it.todo() para garantir que o describe layout está completo.

    ```typescript
    /**
     * voiceMode.race.test.ts — Race condition + D-04 plan B integration tests
     *
     * Phase 43 — D-05 race tests + D-04 plan B (recovery em catch)
     *
     * Wave 0 (Plan 43-01): este arquivo é stub — todos os it() são it.todo() até
     * Plan 43-04 implementar D-04 plano B + assertions reais. Mocks de electron
     * + electron-store + estrutura de 3 cenários já consolidados aqui.
     *
     * Cenários (D-05):
     *  1. Sequencial rápido: 5 trocas Wake→AL→PTT→Wake→AL com await entre cada
     *  2. Concorrente: Promise.all([5 setMode]) — guard rejeita extras
     *  3. Plano B em catch: nova factory falha → strategy antiga é re-instanciada
     *
     * Uses:
     *  - helpers/strategyFactoryMocks.ts (Plan 01)
     *  - __resetPttHotkeyEmitterForTests() (Plan 02 — exported from ptt-hotkey.ts)
     */
    import { describe, it, vi, beforeEach } from 'vitest';

    // Mock electron-store — pattern from voiceMode.test.ts:13-23
    vi.mock('electron-store', () => {
      let mockStore: Record<string, unknown> = {};
      return {
        default: class Store {
          get(key: string) { return mockStore[key]; }
          set(key: string, value: unknown) { mockStore[key] = value; }
          static __resetStore() { mockStore = {}; }
          static __getBackingStore() { return mockStore; }
        },
      };
    });

    // Mock electron — minimal: ipcMain + globalShortcut + BrowserWindow stub
    vi.mock('electron', () => ({
      ipcMain: { on: vi.fn(), off: vi.fn(), once: vi.fn() },
      BrowserWindow: vi.fn(() => ({
        isDestroyed: () => false,
        webContents: { send: vi.fn() },
      })),
      globalShortcut: { register: vi.fn(() => true), unregister: vi.fn() },
    }));

    describe('Voice Mode Race Conditions (D-05)', () => {
      beforeEach(() => {
        // Plan 02 vai prover __resetPttHotkeyEmitterForTests; ainda não disponível.
        // Após Plan 02 lança o helper, descomentar:
        // const { __resetPttHotkeyEmitterForTests } = await import('../ptt-hotkey');
        // __resetPttHotkeyEmitterForTests();
      });

      describe('Cenário 1: 5 trocas sequenciais (sem listener leak)', () => {
        it.todo('Wake→AL→PTT→Wake→AL final state é "always-listening"');
        it.todo('pttHotkeyEmitter.listenerCount("toggle") <= 1 ao final das 5 trocas');
        it.todo('cada strategy disposed é chamada exatamente 1 vez (sem leak)');
        it.todo('nenhum throw/unhandled rejection durante a sequência');
      });

      describe('Cenário 2: Promise.all de 5 setMode concorrentes', () => {
        it.todo('apenas 1 setMode retorna true (transitioning guard rejeita os outros 4)');
        it.todo('estado final == primeiro setMode que entrou no try block');
        it.todo('pttHotkeyEmitter.listenerCount("toggle") <= 1 após settle');
        it.todo('nenhum currentMode === null não-intencional');
      });

      describe('Cenário 3: D-04 plano B — factory nova falha, antiga é re-instanciada', () => {
        it.todo('PTT factory falha 1x → WakeWord factory é chamada 2x (init + recovery)');
        it.todo('manager.getMode() === "wake-word" após factory PTT falhar');
        it.todo('strategy antiga (wwInitial) tem dispose() chamado uma vez');
        it.todo('strategy recuperada (wwRecovered) tem start() chamado uma vez');
        it.todo('setMode retorna false (mode change failed)');
        it.todo('NÃO emite voiceMode:change (recovery é silencioso)');

        it.todo('FALLBACK: se recovery TAMBÉM falhar, currentMode = null + activeStrategy = null');
        it.todo('FALLBACK: log de erro emitido com mensagem mencionando "recovery for"');
      });

      describe('Cenário 3b: transitioning flag durante recovery (D-04 atomicidade)', () => {
        it.todo('transitioning permanece true durante recovery — setMode concorrente é rejeitado');
        it.todo('transitioning volta a false após recovery (tanto sucesso quanto fallback)');
      });
    });
    ```

    NÃO importar VoiceModeManager nem helpers neste plan ainda — Plan 04 fará isso. Estrutura é o contrato.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm vitest run src/main/__tests__/voiceMode.race.test.ts --no-coverage 2>&1 | grep -E "todo|test files"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop/src/main/__tests__/voiceMode.race.test.ts` existe
    - `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode.race.test.ts` retorna >= 16
    - `grep -c "describe" apps/desktop/src/main/__tests__/voiceMode.race.test.ts` retorna >= 5 (top + 4 sub-describes: cenário 1, 2, 3, 3b)
    - `grep "Cenário 1\|Cenário 2\|Cenário 3" apps/desktop/src/main/__tests__/voiceMode.race.test.ts | wc -l` retorna >= 3
    - `grep "FALLBACK" apps/desktop/src/main/__tests__/voiceMode.race.test.ts | wc -l` retorna >= 2 (cobrindo D-04 fallback)
    - `pnpm vitest run src/main/__tests__/voiceMode.race.test.ts --no-coverage` exits 0
  </acceptance_criteria>
  <done>Test file estrutura cobre 3 cenários de D-05 + cenário 3 de D-04 plano B + fallback de currentMode=null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner ↔ main process modules | Vitest carrega módulos do main process (`voiceMode/index.ts`, `ptt-hotkey.ts`) com mocks de `electron` e `electron-store`. Helpers ficam isolados em `__tests__/helpers/` — não exportados para produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01-LEAK | DoS | helpers/strategyFactoryMocks.ts | accept | Mocks são confined a tests; vitest reseta entre arquivos. Sem persistent state. |
| T-43-01-INFO | Information Disclosure | test files | accept | Stubs `it.todo()` não expõem dados sensíveis — apenas estrutura de testes. |
| T-43-01-TAMPER | Tampering | helpers/strategyFactoryMocks.ts | mitigate | Helper exportado apenas com 3 funções específicas (makeMockStrategy / makeFailingFactory / makeSequentialFactory). Não expõe state mutável global. |
</threat_model>

<verification>
- Após Task 1: `find apps/desktop/src/main/__tests__/helpers -name "strategyFactoryMocks.ts"` retorna 1 path
- Após Task 2: `pnpm vitest run src/main/__tests__/voiceMode/pttOnly.test.ts --no-coverage` exits 0
- Após Task 3: `pnpm vitest run src/main/__tests__/voiceMode.race.test.ts --no-coverage` exits 0
- Geral: `pnpm tsc --noEmit -p tsconfig.node.json` sem erros referenciando os 3 novos arquivos
- Geral (regression): `pnpm vitest run src/main/__tests__/voiceMode.test.ts --no-coverage` continua 18/18 verde (estrutura existente preservada)
</verification>

<success_criteria>
- 3 arquivos novos criados: helpers/strategyFactoryMocks.ts + voiceMode/pttOnly.test.ts + voiceMode.race.test.ts
- Todos os 3 compilam (TypeScript sem erros)
- Todos os 3 rodam com vitest sem failure (it.todo() conta como passed)
- Open Questions OQ-3 (currentMode null) e OQ-4 (reuso wakeWord:pause-toggle) documentadas inline nos comments
- Pattern OQ-1/OQ-2 marcado como out-of-scope no header de pttOnly.test.ts
- Suite de testes existente (`voiceMode.test.ts`) continua verde — sem regressão
</success_criteria>

<output>
After completion, create `.planning/phases/43-ptt-only-integration/43-01-SUMMARY.md` documenting:
- Helpers exportados e suas signatures
- Estrutura describe dos 2 test stubs (count de it.todo por requirement)
- Decisões arquiteturais resolvidas (OQ-1 a OQ-4)
- Comando de verificação rápida pra Wave 1 ("rode o quick suite e veja que continua verde antes de implementar")
</output>
