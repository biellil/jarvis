---
phase: 19-voice-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/voice/tts/provider.ts
  - apps/backend-ts/src/voice/tts/elevenlabs.ts
  - apps/backend-ts/src/voice/tts/elevenlabs.test.ts
autonomous: true
requirements: [VOICE-TS-02]
must_haves:
  truths:
    - "TTSProvider interface define synthesize(text) → {audio, format} + readonly name"
    - "ElevenLabsTTSProvider chama api.elevenlabs.io/v1/text-to-speech/{voice_id} via fetch"
    - "Header xi-api-key + body {text, model_id, voice_settings} conforme spec ElevenLabs"
    - "Retorna Buffer + format 'mp3' (default response da ElevenLabs)"
    - "Throw com mensagem clara se ELEVENLABS_API_KEY faltar ou fetch retornar non-2xx"
  artifacts:
    - path: "apps/backend-ts/src/voice/tts/provider.ts"
      provides: "interface TTSProvider + type TTSResult"
    - path: "apps/backend-ts/src/voice/tts/elevenlabs.ts"
      provides: "class ElevenLabsTTSProvider"
  key_links:
    - from: "apps/backend-ts/src/voice/tts/elevenlabs.ts"
      to: "https://api.elevenlabs.io/v1/text-to-speech"
      via: "fetch POST com xi-api-key"
      pattern: "api\\.elevenlabs\\.io"
---

<objective>
Definir interface TTSProvider + implementar ElevenLabsTTSProvider consumindo a API REST da ElevenLabs via fetch nativo. Este plano roda em paralelo com 19-01/19-03; o fallback wrapper vem no plano 19-04.

Purpose: Entregar TTS cloud de alta qualidade como default do backend.
Output: Módulo tts/provider.ts + tts/elevenlabs.ts prontos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@src/jarvis/core/tts.py

<interfaces>
```typescript
export interface TTSResult {
  audio: Buffer;
  format: 'mp3' | 'wav' | 'opus';
}

export interface TTSProvider {
  readonly name: string;
  synthesize(text: string): Promise<TTSResult>;
}
```

ElevenLabs API (from CONTEXT.md canonical_refs):
- POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}
- Headers: `xi-api-key: {API_KEY}`, `Content-Type: application/json`, `Accept: audio/mpeg`
- Body: `{"text": "...", "model_id": "eleven_multilingual_v2", "voice_settings": {"stability": 0.5, "similarity_boost": 0.75}}`
- Response: binary audio bytes (mp3 default)
- Errors: 401 (key inválida), 422 (texto vazio/inválido), 429 (rate limit)

Env vars:
- ELEVENLABS_API_KEY (obrigatório)
- ELEVENLABS_VOICE_ID (default: EXAVITQu4vr4xnSDxMaL — Sarah)
- ELEVENLABS_MODEL_ID (default: eleven_multilingual_v2)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TTSProvider interface + ElevenLabsTTSProvider + testes mockados</name>
  <files>apps/backend-ts/src/voice/tts/provider.ts, apps/backend-ts/src/voice/tts/elevenlabs.ts, apps/backend-ts/src/voice/tts/elevenlabs.test.ts</files>
  <behavior>
    - interface TTSProvider + TTSResult exportadas de provider.ts
    - ElevenLabsTTSProvider.name === 'elevenlabs'
    - Constructor lê env: ELEVENLABS_API_KEY (throw no synthesize se ausente), ELEVENLABS_VOICE_ID (default EXAVITQu4vr4xnSDxMaL), ELEVENLABS_MODEL_ID (default eleven_multilingual_v2)
    - synthesize(text): valida text não-vazio → throw; monta URL /v1/text-to-speech/{voiceId}; fetch POST com headers + JSON body; checa response.ok → throw com `ElevenLabs error ${status}: ${body}`; lê arrayBuffer → Buffer; retorna {audio: Buffer, format: 'mp3'}
    - Usa `globalThis.fetch` (Node 22+) — sem importar undici
    - Teste mocka `global.fetch` via vi.fn() retornando Response mock com arrayBuffer() → Uint8Array
    - Casos de teste: success; missing API key throws; empty text throws; 401 throws com mensagem clara; 500 throws; fetch network error throws
  </behavior>
  <action>
    1. Usar Context7 (`mcp__claude_ai_Context7__*`) pra consultar docs da ElevenLabs API text-to-speech v1 se precisar confirmar shape exato de request/response — caso contrário, o shape documentado na CONTEXT.md é suficiente.
    2. Criar `apps/backend-ts/src/voice/tts/provider.ts` exportando interface TTSProvider e type TTSResult.
    3. Criar `apps/backend-ts/src/voice/tts/elevenlabs.ts`:
       ```typescript
       export class ElevenLabsTTSProvider implements TTSProvider {
         readonly name = 'elevenlabs';
         private readonly voiceId: string;
         private readonly modelId: string;
         constructor() {
           this.voiceId = process.env.ELEVENLABS_VOICE_ID ?? 'EXAVITQu4vr4xnSDxMaL';
           this.modelId = process.env.ELEVENLABS_MODEL_ID ?? 'eleven_multilingual_v2';
         }
         async synthesize(text: string): Promise<TTSResult> {
           if (!text || text.trim().length === 0) throw new Error('ElevenLabsTTSProvider: empty text');
           const apiKey = process.env.ELEVENLABS_API_KEY;
           if (!apiKey) throw new Error('ElevenLabsTTSProvider: ELEVENLABS_API_KEY not set');
           const url = `https://api.elevenlabs.io/v1/text-to-speech/${this.voiceId}`;
           const res = await fetch(url, {
             method: 'POST',
             headers: {
               'xi-api-key': apiKey,
               'Content-Type': 'application/json',
               'Accept': 'audio/mpeg',
             },
             body: JSON.stringify({
               text,
               model_id: this.modelId,
               voice_settings: { stability: 0.5, similarity_boost: 0.75 },
             }),
           });
           if (!res.ok) {
             const body = await res.text().catch(() => '');
             throw new Error(`ElevenLabs error ${res.status}: ${body.slice(0, 200)}`);
           }
           const ab = await res.arrayBuffer();
           return { audio: Buffer.from(ab), format: 'mp3' };
         }
       }
       ```
    4. Testes `elevenlabs.test.ts` usando `vi.stubGlobal('fetch', vi.fn())`:
       - success: mock retorna `new Response(new Uint8Array([1,2,3]), {status: 200})` → synthesize retorna Buffer [1,2,3] format mp3
       - empty text → throw
       - missing API key (delete env) → throw
       - 401 → throw com 'ElevenLabs error 401'
       - fetch throws → propaga
       - Verificar que fetch foi chamado com URL correto, xi-api-key header, body JSON com model_id e voice_settings
       - beforeEach/afterEach resetam ENV e unstub
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/tts/elevenlabs.test.ts</automated>
  </verify>
  <done>ElevenLabsTTSProvider testado com fetch mockado; nenhum request real.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/voice/tts/elevenlabs.test.ts` passa
- `pnpm --filter backend-ts build` limpo
</verification>

<success_criteria>
1. TTSProvider interface existe e é consumível pelos outros providers
2. ElevenLabsTTSProvider chama a API correta com shape correto
3. Erros (missing key, HTTP errors, empty text) são throws estruturados
4. Testes 100% verdes sem tocar rede
</success_criteria>

<output>
Criar `.planning/phases/19-voice-backend/19-02-SUMMARY.md`.
</output>
