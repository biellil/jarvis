---
phase: 19-voice-backend
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/memory/schema.ts
  - apps/backend-ts/src/memory/migrations/
  - apps/backend-ts/src/memory/store.ts
  - apps/backend-ts/src/memory/voice-log.test.ts
autonomous: true
requirements: [VOICE-TS-05]
must_haves:
  truths:
    - "Tabela voice_calls existe com schema idêntico ao definido na CONTEXT.md"
    - "MemoryStore.logVoiceCall(row) insere uma linha e retorna o id"
    - "MemoryStore.updateVoiceCall(id, patch) atualiza campos TTS após a síntese"
    - "Migration Drizzle aplicada automaticamente no startup via runMigrations()"
  artifacts:
    - path: "apps/backend-ts/src/memory/schema.ts"
      provides: "export const voiceCalls (Drizzle table)"
    - path: "apps/backend-ts/src/memory/store.ts"
      provides: "MemoryStore.logVoiceCall + updateVoiceCall methods"
  key_links:
    - from: "MemoryStore.logVoiceCall"
      to: "sqlite voice_calls table"
      via: "drizzle insert returning id"
      pattern: "voiceCalls"
---

<objective>
Adicionar tabela `voice_calls` ao schema Drizzle + gerar migration + estender MemoryStore com logVoiceCall/updateVoiceCall. Plano roda em paralelo com 19-01/19-02/19-03 (Wave 1) pois toca só em memory/.

Output: Schema atualizado + migration nova + métodos MemoryStore testados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@apps/backend-ts/src/memory/schema.ts
@apps/backend-ts/src/memory/store.ts

<interfaces>
CONTEXT.md define o schema:
```sql
CREATE TABLE voice_calls (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  conversation_id INTEGER REFERENCES conversations(id),
  timestamp TEXT NOT NULL,
  audio_bytes INTEGER NOT NULL,
  transcription TEXT,
  stt_provider TEXT NOT NULL,
  stt_latency_ms INTEGER,
  tts_provider TEXT,
  tts_latency_ms INTEGER,
  tts_bytes INTEGER,
  success INTEGER NOT NULL,
  error TEXT
);
```

Métodos novos no MemoryStore:
```typescript
logVoiceCall(row: {
  conversationId: number | null;
  audioBytes: number;
  transcription: string | null;
  sttProvider: string;
  sttLatencyMs: number | null;
  success: boolean;
  error?: string | null;
}): number | null;

updateVoiceCall(id: number, patch: {
  transcription?: string | null;
  ttsProvider?: string | null;
  ttsLatencyMs?: number | null;
  ttsBytes?: number | null;
  success?: boolean;
  error?: string | null;
}): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema voiceCalls + migration Drizzle</name>
  <files>apps/backend-ts/src/memory/schema.ts, apps/backend-ts/src/memory/migrations/</files>
  <behavior>
    - voiceCalls sqliteTable exportado de schema.ts com todas as colunas listadas
    - `drizzle-kit generate` produz uma nova migration em src/memory/migrations/ (NNNN_*.sql) contendo CREATE TABLE voice_calls
    - runMigrations() aplica sem erro em um DB em branco
  </behavior>
  <action>
    1. Editar `schema.ts` adicionando:
       ```typescript
       export const voiceCalls = sqliteTable('voice_calls', {
         id: integer('id').primaryKey({ autoIncrement: true }),
         conversationId: integer('conversation_id').references(() => conversations.id),
         timestamp: text('timestamp').notNull(),
         audioBytes: integer('audio_bytes').notNull(),
         transcription: text('transcription'),
         sttProvider: text('stt_provider').notNull(),
         sttLatencyMs: integer('stt_latency_ms'),
         ttsProvider: text('tts_provider'),
         ttsLatencyMs: integer('tts_latency_ms'),
         ttsBytes: integer('tts_bytes'),
         success: integer('success').notNull(), // 0/1
         error: text('error'),
       });
       ```
    2. Rodar `pnpm --filter backend-ts exec drizzle-kit generate` (confirmar script no package.json; se não existir, usar `pnpm exec drizzle-kit generate` no diretório).
    3. Confirmar nova migration `.sql` criada em `src/memory/migrations/`.
    4. Commit migration file.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm exec tsc --noEmit && ls src/memory/migrations/*.sql | wc -l</automated>
  </verify>
  <done>Schema + migration geradas e compiladas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemoryStore.logVoiceCall + updateVoiceCall + testes</name>
  <files>apps/backend-ts/src/memory/store.ts, apps/backend-ts/src/memory/voice-log.test.ts</files>
  <behavior>
    - logVoiceCall insere com timestamp=nowIso(), success convertido para 0/1, retorna id ou null em erro
    - updateVoiceCall faz UPDATE por id; ignora undefined (só atualiza campos presentes no patch); converte success boolean→integer
    - Ambos seguem padrão MEM-05 (warn em catch, nunca throw)
  </behavior>
  <action>
    1. Importar `voiceCalls` em store.ts.
    2. Adicionar métodos à classe `MemoryStore`:
       ```typescript
       logVoiceCall(row: {...}): number | null {
         try {
           const result = this.db.insert(voiceCalls).values({
             conversationId: row.conversationId,
             timestamp: nowIso(),
             audioBytes: row.audioBytes,
             transcription: row.transcription,
             sttProvider: row.sttProvider,
             sttLatencyMs: row.sttLatencyMs,
             success: row.success ? 1 : 0,
             error: row.error ?? null,
           }).returning({ id: voiceCalls.id }).all();
           return result[0]?.id ?? null;
         } catch (exc) {
           console.warn(`MemoryStore.logVoiceCall failed: ${(exc as Error).message}`);
           return null;
         }
       }
       
       updateVoiceCall(id: number, patch: {...}): void {
         try {
           const set: Record<string, unknown> = {};
           if (patch.transcription !== undefined) set.transcription = patch.transcription;
           if (patch.ttsProvider !== undefined) set.ttsProvider = patch.ttsProvider;
           if (patch.ttsLatencyMs !== undefined) set.ttsLatencyMs = patch.ttsLatencyMs;
           if (patch.ttsBytes !== undefined) set.ttsBytes = patch.ttsBytes;
           if (patch.success !== undefined) set.success = patch.success ? 1 : 0;
           if (patch.error !== undefined) set.error = patch.error;
           if (Object.keys(set).length === 0) return;
           this.db.update(voiceCalls).set(set).where(eq(voiceCalls.id, id)).run();
         } catch (exc) {
           console.warn(`MemoryStore.updateVoiceCall failed (id=${id}): ${(exc as Error).message}`);
         }
       }
       ```
    3. Teste `voice-log.test.ts` usando `createStoreForTests(':memory:')`:
       - Inserir linha com logVoiceCall → id não-null; SELECT raw confirma campos
       - updateVoiceCall patch parcial → apenas campos presentes atualizados
       - DB fechado → logVoiceCall retorna null sem throw
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/memory/voice-log.test.ts</automated>
  </verify>
  <done>Métodos implementados, testes passando com DB in-memory.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/memory/voice-log.test.ts` passa
- `pnpm --filter backend-ts build` limpo
- Suite completa de memory continua verde: `pnpm --filter backend-ts vitest run src/memory`
</verification>

<success_criteria>
1. Tabela voice_calls existe e migration aplica limpa
2. logVoiceCall/updateVoiceCall implementados seguindo padrão existente
3. Testes cobrindo inserção, update parcial, e failure graceful
</success_criteria>

<output>
`.planning/phases/19-voice-backend/19-05-SUMMARY.md`
</output>
