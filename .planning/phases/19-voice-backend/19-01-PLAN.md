---
phase: 19-voice-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/package.json
  - apps/backend-ts/src/voice/stt/provider.ts
  - apps/backend-ts/src/voice/stt/local.ts
  - apps/backend-ts/src/voice/stt/index.ts
  - apps/backend-ts/src/voice/stt/local.test.ts
  - apps/backend-ts/src/voice/stt/index.test.ts
  - apps/backend-ts/src/voice/ffmpeg-check.ts
  - apps/backend-ts/src/voice/ffmpeg-check.test.ts
  - apps/backend-ts/test/fixtures/audio/small.wav
autonomous: true
requirements: [VOICE-TS-01]
must_haves:
  truths:
    - "STTProvider interface está definida em TypeScript com método transcribe(buffer, opts?) e readonly name"
    - "LocalSTTProvider transcreve um buffer WebM/WAV para texto via nodejs-whisper"
    - "Factory createSTTProvider() lê STT_PROVIDER env var (default 'local') e WHISPER_MODEL (default 'base')"
    - "Factory faz fallback para local com warning se STT_PROVIDER=cloud for pedido (não implementado)"
    - "Validação de ffmpeg no PATH loga warning no startup sem ser fatal"
  artifacts:
    - path: "apps/backend-ts/src/voice/stt/provider.ts"
      provides: "interface STTProvider + type STTTranscribeOptions"
    - path: "apps/backend-ts/src/voice/stt/local.ts"
      provides: "class LocalSTTProvider implements STTProvider usando nodejs-whisper"
    - path: "apps/backend-ts/src/voice/stt/index.ts"
      provides: "createSTTProvider() factory"
    - path: "apps/backend-ts/src/voice/ffmpeg-check.ts"
      provides: "assertFfmpegAvailable() retorna boolean + loga warning"
  key_links:
    - from: "apps/backend-ts/src/voice/stt/index.ts"
      to: "LocalSTTProvider"
      via: "createSTTProvider() reading STT_PROVIDER env"
      pattern: "process\\.env\\.STT_PROVIDER"
---

<objective>
Implementar STTProvider interface + LocalSTTProvider usando nodejs-whisper como motor default de STT do backend TypeScript. Factory lê env vars, fallback consciente pra local quando CloudSTTProvider ainda não existe. Valida ffmpeg no PATH.

Purpose: Centralizar STT no backend (princípio arquitetural) com abstração pronta pra cloud STT futuro sem refator.
Output: Módulo apps/backend-ts/src/voice/stt/ pronto pra ser usado pelo voice-handler (plano 19-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-voice-backend/19-CONTEXT.md
@src/jarvis/core/voice.py
@apps/backend-ts/package.json

<interfaces>
<!-- Python reference: WhisperTranscriber em src/jarvis/core/voice.py expõe async transcribe(path) usando faster-whisper modelo 'base'. -->
<!-- Paridade nesta fase: aceita Buffer direto (sem tempfile obrigatório — nodejs-whisper requer path, então internamente escrevemos tempfile). -->

Contract esperado:
```typescript
export interface STTTranscribeOptions {
  language?: string; // default 'pt'
}

export interface STTProvider {
  readonly name: string;
  transcribe(audio: Buffer, opts?: STTTranscribeOptions): Promise<string>;
}

export function createSTTProvider(): STTProvider;
export function assertFfmpegAvailable(): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install nodejs-whisper + criar fixture de áudio</name>
  <files>apps/backend-ts/package.json, apps/backend-ts/test/fixtures/audio/small.wav</files>
  <behavior>
    - nodejs-whisper aparece em dependencies do package.json
    - pnpm install roda sem erro
    - test/fixtures/audio/small.wav existe (WAV 16kHz mono ~1s, silêncio ou senóide)
  </behavior>
  <action>
    1. No diretório apps/backend-ts/, adicionar `nodejs-whisper` como dependency (versão mais recente estável). Usar Context7 (`mcp__claude_ai_Context7__resolve-library-id` + `query-docs`) pra confirmar API atual da lib — em particular como invocar `nodewhisper(audioPath, {modelName, autoDownloadModelName})`.
    2. Rodar `pnpm install` no workspace root.
    3. Criar diretório apps/backend-ts/test/fixtures/audio/.
    4. Gerar small.wav de 1 segundo de silêncio via sox ou ffmpeg: `ffmpeg -f lavfi -i anullsrc=r=16000:cl=mono -t 1 -acodec pcm_s16le apps/backend-ts/test/fixtures/audio/small.wav`. Se ffmpeg não disponível, gerar via script Node usando Buffer manual (WAV header + zeros). Commit o .wav (binário pequeno, ~32KB).
    5. Adicionar `test/fixtures/audio/*.wav` NÃO ao .gitignore — queremos versionado.
  </action>
  <verify>
    <automated>cd apps/backend-ts && node -e "require('nodejs-whisper')" && test -f test/fixtures/audio/small.wav && echo OK</automated>
  </verify>
  <done>nodejs-whisper instalado, fixture small.wav criado e versionado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: STTProvider interface + LocalSTTProvider + ffmpeg check</name>
  <files>apps/backend-ts/src/voice/stt/provider.ts, apps/backend-ts/src/voice/stt/local.ts, apps/backend-ts/src/voice/ffmpeg-check.ts, apps/backend-ts/src/voice/stt/local.test.ts, apps/backend-ts/src/voice/ffmpeg-check.test.ts</files>
  <behavior>
    - provider.ts exporta interface STTProvider e type STTTranscribeOptions
    - LocalSTTProvider.name === 'local'
    - LocalSTTProvider.transcribe(buffer) escreve tempfile, chama nodewhisper, retorna string limpa (trim)
    - LocalSTTProvider rejeita buffer vazio com Error('audio buffer is empty')
    - Modelo default é 'base', overridable via construtor opts ou WHISPER_MODEL
    - Limpa tempfile em finally mesmo se transcribe falhar
    - assertFfmpegAvailable() retorna true se `ffmpeg -version` exit 0, false caso contrário (loga warning)
    - Teste unitário mocka nodejs-whisper com vi.mock — NÃO chama real
  </behavior>
  <action>
    1. Criar `apps/backend-ts/src/voice/stt/provider.ts` com interface STTProvider conforme <interfaces> acima.
    2. Criar `apps/backend-ts/src/voice/stt/local.ts` implementando LocalSTTProvider:
       - Constructor aceita `{modelName?: string}` default `process.env.WHISPER_MODEL ?? 'base'`
       - `transcribe(audio, opts)` valida `audio.length > 0` → throw Error
       - Cria tempfile via `fs.mkdtemp(path.join(os.tmpdir(), 'jarvis-stt-'))` + `fs.writeFile(tmpDir/input.webm, audio)`
       - Chama `nodewhisper(tmpPath, { modelName: this.modelName, autoDownloadModelName: this.modelName, removeWavFileAfterExecution: true, whisperOptions: { language: opts?.language ?? 'pt', outputInText: true } })` — confirmar shape exato via Context7
       - Retorna `String(result).trim()`
       - finally: `fs.rm(tmpDir, {recursive: true, force: true})`
    3. Criar `apps/backend-ts/src/voice/ffmpeg-check.ts` com `assertFfmpegAvailable(): boolean` usando `child_process.spawnSync('ffmpeg', ['-version'])` — retorna `status === 0`. Se false, `console.warn('[voice] ffmpeg not found in PATH — nodejs-whisper may fail on non-WAV inputs. Install: apt install ffmpeg / brew install ffmpeg')`.
    4. Testes vitest:
       - `local.test.ts`: usar `vi.mock('nodejs-whisper', () => ({ nodewhisper: vi.fn().mockResolvedValue('texto fake') }))`. Casos: transcribe retorna 'texto fake'; buffer vazio throw; nome é 'local'; tempfile limpo após success e após throw (mock nodewhisper.mockRejectedValueOnce).
       - `ffmpeg-check.test.ts`: usar `vi.mock('node:child_process', () => ({ spawnSync: vi.fn() }))`. Casos: spawnSync retorna status 0 → true; retorna status 1 → false + warn; throw → false + warn.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/stt/local.test.ts src/voice/ffmpeg-check.test.ts</automated>
  </verify>
  <done>Interface + LocalSTTProvider + ffmpeg-check implementados com testes mockados 100% verdes. Sem side effects reais.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: createSTTProvider() factory lendo env</name>
  <files>apps/backend-ts/src/voice/stt/index.ts, apps/backend-ts/src/voice/stt/index.test.ts</files>
  <behavior>
    - createSTTProvider() default retorna instância de LocalSTTProvider
    - STT_PROVIDER=local → LocalSTTProvider
    - STT_PROVIDER=cloud → loga warning "cloud STT not implemented yet, falling back to local" e retorna LocalSTTProvider
    - STT_PROVIDER=garbage → loga warning + retorna LocalSTTProvider
    - Exporta também {STTProvider, LocalSTTProvider} como re-exports
  </behavior>
  <action>
    1. Criar `apps/backend-ts/src/voice/stt/index.ts`:
       - Re-export `STTProvider`, `STTTranscribeOptions` de ./provider.js
       - Re-export `LocalSTTProvider` de ./local.js
       - `export function createSTTProvider(): STTProvider { const p = (process.env.STT_PROVIDER ?? 'local').toLowerCase(); if (p !== 'local') { console.warn(\`[voice] STT provider '${p}' not implemented, falling back to local\`); } return new LocalSTTProvider(); }`
    2. Teste `index.test.ts`: salvar `process.env.STT_PROVIDER` original, stub com diferentes valores, verificar tipo retornado e console.warn (via `vi.spyOn(console, 'warn')`).
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/voice/stt/index.test.ts</automated>
  </verify>
  <done>Factory createSTTProvider() testada para todos os valores de env var.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/voice/stt src/voice/ffmpeg-check.test.ts` → 100% green
- `pnpm --filter backend-ts build` compila sem erros TS
- Nenhum teste chama nodejs-whisper real (mockado)
</verification>

<success_criteria>
1. Módulo apps/backend-ts/src/voice/stt/ existe com interface, LocalSTTProvider e factory
2. ffmpeg-check implementado como warning no startup (não fatal)
3. Testes unitários 100% verdes com mocks
4. LocalSTTProvider pronto pra ser consumido pelo voice-handler (plano 19-06)
</success_criteria>

<output>
Criar `.planning/phases/19-voice-backend/19-01-SUMMARY.md` ao concluir.
</output>
