---
phase: 999.5-langgraph-execucao-silenciosa-sem-confirmacao
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-py/src/jarvis_desktop/config.py
  - apps/desktop-py/src/jarvis_desktop/chat.py
  - apps/desktop-py/tests/test_chat_silent.py
  - apps/desktop-py/tests/test_config.py
autonomous: true
requirements:
  - D-05

must_haves:
  truths:
    - "Eventos task:step:start e task:step:end NÃO são exibidos no terminal quando agentic_step_progress=False (padrão)"
    - "Eventos task:step:start e task:step:end SÃO exibidos quando agentic_step_progress=True"
    - "Eventos task:error, task:cancelled, task:awaiting-failure-decision SEMPRE exibidos independente da flag"
    - "Evento task:auto-approved é silencioso (não exibe nada ao usuário, exceto em modo debug)"
    - "Menu /config tem item 6 para toggle de agentic_step_progress"
    - "agentic_step_progress persiste em ~/.jarvis/config.json após toggle no menu"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/config.py"
      provides: "campo agentic_step_progress com default=False"
      contains: "agentic_step_progress"
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "filtro de eventos step + handler task:auto-approved + menu item 6"
      contains: "agentic_step_progress"
    - path: "apps/desktop-py/tests/test_chat_silent.py"
      provides: "testes PY-01, PY-02, PY-03, EVT-01"
  key_links:
    - from: "apps/desktop-py/src/jarvis_desktop/chat.py _handle_agentic_event"
      to: "config.agentic_step_progress"
      via: "if event_type in ('task:step:start', 'task:step:end') and not config.agentic_step_progress"
      pattern: "agentic_step_progress"
---

<objective>
Implementar o silent mode no cliente Python: novo campo `agentic_step_progress` em `config.py`, filtro de eventos step em `_handle_agentic_event`, handler silencioso para `task:auto-approved`, e item 6 no menu `/config`.

Purpose: Exibir apenas o resultado final de tarefas por padrão — sem poluição de terminal com progresso step-by-step.
Output: `config.py` com novo campo, `chat.py` com filtro + handler + menu item, testes cobrindo D-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-CONTEXT.md
@.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-RESEARCH.md

<interfaces>
<!-- Interfaces existentes para o executor — ler antes de modificar -->

From apps/desktop-py/src/jarvis_desktop/config.py (JarvisConfig — adicionar campo APÓS debug_events):
```python
class JarvisConfig(BaseModel):
    # ... campos existentes ...
    agentic_confirm: bool = Field(default=False, description="Show plan confirmation prompt before executing tasks (Phase 78)")
    debug_events: bool = Field(default=False, description="Show raw agentic SSE events in terminal (Phase 78)")
    # ADICIONAR AQUI (Phase 999.5):
    # agentic_step_progress: bool = Field(default=False, ...)
```

From apps/desktop-py/src/jarvis_desktop/chat.py _handle_agentic_event (linhas 174–279):
```python
def _handle_agentic_event(event_type: str, payload: str, config: JarvisConfig) -> "str | None":
    # ... parse JSON ...
    task_id = data.get("taskId", "")

    if event_type == "task:plan":
        steps = data.get("plan", {}).get("steps", [])
        _render_plan(steps)

    elif event_type == "task:awaiting-confirmation":
        # ... handler existente ...

    elif event_type == "task:step:start":   # <-- filtrar aqui com agentic_step_progress
        step_id = data.get("stepId", "?")
        description = data.get("description", "")
        _console().print(f"  [{step_id}] {description}...", markup=False)

    elif event_type == "task:step:end":     # <-- filtrar aqui com agentic_step_progress
        step_id = data.get("stepId", "?")
        _console().print(f"  [{step_id}] concluído", markup=False)

    elif event_type == "task:done":
        summary = data.get("summary", "").strip()
        if summary:
            return summary

    elif event_type == "task:cancelled":
        _console().print("[Tarefa cancelada]")

    elif event_type == "task:error":        # <-- SEMPRE exibir (D-05)
        error_msg = data.get("message", "erro desconhecido")
        _console().print(f"[Erro: {error_msg}]")

    elif event_type == "task:awaiting-failure-decision":  # <-- SEMPRE exibir (D-05)
        # ... handler existente ...

    else:
        if _debug_mode or config.debug_events:
            _console().print(f"[debug] {event_type}: {payload}")
```

From apps/desktop-py/src/jarvis_desktop/chat.py _show_config_menu (linhas 558–600):
```python
def _show_config_menu(config: JarvisConfig) -> None:
    while True:
        # ...cabeçalho...
        console.print(f"1. Whisper model      [{config.whisper_model}]", markup=False)
        console.print(f"2. TTS provider       [{config.tts_provider}]", markup=False)
        console.print(f"3. Voice mode         [{config.voice_mode}]", markup=False)
        console.print(f"4. Confirmar planos   [{'sim' if config.agentic_confirm else 'nao'}]", markup=False)
        console.print(f"5. Debug eventos      [{'sim' if config.debug_events else 'nao'}]", markup=False)
        # ADICIONAR: item 6 para agentic_step_progress
        console.print("0. Sair")
        choice = ui.get_input("> ").strip()
        if choice == "0": return
        elif choice == "1": _menu_whisper_model(config)
        # ... outros choices ...
        elif choice == "5":
            global _debug_mode
            config.debug_events = not config.debug_events
            _debug_mode = config.debug_events
            status = "sim" if config.debug_events else "nao"
            console.print(f"[Debug eventos: {status}]", highlight=False)
        # ADICIONAR: elif choice == "6": ...
```

From apps/desktop-py/tests/test_config.py (padrão de teste de config):
```python
def test_load_config_returns_defaults(tmp_home):
    from jarvis_desktop.config import load_config
    config = load_config()
    assert config.gateway_url == "http://localhost:3000"
    # padrão: assertar campo com valor padrão esperado
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Campo agentic_step_progress em config.py + testes</name>
  <files>
    apps/desktop-py/src/jarvis_desktop/config.py,
    apps/desktop-py/tests/test_config.py
  </files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/config.py (ler completo — identificar onde inserir campo após debug_events)
    - apps/desktop-py/tests/test_config.py (ver testes existentes de campos bool para seguir padrão)
  </read_first>
  <behavior>
    - Test PY-01a: load_config() retorna config com agentic_step_progress == False por padrão
    - Test PY-01b: config.json com "agentic_step_progress": true é lido corretamente por load_config()
    - Test PY-01c: config.json sem campo agentic_step_progress (config antiga) carrega com default False (forward-compat)
  </behavior>
  <action>
**Adicionar campo em `JarvisConfig` APÓS `debug_events`:**

```python
# Phase 999.5: Silent mode para execução de tarefas
agentic_step_progress: bool = Field(
    default=False,
    description="Show step-by-step progress during task execution; False = silent until final result (Phase 999.5)"
)
```

Posição exata: após a linha `debug_events: bool = Field(...)` e antes de `wake_word_threshold`.

**Adicionar testes em `test_config.py`** — seguir o padrão dos testes existentes de campos bool:
```python
def test_agentic_step_progress_default(tmp_home):
    """agentic_step_progress defaults to False (D-05, Phase 999.5)."""
    from jarvis_desktop.config import load_config
    config = load_config()
    assert config.agentic_step_progress is False

def test_agentic_step_progress_from_config_json(tmp_home, jarvis_config_dir):
    """agentic_step_progress=True is loaded from ~/.jarvis/config.json."""
    import json
    from pathlib import Path
    from jarvis_desktop.config import load_config
    config_file = Path(tmp_home) / ".jarvis" / "config.json"
    config_file.write_text(json.dumps({"agentic_step_progress": True}), encoding="utf-8")
    config = load_config()
    assert config.agentic_step_progress is True

def test_agentic_step_progress_forward_compat(tmp_home, jarvis_config_dir):
    """Old config.json without agentic_step_progress loads with default False."""
    import json
    from pathlib import Path
    from jarvis_desktop.config import load_config
    config_file = Path(tmp_home) / ".jarvis" / "config.json"
    config_file.write_text(json.dumps({"whisper_model": "base"}), encoding="utf-8")
    config = load_config()
    assert config.agentic_step_progress is False
```
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_config.py -q -k "agentic_step_progress"</automated>
  </verify>
  <done>
    - `config.py` contém `agentic_step_progress: bool = Field(default=False, ...)`
    - 3 testes PY-01* passam
    - Suite completa de test_config.py sem regressões: `uv run pytest tests/test_config.py -q`
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filtro de eventos step + handler task:auto-approved + menu item 6</name>
  <files>
    apps/desktop-py/src/jarvis_desktop/chat.py,
    apps/desktop-py/tests/test_chat_silent.py
  </files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/chat.py (ler linhas 174–600 completas — _handle_agentic_event e _show_config_menu)
    - apps/desktop-py/tests/test_chat.py (ver como os testes mockam _console() e config para seguir o padrão)
    - apps/desktop-py/tests/test_config_menu.py (se existir — ver padrão de teste do config menu)
  </read_first>
  <behavior>
    - Test PY-02: _handle_agentic_event("task:step:start", payload, config) NÃO chama _console().print quando config.agentic_step_progress=False
    - Test PY-02b: _handle_agentic_event("task:step:start", payload, config) chama _console().print quando config.agentic_step_progress=True
    - Test PY-02c: _handle_agentic_event("task:step:end", payload, config) NÃO chama _console().print quando agentic_step_progress=False
    - Test PY-03: _handle_agentic_event("task:error", payload, config) SEMPRE chama _console().print mesmo com agentic_step_progress=False
    - Test PY-03b: _handle_agentic_event("task:cancelled", payload, config) SEMPRE chama _console().print mesmo com agentic_step_progress=False
    - Test PY-03c: _handle_agentic_event("task:awaiting-failure-decision", payload, config) SEMPRE chama _console().print mesmo com agentic_step_progress=False
    - Test EVT-01: _handle_agentic_event("task:auto-approved", payload, config) retorna None e NÃO chama _console().print (quando debug_events=False)
    - Test EVT-01b: _handle_agentic_event("task:auto-approved", payload, config) chama _console().print com "[debug]" quando debug_events=True
  </behavior>
  <action>
**Modificação 1 — `_handle_agentic_event` em `chat.py`:**

Localizar o bloco `elif event_type == "task:step:start":` e adicionar ANTES dele (dentro do corpo da função, após o parse de `task_id`):

```python
# D-05 (Phase 999.5): Filtro silencioso para eventos de progresso step
# task:step:start e task:step:end só são exibidos quando agentic_step_progress=True
if event_type in ("task:step:start", "task:step:end"):
    if not config.agentic_step_progress:
        # Supressão silenciosa — não exibir, retornar sem acumular para TTS
        if _debug_mode or config.debug_events:
            _console().print(f"[debug] {event_type} (suprimido — agentic_step_progress=False)")
        return None
    # Fall through para o handler abaixo quando flag=True
```

Adicionar handler para `task:auto-approved` ANTES do bloco `else:` (após `task:awaiting-failure-decision`):

```python
elif event_type == "task:auto-approved":
    # D-05 (Phase 999.5): Plano auto-aprovado do cache — silencioso para o usuário
    # task:done com o resultado final é suficiente como feedback
    if _debug_mode or config.debug_events:
        _console().print("[debug] plano auto-aprovado do cache")
    return None  # Não acumular para TTS
```

**Modificação 2 — `_show_config_menu` em `chat.py`:**

Adicionar item 6 na lista do menu (após item 5, antes de `"0. Sair"`):
```python
console.print(f"6. Progresso tarefas  [{'sim' if config.agentic_step_progress else 'nao'}]", markup=False)
```

Adicionar handler para choice "6" (após o bloco `elif choice == "5":`):
```python
elif choice == "6":
    config.agentic_step_progress = not config.agentic_step_progress
    status = "sim" if config.agentic_step_progress else "nao"
    console.print(f"[Progresso tarefas: {status}]", highlight=False)
```

**Criar `apps/desktop-py/tests/test_chat_silent.py`** com os testes listados nos behaviors.

Padrão de mock para _handle_agentic_event:
```python
import pytest
from unittest.mock import patch, MagicMock
from jarvis_desktop.config import JarvisConfig

def make_config(**kwargs):
    return JarvisConfig(**kwargs)

@pytest.fixture
def mock_console():
    with patch("jarvis_desktop.chat._console") as mock_c:
        console_instance = MagicMock()
        mock_c.return_value = console_instance
        yield console_instance

def test_step_event_suppressed_when_flag_false(mock_console):
    from jarvis_desktop.chat import _handle_agentic_event
    import json
    payload = json.dumps({"taskId": "t1", "stepId": 1, "description": "Listar arquivos"})
    config = make_config(agentic_step_progress=False)
    result = _handle_agentic_event("task:step:start", payload, config)
    assert result is None
    mock_console.print.assert_not_called()
```

Usar `import jarvis_desktop.chat as chat_module; chat_module._debug_mode = False` no setup para garantir estado limpo do módulo global `_debug_mode`.
  </action>
  <verify>
    <automated>cd apps/desktop-py && uv run pytest tests/test_chat_silent.py -q</automated>
  </verify>
  <done>
    - `chat.py` contém filtro `if event_type in ("task:step:start", "task:step:end"): if not config.agentic_step_progress: return None`
    - `chat.py` contém handler `elif event_type == "task:auto-approved":`
    - `chat.py` menu item 6 com `Progresso tarefas` e toggle `agentic_step_progress`
    - Todos os testes PY-02*, PY-03*, EVT-01* passam em `test_chat_silent.py`
    - Suite completa Python sem regressões: `uv run pytest tests/ -q`
  </done>
</task>

</tasks>

<verification>
```bash
cd apps/desktop-py && uv run pytest tests/ -q
```
Suite completa Python deve passar. Verificar especificamente:
- `test_config.py`: testes agentic_step_progress* verdes + sem regressões
- `test_chat_silent.py`: PY-02*, PY-03*, EVT-01* todos verdes
- `test_chat.py`: sem regressões nos testes existentes
</verification>

<success_criteria>
- `config.py` tem `agentic_step_progress: bool = Field(default=False, ...)`
- `_handle_agentic_event` suprime task:step:start/end quando flag=False
- `_handle_agentic_event` SEMPRE exibe task:error, task:cancelled, task:awaiting-failure-decision
- Handler `task:auto-approved` silencioso (retorna None sem print)
- Menu `/config` item 6 toggle `agentic_step_progress` funcional
- Todos os testes do plano passando, sem regressões
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-02-SUMMARY.md` com:
- Arquivos modificados
- Localização exata das mudanças em chat.py (linhas aproximadas)
- Testes criados e o que cobrem
- Comando de verificação
</output>
