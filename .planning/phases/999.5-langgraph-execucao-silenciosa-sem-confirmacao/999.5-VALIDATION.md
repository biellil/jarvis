---
phase: 999.5
slug: langgraph-execucao-silenciosa-sem-confirmacao
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-27
---

# Phase 999.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (backend-ts tests via vitest) |
| **Config file** | `apps/backend-ts/vitest.config.ts` / `apps/desktop-py/pyproject.toml` |
| **Quick run command** | `cd apps/backend-ts && npm test -- --run` |
| **Full suite command** | `cd apps/backend-ts && npm test -- --run && cd ../desktop-py && python -m pytest tests/ -q` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd apps/backend-ts && npm test -- --run`
- **After every plan wave:** Run full suite (backend-ts + desktop-py)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 999.5-01-01 | 01 | 1 | D-01/D-02 | unit | `cd apps/backend-ts && npm test -- --run --reporter=verbose graph` | ❌ W0 | ⬜ pending |
| 999.5-01-02 | 01 | 1 | D-03 | unit | `cd apps/backend-ts && npm test -- --run --reporter=verbose graph` | ❌ W0 | ⬜ pending |
| 999.5-02-01 | 02 | 1 | D-04 | unit | `cd apps/backend-ts && npm test -- --run --reporter=verbose chat-session` | ❌ W0 | ⬜ pending |
| 999.5-03-01 | 03 | 2 | D-05 | unit | `cd apps/desktop-py && python -m pytest tests/ -q -k agentic_step_progress` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `apps/backend-ts/src/agent/__tests__/graph.test.ts` — stubs for approval memory and critical action detection
- [ ] `apps/backend-ts/src/session/chat-session.test.ts` — stubs for awaitingConfirmation state
- [ ] `apps/desktop-py/tests/test_chat_silent.py` — stubs for agentic_step_progress behavior

*Existing test infrastructure (vitest + pytest) covers both stacks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Auto-skip interrupt on repeat task | D-03 | Requires running JARVIS backend + sending real task twice | Start backend, send a task via chat, confirm, send same task again — should execute without interrupt |
| Critical action always prompts | D-02 | Requires real plan with deletion step | Send "delete file X" — should always show confirmation regardless of approval history |
| Keyword confirmation in chat | D-04 | SSE event flow across backend + Python client | Start task needing confirmation, type "sim" in terminal — should resume |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
