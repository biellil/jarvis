---
phase: 999.5-langgraph-execucao-silenciosa-sem-confirmacao
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/routes/chat.ts
  - apps/backend-ts/src/session/chat-session.test.ts
autonomous: true
requirements:
  - D-04

must_haves:
  truths:
    - "Quando JARVIS está aguardando confirmação (task:awaiting-confirmation emitido), a próxima mensagem do usuário é roteada para POST /tasks/:taskId/resume em vez de nova completion LLM"
    - "Após confirmação resolvida (confirm ou cancel), o flag awaitingConfirmation é limpo e as próximas mensagens voltam ao fluxo normal LLM"
    - "ChatSession expõe setAwaitingConfirmation(taskId, threadId), getAwaitingConfirmation(), clearAwaitingConfirmation()"
    - "chat.ts detecta task:awaiting-confirmation event e chama session.setAwaitingConfirmation()"
    - "chat.ts detecta awaitingConfirmation ativo antes de iniciar novo stream LLM e roteia para /tasks/:taskId/resume"
  artifacts:
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "awaitingConfirmation state management"
      contains: "awaitingConfirmation"
    - path: "apps/backend-ts/src/routes/chat.ts"
      provides: "roteamento de replies de confirmação"
      contains: "getAwaitingConfirmation"
  key_links:
    - from: "apps/backend-ts/src/routes/chat.ts (GET /chat/stream)"
      to: "session.getAwaitingConfirmation()"
      via: "check before starting LLM stream"
      pattern: "getAwaitingConfirmation"
    - from: "apps/backend-ts/src/routes/chat.ts (task:awaiting-confirmation event)"
      to: "session.setAwaitingConfirmation(taskId, threadId)"
      via: "after writing SSE event to response"
      pattern: "setAwaitingConfirmation"
---

<objective>
Implementar o roteamento de respostas de confirmação no backend TypeScript: `ChatSession.awaitingConfirmation` flag e intercepção no router de chat para redirecionar mensagens do usuário para `/tasks/:taskId/resume` quando há confirmação pendente.

Purpose: Confirmação via chat (bubble normal) em vez de prompt bloqueante — o usuário responde na mesma linha de chat, sem fluxo separado.
Output: `chat-session.ts` com 3 métodos de awaitingConfirmation, `chat.ts` com roteamento condicional, testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-CONTEXT.md
@.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-RESEARCH.md

<interfaces>
<!-- Interfaces existentes para o executor — ler antes de modificar -->

From apps/backend-ts/src/session/chat-session.ts (estrutura da classe — campos privados existentes):
```typescript
export class ChatSession {
  public history: BaseMessage[];
  private llm: BaseChatModel;
  private readonly memory: MemoryManager;
  private readonly _convId: number | null;
  private _agent: ReactAgentLike;
  private readonly _toolLogger: ToolLogger;
  private readonly _listenerBox: ListenerBox;
  private readonly _clientIdRef: ClientIdRef;
  private readonly _captureScreenFn: CaptureScreenFn | null;
  private readonly _capabilities: CapabilityMatrix;
  private _activeProvider: string;
  private readonly _signalRef: { signal: AbortSignal | null };
  private readonly _taskMetaRef: { meta: TaskMeta | null };
  private _agenticGraph: unknown = null;
  // ... (ADICIONAR awaitingConfirmation aqui)

  // Métodos existentes relevantes:
  setDispatchListener(fn: OnToolDispatched): void
  clearDispatchListener(): void
  setActiveSignal(signal: AbortSignal | null): void
  setActiveTaskMeta(meta: TaskMeta | null): void
  setClientId(id: string): void
  get agenticEnabled(): boolean
  getOrCreateAgenticGraph(): unknown
  swapLLM(newLlm: BaseChatModel): void
  setActiveProvider(provider: string): void
  async send(text: string, imageBase64?: string): Promise<string>
  async *sendStream(text: string, imageBase64?: string): AsyncGenerator<string, void, unknown>
}
```

From apps/backend-ts/src/routes/chat.ts (fluxo agentic — GET /chat/stream):
```typescript
// Atual: detecta task:awaiting-confirmation APENAS para escrever o evento SSE
const snapshot = await graph.getState({ configurable: { thread_id: taskId } });
const interrupts = (snapshot.tasks?.[0] as { interrupts?: Array<{ value: unknown }> })?.interrupts ?? [];
if (interrupts.length > 0) {
  const v = interrupts[0]!.value as { kind: string; stepId?: number; error?: string };
  if (v.kind === 'plan-confirmation') {
    res.write(`event: task:awaiting-confirmation\ndata: ${JSON.stringify({ taskId })}\n\n`);
    // ADICIONAR AQUI: session.setAwaitingConfirmation(taskId, taskId)
  }
  // ...
}
// NOVO: antes de iniciar qualquer LLM stream, verificar se há confirmação pendente
```

From apps/backend-ts/src/agent/keywords.ts (keyword parsing existente):
```typescript
export function matchTaskKeyword(utterance: string, taskState: TaskUiKind): KeywordMatch | null
// KeywordMatch: { kind: 'confirm' } | { kind: 'cancel' } | { kind: 'edit'; feedback: string }
// TaskUiKind: 'awaiting-confirmation' | 'executing'
```

From apps/backend-ts/src/routes/tasks.ts (resume endpoint):
```typescript
export const activeGraphs = new Map<string, ReturnType<typeof buildTaskGraph>>();
// POST /api/tasks/:taskId/resume — resume a paused agentic graph
// Body: resumeRequestSchema (confirm | edit | continue | replan | abort)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: awaitingConfirmation state em ChatSession</name>
  <files>
    apps/backend-ts/src/session/chat-session.ts,
    apps/backend-ts/src/session/chat-session.test.ts
  </files>
  <read_first>
    - apps/backend-ts/src/session/chat-session.ts (ler completo — ver todos os campos privados e onde adicionar)
    - apps/backend-ts/src/session/chat-session.test.ts (ver padrão de testes existentes — mocks, setup)
  </read_first>
  <behavior>
    - Test AWC-01: session.getAwaitingConfirmation() retorna null por padrão após create()
    - Test AWC-02: session.setAwaitingConfirmation("task-123", "thread-456") → getAwaitingConfirmation() retorna { taskId: "task-123", threadId: "thread-456" }
    - Test AWC-03: após setAwaitingConfirmation + clearAwaitingConfirmation → getAwaitingConfirmation() retorna null
  </behavior>
  <action>
**Adicionar campo privado em `ChatSession`** (após `private _agenticGraph: unknown = null;`):
```typescript
// Phase 999.5 (D-04): tracks pending plan confirmation via chat.
// Set when task:awaiting-confirmation is emitted; cleared after user confirms/cancels.
private _awaitingConfirmation: { taskId: string; threadId: string } | null = null;
```

**Adicionar 3 métodos públicos** (após o método `setActiveProvider`):
```typescript
/** Phase 999.5 D-04: Set when a task is waiting for user confirmation via chat message. */
setAwaitingConfirmation(taskId: string, threadId: string): void {
  this._awaitingConfirmation = { taskId, threadId };
}

/** Phase 999.5 D-04: Check before starting new LLM invocation. Returns null if not waiting. */
getAwaitingConfirmation(): { taskId: string; threadId: string } | null {
  return this._awaitingConfirmation;
}

/** Phase 999.5 D-04: Clear after confirmation is resolved (confirm or cancel). */
clearAwaitingConfirmation(): void {
  this._awaitingConfirmation = null;
}
```

**Adicionar testes em `chat-session.test.ts`:**

Adicionar `describe('awaitingConfirmation (Phase 999.5)', () => { ... })` seguindo o padrão existente de setup com `beforeEach` e `createChatSession`. Os testes devem usar uma sessão criada com `await ChatSession.create(opts)` com mocks já existentes no arquivo.

Verificar se o arquivo de testes já tem um helper de criação de sessão — reutilizar ele. Caso contrário, criar inline com o padrão de mocks existente (vi.mock para createReactAgent, etc.).
  </action>
  <verify>
    <automated>cd apps/backend-ts && npm test -- --run --reporter=verbose chat-session</automated>
  </verify>
  <done>
    - `chat-session.ts` contém `private _awaitingConfirmation`
    - `chat-session.ts` exporta `setAwaitingConfirmation`, `getAwaitingConfirmation`, `clearAwaitingConfirmation` como métodos públicos
    - Testes AWC-01, AWC-02, AWC-03 passam
    - Suite chat-session sem regressões
  </done>
</task>

<task type="auto">
  <name>Task 2: Roteamento de confirmação em chat.ts</name>
  <files>
    apps/backend-ts/src/routes/chat.ts,
    apps/backend-ts/src/routes/chat.test.ts
  </files>
  <read_first>
    - apps/backend-ts/src/routes/chat.ts (ler completo — ver fluxo agentic do GET /chat/stream, especialmente o bloco de interrupt check após o loop de stream)
    - apps/backend-ts/src/routes/chat.test.ts (ver testes existentes para entender mocks)
    - apps/backend-ts/src/agent/keywords.ts (matchTaskKeyword — parsing de intent de confirmação)
    - apps/backend-ts/src/routes/tasks.ts (activeGraphs map — para resume via graph direto)
  </read_first>
  <action>
**Duas modificações em `chat.ts`:**

**Modificação 1 — Detectar task:awaiting-confirmation e armazenar no estado da sessão:**

No bloco de interrupt check (após o loop `for await (const chunk of stream)`), onde `task:awaiting-confirmation` é escrito no SSE:

```typescript
if (v.kind === 'plan-confirmation') {
  res.write(`event: task:awaiting-confirmation\ndata: ${JSON.stringify({ taskId })}\n\n`);
  // D-04 (Phase 999.5): marcar sessão como aguardando confirmação para rotear próxima mensagem
  session.setAwaitingConfirmation(taskId, taskId);
}
```

**Modificação 2 — Interceptar mensagem de confirmação ANTES de iniciar novo stream LLM:**

No início do handler `router.get('/chat/stream', ...)`, APÓS a aquisição do lock e ANTES de `const isAgenticTurn = ...`:

```typescript
// D-04 (Phase 999.5): Se há confirmação pendente, rotear mensagem para /resume em vez de LLM
const pendingConfirmation = session.getAwaitingConfirmation();
if (pendingConfirmation) {
  const { matchTaskKeyword } = await import('../agent/keywords.js');
  const match = matchTaskKeyword(message, 'awaiting-confirmation');
  const resumeKind = match?.kind === 'confirm' ? 'confirm'
    : match?.kind === 'edit' ? 'edit'
    : 'cancel'; // default seguro: cancela se keyword não reconhecida

  session.clearAwaitingConfirmation();

  // Emitir SSE headers primeiro
  res.setHeader('Content-Type', 'text/event-stream');
  res.setHeader('Cache-Control', 'no-cache');
  res.setHeader('Connection', 'keep-alive');
  res.flushHeaders();

  // Buscar o graph ativo para esse task
  const pendingTaskId = pendingConfirmation.taskId;
  const graph = activeGraphs.get(pendingTaskId) as ReturnType<typeof import('../agent/graph.js').buildTaskGraph> | undefined;

  if (!graph) {
    res.write(`event: task:error\ndata: ${JSON.stringify({ taskId: pendingTaskId, atStep: 0, message: 'Task not found — may have expired' })}\n\n`);
    res.end();
    release();
    return;
  }

  const controller = activeControllers.get(pendingTaskId) ?? new AbortController();
  activeControllers.set(pendingTaskId, controller);
  session.setActiveSignal(controller.signal);

  const resumeBody = resumeKind === 'edit' && match?.kind === 'edit'
    ? { kind: 'edit' as const, feedback: match.feedback }
    : { kind: resumeKind as 'confirm' | 'cancel' };

  try {
    const resumeStream = await graph.stream(
      new Command({ resume: resumeBody }),
      {
        configurable: { thread_id: pendingTaskId },
        streamMode: ['custom', 'messages'] as unknown as 'custom'[],
        signal: controller.signal,
      },
    );

    let isTerminal = false;
    for await (const chunk of resumeStream) {
      const [mode, data] = Array.isArray(chunk) ? chunk : ['custom', chunk];
      if (mode === 'custom') {
        const evt = data as { kind: string };
        res.write(`event: ${evt.kind}\ndata: ${JSON.stringify({ taskId: pendingTaskId, ...evt })}\n\n`);
        if (TERMINAL_KINDS.has(evt.kind)) isTerminal = true;
      } else if (mode === 'messages') {
        const [msgChunk] = Array.isArray(data) ? data : [data];
        const content = (msgChunk as { content?: unknown })?.content;
        if (typeof content === 'string' && content) {
          res.write(`data: ${content.replace(/\n/g, '\\n')}\n\n`);
        }
      }
    }

    if (isTerminal) {
      void taskCheckpointer.deleteThread(pendingTaskId).catch(() => {});
      activeControllers.delete(pendingTaskId);
      activeGraphs.delete(pendingTaskId);
    }
  } catch (err) {
    res.write(`event: task:error\ndata: ${JSON.stringify({ taskId: pendingTaskId, atStep: 0, message: (err as Error).message })}\n\n`);
    void taskCheckpointer.deleteThread(pendingTaskId).catch(() => {});
    activeControllers.delete(pendingTaskId);
    activeGraphs.delete(pendingTaskId);
  } finally {
    session.setActiveSignal(null);
    res.end();
    release();
  }
  return; // Não continua para o fluxo normal
}
```

IMPORTANTE: Este bloco deve ser inserido APÓS o `res.status(400)` check de message vazio e APÓS a aquisição do lock (`const release = lock.tryAcquire()`), mas ANTES de `res.setHeader('Content-Type', 'text/event-stream')` do fluxo normal. A lógica de `res.setHeader` e `res.flushHeaders` está DENTRO do bloco de `pendingConfirmation`, não no fluxo normal.

ATENÇÃO: O fluxo normal já tem `res.setHeader + res.flushHeaders` — mover esses calls para APÓS o bloco de `pendingConfirmation` NÃO é necessário porque o `return` garante que os dois caminhos são mutuamente exclusivos.

**Testes em `chat.test.ts`:**

Adicionar testes que verificam:
- Quando `session.getAwaitingConfirmation()` retorna `{ taskId, threadId }` e mensagem é "sim" → `matchTaskKeyword` retorna confirm → `graph.stream` é chamado com `Command({ resume: { kind: 'confirm' } })`
- Quando `session.getAwaitingConfirmation()` retorna `{ taskId, threadId }` e mensagem é "cancel" → `graph.stream` é chamado com `Command({ resume: { kind: 'cancel' } })`
- Após roteamento de confirmação, `session.clearAwaitingConfirmation()` foi chamado

Usar mocks existentes do arquivo `chat.test.ts` para `session` (provavelmente já tem spy functions). Se o arquivo não tem mocks de `getAwaitingConfirmation`, adicionar `vi.fn()` retornando null por padrão para não quebrar testes existentes.
  </action>
  <verify>
    <automated>cd apps/backend-ts && npm test -- --run --reporter=verbose chat</automated>
  </verify>
  <done>
    - `chat.ts` contém `session.getAwaitingConfirmation()` no início do handler GET /chat/stream
    - `chat.ts` contém `session.setAwaitingConfirmation(taskId, taskId)` após emitir task:awaiting-confirmation
    - `chat.ts` contém `session.clearAwaitingConfirmation()` no bloco de roteamento
    - Testes de roteamento de confirmação passam (confirm e cancel)
    - Suite completa backend sem regressões: `cd apps/backend-ts && npm test -- --run`
  </done>
</task>

</tasks>

<verification>
```bash
cd apps/backend-ts && npm test -- --run
```
Suite completa backend deve passar. Verificar especificamente:
- `chat-session.test.ts`: AWC-01, AWC-02, AWC-03 verdes + sem regressões
- `chat.test.ts`: testes de roteamento de confirmação + sem regressões
- TypeScript clean: `cd apps/backend-ts && npx tsc --noEmit`
</verification>

<success_criteria>
- `ChatSession` tem `_awaitingConfirmation` privado e 3 métodos públicos (set/get/clear)
- `chat.ts` detecta `task:awaiting-confirmation` e chama `setAwaitingConfirmation`
- `chat.ts` intercepta mensagem seguinte quando `getAwaitingConfirmation() !== null` e roteia para `/resume`
- Keyword "sim"/"s"/"y" → `{ kind: 'confirm' }`; "cancel"/"n"/"não" → `{ kind: 'cancel' }`
- Após resolução, `clearAwaitingConfirmation()` é chamado
- Testes AWC-01/02/03 e testes de roteamento passam
- Suite completa backend sem regressões
</success_criteria>

<output>
Após completar, criar `.planning/phases/999.5-langgraph-execucao-silenciosa-sem-confirmacao/999.5-03-SUMMARY.md` com:
- Arquivos modificados e onde exatamente
- Decisão sobre posicionamento do bloco de confirmação em chat.ts (antes ou depois de flushHeaders)
- Testes adicionados
- Comando de verificação
</output>
