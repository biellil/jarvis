---
phase: 38-rolling-summarization
plan: 03
type: execute
wave: 3
depends_on:
  - 38-02
files_modified:
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/session/chat-session.test.ts
autonomous: true
requirements:
  - MSUM-01
  - MSUM-02
  - MSUM-03
must_haves:
  truths:
    - "ChatSession.send() chama void this.memory.runRollingSummarization(this._convId) após saveTurn"
    - "ChatSession.sendStream() chama void this.memory.runRollingSummarization(this._convId) após saveTurn"
    - "send() retorna sem aguardar a sumarização — pipeline de voz não bloqueado (MSUM-02)"
    - "Testes existentes de chat-session.test.ts continuam 100% verdes"
    - "Suite completa de testes do backend passa: npm test"
  artifacts:
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "Void calls de runRollingSummarization em send() e sendStream()"
      contains: "runRollingSummarization"
    - path: "apps/backend-ts/src/session/chat-session.test.ts"
      provides: "Testes verificando fire-and-forget do trigger de sumarização"
      contains: "runRollingSummarization"
  key_links:
    - from: "ChatSession.send()"
      to: "MemoryManager.runRollingSummarization"
      via: "void this.memory.runRollingSummarization(this._convId)"
      pattern: "void this\\.memory\\.runRollingSummarization"
    - from: "ChatSession.sendStream()"
      to: "MemoryManager.runRollingSummarization"
      via: "void this.memory.runRollingSummarization(this._convId)"
      pattern: "void this\\.memory\\.runRollingSummarization"
---

<objective>
Adicionar as void calls de trigger de sumarização em ChatSession.send() e sendStream(), e estender os testes de chat-session.test.ts para verificar o comportamento fire-and-forget.

Purpose: Fechar o ciclo MSUM-02 — o trigger dispara após cada turn sem bloquear o pipeline de voz. Replicar exatamente o padrão estabelecido para _extractAndWriteMemories na Phase 36.

Output: chat-session.ts com 2 void calls adicionadas. chat-session.test.ts com 2 testes novos. Suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-rolling-summarization/38-CONTEXT.md
@.planning/phases/38-rolling-summarization/38-02-SUMMARY.md
@apps/backend-ts/src/session/chat-session.ts
@apps/backend-ts/src/session/chat-session.test.ts
@apps/backend-ts/src/memory/manager.ts

<interfaces>
<!-- Contrato existente em chat-session.ts após Phase 36 -->

Em send() (linha ~165 — padrão a replicar EXATAMENTE):
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction
// CRITICAL: void context — never await — extraction must not block message handler
void this._extractAndWriteMemories(text, finalText);

// Phase 38 (MSUM-01, MSUM-02): fire-and-forget rolling summarization
// ADICIONAR AQUI:
void this.memory.runRollingSummarization(this._convId);

return finalText;
```

Em sendStream() (linha ~222 — padrão a replicar EXATAMENTE):
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction (after stream drains)
void this._extractAndWriteMemories(text, assembled);

// Phase 38 (MSUM-01, MSUM-02): fire-and-forget rolling summarization
// ADICIONAR AQUI:
void this.memory.runRollingSummarization(this._convId);
```

Assinatura em manager.ts (verificada na Wave 2):
```typescript
async runRollingSummarization(convId: number | null): Promise<void>
```

Padrão de mock de MemoryManager em chat-session.test.ts (linhas 54-59):
```typescript
function makeMemory(convId: number | null = 42) {
  return {
    startConversation: vi.fn().mockResolvedValue(convId),
    saveTurn: vi.fn().mockResolvedValue(undefined),
    buildContext: vi.fn().mockResolvedValue('### User profile\n- gosto: café'),
  } as any;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatSession — adicionar void calls de runRollingSummarization em send() e sendStream()</name>
  <read_first>
    - apps/backend-ts/src/session/chat-session.ts (arquivo a modificar — ler estado atual, especialmente send() e sendStream())
    - apps/backend-ts/src/memory/manager.ts (verificar que runRollingSummarization existe com assinatura correta)
  </read_first>
  <files>apps/backend-ts/src/session/chat-session.ts</files>
  <behavior>
    - send() chama void this.memory.runRollingSummarization(this._convId) após void _extractAndWriteMemories
    - sendStream() chama void this.memory.runRollingSummarization(this._convId) após void _extractAndWriteMemories
    - A call não é awaited — fire-and-forget puro
    - Nenhum outro código em send() ou sendStream() é alterado
  </behavior>
  <action>
Modificar `apps/backend-ts/src/session/chat-session.ts` em EXATAMENTE 2 locais:

**Local 1 — em send(), após a linha `void this._extractAndWriteMemories(text, finalText);` (linha ~165):**

Substituir o bloco:
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction
// CRITICAL: void context — never await — extraction must not block message handler
void this._extractAndWriteMemories(text, finalText);

return finalText;
```

Por:
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction
// CRITICAL: void context — never await — extraction must not block message handler
void this._extractAndWriteMemories(text, finalText);

// Phase 38 (MSUM-01, MSUM-02): fire-and-forget rolling summarization
// CRITICAL: void context — nunca aguardar — sumarização não bloqueia pipeline de voz
void this.memory.runRollingSummarization(this._convId);

return finalText;
```

**Local 2 — em sendStream(), após a linha `void this._extractAndWriteMemories(text, assembled);` (linha ~222):**

Substituir o bloco:
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction (after stream drains)
void this._extractAndWriteMemories(text, assembled);
```

Por:
```typescript
// Phase 36 (MEMW-01, REL-01): fire-and-forget memory extraction (after stream drains)
void this._extractAndWriteMemories(text, assembled);

// Phase 38 (MSUM-01, MSUM-02): fire-and-forget rolling summarization (after stream drains)
// CRITICAL: void context — nunca aguardar — sumarização não bloqueia pipeline de voz
void this.memory.runRollingSummarization(this._convId);
```

Nenhuma outra linha do arquivo deve ser alterada.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npm test -- src/session/chat-session.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - chat-session.ts contém `void this.memory.runRollingSummarization(this._convId)` exatamente 2 vezes
    - A primeira ocorrência está dentro do método `send()` após `void this._extractAndWriteMemories`
    - A segunda ocorrência está dentro do método `sendStream()` após `void this._extractAndWriteMemories`
    - `npm test -- src/session/chat-session.test.ts` retorna 0 failures (todos os testes existentes verdes)
    - chat-session.ts NÃO contém `await this.memory.runRollingSummarization` (nunca await)
  </acceptance_criteria>
  <done>Void calls adicionadas em send() e sendStream(). Testes existentes de chat-session.test.ts: 100% verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat-session.test.ts — testes de fire-and-forget para runRollingSummarization + verificação final da suite</name>
  <read_first>
    - apps/backend-ts/src/session/chat-session.test.ts (arquivo a estender — ler estado atual, especialmente makeMemory() e os describes existentes)
    - apps/backend-ts/src/session/chat-session.ts (verificar que as void calls foram adicionadas na Task 1)
  </read_first>
  <files>apps/backend-ts/src/session/chat-session.test.ts</files>
  <behavior>
    - Test 1: send() chama memory.runRollingSummarization com convId correto (42)
    - Test 2: send() retorna sem esperar runRollingSummarization — mock de runRollingSummarization que nunca resolve, send() retorna normalmente
    - Test 3: sendStream() chama memory.runRollingSummarization com convId correto após stream drain
    - Test 4: quando convId é null, runRollingSummarization é chamado com null (não levanta exceção)
  </behavior>
  <action>
Modificar `apps/backend-ts/src/session/chat-session.test.ts` em 2 pontos:

**Ponto 1 — Adicionar `runRollingSummarization` ao mock `makeMemory()`:**

Encontrar e substituir a função `makeMemory`:
```typescript
// ANTES:
function makeMemory(convId: number | null = 42) {
  return {
    startConversation: vi.fn().mockResolvedValue(convId),
    saveTurn: vi.fn().mockResolvedValue(undefined),
    buildContext: vi.fn().mockResolvedValue('### User profile\n- gosto: café'),
  } as any;
}

// DEPOIS:
function makeMemory(convId: number | null = 42) {
  return {
    startConversation: vi.fn().mockResolvedValue(convId),
    saveTurn: vi.fn().mockResolvedValue(undefined),
    buildContext: vi.fn().mockResolvedValue('### User profile\n- gosto: café'),
    runRollingSummarization: vi.fn().mockResolvedValue(undefined),
  } as any;
}
```

**Ponto 2 — Adicionar 4 testes no final do describe 'ChatSession (agent runtime)'**, antes do fechamento `})` do describe:

```typescript
  it('send() chama memory.runRollingSummarization com o convId da sessão', async () => {
    const session = await ChatSession.create({ llm, memory });
    await session.send('oi');
    expect(memory.runRollingSummarization).toHaveBeenCalledOnce();
    expect(memory.runRollingSummarization).toHaveBeenCalledWith(42);
  });

  it('send() retorna sem aguardar runRollingSummarization — fire-and-forget', async () => {
    // Mock que nunca resolve para provar que send() não espera
    let resolveSum: () => void;
    memory.runRollingSummarization = vi.fn().mockImplementation(
      () => new Promise<void>((res) => { resolveSum = res; }),
    );
    const session = await ChatSession.create({ llm, memory });
    // send() deve completar mesmo com runRollingSummarization pendente
    const replyPromise = session.send('teste fire-and-forget');
    await expect(replyPromise).resolves.toBe('pong');
    // cleanup: resolver a promise pendente para evitar leaks
    resolveSum!();
  });

  it('sendStream() chama memory.runRollingSummarization após drain do stream', async () => {
    agentStreamImpl = async function* () {
      yield [new AIMessageChunk({ content: 'chunk1' }), {}];
      yield [new AIMessageChunk({ content: 'chunk2' }), {}];
    };
    const session = await ChatSession.create({ llm, memory });
    const chunks: string[] = [];
    for await (const tok of session.sendStream('oi')) {
      chunks.push(tok);
    }
    expect(chunks).toEqual(['chunk1', 'chunk2']);
    expect(memory.runRollingSummarization).toHaveBeenCalledOnce();
    expect(memory.runRollingSummarization).toHaveBeenCalledWith(42);
  });

  it('send() chama runRollingSummarization com null quando convId é null', async () => {
    const nullMemory = makeMemory(null);
    const session = await ChatSession.create({ llm, memory: nullMemory });
    await session.send('oi');
    expect(nullMemory.runRollingSummarization).toHaveBeenCalledWith(null);
  });
```

NOTA: Certificar que `agentStreamImpl` ainda está acessível no escopo onde os novos testes são inseridos (está definida no topo do arquivo de teste como variável de módulo — deve funcionar).
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npm test -- src/session/chat-session.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - chat-session.test.ts contém `runRollingSummarization: vi.fn().mockResolvedValue(undefined)` em makeMemory
    - chat-session.test.ts contém `memory.runRollingSummarization` em pelo menos 3 testes
    - `npm test -- src/session/chat-session.test.ts` retorna 0 failures (todos os testes verdes, incluindo os 4 novos)
    - `npm test -- src/memory/` retorna 0 failures (suite de memória verde)
    - `npm test` retorna 0 failures (suite completa do backend verde)
  </acceptance_criteria>
  <done>4 testes adicionados a chat-session.test.ts. Suite completa do backend: 0 failures. Phase 38 entregue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChatSession.send() → MemoryManager.runRollingSummarization | Chamada fire-and-forget interna — convId vem de startConversation(), não de input do usuário |
| ChatSession.sendStream() → fire-and-forget chain | Same boundary — assembled text de origin interna (LLM response) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08 | Denial | Void call sem await em send() | accept | Comportamento intencional (MSUM-02, D-01). Errors são capturados em runRollingSummarization internamente (MEM-05). Pipeline de voz nunca afetado mesmo que sumarização falhe. |
| T-38-09 | Tampering | convId passado para runRollingSummarization | accept | convId é number | null vindo de startConversation() — gerado internamente pelo banco, não exposto ao input do usuário. Sem risco de injection. |
</threat_model>

<verification>
```bash
# Suite completa para confirmar que nada quebrou
cd /root/jarvis/apps/backend-ts && npm test 2>&1 | tail -40
```

Critérios de aceitação finais da Phase 38:
1. `npm test` → 0 failures, 0 errors
2. `grep -n "runRollingSummarization" apps/backend-ts/src/session/chat-session.ts` → 2 matches (em send e sendStream)
3. `grep -n "_latestSummary" apps/backend-ts/src/memory/manager.ts` → ao menos 3 matches (declaração, atualização em run..., uso em buildContext)
4. `grep -n "effectiveSummary" apps/backend-ts/src/memory/manager.ts` → 1 match
5. `grep -n "countMessages\|getOldestMessages\|deleteMessages\|getLatestSummary" apps/backend-ts/src/memory/store.ts` → 4 matches (um por método)
</verification>

<success_criteria>
- chat-session.ts tem 2 void calls de runRollingSummarization (send + sendStream)
- chat-session.test.ts tem 4 testes novos verificando fire-and-forget
- `npm test` → 0 failures em toda a suite do backend
- Phase 38 entregue: MSUM-01, MSUM-02, MSUM-03 verificados em testes automatizados
- Commit: `feat(38): ChatSession wiring — void runRollingSummarization em send() e sendStream() — MSUM-02`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/38-rolling-summarization/38-03-SUMMARY.md`
</output>
