---
phase: 38-rolling-summarization
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - apps/backend-ts/src/memory/store.ts
  - apps/backend-ts/src/memory/manager.ts
autonomous: true
requirements:
  - MSUM-01
  - MSUM-02
  - MSUM-03
must_haves:
  truths:
    - "MemoryStore tem countMessages, getOldestMessages, deleteMessages, getLatestSummary — todos com try/catch MEM-05"
    - "MemoryManager tem campo privado _latestSummary: string | null = null"
    - "MemoryManager.runRollingSummarization() verifica threshold (count < 20 → retorna sem LLM), depois busca 10 oldest, gera summary, deleta SOMENTE após summary gerado, salva, atualiza cache"
    - "buildContext() usa rollingSum ?? this._latestSummary ?? undefined — call sites existentes não precisam de alteração"
    - "Todos os 9 testes RED de manager.test.ts ficam VERDES"
    - "Todos os 7 testes RED de store.test.ts ficam VERDES"
  artifacts:
    - path: "apps/backend-ts/src/memory/store.ts"
      provides: "4 métodos helper: countMessages, getOldestMessages, deleteMessages, getLatestSummary"
      contains: "countMessages"
    - path: "apps/backend-ts/src/memory/manager.ts"
      provides: "runRollingSummarization + _latestSummary + buildContext patch"
      contains: "_latestSummary"
  key_links:
    - from: "MemoryManager.runRollingSummarization"
      to: "MemoryStore.countMessages"
      via: "this.store.countMessages(convId)"
      pattern: "this\\.store\\.countMessages"
    - from: "MemoryManager.runRollingSummarization"
      to: "this.llm.invoke"
      via: "_generateRollingSummary"
      pattern: "this\\.llm\\.invoke"
    - from: "MemoryManager.buildContext"
      to: "_latestSummary"
      via: "rollingSum ?? this._latestSummary"
      pattern: "_latestSummary"
---

<objective>
Implementar os 4 métodos helper em MemoryStore e a lógica completa de rolling summarization em MemoryManager. Esta é a implementação GREEN que satisfaz todos os testes RED da Wave 1.

Purpose: Converter os testes RED em GREEN entregando MSUM-01 (lógica de compressão), MSUM-02 (fire-and-forget seguro) e MSUM-03 (injeção correta no buildContext).

Output: store.ts e manager.ts modificados. Suite de testes 100% verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-rolling-summarization/38-CONTEXT.md
@.planning/phases/38-rolling-summarization/38-RESEARCH.md
@.planning/phases/38-rolling-summarization/38-01-SUMMARY.md
@apps/backend-ts/src/memory/store.ts
@apps/backend-ts/src/memory/manager.ts
@apps/backend-ts/src/memory/schema.ts
@apps/backend-ts/src/memory/store.test.ts
@apps/backend-ts/src/memory/manager.test.ts

<interfaces>
<!-- Importações existentes em store.ts que precisam de extensão -->

De store.ts linha 11:
```typescript
import { and, eq, desc } from 'drizzle-orm';
// A adicionar: asc, inArray, sql
import { and, eq, desc, asc, inArray, sql } from 'drizzle-orm';
```

Schema de messages (schema.ts):
```typescript
export const messages = sqliteTable('messages', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  conversationId: integer('conversation_id').notNull().references(() => conversations.id),
  role: text('role', { enum: messageRoleEnum }).notNull(),  // ['user', 'assistant', 'system']
  content: text('content').notNull(),
  createdAt: text('created_at').notNull(),
});
```

Schema de summaries (schema.ts):
```typescript
export const summaries = sqliteTable('summaries', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  conversationId: integer('conversation_id').notNull().references(() => conversations.id),
  content: text('content').notNull(),
  createdAt: text('created_at').notNull(),
});
```

Interface MessageInput em store.ts:
```typescript
export interface MessageInput {
  role: (typeof schema.messageRoleEnum)[number];
  content: string;
  createdAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryStore — adicionar 4 métodos helper de sumarização</name>
  <read_first>
    - apps/backend-ts/src/memory/store.ts (arquivo a modificar — ler antes de editar)
    - apps/backend-ts/src/memory/schema.ts (colunas exatas para queries drizzle)
    - apps/backend-ts/src/memory/store.test.ts (testes RED que devem virar GREEN)
  </read_first>
  <files>apps/backend-ts/src/memory/store.ts</files>
  <behavior>
    - countMessages(convId, 20 msgs) → retorna 20; (convId com 0 msgs) → retorna 0; (convId inválido) → retorna 0 sem throw
    - getOldestMessages(convId, 3) → retorna array de 3 objetos com campo id: number, orderados por id ASC
    - deleteMessages([1,2,3]) → remove rows com esses ids; deleteMessages([]) → sem-op sem throw
    - getLatestSummary(convId com 2 summaries) → retorna content do mais recente; (convId sem summaries) → null
  </behavior>
  <action>
1. Atualizar o import de drizzle-orm na linha 11 do store.ts — adicionar `asc`, `inArray`, `sql` aos imports existentes:
   ```typescript
   import { and, eq, desc, asc, inArray, sql } from 'drizzle-orm';
   ```

2. Exportar a interface `MessageWithId` imediatamente após `MessageInput` (linha ~47):
   ```typescript
   export interface MessageWithId extends MessageInput {
     id: number;
   }
   ```

3. Adicionar os 4 métodos na classe `MemoryStore`, após o método `saveSummary` (linha ~146), antes de `upsertProfile`:

   ```typescript
   /**
    * Conta mensagens ativas (roles user/assistant) de uma conversa.
    * Returns 0 on error (MEM-05).
    */
   countMessages(convId: number): number {
     try {
       const result = this.db
         .select({ count: sql<number>`cast(count(*) as integer)` })
         .from(messages)
         .where(
           and(
             eq(messages.conversationId, convId),
             inArray(messages.role, ['user', 'assistant']),
           ),
         )
         .get();
       return result?.count ?? 0;
     } catch (exc) {
       console.warn(
         `MemoryStore.countMessages failed (convId=${convId}): ${(exc as Error).message}`,
       );
       return 0;
     }
   }

   /**
    * Retorna as N mensagens mais antigas de uma conversa (ORDER BY id ASC).
    * Returns [] on error (MEM-05).
    */
   getOldestMessages(convId: number, limit: number): MessageWithId[] {
     try {
       const rows = this.db
         .select()
         .from(messages)
         .where(eq(messages.conversationId, convId))
         .orderBy(asc(messages.id))
         .limit(limit)
         .all();
       return rows as unknown as MessageWithId[];
     } catch (exc) {
       console.warn(
         `MemoryStore.getOldestMessages failed (convId=${convId}): ${(exc as Error).message}`,
       );
       return [];
     }
   }

   /**
    * Deleta mensagens pelos IDs fornecidos. Sem-op se array vazio.
    * Errors logged, never throws (MEM-05).
    */
   deleteMessages(ids: number[]): void {
     if (ids.length === 0) return;
     try {
       this.db
         .delete(messages)
         .where(inArray(messages.id, ids))
         .run();
     } catch (exc) {
       console.warn(
         `MemoryStore.deleteMessages failed (${ids.length} ids): ${(exc as Error).message}`,
       );
     }
   }

   /**
    * Retorna o content do summary mais recente de uma conversa.
    * Returns null se não há summaries ou em caso de erro (MEM-05).
    */
   getLatestSummary(convId: number): string | null {
     try {
       const rows = this.db
         .select({ content: summaries.content })
         .from(summaries)
         .where(eq(summaries.conversationId, convId))
         .orderBy(desc(summaries.createdAt))
         .limit(1)
         .all();
       return rows[0]?.content ?? null;
     } catch (exc) {
       console.warn(
         `MemoryStore.getLatestSummary failed (convId=${convId}): ${(exc as Error).message}`,
       );
       return null;
     }
   }
   ```

4. Não modificar nenhum método existente — apenas adicionar os 4 novos e a interface MessageWithId.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npm test -- src/memory/store.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - store.ts contém `countMessages(convId: number): number`
    - store.ts contém `getOldestMessages(convId: number, limit: number): MessageWithId[]`
    - store.ts contém `deleteMessages(ids: number[]): void`
    - store.ts contém `getLatestSummary(convId: number): string | null`
    - store.ts contém `export interface MessageWithId extends MessageInput`
    - store.ts contém `import { and, eq, desc, asc, inArray, sql } from 'drizzle-orm'`
    - `npm test -- src/memory/store.test.ts` retorna 0 failures (todos os 7 novos testes verdes + testes existentes verdes)
  </acceptance_criteria>
  <done>4 métodos adicionados ao MemoryStore. Interface MessageWithId exportada. Todos os testes de store.test.ts verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemoryManager — _latestSummary, runRollingSummarization, buildContext patch</name>
  <read_first>
    - apps/backend-ts/src/memory/manager.ts (arquivo a modificar — ler estado atual)
    - apps/backend-ts/src/memory/manager.test.ts (testes RED que devem virar GREEN)
    - apps/backend-ts/src/memory/store.ts (verificar que MessageWithId foi exportado na Task 1)
  </read_first>
  <files>apps/backend-ts/src/memory/manager.ts</files>
  <behavior>
    - runRollingSummarization(null) → retorna void imediato, sem chamar store
    - runRollingSummarization(convId) com count < 20 → retorna void imediato, sem chamar LLM
    - runRollingSummarization(convId) com count >= 20 → busca 10 oldest, gera summary, deleta APENAS SE summary não-vazio, salva, atualiza _latestSummary
    - runRollingSummarization lança erro interno → captura, console.warn, resolve normalmente
    - buildContext(userText) sem rollingSum → usa this._latestSummary se não-null
    - buildContext(userText, 'explicit') → usa 'explicit', ignora _latestSummary
    - Summary injetado entre Perfil do usuário e Memórias semânticas
  </behavior>
  <action>
Modificar `apps/backend-ts/src/memory/manager.ts` em 4 pontos:

**Ponto 1 — Adicionar import de MessageWithId:**
Na linha 14, após o import de MemoryStore:
```typescript
import { MemoryStore, type ProfileFact, type MessageWithId } from './store.js';
```

**Ponto 2 — Adicionar campo privado _latestSummary na classe MemoryManager:**
Após `private readonly recallTopK: number;` (linha ~35), adicionar:
```typescript
private _latestSummary: string | null = null;
```

**Ponto 3 — Modificar buildContext() na linha 113:**
Substituir o bloco atual:
```typescript
// Section 2: Rolling summary (Phase 38 interface — D-05)
if (rollingSum) {
  parts.push(rollingSum);
}
```
Por:
```typescript
// Section 2: Rolling summary (Phase 38 — MSUM-03, D-04)
// Usa rollingSum explícito se fornecido; caso contrário usa cache _latestSummary
const effectiveSummary = rollingSum ?? this._latestSummary ?? undefined;
if (effectiveSummary) {
  parts.push(effectiveSummary);
}
```

**Ponto 4 — Adicionar métodos runRollingSummarization e _generateRollingSummary APÓS o método close() (linha ~201), antes do fechamento da classe:**

```typescript
/**
 * Trigger público de sumarização — chamar via void (fire-and-forget).
 * Verifica se a conversa atingiu 20 mensagens; se sim, sumariza as 10 mais antigas.
 * Implementa MSUM-01, MSUM-02 (D-01, D-01b, D-02, D-03, D-05, D-06).
 * Erros capturados internamente — nunca propagados (MEM-05 parity).
 */
async runRollingSummarization(convId: number | null): Promise<void> {
  if (convId === null) return;

  try {
    // D-01b: verificar threshold ANTES de qualquer operação cara
    const count = this.store.countMessages(convId);
    if (count < 20) return; // early exit — zero custo

    // D-06: buscar as 10 mensagens mais antigas
    const oldest = this.store.getOldestMessages(convId, 10);
    if (oldest.length === 0) return;

    // D-03: gerar sumário via LLM
    const summary = await this._generateRollingSummary(oldest);
    // Pitfall 3 protection: só deletar SE sumário foi gerado com sucesso
    if (!summary) return;

    // D-02: deletar as mensagens antigas e persistir o sumário
    const ids = oldest.map((m) => m.id);
    this.store.deleteMessages(ids);
    this.store.saveSummary(convId, summary);

    // D-04: atualizar cache para buildContext() usar sem query adicional
    this._latestSummary = summary;
  } catch (err) {
    // MEM-05: falha silenciosa — log only, never re-throw
    console.warn(
      `MemoryManager.runRollingSummarization failed (convId=${convId}): ${(err as Error).message}`,
    );
  }
}

/**
 * Gera um resumo textual das mensagens mais antigas usando o LLM.
 * Retorna '' se LLM indisponível ou em caso de erro.
 */
private async _generateRollingSummary(msgs: MessageWithId[]): Promise<string> {
  if (!this.llm) {
    console.warn('[summarization] Nenhum LLM disponível; pulando geração de sumário');
    return '';
  }

  try {
    const conversation = msgs
      .map((m) => `${m.role === 'user' ? 'User' : 'JARVIS'}: ${m.content}`)
      .join('\n\n');

    const SUMMARIZATION_PROMPT = `Você é o JARVIS, assistente inteligente. Resuma a conversa abaixo em 2-3 bullets concisos. Capture fatos importantes, decisões e contexto relevante para interações futuras.

=== Conversa ===
${conversation}

=== Resumo ===`;

    const result = await this.llm.invoke(SUMMARIZATION_PROMPT);
    const text = typeof result.content === 'string' ? result.content.trim() : '';
    return text ? `### Resumo da Conversa Anterior\n${text}` : '';
  } catch (err) {
    console.warn(`[summarization] LLM invocation failed: ${(err as Error).message}`);
    return '';
  }
}
```

ATENÇÃO: A assinatura pública de `buildContext(userText: string, rollingSum?: string)` NÃO muda — call sites existentes (tools.ts) continuam funcionando sem alteração (per D-04 e MCTX-04).
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/backend-ts && npm test -- src/memory/manager.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - manager.ts contém `private _latestSummary: string | null = null`
    - manager.ts contém `async runRollingSummarization(convId: number | null): Promise<void>`
    - manager.ts contém `private async _generateRollingSummary(`
    - manager.ts contém `const effectiveSummary = rollingSum ?? this._latestSummary ?? undefined`
    - manager.ts contém `if (count < 20) return`
    - manager.ts NÃO contém `await this.memory.runRollingSummarization` (não é ChatSession)
    - `npm test -- src/memory/manager.test.ts` retorna 0 failures (todos os 9 testes verdes)
    - `npm test -- src/memory/` retorna 0 failures (suite completa de memória verde)
  </acceptance_criteria>
  <done>MemoryManager com _latestSummary, runRollingSummarization e buildContext patch. Todos os 9 testes de manager.test.ts verdes. Suite de memória 100% verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM response → summary content | Resposta do LLM usada como conteúdo de sumário injetado no context |
| SQLite messages table → deleteMessages | IDs passados para deleção — devem vir de getOldestMessages, não de input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Tampering | MemoryStore.deleteMessages — IDs de deleção | mitigate | IDs vêm exclusivamente de getOldestMessages via drizzle select — nunca de input externo do usuário. Drizzle usa queries parametrizadas (inArray), sem string concatenation. |
| T-38-04 | Tampering | MemoryStore.countMessages — SQL injection | mitigate | Query usa drizzle `eq(messages.conversationId, convId)` parametrizado — convId é number TypeScript, sem interpolação de string. |
| T-38-05 | Disclosure | LLM summary content | accept | Sumário contém conteúdo da própria conversa do usuário, armazenado em SQLite local. Não transmitido para terceiros. Assistente pessoal de uso próprio — CLAUDE.md: privacidade por padrão. |
| T-38-06 | Denial | LLM calls sem threshold check | mitigate | D-01b implementado: `if (count < 20) return` antes de qualquer chamada LLM. Threshold por convId evita DDoS por volume de mensagens. |
| T-38-07 | Denial | Deleção antes de summary | mitigate | Pitfall 3 protection: `if (!summary) return` garante que deleteMessages só é chamado após summary gerado com sucesso. Perda de dados impossível por falha LLM. |
</threat_model>

<verification>
```bash
# Verificar que todos os testes da suíte de memória passam
cd /root/jarvis/apps/backend-ts && npm test -- src/memory/ 2>&1 | tail -40
```

Critérios:
- `store.test.ts`: todos os testes originais + 7 novos helper tests → verde
- `manager.test.ts`: todos os 9 testes → verde
- `vectors.test.ts`, `embeddings.test.ts`, `profile.test.ts`, `voice-log.test.ts`: verde (não modificados)
</verification>

<success_criteria>
- store.ts exporta `MessageWithId` interface e 4 métodos helper com padrão MEM-05
- manager.ts tem `_latestSummary`, `runRollingSummarization`, `_generateRollingSummary`, buildContext patch
- `npm test -- src/memory/` → 0 failures
- Commit: `feat(38): implementar MemoryStore helpers e MemoryManager.runRollingSummarization — MSUM-01, MSUM-02, MSUM-03`
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/38-rolling-summarization/38-02-SUMMARY.md`
</output>
