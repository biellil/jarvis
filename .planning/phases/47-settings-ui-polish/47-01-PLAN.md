---
phase: 47-settings-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/main/settingsWindow.ts
  - apps/desktop/src/renderer/src/settings/SettingsForm.tsx
  - apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx
autonomous: false
requirements:
  - POLISH-01
user_setup: []

must_haves:
  truths:
    - "The Settings window opens noticeably wider than before — 600px instead of 480px"
    - "Each of the four sections (PTT, Always-Listening, TTS, Whisper) has visually distinct separation with 32px gaps between them"
    - "Section headers have clear breathing room below them (16px margin-bottom)"
    - "Controls inside TtsProviderSelect have comfortable spacing (16px between input groups)"
    - "All existing settings functionality works identically — save, cancel, VAD slider, hotkey recorder, TTS provider switch, Whisper model select"
  artifacts:
    - path: "apps/desktop/src/main/settingsWindow.ts"
      provides: "Electron BrowserWindow with 600px width"
      contains: "width: 600"
    - path: "apps/desktop/src/renderer/src/settings/SettingsForm.tsx"
      provides: "React form with wider spacing classes"
      contains: "space-y-8"
    - path: "apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx"
      provides: "TTS section with updated internal spacing"
      contains: "space-y-4"
  key_links:
    - from: "settingsWindow.ts"
      to: "BrowserWindow config"
      via: "width property"
      pattern: "width: 600"
    - from: "SettingsForm.tsx"
      to: "main flex container"
      via: "space-y-8 class"
      pattern: "space-y-8"
---

<objective>
Polish the Settings window from a cramped 480px Electron default into an intentionally designed layout at 600px.

Purpose: The Settings window currently feels like a default Electron dialog — narrow, sections blur together, controls feel packed. This plan applies the UI design contract from 47-UI-SPEC.md to make it look polished.

Output:
- settingsWindow.ts: window width 480 → 600
- SettingsForm.tsx: spacing classes updated (space-y-6 → space-y-8, mb-3 → mb-4 on all h2s)
- TtsProviderSelect.tsx: internal spacing updated (space-y-3 → space-y-4)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-settings-ui-polish/47-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the Settings BrowserWindow</name>
  <files>apps/desktop/src/main/settingsWindow.ts</files>
  <read_first>
    - apps/desktop/src/main/settingsWindow.ts — read current width/height values before editing
  </read_first>
  <action>
In `apps/desktop/src/main/settingsWindow.ts`, inside `createSettingsWindow()`, change the BrowserWindow config:

  - Line 10: `width: 480` → `width: 600`
  - Line 11: `height: 520` — keep as-is (520px fits all content at current spacing)

No other changes to this file. The `resizable: false`, `frame: true`, `skipTaskbar: true`, and all webPreferences remain untouched.

The result must be:
```typescript
const win = new BrowserWindow({
  width: 600,
  height: 520,
  show: false,
  frame: true,
  resizable: false,
  skipTaskbar: true,
  // webPreferences unchanged
});
```
  </action>
  <verify>
    <automated>cd apps/desktop && npx vitest run src/renderer/src/settings/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/main/settingsWindow.ts contains exactly `width: 600,` (not 480)
    - apps/desktop/src/main/settingsWindow.ts contains `height: 520,` (unchanged)
    - apps/desktop/src/main/settingsWindow.ts does NOT contain `width: 480`
    - File still contains `resizable: false` and `frame: true` (no regressions)
  </acceptance_criteria>
  <done>settingsWindow.ts opens the Settings window at 600px wide instead of 480px</done>
</task>

<task type="auto">
  <name>Task 2: Update section spacing in SettingsForm.tsx</name>
  <files>apps/desktop/src/renderer/src/settings/SettingsForm.tsx</files>
  <read_first>
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx — read current Tailwind class values before editing
  </read_first>
  <action>
In `apps/desktop/src/renderer/src/settings/SettingsForm.tsx`, make exactly these class substitutions:

**Change 1 — main flex container (line 141):**
`<div className="flex-1 space-y-6">` → `<div className="flex-1 space-y-8">`

This increases the gap between each section block (PTT / hr / Always-Listening / hr / TTS / hr / Whisper) from 24px to 32px.

**Change 2 — all four section h2 headers:**
Replace `mb-3` with `mb-4` on every `h2` in the file. There are exactly 4 occurrences:
- Line 144: `<h2 className="text-base font-semibold text-white mb-3">Push-to-Talk</h2>`
- Line 160: `<h2 className="text-base font-semibold text-white mb-3">Always-Listening</h2>`
- Line 215: `<h2 className="text-base font-semibold text-white mb-3">Text-to-Speech</h2>`
- Line 229: `<h2 className="text-base font-semibold text-white mb-3">Speech-to-Text Model</h2>`

All four become: `className="text-base font-semibold text-white mb-4"`

No other changes to this file. Do not touch:
- The outer div's `p-6` (outer padding stays at 24px)
- The `space-y-3` inside the Always-Listening section's `<div>` (that is separate from TtsProviderSelect)
- Button bar classes (`gap-3 mt-6`)
- Any other classes, logic, or structure
  </action>
  <verify>
    <automated>cd apps/desktop && npx vitest run src/renderer/src/settings/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx contains `space-y-8` (not space-y-6) in the flex-1 div
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx does NOT contain `space-y-6` anywhere (was replaced)
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx contains exactly 0 occurrences of `mb-3` on h2 elements (all replaced with mb-4)
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx contains exactly 4 occurrences of `mb-4` (one per section heading)
    - File still contains `p-6` on the outer div (outer padding unchanged)
    - File still contains `gap-3 mt-6` on the button bar (unchanged)
    - Vitest settings tests pass (no regressions in form logic)
  </acceptance_criteria>
  <done>SettingsForm.tsx has 32px gaps between sections and 16px breathing room below each section heading</done>
</task>

<task type="auto">
  <name>Task 3: Update TtsProviderSelect internal spacing</name>
  <files>apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx</files>
  <read_first>
    - apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx — read current space-y class value before editing
  </read_first>
  <action>
In `apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx`, change the root div's spacing class:

**Line 28:** `<div className="space-y-3">` → `<div className="space-y-4">`

This is the outermost div that wraps the Provider select, API Key input, and Voice ID input groups. Changing from 12px to 16px between these three input groups gives them consistent breathing room with the rest of the form.

The three inner `<div className="space-y-1">` divs (label + input pairings) remain unchanged at `space-y-1`.

No other changes to this file.
  </action>
  <verify>
    <automated>cd apps/desktop && npx vitest run src/renderer/src/settings/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx root div contains `space-y-4` (not space-y-3)
    - apps/desktop/src/renderer/src/settings/TtsProviderSelect.tsx does NOT contain `space-y-3` at the root div level
    - The three inner `<div className="space-y-1">` divs are still present and unchanged
    - Vitest settings tests pass (no regressions)
  </acceptance_criteria>
  <done>TtsProviderSelect has 16px gaps between its three input groups (Provider, API Key, Voice ID)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All three layout changes applied:
    1. Settings window widened to 600px (settingsWindow.ts)
    2. Section gaps increased to 32px, heading margins to 16px (SettingsForm.tsx)
    3. TtsProviderSelect internal spacing increased to 16px (TtsProviderSelect.tsx)
  </what-built>
  <how-to-verify>
    1. Run `pnpm --filter @jarvis/desktop dev` to launch the app
    2. Open Settings from the tray icon
    3. Verify the window is noticeably wider than before — sections should not feel cramped
    4. Check that PTT, Always-Listening, TTS (Text-to-Speech), and Whisper (Speech-to-Text) sections are visually distinct with clear gaps between them
    5. Verify each section heading has comfortable space below it before the first control
    6. Check the TTS section — Provider, API Key, and Voice ID inputs should have visible breathing room between them
    7. Confirm all controls still work: hotkey recorder (click + press keys), VAD slider (drag), TTS provider dropdown, Whisper model dropdown, Save/Cancel buttons
    8. Confirm no scrollbar appears and the Save/Cancel button bar is fully visible at the bottom
  </how-to-verify>
  <resume-signal>Type "approved" if layout looks polished, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
Full settings test suite must pass:
- `cd apps/desktop && npx vitest run src/renderer/src/settings/` — green
- Manual: Settings window opens at 600px with improved spacing (human checkpoint above)
- No scrollbar visible at 520px height
- All four sections (PTT, Always-Listening, TTS, Whisper) clearly separated
- All controls functional after layout change
</verification>

<success_criteria>
1. Settings window is 600px wide (was 480px) — confirmed by `width: 600` in settingsWindow.ts
2. Sections separated by 32px gaps — confirmed by `space-y-8` in SettingsForm.tsx flex-1 div
3. All h2 headers have 16px margin-bottom — confirmed by `mb-4` (4 occurrences) in SettingsForm.tsx
4. TtsProviderSelect groups separated by 16px — confirmed by `space-y-4` in TtsProviderSelect.tsx root div
5. Vitest settings tests green — no regressions
6. Human checkpoint approved — visual inspection confirms polished appearance
</success_criteria>

<output>
After completion, create `.planning/phases/47-settings-ui-polish/47-01-SUMMARY.md` with:
- What was changed (exact class substitutions and file locations)
- Files modified
- Test results
- Any deviations from the plan
</output>
