---
phase: 15-multi-llm-factory-langchain-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/package.json
  - apps/backend-ts/src/llm/types.ts
  - apps/backend-ts/src/llm/config.ts
  - apps/backend-ts/src/llm/config.test.ts
autonomous: true
requirements: [LLM-TS-01, LLM-TS-03]
must_haves:
  truths:
    - "TypeScript config validation rejects invalid env vars and exits with clear errors"
    - "LangChain.js packages installed with correct versions (1.x not 0.3.x)"
    - "Zod schema validates all LLM-related environment variables"
  artifacts:
    - path: "apps/backend-ts/src/llm/types.ts"
      provides: "LLMProvider type and LLMConfig interface"
      exports: ["LLMProvider", "LLMConfig"]
    - path: "apps/backend-ts/src/llm/config.ts"
      provides: "Zod-validated config loading"
      exports: ["loadConfig", "envSchema", "LLMConfig"]
    - path: "apps/backend-ts/src/llm/config.test.ts"
      provides: "Config validation test coverage"
      min_lines: 50
  key_links:
    - from: "apps/backend-ts/src/llm/config.ts"
      to: "process.env"
      via: "Zod safeParse validation"
      pattern: "envSchema.safeParse\\(process.env\\)"
---

<objective>
Install LangChain.js dependencies and create config validation layer

Purpose: Establish foundation for multi-LLM factory with type-safe config validation
Output: Zod-validated config loading with proper TypeScript types
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-multi-llm-factory-langchain-integration/15-CONTEXT.md
@.planning/phases/15-multi-llm-factory-langchain-integration/15-RESEARCH.md
</context>

<interfaces>
<!-- Key types from Python that TypeScript must replicate -->
From src/jarvis/config.py:
- Settings class with llm_provider, lm_studio_url, lm_studio_model, openai_api_key, anthropic_api_key, llm_model

Expected TypeScript equivalents:
- LLMProvider type: 'lmstudio' | 'openai' | 'anthropic'
- LLMConfig interface: strongly typed config object from Zod schema
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install LangChain.js packages</name>
  <files>apps/backend-ts/package.json</files>
  <read_first>
    - apps/backend-ts/package.json
    - .planning/phases/15-multi-llm-factory-langchain-integration/15-RESEARCH.md
  </read_first>
  <action>
    Install @langchain/core, @langchain/openai, @langchain/anthropic, and zod packages.

    Run command: cd apps/backend-ts && pnpm add @langchain/core@^1.1.39 @langchain/openai@^1.4.3 @langchain/anthropic@^1.3.26 zod@^4.3.6

    IMPORTANT: Research confirmed LangChain.js is at version 1.x (NOT 0.3.x as originally stated in CONTEXT.md).
    Use version 1.x packages as documented in RESEARCH.md.

    After installation, verify all packages share the same @langchain/core version by checking node_modules.
    The package.json should add these to dependencies section.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm ls @langchain/core @langchain/openai @langchain/anthropic zod --depth=0</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@langchain/core": "^1.1.39"
    - package.json contains "@langchain/openai": "^1.4.3"
    - package.json contains "@langchain/anthropic": "^1.3.26"
    - package.json contains "zod": "^4.3.6"
    - pnpm ls shows all packages installed
  </acceptance_criteria>
  <done>LangChain.js 1.x packages installed with matching core version</done>
</task>

<task type="auto">
  <name>Task 2: Create types and config schema</name>
  <files>apps/backend-ts/src/llm/types.ts, apps/backend-ts/src/llm/config.ts</files>
  <read_first>
    - src/jarvis/config.py
    - .planning/phases/15-multi-llm-factory-langchain-integration/15-CONTEXT.md
    - .planning/phases/15-multi-llm-factory-langchain-integration/15-RESEARCH.md
  </read_first>
  <action>
    Create llm directory and implement type definitions and Zod config validation.

    1. Create apps/backend-ts/src/llm/types.ts with:
       - LLMProvider type as string literal union: 'lmstudio' | 'openai' | 'anthropic' (per D-06)
       - Export type for use in factory

    2. Create apps/backend-ts/src/llm/config.ts with Zod schema (per D-07):
       - envSchema object validating all LLM env vars:
         * LLM_PROVIDER: z.enum(['lmstudio', 'openai', 'anthropic']).default('lmstudio') (per D-28)
         * LLM_MODEL: z.string().optional()
         * LM_STUDIO_URL: z.string().url().default('http://localhost:1234/v1') (with /v1 suffix per research)
         * LM_STUDIO_MODEL: z.string().optional()
         * OPENAI_API_KEY: z.string().optional()
         * ANTHROPIC_API_KEY: z.string().optional()
         * BACKEND_TS_PORT: z.coerce.number().default(8001) (use coerce for number conversion)
       - LLMConfig type inferred from schema: export type LLMConfig = z.infer<typeof envSchema>
       - loadConfig() function:
         * Calls envSchema.safeParse(process.env)
         * If invalid: console.error with clear messages (per D-10) and process.exit(1)
         * Returns validated config object
       - Export envSchema, LLMConfig type, and loadConfig function

    Must replicate Python config fields for parity (per D-01).
    Use exact error message format from Python for UX consistency (per D-17).
  </action>
  <verify>
    <automated>cd apps/backend-ts && npx tsc --noEmit src/llm/types.ts src/llm/config.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/llm/types.ts exports LLMProvider type with three literal values
    - src/llm/config.ts exports envSchema, LLMConfig type, loadConfig function
    - envSchema includes all fields from Python settings
    - loadConfig calls process.exit(1) on validation failure
    - TypeScript compilation succeeds without errors
  </acceptance_criteria>
  <done>Type-safe config layer with Zod validation implemented</done>
</task>

<task type="auto">
  <name>Task 3: Create config tests</name>
  <files>apps/backend-ts/src/llm/config.test.ts</files>
  <read_first>
    - apps/backend-ts/src/llm/config.ts
    - apps/backend-ts/vitest.config.ts
  </read_first>
  <action>
    Create comprehensive tests for config validation using Vitest.

    Test cases to implement:
    1. Valid config: All env vars set correctly → loadConfig returns validated object
    2. Invalid provider: LLM_PROVIDER='invalid' → process.exit called with error message
    3. Invalid URL: LM_STUDIO_URL='not-a-url' → process.exit with Zod validation error
    4. Missing required fields when provider needs them:
       - LLM_PROVIDER='openai' without OPENAI_API_KEY → should NOT fail here (factory validates this)
    5. Default values: Empty env → defaults to lmstudio with localhost:1234/v1
    6. Port coercion: BACKEND_TS_PORT='8001' (string) → converts to number 8001

    Mock process.exit to capture exits in tests using vi.spyOn(process, 'exit').
    Mock console.error to verify error messages.

    Test structure:
    ```typescript
    import { describe, test, expect, vi, beforeEach } from 'vitest';
    import { loadConfig, envSchema } from './config';

    describe('LLM Config', () => {
      beforeEach(() => {
        vi.clearAllMocks();
        // Reset process.env between tests
      });

      test('loads valid config', () => {
        // Set process.env, call loadConfig, verify result
      });

      test('exits on invalid provider', () => {
        const exitSpy = vi.spyOn(process, 'exit').mockImplementation(() => undefined as never);
        // Test invalid provider causes exit(1)
      });
    });
    ```
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm test src/llm/config.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - config.test.ts contains at least 6 test cases
    - Tests validate default values work correctly
    - Tests verify process.exit(1) on invalid config
    - Tests verify error messages are logged
    - All tests pass when run with vitest
  </acceptance_criteria>
  <done>Config validation has comprehensive test coverage</done>
</task>

</tasks>

<verification>
Run all tests and verify TypeScript compilation:
```bash
cd apps/backend-ts
pnpm test --run
npx tsc --noEmit
```
</verification>

<success_criteria>
- LangChain.js 1.x packages installed (not 0.3.x)
- Config validation with Zod implemented and tested
- Type definitions match Python implementation
- Tests validate error handling and defaults
</success_criteria>

<output>
After completion, create `.planning/phases/15-multi-llm-factory-langchain-integration/15-01-SUMMARY.md`
</output>