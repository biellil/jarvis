---
phase: 23-orb-ux-polish
plan: 02
type: execute
wave: 2
depends_on: [23-01]
autonomous: true
requirements: [WAKE-02, WAKE-03, WAKE-04, ORB-POL-02]
files_modified:
  - apps/desktop/src/main/store.ts
  - apps/desktop/src/main/__tests__/store.test.ts
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/main/ipc/settings.ts
  - apps/desktop/src/main/ipc/__tests__/settings.test.ts
  - apps/desktop/src/main/tray.ts
  - apps/desktop/src/main/__tests__/tray.test.ts
  - apps/desktop/src/renderer/hooks/useWakeWord.ts
  - apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts
tags: [wake-word, tray, ipc, store, useWakeWord, integration, pause-resume]

must_haves:
  truths:
    - "store.ts expõe `getWakeWordPaused(): boolean` e `setWakeWordPaused(paused: boolean): void` com chave `wakeWordPaused` e default `false` (D-03 + D-04)"
    - "setWakeWordPaused rejeita non-boolean inputs (defensive check) — mesmo padrão já aplicado em v1.3 hotkey store"
    - "ipc-types.ts define `WAKE_WORD_PAUSE_TOGGLE = 'wakeWord:pause-toggle'` e `WAKE_WORD_GET_PAUSED = 'wakeWord:get-paused'` em IPC_CHANNELS (D-06)"
    - "WakeWordApi interface em ipc-types.ts inclui `getPaused(): Promise<boolean>` e `onPauseToggle(cb): () => void` (D-06)"
    - "preload expõe window.jarvis.wakeWord.getPaused() via ipcRenderer.invoke(WAKE_WORD_GET_PAUSED)"
    - "preload expõe window.jarvis.wakeWord.onPauseToggle(cb) via ipcRenderer.on(WAKE_WORD_PAUSE_TOGGLE, cb) retornando função de unsubscribe"
    - "main/ipc/settings.ts registra handler `ipcMain.handle('wakeWord:get-paused', () => store.getWakeWordPaused())`"
    - "tray.ts tem item novo NO TOPO do menu (antes de Show) com label dinâmico que é 'Pause listening' quando getWakeWordPaused()===false e 'Resume listening' quando true (D-03)"
    - "tray menu click alterna store.setWakeWordPaused(!current), envia webContents.send('wakeWord:pause-toggle', newValue) para TODAS as BrowserWindows, e rebuilda o menu para refletir novo label"
    - "tray.setToolTip reflete estado: 'JARVIS — listening' quando ativo, 'JARVIS — paused' quando pausado (nice-to-have do D-03, ajuda manutenção do teste de estado)"
    - "useWakeWord no boot lê `const initialPaused = await window.jarvis.wakeWord.getPaused()` e chama `setWakeWordPaused(initialPaused)` do OrbContext (substitui o draft atual baseado em `settings.getWakeWordEnabled`)"
    - "useWakeWord registra listener `onPauseToggle((paused) => setWakeWordPaused(paused))` e retorna cleanup no unmount"
    - "Quando wakeWordPaused vira true, useWakeWord chama engine.suspend(); quando vira false E orb state === 'idle', chama engine.resume() (D-06 behavior)"
    - "onDetected em useWakeWord chama triggerWakeBurst() do OrbContext imediatamente, e SÓ chama setState('listening') + startRecording() após 350ms (D-02 precedendo a transição)"
    - "Quando `window.matchMedia('(prefers-reduced-motion: reduce)').matches` é true, useWakeWord BYPASSA o setTimeout de 350ms e chama setState('listening') imediatamente (D-05 bypass)"
    - "O draft pré-existente do `wakeWordEnabled` + canal `wake-word-settings-changed` é REMOVIDO/RENOMEADO para casar com CONTEXT.md (semântica invertida: paused=!enabled)"
    - "Teste de store verifica default=false, roundtrip get/set, rejeição de non-boolean"
    - "Teste de tray verifica que o primeiro item do menu é 'Pause listening' (quando paused=false) ou 'Resume listening' (quando paused=true), mock store.getWakeWordPaused"
    - "Teste de useWakeWord verifica: (a) lê initialPaused no boot, (b) propaga para setWakeWordPaused do mock OrbContext, (c) onPauseToggle listener é registrado, (d) triggerWakeBurst é chamado antes de setState('listening'), (e) reduced-motion bypassa o delay de 350ms"
  artifacts:
    - path: apps/desktop/src/main/store.ts
      provides: "getWakeWordPaused/setWakeWordPaused com chave wakeWordPaused (substitui o draft `wakeWordEnabled`)"
      contains: "getWakeWordPaused"
    - path: apps/desktop/src/shared/ipc-types.ts
      provides: "IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE + WAKE_WORD_GET_PAUSED + WakeWordApi estendida"
      contains: "WAKE_WORD_PAUSE_TOGGLE"
    - path: apps/desktop/src/main/ipc/settings.ts
      provides: "handlers wakeWord:get-paused + wakeWord:pause-toggle broadcast helper"
      contains: "wakeWord:get-paused"
    - path: apps/desktop/src/main/tray.ts
      provides: "Menu item pause/resume no topo com label dinâmico e tooltip atualizado"
      contains: "Pause listening"
    - path: apps/desktop/src/renderer/hooks/useWakeWord.ts
      provides: "Hook consome wakeWord.getPaused + onPauseToggle, propaga para OrbContext, dispara triggerWakeBurst pré-listening com 350ms delay + reduced-motion bypass"
      contains: "triggerWakeBurst"
  key_links:
    - from: "tray.ts onClick handler"
      to: "store.setWakeWordPaused + webContents.send('wakeWord:pause-toggle')"
      via: "toggle do valor atual + broadcast"
      pattern: "setWakeWordPaused\\(\\s*!"
    - from: "preload/index.ts"
      to: "ipc-types.ts IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE/GET_PAUSED"
      via: "ipcRenderer.invoke + ipcRenderer.on"
      pattern: "WAKE_WORD_GET_PAUSED|WAKE_WORD_PAUSE_TOGGLE"
    - from: "useWakeWord.ts onDetected"
      to: "OrbContext.triggerWakeBurst + setState('listening') após 350ms"
      via: "closure com setTimeout + matchMedia check"
      pattern: "triggerWakeBurst|350"
    - from: "useWakeWord.ts boot"
      to: "window.jarvis.wakeWord.getPaused + setWakeWordPaused do OrbContext"
      via: "await getPaused + propagação"
      pattern: "getPaused"
---

<objective>
Wave 2: fechar o loop completo de WAKE-03 (pause/resume via tray com persistência) + acoplar a wake burst animation do Plan 01 no `useWakeWord` hook precedendo a transição para `listening` (D-02). Substituir o draft pré-existente do `wakeWordEnabled` / canal `wake-word-settings-changed` (commits 77c8763/ffa7aeb/efc7133) por superfícies alinhadas com o CONTEXT.md:

- **Semântica invertida:** `wakeWordPaused` (CONTEXT.md) em vez de `wakeWordEnabled` (draft)
- **Canais IPC corretos:** `wakeWord:pause-toggle` + `wakeWord:get-paused` (D-06)
- **Posição do item de menu:** TOPO antes de Show (D-03, o draft colocou no meio)
- **Label dinâmico:** "Pause listening" ↔ "Resume listening" (D-03, o draft usa label fixo "Wake Word: Listening" checkbox)
- **Integração com OrbContext (Plan 01):** consumir `setWakeWordPaused` + `triggerWakeBurst` em vez de manter state local no hook

Purpose: Entregar WAKE-02 (wake burst visível antes do listening), WAKE-03 (tray pause persistido), WAKE-04 (orb idle paused distinguível — visual já entregue no Plan 01, agora ligado ao store), e ORB-POL-02 (burst polish 200-500ms — 350ms no range). Após este plan, o usuário pode: (1) clicar "Pause listening" no tray e ver o orb ficar mais tênue + reabrir o app e o estado persistir; (2) falar "Hey JARVIS" e ver o burst amber 350ms antes do orb virar laranja.

Output:
- Store realinhado para `wakeWordPaused` (renomeação semântica)
- 2 canais IPC novos + WakeWordApi estendida
- Tray com item pause/resume no topo, label dinâmico
- useWakeWord consumindo OrbContext.setWakeWordPaused/triggerWakeBurst + IPC pause-toggle listener
- Plan 01 draft `wake-word-settings-changed` removido/renomeado (zero código morto)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-orb-ux-polish/23-CONTEXT.md
@.planning/phases/23-orb-ux-polish/23-01-PLAN.md
@.planning/phases/22-voiceinputmanager-refactor-wake-word-core/22-04-SUMMARY.md

@apps/desktop/src/main/store.ts
@apps/desktop/src/main/tray.ts
@apps/desktop/src/main/ipc/settings.ts
@apps/desktop/src/main/ipc/index.ts
@apps/desktop/src/shared/ipc-types.ts
@apps/desktop/src/preload/index.ts
@apps/desktop/src/renderer/hooks/useWakeWord.ts
@apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts

<interfaces>
<!-- Contratos atuais (consumidos) — Plan 01 entregou os novos campos do OrbContext -->

From apps/desktop/src/renderer/components/Orb/OrbContext.tsx (após Plan 01):
```ts
interface OrbContextValue {
  state: OrbState;
  setState: (s: OrbState) => void;
  wakeWordPaused: boolean;
  setWakeWordPaused: (paused: boolean) => void;
  burstActive: boolean;
  triggerWakeBurst: () => void;
}
```

From apps/desktop/src/main/store.ts (atual — tem draft inverso):
```ts
// REMOVER estas APIs (draft semântica invertida):
//   getWakeWordEnabled(): boolean
//   setWakeWordEnabled(enabled: boolean): void
//   chave: 'wakeWordEnabled'
// SUBSTITUIR por:
export function getWakeWordPaused(): boolean;          // default false (D-04)
export function setWakeWordPaused(paused: boolean): void; // defensive boolean check
// chave: 'wakeWordPaused'
```

From apps/desktop/src/shared/ipc-types.ts (alvo):
```ts
export const IPC_CHANNELS = {
  // ...existentes...
  WAKE_WORD_LOAD_MODELS: 'wakeWord:load-models',
  WAKE_WORD_GET_PAUSED: 'wakeWord:get-paused',      // NEW — D-06
  WAKE_WORD_PAUSE_TOGGLE: 'wakeWord:pause-toggle',  // NEW — D-06
  // REMOVER: GET_WAKE_WORD_ENABLED, SET_WAKE_WORD_ENABLED, WAKE_WORD_SETTINGS_CHANGED
} as const;

export interface WakeWordApi {
  loadModels: () => Promise<WakeWordModelBytes>;
  getPaused: () => Promise<boolean>;                              // NEW — D-06
  onPauseToggle: (cb: (paused: boolean) => void) => () => void;   // NEW — D-06 (returns unsubscribe)
}

// REMOVER: SettingsApi inteira (era parte do draft)
```

From apps/desktop/src/main/tray.ts (relevant section — o item a adicionar no TOPO):
```ts
// ANTES (draft atual, após o primeiro separator, como checkbox com label fixo):
{
  label: 'Wake Word: Listening',
  type: 'checkbox',
  checked: wakeWordEnabled,
  click: ... // usa setWakeWordEnabled + wake-word-settings-changed
}
// REMOVER e substituir por (D-03 literal):
{
  label: paused ? 'Resume listening' : 'Pause listening',
  click: () => {
    const next = !paused;
    setWakeWordPaused(next);
    BrowserWindow.getAllWindows().forEach((w) =>
      w.webContents.send(IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE, next)
    );
    tray?.setToolTip(next ? 'JARVIS — paused' : 'JARVIS — listening');
    const rebuilt = buildContextMenu(mainWindow);
    tray?.setContextMenu(rebuilt);
  },
},
{ type: 'separator' },
// ...continua com Show, Hide, etc
```

From apps/desktop/src/renderer/hooks/useWakeWord.ts (areas a mudar):

1. Remover o state local `wakeWordEnabled` e consumir do OrbContext.
2. Substituir `window.jarvis.settings.getWakeWordEnabled` por `window.jarvis.wakeWord.getPaused`.
3. Substituir o listener `ipcRenderer.on('wake-word-settings-changed', ...)` por `window.jarvis.wakeWord.onPauseToggle(...)`.
4. No onDetected, ANTES de setState('listening'), chamar `triggerWakeBurst()` do OrbContext e armar `setTimeout(() => { setState('listening'); void audioRecorder.startRecording(); }, 350)` — EXCETO se `window.matchMedia('(prefers-reduced-motion: reduce)').matches === true`, caso em que pula direto para setState('listening') síncrono (D-05 bypass).
5. Inverter semântica: o hook gate existente `if (!wakeWordEnabled)` vira `if (wakeWordPaused)` (e propagar via `useOrbContext`).

Sequência sincrona vs delayed no onDetected:
```ts
onDetected: (score) => {
  if (stateRef.current !== 'idle') return;
  if (wakeWordPausedRef.current === true) return; // additional gate
  const grant = voiceInputManager.acquire('wakeword');
  if ('error' in grant) return;

  triggerWakeBurst(); // D-02 — visual feedback imediato

  const reducedMotion = typeof window !== 'undefined'
    && window.matchMedia?.('(prefers-reduced-motion: reduce)').matches === true;

  const proceed = () => {
    wakeTriggeredListeningRef.current = true;
    setState('listening');
    void audioRecorder.startRecording();
    // ...arma VAD timeout como antes...
  };

  if (reducedMotion) {
    proceed();
  } else {
    setTimeout(proceed, 350); // D-02 — burst precedes transition
  }
},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Realinhar store + IPC types + preload para `wakeWordPaused` (D-03, D-04, D-06)</name>
  <files>
    apps/desktop/src/main/store.ts,
    apps/desktop/src/main/__tests__/store.test.ts,
    apps/desktop/src/shared/ipc-types.ts,
    apps/desktop/src/preload/index.ts,
    apps/desktop/src/main/ipc/settings.ts,
    apps/desktop/src/main/ipc/__tests__/settings.test.ts
  </files>
  <behavior>
    - Test (store): getWakeWordPaused() retorna false no primeiro acesso (D-04 default)
    - Test (store): setWakeWordPaused(true) → getWakeWordPaused() retorna true
    - Test (store): setWakeWordPaused(false) → volta para false
    - Test (store): setWakeWordPaused('yes' as any) é silenciosamente rejeitado (console.error + valor não muda)
    - Test (store): key usada é 'wakeWordPaused' (mock do electron-store deve receber `store.set('wakeWordPaused', ...)`)
    - Test (ipc settings): handler 'wakeWord:get-paused' retorna o valor do store
    - Test (ipc settings): helper broadcastPauseToggle(paused) chama webContents.send em cada BrowserWindow com canal 'wakeWord:pause-toggle'
    - Test (ipc-types): IPC_CHANNELS contém WAKE_WORD_GET_PAUSED e WAKE_WORD_PAUSE_TOGGLE (regex em source)
    - Test (ipc-types): IPC_CHANNELS NÃO contém mais GET_WAKE_WORD_ENABLED/SET_WAKE_WORD_ENABLED/WAKE_WORD_SETTINGS_CHANGED (regex assert absent — evita drift)
  </behavior>
  <action>
    **1. store.ts** — reescrever a seção wakeWord:
    - Remover `getWakeWordEnabled` e `setWakeWordEnabled`, `DEFAULT_WAKE_WORD_ENABLED`, campo `wakeWordEnabled?: boolean` do `StoreSchema`.
    - Adicionar campo `wakeWordPaused?: boolean` em `StoreSchema`.
    - Adicionar `DEFAULT_WAKE_WORD_PAUSED = false` (D-04 literal).
    - Exportar `getWakeWordPaused(): boolean` — lê `store.get('wakeWordPaused')` e retorna o valor OU `DEFAULT_WAKE_WORD_PAUSED` se undefined.
    - Exportar `setWakeWordPaused(paused: boolean): void` — faz defensive check `typeof paused !== 'boolean'` → `console.error` e retorna (padrão já validado no T-23-01-01). Senão `store.set('wakeWordPaused', paused)`.

    **2. store.test.ts** — criar/estender para cobrir os 5 cenários do behavior. Mockar electron-store via `vi.mock('electron-store', ...)` como vem sendo feito em outros testes do repo. Reusar padrão `vi.hoisted` se necessário.

    **3. ipc-types.ts** — atualizar IPC_CHANNELS:
    - REMOVER: `GET_WAKE_WORD_ENABLED`, `SET_WAKE_WORD_ENABLED`, `WAKE_WORD_SETTINGS_CHANGED`
    - ADICIONAR: `WAKE_WORD_GET_PAUSED: 'wakeWord:get-paused'`, `WAKE_WORD_PAUSE_TOGGLE: 'wakeWord:pause-toggle'`
    - Estender `WakeWordApi`: adicionar `getPaused: () => Promise<boolean>` e `onPauseToggle: (cb: (paused: boolean) => void) => () => void`
    - REMOVER a interface `SettingsApi` e a propriedade `settings: SettingsApi` de `JarvisAPI` (substituída pela extensão da WakeWordApi)
    - Manter `ipcRenderer?` helper existente (usado em outros lugares pro PTT).

    **4. preload/index.ts** — atualizar:
    - Remover bloco `settings: { getWakeWordEnabled, setWakeWordEnabled }` do api object.
    - Estender `wakeWord`:
      ```ts
      wakeWord: {
        loadModels: () => ipcRenderer.invoke(IPC_CHANNELS.WAKE_WORD_LOAD_MODELS),
        getPaused: () => ipcRenderer.invoke(IPC_CHANNELS.WAKE_WORD_GET_PAUSED),
        onPauseToggle: (cb: (paused: boolean) => void) => {
          const handler = (_event: unknown, paused: boolean) => cb(paused);
          ipcRenderer.on(IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE, handler);
          return () => ipcRenderer.removeListener(IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE, handler);
        },
      },
      ```
    - Atualizar imports removendo `SettingsApi` type se tiver.

    **5. ipc/settings.ts** — reescrever:
    ```ts
    import { ipcMain, BrowserWindow, type WebContents } from 'electron';
    import { IPC_CHANNELS } from '../../shared/ipc-types';
    import { getWakeWordPaused } from '../store';

    export function setupSettingsHandlers(): void {
      ipcMain.handle(IPC_CHANNELS.WAKE_WORD_GET_PAUSED, () => getWakeWordPaused());
    }

    /**
     * Broadcast pause toggle a todos renderers. Chamado pelo tray após
     * clicar o item pause/resume.
     */
    export function broadcastPauseToggle(paused: boolean): void {
      BrowserWindow.getAllWindows().forEach((win) => {
        win.webContents.send(IPC_CHANNELS.WAKE_WORD_PAUSE_TOGGLE, paused);
      });
    }
    ```
    Rename opcional do arquivo para `ipc/wakeWordState.ts` NÃO é necessário — manter nome `settings.ts` por enquanto, apenas reescrever internals.

    **6. ipc/__tests__/settings.test.ts** — criar se não existir. Testar:
    - Handler `wakeWord:get-paused` é registrado e retorna o valor do store (mock).
    - `broadcastPauseToggle(true)` chama `webContents.send` em cada janela com canal correto.
    Mockar `electron` via `vi.mock('electron', ...)` reusando padrão dos testes do main process no repo.

    Commit: `♻️ refactor(23-02): realinha store e IPC de wake word pause (D-03, D-04, D-06)`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/main/__tests__/store.test.ts src/main/ipc/__tests__/settings.test.ts && pnpm --filter @jarvis/desktop build</automated>
  </verify>
  <done>
    store.ts expõe getWakeWordPaused/setWakeWordPaused, chave 'wakeWordPaused', default false; IPC_CHANNELS tem WAKE_WORD_GET_PAUSED + WAKE_WORD_PAUSE_TOGGLE (e os 3 draft antigos sumiram); preload expõe wakeWord.getPaused + wakeWord.onPauseToggle; ipc/settings.ts registra handler novo + exporta broadcastPauseToggle; build TypeScript passa sem erros; testes de store e settings passam.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tray menu com item Pause/Resume no topo + label dinâmico (D-03)</name>
  <files>
    apps/desktop/src/main/tray.ts,
    apps/desktop/src/main/__tests__/tray.test.ts
  </files>
  <behavior>
    - Test: primeiro item do menu (index 0) tem label 'Pause listening' quando getWakeWordPaused() retorna false
    - Test: primeiro item tem label 'Resume listening' quando getWakeWordPaused() retorna true
    - Test: segundo item (index 1) é um separator
    - Test: terceiro item é 'Show' (garante que nada quebrou na ordem)
    - Test: click no primeiro item chama setWakeWordPaused com !currentValue
    - Test: click chama broadcastPauseToggle(newValue)
    - Test: click chama tray.setToolTip com 'JARVIS — paused' ou 'JARVIS — listening' conforme novo valor
    - Test: click rebuilda o menu (setContextMenu chamado novamente)
    - NOTA: os testes existentes de tray.test.ts (`has exactly 3 menu items`) são pre-existing deferred desde v1.2 — remover essas expectativas obsoletas. O menu tem ~10 itens hoje e era um artefato velho.
  </behavior>
  <action>
    **1. tray.ts** — edições cirúrgicas:

    - Remover imports `getWakeWordEnabled, setWakeWordEnabled` → adicionar `getWakeWordPaused, setWakeWordPaused`.
    - Adicionar import `{ broadcastPauseToggle }` de `./ipc/settings`.
    - Em `buildContextMenu`, substituir o bloco existente do "Wake Word: Listening" checkbox:

      ```ts
      function buildContextMenu(mainWindow: BrowserWindow): Menu {
        const currentAccelerator = getWidgetHotkey();
        const currentPttAccelerator = getPttHotkey();
        const paused = getWakeWordPaused();

        // Update tooltip para refletir estado atual
        tray?.setToolTip(paused ? 'JARVIS — paused' : 'JARVIS — listening');

        return Menu.buildFromTemplate([
          // D-03: item no TOPO, label dinâmico, sem PNG icon variante
          {
            label: paused ? 'Resume listening' : 'Pause listening',
            click: () => {
              const next = !paused;
              setWakeWordPaused(next);
              broadcastPauseToggle(next);
              // Rebuild para refletir novo label
              const rebuilt = buildContextMenu(mainWindow);
              tray?.setContextMenu(rebuilt);
            },
          },
          { type: 'separator' },
          { label: 'Show', click: () => mainWindow.show() },
          { label: 'Hide', click: () => mainWindow.hide() },
          // ... resto do menu existente (Configure Hotkey submenu, Configure PTT submenu, separator, Open DevTools, separator, Quit)
        ]);
      }
      ```

      Remover o bloco do checkbox wake word antigo e o separator redundante que ele criou. Ordem final do menu:
      1. Pause listening / Resume listening  ← NEW no topo
      2. (separator)
      3. Show
      4. Hide
      5. Configure Hotkey ►
      6. Configure PTT ►
      7. (separator)
      8. Open DevTools
      9. (separator)
      10. Quit

    **2. tray.test.ts** — reescrever:
    - Remover teste "has exactly 3 menu items" (pre-existing deferred — obsoleto desde v1.2).
    - Adicionar cenários do behavior. Usar mock de electron `Menu.buildFromTemplate` capturando o template array, e de store `getWakeWordPaused`/`setWakeWordPaused` + `broadcastPauseToggle` via `vi.mock`. Reusar padrão `vi.hoisted` da Phase 22 (D-07) para instanciar mocks antes dos imports.
    - Manter os testes existentes que continuam válidos (Show calls show, Hide calls hide, tooltip 'JARVIS', icon path, exports, main process integration).

    Commit: `✨ feat(23-02): tray menu pause/resume listening no topo (WAKE-03, D-03)`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/main/__tests__/tray.test.ts</automated>
  </verify>
  <done>
    tray.ts tem item "Pause listening"/"Resume listening" como primeiro elemento do menu; click executa toggle + broadcast + rebuild + tooltip update; tray.test.ts tem ≥8 cenários passando, nenhum esperando 3 itens; build TypeScript verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: useWakeWord consome OrbContext pause + triggerWakeBurst + IPC pause-toggle (D-02, D-05, D-06)</name>
  <files>
    apps/desktop/src/renderer/hooks/useWakeWord.ts,
    apps/desktop/src/renderer/hooks/__tests__/useWakeWord.test.ts
  </files>
  <behavior>
    - Test: no boot, hook chama `window.jarvis.wakeWord.getPaused()` (mock)
    - Test: valor retornado é propagado via `setWakeWordPaused(initial)` do mock OrbContext
    - Test: hook registra `window.jarvis.wakeWord.onPauseToggle(cb)` listener
    - Test: quando o listener recebe true, chama `setWakeWordPaused(true)` do OrbContext e `engine.suspend()`
    - Test: quando o listener recebe false e state==='idle', chama `setWakeWordPaused(false)` + `engine.resume()`
    - Test: quando o listener recebe false e state!=='idle', chama `setWakeWordPaused(false)` mas NÃO chama `engine.resume()` (gate de state mantém suspended)
    - Test: onDetected chama `triggerWakeBurst()` do mock OrbContext ANTES de qualquer setState
    - Test: onDetected com reduced-motion=false → usa setTimeout(350) antes de setState('listening') e startRecording (fake timers + advanceTimersByTime(349) → NÃO transicionou, advanceTimersByTime(1) → transicionou)
    - Test: onDetected com reduced-motion=true (mock matchMedia) → chama setState('listening') síncrono sem setTimeout (D-05 bypass)
    - Test: onDetected ignorado quando `wakeWordPausedRef.current === true` (o gate novo não dispara engine suspend/resume race)
    - Test: unmount chama o unsubscribe retornado por onPauseToggle
    - Test: cleanup do Plan 01 burst timer fica no OrbContext — o hook só chama triggerWakeBurst() (não gerencia timeout)
  </behavior>
  <action>
    **1. useWakeWord.ts** — remover o state local `wakeWordEnabled` e migrar para consumir OrbContext:

    - Extrair do `useOrbContext()`:
      ```ts
      const { state, setState, wakeWordPaused, setWakeWordPaused, triggerWakeBurst } = useOrbContext();
      ```
    - Adicionar ref espelho para closures: `const wakeWordPausedRef = useRef(wakeWordPaused); wakeWordPausedRef.current = wakeWordPaused;` (mesmo padrão do `stateRef` existente).
    - REMOVER: `useState(true) setWakeWordEnabled`, todos os blocos baseados em `wakeWordEnabled`, o `ipcRenderer.on('wake-word-settings-changed', ...)` useEffect.

    - No boot useEffect (primeiro useEffect existente):
      - Substituir `window.jarvis.settings.getWakeWordEnabled()` por `window.jarvis.wakeWord.getPaused()`.
      - Propagar via `setWakeWordPaused(initialPaused)` (OrbContext setter, não mais useState local).
      - Remover o bloco `if (readEnv('VITE_WAKE_WORD_ENABLED', 'true') === 'false')` e SUBSTITUIR por: manter o bloco, mas adicionalmente no boot setar `setWakeWordPaused(true)` se VITE_WAKE_WORD_ENABLED=false (integra a flag de env no paused semantic).
      - Ao finalizar boot com sucesso: se `initialPaused === true`, chamar `engine.suspend()` ao invés do antigo `if (!initialEnabled)` check.

    - onDetected (dentro do boot, callback passado para WakeWordEngine):
      ```ts
      onDetected: (score: number) => {
        if (stateRef.current !== 'idle') {
          console.log('[wakeWord] ignored — orb state:', stateRef.current);
          return;
        }
        if (wakeWordPausedRef.current) {
          console.log('[wakeWord] ignored — paused via tray');
          return;
        }
        const grant = voiceInputManager.acquire('wakeword');
        if ('error' in grant) {
          console.log('[wakeWord] ignored — voiceInputManager:', grant.error);
          return;
        }
        console.log('[wakeWord] detected score=', score);

        // D-02: burst animation precedes the state transition
        triggerWakeBurst();

        const reducedMotion =
          typeof window !== 'undefined' &&
          typeof window.matchMedia === 'function' &&
          window.matchMedia('(prefers-reduced-motion: reduce)').matches === true;

        const proceed = () => {
          wakeTriggeredListeningRef.current = true;
          setState('listening');
          void audioRecorder.startRecording();

          // VAD timeout (existing code unchanged from Phase 22)
          if (vadTimeoutRef.current) clearTimeout(vadTimeoutRef.current);
          vadTimeoutRef.current = setTimeout(() => { /* existing VAD logic */ }, vadTimeoutMs);
        };

        if (reducedMotion) {
          // D-05 bypass: usuário com prefers-reduced-motion não veria o burst,
          // então pular o delay de 350ms para evitar latência percebida.
          proceed();
        } else {
          setTimeout(proceed, 350);
        }
      },
      ```

    - Segundo useEffect existente (listener): reescrever para usar `onPauseToggle`:
      ```ts
      useEffect(() => {
        const unsub = window.jarvis.wakeWord.onPauseToggle((paused: boolean) => {
          console.log('[useWakeWord] pause toggle:', paused);
          setWakeWordPaused(paused);
          const engine = engineRef.current;
          if (!engine) return;
          if (paused) {
            void engine.suspend();
          } else if (stateRef.current === 'idle') {
            void engine.resume();
          }
          // if paused=false but state!=='idle', don't resume — gate do useEffect [state] cuida quando voltar pra idle
        });
        return () => unsub();
      }, [setWakeWordPaused]);
      ```

    - Terceiro useEffect (state gate): atualizar a condição `if (!wakeWordEnabled)` para `if (wakeWordPausedRef.current)` — ou melhor, depender de `wakeWordPaused` diretamente no useEffect deps:
      ```ts
      useEffect(() => {
        const engine = engineRef.current;
        if (!engine) return;
        if (wakeWordPaused) { void engine.suspend(); return; }
        if (state === 'idle') { void engine.resume(); wakeTriggeredListeningRef.current = false; }
        else { void engine.suspend(); /* existing VAD timeout preservation logic */ }
      }, [state, wakeWordPaused]);
      ```

    - Quarto useEffect (ttsHooks): atualizar `afterPlay` para checar `!wakeWordPausedRef.current` em vez de `wakeWordEnabled`:
      ```ts
      afterPlay: async () => {
        if (!wakeWordPausedRef.current && stateRef.current === 'idle') {
          await engineRef.current?.resume();
        }
      },
      ```
      E remover o `[wakeWordEnabled]` dep do useEffect — agora o ref cobre o closure sem precisar re-registrar.

    **2. useWakeWord.test.ts** — estender os 10 cenários existentes:
    - Atualizar o mock do `window.jarvis` para expor `wakeWord.getPaused` + `wakeWord.onPauseToggle` (retornando um unsubscribe mock). Remover `settings.getWakeWordEnabled` do mock.
    - Atualizar mock de `useOrbContext` para retornar `wakeWordPaused`, `setWakeWordPaused`, `triggerWakeBurst` (tudo vi.fn).
    - Adicionar cenários novos per behavior list. Usar `vi.useFakeTimers()` para o teste do delay de 350ms. Mockar `window.matchMedia` via `Object.defineProperty(window, 'matchMedia', { writable: true, value: vi.fn().mockReturnValue({ matches: false }) })` — flipando matches:true para o teste do bypass.
    - Manter os 10 testes existentes funcionando (ajustando mocks para a nova interface do hook).

    Commit: `♻️ refactor(23-02): useWakeWord consome OrbContext.wakeWordPaused + triggerWakeBurst pré-listening (WAKE-02, D-02, D-05, D-06)`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/hooks/__tests__/useWakeWord.test.ts</automated>
  </verify>
  <done>
    useWakeWord.ts consome OrbContext wakeWordPaused + triggerWakeBurst; boot usa wakeWord.getPaused; onPauseToggle listener registrado; onDetected chama triggerWakeBurst antes do setState e respeita reduced-motion bypass; testes 10 existentes + ≥6 novos passam; build TypeScript verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tray menu click → main process | Menu click handlers executam store writes (confiáveis, local) |
| main → renderer IPC (`wakeWord:pause-toggle`) | Evento enviado por webContents.send com payload boolean — renderer apenas consome como UI state |
| renderer → main IPC (`wakeWord:get-paused`) | Invoke retorna valor do store, sem side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | store setter | mitigate | `typeof paused !== 'boolean'` check rejeita inputs inválidos (mesmo padrão T-23-01-01 já validado) |
| T-23-02-02 | Information Disclosure | IPC payload | accept | Payload é boolean (pausa/não pausa) — zero PII, zero credenciais, zero dados sensíveis. Baixo valor como alvo. |
| T-23-02-03 | Denial of Service | onPauseToggle listener spam | accept | main process só envia pause-toggle quando usuário clica no tray — rate-limited por UX (clicks humanos). Sem loop ou recursão. |
| T-23-02-04 | Spoofing | preload exposição indevida | accept | ipcRenderer permanece NÃO exposto; apenas funções tipadas via contextBridge (mesmo padrão do repo). |
| T-23-02-05 | Elevation of Privilege | handler ganha acesso a fs/node | accept | `ipcMain.handle('wakeWord:get-paused')` apenas lê do electron-store — nenhum acesso a fs/net arbitrário. |
</threat_model>

<verification>
Após todas as tasks:
- `pnpm --filter @jarvis/desktop test --run` → full suite passing (expect ~260+ testes); os 3 pre-existing deferred (tray 3-items, 2 Orb obsoletos) devem ter sido corrigidos pelos Plans 01 e 02.
- `pnpm --filter @jarvis/desktop lint` → 0 errors
- `pnpm --filter @jarvis/desktop build` → 0 TypeScript errors, main/preload/renderer bundles ok
- `grep -rn "wakeWordEnabled\|wake-word-settings-changed\|GET_WAKE_WORD_ENABLED\|SET_WAKE_WORD_ENABLED" apps/desktop/src` → ZERO matches (draft semântico antigo totalmente removido)
- `grep -rn "wakeWordPaused\|WAKE_WORD_PAUSE_TOGGLE\|WAKE_WORD_GET_PAUSED" apps/desktop/src` → ≥8 matches (store, ipc-types, preload, ipc/settings, tray, useWakeWord, tests)
- `grep -n "triggerWakeBurst" apps/desktop/src/renderer/hooks/useWakeWord.ts` → 1 match dentro do onDetected
</verification>

<success_criteria>
- Primeiro item do tray menu alterna label "Pause listening" ↔ "Resume listening" conforme `getWakeWordPaused()` (D-03, WAKE-03)
- Click no item persiste via electron-store chave `wakeWordPaused` (default false, D-04)
- Click broadcasta `wakeWord:pause-toggle` para todos renderers com novo valor (D-06)
- useWakeWord no boot lê estado persistido via `window.jarvis.wakeWord.getPaused()` (D-06)
- useWakeWord ouve `onPauseToggle` e propaga para OrbContext.setWakeWordPaused + engine.suspend/resume (D-06)
- onDetected dispara `triggerWakeBurst()` ANTES de setState('listening'), com delay de 350ms entre os dois (D-02 + ORB-POL-02)
- Reduced-motion bypass: onDetected pula o setTimeout(350) quando matchMedia('prefers-reduced-motion: reduce').matches === true (D-05)
- Orb com state='idle' + wakeWordPaused=true renderiza visual sutil paused (flag já exposto pelo OrbContext entregue no Plan 01) — WAKE-04 end-to-end funcional
- Zero código morto: draft `wakeWordEnabled` / `wake-word-settings-changed` / `SettingsApi` totalmente removidos
- Commits conventional + emoji em pt-BR
- Threat model cobre os 5 STRIDE vectors identificados
</success_criteria>

<output>
Após completion, criar `.planning/phases/23-orb-ux-polish/23-02-SUMMARY.md`:
- Files created/modified (10)
- Tests added count (espera-se ≥15 novos entre store, settings, tray, useWakeWord)
- Commits (3 esperados: refactor store/ipc, feat tray, refactor useWakeWord)
- Quais decisões do CONTEXT.md foram implementadas (D-03, D-04 parcial, D-06, + fecha D-02 e D-05 acoplando aos objetos do Plan 01)
- Cobertura completa de WAKE-02/03/04 + ORB-POL-02
- Código draft removido (audit grep result)
- Deviations (se houver)
</output>
