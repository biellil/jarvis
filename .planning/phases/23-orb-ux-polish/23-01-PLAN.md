---
phase: 23-orb-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [WAKE-02, WAKE-04, ORB-POL-01, ORB-POL-02]
files_modified:
  - apps/desktop/src/renderer/components/Orb/OrbContext.tsx
  - apps/desktop/src/renderer/components/Orb/__tests__/OrbContext.test.tsx
  - apps/desktop/src/renderer/components/Orb/Orb.tsx
  - apps/desktop/src/renderer/components/Orb/__tests__/Orb.test.tsx
  - apps/desktop/src/renderer/src/styles/globals.css
  - apps/desktop/tailwind.config.ts
tags: [orb, css, wake-burst, reduced-motion, orbContext, visual]

must_haves:
  truths:
    - "OrbContext expõe `wakeWordPaused: boolean` + `setWakeWordPaused(paused)` além de state/setState (D-01)"
    - "Valor default de `wakeWordPaused` no provider é `false` — primeira render é wake word ativo (D-04)"
    - "Quando `state==='idle' && wakeWordPaused===true`, o root div do Orb aplica opacity 0.6, drop-shadow raio 12px / alpha 0.28, e border rgba(180,180,180,0.22) (D-01 + WAKE-04)"
    - "Quando `state !== 'idle'` OU `wakeWordPaused===false`, o orb renderiza sem modificação visual (wakeWordPaused é ignorado fora de idle)"
    - "OrbContext expõe `burstActive: boolean` + `triggerWakeBurst()` que seta true por 350ms e volta para false via timeout (D-02)"
    - "Quando `burstActive===true`, o root div do Orb aplica a classe `animate-wake-burst` (one-shot) e renderiza um overlay com amber ring (#F59E0B, 2px) controlado pela animação"
    - "keyframes `wake-burst`: 0% scale(1.0)+ring opacity 0, 45% scale(1.1)+ring opacity 1.0, 100% scale(1.0)+ring opacity 0 — duration 350ms ease-out (D-02 spec literal)"
    - "CSS global inclui `@media (prefers-reduced-motion: reduce)` que zera `animation` das classes animate-pulse-idle, animate-pulse-listen, animate-spin-process, animate-ripple e animate-wake-burst (D-05 + ORB-POL-01)"
    - "O bloco reduced-motion PRESERVA os `transition: ...` 0.4s existentes (D-05 — transitions não são problemáticas)"
    - "Teste unitário verifica que useOrbContext() fora do provider ainda faz throw (regressão do v1.2)"
    - "Teste unitário verifica que orb paused aplica opacity 0.6 (regex no style string) quando state==='idle' + wakeWordPaused=true"
    - "Teste unitário verifica que orb paused NÃO aplica opacity 0.6 quando state==='listening' mesmo com wakeWordPaused=true"
    - "Teste unitário verifica que triggerWakeBurst seta burstActive=true, e após 350ms volta para false (fake timers)"
    - "Teste unitário verifica que o bloco @media (prefers-reduced-motion: reduce) existe em globals.css e lista as 5 classes de animation (grep multi-line)"
  artifacts:
    - path: apps/desktop/src/renderer/components/Orb/OrbContext.tsx
      provides: "OrbContext estendido com wakeWordPaused + setWakeWordPaused + burstActive + triggerWakeBurst (D-01 + D-02)"
      exports: [OrbProvider, useOrbContext, OrbState]
    - path: apps/desktop/src/renderer/components/Orb/Orb.tsx
      provides: "Orb renderiza visual paused (opacity/glow/border) + classe animate-wake-burst + amber ring overlay quando burst ativo"
      contains: "wakeWordPaused"
    - path: apps/desktop/src/renderer/src/styles/globals.css
      provides: "@media (prefers-reduced-motion: reduce) zerando keyframes loopadas + wake-burst"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: apps/desktop/tailwind.config.ts
      provides: "Nova keyframe wake-burst e animação animate-wake-burst 350ms ease-out"
      contains: "wake-burst"
  key_links:
    - from: "OrbContext.tsx"
      to: "Orb.tsx"
      via: "useOrbContext() retorna wakeWordPaused + burstActive"
      pattern: "wakeWordPaused|burstActive"
    - from: "Orb.tsx"
      to: "tailwind animate-wake-burst"
      via: "className condicional quando burstActive===true"
      pattern: "animate-wake-burst"
    - from: "globals.css"
      to: "tailwind-generated animate-* classes"
      via: "@media query overridando animation: none"
      pattern: "prefers-reduced-motion"
---

<objective>
Wave 1: Estender `OrbContext` com flag `wakeWordPaused` (D-01) e controle de burst animation `burstActive` (D-02). Atualizar `Orb.tsx` para renderizar a distinção visual idle+paused (opacity/glow/border sutis — WAKE-04) e a wake burst one-shot (scale 1.0→1.1→1.0 + amber ring — WAKE-02 + ORB-POL-02). Adicionar keyframe `wake-burst` no Tailwind config e bloco global `@media (prefers-reduced-motion: reduce)` que zera as 5 animações loopadas/one-shot do orb mas preserva transitions (D-05 + ORB-POL-01). Cobertura por testes usando happy-dom + testing-library (padrão D-07).

Purpose: Entregar 100% do visual/estado do orb em renderer-puro, sem depender de IPC ou main process. Plan 02 consome essa superfície via `setWakeWordPaused()` e `triggerWakeBurst()` quando o tray toggla ou o wake word dispara.

Output:
- OrbContext com 4 novos campos: `wakeWordPaused`, `setWakeWordPaused`, `burstActive`, `triggerWakeBurst`
- Orb.tsx renderiza visual paused + classe wake-burst + amber ring overlay
- Keyframe `wake-burst` 350ms no Tailwind
- Bloco reduced-motion global em `globals.css`
- Testes unitários cobrindo todas as novas superfícies
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/23-orb-ux-polish/23-CONTEXT.md

@apps/desktop/src/renderer/components/Orb/OrbContext.tsx
@apps/desktop/src/renderer/components/Orb/Orb.tsx
@apps/desktop/src/renderer/components/Orb/__tests__/Orb.test.tsx
@apps/desktop/src/renderer/components/Orb/__tests__/OrbContext.test.tsx
@apps/desktop/tailwind.config.ts
@apps/desktop/src/renderer/src/styles/globals.css

<interfaces>
<!-- Contratos atuais do OrbContext (serão estendidos por este plan) -->

From apps/desktop/src/renderer/components/Orb/OrbContext.tsx:
```ts
export type OrbState = 'idle' | 'listening' | 'processing' | 'responding';

interface OrbContextValue {
  state: OrbState;
  setState: (newState: OrbState) => void;
}

export function OrbProvider({ children }: { children: ReactNode }): JSX.Element;
export function useOrbContext(): OrbContextValue; // throws if outside provider
```

Shape alvo (após este plan):
```ts
interface OrbContextValue {
  state: OrbState;
  setState: (newState: OrbState) => void;
  wakeWordPaused: boolean;                                // NEW — D-01
  setWakeWordPaused: (paused: boolean) => void;           // NEW — D-01
  burstActive: boolean;                                   // NEW — D-02
  triggerWakeBurst: () => void;                           // NEW — D-02 (auto-clears after 350ms)
}
```

From apps/desktop/tailwind.config.ts (trecho relevante):
```ts
animation: {
  'pulse-idle': 'pulse-idle 2s ease-in-out infinite',
  'pulse-listen': 'pulse-listen 1s ease-in-out infinite',
  'spin-process': 'spin-process 2s linear infinite',
  'ripple': 'ripple 1.5s ease-out infinite',
  // ADD: 'wake-burst': 'wake-burst 350ms ease-out',   // NEW — D-02
},
```

From apps/desktop/src/renderer/components/Orb/Orb.tsx (trecho):
```ts
const { state } = useOrbContext();
// Aplica filter drop-shadow(0 0 24px ...) + border rgba(255,255,255,0.18)
// Aplica className animate-pulse-idle / animate-pulse-listen / animate-spin-process no inner layer
```

D-01 visual paused (literal do CONTEXT.md):
- `opacity: 0.6` no root div (antes 1.0)
- `filter: drop-shadow(...)` com raio 12px (antes 24px) e alpha 0.28 (antes 0.55)
- border inner `rgba(180,180,180,0.22)` (antes `rgba(255,255,255,0.18)`)
- SOMENTE quando `state === 'idle' && wakeWordPaused === true`

D-02 wake burst (literal do CONTEXT.md):
- keyframes: 0% scale(1.0)+ring opacity 0, 45% scale(1.1)+ring opacity 1.0 (amber #F59E0B, 2px), 100% scale(1.0)+ring opacity 0
- duration: 350ms, ease-out
- triggerWakeBurst() dispara burstActive=true, aplica className no root (NÃO no inner que já tem animate-pulse-*), e `setTimeout(() => setBurstActive(false), 350)`
- A ring amber é um absolute-positioned div overlay com borderRadius 50%, 2px solid #F59E0B, controlado via keyframe opacity

D-05 prefers-reduced-motion (literal):
Classes a desligar: `animate-pulse-idle`, `animate-pulse-listen`, `animate-spin-process`, `animate-ripple`, `animate-wake-burst`
Forma: bloco global `@media (prefers-reduced-motion: reduce) { .animate-...{ animation: none !important; } }`
Preservar: transições `transition: background 0.4s ease-in-out, border-color 0.4s ease-in-out, filter 0.4s ease-in-out, opacity 0.4s ease-in-out`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender OrbContext com wakeWordPaused + burstActive + triggerWakeBurst</name>
  <files>
    apps/desktop/src/renderer/components/Orb/OrbContext.tsx,
    apps/desktop/src/renderer/components/Orb/__tests__/OrbContext.test.tsx
  </files>
  <behavior>
    - Test (existente continua): useOrbContext fora de OrbProvider dá throw com mensagem "useOrbContext must be used within OrbProvider"
    - Test: OrbProvider inicial render retorna wakeWordPaused === false (D-04 default)
    - Test: OrbProvider inicial render retorna burstActive === false
    - Test: setWakeWordPaused(true) muda o valor — rerender reflete
    - Test: setWakeWordPaused aceita false de volta (toggle bidirecional)
    - Test: triggerWakeBurst() seta burstActive=true imediatamente
    - Test: triggerWakeBurst() + vi.advanceTimersByTime(350) → burstActive volta para false
    - Test: triggerWakeBurst() chamado duas vezes rapidamente — timer é reset (segunda chamada cancela o timeout anterior e arma novo, mantém burst por 350ms a partir da segunda chamada)
    - Test: unmount limpa o timeout pendente (sem warning de "state update after unmount")
  </behavior>
  <action>
    Estender `apps/desktop/src/renderer/components/Orb/OrbContext.tsx` implementando o shape alvo do bloco `<interfaces>` acima per D-01 e D-02:

    1. Adicionar ao `OrbContextValue` os 4 campos novos: `wakeWordPaused`, `setWakeWordPaused`, `burstActive`, `triggerWakeBurst`.

    2. No `OrbProvider`:
       - `const [wakeWordPaused, setWakeWordPaused] = useState<boolean>(false);` — default false per D-04.
       - `const [burstActive, setBurstActive] = useState<boolean>(false);`
       - `const burstTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);`
       - `const triggerWakeBurst = useCallback(() => { if (burstTimeoutRef.current) clearTimeout(burstTimeoutRef.current); setBurstActive(true); burstTimeoutRef.current = setTimeout(() => { setBurstActive(false); burstTimeoutRef.current = null; }, 350); }, []);`
       - `useEffect(() => () => { if (burstTimeoutRef.current) clearTimeout(burstTimeoutRef.current); }, []);` — cleanup on unmount.
       - `value` do Provider inclui todos os 6 campos.

    3. Atualizar/criar `apps/desktop/src/renderer/components/Orb/__tests__/OrbContext.test.tsx` com @vitest-environment happy-dom docblock, usar @testing-library/react `renderHook` + `act`, fake timers via `vi.useFakeTimers()` para o teste do burst timeout. Reusar padrões v1.2 (D-07).

    Usar `useCallback` e `useRef` (imports de 'react'). NÃO introduzir nova dependência. NÃO alterar o tipo `OrbState` — continua 'idle' | 'listening' | 'processing' | 'responding' conforme D-01 explícito ("NÃO criar 5º state idle-paused").

    Commit: `✨ feat(23-01): OrbContext com wakeWordPaused e wake burst (D-01, D-02)`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/components/Orb/__tests__/OrbContext.test.tsx</automated>
  </verify>
  <done>
    OrbContext.tsx exporta os 4 campos novos; OrbContext.test.tsx tem ≥8 cenários cobrindo defaults, setters, triggerWakeBurst timeout lifecycle, re-trigger cancelation, unmount cleanup; todos os testes passam.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orb.tsx visual paused + wake burst + amber ring overlay</name>
  <files>
    apps/desktop/src/renderer/components/Orb/Orb.tsx,
    apps/desktop/src/renderer/components/Orb/__tests__/Orb.test.tsx,
    apps/desktop/tailwind.config.ts
  </files>
  <behavior>
    - Test: quando state='idle' e wakeWordPaused=false, root div NÃO contém `opacity: 0.6` no style
    - Test: quando state='idle' e wakeWordPaused=true, root div contém `opacity: 0.6` no style (D-01)
    - Test: quando state='idle' e wakeWordPaused=true, root div contém drop-shadow raio 12px e alpha 0.28 (regex: `drop-shadow\(0 0 12px rgba\(.*0\.28`)
    - Test: quando state='idle' e wakeWordPaused=true, inner sphere border é `rgba(180,180,180,0.22)`
    - Test: quando state='listening' e wakeWordPaused=true, root div NÃO contém `opacity: 0.6` (wakeWordPaused ignorado fora de idle — D-01)
    - Test: quando burstActive=true, root div tem className contendo `animate-wake-burst`
    - Test: quando burstActive=true, há um elemento amber ring overlay (2px solid #F59E0B, borderRadius 50%, absolute positioned)
    - Test: quando burstActive=false, não há elemento com className animate-wake-burst
    - Test: o teste existente de idle mantém compatibilidade (gradient ainda contém `#2BA8D4`) — NOTA: testes pré-existentes que esperam `#06B6D4` ficam quebrados desde v1.2 (pre-existing deferred conforme 22-04-SUMMARY). Remover essas expectativas obsoletas nesta passada: atualizar para `#2BA8D4` (idle) e `#F59E0B` (listening) que é o que o render atual já produz.
  </behavior>
  <action>
    1. **Tailwind config** (`apps/desktop/tailwind.config.ts`):
       - Adicionar em `theme.extend.animation`: `'wake-burst': 'wake-burst 350ms ease-out'`
       - Adicionar em `theme.extend.keyframes`:
         ```ts
         'wake-burst': {
           '0%':   { transform: 'scale(1.0)' },
           '45%':  { transform: 'scale(1.1)' },
           '100%': { transform: 'scale(1.0)' },
         },
         ```
       - NOTA: a ring amber NÃO é animada via este keyframe (que só controla transform). A ring tem seu próprio keyframe de opacity — mais simples: usar mesmo nome `wake-burst-ring` OU controlar opacity via mesma curva no mesmo keyframe usando elementos filhos. Decisão: criar UM SÓ keyframe `wake-burst` que afeta transform do root, e a ring tem sua própria `wake-burst-ring` animation com opacity 0→1→0:
         ```ts
         'wake-burst-ring': {
           '0%':   { opacity: '0' },
           '45%':  { opacity: '1' },
           '100%': { opacity: '0' },
         },
         ```
         Adicionar `'wake-burst-ring': 'wake-burst-ring 350ms ease-out'` em animation.

    2. **Orb.tsx** — Extrair `wakeWordPaused` e `burstActive` do `useOrbContext()`:
       ```ts
       const { state, wakeWordPaused, burstActive } = useOrbContext();
       ```

       Computar valores derivados:
       ```ts
       const isPausedVisual = state === 'idle' && wakeWordPaused;
       const glowRadius = isPausedVisual ? 12 : 24;
       const glowAlpha = isPausedVisual ? 0.28 : 0.55;
       const innerBorder = isPausedVisual ? 'rgba(180,180,180,0.22)' : 'rgba(255,255,255,0.18)';
       const rootOpacity = isPausedVisual ? 0.6 : 1;
       ```

       Montar o glow string do jeito que preserva o drop-shadow cinza inferior:
       ```ts
       const glowColor = stateGlow[state]; // mas com alpha overridden se paused:
       // Melhor: construir o rgba manualmente quando paused.
       const glowRgba = isPausedVisual
         ? `rgba(43,168,212,${glowAlpha})` // cyan with reduced alpha
         : stateGlow[state];
       filter: `drop-shadow(0 0 ${glowRadius}px ${glowRgba}) drop-shadow(0 4px 12px rgba(0,0,0,0.35))`
       ```

       Adicionar `opacity: rootOpacity` no style do root div + estender a transition existente para incluir `opacity 0.4s ease-in-out` (D-05 preserva transitions).

       Aplicar className root condicional — o root div atualmente não tem className, adicionar:
       ```ts
       <div
         aria-label={...}
         className={burstActive ? 'animate-wake-burst' : undefined}
         style={{ ..., transition: 'filter 0.4s ease-in-out, opacity 0.4s ease-in-out', opacity: rootOpacity }}
       >
       ```

       No Layer 1 (inner sphere), usar `border: `1px solid ${innerBorder}``. Manter todo o resto (gradient, boxShadow, specular highlight, ripple) inalterado.

       **Amber ring overlay** — novo Layer (após Layer 4 ripple). Renderizar condicionalmente quando `burstActive`:
       ```tsx
       {burstActive && (
         <div
           className="animate-wake-burst-ring"
           style={{
             position: 'absolute',
             top: 0, left: 0, right: 0, bottom: 0,
             borderRadius: '50%',
             border: '2px solid #F59E0B',
             pointerEvents: 'none',
             opacity: 0, // keyframe takes over
           }}
         />
       )}
       ```

    3. **Orb.test.tsx** — atualizar o mock para retornar os 4 campos novos (default false/false), adicionar os ≥8 cenários listados em `<behavior>`. Corrigir os 2 testes pré-existentes deferred (`#06B6D4` → `#2BA8D4`, e seletor de transition 300ms → 400ms se houver). Reutilizar padrões happy-dom + vi.mocked(useOrbContext) já usados no teste atual.

    Commit: `✨ feat(23-01): Orb paused visual + wake burst ring (WAKE-02, WAKE-04, ORB-POL-02)`
  </action>
  <verify>
    <automated>pnpm --filter @jarvis/desktop test --run src/renderer/components/Orb/__tests__/Orb.test.tsx</automated>
  </verify>
  <done>
    Orb.tsx consome os 4 campos de OrbContext; style inclui opacity condicional; amber ring renderiza condicionalmente com animate-wake-burst-ring; tailwind.config.ts contém os 2 novos keyframes + animations; Orb.test.tsx tem ≥8 cenários passando (incluindo os 2 pré-existentes corrigidos).
  </done>
</task>

<task type="auto">
  <name>Task 3: Global prefers-reduced-motion block</name>
  <files>
    apps/desktop/src/renderer/src/styles/globals.css
  </files>
  <action>
    Editar `apps/desktop/src/renderer/src/styles/globals.css`. Adicionar ao final do arquivo (após qualquer `@tailwind` / layer existente) um bloco global:

    ```css
    /* Phase 23 ORB-POL-01 + D-05: Desliga animações loopadas e one-shot
       para usuários com `prefers-reduced-motion: reduce`. Transições
       (color, border, filter, opacity 0.4s) são PRESERVADAS — elas não
       são problemáticas para sensibilidade vestibular e são essenciais
       para comunicar mudança de state do orb. */
    @media (prefers-reduced-motion: reduce) {
      .animate-pulse-idle,
      .animate-pulse-listen,
      .animate-spin-process,
      .animate-ripple,
      .animate-wake-burst,
      .animate-wake-burst-ring {
        animation: none !important;
      }
    }
    ```

    NÃO mexer nas `transition` existentes — elas permanecem ativas per D-05.

    Commit: `✨ feat(23-01): prefers-reduced-motion global rule (ORB-POL-01)`
  </action>
  <verify>
    <automated>grep -c "prefers-reduced-motion" apps/desktop/src/renderer/src/styles/globals.css && grep -c "animate-wake-burst" apps/desktop/src/renderer/src/styles/globals.css</automated>
  </verify>
  <done>
    globals.css contém o bloco @media com as 6 classes (5 animate-* + wake-burst-ring) zeradas; transições preservadas; grep retorna ≥1 para ambos.
  </done>
</task>

</tasks>

<threat_model>
security_enforcement: disabled (Phase 23 é 100% renderer CSS + React state — zero trust boundary crossing, zero IPC novo neste plano, zero input externo processado, zero persistência. Plan 02 cobre o threat model da camada de IPC.)
</threat_model>

<verification>
Após todas as tasks:
- `pnpm --filter @jarvis/desktop test --run src/renderer/components/Orb/` → 100% passing (OrbContext + Orb suites)
- `pnpm --filter @jarvis/desktop lint` → 0 errors
- `pnpm --filter @jarvis/desktop build` → 0 TypeScript errors
- `grep -n "prefers-reduced-motion" apps/desktop/src/renderer/src/styles/globals.css` retorna 1 match
- `grep -n "wake-burst" apps/desktop/tailwind.config.ts` retorna ≥2 matches (wake-burst + wake-burst-ring)
</verification>

<success_criteria>
- OrbContext expõe `wakeWordPaused`, `setWakeWordPaused`, `burstActive`, `triggerWakeBurst` (D-01 + D-02)
- Default de `wakeWordPaused` é `false` (D-04)
- Orb.tsx renderiza visual paused distintivo (opacity/glow/border) SOMENTE quando idle+paused (WAKE-04)
- Orb.tsx renderiza amber ring overlay 2px #F59E0B durante burstActive com keyframe opacity 0→1→0 (WAKE-02)
- Tailwind config tem keyframes `wake-burst` (transform) e `wake-burst-ring` (opacity), ambos 350ms ease-out (D-02, ORB-POL-02)
- globals.css tem `@media (prefers-reduced-motion: reduce)` zerando as 6 classes de animação (D-05 + ORB-POL-01)
- Testes de OrbContext e Orb cobrem todos os cenários listados em `<behavior>` e passam
- Zero alteração em: `stateGradients`, `rippleColor`, `stateGlow` maps, Layer 2/3 specular highlights, Layer 4 ripple (responding)
- Commits conventional + emoji em pt-BR (✨ feat)
</success_criteria>

<output>
Após completion, criar `.planning/phases/23-orb-ux-polish/23-01-SUMMARY.md` seguindo o template de summary. Documentar:
- Files created/modified (6)
- Tests added count
- Commits (3 esperados: OrbContext, Orb.tsx+tailwind, globals.css)
- Quais decisões do CONTEXT.md foram implementadas (D-01, D-02, D-04, D-05, ORB-POL-01)
- Quais decisões ficam para Plan 02 (D-03, D-06 — tray + IPC + hook wiring)
- Deviations (se houver)
</output>
