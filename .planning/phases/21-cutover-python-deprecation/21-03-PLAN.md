---
phase: 21-cutover-python-deprecation
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01-PLAN.md
  - 21-02-PLAN.md
files_modified:
  - README.md
  - .env.example
  - .env
autonomous: true
requirements:
  - VAL-08
  - VAL-09
  - VAL-10

must_haves:
  truths:
    - "README.md não menciona Python backend, porta 8000, FastAPI ou uvicorn"
    - "README.md reflete stack TypeScript-only na arquitetura e na tabela de stack"
    - ".env.example não contém FASTAPI_URL nem SQLITE_PATH"
    - ".env não contém FASTAPI_URL nem SQLITE_PATH ativos"
    - "README.md mantém a estrutura e formato existente (não foi reescrito do zero)"
  artifacts:
    - path: "README.md"
      provides: "Documentação atualizada — stack TypeScript-only"
      contains: "backend-ts"
    - path: ".env.example"
      provides: "Template de env vars sem vars Python-only"
      contains: "BACKEND_TS_URL"
  key_links:
    - from: "README.md"
      to: "estrutura de projeto"
      via: "seção 'Estrutura do projeto'"
      pattern: "backend-ts"
---

<objective>
Atualizar documentação e env vars (D-04, D-05): remover todas as referências ao Python backend do README.md, e remover FASTAPI_URL e SQLITE_PATH do .env.example e do .env ativo.

Purpose: Documentação e configuração de exemplo devem refletir o estado atual do sistema — TypeScript-only. Desenvolvedores clonando o repo não devem ver artefatos Python obsoletos.

Output: README.md sem menções Python, .env.example limpo, .env sem vars obsoletas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-cutover-python-deprecation/21-CONTEXT.md

<interfaces>
<!-- Estado atual dos arquivos — extraído antes de qualquer edição -->

README.md — Referências Python a remover/atualizar:
- Linha 5: status badge menciona "migração Python → TypeScript" (atualizar para v1.3 completo)
- Linhas 168-169: `src/jarvis/              # Python backend (legado v1.0-1.2, será removido na Phase 21)` → REMOVER linha
- Linha 119: ChromaDB rodando na porta 8000 — MANTER (ChromaDB é válido, não é Python)
- Seção "Porta em uso": menciona "ChromaDB: 8000 (quando usado)" — MANTER
- Seção "Estrutura": bloco de estrutura do projeto mostrando src/jarvis/ → remover esse bloco

.env.example — Vars a remover (per D-05):
- `SQLITE_PATH=data/jarvis.db` (linhas 22-23, seção Memory) → REMOVER
- `FASTAPI_URL=http://localhost:8000` (linhas 34-37, seção Gateway) → REMOVER a var e o comentário

.env (ativo) — Vars a remover se presentes:
- `SQLITE_PATH=data/jarvis.db` → REMOVER
- `FASTAPI_URL=http://127.0.0.1:8000` → REMOVER
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar README.md — remover referências Python</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md
  </read_first>
  <action>
    Editar README.md (per D-04) — modificações cirúrgicas, mantendo estrutura existente:

    **1. Linha 5 — Status badge:**
    Alterar:
    ```
    > **Status:** v1.3 em desenvolvimento — migração Python → TypeScript. 14/18 fases completas.
    ```
    Para:
    ```
    > **Status:** v1.3 completo — stack TypeScript-only. Backend Python removido.
    ```

    **2. Seção "Estrutura do projeto" (bloco de árvore, ~linhas 149-179):**
    Remover a linha:
    ```
    ├── src/jarvis/              # Python backend (legado v1.0-1.2, será removido na Phase 21)
    ```
    Manter todas as outras linhas da estrutura intactas.

    **3. Seção "Problemas comuns" — item "Porta em uso" (~linha 197):**
    A linha:
    ```
    - ChromaDB: 8000 (quando usado)
    ```
    Manter — ChromaDB na porta 8000 é válido (não é o Python backend).

    **NÃO alterar:**
    - Arquitetura ASCII art (já reflete TS-only)
    - Tabela de stack (já é TypeScript-only)
    - Comandos de dev (`pnpm dev`, `pnpm dev:backend`)
    - Seção "Desenvolvimento" / GSD workflow
    - Seção "Rodando testes"
    - Qualquer outro conteúdo não relacionado ao Python backend

    Nota: O README já foi atualizado em fases anteriores para refletir a arquitetura TS. A tarefa é remover os poucos remanescentes da era Python.
  </action>
  <verify>
    <automated>grep -i "python backend\|FastAPI\|uvicorn\|porta 8000\|src/jarvis" /root/jarvis/README.md | grep -v "ChromaDB"</automated>
    Deve retornar vazio (sem matches além de ChromaDB, que usa porta 8000 mas não é Python backend).
    <automated>grep "backend-ts" /root/jarvis/README.md | wc -l</automated>
    Deve retornar 2 ou mais (referências ao backend TypeScript permanecem).
  </verify>
  <acceptance_criteria>
    - `grep -i "python backend" /root/jarvis/README.md` retorna vazio
    - `grep "src/jarvis" /root/jarvis/README.md` retorna vazio
    - `grep "FastAPI\|uvicorn" /root/jarvis/README.md` retorna vazio
    - `grep "legado v1.0" /root/jarvis/README.md` retorna vazio
    - `grep "será removido na Phase 21" /root/jarvis/README.md` retorna vazio
    - `grep "backend-ts" /root/jarvis/README.md | wc -l` retorna >= 2 (estrutura TS preservada)
    - `grep "ChromaDB" /root/jarvis/README.md | wc -l` retorna >= 2 (ChromaDB não foi removido)
  </acceptance_criteria>
  <done>README.md sem referências ao Python backend. Estrutura, comandos e stack TypeScript preservados. Status badge atualizado para v1.3 completo.</done>
</task>

<task type="auto">
  <name>Task 2: Limpar .env.example e .env — remover vars Python-only</name>
  <files>
    .env.example
    .env
  </files>
  <read_first>
    .env.example
    .env
  </read_first>
  <action>
    **Arquivo 1: .env.example** (per D-05)

    Remover as seguintes linhas do arquivo:

    Da seção `# Memory (SQLite + ChromaDB)` — remover apenas `SQLITE_PATH`:
    ```
    SQLITE_PATH=data/jarvis.db
    ```
    A var `CHROMA_PATH` deve ser MANTIDA (ChromaDB continua sendo usado pelo backend-ts).

    Da seção `# Gateway (Express)` — remover a var FASTAPI_URL e seu comentário:
    ```
    # FastAPI URL (gateway -> python service, legado — usado só na Phase 20 E2E)
    # For Docker: overridden to http://python-service:8000 by docker-compose.yml (internal network)
    FASTAPI_URL=http://localhost:8000
    ```
    As outras vars da seção Gateway (`GATEWAY_PORT`, `BACKEND_TS_URL`) devem ser MANTIDAS.

    **Arquivo 2: .env** (arquivo ativo)

    Remover as seguintes linhas se presentes:
    - `SQLITE_PATH=data/jarvis.db` → deletar linha
    - `FASTAPI_URL=http://127.0.0.1:8000` → deletar linha

    Verificar se há outros comentários associados a essas vars no .env e remover também.
    Não remover nenhuma outra var — apenas as duas Python-only.
  </action>
  <verify>
    <automated>grep "FASTAPI_URL\|SQLITE_PATH" /root/jarvis/.env.example</automated>
    Deve retornar vazio.
    <automated>grep "FASTAPI_URL\|SQLITE_PATH" /root/jarvis/.env</automated>
    Deve retornar vazio.
    <automated>grep "CHROMA_PATH\|BACKEND_TS_URL\|GATEWAY_PORT" /root/jarvis/.env.example</automated>
    Deve retornar 3 matches (vars mantidas).
  </verify>
  <acceptance_criteria>
    - `grep "FASTAPI_URL" /root/jarvis/.env.example` retorna vazio
    - `grep "SQLITE_PATH" /root/jarvis/.env.example` retorna vazio
    - `grep "FASTAPI_URL" /root/jarvis/.env` retorna vazio
    - `grep "SQLITE_PATH" /root/jarvis/.env` retorna vazio
    - `grep "CHROMA_PATH" /root/jarvis/.env.example` retorna match (mantido)
    - `grep "BACKEND_TS_URL" /root/jarvis/.env.example` retorna match (mantido)
    - `grep "JARVIS_API_KEY" /root/jarvis/.env.example` retorna match (mantido)
  </acceptance_criteria>
  <done>.env.example e .env sem FASTAPI_URL e SQLITE_PATH. Todas as outras vars preservadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env→processo | Vars de ambiente carregadas no startup dos serviços |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Information Disclosure | .env.example | accept | Remover FASTAPI_URL reduz superfície — menos vars obsoletas expostas em exemplo público |
| T-21-08 | Tampering | .env (ativo) | accept | Remoção de SQLITE_PATH não afeta backend-ts (usa DATABASE_PATH se necessário) |
</threat_model>

<verification>
Verificação completa do Plan 03:

```bash
# 1. README.md sem referências Python
grep -i "python backend\|FastAPI\|uvicorn\|src/jarvis\|legado v1.0" /root/jarvis/README.md  # sem output
grep "backend-ts" /root/jarvis/README.md | wc -l  # >= 2

# 2. .env.example sem vars Python-only
grep "FASTAPI_URL\|SQLITE_PATH" /root/jarvis/.env.example  # sem output
grep "CHROMA_PATH\|BACKEND_TS_URL" /root/jarvis/.env.example  # com output (mantidos)

# 3. .env ativo sem vars Python-only
grep "FASTAPI_URL\|SQLITE_PATH" /root/jarvis/.env  # sem output
```
</verification>

<success_criteria>
- README.md sem "python backend", "FastAPI", "uvicorn", "src/jarvis", "legado v1.0-1.2"
- Status badge atualizado para "v1.3 completo — stack TypeScript-only"
- .env.example sem FASTAPI_URL e SQLITE_PATH, mas com CHROMA_PATH e BACKEND_TS_URL intactos
- .env ativo sem FASTAPI_URL e SQLITE_PATH
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/21-cutover-python-deprecation/21-03-SUMMARY.md`
</output>
