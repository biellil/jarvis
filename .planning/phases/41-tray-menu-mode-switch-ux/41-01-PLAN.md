---
phase: 41-tray-menu-mode-switch-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src/main/__tests__/tray.test.ts
  - apps/desktop/src/shared/ipc-types.ts
autonomous: true
requirements:
  - VUI-01
must_haves:
  truths:
    - "tray.test.ts contém stubs (it.todo) para os 6 comportamentos do Voice Mode submenu"
    - "shared/ipc-types.ts exporta VoiceModeSwitchResult e IPC_CHANNELS.VOICE_MODE_SWITCH_RESULT"
    - "Os stubs existentes de 'Pause listening' são atualizados para refletir a remoção do item (D-03)"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/tray.test.ts"
      provides: "Test stubs Wave 0 para VUI-01 (6 comportamentos)"
      contains: "Voice Mode submenu"
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "VoiceModeSwitchResult type + VOICE_MODE_SWITCH_RESULT channel constant"
      exports: ["VoiceModeSwitchResult", "IPC_CHANNELS"]
  key_links:
    - from: "apps/desktop/src/main/__tests__/tray.test.ts"
      to: "apps/desktop/src/main/tray.ts"
      via: "fs.readFileSync source-level assertions"
      pattern: "traySource.toContain"
    - from: "apps/desktop/src/shared/ipc-types.ts"
      to: "apps/desktop/src/main/ipc/voiceMode.ts"
      via: "IPC_CHANNELS.VOICE_MODE_SWITCH_RESULT import"
      pattern: "VOICE_MODE_SWITCH_RESULT"
---

<objective>
Wave 0 — Nyquist test stubs + IPC type contract

Estende o arquivo de testes do tray com stubs (it.todo) para todos os comportamentos do Voice Mode submenu antes que qualquer implementação exista. Também adiciona o tipo IPC `VoiceModeSwitchResult` e a constante de canal `VOICE_MODE_SWITCH_RESULT` em shared/ipc-types.ts — contrato que implementação (Wave 1) e renderer (Phase 42) precisam antes de implementar.

Purpose: Garantir que a execução Wave 1 tenha testes prontos para tornar verdes (Nyquist compliance). Os stubs de "Pause listening" / "Resume listening" são atualizados para não falhar na presença de D-03 (remoção do item).

Output:
- tray.test.ts estendido com bloco "Voice Mode submenu (VUI-01)" contendo 6 it.todo stubs
- tray.test.ts com stubs obsoletos de "Pause listening" marcados como `it.skip` (blocos atualizados para não quebrar o CI quando D-03 remover o item)
- shared/ipc-types.ts com `VoiceModeSwitchResult` + `IPC_CHANNELS.VOICE_MODE_SWITCH_RESULT`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-tray-menu-mode-switch-ux/41-CONTEXT.md
@.planning/phases/41-tray-menu-mode-switch-ux/41-VALIDATION.md

@apps/desktop/src/main/__tests__/tray.test.ts
@apps/desktop/src/shared/ipc-types.ts
</context>

<interfaces>
<!-- Tipos existentes que o executor precisa saber -->
<!-- De apps/desktop/src/shared/ipc-types.ts -->

```typescript
// Tipo existente — padrão a seguir:
export interface VoiceModeChangeEvent {
  oldMode: VoiceMode;
  newMode: VoiceMode;
  reason: 'user' | 'system';
  timestamp: number;
}

// Registro existente de canais — VOICE_MODE_SWITCH_RESULT deve ser adicionado aqui:
export const IPC_CHANNELS = {
  // ... canais existentes ...
  VOICE_MODE_DEGRADED: 'voiceMode:degraded',
  // ^ último canal existente antes da adição do Phase 41
} as const;

// Tipo VoiceMode já definido:
export type VoiceMode = 'wake-word' | 'always-listening' | 'ptt-only';
```

<!-- De apps/desktop/src/main/__tests__/tray.test.ts — padrão existente de teste: -->
```typescript
// Padrão source-level: lê tray.ts como string e faz assertions sobre o conteúdo
const traySource = fs.readFileSync(path.join(__dirname, '../tray.ts'), 'utf-8');

describe('Tray Module (source-level assertions)', () => {
  describe('WAKE-03 / D-03: Pause/Resume listening item', () => {
    it('imports getWakeWordPaused + setWakeWordPaused from store', () => {
      expect(traySource).toMatch(/getWakeWordPaused/);
    });
    // ... outros it() existentes
  });
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar VoiceModeSwitchResult e canal IPC em shared/ipc-types.ts</name>
  <read_first>
    - apps/desktop/src/shared/ipc-types.ts (arquivo completo — ler antes de editar)
  </read_first>
  <files>apps/desktop/src/shared/ipc-types.ts</files>
  <action>
Adicionar após o bloco "Always-Listening Types" (após VoiceModeDegradedEvent, antes de "Channel Names"):

```typescript
// ============================================
// Voice Mode Switch Result — Phase 41 (VUI-01)
// ============================================

/**
 * VoiceModeSwitchResult — payload do canal IPC 'voice-mode:switch-result'.
 *
 * D-02: Canal unificado para sucesso e bloqueio — renderer decide como renderizar.
 * D-05: Enviado em ambos os casos (success:true e success:false).
 *
 * Consumer: Phase 42 renderer (toast de confirmação).
 * Producer: apps/desktop/src/main/ipc/voiceMode.ts broadcastModeSwitch().
 */
export interface VoiceModeSwitchResult {
  /** true = modo trocado com sucesso; false = bloqueado (captura ativa ou transition em progresso) */
  success: boolean;
  /** Modo para o qual a troca ocorreu. Presente apenas quando success:true. */
  newMode?: VoiceMode;
  /** Label human-readable do novo modo. Presente apenas quando success:true. D-07: "Wake Word" | "Always-Listening" | "PTT-only" */
  label?: string;
}
```

Adicionar a constante de canal no objeto `IPC_CHANNELS` (após `VOICE_MODE_DEGRADED`):

```typescript
  // Phase 41 — resultado de troca de modo via tray (main → renderer)
  // D-02: canal unificado — payload { success, newMode?, label? }
  VOICE_MODE_SWITCH_RESULT: 'voice-mode:switch-result',
```

Não alterar nenhum outro campo existente.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm test -- --testPathPattern="tray" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "VoiceModeSwitchResult" apps/desktop/src/shared/ipc-types.ts → retorna pelo menos 2 linhas (interface + comentário)
    - grep -n "VOICE_MODE_SWITCH_RESULT" apps/desktop/src/shared/ipc-types.ts → retorna exatamente 1 linha com valor 'voice-mode:switch-result'
    - grep -n "voice-mode:switch-result" apps/desktop/src/shared/ipc-types.ts → retorna 1 linha
    - Interface VoiceModeSwitchResult contém campos: success (boolean), newMode? (VoiceMode), label? (string)
    - Arquivo compila sem erros (tsc --noEmit ou verificado pelos testes que importam o arquivo)
  </acceptance_criteria>
  <done>VoiceModeSwitchResult interface e VOICE_MODE_SWITCH_RESULT canal definidos e exportados de shared/ipc-types.ts</done>
</task>

<task type="auto">
  <name>Task 2: Estender tray.test.ts com stubs Wave 0 e atualizar testes obsoletos</name>
  <read_first>
    - apps/desktop/src/main/__tests__/tray.test.ts (arquivo completo — ler antes de editar)
    - apps/desktop/src/main/tray.ts (para entender o que será removido em Wave 1)
    - .planning/phases/41-tray-menu-mode-switch-ux/41-VALIDATION.md (mapa de testes)
  </read_first>
  <files>apps/desktop/src/main/__tests__/tray.test.ts</files>
  <action>
**Passo 1 — Converter stubs de "Pause listening" que quebrarão com D-03:**

Os testes que afirmam a PRESENÇA de "Pause listening"/"Resume listening" vão quebrar quando tray.ts for modificado em Wave 1. Converter cada `it(...)` que verifica presença do item legado para `it.skip(...)` com comentário explicando que D-03 removeu o item:

Converter os seguintes testes existentes no describe 'WAKE-03 / D-03':
- `it("contains 'Pause listening' label", ...)` → `it.skip('D-03: item removido em Phase 41 — substituído por Voice Mode submenu', ...)`
- `it("contains 'Resume listening' label", ...)` → `it.skip('D-03: item removido em Phase 41 — substituído por Voice Mode submenu', ...)`
- `it('Pause/Resume label is the FIRST menu entry (appears before Show)', ...)` → `it.skip('D-03: item removido em Phase 41 — Voice Mode submenu é agora o primeiro item', ...)`
- `it('click handler calls setWakeWordPaused with !current', ...)` → `it.skip('D-03: item removido em Phase 41', ...)`
- `it('click handler calls broadcastPauseToggle', ...)` → `it.skip('D-03: item removido em Phase 41', ...)`
- `it('tooltip reflects paused state (JARVIS — paused / JARVIS — listening)', ...)` → `it.skip('D-03: tooltip atualizado para modo ativo em Phase 41', ...)`
- `it('click rebuilds context menu (setContextMenu called inside click)', ...)` → `it.skip('D-03: item removido em Phase 41', ...)`
- `it('no residual references to wake-word-settings-changed / wakeWordEnabled', ...)` → mantém como `it()` (verificação negativa continua válida)
- `it('imports broadcastPauseToggle from ipc/settings', ...)` → `it.skip('D-03: broadcastModeSwitch substitui em Phase 41', ...)`
- `it('imports getWakeWordPaused + setWakeWordPaused from store', ...)` → `it.skip('D-03: getWakeWordPaused removido do tray em Phase 41', ...)`

**Passo 2 — Adicionar novo describe block com stubs para Voice Mode submenu:**

Adicionar APÓS o describe 'Preserved behaviors (regression)':

```typescript
describe('Voice Mode submenu (VUI-01 — Phase 41)', () => {
  it.todo('D-07: submenu contém exatamente 3 itens: "Wake Word", "Always-Listening", "PTT-only"');
  it.todo('D-06: submenu "Voice Mode" aparece como primeiro item após separador inicial (antes de Show)');
  it.todo('D-04: buildContextMenu lê VoiceModeManager.getMode() para determinar checked state');
  it.todo('D-01/D-05: click handler chama voiceModeManager.setMode() com o modo correto');
  it.todo('D-02/D-05: broadcastModeSwitch é chamado tanto em success:true quanto em success:false');
  it.todo('D-03: "Pause listening" e "Resume listening" NÃO aparecem em tray.ts');
});
```

**Passo 3 — Adicionar describe block para wiring em main/index.ts:**

Adicionar no describe 'Main process tray integration':

```typescript
  it.todo('VUI-01: createTray aceita VoiceModeManager como segundo parâmetro');
  it.todo('VUI-01: main/index.ts instancia VoiceModeManager e passa para createTray()');
```

**Importante:** Manter todos os testes do describe 'Preserved behaviors' e 'D-05: Icon path' e 'Exports' intactos — eles não são afetados pela mudança.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && pnpm test -- --testPathPattern="tray" --no-coverage 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Todos os testes passam (os stubs são it.todo e portanto não falham)
    - grep "it.todo" apps/desktop/src/main/__tests__/tray.test.ts | wc -l → retorna pelo menos 8 linhas (6 stubs de Voice Mode + 2 stubs de wiring)
    - grep "Voice Mode submenu" apps/desktop/src/main/__tests__/tray.test.ts → retorna 1 linha (o describe)
    - grep "it.skip" apps/desktop/src/main/__tests__/tray.test.ts | wc -l → retorna pelo menos 8 linhas (stubs obsoletos de Pause/Resume)
    - pnpm test -- --testPathPattern="tray" --no-coverage → exit code 0 (sem falhas)
    - grep "D-03: item removido" apps/desktop/src/main/__tests__/tray.test.ts → retorna pelo menos 5 linhas
  </acceptance_criteria>
  <done>tray.test.ts atualizado: stubs obsoletos de Pause/Resume marcados como it.skip, bloco "Voice Mode submenu (VUI-01)" com 6 it.todo stubs adicionado, 2 stubs de wiring adicionados em "Main process tray integration"</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ipc-types.ts → consumidores | Tipo VoiceModeSwitchResult define contrato — consumidores (Phase 42 renderer) recebem sem validação adicional (main é o produtor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-MODE | Tampering | VoiceModeSwitchResult.newMode | accept | Wave 0 é apenas tipos/stubs — nenhum canal IPC ativo ainda. Validação implementada em Wave 1 Plan 02 Task 2 (VoiceMode union type check antes de setMode). |
| T-41-IPC | Spoofing | 'voice-mode:switch-result' canal | accept | Canal não está ativo neste wave. Mitigação em Wave 1: main process é o único produtor; renderer apenas recebe via contextBridge. |

*Nenhuma ameaça de alta severidade neste wave — apenas definições de tipo e stubs de teste.*
</threat_model>

<verification>
```bash
# Executar suite de testes do tray
cd /root/jarvis/apps/desktop && pnpm test -- --testPathPattern="tray" --no-coverage

# Verificar tipo adicionado
grep -n "VoiceModeSwitchResult" apps/desktop/src/shared/ipc-types.ts

# Verificar canal IPC
grep -n "VOICE_MODE_SWITCH_RESULT\|voice-mode:switch-result" apps/desktop/src/shared/ipc-types.ts

# Verificar stubs todo
grep -c "it.todo" apps/desktop/src/main/__tests__/tray.test.ts

# Verificar stubs skip
grep -c "it.skip" apps/desktop/src/main/__tests__/tray.test.ts
```

Critérios globais do wave:
- Testes passam com 0 falhas
- Pelo menos 8 it.todo presentes
- Pelo menos 8 it.skip presentes (stubs legados de Pause/Resume)
- VoiceModeSwitchResult e VOICE_MODE_SWITCH_RESULT exportados de ipc-types.ts
</verification>

<success_criteria>
1. `pnpm test -- --testPathPattern="tray" --no-coverage` retorna 0 falhas
2. `shared/ipc-types.ts` exporta `VoiceModeSwitchResult` (interface com success, newMode?, label?) e `IPC_CHANNELS.VOICE_MODE_SWITCH_RESULT = 'voice-mode:switch-result'`
3. `tray.test.ts` contém describe block "Voice Mode submenu (VUI-01 — Phase 41)" com 6 it.todo stubs
4. Stubs de "Pause listening"/"Resume listening" que verificam presença do item estão como it.skip
5. Nenhum teste existente quebrado (verified behaviors, icon path, exports continuam verdes)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/41-tray-menu-mode-switch-ux/41-01-SUMMARY.md` com:
- Tipos adicionados (VoiceModeSwitchResult, VOICE_MODE_SWITCH_RESULT)
- Stubs criados (lista dos 8 it.todo)
- Stubs convertidos para it.skip (lista dos ≥8 it.skip)
- Resultado do pnpm test (número de testes passados/pulados)
</output>
