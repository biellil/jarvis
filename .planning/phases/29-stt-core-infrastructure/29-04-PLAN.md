---
phase: 29-stt-core-infrastructure
plan: "04"
type: execute
wave: 4
depends_on:
  - "29-03"
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - STT-01

must_haves:
  truths:
    - "pnpm build:dist produces release-v2/ directory with app.asar.unpacked/node_modules/@fugood/ containing .node binaries"
    - "With USE_WHISPER_CPP=true, app logs 'Using GPU backend: X' or 'Falling back to CPU' during startup (no crash)"
    - "With USE_WHISPER_CPP=false, PTT and wake word function identically to Phase 28 (no regression)"
    - "Phase 29 PoC manually verified: whisper.cpp module loads without ASAR error in packaged build"
  artifacts: []
  key_links:
    - from: release-v2/
      to: app.asar.unpacked/node_modules/@fugood/
      via: electron-builder asarUnpack config
      pattern: "app.asar.unpacked"
---

<objective>
Manual verification checkpoint. Validates INFRA-01 (ASAR packaging) and STT-01 (transcription PoC). These two success criteria cannot be verified with automated tests — they require a real build artifact and runtime execution.

Purpose: Gate before Phase 30 can begin. Phase 30 depends on whisper.cpp PoC being verified.
Output: Human sign-off that ASAR unpacking works and whisper.node loads in the packaged app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-stt-core-infrastructure/29-CONTEXT.md
@.planning/phases/29-stt-core-infrastructure/29-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dist artifact and verify ASAR unpacking (INFRA-01)</name>
  <files></files>
  <read_first>
    - apps/desktop/electron-builder.yml (confirm asarUnpack section from Plan 02 is present before running build)
    - apps/desktop/package.json (confirm @fugood/whisper.node is in dependencies before building)
  </read_first>
  <action>
    Run the production build and inspect the output for ASAR unpacking correctness.

    Step 1 — Run build:
    ```bash
    cd /c/jarvis && pnpm --filter @jarvis/desktop build:dist 2>&1
    ```
    This will take several minutes. Watch for errors related to native addons or ASAR packing.

    Step 2 — Inspect the release output for unpacked @fugood binaries:
    ```bash
    # Windows path — check if app.asar.unpacked directory exists
    ls "release-v2/win-unpacked/resources/app.asar.unpacked/node_modules/@fugood/" 2>/dev/null || \
    find release-v2/ -name "*.node" -path "*fugood*" 2>/dev/null | head -10
    ```

    Step 3 — If .node files ARE found in app.asar.unpacked: INFRA-01 is satisfied. Log the paths found.

    Step 4 — If .node files are NOT found or are inside app.asar (not unpacked): The asarUnpack config did not work. Diagnose:
    - Check if electron-builder.yml asarUnpack glob matches the installed package path
    - Try more specific pattern: `"node_modules/@fugood/whisper.node/**/*.node"`
    - Rebuild after fixing

    Note: On Windows, the build output will be in `release-v2/win-unpacked/`. The path to check is:
    `release-v2/win-unpacked/resources/app.asar.unpacked/node_modules/@fugood/`
  </action>
  <verify>
    <automated>find /c/jarvis/release-v2 -name "*.node" -path "*fugood*" 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build:dist` completes without errors
    - At least one `.node` file exists under `release-v2/win-unpacked/resources/app.asar.unpacked/node_modules/@fugood/` (or equivalent path for platform)
    - No "Cannot find module" errors related to @fugood in build output
    - release-v2/ directory exists and contains the built installer
  </acceptance_criteria>
  <done>ASAR unpacking verified — @fugood .node binaries are in app.asar.unpacked, not inside the ASAR archive. INFRA-01 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 29 infrastructure modules:
    - gpuDetection.ts (CUDA/Vulkan/Metal/CPU auto-detection with exact log strings)
    - audioNormalizer.ts (WebM → 16kHz PCM mono WAV via ffmpeg)
    - whisperResources.ts (model path resolver)
    - electron-builder.yml asarUnpack for @fugood .node binaries
    - Feature flag USE_WHISPER_CPP in index.ts and ipc/chat.ts
    - All 8 unit tests GREEN
    - ASAR build verified in Task 1
  </what-built>
  <how-to-verify>
    **Regression check (USE_WHISPER_CPP=false — must work identically to Phase 28):**
    1. Ensure .env has `USE_WHISPER_CPP=false` (or the var is absent — defaults to false)
    2. Run: `cd /c/jarvis && pnpm dev` (starts Electron app)
    3. Use PTT (Ctrl+Space) to record a voice message — should work exactly as before Phase 29
    4. Use wake word "Hey JARVIS" — should work exactly as before Phase 29
    5. Confirm no new errors or console warnings related to whisper appear

    **GPU detection check (USE_WHISPER_CPP=true):**
    6. Add `USE_WHISPER_CPP=true` to your .env file
    7. Run: `cd /c/jarvis && pnpm dev`
    8. Check the Electron main process console output (DevTools → Console or terminal output)
    9. Confirm you see EXACTLY ONE of:
       - `Using GPU backend: cuda` (if NVIDIA GPU with CUDA)
       - `Using GPU backend: vulkan` (if AMD/Intel GPU with Vulkan)
       - `Using GPU backend: metal` (Apple Silicon only)
       - `Falling back to CPU` (no compatible GPU / driver not installed)
    10. Confirm the app does NOT crash — it starts normally and the orb appears

    **Notes:**
    - With USE_WHISPER_CPP=true, PTT will return a "NOT_IMPLEMENTED" error — this is expected for Phase 29 PoC
    - The whisper model download does NOT happen yet at this stage (that's wired in Phase 30/31)
    - GPU detection may fail with "model path is empty" error from @fugood/whisper.node — this is acceptable; what matters is that the GPU log line appears before the error
  </how-to-verify>
  <resume-signal>
    Type "approved" if:
    - Regression check passed (USE_WHISPER_CPP=false, PTT and wake word work normally)
    - GPU detection log appeared (one of the four expected log lines)
    - App did not crash with USE_WHISPER_CPP=true

    Or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification is the gate. Automated test suite was validated in Plans 01-03.

Final automated sanity check before human sign-off:
```bash
# All tests must be green
cd /c/jarvis && pnpm --filter @jarvis/desktop test 2>&1 | tail -5

# Confirm ASAR unpacking
find /c/jarvis/release-v2 -name "*.node" 2>/dev/null | head -5
```
</verification>

<success_criteria>
- INFRA-01: pnpm build:dist produces app.asar.unpacked/node_modules/@fugood/ with .node binaries
- STT-01: GPU detection log appears on startup with USE_WHISPER_CPP=true (exact string match)
- INFRA-02: USE_WHISPER_CPP=false preserves Phase 28 PTT + wake word behavior (human verified)
- Phase 29 PoC manually signed off — Phase 30 may proceed
</success_criteria>

<output>
After completion, create `.planning/phases/29-stt-core-infrastructure/29-04-SUMMARY.md`
</output>
