---
phase: 31-ipc-refactor-e2e-rollout
plan: 02
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - ARCH-06
must_haves:
  truths:
    - "With USE_WHISPER_CPP=true: user speaks and receives audio response via local whisper.cpp (no /chat/audio gateway call)"
    - "PTT hotkey works identically with USE_WHISPER_CPP=true — no regression"
    - "Multi-turn voice (awaiting-followup state) preserved on new IPC path"
    - "With USE_WHISPER_CPP=false: original HTTP gateway path 100% operational (killswitch works)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ARCH-06 marked complete after sign-off"
      contains: "[x] **ARCH-06**"
  key_links:
    - from: "renderer sendAudioAndHandle"
      to: "main handleSendAudio"
      via: "ipcRenderer.invoke('chat:send-audio')"
      pattern: "chat:send-audio"
    - from: "main handleSendAudio (USE_WHISPER_CPP=true branch)"
      to: "voiceHandler.handleAudio()"
      via: "direct function call"
      pattern: "handleAudio"
---

<objective>
Manual E2E sign-off for ARCH-06: validate the full IPC audio pipeline with USE_WHISPER_CPP=true (wake word + PTT → local STT → LLM → TTS → audio response) and confirm the feature flag works as a bidirectional killswitch (USE_WHISPER_CPP=false preserves Phase 28 behavior).

Purpose: Phase 31's production code was completed in Phase 30. Unit tests added in Plan 01 verify the routing logic. This checkpoint validates the complete E2E pipeline on real hardware before closing the phase and proceeding to Phase 32 cleanup.

Output: Human sign-off confirming ARCH-06 is functionally complete. REQUIREMENTS.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-ipc-refactor-e2e-rollout/31-CONTEXT.md
@.planning/phases/31-ipc-refactor-e2e-rollout/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify full test suite green</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - apps/desktop/src/main/ipc/chat.ts (confirm bifurcation is present at lines 187-197)
    - apps/desktop/src/main/index.ts (confirm voiceHandler startup wiring is present)
    - .planning/REQUIREMENTS.md (current ARCH-06 status)
  </read_first>
  <action>
Run automated pre-flight checks before starting manual E2E:

1. Run full desktop test suite:
   ```bash
   cd C:/jarvis && pnpm --filter desktop test --run
   ```
   All tests must pass. If any fail, stop and fix before proceeding.

2. Verify production code has the IPC bifurcation:
   ```bash
   grep -n "USE_WHISPER_CPP" C:/jarvis/apps/desktop/src/main/ipc/chat.ts
   grep -n "voiceHandler" C:/jarvis/apps/desktop/src/main/index.ts | head -10
   ```

3. Confirm .env has USE_WHISPER_CPP flag:
   ```bash
   grep "USE_WHISPER_CPP" C:/jarvis/.env 2>/dev/null || echo "Not in .env — will set inline for testing"
   ```

4. To test with USE_WHISPER_CPP=true, set it in `.env` (monorepo root):
   ```
   USE_WHISPER_CPP=true
   ```
   Then run: `pnpm --filter desktop dev` in a separate terminal.

Log all outputs. If suite fails or app fails to start, stop and report the error.
  </action>
  <verify>
    <automated>cd C:/jarvis && pnpm --filter desktop test --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter desktop test --run` exits 0 (all tests green, 0 failures)
    - `grep -c "USE_WHISPER_CPP" C:/jarvis/apps/desktop/src/main/ipc/chat.ts` returns 2 or more
    - `grep -c "voiceHandler" C:/jarvis/apps/desktop/src/main/index.ts` returns 3 or more
  </acceptance_criteria>
  <done>
    Full test suite green, production code confirmed present, .env ready for E2E verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E manual sign-off — both flag values</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/31-ipc-refactor-e2e-rollout/31-CONTEXT.md (success criteria)
    - .planning/REQUIREMENTS.md (ARCH-06 definition)
  </read_first>
  <action>
Prepare the desktop app for manual E2E testing with both flag values. The test sequence covers 4 scenarios:
- Test A: PTT with USE_WHISPER_CPP=true (local whisper.cpp path)
- Test B: Wake word with USE_WHISPER_CPP=true (full pipeline)
- Test C: Multi-turn voice follow-up (state preservation)
- Test D: USE_WHISPER_CPP=false killswitch (legacy path regression guard)

After human sign-off is received, update REQUIREMENTS.md to mark ARCH-06 as complete:
- Change `- [ ] **ARCH-06**` to `- [x] **ARCH-06**` in `.planning/REQUIREMENTS.md`
- Update `.planning/STATE.md`: set current_phase to 32, add Phase 31 to completed archive
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*ARCH-06\*\*" C:/jarvis/.planning/REQUIREMENTS.md</automated>
  </verify>
  <what-built>
IPC audio pipeline bifurcation (already implemented in Phases 29-30):
- With USE_WHISPER_CPP=true: renderer audio → IPC → main handleSendAudio → voiceHandler.handleAudio() → local whisper.cpp STT → gateway /api/chat (text) → TTS in main → audio response back via IPC
- With USE_WHISPER_CPP=false: original path preserved (renderer audio → IPC → main → HTTP POST /api/chat/audio → gateway → response)
  </what-built>
  <how-to-verify>
**Setup:** Ensure docker-compose is running (gateway + backend-ts needed for LLM calls). Have a microphone available.

**Test A — USE_WHISPER_CPP=true, PTT flow:**
1. Set USE_WHISPER_CPP=true in .env (monorepo root), restart Electron dev app
2. Press PTT hotkey (CmdOrCtrl+Space), speak a short phrase in pt-BR (e.g., "que horas são?")
3. Release PTT
4. EXPECTED: Orb transitions processing → responding, audio TTS plays, text appears in chat
5. Check main process logs for: `[IPC:chat:send-audio] USE_WHISPER_CPP=true — routing to local voiceHandler`
6. Check main process logs for: `[voice-handler] Transcribing with model:`
7. PASS if: audio response received, text in chat, logs show local path taken

**Test B — USE_WHISPER_CPP=true, Wake Word flow:**
1. Still with USE_WHISPER_CPP=true, say "Hey JARVIS" followed by a question
2. EXPECTED: wake burst → listening → processing → audio response
3. PASS if: full wake word cycle completes, audio response received

**Test C — Multi-turn voice preserved:**
1. Still with USE_WHISPER_CPP=true, complete Test A or B successfully
2. After TTS audio plays, speak a follow-up question within 8 seconds (no "Hey JARVIS")
3. EXPECTED: orb enters awaiting-followup state, follow-up question processed normally
4. PASS if: follow-up response received without needing wake word

**Test D — USE_WHISPER_CPP=false killswitch (regression guard):**
1. Set USE_WHISPER_CPP=false (or remove it) in .env, restart Electron dev app
2. Press PTT, speak, release
3. EXPECTED: same behavior as pre-Phase 31 (audio response via gateway HTTP path)
4. Check logs: NO `routing to local voiceHandler` message, gateway /api/chat/audio called
5. PASS if: response received identically to Phase 28 behavior

Report results for each test (A/B/C/D): PASS or FAIL with observed behavior.
  </how-to-verify>
  <acceptance_criteria>
    - Test A: PTT with USE_WHISPER_CPP=true → audio response received, logs confirm local voiceHandler path
    - Test B: Wake word with USE_WHISPER_CPP=true → full pipeline works end-to-end
    - Test C: Multi-turn voice → awaiting-followup state preserved on new IPC path
    - Test D: USE_WHISPER_CPP=false → original gateway path operational, no regression
    - REQUIREMENTS.md: `grep -c "[x] **ARCH-06**" .planning/REQUIREMENTS.md` returns 1
  </acceptance_criteria>
  <done>
    Human sign-off received for all 4 tests. ARCH-06 marked [x] in REQUIREMENTS.md. STATE.md updated to Phase 32.
  </done>
  <resume-signal>Type "approved" if all 4 tests pass. Type "partial: A,B,C pass" if Test D not needed urgently. Describe any failures with log output.</resume-signal>
</task>

</tasks>

<verification>
Phase 31 is complete when:
1. `pnpm --filter desktop test --run` exits 0 (all tests green)
2. Human sign-off received confirming all 4 E2E tests pass
3. `grep "\[x\] \*\*ARCH-06\*\*" .planning/REQUIREMENTS.md` returns a match
</verification>

<success_criteria>
- Human confirms all 4 E2E tests (A: PTT local, B: wake word local, C: multi-turn preserved, D: killswitch) pass
- ARCH-06 checkbox updated to [x] in REQUIREMENTS.md
- STATE.md updated: current_phase=32, Phase 31 in completed archive
- Phase 31 ready for Phase 32 cleanup (removal of /chat/audio endpoints and nodejs-whisper)
</success_criteria>

<output>
After completion, create `.planning/phases/31-ipc-refactor-e2e-rollout/31-02-SUMMARY.md`

Also update:
- `.planning/REQUIREMENTS.md`: mark ARCH-06 as `[x]`
- `.planning/STATE.md`: update current_phase to 32, status to planning, mark Phase 31 as Complete in archive
</output>
