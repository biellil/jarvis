---
phase: 55-llm-actions-tool-execution
plan: 02
type: execute
wave: 1
depends_on: [55-01]
files_modified:
  - apps/gateway/src/lib/path-validator.ts
  - apps/gateway/src/lib/action-dispatcher.ts
  - apps/gateway/src/routes/dispatch-action.ts
  - apps/gateway/src/app.ts
autonomous: true
requirements: [LACT-01, LACT-02, LACT-03, LACT-04, LACT-05]

must_haves:
  truths:
    - "ActionAckSchema aceita campo content? string opcional"
    - "sendActionRequest retorna { status, content? } em vez de só status string"
    - "POST /internal/dispatch-action existe, valida payload com Zod e chama sendActionRequest"
    - "Endpoint montado em /internal prefix no app Express do gateway"
    - "Tests da Wave 0 passam no estado GREEN para este plano"
  artifacts:
    - path: "apps/gateway/src/lib/path-validator.ts"
      provides: "ActionAckSchema com content?: string adicionado"
      contains: "content: z.string().optional()"
    - path: "apps/gateway/src/lib/action-dispatcher.ts"
      provides: "sendActionRequest com retorno { status, content? }"
      exports: ["sendActionRequest", "ActionDispatchRequest"]
    - path: "apps/gateway/src/routes/dispatch-action.ts"
      provides: "dispatchActionRouter com POST /dispatch-action"
      exports: ["dispatchActionRouter"]
    - path: "apps/gateway/src/app.ts"
      provides: "router montado em /internal"
      contains: "/internal"
  key_links:
    - from: "apps/gateway/src/routes/dispatch-action.ts"
      to: "apps/gateway/src/lib/action-dispatcher.ts"
      via: "sendActionRequest chamado no handler da rota"
      pattern: "sendActionRequest"
    - from: "apps/gateway/src/lib/ws-server.ts"
      to: "apps/gateway/src/lib/path-validator.ts"
      via: "ActionAckSchema.parse valida ACK recebido do Electron"
      pattern: "ActionAckSchema"
---

<objective>
Estender o gateway com o campo `content` no ACK schema, atualizar `sendActionRequest` para retornar `{ status, content? }`, e criar o endpoint `POST /internal/dispatch-action` que o backend-ts usará para solicitar ações via LangGraph tool.

Purpose: Bridge entre o LangGraph tool no backend-ts e o canal WS para o Electron. Sem este endpoint, a tool não tem como acionar o Electron.
Output: 3 arquivos modificados/criados; tests da Wave 0 (dispatch-action.test.ts) verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-llm-actions-tool-execution/55-CONTEXT.md
@.planning/phases/55-llm-actions-tool-execution/55-01-SUMMARY.md

<!-- Canonical refs do CONTEXT.md -->
@apps/gateway/src/lib/path-validator.ts
@apps/gateway/src/lib/action-dispatcher.ts
@apps/gateway/src/routes/chat.ts
</context>

<interfaces>
<!-- Estado atual dos arquivos-alvo. Executor deve ler antes de modificar. -->

From apps/gateway/src/lib/path-validator.ts (atual — a ser modificado):
```typescript
export const ActionAckSchema = z.object({
  type: z.literal('action_ack'),
  requestId: z.string().uuid(),
  status: z.enum(['confirmed', 'denied', 'timeout']),
  // ADICIONAR: content: z.string().optional()
});
export type ActionAck = z.infer<typeof ActionAckSchema>;
```

From apps/gateway/src/lib/action-dispatcher.ts (atual — retorno a ser modificado):
```typescript
export async function sendActionRequest(
  req: ActionDispatchRequest
): Promise<'confirmed' | 'denied' | 'timeout'>  // MUDAR para: Promise<{ status: ...; content?: string }>
// Linha 96: return ack.status;  → MUDAR para: return { status: ack.status, content: ack.content };
```

From apps/gateway/src/routes/chat.ts (padrão a seguir para novo router):
```typescript
import { Router } from 'express';
export const chatRouter = Router();
chatRouter.post('/chat', validate(ChatRequestSchema), async (req, res, next) => { ... });
```

From apps/gateway/src/lib/ws-server.ts (usa ActionAckSchema.parse — não quebrar):
Verificar que ActionAckSchema.parse() continua funcionando para ACKs sem content (campo é optional).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender ActionAckSchema com content? e atualizar sendActionRequest return type</name>
  <files>
    apps/gateway/src/lib/path-validator.ts
    apps/gateway/src/lib/action-dispatcher.ts
  </files>
  <read_first>
    - apps/gateway/src/lib/path-validator.ts (ActionAckSchema atual — adicionar content)
    - apps/gateway/src/lib/action-dispatcher.ts (sendActionRequest — linha 96 retorna ack.status, mudar para objeto)
    - apps/gateway/src/lib/ws-server.ts (verificar todos os usos de ActionAckSchema e ActionAck para garantir compatibilidade)
  </read_first>
  <behavior>
    - ActionAckSchema.parse({ type: 'action_ack', requestId: uuid, status: 'confirmed', content: 'text' }) → sucesso
    - ActionAckSchema.parse({ type: 'action_ack', requestId: uuid, status: 'confirmed' }) → sucesso (content é optional)
    - sendActionRequest(...) retorna Promise com { status: 'confirmed', content: 'texto' } para viewContent
    - sendActionRequest(...) retorna Promise com { status: 'confirmed', content: undefined } para openFolder
    - sendActionRequest(...) retorna Promise com { status: 'denied' } quando ACK nega
    - sendActionRequest(...) retorna Promise com { status: 'timeout' } quando timer expira (linha que rejeitava com timeout ainda lança Error — o caller do endpoint 55-03 captura)
  </behavior>
  <action>
**Em `apps/gateway/src/lib/path-validator.ts`:**

Adicionar `content: z.string().optional()` ao ActionAckSchema logo após o campo `status`:

```typescript
export const ActionAckSchema = z.object({
  type: z.literal('action_ack'),
  requestId: z.string().uuid(),
  status: z.enum(['confirmed', 'denied', 'timeout']),
  content: z.string().optional().describe('File content for viewContent actions only'),
});
```

**Em `apps/gateway/src/lib/action-dispatcher.ts`:**

1. Atualizar a assinatura da função na linha 24-26:
```typescript
export async function sendActionRequest(
  req: ActionDispatchRequest
): Promise<{ status: 'confirmed' | 'denied' | 'timeout'; content?: string }>
```

2. Atualizar o retorno na linha 96 (atualmente `return ack.status;`):
```typescript
  return {
    status: ack.status,
    content: ack.content,  // undefined para ações que não são viewContent
  };
```

3. O timeout path (linha ~64) lança `reject(new Error(...))` — esse Error vai ser capturado pelo try/catch do novo endpoint. NÃO mudar o comportamento de timeout aqui — o endpoint trata isso retornando HTTP 500.

Verificar: O tipo `ActionAck` em `ws-server.ts` que usa `ActionAckSchema` é inferido automaticamente e passa a incluir `content?` — sem mudança necessária em ws-server.ts.
  </action>
  <verify>
    <automated>cd C:/Users/biel1/OneDrive/Documentos/GitHub/jarvis && npx tsc --project apps/gateway/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - path-validator.ts contém `content: z.string().optional()`
    - action-dispatcher.ts tem return type `Promise<{ status: ...; content?: string }>`
    - action-dispatcher.ts linha de retorno é `return { status: ack.status, content: ack.content }`
    - TypeScript compila sem erros no gateway
  </done>
  <acceptance_criteria>
    - apps/gateway/src/lib/path-validator.ts contains "content: z.string().optional()"
    - apps/gateway/src/lib/action-dispatcher.ts contains "content?: string"
    - apps/gateway/src/lib/action-dispatcher.ts contains "content: ack.content"
    - `npx tsc --project apps/gateway/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar POST /internal/dispatch-action e montar no app Express</name>
  <files>
    apps/gateway/src/routes/dispatch-action.ts
    apps/gateway/src/app.ts
  </files>
  <read_first>
    - apps/gateway/src/routes/chat.ts (padrão Router + async handler)
    - apps/gateway/src/app.ts (como os routers existentes são montados — usar mesmo padrão)
    - apps/gateway/src/lib/action-dispatcher.ts (sendActionRequest atualizado — Task 1 deste plano)
  </read_first>
  <behavior>
    - POST /internal/dispatch-action com payload válido → chama sendActionRequest e retorna JSON { status, content? }
    - POST /internal/dispatch-action com payload inválido → 400 { error: 'Invalid payload' }
    - POST /internal/dispatch-action quando sendActionRequest lança (CLIENT_NOT_CONNECTED, timeout) → 500 { error: message }
    - Rota não exposta externamente (prefix /internal, fora do proxy do gateway para usuários)
  </behavior>
  <action>
**Criar `apps/gateway/src/routes/dispatch-action.ts`:**

```typescript
/**
 * POST /internal/dispatch-action — Phase 55 (LACT-01..05)
 *
 * Internal-only endpoint (not proxied externally) called by backend-ts
 * LangGraph tool to request OS-level file actions via the Electron client.
 *
 * Calls sendActionRequest which validates path, sends WS action_request,
 * awaits ACK from Electron (12s timeout), and logs to audit log.
 *
 * Per D-08: Follows /internal/actions-log pattern from Phase 54.
 * Per D-09: backend-ts fetches this with 12s+ timeout matching sendActionRequest.
 */
import { Router } from 'express';
import { z } from 'zod';
import { sendActionRequest } from '../lib/action-dispatcher.js';
import { logger } from '../lib/logger.js';

export const dispatchActionRouter = Router();

const DispatchActionBodySchema = z.object({
  clientId: z.string().min(1),
  action: z.enum(['openFolder', 'openFile', 'closeFile', 'viewContent']),
  path: z.string().min(1),
  model: z.string().min(1),
});

dispatchActionRouter.post('/dispatch-action', async (req, res) => {
  const parsed = DispatchActionBodySchema.safeParse(req.body);
  if (!parsed.success) {
    logger.warn({ issues: parsed.error.issues }, '/internal/dispatch-action: invalid payload');
    res.status(400).json({ error: 'Invalid payload' });
    return;
  }

  try {
    const result = await sendActionRequest(parsed.data);
    res.json(result);
  } catch (err) {
    const message = (err as Error).message;
    logger.error({ err }, '/internal/dispatch-action: sendActionRequest failed');
    res.status(500).json({ error: message });
  }
});
```

**Em `apps/gateway/src/app.ts`:**

Ler o arquivo atual e adicionar o import e o router mount no ponto onde outros routers internos estão montados. Seguir o padrão dos routers existentes. Adicionar:

```typescript
import { dispatchActionRouter } from './routes/dispatch-action.js';
```

E no corpo do `createApp()` ou equivalente, após os outros routers:

```typescript
app.use('/internal', dispatchActionRouter);
```

Verificar se já existe `/internal` prefix montado (para o actions-log do Phase 54). Se sim, checar se pode reutilizar o mesmo mount ou precisa de um segundo `app.use('/internal', ...)`. Express suporta múltiplos mounts no mesmo prefix.
  </action>
  <verify>
    <automated>cd C:/Users/biel1/OneDrive/Documentos/GitHub/jarvis && npx vitest run apps/gateway/src/__tests__/dispatch-action.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - dispatch-action.ts existe e exporta dispatchActionRouter
    - app.ts monta dispatchActionRouter em /internal
    - 4 tests da Wave 0 passam (GREEN)
    - TypeScript compila sem erros
  </done>
  <acceptance_criteria>
    - apps/gateway/src/routes/dispatch-action.ts exists
    - File contains "dispatchActionRouter" export
    - File contains "DispatchActionBodySchema" with action enum
    - apps/gateway/src/app.ts contains "dispatchActionRouter" import
    - apps/gateway/src/app.ts contains "/internal"
    - `npx vitest run apps/gateway/src/__tests__/dispatch-action.test.ts` exits 0 (all GREEN)
    - `npx tsc --project apps/gateway/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Comandos finais:
```bash
npx vitest run apps/gateway/src/__tests__/dispatch-action.test.ts
npx tsc --project apps/gateway/tsconfig.json --noEmit
```
Ambos devem sair com código 0.
</verification>

<success_criteria>
- ActionAckSchema inclui `content: z.string().optional()`
- sendActionRequest retorna `{ status, content? }` em vez de string simples
- POST /internal/dispatch-action existe e responde com 200/400/500 conforme esperado
- Todos os tests da Wave 0 para este escopo (dispatch-action.test.ts) estão GREEN
- TypeScript do gateway compila limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/55-llm-actions-tool-execution/55-02-SUMMARY.md`
</output>
