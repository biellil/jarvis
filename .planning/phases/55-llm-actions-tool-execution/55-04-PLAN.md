---
phase: 55-llm-actions-tool-execution
plan: 04
type: execute
wave: 2
depends_on: [55-02, 55-03]
files_modified:
  - apps/backend-ts/src/session/request-file-action.ts
  - apps/backend-ts/src/session/tools.ts
  - apps/backend-ts/src/session/chat-session.ts
autonomous: true
requirements: [LACT-01, LACT-02, LACT-03, LACT-04, LACT-05]

must_haves:
  truths:
    - "LangGraph tool request_file_action existe e é registrada no agent do ChatSession"
    - "Tool faz POST a http://localhost:3000/internal/dispatch-action com { clientId, action, path, model }"
    - "Tool retorna string com conteúdo do arquivo para viewContent confirmado"
    - "Tool retorna string de erro em vez de propagar exceção (padrão recall_memory)"
    - "clientId é injetado via factory function createRequestFileActionTool(clientId)"
    - "ChatSession.create aceita clientId opcional em ChatSessionOptions; sem clientId, tool não é registrada"
    - "Tests da Wave 0 (request-file-action.test.ts) passam no GREEN"
  artifacts:
    - path: "apps/backend-ts/src/session/request-file-action.ts"
      provides: "createRequestFileActionTool factory function"
      exports: ["createRequestFileActionTool"]
    - path: "apps/backend-ts/src/session/tools.ts"
      provides: "re-exporta createRequestFileActionTool para uso externo"
      contains: "createRequestFileActionTool"
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "ChatSessionOptions.clientId? e registro condicional da tool"
      contains: "clientId"
  key_links:
    - from: "apps/backend-ts/src/session/chat-session.ts"
      to: "apps/backend-ts/src/session/request-file-action.ts"
      via: "createRequestFileActionTool(opts.clientId) na factory de ChatSession"
      pattern: "createRequestFileActionTool"
    - from: "apps/backend-ts/src/session/request-file-action.ts"
      to: "apps/gateway/src/routes/dispatch-action.ts"
      via: "fetch('http://localhost:3000/internal/dispatch-action', { method: 'POST' })"
      pattern: "dispatch-action"
---

<objective>
Criar a LangGraph tool `request_file_action` no backend-ts e registrá-la no ChatSession. Esta tool é o ponto de entrada do LLM para solicitar ações de arquivo — ela faz fetch para o gateway que, por sua vez, envia o pedido ao Electron via WebSocket.

Purpose: Sem esta tool, o LLM não tem como iniciar ações de arquivo. Este é o último elo do pipeline LLM → gateway → Electron.
Output: 1 novo arquivo, 2 arquivos modificados; tests da Wave 0 (request-file-action.test.ts) verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-llm-actions-tool-execution/55-CONTEXT.md
@.planning/phases/55-llm-actions-tool-execution/55-02-SUMMARY.md
@.planning/phases/55-llm-actions-tool-execution/55-03-SUMMARY.md

@apps/backend-ts/src/session/tools.ts
@apps/backend-ts/src/session/chat-session.ts
</context>

<interfaces>
<!-- Contratos existentes a seguir. -->

From apps/backend-ts/src/session/tools.ts (padrão createRecallMemoryTool):
```typescript
import { tool } from '@langchain/core/tools';
import { z } from 'zod';

export function createRecallMemoryTool(memory: MemoryManager) {
  return tool(
    async ({ query }: { query: string }): Promise<string> => {
      try {
        const ctx = await memory.buildContext(query);
        return ctx === '' ? EMPTY_FALLBACK : ctx;
      } catch (exc) {
        return `Erro ao buscar memórias: ${(exc as Error).message}`;
      }
    },
    {
      name: 'recall_memory',
      description: '...',
      schema: recallSchema,
    },
  );
}
```

From apps/backend-ts/src/session/chat-session.ts (ChatSessionOptions e create):
```typescript
export interface ChatSessionOptions {
  llm: BaseChatModel;
  memory: MemoryManager;
  toolLogger?: ToolLogger;
  // Phase 55 ADICIONAR: clientId?: string
}

static async create(opts: ChatSessionOptions): Promise<ChatSession> {
  // ...
  const recallMemoryTool = createRecallMemoryTool(opts.memory);
  // Phase 55: se opts.clientId, também criar createRequestFileActionTool(opts.clientId)
  const agent = createReactAgent({
    llm: opts.llm,
    tools: [recallMemoryTool, ...pcToolsWrapped],
    // Phase 55: adicionar requestFileActionTool ao array se clientId presente
  });
}
```

Gateway URL: A tool deve usar `process.env['GATEWAY_URL'] ?? 'http://localhost:3000'` (não hardcoded, por D-09 via env).
Note: Gateway URL para HTTP usa porta 3000; o env var GATEWAY_URL pode ser 'ws://localhost:3000' — tool deve normalizar para http://.

clientId propagation (D-10): ChatSession.create recebe opts.clientId? como string opcional.
Se undefined → requestFileActionTool não é criada → agent não tem a tool (graceful degradation).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar createRequestFileActionTool e exportar de tools.ts</name>
  <files>
    apps/backend-ts/src/session/request-file-action.ts
    apps/backend-ts/src/session/tools.ts
  </files>
  <read_first>
    - apps/backend-ts/src/session/tools.ts (padrão completo de createRecallMemoryTool — replicar estrutura)
    - apps/backend-ts/src/session/__tests__/request-file-action.test.ts (stubs criados na Wave 0 — tool deve satisfazer)
  </read_first>
  <behavior>
    - tool.name = 'request_file_action'
    - tool.schema = { action: enum(openFolder, openFile, closeFile, viewContent), path: string }
    - Executa: POST http://localhost:3000/internal/dispatch-action com { clientId, action, path, model: 'unknown' }
    - status=confirmed sem content → retorna 'Ação confirmada e executada.'
    - status=confirmed com content → retorna 'Arquivo lido. Conteúdo:\n{content}'
    - status=denied → retorna 'Ação negada: {content ?? "usuário recusou"}'
    - status=timeout → retorna 'Ação timeout — sem resposta do usuário.'
    - fetch lança (network error, AbortSignal timeout) → retorna 'Erro ao executar ação: {message}' (nunca propaga)
    - AbortSignal.timeout(13_000) — 1s acima dos 12s do sendActionRequest para margem
  </behavior>
  <action>
**Criar `apps/backend-ts/src/session/request-file-action.ts`:**

```typescript
/**
 * request_file_action — Phase 55 LangGraph tool (LACT-01..05)
 *
 * Direct execution tool (like recall_memory, NOT payload-based like PC tools).
 * Per D-11: does not pass through wrapPcTool.
 * Per D-03: content flows Electron → gateway (WS ACK) → this tool → LLM.
 * Per D-09: fetches http://localhost:3000/internal/dispatch-action with 13s timeout.
 */
import { tool } from '@langchain/core/tools';
import { z } from 'zod';

const REQUEST_FILE_ACTION_DESCRIPTION =
  'Abre pastas ou arquivos no app padrão, fecha aplicativos por nome de processo, ou lê o conteúdo de arquivos texto (< 1MB) no chat. ' +
  'ATENÇÃO: fecha TODOS os processos com o nome especificado (limitação MVP). ' +
  'Requer confirmação do usuário via toast (timeout 10s = ação abortada silenciosamente). ' +
  'Paths devem estar dentro de: home, Downloads, Documents, Desktop.';

const requestFileActionSchema = z.object({
  action: z
    .enum(['openFolder', 'openFile', 'closeFile', 'viewContent'])
    .describe(
      'Tipo de ação: openFolder (abrir pasta), openFile (abrir arquivo no app padrão), ' +
      'closeFile (fechar app por nome de processo, ex: notepad.exe), ' +
      'viewContent (ler conteúdo de arquivo texto < 1MB)',
    ),
  path: z
    .string()
    .describe(
      'Para openFolder/openFile/viewContent: path absoluto do arquivo ou pasta (deve estar em ~/Downloads, ~/Documents, ~/Desktop ou home). ' +
      'Para closeFile: nome do processo a matar (ex: notepad.exe, code, chrome).',
    ),
});

interface DispatchResult {
  status: 'confirmed' | 'denied' | 'timeout';
  content?: string;
}

function getGatewayHttpUrl(): string {
  const raw = process.env['GATEWAY_URL'] ?? 'http://localhost:3000';
  // Normalize ws:// → http:// if env var uses WebSocket scheme
  return raw.replace(/^ws:\/\//i, 'http://').replace(/^wss:\/\//i, 'https://');
}

export function createRequestFileActionTool(clientId: string) {
  const gatewayUrl = getGatewayHttpUrl();

  return tool(
    async ({ action, path }: { action: string; path: string }): Promise<string> => {
      try {
        const response = await fetch(`${gatewayUrl}/internal/dispatch-action`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ clientId, action, path, model: 'unknown' }),
          signal: AbortSignal.timeout(13_000), // 1s margin above sendActionRequest 12s
        });

        if (!response.ok) {
          const err = await response.json().catch(() => ({ error: `HTTP ${response.status}` }));
          return `Erro ao executar ação: ${(err as { error?: string }).error ?? 'erro desconhecido'}`;
        }

        const result = (await response.json()) as DispatchResult;

        if (result.status === 'confirmed') {
          if (result.content) {
            return `Arquivo lido. Conteúdo:\n${result.content}`;
          }
          return 'Ação confirmada e executada.';
        }
        if (result.status === 'denied') {
          return `Ação negada: ${result.content ?? 'usuário recusou ou execução falhou'}`;
        }
        return 'Ação timeout — sem resposta do usuário.';
      } catch (err) {
        return `Erro ao executar ação: ${(err as Error).message}`;
      }
    },
    {
      name: 'request_file_action',
      description: REQUEST_FILE_ACTION_DESCRIPTION,
      schema: requestFileActionSchema,
    },
  );
}
```

**Atualizar `apps/backend-ts/src/session/tools.ts`:**

Adicionar re-export ao final do arquivo (manter createRecallMemoryTool intacto):
```typescript
export { createRequestFileActionTool } from './request-file-action.js';
```
  </action>
  <verify>
    <automated>cd C:/Users/biel1/OneDrive/Documentos/GitHub/jarvis && npx vitest run apps/backend-ts/src/session/__tests__/request-file-action.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>
    - request-file-action.ts existe e exporta createRequestFileActionTool
    - tools.ts re-exporta createRequestFileActionTool
    - 5 tests da Wave 0 passam no GREEN
    - TypeScript do backend-ts compila sem erros
  </done>
  <acceptance_criteria>
    - apps/backend-ts/src/session/request-file-action.ts exists
    - File contains "createRequestFileActionTool"
    - File contains "request_file_action" (tool name)
    - File contains "AbortSignal.timeout(13_000)"
    - File contains "getGatewayHttpUrl" or "GATEWAY_URL"
    - File contains "result.content" (for viewContent path)
    - apps/backend-ts/src/session/tools.ts contains "createRequestFileActionTool"
    - `npx vitest run apps/backend-ts/src/session/__tests__/request-file-action.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar request_file_action no ChatSession com clientId opcional</name>
  <files>
    apps/backend-ts/src/session/chat-session.ts
  </files>
  <read_first>
    - apps/backend-ts/src/session/chat-session.ts (ChatSessionOptions interface + create() factory — ler inteiro antes de modificar)
    - apps/backend-ts/src/session/request-file-action.ts (createRequestFileActionTool criado na Task 1)
  </read_first>
  <behavior>
    - ChatSessionOptions ganha campo `clientId?: string` opcional
    - Se opts.clientId está presente: createRequestFileActionTool(opts.clientId) é criada e adicionada ao array de tools do agent
    - Se opts.clientId está ausente: tool não é criada; agent tem apenas recall_memory + pcToolsWrapped (sem regressão)
    - A tool request_file_action NÃO passa por wrapAllPcTools — é adicionada diretamente ao array de tools (per D-11)
  </behavior>
  <action>
**Modificar `apps/backend-ts/src/session/chat-session.ts`:**

1. Adicionar import ao topo (junto com os outros imports de tools):
```typescript
import { createRequestFileActionTool } from './request-file-action.js';
```

2. Adicionar `clientId?` a ChatSessionOptions:
```typescript
export interface ChatSessionOptions {
  llm: BaseChatModel;
  memory: MemoryManager;
  toolLogger?: ToolLogger;
  /** Phase 55 (D-10): clientId do Electron para a LangGraph tool request_file_action.
   *  Se omitido, a tool não é registrada (graceful degradation). */
  clientId?: string;
}
```

3. Dentro de `create()`, após `const pcToolsWrapped = wrapAllPcTools(...)` e antes do `createReactAgent`, adicionar:
```typescript
    // Phase 55 (LACT-01..05): request_file_action tool — only when clientId is available.
    // Per D-11: direct execution tool, NOT wrapped via wrapAllPcTools.
    const allTools = opts.clientId
      ? [recallMemoryTool, ...pcToolsWrapped, createRequestFileActionTool(opts.clientId)]
      : [recallMemoryTool, ...pcToolsWrapped];
```

4. Atualizar a chamada de createReactAgent para usar `allTools`:
```typescript
    const agent = createReactAgent({
      llm: opts.llm,
      tools: allTools,
      prompt: SYSTEM_PROMPT,
    }) as unknown as ReactAgentLike;
```

ATENÇÃO: Ler o arquivo inteiro antes de modificar. A linha `tools: [recallMemoryTool, ...pcToolsWrapped]` está dentro do `create()`. Substituir APENAS essa linha/expressão — não reescrever o arquivo inteiro.
  </action>
  <verify>
    <automated>cd C:/Users/biel1/OneDrive/Documentos/GitHub/jarvis && npx tsc --project apps/backend-ts/tsconfig.json --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ChatSessionOptions tem clientId?: string
    - ChatSession.create cria requestFileActionTool quando clientId presente
    - Tool é adicionada diretamente ao array (não via wrapAllPcTools)
    - TypeScript compila sem erros
  </done>
  <acceptance_criteria>
    - apps/backend-ts/src/session/chat-session.ts contains "clientId?: string"
    - apps/backend-ts/src/session/chat-session.ts contains "createRequestFileActionTool"
    - apps/backend-ts/src/session/chat-session.ts contains "opts.clientId"
    - apps/backend-ts/src/session/chat-session.ts does NOT contain "wrapAllPcTools" applied to "createRequestFileActionTool" (tool is added directly)
    - `npx tsc --project apps/backend-ts/tsconfig.json --noEmit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
Comandos finais:
```bash
npx vitest run apps/backend-ts/src/session/__tests__/request-file-action.test.ts
npx tsc --project apps/backend-ts/tsconfig.json --noEmit
```
Ambos devem sair com código 0.
</verification>

<success_criteria>
- createRequestFileActionTool existe e segue o padrão de recall_memory
- Tool faz fetch para /internal/dispatch-action com clientId, action, path, model
- ChatSession aceita clientId opcional e registra a tool quando presente
- Sem clientId: comportamento anterior preservado (sem regressão)
- Todos os 5 tests da Wave 0 (request-file-action.test.ts) estão GREEN
- TypeScript do backend-ts compila limpo
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/55-llm-actions-tool-execution/55-04-SUMMARY.md`
</output>
