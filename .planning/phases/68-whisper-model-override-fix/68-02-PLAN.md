---
phase: 68-whisper-model-override-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx
  - apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
autonomous: true
requirements:
  - WBUG-01

must_haves:
  truths:
    - "Dropdown Whisper Model na Settings UI não exibe opção 'Auto (by VRAM)'"
    - "Dropdown exibe exatamente 4 opções: Tiny, Base, Medium, Large v3 Turbo"
    - "Estado inicial do SettingsLayout usa 'base' como default (não 'auto')"
    - "Helper text 'Auto: model selected based on available VRAM' nunca aparece"
  artifacts:
    - path: "apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx"
      provides: "WHISPER_OPTIONS sem entrada 'auto'"
      contains: "WHISPER_OPTIONS"
    - path: "apps/desktop/src/renderer/src/settings/SettingsLayout.tsx"
      provides: "useState inicial 'base' para whisperModel"
      contains: "useState<WhisperModelOption>('base')"
  key_links:
    - from: "apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx"
      to: "apps/desktop/src/shared/ipc-types.ts"
      via: "import type { WhisperModelOption }"
      pattern: "WhisperModelOption"
---

<objective>
Remover a opção 'auto' do dropdown de modelo Whisper na Settings UI. O usuário escolhe explicitamente entre Tiny, Base, Medium e Large v3 Turbo. O estado inicial do SettingsLayout passa de 'auto' para 'base'.

Purpose: D-03 — sem modo 'auto' na UI. D-04 — default 'base'. A mudança de tipo em ipc-types.ts (Plan 01) vai gerar erro TypeScript nestes arquivos se 'auto' não for removido aqui.
Output: 2 arquivos renderer modificados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-whisper-model-override-fix/68-CONTEXT.md
</context>

<interfaces>
<!-- Contratos existentes que o executor DEVE usar. -->

De apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx (estado atual):
```typescript
const WHISPER_OPTIONS: { label: string; value: WhisperModelOption }[] = [
  { label: 'Auto (by VRAM)',        value: 'auto' },       // ← REMOVER
  { label: 'Tiny (~75 MB)',         value: 'tiny' },
  { label: 'Base (~142 MB)',        value: 'base' },
  { label: 'Small (~244 MB)',       value: 'small' },
  { label: 'Medium (~1.5 GB)',      value: 'medium' },
  { label: 'Large v3 Turbo (~809 MB)', value: 'large-v3-turbo' },
];
// MODEL_PROGRESS_LABELS também tem 'auto' entry — remover
const MODEL_PROGRESS_LABELS: Record<string, string> = {
  auto: 'Auto',      // ← REMOVER
  ...
};
// helperText branch 'auto' — remover (linha ~62-64):
// } else if (whisperModel === 'auto') {
//   helperText = 'Auto: model selected based on available VRAM';
// }
```

De apps/desktop/src/renderer/src/settings/SettingsLayout.tsx (estado atual):
```typescript
const [whisperModel, setWhisperModel] = useState<WhisperModelOption>('auto'); // linha 98 — mudar para 'base'
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remover 'auto' de WhisperSection.tsx e SettingsLayout.tsx</name>
  <read_first>
    - apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx (arquivo completo)
    - apps/desktop/src/renderer/src/settings/SettingsLayout.tsx linhas 90-105 (useState whisperModel)
  </read_first>
  <files>
    apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx,
    apps/desktop/src/renderer/src/settings/SettingsLayout.tsx
  </files>
  <action>
**1. WhisperSection.tsx — remover 'auto' de todos os lugares (D-03):**

a) `WHISPER_OPTIONS`: remover a entrada `{ label: 'Auto (by VRAM)', value: 'auto' }`. O array deve ficar com 4 entradas: tiny, base, small, medium, large-v3-turbo. Nota: 'small' continua na UI mesmo mapeando para 'base' internamente — isso é correto per D-12 (o usuário vê 'Small' como opção; o backend silenciosamente usa 'base').

WHISPER_OPTIONS resultado esperado:
```typescript
const WHISPER_OPTIONS: { label: string; value: WhisperModelOption }[] = [
  { label: 'Tiny (~75 MB)',            value: 'tiny' },
  { label: 'Base (~142 MB)',           value: 'base' },
  { label: 'Small (~244 MB)',          value: 'small' },
  { label: 'Medium (~1.5 GB)',         value: 'medium' },
  { label: 'Large v3 Turbo (~809 MB)', value: 'large-v3-turbo' },
];
```

b) `MODEL_PROGRESS_LABELS`: remover a entrada `auto: 'Auto'`.

c) Helper text logic (linhas ~58-66): remover o branch `else if (whisperModel === 'auto')`. O bloco de helper text fica:
```typescript
let helperText: string;
if (downloadState?.status === 'success') {
  const modelLabel = MODEL_PROGRESS_LABELS[whisperModel] ?? whisperModel;
  helperText = `Model: ${modelLabel} (ready)`;
} else {
  helperText = `Manual: ${whisperModel}`;
}
```

**2. SettingsLayout.tsx — alterar default inicial (D-04):**

Localizar linha ~98:
```typescript
const [whisperModel, setWhisperModel] = useState<WhisperModelOption>('auto');
```
Alterar para:
```typescript
// Phase 68 D-04: default 'base' — 'auto' removido (D-03)
const [whisperModel, setWhisperModel] = useState<WhisperModelOption>('base');
```

Nenhuma outra mudança em SettingsLayout.tsx. O `setWhisperModel(data.whisperModelOverride)` na linha ~154 continua correto — recebe `WhisperModelOption` do store (que normaliza 'auto' → 'base' via Plan 01).
  </action>
  <verify>
    <automated>grep -n "auto" /root/jarvis/apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep "'auto'" apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx retorna vazio
    - grep "WHISPER_OPTIONS" apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx seguido por leitura confirma 5 entradas (tiny/base/small/medium/large-v3-turbo)
    - grep "Auto: model selected" apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx retorna vazio
    - grep "useState.*WhisperModelOption.*'base'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx retorna match
    - grep "useState.*WhisperModelOption.*'auto'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx retorna vazio
  </acceptance_criteria>
  <done>Dropdown não exibe 'Auto'; estado inicial é 'base'; helperText não menciona auto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer state → IPC save | whisperModelOverride enviado ao main não deve mais incluir 'auto' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04 | Tampering | WhisperSection.tsx onValueChange | accept | cast `v as WhisperModelOption` — após D-03 o tipo não inclui 'auto'; Radix Select só emite valores de SelectItem presentes, todos válidos |
| T-68-05 | Spoofing | SettingsLayout useState default | accept | Default 'base' é valor válido; não há forma de injetar 'auto' via UI após remoção do SelectItem |
</threat_model>

<verification>
Após execução deste plano:
1. `grep "'auto'" apps/desktop/src/renderer/src/settings/sections/WhisperSection.tsx` — vazio
2. `grep "useState.*'auto'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx` — vazio
3. `grep "'base'" apps/desktop/src/renderer/src/settings/SettingsLayout.tsx | grep useState` — tem match
4. WHISPER_OPTIONS tem 5 linhas (tiny, base, small, medium, large-v3-turbo)
</verification>

<success_criteria>
- Dropdown de modelo Whisper exibe 4 opções explícitas (sem Auto)
- Estado inicial do SettingsLayout é 'base'
- Helper text não menciona VRAM ou auto
- Sem erros TypeScript causados pela remoção de 'auto' do tipo (depende de Plan 01 aplicado)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/68-whisper-model-override-fix/68-02-SUMMARY.md`
</output>
