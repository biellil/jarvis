---
phase: 68-whisper-model-override-fix
plan: 03
type: tdd
wave: 2
depends_on:
  - 68-01
files_modified:
  - apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts
  - apps/desktop/src/main/__tests__/selectWhisperModel.test.ts
  - apps/desktop/src/main/ipc/__tests__/settings.test.ts
autonomous: true
requirements:
  - WBUG-03

must_haves:
  truths:
    - "Todos os testes de whisper-model-resolver.test.ts passam (testes antigos + novos)"
    - "selectWhisperModel.test.ts cobre a matriz 5 UI options × 3 VRAM scenarios = 15 casos"
    - "D-10: para qualquer override explícito, resultado é OPTION_TO_MODEL[override] independente do vramModel"
    - "settings.test.ts não usa 'auto' como fixture — defaults atualizados para 'base'"
  artifacts:
    - path: "apps/desktop/src/main/__tests__/selectWhisperModel.test.ts"
      provides: "Teste da matriz completa de override × VRAM"
      contains: "selectWhisperModel"
    - path: "apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts"
      provides: "Testes estendidos — sem branch 'auto' em resolveWhisperModel"
      contains: "resolveWhisperModel"
    - path: "apps/desktop/src/main/ipc/__tests__/settings.test.ts"
      provides: "Fixtures atualizadas — sem 'auto' como valor mock"
      contains: "whisperModelOverride"
  key_links:
    - from: "apps/desktop/src/main/__tests__/selectWhisperModel.test.ts"
      to: "apps/desktop/src/main/voiceInput/selectWhisperModel.ts"
      via: "dynamic import"
      pattern: "selectWhisperModel"
---

<objective>
Criar testes de regressão que garantam que o bug WBUG-01 não reincida. A matriz de testes (D-09/D-10) cobre as 5 UI options contra 3 cenários de VRAM, e garante que qualquer override explícito retorna OPTION_TO_MODEL[override] independente do vramModel recebido.

Purpose: WBUG-03 — teste automatizado previne regressão. Sem este plano, o bug pode ser reintroduzido silenciosamente.
Output: 3 arquivos de teste modificados/criados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-whisper-model-override-fix/68-CONTEXT.md
</context>

<interfaces>
<!-- Contratos dos módulos sendo testados (pós-Plan 01). -->

De apps/desktop/src/main/voiceInput/selectWhisperModel.ts (pós-Plan 01):
```typescript
import { OPTION_TO_MODEL } from './whisperModelResolver.js';
// OPTION_TO_MODEL: { tiny:'tiny', base:'base', small:'base', medium:'medium', 'large-v3-turbo':'large' }

export function selectWhisperModel(
  vramModel: WhisperModel,   // ignorado quando override explícito
  override: WhisperModelOption,  // 'tiny'|'base'|'small'|'medium'|'large-v3-turbo' (sem 'auto')
): WhisperModel;
// Comportamento esperado:
//   override='tiny'          → 'tiny'  (independente de vramModel)
//   override='base'          → 'base'  (independente de vramModel)
//   override='small'         → 'base'  (mapping D-12)
//   override='medium'        → 'medium'(independente de vramModel)
//   override='large-v3-turbo'→ 'large' (mapping D-12)
//   override='auto' (legado) → vramModel (branch defensivo)
```

De apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts (estado atual — 8 testes):
```typescript
// Os testes 'resolves auto with X MB vram' vão falhar se 'auto' foi removido do tipo.
// Manter como testes de comportamento (passam 'auto' como string) OU remover
// — decisão: remover os 3 testes de 'auto' pois resolveWhisperModel não recebe mais 'auto'.
// Adicionar: confirmação que todos os 5 explicit options resolvem corretamente (tiny/base/small/medium/large-v3-turbo).
```

De apps/desktop/src/main/ipc/__tests__/settings.test.ts linha 23 e 288:
```typescript
const getWhisperModelOverrideMock = vi.fn<[], 'auto' | 'tiny' | ...>(() => 'auto');
// linha 288: whisperModelOverride: 'auto'  ← fixture de default
// Ambos devem mudar para 'base'
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar selectWhisperModel.test.ts com matriz 5×3</name>
  <read_first>
    - apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts (padrão de test — vi.mock electron, dynamic import, beforeEach resetModules)
    - apps/desktop/src/main/voiceInput/selectWhisperModel.ts (assinatura e comportamento pós-Plan 01)
  </read_first>
  <files>apps/desktop/src/main/__tests__/selectWhisperModel.test.ts</files>
  <behavior>
    Matriz 5 UI options × 3 VRAM scenarios (D-09):
    - UI options: 'tiny', 'base', 'small', 'medium', 'large-v3-turbo'
    - VRAM scenarios (vramModel arg): 'tiny' (CPU), 'base' (GPU baixo), 'large' (GPU alto)

    Esperado para cada combo (D-10 — VRAM nunca sobrescreve override):
    - ('tiny', 'tiny') → 'tiny'          // VRAM CPU + override tiny
    - ('base', 'tiny') → 'tiny'          // VRAM GPU baixo + override tiny
    - ('large', 'tiny') → 'tiny'         // VRAM GPU alto + override tiny
    - ('tiny', 'base') → 'base'
    - ('base', 'base') → 'base'
    - ('large', 'base') → 'base'
    - ('tiny', 'small') → 'base'         // D-12 mapping
    - ('base', 'small') → 'base'
    - ('large', 'small') → 'base'        // GPU alto NÃO sobrescreve 'small'→'base'
    - ('tiny', 'medium') → 'medium'
    - ('base', 'medium') → 'medium'
    - ('large', 'medium') → 'medium'
    - ('tiny', 'large-v3-turbo') → 'large'   // D-12 mapping
    - ('base', 'large-v3-turbo') → 'large'
    - ('large', 'large-v3-turbo') → 'large'

    Caso adicional (branch defensivo):
    - ('medium', 'auto' as unknown as WhisperModelOption) → 'medium' // legado auto retorna vramModel
  </behavior>
  <action>
Criar o arquivo de teste seguindo o padrão de `whisper-model-resolver.test.ts`:
- Mock `electron` (necessário pois `selectWhisperModel.ts` importa de `vramDetection.ts` que importa `electron`)
- `vi.resetModules()` + `vi.restoreAllMocks()` em `beforeEach`
- Dynamic import em cada test (ou grupo via `beforeAll` dentro de describe)
- Usar `describe` para agrupar por override, `it` para cada VRAM scenario

Estrutura do arquivo:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

vi.mock('electron', () => ({
  app: { isPackaged: false },
}));

beforeEach(() => {
  vi.resetModules();
  vi.restoreAllMocks();
});

describe('selectWhisperModel', () => {
  describe('override: tiny — VRAM never overrides (D-10)', () => {
    it('vramModel=tiny → tiny', async () => { ... });
    it('vramModel=base → tiny', async () => { ... });
    it('vramModel=large → tiny', async () => { ... });
  });

  describe('override: base', () => { ... }); // 3 casos

  describe('override: small (maps to base via OPTION_TO_MODEL D-12)', () => {
    it('vramModel=tiny → base', async () => { ... });
    it('vramModel=base → base', async () => { ... });
    it('vramModel=large → base (D-10: VRAM alto não sobrescreve small→base)', async () => { ... });
  });

  describe('override: medium', () => { ... }); // 3 casos

  describe('override: large-v3-turbo (maps to large via OPTION_TO_MODEL D-12)', () => { ... }); // 3 casos

  describe('defensive: legacy auto override falls back to vramModel', () => {
    it('vramModel=medium, override=auto (legado) → medium', async () => {
      const { selectWhisperModel } = await import('../voiceInput/selectWhisperModel.js');
      expect(selectWhisperModel('medium', 'auto' as unknown as WhisperModelOption)).toBe('medium');
    });
  });
});
```
  </action>
  <verify>
    <automated>cd /root/jarvis && pnpm --filter desktop test -- --reporter=verbose --run src/main/__tests__/selectWhisperModel.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe em apps/desktop/src/main/__tests__/selectWhisperModel.test.ts
    - Todos os 16 testes passam (15 da matriz + 1 defensivo)
    - Nenhum teste tem status FAIL ou SKIP
    - grep "override.*large-v3-turbo.*large\|small.*base.*D-12" do arquivo confirma os casos críticos D-10/D-12
  </acceptance_criteria>
  <done>16 testes passando. Matriz 5×3 cobre todas as combinações UI option × VRAM.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atualizar whisper-model-resolver.test.ts e settings.test.ts</name>
  <read_first>
    - apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts (arquivo completo — remover/adaptar testes de 'auto')
    - apps/desktop/src/main/ipc/__tests__/settings.test.ts linhas 1-55 (mocks) e linhas 280-310 (fixtures com 'auto')
    - apps/desktop/src/main/voiceInput/whisperModelResolver.ts (pós-Plan 01 — confirmar se 'auto' branch ainda existe)
  </read_first>
  <files>
    apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts,
    apps/desktop/src/main/ipc/__tests__/settings.test.ts
  </files>
  <action>
**1. whisper-model-resolver.test.ts — adaptar testes de 'auto' (D-09):**

Os 3 testes atuais que testam `resolveWhisperModel('auto', vramMb)` vão falhar se `resolveWhisperModel` não aceita mais 'auto' (type narrowing). Verificar o estado do arquivo após Plan 01:

- Se `resolveWhisperModel` manteve o branch 'auto' como fallback defensivo: os testes podem permanecer usando `'auto' as unknown as WhisperModelOption`
- Se `resolveWhisperModel` não tem mais branch 'auto': remover os 3 testes de 'auto' e substituir por testes que confirmam que as 5 opções explícitas resolvem corretamente via OPTION_TO_MODEL

Adicionar 2 novos testes explícitos para garantir OPTION_TO_MODEL foi exportado corretamente:
```typescript
it('OPTION_TO_MODEL is exported (D-02)', async () => {
  const { OPTION_TO_MODEL } = await import('../voiceInput/whisperModelResolver.js');
  expect(OPTION_TO_MODEL).toBeDefined();
  expect(OPTION_TO_MODEL['small']).toBe('base');
  expect(OPTION_TO_MODEL['large-v3-turbo']).toBe('large');
});
```

**2. settings.test.ts — atualizar fixtures com 'auto' (D-07):**

Localizar todas as ocorrências de `'auto'` no contexto de `whisperModelOverride`:

a) Linha ~23 — tipo do mock:
```typescript
const getWhisperModelOverrideMock = vi.fn<[], 'auto' | 'tiny' | 'base' | 'small' | 'medium' | 'large-v3-turbo'>(() => 'auto');
```
Alterar para:
```typescript
const getWhisperModelOverrideMock = vi.fn<[], WhisperModelOption>(() => 'base');
```
Adicionar import de `WhisperModelOption` de `../../shared/ipc-types` no topo do arquivo se não presente.

b) Linha ~288 — fixture `returns defaults when store has no overrides`:
```typescript
whisperModelOverride: 'auto',
```
Alterar para:
```typescript
whisperModelOverride: 'base',
```

c) Verificar se há outros places onde `'auto'` aparece em contexto de whisper — buscar com `grep -n "auto" settings.test.ts` e avaliar cada ocorrência.

Nota: o teste `returns all four settings fields from store` (linha ~245) já usa `'base'` — verificar e deixar como está.
  </action>
  <verify>
    <automated>cd /root/jarvis && pnpm --filter desktop test -- --reporter=verbose --run src/main/__tests__/whisper-model-resolver.test.ts src/main/ipc/__tests__/settings.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test whisper-model-resolver.test.ts — todos os testes PASS (pelo menos 6 testes)
    - pnpm test settings.test.ts — todos os testes PASS (sem regressões)
    - grep "'auto'" apps/desktop/src/main/ipc/__tests__/settings.test.ts | grep "whisper\|WhisperModel\|getWhisperModelOverride" retorna vazio (sem fixtures 'auto')
    - grep "OPTION_TO_MODEL" apps/desktop/src/main/__tests__/whisper-model-resolver.test.ts retorna match (novo teste de export)
  </acceptance_criteria>
  <done>whisper-model-resolver.test.ts confirma export de OPTION_TO_MODEL; settings.test.ts sem fixtures 'auto'.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → mocked store | Fixtures erradas mascaram bugs reais — fixtures devem refletir estado pós-fix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-06 | Tampering | selectWhisperModel.test.ts matriz | mitigate | Testar explicitamente que 'large' VRAM não sobrescreve 'small' override (caso mais enganoso do bug original) |
| T-68-07 | Repudiation | settings.test.ts defaults | mitigate | Fixture default 'base' documenta o comportamento esperado pós-D-04; regressão seria imediata |
</threat_model>

<verification>
Após execução deste plano:
1. `cd /root/jarvis && pnpm --filter desktop test -- --run 2>&1 | tail -10` — todos os testes passam (zero failures)
2. `grep -c "it(" apps/desktop/src/main/__tests__/selectWhisperModel.test.ts` — pelo menos 16
3. `grep "'auto'" apps/desktop/src/main/ipc/__tests__/settings.test.ts | grep -i "whisper\|getWhisper"` — vazio
</verification>

<success_criteria>
- selectWhisperModel.test.ts com 16 testes passando (15 matriz + 1 defensivo)
- whisper-model-resolver.test.ts inclui teste de OPTION_TO_MODEL exportado
- settings.test.ts sem fixtures 'auto' para whisperModelOverride
- Zero regressões no suite completo de testes do desktop app
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/68-whisper-model-override-fix/68-03-SUMMARY.md`
</output>
