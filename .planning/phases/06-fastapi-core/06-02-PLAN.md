---
phase: 06-fastapi-core
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/jarvis/core/session.py
  - src/jarvis/api/__init__.py
  - src/jarvis/api/routes/chat.py
  - tests/api/test_chat.py
  - tests/api/test_stream.py
  - tests/api/conftest.py
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "POST /chat com {\"message\":\"oi\"} retorna JSON com response text do JARVIS"
    - "POST /chat com body vazio ou sem campo message retorna 422"
    - "GET /chat/stream?message=oi retorna text/event-stream com tokens incrementais"
    - "send_stream() no ChatSession yield tokens como async generator"
    - "send() original permanece 100% intocado — CLI nao muda"
    - "Requests concorrentes ao /chat retornam 429 (session lock)"
  artifacts:
    - path: "src/jarvis/api/routes/chat.py"
      provides: "POST /chat e GET /chat/stream endpoints"
      exports: ["router"]
      contains: "EventSourceResponse"
    - path: "src/jarvis/core/session.py"
      provides: "send_stream() metodo novo no ChatSession"
      contains: "async def send_stream"
    - path: "tests/api/test_chat.py"
      provides: "Testes do POST /chat"
      contains: "test_chat_post"
    - path: "tests/api/test_stream.py"
      provides: "Testes do GET /chat/stream SSE"
      contains: "test_chat_stream"
  key_links:
    - from: "src/jarvis/api/routes/chat.py"
      to: "src/jarvis/core/session.py"
      via: "request.app.state.session.send() e .send_stream()"
      pattern: "session\\.send"
    - from: "src/jarvis/api/routes/chat.py"
      to: "fastapi.sse"
      via: "EventSourceResponse para streaming SSE"
      pattern: "EventSourceResponse"
    - from: "src/jarvis/api/__init__.py"
      to: "src/jarvis/api/routes/chat.py"
      via: "app.include_router(chat_router)"
      pattern: "include_router.*chat"
---

<objective>
Implementar os endpoints de chat (POST /chat e GET /chat/stream) e o metodo send_stream() no ChatSession.

Purpose: Completar a camada HTTP do JARVIS — chat completo via API (API-01) e streaming SSE token-a-token (API-02), mantendo CLI 100% intacto.
Output: Endpoints de chat funcionais com testes, send_stream() no ChatSession, full regression green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fastapi-core/06-CONTEXT.md
@.planning/phases/06-fastapi-core/06-RESEARCH.md
@.planning/phases/06-fastapi-core/06-VALIDATION.md
@.planning/phases/06-fastapi-core/06-01-SUMMARY.md

@src/jarvis/core/session.py
@src/jarvis/config.py
@src/jarvis/api/__init__.py
@src/jarvis/api/lifespan.py
@src/jarvis/api/models.py
@src/jarvis/api/routes/health.py
@tests/api/conftest.py

<interfaces>
<!-- Interfaces from Plan 01 that this plan depends on -->

From src/jarvis/api/__init__.py (created by Plan 01):
```python
from fastapi import FastAPI
app = FastAPI(title="JARVIS API", lifespan=lifespan)
# health_router already included
# chat_router will be added by this plan
```

From src/jarvis/api/models.py (created by Plan 01):
```python
class ChatRequest(BaseModel):
    message: str

class ChatResponse(BaseModel):
    message: str
```

From src/jarvis/api/lifespan.py (created by Plan 01):
```python
# app.state.session = ChatSession instance
# app.state.db = MemoryStore instance
# app.state.vectors = MemoryVectors instance
```

From src/jarvis/core/session.py (existing):
```python
class ChatSession:
    async def send(self, user_input: str, image: str | None = None) -> str:
        # Lines 114-344 — complex method with tool calls, vision, compression
        # Uses print(token, end="", flush=True) for stdout streaming
        # Returns full response string
    
    async def save(self) -> None:
    
    # Attributes set in __init__:
    # self.llm, self.history, self._db, self._vectors
    # self._tools, self._tool_map, self._llm_with_tools
    # self._executor, self._screen_analyzer
```

From tests/api/conftest.py (created by Plan 01):
```python
# Fixtures: mock_session, mock_db, mock_vectors, client
# client fixture creates test_app with mocked state
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar send_stream() ao ChatSession</name>
  <files>src/jarvis/core/session.py, tests/api/test_stream.py</files>
  <read_first>
    src/jarvis/core/session.py,
    .planning/phases/06-fastapi-core/06-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: send_stream() yields tokens como async generator — mock LLM astream retorna 3 chunks ["Ola", " mundo", "!"], send_stream yields os 3 tokens na ordem
    - Test 2: send_stream() retorna TODOS os tokens que compoe a resposta completa — concatenar yields == resposta total
    - Test 3: send_stream() NAO chama print() — stdout deve ficar vazio durante streaming (diferente de send())
    - Test 4: send_stream() lida com erro no astream() — se LLM astream raisa exception, send_stream propaga a exception (nao hang forever)
  </behavior>
  <action>
Implementar `send_stream()` como novo metodo em `ChatSession` (per D-02). O metodo send() existente NAO deve ser modificado em nenhuma linha.

**Implementacao de send_stream() em src/jarvis/core/session.py:**

Adicionar APOS o metodo `send()` (depois da linha ~344) e ANTES de `_maybe_compress()`:

```python
async def send_stream(self, user_input: str) -> AsyncGenerator[str, None]:
    """Stream tokens one-by-one via async generator (API mode).
    
    Per D-02: Does NOT print to stdout. Yields each token as a string.
    Unlike send(), this method does NOT support image= or tool calls.
    Memory, profile extraction, and compression logic are identical to send().
    
    Uses asyncio.Queue internally to bridge the astream() loop with the
    async generator interface. Background task runs the full pipeline,
    putting tokens in the queue. Generator yields from the queue.
    """
    import asyncio
    from collections.abc import AsyncGenerator
    
    # Step 1: Compression check (same as send())
    await self._maybe_compress()
    
    # Step 2: Build augmented system prompt (same as send())
    augmented_system = SYSTEM_PROMPT
    if self._db:
        facts = self._db.get_profile_facts()
        if facts:
            facts_block = "\n".join(f"- {k}: {v}" for k, v, _, _ in facts)
            augmented_system += f"\n\nFatos sobre o usuario:\n{facts_block}"
    
    if self._vectors:
        try:
            memories = self._vectors.query_memories(user_input)
            if memories:
                memories_block = "\n".join(f"- {m}" for m in memories)
                augmented_system += f"\n\nMemorias relevantes de sessoes anteriores:\n{memories_block}"
        except Exception as e:
            logger.warning(f"Memory retrieval failed: {e}")
    
    messages_to_send = [SystemMessage(content=augmented_system)] + self.history[1:]
    
    # Step 3: Append HumanMessage (no image support in stream mode)
    human_msg = HumanMessage(content=user_input)
    self.history.append(human_msg)
    messages_to_send.append(human_msg)
    
    # Save user message
    if self._db and self._conv_id is not None:
        now = datetime.now(timezone.utc).isoformat()
        self._db.save_messages(self._conv_id, [("user", user_input, now)])
    
    # Step 4: Stream via asyncio.Queue (per D-02 pattern from RESEARCH)
    queue: asyncio.Queue[str | None] = asyncio.Queue()
    full_response = ""
    
    async def _run():
        nonlocal full_response
        try:
            # Use base llm (no tools) for streaming — tool calls not supported in stream mode
            async for chunk in self.llm.astream(messages_to_send):
                token = chunk.content
                if token:
                    await queue.put(token)
                    full_response += token
        finally:
            # CRITICAL (Pitfall 3 from RESEARCH): Always send sentinel even on error
            await queue.put(None)
    
    task = asyncio.create_task(_run())
    
    try:
        while True:
            token = await queue.get()
            if token is None:
                break
            yield token
    finally:
        await task  # Propagate exceptions from background task
    
    # Post-streaming: save AI message and extract profile (same as send())
    self.history.append(AIMessage(content=full_response))
    
    if self._db and self._conv_id is not None:
        now = datetime.now(timezone.utc).isoformat()
        self._db.save_messages(self._conv_id, [("assistant", full_response, now)])
    
    if self._db:
        try:
            facts = await extract_profile_facts(self.llm, user_input)
            source = "explicit" if is_explicit_profile_command(user_input) else "implicit"
            for key, value in facts.items():
                self._db.upsert_profile(key, value, source)
                if self._vectors:
                    self._vectors.add_memory(f"profile:{key}", f"{key}: {value}")
        except Exception as e:
            logger.warning(f"Profile extraction failed: {e}")
```

IMPORTANTE: Adicionar `AsyncGenerator` ao import de typing no topo do arquivo:
```python
from collections.abc import AsyncGenerator
```
Ou verificar se ja esta importado. Se `typing` ja importa algo, adicionar `AsyncGenerator` ao import de `collections.abc`.

NOTA sobre tool calls: send_stream() usa `self.llm.astream()` (sem tools) em vez de `self._llm_with_tools.astream()`. Isso e intencional para Phase 6 — tool streaming sera adicionado em fase futura (per RESEARCH open question 2).

**Testes em tests/api/test_stream.py** (RED primeiro, depois GREEN):

Os testes devem usar mocks do LLM astream para simular chunks. Pattern:
```python
import pytest
import asyncio
from unittest.mock import AsyncMock, MagicMock, patch

async def fake_astream(messages):
    """Simula LLM astream com 3 chunks."""
    for text in ["Ola", " mundo", "!"]:
        chunk = MagicMock()
        chunk.content = text
        yield chunk

@pytest.mark.anyio
async def test_send_stream_yields_tokens():
    """send_stream yields tokens na mesma ordem do LLM."""
    # Setup mock ChatSession com LLM mockado
    # ... (construir session com mocks, llm.astream = fake_astream)
    tokens = [t async for t in session.send_stream("oi")]
    assert tokens == ["Ola", " mundo", "!"]

@pytest.mark.anyio
async def test_send_stream_no_print(capsys):
    """send_stream NAO imprime no stdout (diferente de send())."""
    # ... session com mock LLM
    tokens = [t async for t in session.send_stream("oi")]
    captured = capsys.readouterr()
    assert captured.out == ""

@pytest.mark.anyio
async def test_send_stream_concatenation():
    """Concatenar todos os tokens de send_stream == resposta completa."""
    tokens = [t async for t in session.send_stream("oi")]
    assert "".join(tokens) == "Ola mundo!"

@pytest.mark.anyio
async def test_send_stream_error_propagation():
    """Se LLM astream raisa exception, send_stream propaga."""
    async def failing_astream(messages):
        raise RuntimeError("LLM error")
        yield  # make it a generator
    # ... session com failing astream
    with pytest.raises(RuntimeError, match="LLM error"):
        async for _ in session.send_stream("oi"):
            pass
```

Esses testes precisam construir um ChatSession com LLM mockado (sem DB, sem vectors para simplificar). A inicializacao minima e: `ChatSession(llm=mock_llm)` com `mock_llm.astream = fake_astream`.
  </action>
  <verify>
    <automated>python -m pytest tests/api/test_stream.py -x -q --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - src/jarvis/core/session.py contains `async def send_stream(self, user_input: str) -> AsyncGenerator[str, None]:`
    - src/jarvis/core/session.py send() method is UNCHANGED (diff shows NO modifications to send() lines 114-344)
    - src/jarvis/core/session.py send_stream() contains `asyncio.Queue`
    - src/jarvis/core/session.py send_stream() contains `await queue.put(None)` in a finally block
    - src/jarvis/core/session.py send_stream() does NOT contain `print(`
    - src/jarvis/core/session.py send_stream() uses `self.llm.astream` (NOT self._llm_with_tools)
    - tests/api/test_stream.py contains `test_send_stream_yields_tokens`
    - tests/api/test_stream.py contains `test_send_stream_no_print`
    - tests/api/test_stream.py contains `test_send_stream_error_propagation`
    - `python -m pytest tests/api/test_stream.py -x -q` exits 0
  </acceptance_criteria>
  <done>send_stream() funcional no ChatSession, yielding tokens via async generator sem print(). 4 testes passando. send() original intocado.</done>
</task>

<task type="auto">
  <name>Task 2: Criar endpoints de chat, registrar no app, e testes completos</name>
  <files>
    src/jarvis/api/routes/chat.py,
    src/jarvis/api/__init__.py,
    tests/api/conftest.py,
    tests/api/test_chat.py
  </files>
  <read_first>
    src/jarvis/api/__init__.py,
    src/jarvis/api/models.py,
    src/jarvis/api/routes/health.py,
    src/jarvis/core/session.py,
    tests/api/conftest.py,
    .planning/phases/06-fastapi-core/06-RESEARCH.md
  </read_first>
  <action>
**src/jarvis/api/routes/chat.py** — Chat endpoints (API-01, API-02):

```python
import asyncio
from collections.abc import AsyncIterable

from fastapi import APIRouter, Request, HTTPException
from fastapi.sse import EventSourceResponse, ServerSentEvent

from jarvis.api.models import ChatRequest, ChatResponse

router = APIRouter()

# Session lock — per RESEARCH open question 1: prevent concurrent sends
_session_lock = asyncio.Lock()

@router.post("/chat", response_model=ChatResponse)
async def chat(request: Request, body: ChatRequest) -> ChatResponse:
    """API-01: Send message and receive complete response.
    
    Wraps ChatSession.send(). Returns 429 if another request is in progress.
    """
    session = request.app.state.session
    if _session_lock.locked():
        raise HTTPException(status_code=429, detail="Session busy — try again later")
    async with _session_lock:
        response_text = await session.send(body.message)
    return ChatResponse(message=response_text)

@router.get("/chat/stream", response_class=EventSourceResponse)
async def chat_stream(
    request: Request, message: str
) -> AsyncIterable[ServerSentEvent]:
    """API-02: Stream tokens via Server-Sent Events.
    
    Per success criteria: GET /chat/stream?message=oi (query param, not POST body).
    Per D-02: Uses send_stream() which yields tokens without print().
    Returns 429 if another request is in progress.
    """
    session = request.app.state.session
    if _session_lock.locked():
        raise HTTPException(status_code=429, detail="Session busy — try again later")
    async with _session_lock:
        async for token in session.send_stream(message):
            yield ServerSentEvent(data=token)
```

NOTA sobre 429: O lock `_session_lock` e um asyncio.Lock por modulo. Funciona porque D-01 garante single worker. Se um request esta em andamento, o segundo recebe 429 imediatamente (nao espera). Isso e simples e correto para uso pessoal.

NOTA sobre SSE com lock: O `async with _session_lock` dentro do generator do `chat_stream` pode ter subtilezas. O lock e adquirido quando o primeiro token e gerado e liberado quando o generator termina. Verificar se isso funciona corretamente com FastAPI's EventSourceResponse. Se houver problemas, alternativa: adquirir lock antes de iniciar o generator e liberar apos.

**Atualizar src/jarvis/api/__init__.py** — Adicionar chat router:

Ler o arquivo atual e adicionar:
```python
from jarvis.api.routes.chat import router as chat_router
# ...
app.include_router(chat_router)
```

**Atualizar tests/api/conftest.py** — Adicionar fixture com chat router:

Atualizar a fixture `client` para incluir o chat_router tambem:
```python
@pytest.fixture
async def client(mock_session, mock_db, mock_vectors):
    from jarvis.api.routes.health import router as health_router
    from jarvis.api.routes.chat import router as chat_router
    from fastapi import FastAPI
    test_app = FastAPI()
    test_app.include_router(health_router)
    test_app.include_router(chat_router)
    test_app.state.session = mock_session
    test_app.state.db = mock_db
    test_app.state.vectors = mock_vectors
    async with AsyncClient(
        transport=ASGITransport(app=test_app),
        base_url="http://test",
    ) as ac:
        yield ac
```

**tests/api/test_chat.py** — Testes para API-01 e API-02:

```python
import pytest
from unittest.mock import AsyncMock, MagicMock

@pytest.mark.anyio
async def test_chat_post(client, mock_session):
    """API-01: POST /chat retorna JSON com response text."""
    mock_session.send = AsyncMock(return_value="Ola! Como posso ajudar?")
    response = await client.post("/chat", json={"message": "oi"})
    assert response.status_code == 200
    data = response.json()
    assert data["message"] == "Ola! Como posso ajudar?"
    mock_session.send.assert_called_once_with("oi")

@pytest.mark.anyio
async def test_chat_post_empty_body(client):
    """API-01: POST /chat sem body retorna 422."""
    response = await client.post("/chat")
    assert response.status_code == 422

@pytest.mark.anyio
async def test_chat_post_missing_message(client):
    """API-01: POST /chat com JSON sem campo message retorna 422."""
    response = await client.post("/chat", json={"text": "oi"})
    assert response.status_code == 422

@pytest.mark.anyio
async def test_chat_stream_content_type(client, mock_session):
    """API-02: GET /chat/stream retorna text/event-stream."""
    async def fake_send_stream(msg):
        yield "token1"
        yield "token2"
    mock_session.send_stream = fake_send_stream
    response = await client.get("/chat/stream", params={"message": "oi"})
    assert response.status_code == 200
    assert "text/event-stream" in response.headers["content-type"]

@pytest.mark.anyio
async def test_chat_stream_tokens(client, mock_session):
    """API-02: GET /chat/stream yields tokens como SSE events."""
    async def fake_send_stream(msg):
        for t in ["Ola", " mundo", "!"]:
            yield t
    mock_session.send_stream = fake_send_stream
    response = await client.get("/chat/stream", params={"message": "oi"})
    assert response.status_code == 200
    # SSE format: "data: token\n\n" per event
    body = response.text
    assert "Ola" in body
    assert " mundo" in body
    assert "!" in body

@pytest.mark.anyio
async def test_chat_stream_missing_message(client):
    """API-02: GET /chat/stream sem query param message retorna 422."""
    response = await client.get("/chat/stream")
    assert response.status_code == 422
```

NOTA sobre SSE testing com httpx: httpx AsyncClient nao tem suporte nativo para SSE streaming (nao emula EventSource). O `response.text` vai conter o body completo APOS o stream terminar. Isso e suficiente para testes unitarios — verificar que os tokens estao no body e que o content-type e correto. Teste de streaming real (tokens chegando incrementalmente) e manual per VALIDATION.md.

**Regression final:**
Rodar `python -m pytest tests/ -q --timeout=60` para confirmar que TODOS os testes (234 existentes + novos de API) passam.
  </action>
  <verify>
    <automated>python -m pytest tests/api/ -x -q --timeout=30 && python -m pytest tests/ -q --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - src/jarvis/api/routes/chat.py contains `@router.post("/chat", response_model=ChatResponse)`
    - src/jarvis/api/routes/chat.py contains `@router.get("/chat/stream", response_class=EventSourceResponse)`
    - src/jarvis/api/routes/chat.py contains `from fastapi.sse import EventSourceResponse, ServerSentEvent`
    - src/jarvis/api/routes/chat.py contains `_session_lock = asyncio.Lock()`
    - src/jarvis/api/routes/chat.py contains `HTTPException(status_code=429`
    - src/jarvis/api/routes/chat.py contains `session.send(body.message)`
    - src/jarvis/api/routes/chat.py contains `session.send_stream(message)`
    - src/jarvis/api/__init__.py contains `app.include_router(chat_router)`
    - tests/api/test_chat.py contains `test_chat_post`
    - tests/api/test_chat.py contains `test_chat_post_empty_body`
    - tests/api/test_chat.py contains `test_chat_stream_content_type`
    - tests/api/test_chat.py contains `test_chat_stream_tokens`
    - tests/api/test_chat.py contains `test_chat_stream_missing_message`
    - `python -m pytest tests/api/ -x -q` exits 0
    - `python -m pytest tests/ -q --timeout=60` exits 0 (full regression)
  </acceptance_criteria>
  <done>POST /chat (API-01) e GET /chat/stream SSE (API-02) funcionais com testes. Session lock previne requests concorrentes. Full test suite (234 + novos) green. Phase 6 completa.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/api/ -x -q --timeout=30` — todos os testes de API passam
2. `python -m pytest tests/ -q --timeout=60` — full suite green (234 existentes + novos)
3. `python -c "from jarvis.api import app; print([r.path for r in app.routes])"` — mostra /chat, /chat/stream, /health, /health/ready
4. `python -c "from jarvis.core.session import ChatSession; assert hasattr(ChatSession, 'send_stream')"` — send_stream existe
5. Verificacao manual (per VALIDATION.md): `python -m jarvis.api` e `curl http://localhost:8000/health` retorna `{"status":"ok"}`
</verification>

<success_criteria>
- POST /chat retorna resposta JSON completa (API-01)
- GET /chat/stream retorna SSE com tokens incrementais (API-02)
- send_stream() no ChatSession funciona como async generator sem print()
- send() original 100% intocado — CLI nao muda
- Session lock previne requests concorrentes (429)
- Todos os testes (existentes + novos) passando
</success_criteria>

<output>
After completion, create `.planning/phases/06-fastapi-core/06-02-SUMMARY.md`
</output>
