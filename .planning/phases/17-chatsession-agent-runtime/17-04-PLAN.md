---
phase: 17-chatsession-agent-runtime
plan: 04
type: execute
wave: 4
depends_on:
  - "17-03"
files_modified:
  - apps/backend-ts/src/session/lock.ts
  - apps/backend-ts/src/session/lock.test.ts
  - apps/backend-ts/src/routes/chat.ts
  - apps/backend-ts/src/routes/chat.test.ts
  - apps/backend-ts/src/app.ts
  - apps/backend-ts/src/index.ts
autonomous: true
requirements:
  - LLM-TS-04
  - LLM-TS-05
  - LLM-TS-06
  - LLM-TS-07
must_haves:
  truths:
    - "POST /chat com body {message: string} retorna 200 + {message: string} com resposta do agente"
    - "GET /chat/stream?message=... retorna 200 text/event-stream com eventos `data: <token>\\n\\n` por token"
    - "Se uma request chega enquanto o lock está ocupado, retorna 429 com body {detail: 'Session busy — try again later'}"
    - "O lock libera mesmo quando o handler lança (try/finally)"
    - "Startup cria uma única ChatSession em app.locals.session via ChatSession.create(), compartilhada entre todas as requests"
    - "Body inválido (sem `message`) retorna 400 (validação via zod ou checagem explícita)"
  artifacts:
    - path: "apps/backend-ts/src/session/lock.ts"
      provides: "SessionLock simples baseado em Promise"
      exports: ["SessionLock"]
    - path: "apps/backend-ts/src/routes/chat.ts"
      provides: "Express Router com POST /chat e GET /chat/stream"
      exports: ["createChatRouter"]
    - path: "apps/backend-ts/src/routes/chat.test.ts"
      provides: "testes integration via supertest"
    - path: "apps/backend-ts/src/app.ts"
      provides: "createApp aceita session + monta /chat router"
  key_links:
    - from: "src/index.ts main()"
      to: "ChatSession.create({ llm, memory })"
      via: "instanciação única no startup"
    - from: "createApp(session)"
      to: "createChatRouter(session, lock)"
      via: "DI do session + lock via closure"
    - from: "POST /chat handler"
      to: "session.send(body.message)"
      via: "dentro de lock.acquire() try/finally"
    - from: "GET /chat/stream handler"
      to: "session.sendStream(query.message)"
      via: "res.flushHeaders() + res.write(`data: ${token}\\n\\n`)"
---

<objective>
Plugar tudo: criar o `SessionLock` (Promise-based), os endpoints Express `POST /chat` e `GET /chat/stream` com 429 busy, validação de payload, SSE raw, e wiring no `app.ts` + `index.ts` para instanciar uma `ChatSession` global no startup. Teste E2E via supertest valida o ciclo completo.

Purpose: entregar paridade externa 1:1 com `src/jarvis/api/routes/chat.py`. Depois deste plano, o frontend Electron (e o gateway Express) podem apontar para o backend TS em 8001 trocando só o host, sem adapters.

Output: endpoints funcionais testados via supertest, startup cria ChatSession, lock protege concorrência, SSE funciona com EventSource-friendly format.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-chatsession-agent-runtime/17-CONTEXT.md
@.planning/phases/17-chatsession-agent-runtime/17-03-SUMMARY.md
@src/jarvis/api/routes/chat.py
@src/jarvis/api/models.py
@apps/backend-ts/src/app.ts
@apps/backend-ts/src/index.ts
@apps/backend-ts/src/session/chat-session.ts

<interfaces>
Python endpoint contract (src/jarvis/api/routes/chat.py):
- POST /chat
  - Body: {"message": string}
  - 200: {"message": string}
  - 429: {"detail": "Session busy — try again later"}
- GET /chat/stream?message=...
  - 200 text/event-stream, cada token: `data: <token>\n\n`
  - 429: {"detail": "Session busy — try again later"}

Express 5 res methods:
- res.status(429).json({detail: "..."})
- res.setHeader('Content-Type', 'text/event-stream')
- res.setHeader('Cache-Control', 'no-cache')
- res.setHeader('Connection', 'keep-alive')
- res.flushHeaders()
- res.write(`data: ${token}\n\n`)
- res.end()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionLock + testes</name>
  <files>apps/backend-ts/src/session/lock.ts, apps/backend-ts/src/session/lock.test.ts</files>
  <behavior>
    - `lock.isBusy()` retorna false quando livre, true quando ocupado
    - `lock.tryAcquire()` retorna um release function quando consegue (e marca busy), ou `null` quando já ocupado
    - Release libera o lock (isBusy volta false)
    - Teste: duas chamadas consecutivas de tryAcquire — segunda retorna null; após release, terceira consegue
    - Teste: release é idempotente (chamar 2x não quebra)
  </behavior>
  <action>
    1. Implementar `SessionLock` classe simples com boolean `_busy`. `tryAcquire(): (() => void) | null`. Sem Promise chain nesta versão — só um mutex boolean (suficiente porque Node é single-threaded e só um handler entra por vez).
    2. Testes em `lock.test.ts` cobrindo os casos acima.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/lock.test.ts</automated>
  </verify>
  <done>Testes passam. SessionLock exportado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Router /chat + /chat/stream + wiring app.ts/index.ts + testes supertest</name>
  <files>apps/backend-ts/src/routes/chat.ts, apps/backend-ts/src/routes/chat.test.ts, apps/backend-ts/src/app.ts, apps/backend-ts/src/index.ts</files>
  <behavior>
    - POST /chat com body válido → 200 `{message: <resposta>}` chamando session.send
    - POST /chat com body `{}` → 400 com mensagem de validação
    - POST /chat quando lock ocupado → 429 `{detail: "Session busy — try again later"}`
    - POST /chat: lock libera mesmo se session.send lançar (verificar chamando de novo depois e ver que retorna 200)
    - GET /chat/stream?message=oi → 200, Content-Type text/event-stream, body contém sequência de `data: <token>\n\n` para cada token emitido por sendStream
    - GET /chat/stream sem query `message` → 400
    - GET /chat/stream quando lock ocupado → 429
    - Startup integration: `createApp({ session, lock })` monta o router em `/`. `src/index.ts` cria MemoryManager, runMigrations (já existe), cria LLM, cria ChatSession via `ChatSession.create`, passa para createApp.
  </behavior>
  <action>
    1. Criar `src/routes/chat.ts` exportando `createChatRouter(session: ChatSession, lock: SessionLock): Router`.
       - POST /chat: valida body (checagem simples `if (typeof req.body?.message !== 'string') return res.status(400).json({detail:'message required'})`). Tenta `lock.tryAcquire()`; se null → 429. Try/finally chama release. Response: `res.json({ message: await session.send(body.message) })`. Catch erro → 500 + release.
       - GET /chat/stream: valida query.message. tryAcquire ou 429. setHeaders SSE, flushHeaders(). `for await (const token of session.sendStream(message)) { res.write(`data: ${token}\n\n`); }`. `res.end()`. try/finally release. Em erro: escreve `data: [error]\n\n` e fecha.
    2. Editar `src/app.ts`: `createApp(opts: { session: ChatSession; lock: SessionLock })` — adiciona `app.use('/', createChatRouter(opts.session, opts.lock))` ANTES do errorHandler. Manter backward compat ou atualizar callers (index.ts).
    3. Editar `src/index.ts`:
       - Após runMigrations: `const memory = new MemoryManager({...})` (com opções default/env), `const llm = createLLM()`, `const session = await ChatSession.create({ llm, memory })`, `const lock = new SessionLock()`, `const app = createApp({ session, lock })`. Listen.
    4. Criar `src/routes/chat.test.ts` usando `supertest` (já é dep do projeto? verificar; se não, `pnpm add -D supertest @types/supertest`). Montar app com ChatSession mockada (`{ send: vi.fn().mockResolvedValue("olá"), sendStream: async function*(){ yield "a"; yield "b"; } } as unknown as ChatSession`). Cobrir todos os casos da `<behavior>`.
    5. Rodar suíte completa do backend-ts.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run && pnpm typecheck</automated>
  </verify>
  <done>
    - Todos os testes passam (session + routes)
    - `pnpm --filter backend-ts build` compila
    - Smoke manual (opcional, documentado no SUMMARY): `pnpm --filter backend-ts dev` + `curl -X POST http://localhost:8001/chat -H 'Content-Type: application/json' -d '{"message":"oi"}'` retorna JSON, e `curl -N 'http://localhost:8001/chat/stream?message=oi'` streama tokens.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run` — toda a suíte verde
- `pnpm --filter backend-ts typecheck` — zero erros
- `pnpm --filter backend-ts build` — compila
- Diff manual contra `src/jarvis/api/routes/chat.py`: shapes de request/response, status codes, formato SSE conferem char-a-char
</verification>

<success_criteria>
Fecha todos os 5 success criteria da Fase 17:
1. POST /chat → JSON completa ✅ (Task 2)
2. GET /chat/stream → SSE incrementais ✅ (Task 2)
3. Conversation history persiste no SQLite ✅ (via MemoryManager.saveTurn em send/sendStream)
4. Semantic retrieval via tool calling ✅ (herdado do Plan 17-02)
5. Agent ReAct loop funciona ✅ (herdado do Plan 17-02, exercitado em send())
</success_criteria>

<output>
Criar `17-04-SUMMARY.md` com:
- Comandos curl de smoke test e output observado
- Nota sobre divergência consciente 4b ↔ 5a (memory via tool vs system prompt injection) — confirmar que a decisão ficou registrada
- Status de cada success criterion da fase
- Bug conhecido herdado da Fase 16 (MemoryVectorsOptions sem `collection`) — não bloqueia

Commit: `✨ feat(17-04): expõe POST /chat e GET /chat/stream com lock global e SSE`

Ao final, atualizar `.planning/STATE.md` marcando Fase 17 como completa.
</output>
