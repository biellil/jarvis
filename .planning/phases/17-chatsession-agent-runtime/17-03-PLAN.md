---
phase: 17-chatsession-agent-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - "17-02"
files_modified:
  - apps/backend-ts/src/session/chat-session.ts
  - apps/backend-ts/src/session/chat-session.test.ts
autonomous: true
requirements:
  - LLM-TS-05
must_haves:
  truths:
    - "ChatSession expõe `sendStream(text): AsyncIterable<string>` que yielda tokens individuais"
    - "Durante o stream, NADA é impresso em stdout (paridade com Python send_stream, D-02)"
    - "Ao fim do stream, history é atualizada com HumanMessage + AIMessage final e memory.saveTurn é chamada"
    - "Se o stream falhar no meio, o lock não fica preso (try/finally garante cleanup) e o erro propaga pro consumer"
    - "sendStream NÃO suporta tool calling nesta fase — usa llm.stream() direto (paridade com Python send_stream que também bypassa tools)"
  artifacts:
    - path: "apps/backend-ts/src/session/chat-session.ts"
      provides: "sendStream async generator"
      exports: ["ChatSession.sendStream"]
  key_links:
    - from: "ChatSession.sendStream()"
      to: "this.llm.stream(messages)"
      via: "for await (const chunk of ...) yield chunk.content"
    - from: "ChatSession.sendStream() finally"
      to: "memory.saveTurn(convId, user, assembled)"
      via: "persistência após drain completo"
---

<objective>
Adicionar `sendStream(text): AsyncIterable<string>` ao `ChatSession` que retorna tokens um a um via `llm.stream()`. Espelha `send_stream()` do Python (`src/jarvis/core/session.py:347`) incluindo a decisão consciente de não suportar tool calling no stream (divergência aceita na fase anterior — o agent só roda via `send()` não-streaming por enquanto).

Purpose: habilitar o endpoint SSE do Plan 17-04. Streaming sem tool calls é suficiente porque a memória é tool-based apenas no modo síncrono; o stream é para resposta rápida conversacional.

Output: `sendStream` assíncrono gerador, testado com stream mockado, history atualizada corretamente ao final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-chatsession-agent-runtime/17-CONTEXT.md
@.planning/phases/17-chatsession-agent-runtime/17-02-SUMMARY.md
@src/jarvis/core/session.py
@apps/backend-ts/src/session/chat-session.ts

<interfaces>
LangChain.js 1.x stream API:
```typescript
for await (const chunk of llm.stream(messages)) {
  // chunk: AIMessageChunk
  // chunk.content: string | complex (we want string path)
}
```

Python reference (src/jarvis/core/session.py:347-437) — sendStream deve manter comportamento equivalente:
- não imprime em stdout
- yielda tokens
- ao fim, append AIMessage(full_response) em history
- persiste via saveTurn
- sem tool calling
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes vitest para sendStream (RED) + implementação (GREEN)</name>
  <files>apps/backend-ts/src/session/chat-session.ts, apps/backend-ts/src/session/chat-session.test.ts</files>
  <behavior>
    - Teste 1: llm mock com `stream` retornando async iterable de 3 chunks `[{content:"oi"},{content:" "},{content:"mundo"}]`. Consumindo `for await (const t of session.sendStream("teste"))` acumula `["oi"," ","mundo"]`.
    - Teste 2: após drain, `session.history` tem append de HumanMessage("teste") + AIMessage("oi mundo"). E `memory.saveTurn(convId, "teste", "oi mundo")` foi chamado 1 vez.
    - Teste 3: se o stream throw no meio (chunk 2 rejeita), o erro propaga pro consumer do generator, mas o `saveTurn` NÃO é chamado (pois resposta está incompleta) E nenhum stdout foi escrito.
    - Teste 4: sendStream NÃO usa `this._agent` — chama `this.llm.stream()` direto. Confirmar via spy que `agent.invoke` não foi chamado.
    - Teste 5: chunks com `content` vazio ("") são silenciosamente ignorados (não yieldados) mas não quebram o loop.
  </behavior>
  <action>
    1. Adicionar os 5 testes em `chat-session.test.ts`. Para mockar o stream do llm, usar `stream: vi.fn().mockImplementation(async function*() { yield {content:"oi"}; ... })`.
    2. Implementar `async *sendStream(text: string): AsyncGenerator<string>` em `chat-session.ts`:
       - Append HumanMessage em history (antes do stream)
       - `let assembled = ""`
       - `try { for await (const chunk of this.llm.stream(this.history)) { const t = typeof chunk.content === 'string' ? chunk.content : ''; if (t) { assembled += t; yield t; } } }`
       - Após o loop (sucesso): append AIMessage(assembled) em history, `await memory.saveTurn(convId, text, assembled).catch(e => console.warn(...))`
       - Erros do stream se propagam naturalmente (não try/catch envolvendo o for-await).
       - Importante: construir `messagesToSend = [...this.history]` ANTES de fazer append, pra não incluir a HumanMessage duas vezes — OU fazer append e passar `this.history` direto (cuidado: a HumanMessage JÁ deve estar na lista passada ao llm.stream). Escolher: append primeiro, passar this.history.
    3. Rodar testes até GREEN.
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm vitest run src/session/chat-session.test.ts</automated>
  </verify>
  <done>Os 5 testes novos + os antigos passam. Typecheck limpo. sendStream exportado como método de ChatSession.</done>
</task>

</tasks>

<verification>
- `pnpm --filter backend-ts vitest run src/session/`
- `pnpm --filter backend-ts typecheck`
- Leitura cruzada com `src/jarvis/core/session.py:347-437` confirma paridade comportamental
</verification>

<success_criteria>
Base para success criterion #2 da fase ("GET /chat/stream → tokens SSE incrementais") — o endpoint do Plan 17-04 vai consumir este gerador.
</success_criteria>

<output>
Criar `17-03-SUMMARY.md` com nota sobre a decisão de sendStream não passar pelo agent (paridade com Python), e referência de linha exata do send_stream do Python.

Commit: `✨ feat(17-03): adiciona sendStream async generator na ChatSession`
</output>
