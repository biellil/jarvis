---
phase: 82-langgraph-execucao-silenciosa-sem-confirmacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/memory/schema.ts
  - apps/backend-ts/src/memory/migrations/0006_approved_plans.sql
  - apps/backend-ts/src/agent/approval.ts
  - apps/backend-ts/src/agent/graph.ts
  - apps/backend-ts/src/agent/__tests__/approval.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03

must_haves:
  truths:
    - "Plan auto-executado silenciosamente (sem interrupt) quando chave SHA-256 existe em approved_plans e dentro do TTL de 90 dias"
    - "Plano com keyword crítica (delete, deletar, chmod, etc.) sempre dispara interrupt, independente de histórico"
    - "Chave canônica gerada identicamente para qualquer ordenação dos mesmos step descriptions"
    - "Aprovação salva em approved_plans após usuário confirmar um plano novo"
    - "Evento task:auto-approved emitido via writer quando plano é auto-aprovado"
  artifacts:
    - path: "apps/backend-ts/src/memory/migrations/0006_approved_plans.sql"
      provides: "DDL da tabela approved_plans"
      contains: "CREATE TABLE `approved_plans`"
    - path: "apps/backend-ts/src/agent/approval.ts"
      provides: "hasCriticalAction, canonicalPlanKey, isApprovedPlan, saveApproval"
      exports: ["hasCriticalAction", "canonicalPlanKey", "isApprovedPlan", "saveApproval"]
    - path: "apps/backend-ts/src/agent/graph.ts"
      provides: "planner node com interrupt condicional"
      contains: "hasCriticalAction"
    - path: "apps/backend-ts/src/agent/__tests__/approval.test.ts"
      provides: "testes unit para approval.ts"
  key_links:
    - from: "apps/backend-ts/src/agent/graph.ts planner node"
      to: "apps/backend-ts/src/agent/approval.ts hasCriticalAction + isApprovedPlan"
      via: "import approval functions before interrupt() call"
      pattern: "hasCriticalAction\\(plan\\)"
    - from: "apps/backend-ts/src/agent/approval.ts isApprovedPlan"
      to: "apps/backend-ts/src/memory/schema.ts approvedPlans table"
      via: "Drizzle ORM query with datetime TTL check"
      pattern: "approvedPlans"
---

<objective>
Implementar a memória de aprovações de planos no backend TypeScript: tabela SQLite `approved_plans`, módulo `approval.ts` com detecção de ações críticas e lookup de aprovações, e modificar o nó planner em `graph.ts` para interrupt condicional.

Purpose: Eliminar o interrupt obrigatório para planos já aprovados, mantendo segurança para ações críticas (deleção/permissões).
Output: `approval.ts` (4 funções), migration SQL, `schema.ts` atualizado, `graph.ts` com lógica condicional, testes unitários.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/82-langgraph-execucao-silenciosa-sem-confirmacao/82-CONTEXT.md
@.planning/phases/82-langgraph-execucao-silenciosa-sem-confirmacao/82-RESEARCH.md

<interfaces>
<!-- Interfaces existentes que o executor DEVE usar como base — não explorar o codebase -->

From apps/backend-ts/src/agent/types.ts:
```typescript
export const planSchema = z.object({
  steps: z.array(z.object({
    id: z.number().int().positive(),
    description: z.string().min(3).max(200),
    expectedOutcome: z.string().min(3).max(200),
  })).min(1).max(15),
});
export type Plan = z.infer<typeof planSchema>;

export type ResumeCommand =
  | { kind: 'confirm' }
  | { kind: 'cancel' }
  | { kind: 'edit'; feedback: string }
  | { kind: 'continue' }
  | { kind: 'replan' }
  | { kind: 'abort' };
```

From apps/backend-ts/src/agent/graph.ts (current planner node, simplified):
```typescript
import { interrupt, Command, END } from '@langchain/langgraph';
import type { LangGraphRunnableConfig } from '@langchain/langgraph';

// Planner node atualmente sempre chama interrupt():
.addNode('planner', async (state: TaskState, config: LangGraphRunnableConfig) => {
  const writer = config.writer as ((payload: unknown) => void) | undefined;
  if (state.editFeedback) { writer?.({ kind: 'task:edit-loop' }); }
  let plan: Plan;
  try {
    plan = await generatePlan(args.llm, state.userInput, {
      editFeedback: state.editFeedback ?? undefined,
      previousPlan: state.plan ?? undefined,
    });
  } catch (err) { /* fallback plan + error event */ }
  if (!state.plan) { writer?.({ kind: 'task:plan', plan }); }
  // ATUALMENTE: sempre interrupt()
  const decision = interrupt({ kind: 'plan-confirmation', plan }) as ResumeCommand;
  if (decision.kind === 'cancel') {
    writer?.({ kind: 'task:cancelled', atStep: 0 });
    return new Command({ goto: END, update: { plan, cancelRequested: true, editFeedback: null } });
  }
  if (decision.kind === 'edit') {
    return new Command({ goto: 'planner', update: { plan, editFeedback: decision.feedback } });
  }
  return { plan, editFeedback: null };
})
```

From apps/backend-ts/src/memory/schema.ts (padrão Drizzle existente):
```typescript
import { sqliteTable, text, integer, real, uniqueIndex } from 'drizzle-orm/sqlite-core';
// Exemplo de tabela com o padrão do projeto:
export const actionsLog = sqliteTable('actions_log', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  timestamp: text('timestamp').notNull(),
  // ...campos text/integer...
});
```

From apps/backend-ts/src/memory/store.ts:
```typescript
// createStoreForTests: abre SQLite, roda migrações, retorna { db, sqlite }
export function createStoreForTests(dbPath: string): { db: Drizzle; sqlite: Database.Database; }
// migrationsFolder() aponta para src/memory/migrations/
```

From apps/backend-ts/src/memory/db.ts:
```typescript
// db singleton — usado por approval.ts para operações em produção
import { db } from '../memory/db.js';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + migration + approval.ts</name>
  <files>
    apps/backend-ts/src/memory/schema.ts,
    apps/backend-ts/src/memory/migrations/0006_approved_plans.sql,
    apps/backend-ts/src/agent/approval.ts,
    apps/backend-ts/src/agent/__tests__/approval.test.ts
  </files>
  <read_first>
    - apps/backend-ts/src/memory/schema.ts (padrão Drizzle existente — adicionar após actionsLog)
    - apps/backend-ts/src/memory/migrations/0004_actions_log.sql (modelo de SQL de migration)
    - apps/backend-ts/src/agent/types.ts (Plan type)
    - apps/backend-ts/src/memory/store.ts (createStoreForTests pattern para testes)
    - apps/backend-ts/src/memory/db.ts (db singleton)
  </read_first>
  <behavior>
    - Test KWD-01: hasCriticalAction({steps:[{id:1,description:'delete file.txt',expectedOutcome:'done'}]}) === true
    - Test KWD-01b: hasCriticalAction com "DELETE FILE.TXT" (uppercase) === true (lowercase interno)
    - Test KWD-01c: hasCriticalAction com "chmod 755 script.sh" === true
    - Test KWD-01d: hasCriticalAction com "listar arquivos em Downloads" === false
    - Test KWD-01e: hasCriticalAction com "remover item da lista de display" === true (keyword "remover")
    - Test KEY-01: canonicalPlanKey com steps [{id:1,"Passo A"},{id:2,"Passo B"}] == canonicalPlanKey com mesmo plano reordenado em sort
    - Test KEY-01b: canonicalPlanKey difere quando descriptions diferem
    - Test APR-01: isApprovedPlan(key, db) retorna false quando tabela vazia
    - Test APR-02: saveApproval + isApprovedPlan retorna true dentro do TTL
    - Test APR-03: isApprovedPlan retorna false para key com expires_at no passado
  </behavior>
  <action>
**Step 1 — Migration SQL** (`apps/backend-ts/src/memory/migrations/0006_approved_plans.sql`):
```sql
CREATE TABLE `approved_plans` (
  `id` integer PRIMARY KEY AUTOINCREMENT NOT NULL,
  `key` text NOT NULL,
  `plan_json` text NOT NULL,
  `approved_at` text NOT NULL,
  `expires_at` text NOT NULL,
  `created_at` text NOT NULL,
  UNIQUE(`key`)
);
```

**Step 2 — schema.ts**: Adicionar após a tabela `reminders` (último export):
```typescript
// ============================================================
// Phase 82 — Approval Memory (D-01)
// ============================================================

export const approvedPlans = sqliteTable('approved_plans', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  key: text('key').notNull().unique(),          // SHA-256 de steps normalizados
  planJson: text('plan_json').notNull(),         // JSON do plano completo (audit trail)
  approvedAt: text('approved_at').notNull(),     // ISO timestamp da aprovação
  expiresAt: text('expires_at').notNull(),       // ISO timestamp de expiração (approved_at + 90d)
  createdAt: text('created_at').notNull(),       // ISO timestamp de criação do registro
});
```

**Step 3 — approval.ts** (`apps/backend-ts/src/agent/approval.ts`):
```typescript
/**
 * Phase 82 — Conditional plan approval memory.
 *
 * D-02: Critical actions always interrupt (keyword detection).
 * D-01: Approved plans skip interrupt (SHA-256 key + SQLite TTL).
 * D-03: saveApproval called on user confirmation; isApprovedPlan checked before interrupt.
 */
import crypto from 'node:crypto';
import type { BetterSQLite3Database } from 'drizzle-orm/better-sqlite3';
import { eq, gt, sql } from 'drizzle-orm';
import type * as schema from '../memory/schema.js';
import { approvedPlans } from '../memory/schema.js';
import type { Plan } from './types.js';

type DrizzleDb = BetterSQLite3Database<typeof schema>;

// D-02: keywords que indicam ação crítica — detectados em step.description (lowercase)
const CRITICAL_KEYWORDS = [
  'deletar', 'delete', 'remover', 'remove',
  'apagar', 'erase', 'unlink', 'rm', 'trash',
  'permission', 'chmod', 'chown', 'registry',
  'system config', 'configuração do sistema',
] as const;

/**
 * D-02: Retorna true se qualquer step do plano contém keyword crítica.
 * Normaliza para lowercase antes da comparação.
 */
export function hasCriticalAction(plan: Plan): boolean {
  return plan.steps.some((step) => {
    const desc = step.description.toLowerCase();
    return CRITICAL_KEYWORDS.some((kw) => desc.includes(kw));
  });
}

/**
 * D-01: Gera chave SHA-256 canônica do plano.
 * Normalização: lowercase + trim de cada description, sort alfabético, join com \n.
 * Sort garante que a mesma ordem sempre produz a mesma chave.
 */
export function canonicalPlanKey(plan: Plan): string {
  const normalized = plan.steps
    .map((s) => s.description.toLowerCase().trim())
    .sort()
    .join('\n');
  return crypto.createHash('sha256').update(normalized).digest('hex');
}

/**
 * D-01: Verifica se a chave existe em approved_plans e não está expirada.
 * TTL checado via SQLite datetime comparison.
 */
export async function isApprovedPlan(planKey: string, db: DrizzleDb): Promise<boolean> {
  const now = new Date().toISOString();
  const rows = await db
    .select({ id: approvedPlans.id })
    .from(approvedPlans)
    .where(eq(approvedPlans.key, planKey))
    .limit(1);
  if (rows.length === 0) return false;
  // Verificar TTL separadamente (Drizzle tem suporte limitado a datetime() expressions)
  const row = await db
    .select({ expiresAt: approvedPlans.expiresAt })
    .from(approvedPlans)
    .where(eq(approvedPlans.key, planKey))
    .limit(1);
  const expiresAt = row[0]?.expiresAt;
  if (!expiresAt) return false;
  return expiresAt > now; // ISO strings são comparáveis lexicograficamente
}

/**
 * D-03: Salva ou atualiza uma aprovação em approved_plans.
 * ttlDays padrão = 90 (D-01).
 * ON CONFLICT DO UPDATE atualiza expiresAt (re-aprovação renova TTL).
 */
export async function saveApproval(
  planKey: string,
  plan: Plan,
  db: DrizzleDb,
  ttlDays = 90,
): Promise<void> {
  const now = new Date().toISOString();
  const expiresAt = new Date(Date.now() + ttlDays * 24 * 60 * 60 * 1000).toISOString();
  await db
    .insert(approvedPlans)
    .values({
      key: planKey,
      planJson: JSON.stringify(plan),
      approvedAt: now,
      expiresAt,
      createdAt: now,
    })
    .onConflictDoUpdate({
      target: approvedPlans.key,
      set: {
        approvedAt: now,
        expiresAt,
        planJson: JSON.stringify(plan),
      },
    });
}
```

**Step 4 — Testes** (`apps/backend-ts/src/agent/__tests__/approval.test.ts`):
- Importar `createStoreForTests` de `../../memory/store.js` para criar DB in-memory com migrações
- Usar `beforeAll/afterAll` para setup/teardown do DB SQLite
- Testar todos os behaviors listados acima
- Padrão: `import { describe, it, expect, beforeAll, afterAll } from 'vitest';`
  </action>
  <verify>
    <automated>cd apps/backend-ts && npm test -- --run --reporter=verbose approval</automated>
  </verify>
  <done>
    - `approval.ts` exporta `hasCriticalAction`, `canonicalPlanKey`, `isApprovedPlan`, `saveApproval`
    - `0006_approved_plans.sql` contém `CREATE TABLE \`approved_plans\``
    - `schema.ts` exporta `approvedPlans` com campos `key`, `planJson`, `approvedAt`, `expiresAt`, `createdAt`
    - Todos os testes de approval passam (KWD-01*, KEY-01*, APR-01, APR-02, APR-03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Modificar graph.ts — interrupt condicional (D-03)</name>
  <files>
    apps/backend-ts/src/agent/graph.ts,
    apps/backend-ts/src/agent/__tests__/graph.test.ts
  </files>
  <read_first>
    - apps/backend-ts/src/agent/graph.ts (planner node atual, linhas 80–162 — ler completo)
    - apps/backend-ts/src/agent/approval.ts (recém-criado na Task 1)
    - apps/backend-ts/src/agent/__tests__/graph.test.ts (testes existentes — ver padrão de drainStream e fixtures)
    - apps/backend-ts/src/agent/__tests__/fixtures/mockChatModel.ts (se existir — fixture de LLM mock)
    - apps/backend-ts/src/memory/db.ts (db singleton a ser passado para approval functions)
  </read_first>
  <action>
**Modificar o nó `planner` em `graph.ts`:**

1. Adicionar imports no topo do arquivo:
```typescript
import crypto from 'node:crypto'; // já existe (randomUUID)
import { hasCriticalAction, canonicalPlanKey, isApprovedPlan, saveApproval } from './approval.js';
import { db as approvalDb } from '../memory/db.js';
```

2. Substituir o bloco `interrupt()` do planner node (após `writer?.({ kind: 'task:plan', plan })`) com:
```typescript
// D-02: Ações críticas sempre pedem confirmação (nunca skip de cache)
if (hasCriticalAction(plan)) {
  const decision = interrupt({
    kind: 'plan-confirmation',
    plan,
  }) as ResumeCommand;

  if (decision.kind === 'cancel') {
    writer?.({ kind: 'task:cancelled', atStep: 0 });
    return new Command({ goto: END, update: { plan, cancelRequested: true, editFeedback: null } });
  }
  if (decision.kind === 'edit') {
    return new Command({ goto: 'planner', update: { plan, editFeedback: decision.feedback } });
  }
  // confirm — salva aprovação e executa (ação crítica: não salva em cache)
  return { plan, editFeedback: null };
}

// D-01: Verificar cache de aprovações
const planKey = canonicalPlanKey(plan);
const alreadyApproved = await isApprovedPlan(planKey, approvalDb as any);

if (alreadyApproved) {
  // D-03 step 3: auto-aprovado — executa silenciosamente
  writer?.({ kind: 'task:auto-approved' });
  return { plan, editFeedback: null };
}

// D-03 step 4: plano novo — pedir confirmação
const decision = interrupt({
  kind: 'plan-confirmation',
  plan,
}) as ResumeCommand;

if (decision.kind === 'cancel') {
  writer?.({ kind: 'task:cancelled', atStep: 0 });
  return new Command({ goto: END, update: { plan, cancelRequested: true, editFeedback: null } });
}

if (decision.kind === 'edit') {
  return new Command({ goto: 'planner', update: { plan, editFeedback: decision.feedback } });
}

// D-03 step 5: usuário confirmou — salvar no cache
await saveApproval(planKey, plan, approvalDb as any);
return { plan, editFeedback: null };
```

**Adicionar testes em `graph.test.ts`:**

Adicionar `describe('conditional interrupt (Phase 82)', () => { ... })` com:

- APR-02: Plan sem ação crítica, chave pré-salva em approvedPlans → stream drena sem evento `task:awaiting-confirmation`, emite `task:auto-approved`. Usar um DB in-memory e `vi.mock('../memory/db.js', ...)` ou passar DB como parâmetro via módulo mock.
- APR-03: Plan com keyword "delete" → stream SEMPRE emite `task:awaiting-confirmation`, mesmo que chave esteja em cache. Mock `isApprovedPlan` para retornar `true` e verificar que o check de critical keywords tem precedência.
- KWD-check: Plan sem keywords críticas, chave NOT in cache → emite `task:awaiting-confirmation` (fluxo normal).

Para mockar o DB sem alterar assinatura de `buildTaskGraph`, usar `vi.mock('../memory/db.js')` no topo do arquivo de teste para injetar um db in-memory que já tem a chave pré-salva.

IMPORTANTE: O type cast `approvalDb as any` é necessário porque o tipo Drizzle inferido de `db.ts` difere levemente do tipo genérico de `approval.ts`. Isso é aceitável — o comportamento em runtime é correto.
  </action>
  <verify>
    <automated>cd apps/backend-ts && npm test -- --run --reporter=verbose graph</automated>
  </verify>
  <done>
    - `graph.ts` contém `hasCriticalAction(plan)` antes do primeiro `interrupt()`
    - `graph.ts` contém `task:auto-approved` no corpo do planner node
    - `graph.ts` contém `saveApproval(planKey, plan, approvalDb` após confirm do usuário
    - Testes novos de APR-02 e APR-03 passam junto com os testes existentes do graph
    - Suite completa do backend passa: `cd apps/backend-ts && npm test -- --run` sem falhas
  </done>
</task>

</tasks>

<verification>
```bash
cd apps/backend-ts && npm test -- --run
```
Toda a suite backend deve passar. Verificar especificamente:
- `approval.test.ts`: todos os testes KWD-01*, KEY-01*, APR-01, APR-02, APR-03 verdes
- `graph.test.ts`: testes novos de Phase 82 + testes existentes todos verdes
- Sem TypeScript errors: `cd apps/backend-ts && npx tsc --noEmit`
</verification>

<success_criteria>
- `approved_plans` table existe em migrations como `0006_approved_plans.sql`
- `approval.ts` exporta as 4 funções com comportamentos corretos nos testes
- `graph.ts` planner node tem lógica de 3 ramos: crítico → interrupt, aprovado → auto-approved, novo → interrupt + save
- `task:auto-approved` SSE event emitido quando cache hit
- Testes APR-01, APR-02, APR-03, KWD-01*, KEY-01* todos verdes
- Suite completa backend sem regressões
</success_criteria>

<output>
Após completar, criar `.planning/phases/82-langgraph-execucao-silenciosa-sem-confirmacao/82-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Decisões de implementação (especialmente o type cast approvalDb as any e por quê)
- Testes adicionados e o que cobrem
- Comando de verificação
</output>
