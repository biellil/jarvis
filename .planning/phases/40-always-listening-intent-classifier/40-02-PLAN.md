---
phase: 40-always-listening-intent-classifier
plan: 02
type: execute
wave: 1
depends_on: [40-01]
files_modified:
  - apps/desktop/src/main/store.ts
  - apps/desktop/src/shared/ipc-types.ts
  - apps/desktop/src/main/voiceInput/whisperResources.ts
  - apps/desktop/src/main/__tests__/store.test.ts
autonomous: true
requirements: [VLISTEN-01, VLISTEN-02, VLISTEN-04]

must_haves:
  truths:
    - "electron-store aceita e retorna vadSilenceThresholdMs com default 500 quando campo ausente (v1.8 upgrade)"
    - "ipc-types.ts exporta AlwaysListeningUtterancePayload, VoiceModeDegradedEvent, e os 4 novos canais IPC"
    - "whisperResources.ts expõe confidence do whisper.cpp suficientemente para o pre-filtro D-08 (ou fallback heurístico documentado)"
    - "SettingsData interface inclui vadSilenceThresholdMs: number"
  artifacts:
    - path: "apps/desktop/src/main/store.ts"
      provides: "getVadSilenceThresholdMs() / setVadSilenceThresholdMs() accessors + StoreSchema.vadSilenceThresholdMs"
      exports: ["getVadSilenceThresholdMs", "setVadSilenceThresholdMs"]
    - path: "apps/desktop/src/shared/ipc-types.ts"
      provides: "AlwaysListeningUtterancePayload + VoiceModeDegradedEvent + 4 novos IPC_CHANNELS"
      contains: "AlwaysListeningUtterancePayload"
    - path: "apps/desktop/src/main/voiceInput/whisperResources.ts"
      provides: "TranscribeResult interface com campo confidence (0-1) ou fallback heurístico documentado"
  key_links:
    - from: "apps/desktop/src/main/store.ts"
      to: "apps/desktop/src/shared/ipc-types.ts"
      via: "import type { VoiceMode }"
      pattern: "import.*VoiceMode.*ipc-types"
    - from: "apps/desktop/src/main/ipc/settings.ts"
      to: "apps/desktop/src/main/store.ts"
      via: "getVadSilenceThresholdMs() no handler settings:get"
      pattern: "getVadSilenceThresholdMs"
---

<objective>
Adicionar os tipos e contratos IPC de Phase 40 aos arquivos de fundação existentes: store.ts (vadSilenceThresholdMs), ipc-types.ts (novos channels + payloads), e whisperResources.ts (exposição de confidence para o pre-filtro D-08). Esses contratos desbloqueiam o trabalho paralelo dos Plans 03 e as waves seguintes.

Purpose: Estabelecer contratos de interface que evitam que Plans 03-06 precisem explorar o codebase para descobrir tipos e nomes de canais.

Output: 3 arquivos de fundação modificados + testes de regressão em store.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md
@.planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md
@.planning/phases/39-voice-mode-state-machine/39-01-SUMMARY.md

<interfaces>
<!-- Estado atual dos arquivos — leia antes de modificar -->

De apps/desktop/src/main/store.ts (Phase 39 handoff):
```typescript
export interface StoreSchema {
  voiceMode?: VoiceMode;
  hotkey?: string;
  pttHotkey?: string;
  wakeWordPaused?: boolean;
  orbPosition?: { x: number; y: number };
  ttsProvider?: 'murf' | 'elevenlabs';
  ttsApiKey?: string;
  whisperModelOverride?: string;
}
export function getVoiceMode(): VoiceMode { ... }
export function setVoiceMode(mode: VoiceMode): void { ... }
```

De apps/desktop/src/shared/ipc-types.ts (Phase 39 handoff):
```typescript
export const IPC_CHANNELS = {
  CHAT_SEND_TEXT: 'chat:send-text',
  CHAT_SEND_AUDIO: 'chat:send-audio',
  SETTINGS_GET: 'settings:get',
  SETTINGS_SAVE: 'settings:save',
  SETTINGS_CLOSE: 'settings:close',
  VOICE_MODE_CHANGE: 'voiceMode:change',
  // ... outros
} as const;

export interface SettingsData {
  pttHotkey: string;
  ttsProvider: TtsProviderOption;
  ttsApiKey: string;
  whisperModelOverride: WhisperModelOption;
  // vadSilenceThresholdMs FALTANDO — adicionar aqui
}
```

De apps/desktop/src/main/voiceInput/whisperResources.ts (verificado):
```typescript
// A interface WhisperInstance.transcribeData() retorna:
// { result?: string } — SEM campo de confidence!
// Necessita extensão para D-08 (pre-filtro por STT confidence < 0.5)
transcribeData(audioBuffer: ArrayBuffer, options?: { language?: string }): {
  stop: () => Promise<void>;
  promise: Promise<{ result?: string }>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar vadSilenceThresholdMs ao store.ts + testes</name>
  <read_first>
    - apps/desktop/src/main/store.ts (ler arquivo completo — padrão existente de accessors com default via ??)
    - apps/desktop/src/main/__tests__/store.test.ts (ler testes existentes para adicionar no bloco correto)
  </read_first>
  <action>
Em `apps/desktop/src/main/store.ts`:

1. Adicionar campo ao `StoreSchema` interface:
```typescript
export interface StoreSchema {
  // ... campos existentes ...
  voiceMode?: VoiceMode;
  vadSilenceThresholdMs?: number; // Phase 40 — range 300-800ms, default 500 (VLISTEN-04)
}
```

2. Adicionar constante e accessors após os accessors de `voiceMode`:
```typescript
const VAD_SILENCE_THRESHOLD_DEFAULT = 500; // ms — alinhado com OpenAI/Alexa/Google standard (D-07 Claude's Discretion)
const VAD_SILENCE_THRESHOLD_MIN = 300;
const VAD_SILENCE_THRESHOLD_MAX = 800;

/**
 * getVadSilenceThresholdMs — lê vadSilenceThresholdMs com default 500ms.
 * Clamp defensivo no read: valores fora de range (corrupção de store) retornam default.
 * Pattern idêntico ao getVoiceMode() — default implícito, sem migração explícita (D-07 Phase 39).
 */
export function getVadSilenceThresholdMs(): number {
  const stored = store.get('vadSilenceThresholdMs');
  if (stored === undefined || stored < VAD_SILENCE_THRESHOLD_MIN || stored > VAD_SILENCE_THRESHOLD_MAX) {
    return VAD_SILENCE_THRESHOLD_DEFAULT;
  }
  return stored;
}

/**
 * setVadSilenceThresholdMs — persiste valor clamped ao range [300, 800].
 */
export function setVadSilenceThresholdMs(ms: number): void {
  const clamped = Math.max(VAD_SILENCE_THRESHOLD_MIN, Math.min(VAD_SILENCE_THRESHOLD_MAX, ms));
  store.set('vadSilenceThresholdMs', clamped);
}
```

Em `apps/desktop/src/main/__tests__/store.test.ts`, adicionar describe block após os testes de Voice Mode:
```typescript
describe('VAD Silence Threshold accessors (Phase 40 — VLISTEN-04, T-40-VAD)', () => {
  it('getVadSilenceThresholdMs() returns 500 when not set (D-07 upgrade default)', () => {
    expect(getVadSilenceThresholdMs()).toBe(500);
  });

  it('setVadSilenceThresholdMs(600) → getVadSilenceThresholdMs() returns 600', () => {
    setVadSilenceThresholdMs(600);
    expect(getVadSilenceThresholdMs()).toBe(600);
  });

  it('setVadSilenceThresholdMs(100) clamps to 300 (T-40-VAD min boundary)', () => {
    setVadSilenceThresholdMs(100);
    expect(getVadSilenceThresholdMs()).toBe(300);
  });

  it('setVadSilenceThresholdMs(1000) clamps to 800 (T-40-VAD max boundary)', () => {
    setVadSilenceThresholdMs(1000);
    expect(getVadSilenceThresholdMs()).toBe(800);
  });

  it('getVadSilenceThresholdMs() returns 500 when store contains value below 300 (corruption guard)', () => {
    store.set('vadSilenceThresholdMs', 50); // corrupção direta
    expect(getVadSilenceThresholdMs()).toBe(500);
  });
});
```

Importar `getVadSilenceThresholdMs, setVadSilenceThresholdMs` no arquivo de teste.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run src/main/__tests__/store.test.ts --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "vadSilenceThresholdMs?: number" apps/desktop/src/main/store.ts` retorna match
    - `grep "export function getVadSilenceThresholdMs" apps/desktop/src/main/store.ts` retorna match
    - `grep "export function setVadSilenceThresholdMs" apps/desktop/src/main/store.ts` retorna match
    - `grep "VAD_SILENCE_THRESHOLD_DEFAULT = 500" apps/desktop/src/main/store.ts` retorna match
    - `grep -c "VAD Silence Threshold accessors" apps/desktop/src/main/__tests__/store.test.ts` retorna 1
    - Todos os testes de store.test.ts passam (suite existente + 5 novos = total >= 20)
    - `npx tsc --noEmit` não produz erros relacionados a `vadSilenceThresholdMs` em store.ts
  </acceptance_criteria>
  <done>vadSilenceThresholdMs accessor com clamp [300,800] + default 500 adicionado ao store; 5 testes passando</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar tipos Phase 40 ao ipc-types.ts + expor confidence em whisperResources.ts</name>
  <read_first>
    - apps/desktop/src/shared/ipc-types.ts (ler arquivo completo — padrão de interface e IPC_CHANNELS)
    - apps/desktop/src/main/voiceInput/whisperResources.ts (ler arquivo completo — verificar WhisperInstance.transcribeData return type)
  </read_first>
  <action>
**Em `apps/desktop/src/shared/ipc-types.ts`:**

1. Adicionar novos IPC channels ao objeto `IPC_CHANNELS`:
```typescript
export const IPC_CHANNELS = {
  // ... channels existentes ...
  VOICE_MODE_CHANGE: 'voiceMode:change',
  // Phase 40 — Always-Listening channels
  ALWAYS_LISTENING_START: 'always-listening:start',     // main → renderer: inicia engine
  ALWAYS_LISTENING_STOP: 'always-listening:stop',       // main → renderer: para engine
  ALWAYS_LISTENING_UTTERANCE: 'always-listening:utterance', // renderer → main: WAV pronto
  ALWAYS_LISTENING_VAD_THRESHOLD: 'always-listening:vad-threshold', // Settings → main → renderer: reconfigure
  VOICE_MODE_DEGRADED: 'voiceMode:degraded',            // main → tray (Phase 41 consumer)
} as const;
```

2. Adicionar interfaces Phase 40 (inserir na seção "Voice Mode Types — Phase 39"):
```typescript
// ============================================
// Always-Listening Types — Phase 40
// ============================================

/**
 * AlwaysListeningUtterancePayload — payload do IPC 'always-listening:utterance'.
 * Enviado pelo renderer ao main quando VAD detecta fim de fala + classifier aprova (D-04).
 * Volume IPC bounded: WAV ~5–100KB por utterance de 0.5–10s @ 16kHz mono Int16LE.
 */
export interface AlwaysListeningUtterancePayload {
  wavBuffer: Uint8Array;  // WAV encoded — encodeFloat32ToWav.ts output
  timestamp: number;      // Unix ms — quando VAD disparou speech-end
}

/**
 * VoiceModeDegradedEvent — emitido quando Always-Listening falha ao iniciar (D-09, D-16).
 * Consumer: Phase 41 tray (toast acionável). Main emite via EventEmitter + IPC broadcast.
 */
export interface VoiceModeDegradedEvent {
  attemptedMode: VoiceMode;
  reason: 'classifier-load-fail' | 'classifier-download-fail' | 'timeout' | 'permission-denied';
  message: string; // Mensagem pt-BR para exibir no toast (Phase 41)
}
```

3. Adicionar `vadSilenceThresholdMs` ao `SettingsData` interface:
```typescript
export interface SettingsData {
  pttHotkey: string;
  ttsProvider: TtsProviderOption;
  ttsApiKey: string;
  whisperModelOverride: WhisperModelOption;
  vadSilenceThresholdMs: number; // Phase 40 — default 500ms, range 300-800ms (VLISTEN-04)
}
```

Também adicionar ao `SaveSettingsRequest`:
```typescript
export interface SaveSettingsRequest {
  pttHotkey?: string;
  ttsProvider?: TtsProviderOption;
  ttsApiKey?: string;
  whisperModelOverride?: WhisperModelOption;
  // vadSilenceThresholdMs NÃO está aqui — aplicado via IPC 'always-listening:vad-threshold'
  // (real-time apply sem save button, conforme UI-SPEC.md)
}
```

**Em `apps/desktop/src/main/voiceInput/whisperResources.ts`:**

A `WhisperInstance.transcribeData()` retorna `Promise<{ result?: string }>` — sem confidence.
Para implementar D-08 (pre-filtro por STT confidence < 0.5) sem depender de API inexistente:

1. Adicionar interface `TranscribeResult` com campos opcionais:
```typescript
/**
 * TranscribeResult — resultado do transcribeData().
 * Phase 40 (D-08): se whisper.cpp expuser confidence por segment, usar 'confidence'.
 * Caso contrário, AlwaysListeningStrategy usa heurística de fallback:
 *   - transcript vazio ou só pontuação → confidence = 0 (skip classifier)
 *   - transcript válido sem confidence field → confidence = 1.0 (assume confident)
 * Fallback documentado aqui para evitar descoberta silenciosa nas waves seguintes.
 */
export interface TranscribeResult {
  result?: string;         // Transcript text (já existia)
  confidence?: number;     // 0-1 per-segment confidence (se disponível no @fugood/whisper.node)
}
```

2. Verificar se `@fugood/whisper.node` expõe confidence: caso positivo, atualizar o tipo `WhisperInstance`:
```typescript
// ATUALIZAR esta linha em whisperResources.ts:
// DE:
//   transcribeData(audioBuffer: ArrayBuffer, options?: { language?: string }): { stop: () => Promise<void>; promise: Promise<{ result?: string }> };
// PARA:
transcribeData(audioBuffer: ArrayBuffer, options?: { language?: string }): {
  stop: () => Promise<void>;
  promise: Promise<TranscribeResult>;
};
```

Se `@fugood/whisper.node` não expõe confidence (likely — verificar durante execução), adicionar comentário explícito:
```typescript
// NOTE (Phase 40 D-08): @fugood/whisper.node não expõe per-segment confidence.
// AlwaysListeningStrategy usa heurística fallback: transcript vazio → skip classifier.
// Ver alwaysListening.ts extractSttConfidence() para implementação.
```

Isso fecha o Open Question A3/Q1 do RESEARCH.md sem bloquear as waves seguintes.
  </action>
  <verify>
    <automated>grep "AlwaysListeningUtterancePayload" /root/jarvis/apps/desktop/src/shared/ipc-types.ts && grep "VoiceModeDegradedEvent" /root/jarvis/apps/desktop/src/shared/ipc-types.ts && grep "ALWAYS_LISTENING_UTTERANCE" /root/jarvis/apps/desktop/src/shared/ipc-types.ts && grep "vadSilenceThresholdMs" /root/jarvis/apps/desktop/src/shared/ipc-types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ALWAYS_LISTENING_START: 'always-listening:start'" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "ALWAYS_LISTENING_UTTERANCE: 'always-listening:utterance'" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "VOICE_MODE_DEGRADED: 'voiceMode:degraded'" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "export interface AlwaysListeningUtterancePayload" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "export interface VoiceModeDegradedEvent" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "reason: 'classifier-load-fail'" apps/desktop/src/shared/ipc-types.ts` retorna match
    - `grep "vadSilenceThresholdMs: number" apps/desktop/src/shared/ipc-types.ts` retorna match (em SettingsData)
    - `grep "TranscribeResult" apps/desktop/src/main/voiceInput/whisperResources.ts` retorna match
    - `npx tsc --noEmit` executado em apps/desktop/ não produz erros novos em ipc-types.ts ou whisperResources.ts
  </acceptance_criteria>
  <done>ipc-types.ts tem todos os tipos e channels Phase 40; whisperResources.ts tem TranscribeResult com confidence opcional e fallback documentado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → IPC handler | vadSilenceThresholdMs vem de input do usuário — clamp obrigatório antes de persistir |
| electron-store → main process | Valor lido pode estar corrompido (user editou JSON) — clamp no read necessário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-VAD | Tampering | getVadSilenceThresholdMs() / setVadSilenceThresholdMs() | mitigate | Clamp duplo: no write (setVadSilenceThresholdMs) E no read (getVadSilenceThresholdMs retorna 500 se fora de range) — protege contra corrupção de store e input malicioso do UI |
| T-40-DEGRADE | Denial of Service | VoiceModeDegradedEvent interface | mitigate | Interface define payload tipado com `reason` discriminado — consumer Phase 41 pode exibir mensagem acionável ao invés de travar em always-listening |
| T-40-AUDIT | Information Disclosure | TranscribeResult.confidence | accept | confidence é metadata de qualidade, não PII; não persiste em log por default (D-12 off by default) |
| T-40-MODEL-DL | Tampering | whisperResources.ts (fallback doc) | accept | TranscribeResult.confidence é opcional — fallback heurístico documentado previne dependência de API não-confiável; Transformers.js verifica ONNX signatures via Hugging Face CDN |
</threat_model>

<verification>
1. `npx tsc --noEmit` no diretório apps/desktop — zero erros novos
2. `npx vitest run src/main/__tests__/store.test.ts --no-coverage` — todos passando (>= 20 testes)
3. `grep -c "ALWAYS_LISTENING" apps/desktop/src/shared/ipc-types.ts` — retorna 4 (4 channels novos)
4. `grep "vadSilenceThresholdMs" apps/desktop/src/shared/ipc-types.ts apps/desktop/src/main/store.ts` — matches em ambos os arquivos
</verification>

<success_criteria>
- store.ts exporta getVadSilenceThresholdMs/setVadSilenceThresholdMs com clamp [300,800] e default 500
- ipc-types.ts tem 4 novos channels ALWAYS_LISTENING_* + VOICE_MODE_DEGRADED, 2 novas interfaces
- SettingsData inclui vadSilenceThresholdMs: number
- whisperResources.ts tem TranscribeResult interface com confidence?: number e fallback documentado
- 5 novos testes de store passando; nenhuma regressão na suite existente
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/40-always-listening-intent-classifier/40-02-SUMMARY.md`
</output>
