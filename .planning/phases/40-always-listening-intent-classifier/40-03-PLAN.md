---
phase: 40-always-listening-intent-classifier
plan: 03
type: execute
wave: 1
depends_on: [40-01]
files_modified:
  - apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts
  - apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts
  - apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts
autonomous: true
requirements: [VLISTEN-02, VLISTEN-03]

must_haves:
  truths:
    - "AudioRingBuffer com 16000 samples de capacidade nunca cresce além desse limite — escrita circular descarta samples antigos"
    - "IntentClassifier classifica 'abre o terminal' como hasIntent=true (score > 0.6) e 'uh' como hasIntent=false"
    - "Timeout de 300ms → send-anyway path executa sem exceção não tratada"
    - "Unload() libera o extractor (evita leak de memória ao sair de always-listening)"
  artifacts:
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts"
      provides: "AudioRingBuffer classe com write/toArray/clear/getSize"
      exports: ["AudioRingBuffer"]
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts"
      provides: "IntentClassifier classe com load/classify/unload"
      exports: ["IntentClassifier", "IntentClassifierOptions", "IntentClassificationResult", "AuditLogEntry"]
    - path: "apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts"
      provides: "Few-shot examples pt-BR pre-computados (25 exemplos: 15 positive, 10 negative)"
      exports: ["INTENT_EXAMPLES_PT_BR"]
  key_links:
    - from: "apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts"
      to: "apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts"
      via: "import INTENT_EXAMPLES_PT_BR"
      pattern: "INTENT_EXAMPLES_PT_BR"
    - from: "apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts"
      to: "apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts"
      via: "new AudioRingBuffer(preRollFrames * 2)"
      pattern: "new AudioRingBuffer"
---

<objective>
Implementar os dois componentes de renderer que rodam dentro do loop de always-listening: AudioRingBuffer (VLISTEN-03 — pre-roll 500ms sem leak) e IntentClassifier (VLISTEN-02 — multilingual-e5-small ONNX, cosine similarity, few-shot pt-BR). Esses dois módulos são independentes entre si e do Plan 02 — rodando em Wave 1 paralela.

Purpose: Componentes atômicos testáveis individualmente — AudioRingBuffer sem dependencies externas; IntentClassifier com @xenova/transformers. AlwaysListeningEngine (Plan 04, Wave 2) os compõe.

Output: 3 arquivos — audioRingBuffer.ts, intentClassifier.ts, intentExamples.pt-BR.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md
@.planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md
@.planning/phases/40-always-listening-intent-classifier/40-VALIDATION.md

<interfaces>
<!-- Contratos que esses módulos devem implementar -->

IntentClassificationResult (publicado pelo Plan 02 via ipc-types, mas definido aqui para isolamento):
```typescript
export interface IntentClassificationResult {
  hasIntent: boolean;
  score: number;
  verdict: 'classified-intent' | 'classified-no-intent' | 'timeout-send-anyway' | 'stt-confidence-filtered';
  latencyMs: number;
}

export interface AuditLogEntry {
  timestamp: string;
  transcript: string;
  sttConfidence: number;
  classifierScore: number;
  verdict: IntentClassificationResult['verdict'];
}

export interface IntentClassifierOptions {
  modelId: string;          // 'Xenova/multilingual-e5-small'
  threshold: number;        // 0.6 (D-07, override via INTENT_THRESHOLD env)
  sttConfidenceThreshold: number; // 0.5 (D-08)
  timeoutMs: number;        // 300 (D-10)
  auditLog?: (entry: AuditLogEntry) => void; // D-12 opt-in
}
```

AudioRingBuffer API:
```typescript
export class AudioRingBuffer {
  constructor(capacityInSamples: number);
  write(samples: Float32Array): void;
  toArray(): Float32Array; // drains buffer
  clear(): void;
  getSize(): number;
}
```

Few-shot examples structure:
```typescript
export const INTENT_EXAMPLES_PT_BR: {
  positive: string[];  // 15 exemplos
  negative: string[];  // 10 exemplos
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar AudioRingBuffer (VLISTEN-03 — ring buffer sem leak)</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts (scaffold Wave 0 — testes que devem passar)
    - apps/desktop/src/renderer/src/voice/encodeFloat32ToWav.ts (padrão Float32Array no renderer)
    - .planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md (Pattern 3: Audio Ring Buffer — implementação manual Float32Array)
  </read_first>
  <behavior>
    - Construtor com `capacityInSamples` aloca `Float32Array(capacityInSamples)` FIXO — sem crescimento dinâmico
    - `write(samples)` escreve circular: when full, readHead avança (descarta oldest)
    - `toArray()` retorna Float32Array com amostras na ordem correta; drena o buffer (clear interno)
    - `clear()` reseta writeHead/readHead para 0; NÃO realoca o backing array
    - `getSize()` retorna número de samples disponíveis (0..capacity)
    - Writing 1M samples nunca aloca heap além do buffer inicial (sem push, sem concat)
  </behavior>
  <action>
Criar `apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts` com implementação manual Float32Array circular (sem ringbufferjs — conforme Open Question 5 do RESEARCH.md: ringbufferjs tem 6 anos sem updates; implementação manual provada no RESEARCH.md Pattern 3 é mais segura):

```typescript
/**
 * AudioRingBuffer — Fixed-size circular buffer para pre-roll de áudio (VLISTEN-03).
 *
 * Implementação: Float32Array estática com writeHead/readHead circulares.
 * NÃO usa dynamic array (push/pop) — previne memory leak em sessões longas (Pitfall #2).
 *
 * Capacidade recomendada: 500ms @ 16kHz = 8000 samples → construir com 16000 para margem.
 *
 * @see .planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md §Pattern 3
 * @see .planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md D-03
 */
export class AudioRingBuffer {
  private readonly buffer: Float32Array;
  private writeHead = 0;
  private readHead = 0;
  private count = 0; // samples disponíveis para read

  constructor(private readonly capacity: number) {
    this.buffer = new Float32Array(capacity);
  }

  /**
   * write — escreve samples no buffer circular.
   * Se buffer está cheio, sobrescreve os samples mais antigos (oldest-first discard).
   */
  write(samples: Float32Array): void {
    for (let i = 0; i < samples.length; i++) {
      this.buffer[this.writeHead] = samples[i]!;
      this.writeHead = (this.writeHead + 1) % this.capacity;

      if (this.count < this.capacity) {
        this.count++;
      } else {
        // Buffer cheio: avança readHead (descarta oldest)
        this.readHead = (this.readHead + 1) % this.capacity;
      }
    }
  }

  /**
   * toArray — retorna todos os samples disponíveis em ordem cronológica.
   * Drena o buffer após a leitura (clear implícito).
   */
  toArray(): Float32Array {
    const size = this.count;
    const result = new Float32Array(size);

    for (let i = 0; i < size; i++) {
      const pos = (this.readHead + i) % this.capacity;
      result[i] = this.buffer[pos]!;
    }

    this.clear();
    return result;
  }

  /**
   * clear — reseta ponteiros sem realocar o backing array (zero GC pressure).
   */
  clear(): void {
    this.writeHead = 0;
    this.readHead = 0;
    this.count = 0;
  }

  /** Número de samples disponíveis para leitura. */
  getSize(): number {
    return this.count;
  }
}
```

Rodar os testes do scaffold: `npm test --prefix apps/desktop -- --testPathPattern=audioRingBuffer --no-coverage`

Os it.todo ainda devem aparecer como "todo" — marcar como `it(` (não `it.todo(`) nos describe blocks relevantes e implementar os assertions concretos para os comportamentos listados no `<behavior>`.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export class AudioRingBuffer" apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts` retorna match
    - `grep "private readonly buffer: Float32Array" apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts` retorna match (prova que não usa dynamic array)
    - `grep "push\|pop\|concat\|Array.from" apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts` retorna VAZIO (sem dynamic array — T-40-RING)
    - Testes de audioRingBuffer.test.ts passam (0 falhas relacionadas ao buffer, it.todo excluídos)
    - `npx tsc --noEmit` não produz erros em audioRingBuffer.ts
  </acceptance_criteria>
  <done>AudioRingBuffer com backing array fixo implementado; testes de circular behavior, clear sem realloc e getSize passando</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar few-shot examples e IntentClassifier (VLISTEN-02)</name>
  <read_first>
    - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts (scaffold Wave 0)
    - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json (fixtures para validação)
    - .planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md (Pattern 2: Intent Classifier — código completo)
    - .planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md (D-05..D-08, D-12 — decisions locked)
  </read_first>
  <behavior>
    - `load()` chama `pipeline('feature-extraction', { model: 'Xenova/multilingual-e5-small', quantized: false })` e pré-computa embeddings dos few-shot examples
    - `classify('abre o terminal', 1.0)` → `{ hasIntent: true, score > 0.6, verdict: 'classified-intent' }`
    - `classify('uh', 1.0)` → `{ hasIntent: false, score <= 0.6, verdict: 'classified-no-intent' }`
    - `classify('qualquer coisa', 0.3)` (sttConfidence < 0.5) → `{ hasIntent: true, verdict: 'stt-confidence-filtered' }` (D-08)
    - `classify('qualquer coisa', 1.0)` com timeout artificial de 300ms → `{ hasIntent: true, verdict: 'timeout-send-anyway' }` (D-10)
    - `unload()` seta extractor = null e limpa fewShotEmbeddings
    - INTENT_THRESHOLD = process.env.INTENT_THRESHOLD ? parseFloat(process.env.INTENT_THRESHOLD) : 0.6 (D-07)
  </behavior>
  <action>
**1. Criar `apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts`:**

```typescript
/**
 * intentExamples.pt-BR.ts — Few-shot examples pt-BR para intent classifier (D-06, VLISTEN-02).
 *
 * 15 positivos (comandos/perguntas que JARVIS deve processar) +
 * 10 negativos (ruído ambiente, filler words, conversas casuais descartáveis).
 *
 * Rationale de seleção (Claude's Discretion — D-06):
 *   - Positivos cobrem: comandos curtos (abre/fecha/liga), perguntas WH (qual/como/quando),
 *     confirmações com contexto (oi JARVIS + intent), pedidos de ação complexos
 *   - Negativos cobrem: filler words universais (uh/hmm), confirmações sem intent (tá bom/sim),
 *     fragmentos de frase, conversas que não pedem ação do JARVIS
 */
export const INTENT_EXAMPLES_PT_BR = {
  positive: [
    'abre o terminal',
    'abre o navegador',
    'fecha essa janela',
    'qual a hora agora',
    'como tá o tempo hoje',
    'liga a música',
    'pausa a música',
    'próxima música',
    'reproduz a playlist do trabalho',
    'aumenta o volume',
    'diminui o volume',
    'qual é a previsão do tempo para amanhã',
    'me faz um resumo do dia',
    'ei JARVIS preciso de ajuda',
    'oi JARVIS qual a data de hoje',
  ],
  negative: [
    'uh',
    'hmm',
    'deixa aí',
    'tá bom',
    'é isso aí',
    'então tá',
    'ah tá',
    'sei lá',
    'vamos ver',
    'hm hm',
  ],
} as const;
```

**2. Criar `apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts`:**

Implementar conforme Pattern 2 do RESEARCH.md com as seguintes precisões:

- `INTENT_THRESHOLD = process.env.INTENT_THRESHOLD ? parseFloat(process.env.INTENT_THRESHOLD) : 0.6` (D-07 com override env)
- Import: `import { pipeline } from '@xenova/transformers';` — checar se deve ser `@xenova/transformers` ou `@huggingface/transformers` (pacote migrou de nome; usar o que estiver em package.json ou instalar @xenova/transformers@2.6)
- Instalar dependência se ausente: `npm install --save --prefix apps/desktop @xenova/transformers@^2.6.0`
- `load()` deve configurar `env.cacheDir` para `${userData}/models/multilingual-e5-small/` ANTES de chamar `pipeline()` (D-14). No renderer, `userData` não está disponível via `app.getPath()` — usar IPC para buscar o path OU usar env var `TRANSFORMERS_CACHE` se disponível. Alternativa prática: `env.localModelPath = undefined` (usa cache padrão do Transformers.js em `~/.cache/huggingface`) e anotar que D-14 path customizado é best-effort no renderer; main process pre-download (D-15, Plan 05) usa o path correto.
- Pre-computar embeddings dos exemplos em `precomputeExamples()` chamado dentro de `load()` — usando `INTENT_EXAMPLES_PT_BR` importado de `intentExamples.pt-BR.ts`
- `cosineSimilarity(a, b)` implementado com guard `+ 1e-8` no denominador (evita divisão por zero)
- Audit log (D-12): se `process.env.ALWAYS_LISTENING_AUDIT === 'true'` e `opts.auditLog` fornecido, escrever entry — OFF by default
- Classificação: calcular cosine similarity do embedding do input vs cada few-shot embedding; max similarity entre positivos vs max similarity entre negativos; se `maxPositive > threshold` E `maxPositive > maxNegative` → `hasIntent = true`

Nota sobre mocking em testes: `@xenova/transformers` deve ser mockado no Jest/Vitest para testes unitários (sem download real do modelo). O scaffold de teste deve incluir:
```typescript
vi.mock('@xenova/transformers', () => ({
  pipeline: vi.fn().mockResolvedValue(vi.fn().mockResolvedValue({ data: new Float32Array(384).fill(0.1) })),
  env: { cacheDir: '' },
}));
```
Adicionar isso ao `intentClassifier.test.ts` (que foi criado no Wave 0).
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export class IntentClassifier" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna match
    - `grep "export interface IntentClassificationResult" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna match
    - `grep "timeout-send-anyway" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna match (D-10)
    - `grep "stt-confidence-filtered" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna match (D-08)
    - `grep "ALWAYS_LISTENING_AUDIT" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna match (D-12 — T-40-AUDIT)
    - `grep "export const INTENT_EXAMPLES_PT_BR" apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts` retorna match
    - `grep -c "" apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts` retorna > 20 (arquivo não vazio)
    - Testes de intentClassifier.test.ts passam com mock de @xenova/transformers (0 falhas não-todo)
    - `grep "push\|splice" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` retorna VAZIO em contexto de fewShotEmbeddings (usa Map, não array dinâmico)
  </acceptance_criteria>
  <done>IntentClassifier com pipeline multilingual-e5-small, cosine similarity, pre-filter D-08, timeout D-10, audit log D-12 opt-in implementado; few-shot examples pt-BR em arquivo separado</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VAD audio stream → AudioRingBuffer | Chunks de audio não validados chegam continuamente — buffer deve descartar, não crescer |
| STT transcript → IntentClassifier | Transcript pode ser vazio, só pontuação, ou texto garbled — pre-filtro D-08 protege |
| Hugging Face CDN → Transformers.js | Modelo baixado pode estar corrompido ou ser poisoned — T-40-MODEL-DL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-RING | Information Disclosure | AudioRingBuffer | mitigate | Float32Array fixo com `const capacity` — `write()` never increases `this.buffer.length`; verificado via `grep "push\|concat"` vazio; testes de write 1M samples |
| T-40-INTENT | Tampering | IntentClassifier.classify() | mitigate | `cosineSimilarity()` opera em embeddings normalizados (L2 normalize via pooling); guard 1e-8 no denominador; binary output (não execução de comando direto) |
| T-40-TIMEOUT | Business Logic | IntentClassifier.classify() timeout path | mitigate | `Promise.race([classify(), timeout(300)])` → send-anyway; log warn com latencyMs; nunca silencia utterance real em falha de CPU |
| T-40-AUDIT | Information Disclosure | IntentClassifier auditLog (D-12) | mitigate | `ALWAYS_LISTENING_AUDIT=true` env var requerido (off by default); log textual apenas (transcript + scores), nunca áudio raw; verificado via grep de `ALWAYS_LISTENING_AUDIT` |
| T-40-MODEL-DL | Tampering | @xenova/transformers pipeline() | accept | Transformers.js verifica hashes ONNX via Hugging Face CDN; modelo é read-only após download; classificação é advisory (não executa código arbitrário) |
</threat_model>

<verification>
1. `cd apps/desktop && npx vitest run src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts --no-coverage` — ambos passando
2. `grep "push\|splice\|concat" apps/desktop/src/renderer/src/voice/alwaysListening/audioRingBuffer.ts` — VAZIO (sem dynamic array)
3. `grep "export" apps/desktop/src/renderer/src/voice/alwaysListening/intentClassifier.ts` — contém IntentClassifier, IntentClassifierOptions, IntentClassificationResult, AuditLogEntry
4. `cat apps/desktop/src/main/voiceMode/intentExamples.pt-BR.ts | grep -c "'"` — retorna >= 25 (15+10 strings)
</verification>

<success_criteria>
- AudioRingBuffer: backing array fixo, write circular, toArray drena, clear sem realloc, getSize correto
- IntentClassifier: load/classify/unload funcionando com mock; pre-filter sttConfidence; timeout send-anyway; audit log opt-in
- intentExamples.pt-BR.ts: 15 positivos + 10 negativos pt-BR cobrindo comandos, perguntas, greetings e noise
- Nenhum dynamic array em nenhum dos dois arquivos (prova contra Pitfall #2)
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/40-always-listening-intent-classifier/40-03-SUMMARY.md`
</output>
