---
phase: 40-always-listening-intent-classifier
plan: 06
type: execute
wave: 3
depends_on: [40-04, 40-05]
files_modified:
  - apps/desktop/src/main/ipc/settings.ts
  - apps/desktop/src/renderer/src/settings/SettingsForm.tsx
  - apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts
autonomous: false
requirements: [VLISTEN-04]

user_setup: []

must_haves:
  truths:
    - "Slider 'VAD Silence Threshold' aparece em Settings entre 'Push-to-Talk' e 'Text-to-Speech'"
    - "Mover o slider de 300→800ms e soltar aplica a mudança sem reiniciar o app"
    - "settings:get retorna vadSilenceThresholdMs com default 500 para usuários do v1.8"
    - "Reset button retorna para 500ms e aplica imediatamente"
  artifacts:
    - path: "apps/desktop/src/main/ipc/settings.ts"
      provides: "handler settings:get inclui vadSilenceThresholdMs; handler always-listening:vad-threshold registrado"
      contains: "vadSilenceThresholdMs"
    - path: "apps/desktop/src/renderer/src/settings/SettingsForm.tsx"
      provides: "Seção Always-Listening com slider 300-800ms, value display, helper text, reset button"
      contains: "VAD Silence Threshold"
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts"
      provides: "listener para vad:threshold-changed IPC event que chama reconfigureVadThreshold()"
      contains: "vad:threshold-changed"
  key_links:
    - from: "apps/desktop/src/renderer/src/settings/SettingsForm.tsx"
      to: "apps/desktop/src/main/ipc/settings.ts"
      via: "window.ipc.invoke('always-listening:vad-threshold', ms)"
      pattern: "always-listening:vad-threshold"
    - from: "apps/desktop/src/main/ipc/settings.ts"
      to: "apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts"
      via: "mainWindow.webContents.send('vad:threshold-changed', clamped)"
      pattern: "vad:threshold-changed"
---

<objective>
Implementar o Settings UI slider para VAD Silence Threshold (VLISTEN-04) e o IPC handler de runtime apply, fechando o ciclo de configuração em tempo real. Inclui: atualizar settings:get handler, adicionar seção Always-Listening ao SettingsForm.tsx conforme UI-SPEC.md, e conectar o event 'vad:threshold-changed' ao reconfigureVadThreshold() do AlwaysListeningEngine. Checkpoint de verificação visual ao final.

Purpose: Usuário pode calibrar o VAD silence threshold sem reiniciar o app — requisito VLISTEN-04.

Output: Slider funcional em Settings + IPC round-trip de aplicação em tempo real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md
@.planning/phases/40-always-listening-intent-classifier/40-UI-SPEC.md
@.planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md

<interfaces>
<!-- Contratos existentes que este plan conecta -->

De apps/desktop/src/main/ipc/settings.ts (settings:get atual — Phase 34):
```typescript
ipcMain.handle(IPC_CHANNELS.SETTINGS_GET, (): SettingsData => {
  return {
    pttHotkey: getPttHotkey(),
    ttsProvider: getTtsProvider(),
    ttsApiKey: getTtsApiKey(),
    whisperModelOverride: getWhisperModelOverride(),
    // vadSilenceThresholdMs FALTANDO — Plan 02 adicionou ao tipo, aqui adicionar ao retorno
  };
});
```

De apps/desktop/src/shared/ipc-types.ts (Plan 02):
```typescript
export interface SettingsData {
  pttHotkey: string;
  ttsProvider: TtsProviderOption;
  ttsApiKey: string;
  whisperModelOverride: WhisperModelOption;
  vadSilenceThresholdMs: number; // adicionado no Plan 02
}
IPC_CHANNELS.ALWAYS_LISTENING_VAD_THRESHOLD = 'always-listening:vad-threshold'
```

De apps/desktop/src/main/store.ts (Plan 02):
```typescript
export function getVadSilenceThresholdMs(): number; // default 500
export function setVadSilenceThresholdMs(ms: number): void; // clamp [300,800]
```

De apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts (Plan 04):
```typescript
async reconfigureVadThreshold(negativeFramesToClose: number): Promise<void>;
// engine instance acessível via variável singleton no renderer ou via window global
```

De apps/desktop/src/renderer/src/settings/SettingsForm.tsx (padrão existente Phase 34):
```typescript
// Componente React existente com sections: Push-to-Talk, TTS, STT
// Usa useState + useEffect + window.settings.get() para carregar valores
// Slider novo deve inserir ENTRE Push-to-Talk e TTS
// window.ipc.invoke('channel', data) — pattern existente para send-and-await
```

UI-SPEC.md Slider (APROVADO, 6/6 dimensões):
```
Section: "Always-Listening"
Label: "VAD Silence Threshold" + right-aligned "{value} ms"
Input: type=range min=300 max=800 step=50 aria-label="VAD silence threshold in milliseconds"
Helper: "Silence threshold after speech ends. Lower = more responsive but may trigger on breathing/clicks. Higher = patient but may miss end of phrase. Typical: 400–600ms."
Button: "Reset to Default (500ms)"
CSS: cyan-500 thumb, gray-800 track, glow box-shadow on hover
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar settings:get e registrar always-listening:vad-threshold handler</name>
  <read_first>
    - apps/desktop/src/main/ipc/settings.ts (LEITURA OBRIGATÓRIA — arquivo completo para ver estrutura e onde adicionar)
    - apps/desktop/src/main/ipc/__tests__/settings.test.ts (padrão de teste para verificar que não quebramos nada)
    - apps/desktop/src/main/store.ts (importar getVadSilenceThresholdMs adicionada no Plan 02)
  </read_first>
  <action>
Em `apps/desktop/src/main/ipc/settings.ts`:

1. Adicionar import de `getVadSilenceThresholdMs` e `setVadSilenceThresholdMs` de store.ts:
```typescript
import {
  // ... imports existentes ...
  getVadSilenceThresholdMs,
  setVadSilenceThresholdMs,
} from '../../store.js'; // verificar path relativo
```

2. Atualizar handler `settings:get` para incluir `vadSilenceThresholdMs`:
```typescript
ipcMain.handle(IPC_CHANNELS.SETTINGS_GET, (): SettingsData => {
  return {
    pttHotkey: getPttHotkey(),
    ttsProvider: getTtsProvider(),
    ttsApiKey: getTtsApiKey(),
    whisperModelOverride: getWhisperModelOverride(),
    vadSilenceThresholdMs: getVadSilenceThresholdMs(), // Phase 40 VLISTEN-04 — default 500
  };
});
```

3. Adicionar handler `always-listening:vad-threshold` dentro da função `setupSettingsHandlers(mainWindow)`:
```typescript
// Phase 40 VLISTEN-04 — VAD threshold runtime apply (sem restart)
ipcMain.handle(IPC_CHANNELS.ALWAYS_LISTENING_VAD_THRESHOLD, async (_, ms: number) => {
  const clamped = Math.max(300, Math.min(800, ms)); // T-40-VAD: clamp defensivo na borda do IPC
  setVadSilenceThresholdMs(clamped); // persiste no store
  
  // Broadcast para renderer aplicar em runtime (reconfigureVadThreshold no AlwaysListeningEngine)
  if (!mainWindow.isDestroyed()) {
    mainWindow.webContents.send('vad:threshold-changed', clamped);
  }
  
  return { success: true };
});
```

**Nota:** O handler `always-listening:vad-threshold` também está sendo registrado em AlwaysListeningStrategy (Plan 05). Verificar duplicação — se já está em AlwaysListeningStrategy, remover de settings.ts e garantir que AlwaysListeningStrategy.stop() faz removeHandler. Se preferir centralizar em settings.ts (sempre ativo, não só em always-listening), remover de AlwaysListeningStrategy e manter aqui. Decisão: manter em settings.ts (sempre disponível para salvar a preferência mesmo fora de always-listening mode) e remover o handler duplicado de AlwaysListeningStrategy.start(). AlwaysListeningStrategy.start() apenas envia o valor atual via IPC ALWAYS_LISTENING_START.
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx vitest run src/main/ipc/__tests__/settings.test.ts --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "vadSilenceThresholdMs: getVadSilenceThresholdMs" apps/desktop/src/main/ipc/settings.ts` retorna match
    - `grep "ALWAYS_LISTENING_VAD_THRESHOLD" apps/desktop/src/main/ipc/settings.ts` retorna match
    - `grep "Math.max(300, Math.min(800" apps/desktop/src/main/ipc/settings.ts` retorna match (T-40-VAD clamp)
    - `grep "vad:threshold-changed" apps/desktop/src/main/ipc/settings.ts` retorna match (broadcast)
    - Testes de settings.test.ts passam sem regressão (>= testes anteriores)
    - `grep "vadSilenceThresholdMs" apps/desktop/src/main/ipc/__tests__/settings.test.ts` retorna match (teste de coverage)
  </acceptance_criteria>
  <done>settings:get inclui vadSilenceThresholdMs; always-listening:vad-threshold handler registrado com clamp + broadcast; testes atualizados</done>
</task>

<task type="auto">
  <name>Task 2: Implementar slider no SettingsForm.tsx e listener vad:threshold-changed no AlwaysListeningEngine</name>
  <read_first>
    - apps/desktop/src/renderer/src/settings/SettingsForm.tsx (LEITURA OBRIGATÓRIA — arquivo completo, ver estrutura de seções e padrão de state + handlers)
    - apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts (Plan 04 — onde adicionar listener de vad:threshold-changed)
    - .planning/phases/40-always-listening-intent-classifier/40-UI-SPEC.md (contrato visual APROVADO 6/6)
  </read_first>
  <action>
**Em `apps/desktop/src/renderer/src/settings/SettingsForm.tsx`:**

1. Adicionar estado para o slider:
```typescript
const [vadThresholdMs, setVadThresholdMs] = useState(500);
```

2. Carregar o valor no useEffect existente (onde window.settings.get() é chamado):
```typescript
useEffect(() => {
  window.settings.get().then((data) => {
    // ... campos existentes ...
    setVadThresholdMs(data.vadSilenceThresholdMs ?? 500); // default 500 para v1.8 upgrade
  });
}, []);
```

3. Adicionar handler de mudança com debounce (não enviar IPC em cada pixel do slider — enviar no mouseup):
```typescript
async function handleVadThresholdChange(ms: number): Promise<void> {
  setVadThresholdMs(ms); // UI update imediato
  try {
    // window.ipc.invoke ou window.jarvis.ipcRenderer — verificar API disponível no settings preload
    await (window as any).ipc?.invoke?.('always-listening:vad-threshold', ms)
       ?? window.settings?.get?.(); // fallback se ipc não disponível (verificar preload)
  } catch (err) {
    console.error('[SettingsForm] Failed to apply VAD threshold:', err);
  }
}

function handleVadThresholdReset(): void {
  void handleVadThresholdChange(500);
}
```

4. Inserir a seção "Always-Listening" entre Push-to-Talk e TTS (exatamente conforme UI-SPEC.md):
```jsx
{/* Phase 40 — Always-Listening Section (VLISTEN-04) */}
<section className="space-y-4">
  <div className="border-t border-white/20 my-4" />
  
  <h2 className="text-base font-medium text-white mb-3">Always-Listening</h2>
  
  <div className="space-y-3">
    {/* Label + value display */}
    <div className="flex justify-between items-baseline">
      <label
        htmlFor="vad-threshold-slider"
        className="text-xs font-medium text-white/70"
      >
        VAD Silence Threshold
      </label>
      <span className="text-xs font-medium text-white">
        {vadThresholdMs} ms
      </span>
    </div>
    
    {/* Slider input */}
    <input
      id="vad-threshold-slider"
      type="range"
      min="300"
      max="800"
      step="50"
      value={vadThresholdMs}
      onChange={(e) => void handleVadThresholdChange(parseInt(e.target.value, 10))}
      aria-label="VAD silence threshold in milliseconds"
      aria-valuemin={300}
      aria-valuemax={800}
      aria-valuenow={vadThresholdMs}
      aria-valuetext={`${vadThresholdMs} milliseconds`}
      className="w-full h-2 bg-gray-800 rounded-full appearance-none cursor-pointer"
      style={{
        background: `linear-gradient(to right, #1f2937 0%, #1f2937 ${((vadThresholdMs - 300) / 500) * 100}%, rgba(255,255,255,0.1) ${((vadThresholdMs - 300) / 500) * 100}%, rgba(255,255,255,0.1) 100%)`,
      }}
    />
    
    {/* Helper text */}
    <p className="text-xs text-white/50">
      Silence threshold after speech ends. Lower = more responsive but may trigger
      on breathing/clicks. Higher = patient but may miss end of phrase.
      Typical: 400–600ms.
    </p>
    
    {/* Reset button */}
    <button
      type="button"
      onClick={handleVadThresholdReset}
      className="mt-2 px-3 py-1.5 text-xs font-medium text-white/60 bg-transparent border border-white/20 rounded hover:border-white/40 hover:text-white transition-colors"
    >
      Reset to Default (500ms)
    </button>
  </div>
</section>
```

5. Adicionar CSS para o thumb do slider (em arquivo de CSS global ou via style tag no componente):
```css
/* Em apps/desktop/src/renderer/src/styles/globals.css ou equivalente */
input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: rgb(6, 182, 212); /* cyan-500 */
  cursor: pointer;
  box-shadow: 0 0 8px rgba(6, 182, 212, 0.4);
  transition: box-shadow 0.2s ease-out;
}
input[type="range"]::-moz-range-thumb {
  width: 20px;
  height: 20px;
  border-radius: 50%;
  background: rgb(6, 182, 212);
  cursor: pointer;
  border: none;
  box-shadow: 0 0 8px rgba(6, 182, 212, 0.4);
}
input[type="range"]:hover::-webkit-slider-thumb {
  box-shadow: 0 0 12px rgba(6, 182, 212, 0.6);
}
input[type="range"]:focus {
  outline: 2px solid rgba(6, 182, 212, 0.5);
  outline-offset: 2px;
}
```

**Em `apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts` (Plan 04):**

Adicionar no método `start()` um listener para `'vad:threshold-changed'` que chama `reconfigureVadThreshold()`:

```typescript
// Dentro de start(), após inicializar VAD session:
// Escutar mudanças de threshold em runtime (VLISTEN-04)
const handleThresholdChange = (_: any, ms: number) => {
  const negFrames = Math.floor((ms / 1000) * 16000 / 1536);
  void this.reconfigureVadThreshold(negFrames);
};

window.jarvis.ipcRenderer?.on('vad:threshold-changed', handleThresholdChange);

// Guardar referência para remover em stop():
this.thresholdChangeListener = handleThresholdChange;
```

Em `stop()`, adicionar:
```typescript
if (this.thresholdChangeListener) {
  window.jarvis.ipcRenderer?.off('vad:threshold-changed', this.thresholdChangeListener);
  this.thresholdChangeListener = null;
}
```

Adicionar campo privado: `private thresholdChangeListener: ((event: any, ms: number) => void) | null = null;`
  </action>
  <verify>
    <automated>grep "VAD Silence Threshold" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsForm.tsx && grep "vadThresholdMs" /root/jarvis/apps/desktop/src/renderer/src/settings/SettingsForm.tsx && grep "vad:threshold-changed" /root/jarvis/apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "VAD Silence Threshold" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna match
    - `grep "vadThresholdMs" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna múltiplos matches (state + render + handler)
    - `grep "min.*300.*max.*800\|min={300}.*max={800}" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna match (range correto)
    - `grep "step.*50\|step={50}" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna match (50ms increments)
    - `grep "Reset to Default (500ms)" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna match
    - `grep "aria-label.*VAD silence threshold" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` retorna match (acessibilidade)
    - `grep "vad:threshold-changed" apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts` retorna match (listener registrado)
    - `grep "thresholdChangeListener" apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts` retorna match (cleanup em stop)
    - `npx tsc --noEmit` não produz erros em SettingsForm.tsx ou AlwaysListeningEngine.ts
  </acceptance_criteria>
  <done>Slider Always-Listening em Settings com range 300-800ms, step 50, aria labels, reset button, e listener vad:threshold-changed no engine para runtime apply</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Settings UI com slider "VAD Silence Threshold" (Phase 40 VLISTEN-04):
    - Seção "Always-Listening" entre Push-to-Talk e Text-to-Speech
    - Slider range 300-800ms, step 50ms, default 500ms
    - Value display dinâmico "XXX ms" à direita do label
    - Helper text explicando trade-off responsiveness vs patience
    - Reset button "Reset to Default (500ms)"
    - Slider aplica mudança em tempo real via IPC sem reiniciar o app
  </what-built>
  <how-to-verify>
    1. Iniciar o app: `npm run dev --prefix apps/desktop`
    2. Abrir Settings (ícone de engrenagem ou hotkey de Settings)
    3. Verificar: seção "Always-Listening" aparece entre "Push-to-Talk" e "Text-to-Speech"
    4. Verificar: slider visível com thumb cyan, track cinza, valor "500 ms" à direita
    5. Arrastar slider para 300ms — verificar label muda para "300 ms" imediatamente
    6. Arrastar slider para 800ms — verificar label muda para "800 ms" imediatamente
    7. Clicar "Reset to Default (500ms)" — verificar que retorna para 500ms
    8. Fechar e reabrir Settings — verificar que o valor salvo persiste (não volta para 500 se mudou)
    9. (Se modo always-listening ativo) Verificar nos logs do main que "vad:threshold-changed" aparece ao mover slider
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se todos os 9 pontos passaram.
    Ou descreva os problemas para diagnóstico.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsForm → IPC always-listening:vad-threshold | Valor de slider (número) cruzando de renderer para main |
| main always-listening:vad-threshold handler → store | Valor deve ser clamped antes de persistir |
| main → renderer vad:threshold-changed | Valor broadcast de volta ao renderer engine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-VAD | Tampering | always-listening:vad-threshold IPC handler | mitigate | Clamp triplo: (1) HTML input min/max previne valores fora de 300-800 no renderer; (2) ipc handler faz Math.max(300, Math.min(800, ms)); (3) setVadSilenceThresholdMs também clamp — defesa em profundidade |
| T-40-RING | Information Disclosure | thresholdChangeListener cleanup | mitigate | `stop()` remove listener via `ipcRenderer.off` e anula referência — sem listener fantasma após dispose() que poderia acionar reconfigureVadThreshold() em engine descartado |
| T-40-AUDIT | Information Disclosure | SettingsForm.tsx | accept | Nenhum dado de áudio ou transcript exibido em Settings — apenas threshold em ms (metadata de configuração, sem PII) |
</threat_model>

<verification>
1. `grep "vadSilenceThresholdMs" apps/desktop/src/main/ipc/settings.ts` — presente no handler settings:get
2. `grep "ALWAYS_LISTENING_VAD_THRESHOLD" apps/desktop/src/main/ipc/settings.ts` — handler registrado
3. `grep "VAD Silence Threshold" apps/desktop/src/renderer/src/settings/SettingsForm.tsx` — seção presente
4. `grep "vad:threshold-changed" apps/desktop/src/renderer/src/voice/alwaysListening/AlwaysListeningEngine.ts` — listener presente
5. `npx tsc --noEmit` — zero erros novos
6. Checkpoint humano: slider visual, value update, runtime apply, reset, persist
</verification>

<success_criteria>
- settings:get handler retorna vadSilenceThresholdMs (default 500 para v1.8 upgrade path)
- Slider renderiza na posição correta no Settings entre Push-to-Talk e TTS
- Mover slider aplica threshold em runtime via IPC round-trip (renderer→main→renderer→engine)
- Reset button retorna para 500ms e aplica
- Valor persiste entre sessões (electron-store)
- Listener removido em dispose() — sem memory leak
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/40-always-listening-intent-classifier/40-06-SUMMARY.md`
</output>
