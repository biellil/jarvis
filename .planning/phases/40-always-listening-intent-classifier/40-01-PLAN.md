---
phase: 40-always-listening-intent-classifier
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/AlwaysListeningEngine.test.ts
  - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts
  - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts
  - apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json
  - apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts
autonomous: true
requirements: [VLISTEN-01, VLISTEN-02, VLISTEN-03, VLISTEN-04]

must_haves:
  truths:
    - "Test stubs existem para os 4 módulos Phase 40 — executar `npm test --prefix apps/desktop -- --testPathPattern=alwaysListening` retorna tests encontrados (mesmo que falhem com 'not implemented')"
    - "Fixtures pt-BR contêm 15 positivos e 10 negativos cobertos por describe blocks de teste"
    - "Cada arquivo de teste tem pelo menos um describe block + it.todo para cada critério de aceitação de VLISTEN-01..04"
  artifacts:
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/AlwaysListeningEngine.test.ts"
      provides: "Test scaffold para VLISTEN-01 (VAD speech-end, pre-roll, ring buffer drain)"
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts"
      provides: "Test scaffold para VLISTEN-02 (classify, threshold 0.6, timeout 300ms, pt-BR accuracy)"
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts"
      provides: "Test scaffold para VLISTEN-03 (fixed-size circular, no leak, pre-roll 8000 samples)"
    - path: "apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json"
      provides: "Fixtures pt-BR JSON para testes do classificador"
    - path: "apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts"
      provides: "Test scaffold para IPC contract + Settings slider + degrade event (VLISTEN-04)"
  key_links:
    - from: "intentClassifier.test.ts"
      to: "fixtures/intent-pt-br.json"
      via: "import/require JSON"
      pattern: "intent-pt-br.json"
---

<objective>
Criar 5 arquivos de test scaffold (Wave 0 — Nyquist) para os módulos da Phase 40, com fixtures pt-BR. Nenhuma implementação real — apenas stubs RED que falharão com "module not found" ou "not implemented" até as waves 1-3 preencherem os módulos reais.

Purpose: Garantir que o executor de cada wave subsequente saiba exatamente quais comportamentos implementar — os testes são o contrato executável.

Output: 5 arquivos de teste com describe/it.todo, 1 fixture JSON, diretório alwaysListening/ criado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-always-listening-intent-classifier/40-CONTEXT.md
@.planning/phases/40-always-listening-intent-classifier/40-RESEARCH.md
@.planning/phases/40-always-listening-intent-classifier/40-VALIDATION.md
@.planning/phases/39-voice-mode-state-machine/39-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes que os testes devem verificar -->

De apps/desktop/src/shared/ipc-types.ts (Phase 39):
```typescript
export type VoiceMode = 'wake-word' | 'always-listening' | 'ptt-only';
export interface VoiceModeChangeEvent {
  oldMode: VoiceMode;
  newMode: VoiceMode;
  reason: 'user' | 'system';
  timestamp: number;
}
```

De apps/desktop/src/main/voiceMode/index.ts (Phase 39):
```typescript
export interface VoiceCaptureStrategy {
  start(): Promise<void>;
  stop(): Promise<void>;
  dispose(): Promise<void>;
  getStatus(): 'idle' | 'capturing' | 'processing';
}
```

Interfaces planejadas para Phase 40 (declaradas nos testes, implementadas nas waves seguintes):
```typescript
// intentClassifier.ts
export interface IntentClassificationResult {
  hasIntent: boolean;
  score: number;
  verdict: 'classified-intent' | 'classified-no-intent' | 'timeout-send-anyway' | 'stt-confidence-filtered';
  latencyMs: number;
}

// audioRingBuffer.ts
export class AudioRingBuffer {
  constructor(capacityInSamples: number);
  write(samples: Float32Array): void;
  toArray(): Float32Array;
  clear(): void;
  getSize(): number;
}

// AlwaysListeningEngine.ts (renderer)
export class AlwaysListeningEngine {
  constructor(opts: AlwaysListeningEngineOptions);
  start(stream: MediaStream): Promise<void>;
  stop(): Promise<void>;
  dispose(): Promise<void>;
  reconfigureVadThreshold(negativeFramesToClose: number): Promise<void>;
}

// AlwaysListeningStrategy (main)
export class AlwaysListeningStrategy implements VoiceCaptureStrategy {
  start(): Promise<void>;
  stop(): Promise<void>;
  dispose(): Promise<void>;
  getStatus(): 'idle' | 'capturing' | 'processing';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar fixtures pt-BR e test scaffolds do renderer (3 arquivos)</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (padrão de test scaffold existente — vitest, describe, it.todo)
    - apps/desktop/jest.config.js (verificar se existe; se não, verificar vitest.config.ts ou package.json scripts)
    - apps/desktop/src/renderer/src/voice/wakeWord/__tests__/ (padrão de teste no renderer)
  </read_first>
  <action>
Criar o diretório e os 4 arquivos de teste do renderer + fixture JSON:

**1. Fixture `intent-pt-br.json`** (15 positivos, 10 negativos):
```json
{
  "positive": [
    "abre o terminal",
    "abre o navegador",
    "fecha essa janela",
    "qual a hora agora",
    "como tá o tempo",
    "liga a música",
    "pausa a música",
    "próxima música",
    "reproduz a playlist",
    "aumenta o volume",
    "diminui o volume",
    "toca alguma coisa",
    "ei JARVIS me ajuda",
    "olá JARVIS",
    "oi JARVIS qual a previsão"
  ],
  "negative": [
    "uh",
    "hmm",
    "deixa aí",
    "tá bom",
    "é isso aí",
    "então tá",
    "ah tá",
    "sei lá",
    "vamos ver",
    "hm hm"
  ]
}
```

**2. `audioRingBuffer.test.ts`** — VLISTEN-03:
```typescript
// Wave 0 scaffold — testa AudioRingBuffer (implementado na Wave 1)
// Requisito: VLISTEN-03 — ring buffer fixo 500ms (8000 samples @ 16kHz)

describe('AudioRingBuffer (VLISTEN-03, T-40-RING)', () => {
  describe('construction', () => {
    it.todo('creates buffer with given capacityInSamples');
    it.todo('initial getSize() returns 0');
  });

  describe('write()', () => {
    it.todo('write() stores samples up to capacity');
    it.todo('write() overwrites oldest samples when full (circular behavior)');
    it.todo('write() 8000 samples (500ms @ 16kHz pre-roll) fits exactly in capacity 16000');
  });

  describe('toArray()', () => {
    it.todo('toArray() returns Float32Array with written samples in order');
    it.todo('toArray() drains buffer — getSize() is 0 after call');
    it.todo('toArray() returns empty Float32Array when buffer is empty');
  });

  describe('clear()', () => {
    it.todo('clear() resets getSize() to 0 without freeing backing buffer');
    it.todo('clear() allows reuse without creating new buffer (no GC pressure)');
  });

  describe('memory safety (T-40-RING)', () => {
    it.todo('writing 1M samples does not grow backing buffer size beyond initial capacity');
    it.todo('getSize() never exceeds capacityInSamples');
  });
});
```

**3. `intentClassifier.test.ts`** — VLISTEN-02:
```typescript
// Wave 0 scaffold — testa IntentClassifier (implementado na Wave 1)
// Requisito: VLISTEN-02 — classifier local filtra falsos positivos

import fixtures from './fixtures/intent-pt-br.json';

describe('IntentClassifier (VLISTEN-02, T-40-INTENT)', () => {
  describe('load()', () => {
    it.todo('load() resolves without throwing when model available');
    it.todo('load() throws descriptive error when model unavailable — triggers D-09 path');
  });

  describe('classify() — binary output', () => {
    it.todo('returns { hasIntent: boolean, score: 0-1, verdict, latencyMs }');
    it.todo('score > 0.6 (INTENT_THRESHOLD) → hasIntent = true, verdict = classified-intent');
    it.todo('score <= 0.6 → hasIntent = false, verdict = classified-no-intent');
  });

  describe('classify() — pt-BR fixtures (VLISTEN-02 accuracy)', () => {
    fixtures.positive.forEach((text) => {
      it.todo(`positive: "${text}" → hasIntent = true`);
    });
    fixtures.negative.forEach((text) => {
      it.todo(`negative: "${text}" → hasIntent = false`);
    });
  });

  describe('STT confidence pre-filter (D-08)', () => {
    it.todo('sttConfidence < 0.5 → verdict = stt-confidence-filtered, hasIntent = true (send-anyway)');
    it.todo('sttConfidence >= 0.5 → runs full classification');
    it.todo('empty transcript with any confidence → stt-confidence-filtered');
  });

  describe('timeout path (D-10, T-40-TIMEOUT)', () => {
    it.todo('classify() resolves within 300ms for typical transcripts');
    it.todo('if classification exceeds 300ms → hasIntent = true, verdict = timeout-send-anyway');
    it.todo('timeout path logs warn with latencyMs measurement');
  });

  describe('unload()', () => {
    it.todo('unload() releases model from memory — extractor set to null');
    it.todo('classify() after unload() throws or returns timeout-send-anyway');
  });
});
```

**4. `AlwaysListeningEngine.test.ts`** — VLISTEN-01 + VLISTEN-03:
```typescript
// Wave 0 scaffold — testa AlwaysListeningEngine (implementado na Wave 2)
// Requisito: VLISTEN-01 (VAD detects speech-end) + VLISTEN-03 (pre-roll)

describe('AlwaysListeningEngine (VLISTEN-01, VLISTEN-03, T-40-VAD, T-40-RING)', () => {
  describe('start() + VAD integration (VLISTEN-01)', () => {
    it.todo('start(stream) initializes AudioContext and VAD session');
    it.todo('VAD onSpeechEnd callback fires after negativeFramesToClose frames of silence');
    it.todo('VAD onVoiceStart resets utteranceChunks and records speechStartedAt');
    it.todo('utterances < minUtteranceDurationMs (200ms) are discarded without sending');
  });

  describe('pre-roll concatenation (VLISTEN-03)', () => {
    it.todo('handleSpeechEnd() prepends ring buffer snapshot to utterance chunks');
    it.todo('ring buffer is cleared after each utterance capture');
    it.todo('resulting wavBuffer starts with pre-roll audio (not silenced leading edge)');
  });

  describe('onUtteranceReady callback', () => {
    it.todo('onUtteranceReady called with Uint8Array (valid WAV buffer) after speech-end');
    it.todo('onUtteranceReady NOT called for utterances below minUtteranceDurationMs');
  });

  describe('reconfigureVadThreshold() (VLISTEN-04)', () => {
    it.todo('reconfigureVadThreshold(n) updates negativeFramesToClose in active VAD session');
    it.todo('reconfigureVadThreshold() before start() does not throw');
  });

  describe('stop() + dispose() lifecycle (T-40-RING, D-04 lazy lifecycle)', () => {
    it.todo('stop() ends VAD session and stops all mic stream tracks');
    it.todo('dispose() calls stop() then unloads classifier and clears ring buffer');
    it.todo('dispose() called twice does not throw (idempotent)');
  });
});
```
  </action>
  <verify>
    <automated>ls /root/jarvis/apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json && ls /root/jarvis/apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/AlwaysListeningEngine.test.ts && ls /root/jarvis/apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/intentClassifier.test.ts && ls /root/jarvis/apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/audioRingBuffer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json` existe e contém arrays "positive" (15 itens) e "negative" (10 itens) verificável via `cat ... | grep -c '"abre o terminal"'`
    - `audioRingBuffer.test.ts` contém `describe('AudioRingBuffer (VLISTEN-03, T-40-RING)'` verificável via grep
    - `intentClassifier.test.ts` contém `import fixtures from './fixtures/intent-pt-br.json'` verificável via grep
    - `intentClassifier.test.ts` contém `it.todo(\`positive: "${fixtures}"\`` via grep de "stt-confidence-filtered"
    - `AlwaysListeningEngine.test.ts` contém `describe('AlwaysListeningEngine (VLISTEN-01, VLISTEN-03, T-40-VAD, T-40-RING)'` via grep
    - Cada arquivo de teste tem pelo menos 10 `it.todo` verificável via `grep -c "it.todo" <file>`
  </acceptance_criteria>
  <done>4 arquivos de teste scaffold + 1 fixture JSON criados no diretório alwaysListening/__tests__/</done>
</task>

<task type="auto">
  <name>Task 2: Criar test scaffold do main (AlwaysListeningStrategy + IPC + Settings)</name>
  <read_first>
    - apps/desktop/src/main/__tests__/voiceMode.test.ts (padrão de mock para VoiceModeManager)
    - apps/desktop/src/main/ipc/__tests__/settings.test.ts (padrão de teste de IPC handlers)
    - apps/desktop/src/shared/ipc-types.ts (canais IPC existentes para verificar naming)
  </read_first>
  <action>
Criar o arquivo de teste do main process para AlwaysListeningStrategy + IPC + Settings:

**Criar diretório e arquivo:** `apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts`

```typescript
// Wave 0 scaffold — testa AlwaysListeningStrategy (main coordinator) + IPC + Settings
// Requisitos: VLISTEN-01 (lifecycle), VLISTEN-04 (settings IPC), D-09 (degrade event), D-10 (timeout)

describe('AlwaysListeningStrategy — main coordinator (VLISTEN-01, D-01)', () => {
  describe('VoiceCaptureStrategy interface compliance', () => {
    it.todo('implements start() → sends always-listening:start IPC to renderer');
    it.todo('implements stop() → sends always-listening:stop IPC to renderer');
    it.todo('implements dispose() → stops + cleans up IPC listeners');
    it.todo('getStatus() returns idle initially');
    it.todo('getStatus() returns capturing after start() succeeds');
    it.todo('getStatus() returns idle after stop()');
  });

  describe('IPC lifecycle (D-01, D-04)', () => {
    it.todo('registers ipcMain handler for always-listening:utterance on start()');
    it.todo('removes ipcMain handler for always-listening:utterance on dispose()');
    it.todo('always-listening:utterance payload dispatched to voiceHandler.handleAudio pipeline');
  });

  describe('degraded event (D-09, T-40-DEGRADE)', () => {
    it.todo('start() failure emits voiceMode:degraded with reason: classifier-load-fail via VoiceModeManager');
    it.todo('degraded event payload: { attemptedMode: always-listening, reason, message }');
    it.todo('after degraded event, VoiceModeManager remains in previous mode (not stuck in always-listening)');
  });
});

describe('IPC handler: always-listening:vad-threshold (VLISTEN-04, T-40-VAD)', () => {
  it.todo('registers ipcMain.handle for always-listening:vad-threshold');
  it.todo('clamps incoming ms to range [300, 800] — values below 300 become 300');
  it.todo('clamps incoming ms to range [300, 800] — values above 800 become 800');
  it.todo('saves clamped value to store via store.set(vadSilenceThresholdMs, clamped)');
  it.todo('sends vad:threshold-changed event to mainWindow.webContents after store save');
  it.todo('handler is a no-op when always-listening is not active (does not crash)');
});

describe('IPC handler: settings:get — vadSilenceThresholdMs field (VLISTEN-04)', () => {
  it.todo('settings:get response includes vadSilenceThresholdMs field');
  it.todo('vadSilenceThresholdMs defaults to 500 when store has no value (v1.8 upgrade path)');
  it.todo('vadSilenceThresholdMs reflects stored value when set');
});

describe('Pre-download background (D-15, T-40-MODEL-DL)', () => {
  it.todo('scheduleModelPreDownload() called after app.whenReady resolves');
  it.todo('download failure does NOT throw — emits voiceMode:degraded with reason: classifier-download-fail (D-16)');
  it.todo('download success sets model available flag without emitting any event');
});
```

Nota: criar o diretório `apps/desktop/src/main/__tests__/voiceMode/` se não existir.
  </action>
  <verify>
    <automated>ls /root/jarvis/apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` existe verificável via ls
    - Arquivo contém `describe('AlwaysListeningStrategy — main coordinator (VLISTEN-01, D-01)'` via grep
    - Arquivo contém `describe('IPC handler: always-listening:vad-threshold (VLISTEN-04, T-40-VAD)'` via grep
    - Arquivo contém `describe('Pre-download background (D-15, T-40-MODEL-DL)'` via grep
    - `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` retorna >= 15
    - Arquivo contém menção a `voiceMode:degraded` e `classifier-load-fail` via grep (cobre T-40-DEGRADE)
  </acceptance_criteria>
  <done>Test scaffold do main criado com coverage de VLISTEN-01, VLISTEN-04, D-09, D-15, D-16 e threats T-40-VAD, T-40-DEGRADE, T-40-MODEL-DL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → source modules | Testes importam módulos que ainda não existem — falhas esperadas (module not found), não erros de segurança |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-RING | Information Disclosure | audioRingBuffer.test.ts | mitigate | Scaffoldar testes que verificam `getSize() <= capacity` — implementação Wave 1 deve provar via teste que buffer não cresce além do limite |
| T-40-INTENT | Tampering | intentClassifier.test.ts | mitigate | Fixtures pt-BR específicas previnem English-only bias; testes de timeout send-anyway previnem silêncio em falha |
| T-40-DEGRADE | Denial of Service | alwaysListening.test.ts (main) | mitigate | Scaffoldar teste que verifica que load fail não deixa manager preso em always-listening |
| T-40-TIMEOUT | Business Logic | intentClassifier.test.ts | mitigate | it.todo para timeout → send-anyway evita que utterance real seja descartada por CPU congestionado |
| T-40-AUDIT | Information Disclosure | intentClassifier.test.ts | accept | Audit log off by default — ALWAYS_LISTENING_AUDIT=true opt-in; Wave 0 não precisa testar (sem impl) |
</threat_model>

<verification>
Após criar os 5 arquivos:
1. `ls apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/` — 4 arquivos presentes
2. `cat apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/fixtures/intent-pt-br.json | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['positive'])==15; assert len(d['negative'])==10; print('fixtures OK')`
3. `grep -c "it.todo" apps/desktop/src/renderer/src/voice/alwaysListening/__tests__/AlwaysListeningEngine.test.ts` — >= 10
4. `grep -c "it.todo" apps/desktop/src/main/__tests__/voiceMode/alwaysListening.test.ts` — >= 15
</verification>

<success_criteria>
- 5 arquivos de test scaffold existem nos caminhos corretos
- Fixture JSON tem estrutura válida com 15 positivos e 10 negativos em pt-BR
- Cada arquivo cobre pelo menos 1 requirement VLISTEN-XX nos describe blocks
- Nenhum arquivo importa módulos que ainda existem (todo import de módulos Phase 40 fica comentado ou em it.todo)
- Todos os threat IDs T-40-RING, T-40-INTENT, T-40-DEGRADE, T-40-TIMEOUT aparecem nos describe/it labels
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/40-always-listening-intent-classifier/40-01-SUMMARY.md`
</output>
