---
phase: 16-memory-layer
plan: 01
type: execute
wave: 1
depends_on: [14-02]
files_modified:
  - apps/backend-ts/package.json
  - apps/backend-ts/src/memory/schema.ts
  - apps/backend-ts/src/memory/db.ts
  - apps/backend-ts/src/memory/index.ts
  - apps/backend-ts/src/memory/migrations/0000_init.sql
autonomous: true
requirements: [MEM-TS-01, MEM-TS-03]
must_haves:
  truths:
    - "Drizzle ORM is configured to connect to SQLite"
    - "Database schema matches Python's store.py for conversations, messages, summaries, user_profile, tool_calls"
    - "Migrations are set up for Drizzle to create/update the schema"
  artifacts:
    - path: "apps/backend-ts/src/memory/schema.ts"
      provides: "Drizzle schema definitions for all memory tables"
      exports: ["conversations", "messages", "summaries", "userProfile", "toolCalls"]
    - path: "apps/backend-ts/src/memory/db.ts"
      provides: "Drizzle database client instance"
      exports: ["db"]
    - path: "apps/backend-ts/src/memory/index.ts"
      provides: "Memory module entry point"
    - path: "apps/backend-ts/src/memory/migrations/0000_init.sql"
      provides: "Initial SQLite schema migration"
  key_links:
    - from: "apps/backend-ts/src/memory/db.ts"
      to: "better-sqlite3"
      via: "Database connection"
      pattern: "new Database\(dbPath\)"
    - from: "apps/backend-ts/src/memory/db.ts"
      to: "drizzle-orm"
      via: "Drizzle client initialization"
      pattern: "drizzle\(sqlite,"
---

<objective>
Install Drizzle ORM dependencies, define SQLite schema, and set up migrations.

Purpose: Establish the foundational database layer for TypeScript backend memory.
Output: Drizzle ORM configured, schema defined, and initial migration generated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/16-memory-layer-RESEARCH.md
</context>

<interfaces>
<!-- Replicate Python schema definitions from src/jarvis/memory/store.py -->
From src/jarvis/memory/store.py:
- conversations (id, started_at, ended_at)
- messages (id, conversation_id, role, content, created_at)
- summaries (id, conversation_id, content, created_at)
- user_profile (id, key, value, source, created_at)
- tool_calls (id, timestamp, tool_name, params_json, outcome, error)

Expected TypeScript equivalents (Drizzle schema):
- Each table defined with corresponding columns and types.
- `text` columns for timestamps and content.
- `integer` primary keys, foreign keys.
- `enum` for role and source, `json` for params_json.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Drizzle ORM, better-sqlite3, and related dependencies</name>
  <files>apps/backend-ts/package.json</files>
  <read_first>
    - apps/backend-ts/package.json
    - .planning/research/16-memory-layer-RESEARCH.md
  </read_first>
  <action>
    Add Drizzle ORM, better-sqlite3, and their TypeScript types to `apps/backend-ts/package.json`.
    Add drizzle-kit as a dev dependency.

    Run the following commands:
    ```bash
    cd apps/backend-ts
    pnpm add drizzle-orm better-sqlite3
    pnpm add -D drizzle-kit @types/better-sqlite3
    ```
  </action>
  <verify>
    <automated>cd apps/backend-ts && pnpm ls drizzle-orm better-sqlite3 drizzle-kit @types/better-sqlite3 --depth=0</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains "drizzle-orm" in dependencies
    - `package.json` contains "better-sqlite3" in dependencies
    - `package.json` contains "drizzle-kit" in devDependencies
    - `package.json` contains "@types/better-sqlite3" in devDependencies
    - `pnpm ls` shows all packages installed
  </acceptance_criteria>
  <done>Drizzle ORM and better-sqlite3 installed and configured.</done>
</task>

<task type="auto">
  <name>Task 2: Define Drizzle ORM schema for memory tables</name>
  <files>apps/backend-ts/src/memory/schema.ts</files>
  <action>
    Create `apps/backend-ts/src/memory/schema.ts` defining Drizzle ORM schemas for:
    - `conversations`
    - `messages`
    - `summaries`
    - `userProfile`
    - `toolCalls`

    Ensure column types and constraints (PK, FK, NOT NULL, UNIQUE, CHECK) match Python's `src/jarvis/memory/store.py`.
    Use `text` for timestamps and `sqliteTable` from `drizzle-orm/sqlite-core`.
    For `role` and `source`, use `sqliteEnum`.
    For `params_json`, use `text('params_json', { mode: 'json' })`.
  </action>
  <verify>
    <file_content path="apps/backend-ts/src/memory/schema.ts">
      - Check for `sqliteTable('conversations', ...)`
      - Check for `sqliteTable('messages', ...)` with FK to `conversations`
      - Check for `sqliteTable('user_profile', ...)` with `unique()` on `key`
      - Check for `sqliteTable('tool_calls', ...)`
      - Check for `sqliteEnum('role', ['user', 'assistant', 'system'])`
      - Check for `sqliteEnum('outcome', ['success', 'error', 'cancelled'])`
      - Check for `text('params_json', { mode: 'json' })`
    </file_content>
  </verify>
  <acceptance_criteria>
    - `apps/backend-ts/src/memory/schema.ts` is created
    - All five tables (`conversations`, `messages`, `summaries`, `user_profile`, `tool_calls`) are defined.
    - Column definitions and types match Python `store.py`
    - Relationships (FKs) are correctly defined.
  </acceptance_criteria>
  <done>Drizzle ORM schema defined.</done>
</task>

<task type="auto">
  <name>Task 3: Create Drizzle DB client and migrator</name>
  <files>apps/backend-ts/src/memory/db.ts</files>
  <action>
    Create `apps/backend-ts/src/memory/db.ts` to:
    - Initialize `better-sqlite3` database connection.
    - Create a Drizzle ORM client using `drizzle(sqlite, { schema })`.
    - Export the `db` client.
  </action>
  <verify>
    <file_content path="apps/backend-ts/src/memory/db.ts">
      - Check for import of `Database` from `better-sqlite3`
      - Check for import of `drizzle` from `drizzle-orm/better-sqlite3`
      - Check for `const sqlite = new Database(dbPath);`
      - Check for `export const db = drizzle(sqlite, { schema });`
    </file_content>
  </verify>
  <acceptance_criteria>
    - `apps/backend-ts/src/memory/db.ts` is created
    - `db` client is exported
    - Connects to SQLite using `better-sqlite3`
    - Uses the schema defined in `schema.ts`
  </acceptance_criteria>
  <done>Drizzle DB client created.</done>
</task>

<task type="auto">
  <name>Task 4: Configure Drizzle Kit and generate initial migration</name>
  <files>drizzle.config.ts, apps/backend-ts/src/memory/migrations/</files>
  <read_first>
    - apps/backend-ts/package.json
  </read_first>
  <action>
    Create `drizzle.config.ts` at the root of `apps/backend-ts` to configure Drizzle Kit.
    The config should point to `src/memory/db.ts` and `src/memory/schema.ts`.
    Then, generate the initial migration file.

    Run the following commands:
    ```bash
    cd apps/backend-ts
    npx drizzle-kit generate:sqlite --schema src/memory/schema.ts --out src/memory/migrations
    ```
  </action>
  <verify>
    <automated>ls apps/backend-ts/src/memory/migrations</automated>
    <file_content path="apps/backend-ts/drizzle.config.ts">
      - Check for schema path: `src/memory/schema.ts`
      - Check for out path: `src/memory/migrations`
    </file_content>
  </verify>
  <acceptance_criteria>
    - `apps/backend-ts/drizzle.config.ts` is created and correctly configured.
    - A new directory `apps/backend-ts/src/memory/migrations/` is created.
    - An initial migration SQL file (`0000_*.sql`) is generated within `migrations`.
  </acceptance_criteria>
  <done>Drizzle Kit configured and initial migration generated.</done>
</task>

<task type="auto">
  <name>Task 5: Implement migration logic and apply at startup</name>
  <files>apps/backend-ts/src/memory/migrate.ts, apps/backend-ts/src/index.ts</files>
  <action>
    Create `apps/backend-ts/src/memory/migrate.ts` to:
    - Import `db` client and `migrate` function from `drizzle-orm/better-sqlite3`.
    - Implement a `runMigrations` function that applies all pending migrations.
    Modify `apps/backend-ts/src/index.ts` to call `runMigrations` before starting the Express server.
  </action>
  <verify>
    <file_content path="apps/backend-ts/src/memory/migrate.ts">
      - Check for `import { migrate } from 'drizzle-orm/better-sqlite3/migrator';`
      - Check for `import { db } from './db';`
      - Check for `migrate(db, { migrationsFolder: './src/memory/migrations' });`
    </file_content>
    <file_content path="apps/backend-ts/src/index.ts">
      - Check for `runMigrations();` call before `app.listen`.
    </file_content>
  </verify>
  <acceptance_criteria>
    - `apps/backend-ts/src/memory/migrate.ts` is created and exports `runMigrations`.
    - `apps/backend-ts/src/index.ts` calls `runMigrations` during startup.
    - Migrations are applied successfully without errors.
  </acceptance_criteria>
  <done>Migration logic implemented and applied at startup.</done>
</task>

</tasks>
