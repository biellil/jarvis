---
phase: 90-polish-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-py/src/jarvis_desktop/speaker.py
  - apps/desktop-py/src/jarvis_desktop/chat.py
  - apps/desktop-py/tests/test_speaker.py
  - .planning/phases/90-polish-stability/90-REVIEW-FIX.md
autonomous: true
requirements:
  - POL-01
tags:
  - speaker-recognition
  - code-review
  - tests

must_haves:
  truths:
    - "save_profile escreve via tempfile.mkstemp + os.replace (atômico) — não corrompe perfil em crash"
    - "load_profile passa allow_pickle=False explícito para np.load"
    - "identify_speaker tolera perfil .npy corrompido e continua com os demais"
    - "enroll_speaker levanta EnrollmentAborted quando retries esgotam"
    - "_build_speaker_prefix não aceita mais parâmetro threshold"
    - "list_profiles filtra nomes via _safe_profile_name (silenciosamente)"
    - "test_safe_profile_name parametrizado cobre absolute path, backslash, leading dot, vazio, unicode, 64/65 chars, a-b_c"
    - "test_identify_speaker_skips_corrupted_profile passa"
    - "test_enroll_aborts_after_max_retries passa"
    - "90-REVIEW-FIX.md lista todos os 13 findings (WR-01..WR-06 + IN-01..IN-07) com status/SHA/rationale"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/speaker.py"
      provides: "Speaker recognition module com 6 warnings fechados"
      contains: "tempfile.mkstemp"
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "_build_speaker_prefix sem parâmetro threshold (WR-05)"
    - path: "apps/desktop-py/tests/test_speaker.py"
      provides: "3 testes novos (IN-04, IN-05, IN-06)"
      contains: "test_safe_profile_name"
    - path: ".planning/phases/90-polish-stability/90-REVIEW-FIX.md"
      provides: "Tabela de resolução dos 13 findings"
      contains: "| WR-01 |"
  key_links:
    - from: "apps/desktop-py/src/jarvis_desktop/speaker.py:save_profile"
      to: "apps/desktop-py/src/jarvis_desktop/config.py:218-245 (pattern atomic save)"
      via: "tempfile.mkstemp + os.replace"
      pattern: "tempfile\\.mkstemp"
    - from: "apps/desktop-py/src/jarvis_desktop/chat.py:777 (chat_loop call site)"
      to: "_build_speaker_prefix (linha 536)"
      via: "chamada sem threshold=..."
      pattern: "_build_speaker_prefix\\(speaker_result\\)"
---

<objective>
Fechar os 13 findings do code review da Phase 89 (POL-01).

Purpose: Endurecer `speaker.py` antes que a Phase 94 (Per-Speaker Memory) toque a mesma área. Resolver dívida formal v3.5 com rastro auditável.
Output: 6 warnings corrigidos, 3 testes de cobertura novos, 4 info aceitos com rationale escrito em `90-REVIEW-FIX.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-polish-stability/90-CONTEXT.md
@.planning/milestones/v3.5-phases/89-speaker-recognition/89-REVIEW.md
@apps/desktop-py/src/jarvis_desktop/speaker.py
@apps/desktop-py/src/jarvis_desktop/chat.py
@apps/desktop-py/src/jarvis_desktop/config.py
@apps/desktop-py/tests/test_speaker.py
@apps/desktop-py/tests/conftest.py

<interfaces>
<!-- Reference da assinatura atual que vai mudar -->

speaker.py (antes):
```python
def save_profile(name: str, embedding: np.ndarray) -> None: ...
def load_profile(name: str) -> np.ndarray: ...
def list_profiles() -> list[str]: ...
def identify_speaker(audio: np.ndarray, config: "JarvisConfig") -> dict[str, Any]: ...
def enroll_speaker(name: str, config: "JarvisConfig", n_utterances: int = ...) -> None: ...
```

speaker.py (depois — adiciona):
```python
class EnrollmentAborted(RuntimeError): ...
# enroll_speaker passa a levantar EnrollmentAborted ao esgotar retries (WR-04)
```

chat.py:
```python
# WR-05: remove parâmetro threshold
def _build_speaker_prefix(speaker_result) -> str: ...
# Caller em chat_loop (linha 777) passa a chamar sem threshold=
```

config.py:218-245 (referência atômica — usar como template para WR-01):
```python
with tempfile.NamedTemporaryFile(mode="w", dir=config_file.parent, delete=False, ...) as tmp:
    json.dump(config.model_dump(), tmp, indent=2)
    tmp_path = tmp.name
os.replace(tmp_path, config_file)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir WR-01..WR-04 e WR-06 em speaker.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/speaker.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/speaker.py (estado atual — funções save_profile L124, load_profile L133, list_profiles L140, identify_speaker L167, enroll_speaker L224)
    - apps/desktop-py/src/jarvis_desktop/config.py linhas 218-245 (pattern atômico de save_config — modelo para WR-01)
    - .planning/milestones/v3.5-phases/89-speaker-recognition/89-REVIEW.md seções WR-01..WR-06 (fix sugerido literal em cada)
    - apps/desktop-py/tests/conftest.py (fixtures tmp_home, mock_voice_encoder — usadas pelos testes da Task 3)
  </read_first>
  <behavior>
    - WR-01: save_profile escreve em tempfile.mkstemp(dir=str(dir_), suffix=".npy.tmp") e faz os.replace para path final. Limpa tmp em qualquer exceção (try/except OSError no unlink).
    - WR-02: load_profile passa np.load(str(path), allow_pickle=False) explícito.
    - WR-03: identify_speaker NÃO usa load_all_profiles; itera list_profiles() e faz try/except (ValueError, EOFError, OSError) por perfil, logando via _console().print(f"[SPK] perfil '{name}' corrompido — ignorando ({exc})") e continuando. Mantém variável has_any e retorna {name:"unknown", confidence:0.0, is_known:False, candidate_name:"unknown"} se has_any=False.
    - WR-04: Adicionar `class EnrollmentAborted(RuntimeError): """Enrollment cancelado por falha de captura após retries."""` no topo do módulo (após imports). Substituir o `return` silencioso após exaustão de retries por `raise EnrollmentAborted(f"Falha ao gravar amostra {slot} após {_MAX_RETRIES_PER_SLOT} tentativas")`.
    - WR-06: list_profiles itera p.glob("*.npy"), tenta _safe_profile_name(p.stem) em try/except ValueError; só adiciona ao resultado se `safe == p.stem` (stem inalterado pela sanitização). Retorna sorted(result).
  </behavior>
  <action>
    Aplicar 5 fixes em `apps/desktop-py/src/jarvis_desktop/speaker.py`:

    1. **WR-01 (save_profile, L124-130)** — substituir corpo por:
    ```python
    def save_profile(name: str, embedding: np.ndarray) -> None:
        """Salva embedding como .npy atomicamente (WR-01 — tempfile + os.replace)."""
        import os
        import tempfile
        safe = _safe_profile_name(name)
        dir_ = _speakers_dir()
        dir_.mkdir(parents=True, exist_ok=True)
        final_path = dir_ / f"{safe}.npy"
        fd, tmp_path = tempfile.mkstemp(dir=str(dir_), suffix=".npy.tmp")
        try:
            with os.fdopen(fd, "wb") as f:
                np.save(f, embedding.astype(np.float32))
            os.replace(tmp_path, final_path)
        except Exception:
            try:
                os.unlink(tmp_path)
            except OSError:
                pass
            raise
    ```

    2. **WR-02 (load_profile, L133-138)** — trocar `np.load(str(path))` por `np.load(str(path), allow_pickle=False)`.

    3. **WR-06 (list_profiles, L140-145)** — substituir corpo por:
    ```python
    def list_profiles() -> list[str]:
        """Lista nomes de perfis válidos (WR-06 — filtra via _safe_profile_name)."""
        dir_ = _speakers_dir()
        if not dir_.exists():
            return []
        result: list[str] = []
        for p in dir_.glob("*.npy"):
            try:
                safe = _safe_profile_name(p.stem)
                if safe == p.stem:
                    result.append(safe)
            except ValueError:
                continue
        return sorted(result)
    ```

    4. **WR-03 (identify_speaker, L167-220)** — substituir o bloco `profiles = load_all_profiles()` + loop por iteração defensiva. Manter assinatura e dict de retorno idênticos. Código exato:
    ```python
    best_name = "unknown"
    best_score = 0.0
    has_any = False
    for name in list_profiles():
        try:
            profile_emb = load_profile(name)
        except (ValueError, EOFError, OSError) as exc:
            _console().print(f"[SPK] perfil '{name}' corrompido — ignorando ({exc})")
            continue
        has_any = True
        score = _cosine_similarity(turn_emb, profile_emb)
        if score > best_score:
            best_score = score
            best_name = name

    if not has_any:
        return {"name": "unknown", "confidence": 0.0, "is_known": False, "candidate_name": "unknown"}
    ```

    5. **WR-04** — adicionar logo após os imports do módulo (antes de `_console`):
    ```python
    class EnrollmentAborted(RuntimeError):
        """Enrollment cancelado por falha de captura após retries (WR-04)."""
    ```
    Em `enroll_speaker`, encontrar o local onde retries esgotam (loop com `_MAX_RETRIES_PER_SLOT` e a mensagem "Abortando"); substituir o `return` final por:
    ```python
    raise EnrollmentAborted(
        f"Falha ao gravar amostra {slot} após {_MAX_RETRIES_PER_SLOT} tentativas"
    )
    ```

    Commits atômicos (5 commits para esta task, 1 por finding) — D-04:
    - `🐛 fix(speaker): salvar perfil atomicamente via tempfile + os.replace (WR-01)`
    - `🔒️ security(speaker): explicitar allow_pickle=False em np.load (WR-02)`
    - `🐛 fix(speaker): tolerar perfis corrompidos em identify_speaker (WR-03)`
    - `♻️ refactor(speaker): introduzir EnrollmentAborted em enroll_speaker (WR-04)`
    - `🐛 fix(speaker): filtrar nomes inválidos em list_profiles (WR-06)`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_speaker.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "tempfile.mkstemp" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna match em save_profile
    - `grep -n "allow_pickle=False" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna match em load_profile
    - `grep -n "class EnrollmentAborted" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna 1 match
    - `grep -n "raise EnrollmentAborted" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna pelo menos 1 match em enroll_speaker
    - `grep -n "perfil.*corrompido" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna match em identify_speaker
    - `grep -n "if safe == p.stem" apps/desktop-py/src/jarvis_desktop/speaker.py` retorna match em list_profiles
    - `cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_speaker.py -x` exits 0 (testes existentes continuam passando)
    - `git log --oneline -5 apps/desktop-py/src/jarvis_desktop/speaker.py` mostra 5 commits referenciando WR-01..WR-04, WR-06
  </acceptance_criteria>
  <done>
    speaker.py com WR-01, WR-02, WR-03, WR-04, WR-06 aplicados. 5 commits atômicos no git log. Suite test_speaker.py existente continua passando.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrigir WR-05 em chat.py (remover parâmetro threshold morto)</name>
  <files>apps/desktop-py/src/jarvis_desktop/chat.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 536-560 (definição de _build_speaker_prefix)
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 775-785 (call site em chat_loop)
    - apps/desktop-py/tests/test_chat.py (verificar se há testes chamando _build_speaker_prefix com threshold=)
  </read_first>
  <behavior>
    - _build_speaker_prefix passa a aceitar APENAS speaker_result (sem threshold).
    - Call site em chat_loop (linha 777) deixa de passar threshold=.
    - Docstring atualizada para refletir nova API.
    - Comportamento observável idêntico: "" se None, "[Name]: " se is_known, "[Name?]: " se candidate != unknown, "[unknown]: " caso contrário.
  </behavior>
  <action>
    Em `apps/desktop-py/src/jarvis_desktop/chat.py`:

    1. **Linha 536** — substituir assinatura:
    ```python
    def _build_speaker_prefix(speaker_result) -> str:
        """Constrói prefixo do turno baseado em speaker_result (D-08, WR-05).

        Args:
            speaker_result: dict {name, confidence, is_known, candidate_name} ou None

        Returns:
            "" se speaker_result is None (feature desabilitada)
            "[Name]: " se is_known=True (alta confiança)
            "[Name?]: " se is_known=False mas candidate_name != "unknown"
            "[unknown]: " se candidate_name == "unknown"
        """
    ```

    2. **Linhas 777-779 (chat_loop call site)** — substituir:
    ```python
    prefix = _build_speaker_prefix(
        speaker_result,
        threshold=getattr(config, "speaker_threshold", 0.75),
    )
    ```
    por:
    ```python
    prefix = _build_speaker_prefix(speaker_result)
    ```

    Commit único:
    - `♻️ refactor(chat): remover parâmetro threshold morto em _build_speaker_prefix (WR-05)`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_chat.py tests/test_voice_modes.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _build_speaker_prefix" apps/desktop-py/src/jarvis_desktop/chat.py` retorna exatamente 1 match e a linha NÃO contém "threshold"
    - `grep -n "_build_speaker_prefix" apps/desktop-py/src/jarvis_desktop/chat.py | wc -l` ≥ 2 (definição + 1+ call sites)
    - `grep "threshold=getattr" apps/desktop-py/src/jarvis_desktop/chat.py` NÃO retorna match (linha 779 removida)
    - `cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_chat.py -x` exits 0
    - `git log --oneline -1 apps/desktop-py/src/jarvis_desktop/chat.py` referencia WR-05
  </acceptance_criteria>
  <done>
    _build_speaker_prefix sem parâmetro threshold. Call site em chat_loop ajustado. test_chat.py + test_voice_modes.py passam. 1 commit referenciando WR-05.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Adicionar testes IN-04, IN-05, IN-06 em test_speaker.py</name>
  <files>apps/desktop-py/tests/test_speaker.py</files>
  <read_first>
    - apps/desktop-py/tests/test_speaker.py (estado atual — verificar imports e fixtures em uso)
    - apps/desktop-py/tests/conftest.py linhas 8-30 (fixtures tmp_home, mock_voice_encoder)
    - apps/desktop-py/src/jarvis_desktop/speaker.py (estado pós-Task 1 — _safe_profile_name, EnrollmentAborted, identify_speaker robusto)
    - .planning/milestones/v3.5-phases/89-speaker-recognition/89-REVIEW.md seções IN-04, IN-05, IN-06 (snippets sugeridos)
  </read_first>
  <behavior>
    - test_safe_profile_name parametrizado: rejeita "/etc/passwd", "..\\Windows\\System32", ".hidden", "", "   ", "José" (unicode), nome com 65 chars; aceita nome com 64 chars, "a-b_c".
    - test_identify_speaker_skips_corrupted_profile: planta perfil válido + .npy corrompido lado a lado; identify_speaker retorna sem exceção; resultado considera apenas o perfil válido.
    - test_enroll_aborts_after_max_retries: monkeypatch record_until_silence para retornar áudio curto; verifica que EnrollmentAborted é levantada E que arquivo .npy NÃO foi criado.
  </behavior>
  <action>
    Adicionar 3 testes em `apps/desktop-py/tests/test_speaker.py`:

    1. **IN-04: test_safe_profile_name parametrizado** — adicionar:
    ```python
    import pytest

    @pytest.mark.parametrize("name,should_raise", [
        ("/etc/passwd", True),
        ("..\\Windows\\System32", True),
        (".hidden", True),
        ("", True),
        ("   ", True),
        ("José", True),
        ("a" * 65, True),
        ("a" * 64, False),
        ("a-b_c", False),
        ("valid_name", False),
    ])
    def test_safe_profile_name(name, should_raise):
        from jarvis_desktop.speaker import _safe_profile_name
        if should_raise:
            with pytest.raises(ValueError):
                _safe_profile_name(name)
        else:
            result = _safe_profile_name(name)
            assert result == name.strip() if name.strip() else result
    ```

    2. **IN-05: test_identify_speaker_skips_corrupted_profile** — adicionar:
    ```python
    def test_identify_speaker_skips_corrupted_profile(tmp_home, mock_voice_encoder):
        import numpy as np
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        # Perfil válido + perfil corrompido lado a lado
        valid_emb = np.ones(256, dtype=np.float32) / np.sqrt(256)
        speaker.save_profile("alice", valid_emb)
        bad_path = tmp_home / ".jarvis" / "speakers" / "bad.npy"
        bad_path.parent.mkdir(parents=True, exist_ok=True)
        bad_path.write_bytes(b"NOT A VALID NPY")

        config = JarvisConfig(speaker_recognition_enabled=True)
        result = speaker.identify_speaker(np.zeros(16000, dtype=np.float32), config)

        # Deve completar sem exceção e ignorar bad.npy
        assert "candidate_name" in result
        assert result["candidate_name"] in ("alice", "unknown")
    ```

    3. **IN-06: test_enroll_aborts_after_max_retries** — adicionar:
    ```python
    def test_enroll_aborts_after_max_retries(tmp_home, mock_voice_encoder, monkeypatch):
        import numpy as np
        from jarvis_desktop import speaker
        from jarvis_desktop.config import JarvisConfig

        # Áudio sempre curto demais (1s < 2s mínimo) — dispara retry exhaust
        short_audio = np.zeros(16000, dtype=np.float32)
        monkeypatch.setattr(
            "jarvis_desktop.stt.record_until_silence",
            lambda **kw: short_audio,
        )

        config = JarvisConfig()
        with pytest.raises(speaker.EnrollmentAborted):
            speaker.enroll_speaker("test_user", config, n_utterances=1)

        # Perfil NÃO deve ter sido criado
        assert not (tmp_home / ".jarvis" / "speakers" / "test_user.npy").exists()
    ```

    Importante: se `_safe_profile_name` aceita `"   "` (após strip vira `""`) sem ValueError, ajustar o parametrize. Verifique comportamento real em speaker.py antes de fixar — caso atual já rejeita string vazia após strip.

    Commits atômicos (3 commits, 1 por teste — D-04):
    - `✅ test(speaker): cobrir _safe_profile_name com casos parametrizados (IN-04)`
    - `✅ test(speaker): cobrir perfil corrompido em identify_speaker (IN-05)`
    - `✅ test(speaker): cobrir EnrollmentAborted após exaustão de retries (IN-06)`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_speaker.py::test_safe_profile_name tests/test_speaker.py::test_identify_speaker_skips_corrupted_profile tests/test_speaker.py::test_enroll_aborts_after_max_retries -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def test_safe_profile_name" apps/desktop-py/tests/test_speaker.py` retorna match
    - `grep "def test_identify_speaker_skips_corrupted_profile" apps/desktop-py/tests/test_speaker.py` retorna match
    - `grep "def test_enroll_aborts_after_max_retries" apps/desktop-py/tests/test_speaker.py` retorna match
    - `cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_speaker.py -v` exits 0; saída contém "test_safe_profile_name", "test_identify_speaker_skips_corrupted_profile", "test_enroll_aborts_after_max_retries" todos PASSED
    - `git log --oneline -3 apps/desktop-py/tests/test_speaker.py` mostra 3 commits referenciando IN-04, IN-05, IN-06
  </acceptance_criteria>
  <done>
    3 testes novos em test_speaker.py, todos passando. 3 commits atômicos no git log.
  </done>
</task>

<task type="auto">
  <name>Task 4: Criar 90-REVIEW-FIX.md com tabela de resolução dos 13 findings</name>
  <files>.planning/phases/90-polish-stability/90-REVIEW-FIX.md</files>
  <read_first>
    - .planning/milestones/v3.5-phases/89-speaker-recognition/89-REVIEW.md (lista completa dos 13 findings — WR-01..WR-06 + IN-01..IN-07)
    - .planning/phases/90-polish-stability/90-CONTEXT.md seção D-02 (rationale dos 4 info aceitos: IN-01, IN-02, IN-03, IN-07)
  </read_first>
  <action>
    Criar `.planning/phases/90-polish-stability/90-REVIEW-FIX.md` com a estrutura:

    ```markdown
    # Phase 90: Resolução do Code Review da Phase 89

    **Data:** 2026-06-02
    **Source:** `.planning/milestones/v3.5-phases/89-speaker-recognition/89-REVIEW.md`
    **Status:** 9 fixed (6 warnings + 3 testes), 4 accepted (info)

    ## Resumo

    | Tipo | Total | Fixed | Accepted |
    |------|-------|-------|----------|
    | Warnings | 6 | 6 | 0 |
    | Info | 7 | 3 | 4 |
    | **Total** | **13** | **9** | **4** |

    ## Tabela de Resolução

    | ID | Status | Commit SHA | Rationale |
    |----|--------|------------|-----------|
    | WR-01 | fixed | <SHA-WR-01> | save_profile agora atômico via tempfile.mkstemp + os.replace |
    | WR-02 | fixed | <SHA-WR-02> | allow_pickle=False explícito em load_profile |
    | WR-03 | fixed | <SHA-WR-03> | identify_speaker tolera .npy corrompido por perfil |
    | WR-04 | fixed | <SHA-WR-04> | enroll_speaker levanta EnrollmentAborted ao esgotar retries |
    | WR-05 | fixed | <SHA-WR-05> | Removido parâmetro threshold morto em _build_speaker_prefix |
    | WR-06 | fixed | <SHA-WR-06> | list_profiles filtra nomes via _safe_profile_name |
    | IN-01 | accepted | — | Warmup proativo do VoiceEncoder é micro-otimização; padrão atual (lazy + lock) é seguro. Pode entrar em Phase 94 quando per-speaker memory tocar a área. |
    | IN-02 | accepted | — | Prints fora do lock evitam deadlock teórico mas sem evidência de problema real; padrão atual é mais simples e seguro. |
    | IN-03 | accepted | — | Log `[SPK]` gated por debug_events é polimento de UX; aceito como tech-debt para uso prolongado. |
    | IN-04 | fixed | <SHA-IN-04> | test_safe_profile_name parametrizado cobrindo absolute path, backslash, leading dot, vazio, unicode, 64/65 chars, a-b_c |
    | IN-05 | fixed | <SHA-IN-05> | test_identify_speaker_skips_corrupted_profile valida WR-03 |
    | IN-06 | fixed | <SHA-IN-06> | test_enroll_aborts_after_max_retries valida WR-04 |
    | IN-07 | accepted | — | Import top-level em voice_modes esconderia ImportError em startup; try/except atual já cobre. Ajustar quando houver bug real de import. |

    ## Próximos passos

    Substituir cada `<SHA-...>` pelo SHA real após commitar cada finding. Executar:
    ```bash
    git log --oneline --grep="WR-01" -1
    git log --oneline --grep="WR-02" -1
    # ... etc
    ```
    e popular a tabela.
    ```

    **Após** as Tasks 1-3 commitarem, atualizar os `<SHA-...>` placeholders com os hashes reais (extrair via `git log --oneline --grep="<ID>"`). Commit final:
    - `📝 docs(90): registrar resolução do code review Phase 89 em 90-REVIEW-FIX.md`
  </action>
  <verify>
    <automated>test -f /root/jarvis/.planning/phases/90-polish-stability/90-REVIEW-FIX.md && grep -c "^| WR-\|^| IN-" /root/jarvis/.planning/phases/90-polish-stability/90-REVIEW-FIX.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/90-polish-stability/90-REVIEW-FIX.md` existe
    - `grep -c "^| WR-" .planning/phases/90-polish-stability/90-REVIEW-FIX.md` retorna 6
    - `grep -c "^| IN-" .planning/phases/90-polish-stability/90-REVIEW-FIX.md` retorna 7
    - `grep "| fixed |" .planning/phases/90-polish-stability/90-REVIEW-FIX.md | wc -l` retorna 9
    - `grep "| accepted |" .planning/phases/90-polish-stability/90-REVIEW-FIX.md | wc -l` retorna 4
    - SHAs preenchidos (não há mais `<SHA-` no arquivo após popular) — verificar com `grep -c "<SHA-" .planning/phases/90-polish-stability/90-REVIEW-FIX.md` retorna 0
  </acceptance_criteria>
  <done>
    90-REVIEW-FIX.md criado com tabela completa, todos os 13 findings listados, 9 fixed com SHAs reais, 4 accepted com rationale escrito. Commit final referenciando POL-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem (~/.jarvis/speakers/) | .npy files podem ser plantados manualmente fora-de-banda (backup/restore, terceiros) |
| Process crash | Crash entre open/close de save_profile pode corromper perfil |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01-01 | Tampering | speaker.save_profile filesystem | mitigate | WR-01: tempfile.mkstemp + os.replace previne perfil corrompido em crash. Race condition em concurrent saves: nomes diferentes por design (1 perfil por nome único), `os.replace` é atômico no mesmo filesystem (POSIX + Windows). |
| T-90-01-02 | Elevation of Privilege | speaker.load_profile np.load | mitigate | WR-02: `allow_pickle=False` explícito previne arbitrary code execution via pickle embedded em .npy malicioso. Defesa em profundidade — default do NumPy desde 1.16.3 é False mas explicitar protege contra monkey-patching. |
| T-90-01-03 | Denial of Service | speaker.identify_speaker | mitigate | WR-03: try/except por perfil. Um .npy corrompido NÃO derruba o pipeline; perfil é logado e ignorado. |
| T-90-01-04 | Information Disclosure | speaker.list_profiles | mitigate | WR-06: filtra nomes via `_safe_profile_name` antes de expor na UI. Perfis com nome inválido (`evil..name.npy`) não vazam para o menu mesmo se plantados fora-de-banda. |
| T-90-01-05 | Tampering | tempfile race symlink attack | accept | `tempfile.mkstemp(dir=str(dir_))` cria arquivo com permissões 0600 e nome aleatório no mesmo dir. Symlink no destino seria do próprio usuário (~/.jarvis/speakers/) — fora do modelo de ameaça (assistente single-user local). Risco residual baixo. |
</threat_model>

<verification>
- Todos os testes do módulo speaker passam: `cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_speaker.py tests/test_chat.py tests/test_voice_modes.py -v`
- 90-REVIEW-FIX.md preenchido com 13 entradas
- `git log --oneline -10 apps/desktop-py/src/jarvis_desktop/speaker.py apps/desktop-py/src/jarvis_desktop/chat.py apps/desktop-py/tests/test_speaker.py` mostra 9 commits atômicos referenciando WR-01..WR-06 + IN-04, IN-05, IN-06 + 1 commit do REVIEW-FIX.md
</verification>

<success_criteria>
- 6 warnings WR-01..WR-06 corrigidos em código
- 3 testes IN-04, IN-05, IN-06 adicionados e passando
- 4 info IN-01, IN-02, IN-03, IN-07 aceitos com rationale escrito em 90-REVIEW-FIX.md
- 9 commits atômicos no git log, cada um referenciando o ID do finding
- Suite test_speaker.py + test_chat.py + test_voice_modes.py continua passando
</success_criteria>

<output>
After completion, create `.planning/phases/90-polish-stability/90-01-SUMMARY.md`
</output>
