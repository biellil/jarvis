---
phase: 90-polish-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-py/tests/fixtures/hello.wav
  - apps/desktop-py/tests/fixtures/README.md
  - apps/desktop-py/tests/test_e2e_pipeline.py
  - apps/desktop-py/tests/conftest.py
  - apps/desktop-py/pyproject.toml
  - apps/desktop-py/src/jarvis_desktop/voice_modes.py
  - .github/workflows/desktop-py-tests.yml
autonomous: true
requirements:
  - POL-04
tags:
  - e2e
  - ci
  - testing

must_haves:
  truths:
    - "Workflow `.github/workflows/desktop-py-tests.yml` roda `uv run pytest` em push/PR e cacheia modelos Whisper"
    - "Fixture `tests/fixtures/hello.wav` existe (3s, 16kHz mono, gerada por espeak-ng dizendo 'olá jarvis')"
    - "voice_modes.run_ptt_once(config, *, audio_provider, http_call, tts_play) -> dict é a API pública 1-shot determinística do PTT"
    - "Teste `test_ptt_full_pipeline_with_mocked_llm` cobre PTT → STT real → LLM(mock urllib) → TTS via run_ptt_once"
    - "Suite unit existente passa com `pytest -m \"not e2e\"`"
    - "Suite e2e passa isolada com `pytest -m e2e`"
    - "Header `x-jarvis-speaker` é validado: quando speaker_recognition_enabled=False, header NÃO está presente no request capturado"
  artifacts:
    - path: "apps/desktop-py/tests/fixtures/hello.wav"
      provides: "Áudio PT-BR sintetizado por espeak-ng para Whisper transcrever no CI"
    - path: "apps/desktop-py/src/jarvis_desktop/voice_modes.py"
      provides: "Função pública run_ptt_once para execução determinística 1-turn do pipeline PTT"
      contains: "def run_ptt_once"
    - path: "apps/desktop-py/tests/test_e2e_pipeline.py"
      provides: "Teste E2E do pipeline PTT"
      contains: "@pytest.mark.e2e"
    - path: ".github/workflows/desktop-py-tests.yml"
      provides: "CI workflow rodando pytest do desktop-py"
      contains: "actions/cache@v4"
    - path: "apps/desktop-py/pyproject.toml"
      provides: "Marker e2e em [tool.pytest.ini_options]"
      contains: "e2e:"
  key_links:
    - from: "test_e2e_pipeline.py"
      to: "tests/fixtures/hello.wav"
      via: "soundfile.read em fixture e2e_audio_wav"
      pattern: "fixtures/hello\\.wav"
    - from: "test_e2e_pipeline.py"
      to: "voice_modes.run_ptt_once"
      via: "chamada direta com audio_provider, http_call, tts_play injetados"
      pattern: "voice_modes\\.run_ptt_once"
    - from: "test_e2e_pipeline.py"
      to: "ui.set_state"
      via: "monkeypatch.setattr coletando estados em lista"
      pattern: "jarvis_desktop\\.ui\\.set_state"
    - from: ".github/workflows/desktop-py-tests.yml"
      to: "~/.cache/huggingface"
      via: "actions/cache@v4"
      pattern: "actions/cache@v4"
---

<objective>
Adicionar teste E2E automatizado em CI cobrindo PTT → STT → LLM(mock) → TTS sem hardware real (POL-04).

Purpose: Detectar regressões de wiring no pipeline de voz a cada PR. v3.5 não tinha cobertura E2E — refactor pode quebrar silenciosamente. Pré-requisito: extrair `run_ptt_once` em `voice_modes.py` (1-shot determinístico) para que o teste possa rodar sem listener pynput, threading, ou queue.
Output: Função `run_ptt_once` em voice_modes + workflow GH Actions + fixture WAV (espeak-ng) + 1 teste pytest com 5 asserções de pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/90-polish-stability/90-CONTEXT.md
@apps/desktop-py/tests/conftest.py
@apps/desktop-py/src/jarvis_desktop/stt.py
@apps/desktop-py/src/jarvis_desktop/tts.py
@apps/desktop-py/src/jarvis_desktop/voice_modes.py
@apps/desktop-py/src/jarvis_desktop/sse_listener.py
@apps/desktop-py/src/jarvis_desktop/chat.py
@apps/desktop-py/src/jarvis_desktop/ui.py
@.github/workflows/ci-lockfile-ban.yml

<interfaces>
<!-- API real descoberta na leitura de voice_modes.py + ui.py + chat.py -->

```python
# voice_modes.py — estado atual (pré-Task 2c):
def init_voice_modes(config: JarvisConfig) -> None: ...
def start_mode(mode: str, config: JarvisConfig) -> None: ...  # cria thread daemon com _ptt_loop
def stop_mode() -> None: ...
def switch_mode(new_mode: str, config: JarvisConfig) -> None: ...
def get_text_queue() -> Queue: ...
def _ptt_loop(config: JarvisConfig) -> None: ...  # loop infinito, listener pynput, queue
def _identify_speaker_safe(audio: np.ndarray, config: JarvisConfig) -> dict | None: ...

# voice_modes.py — Task 2c adiciona:
def run_ptt_once(
    config: JarvisConfig,
    *,
    audio_provider: Callable[[], np.ndarray],
    http_call: Callable[[str, dict], str],
    tts_play: Callable[[str], None],
) -> dict:
    """Executa 1 turn determinístico do pipeline PTT (POL-04).

    Args:
        config: JarvisConfig
        audio_provider: callable que retorna np.ndarray float32 16kHz mono (substitui captura de mic)
        http_call: callable(message: str, headers: dict) -> str (substitui urlopen + SSE; retorna texto final acumulado)
        tts_play: callable(text: str) -> None (substitui tts.speak; usado para spy)

    Returns:
        {"transcript": str, "response": str, "speaker": dict | None, "states": list[str]}
    """

# ui.py — API existente confirmada (linha 103-122):
def set_state(state: str) -> None: ...
#   Valid states: "idle", "listening", "transcribing", "thinking", "speaking"
#   Thread-safe via _lock; ignora estados inválidos silenciosamente.

# chat.py build_request_headers — assinatura real:
def build_request_headers(api_key: str, client_id: str = "", speaker_name: str = "") -> dict:
#   x-jarvis-speaker SÓ é adicionado quando speaker_name != ""
#   Quando speaker_recognition_enabled=False, _identify_speaker_safe retorna None,
#   speaker_name vira "" e o header NÃO aparece.

# chat.py usa urllib.request.urlopen (NÃO httpx) — logo mock é via monkeypatch direto.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gerar fixture hello.wav via espeak-ng (16kHz mono, ~3s, "olá jarvis")</name>
  <files>apps/desktop-py/tests/fixtures/hello.wav, apps/desktop-py/tests/fixtures/README.md</files>
  <read_first>
    - apps/desktop-py/tests/conftest.py (verificar imports soundfile, numpy disponíveis)
    - apps/desktop-py/pyproject.toml (verificar dependências de test)
  </read_first>
  <action>
    Método único e determinístico: **espeak-ng + soundfile**. Justificativa: espeak-ng está documentado em CLAUDE.md como dependência Linux (kokoro requirement) e gera PT-BR reconhecível por Whisper-tiny sem precisar carregar modelo neural durante planning/build. Sox NÃO é dependência — fazer resample em Python.

    Comando exato (rodar no diretório `apps/desktop-py`):

    ```bash
    cd /root/jarvis/apps/desktop-py
    mkdir -p tests/fixtures

    # Passo 1: gerar WAV em PT-BR via espeak-ng (sample rate default ~22050)
    espeak-ng -v pt-br -s 130 -w /tmp/hello_raw.wav "olá jarvis"

    # Passo 2: resample para 16kHz mono via Python (sem sox)
    uv run python - <<'PY'
    import soundfile as sf
    import numpy as np
    from scipy.signal import resample_poly

    data, sr = sf.read("/tmp/hello_raw.wav", dtype="float32")
    # Mono: se estéreo, fazer média (espeak-ng emite mono mas defensivo)
    if data.ndim == 2:
        data = data.mean(axis=1)

    # Resample para 16000 Hz
    if sr != 16000:
        # Cálculo dinâmico de fator: usa fração reduzida
        from math import gcd
        g = gcd(sr, 16000)
        up = 16000 // g
        down = sr // g
        data = resample_poly(data, up, down).astype(np.float32)

    # Padroniza para ~3s (48000 amostras a 16kHz). Pad com silêncio ou trim.
    target_len = 48000
    if len(data) > target_len:
        data = data[:target_len]
    else:
        data = np.pad(data, (0, target_len - len(data)))

    sf.write("tests/fixtures/hello.wav", data, 16000, subtype="PCM_16")
    print(f"OK: shape={data.shape} sr=16000 bytes~={data.nbytes}")
    PY

    # Passo 3: cleanup temp
    rm -f /tmp/hello_raw.wav

    # Verificação local antes de commit
    uv run python -c "
    import soundfile as sf
    d, sr = sf.read('tests/fixtures/hello.wav')
    assert sr == 16000 and d.ndim == 1 and 32000 <= len(d) <= 64000
    print('OK', d.shape, sr)
    "
    ```

    Pré-requisito de ambiente: `apt install espeak-ng` no host. Se `espeak-ng` não estiver instalado, o executor deve `sudo apt-get install -y espeak-ng` antes de rodar — JARVIS já lista espeak-ng como dep Linux do Kokoro (CLAUDE.md §Cross-Platform Audio Notes).

    Criar `apps/desktop-py/tests/fixtures/README.md`:

    ```markdown
    # Test Fixtures

    ## hello.wav

    - **Format:** WAV PCM_16, 16000 Hz mono, ~3 segundos (48000 amostras)
    - **Content:** "olá jarvis" (PT-BR)
    - **Generated by:** `espeak-ng -v pt-br -s 130 -w` + resample via `scipy.signal.resample_poly` para 16kHz
    - **Used by:** `tests/test_e2e_pipeline.py::test_ptt_full_pipeline_with_mocked_llm`

    ### Regenerar

    Pré-requisito: `sudo apt-get install -y espeak-ng` (Linux) ou equivalente no macOS/Windows.

    Comando completo está em `.planning/phases/90-polish-stability/90-02-PLAN.md` Task 1.
    ```

    Commit:
    - `🔧 chore(tests): adicionar fixture hello.wav PT-BR (espeak-ng) para teste E2E`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run python -c "import soundfile as sf; d, sr = sf.read('tests/fixtures/hello.wav', dtype='float32'); assert sr == 16000, f'sr={sr}'; assert d.ndim == 1, f'ndim={d.ndim}'; assert 32000 <= len(d) <= 64000, f'len={len(d)}'; print('OK', d.shape, sr)"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `apps/desktop-py/tests/fixtures/hello.wav` existe
    - `uv run python -c "import soundfile; d,sr=soundfile.read('apps/desktop-py/tests/fixtures/hello.wav'); print(sr, d.ndim, len(d))"` imprime `16000 1 48000`
    - Tamanho do arquivo entre 60KB e 200KB: `[ $(stat -c%s apps/desktop-py/tests/fixtures/hello.wav) -ge 60000 -a $(stat -c%s apps/desktop-py/tests/fixtures/hello.wav) -le 200000 ]`
    - Arquivo `apps/desktop-py/tests/fixtures/README.md` existe e contém "espeak-ng" e "hello.wav"
    - `git log --oneline -1 apps/desktop-py/tests/fixtures/hello.wav` mostra commit chore(tests)
  </acceptance_criteria>
  <done>
    hello.wav (16kHz mono ~3s PT-BR via espeak-ng) commitado em tests/fixtures/. README explica origem e como regenerar. 1 commit.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Configurar pyproject.toml — marker e2e + dep pytest opcional</name>
  <files>apps/desktop-py/pyproject.toml</files>
  <read_first>
    - apps/desktop-py/pyproject.toml (estado atual — verificar se [tool.pytest.ini_options] e markers já existem)
  </read_first>
  <action>
    Editar `apps/desktop-py/pyproject.toml`.

    1. Localizar (ou criar) a seção `[tool.pytest.ini_options]`. Adicionar/anexar lista de markers:

    ```toml
    [tool.pytest.ini_options]
    markers = [
        "e2e: end-to-end pipeline tests (PTT → STT → LLM → TTS) — slow",
    ]
    ```

    Se a seção já existir com outros markers, ANEXAR a entrada `e2e: ...` mantendo os existentes. Se `markers` ainda não existir, criar como acima. Se a seção tiver `testpaths`, `addopts`, etc., preservar tudo.

    2. **NÃO adicionar `pytest-httpx`.** Após inspeção de `chat.py`, descobrimos que o cliente HTTP é `urllib.request.urlopen`, não httpx. Por isso o teste E2E faz monkeypatch direto em `urllib.request.urlopen` (não usa pytest-httpx). Manter pyproject.toml sem deps novas.

    Commit:
    - `🏗️ build(tests): adicionar marker pytest e2e em pyproject.toml`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run python -c "import tomllib; data = tomllib.load(open('pyproject.toml', 'rb')); markers = data.get('tool', {}).get('pytest', {}).get('ini_options', {}).get('markers', []); assert any(m.startswith('e2e:') for m in markers), f'marker e2e ausente em {markers}'; print('OK markers=', markers)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\[tool.pytest.ini_options\]" apps/desktop-py/pyproject.toml` retorna 1 match
    - `grep -n "\"e2e:" apps/desktop-py/pyproject.toml` retorna 1 match
    - `cd /root/jarvis/apps/desktop-py && uv run pytest --collect-only -m e2e 2>&1 | head -5` NÃO contém "PytestUnknownMarkWarning"
    - `git log --oneline -1 apps/desktop-py/pyproject.toml` referencia "marker pytest e2e"
  </acceptance_criteria>
  <done>
    pyproject.toml com marker e2e configurado. pytest reconhece o marker sem warnings. 1 commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Adicionar fixture e2e_audio_wav em conftest.py</name>
  <files>apps/desktop-py/tests/conftest.py</files>
  <read_first>
    - apps/desktop-py/tests/conftest.py (linhas 1-50 — verificar imports e padrão de fixtures)
  </read_first>
  <behavior>
    - Fixture `e2e_audio_wav` carrega `tests/fixtures/hello.wav` via `soundfile.read(dtype="float32")` e retorna np.ndarray 1-D.
    - Asserta sr=16000 e mono — falha cedo se fixture estiver malformada.
    - Usa `Path(__file__).parent / "fixtures" / "hello.wav"` (path relativo ao conftest, independente de CWD).
  </behavior>
  <action>
    Anexar ao final de `apps/desktop-py/tests/conftest.py`:

    ```python
    @pytest.fixture
    def e2e_audio_wav():
        """Carrega tests/fixtures/hello.wav como np.float32 array (16kHz mono ~3s).

        Usado pelo teste E2E (POL-04 D-13) para mockar a captura de microfone.
        """
        import soundfile as sf
        from pathlib import Path

        path = Path(__file__).parent / "fixtures" / "hello.wav"
        audio, sr = sf.read(str(path), dtype="float32")
        assert sr == 16000, f"Expected 16kHz, got {sr}"
        assert audio.ndim == 1, f"Expected mono, got ndim={audio.ndim}"
        return audio
    ```

    Verificar se `import pytest` já está no topo de conftest.py — se não, adicionar.

    Commit:
    - `✅ test(conftest): adicionar fixture e2e_audio_wav carregando hello.wav`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run pytest --collect-only tests/conftest.py 2>&1 | grep -E "e2e_audio_wav|ERROR" | head -5 ; uv run python -c "
import sys; sys.path.insert(0, 'tests')
import importlib.util
spec = importlib.util.spec_from_file_location('conftest', 'tests/conftest.py')
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
assert hasattr(m, 'e2e_audio_wav'), 'fixture e2e_audio_wav ausente'
print('OK fixture e2e_audio_wav presente')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def e2e_audio_wav" apps/desktop-py/tests/conftest.py` retorna exatamente 1 match
    - `grep "@pytest.fixture" apps/desktop-py/tests/conftest.py | wc -l` ≥ 1
    - Suite existente continua passando: `cd /root/jarvis/apps/desktop-py && uv run pytest -m "not e2e" -q` exits 0
    - `git log --oneline -1 apps/desktop-py/tests/conftest.py` referencia "e2e_audio_wav"
  </acceptance_criteria>
  <done>
    Fixture e2e_audio_wav adicionada em conftest.py. Suite unit continua passando. 1 commit.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2c: Refactor voice_modes — extrair run_ptt_once + criar test_e2e_pipeline.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/voice_modes.py, apps/desktop-py/tests/test_e2e_pipeline.py</files>
  <read_first>
    - apps/desktop-py/src/jarvis_desktop/voice_modes.py linhas 209-319 (lógica completa de _ptt_loop — fonte do extract)
    - apps/desktop-py/src/jarvis_desktop/voice_modes.py linhas 163-185 (_identify_speaker_safe — reusar direto)
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 562-605 (_stream_response — referência para http_call signature)
    - apps/desktop-py/src/jarvis_desktop/chat.py linhas 118-138 (build_request_headers — entender quando x-jarvis-speaker é setado)
    - apps/desktop-py/src/jarvis_desktop/ui.py linhas 103-122 (set_state — confirma API válida para monkeypatch)
  </read_first>
  <behavior>
    **Parte 1 — refactor voice_modes:**
    - Nova função pública `run_ptt_once(config, *, audio_provider, http_call, tts_play) -> dict`
    - Determinística: zero threading, zero pynput, zero queue, zero sd.InputStream.
    - Executa o caminho crítico do PTT em ordem: audio_provider() → _identify_speaker_safe → transcribe → http_call(prefixed_message, headers) → tts_play(response).
    - Chama `ui.set_state` nos 4 estados canônicos: "listening" (antes de capturar) → "transcribing" (após capturar) → "thinking" (durante http_call) → "speaking" (durante tts_play) → "idle" (final).
    - Retorna dict: `{"transcript": str, "response": str, "speaker": dict | None}`.
    - _ptt_loop existente NÃO é alterado — continua sendo a entrada interativa real. A refatoração apenas EXTRAI a lógica determinística para reuso pelo teste.

    **Parte 2 — teste E2E:**
    - `@pytest.mark.e2e` em test_ptt_full_pipeline_with_mocked_llm.
    - Usa `e2e_audio_wav` como `audio_provider`.
    - Mocka `http_call` para retornar `"olá humano"` (resposta determinística do "LLM").
    - Mocka `tts_play` para coletar chamadas em lista (spy).
    - Monkeypatch `ui.set_state` para coletar estados em lista.
    - Asserções (D-18):
      1. transcript não-vazio com pelo menos 1 palavra ≥ 2 chars.
      2. http_call foi chamado com headers contendo Content-Type=application/json E x-jarvis-speaker NÃO está em headers (porque config.speaker_recognition_enabled=False).
      3. response acumulada == "olá humano".
      4. tts_play recebeu o texto da response.
      5. ui_states contém ["listening", "transcribing", "thinking", "speaking"] (ordem preservada, "idle" opcional ao final).
  </behavior>
  <action>
    **Parte 1: Editar `apps/desktop-py/src/jarvis_desktop/voice_modes.py`**

    Adicionar import no topo (junto com os outros):
    ```python
    from typing import Callable, Optional
    ```

    Adicionar função pública APÓS `get_text_queue()` e ANTES de `_stop_current()` (logo após linha 138, antes da seção "Internal helpers"). Marcar como público no docstring do módulo se desejado, mas não é obrigatório editar o cabeçalho.

    ```python
    def run_ptt_once(
        config: JarvisConfig,
        *,
        audio_provider: Callable[[], np.ndarray],
        http_call: Callable[[str, dict], str],
        tts_play: Callable[[str], None],
    ) -> dict:
        """Executa 1 turn determinístico do pipeline PTT (POL-04 D-13..D-18).

        Função pública usada por testes E2E para validar wiring PTT → STT → LLM → TTS
        sem hardware real, sem threading e sem listener de teclado. Para uso interativo,
        continuar usando `start_mode("ptt", config)` que dispara o loop daemon.

        Args:
            config: JarvisConfig (usado para speaker_recognition_enabled, api_key, etc.)
            audio_provider: Callable retornando np.ndarray float32 16kHz mono.
                Substitui sd.InputStream — o teste passa fixture WAV pré-carregada.
            http_call: Callable(message, headers) -> response_text.
                Substitui urllib.request.urlopen + SSE parsing — o teste retorna texto direto.
            tts_play: Callable(text) -> None.
                Substitui tts.speak — o teste usa spy para validar TTS recebeu o texto.

        Returns:
            dict {"transcript": str, "response": str, "speaker": dict | None}
        """
        from jarvis_desktop import ui as _ui
        from jarvis_desktop.stt import transcribe
        from jarvis_desktop.chat import build_request_headers

        _ui.set_state("listening")
        audio = audio_provider()

        speaker_result = _identify_speaker_safe(audio, config)

        _ui.set_state("transcribing")
        transcript = transcribe(audio)

        # Monta mensagem com prefixo de speaker (mesmo padrão de chat_loop).
        speaker_name = ""
        prefix = ""
        if speaker_result is not None:
            speaker_name = speaker_result.get("name", "")
            if speaker_result.get("is_known"):
                prefix = f"[{speaker_result['name']}]: "
            elif speaker_result.get("candidate_name", "unknown") != "unknown":
                prefix = f"[{speaker_result['candidate_name']}?]: "
            else:
                prefix = "[unknown]: "
        message = f"{prefix}{transcript}".strip()

        headers = {
            "Content-Type": "application/json",
            **build_request_headers(
                getattr(config, "api_key", ""),
                getattr(config, "client_id", ""),
                speaker_name=speaker_name,
            ),
        }

        _ui.set_state("thinking")
        response_text = http_call(message, headers)

        _ui.set_state("speaking")
        if response_text and response_text.strip():
            tts_play(response_text)

        _ui.set_state("idle")
        return {
            "transcript": transcript,
            "response": response_text,
            "speaker": speaker_result,
        }
    ```

    Verificar que `JarvisConfig` já é importado no topo (linha 30 — confirmado). Verificar que `np` já é importado (linha 28 — confirmado).

    **Parte 2: Criar `apps/desktop-py/tests/test_e2e_pipeline.py`**

    ```python
    """Teste E2E do pipeline PTT → STT → LLM(mock) → TTS (POL-04, D-13..D-18).

    Marker @pytest.mark.e2e — rodar isolado com `pytest -m e2e`.
    Unit suite ignora com `pytest -m "not e2e"`.

    Pré-requisito: Plan 02 Task 2c expõe `voice_modes.run_ptt_once` com injeção de
    audio_provider / http_call / tts_play, eliminando threading + pynput + sd.InputStream.
    """
    from __future__ import annotations

    import numpy as np
    import pytest


    @pytest.mark.e2e
    def test_ptt_full_pipeline_with_mocked_llm(
        tmp_home,
        e2e_audio_wav,
        monkeypatch,
    ):
        """Pipeline PTT determinístico: STT real (Whisper tiny) + LLM mockado + TTS spy.

        Asserções (D-18):
          1. STT transcreveu (string não-vazia, pelo menos 1 palavra ≥ 2 chars)
          2. http_call recebeu headers corretos (Content-Type=application/json
             e x-jarvis-speaker AUSENTE quando speaker_recognition_enabled=False)
          3. SSE/LLM response acumulada e propagada
          4. tts_play recebeu o texto da response
          5. UI state sequence: listening → transcribing → thinking → speaking (→ idle)
        """
        from jarvis_desktop import voice_modes
        from jarvis_desktop.config import JarvisConfig

        # Estado: config sem speaker recognition (asserção D-18.2 valida ausência do header).
        config = JarvisConfig(
            whisper_model="tiny",
            tts_provider="kokoro",
            speaker_recognition_enabled=False,
        )

        # --- Spies ---
        http_calls: list[tuple[str, dict]] = []
        tts_calls: list[str] = []
        ui_states: list[str] = []

        # UI state spy — patch direto em jarvis_desktop.ui.set_state (existe, validado)
        monkeypatch.setattr(
            "jarvis_desktop.ui.set_state",
            lambda s: ui_states.append(s),
            raising=True,
        )

        # Mock http_call retornando resposta determinística "olá humano".
        def fake_http(message: str, headers: dict) -> str:
            http_calls.append((message, dict(headers)))
            return "olá humano"

        # Mock tts_play coletando o texto recebido.
        def fake_tts(text: str) -> None:
            tts_calls.append(text)

        # --- Execução ---
        result = voice_modes.run_ptt_once(
            config,
            audio_provider=lambda: e2e_audio_wav,
            http_call=fake_http,
            tts_play=fake_tts,
        )

        # --- Asserções (D-18) ---

        # 1. STT transcreveu — string não-vazia com pelo menos 1 palavra ≥ 2 chars
        transcript = result["transcript"]
        assert isinstance(transcript, str) and transcript.strip(), (
            f"STT deve retornar string não-vazia, recebeu: {transcript!r}"
        )
        assert any(len(w) >= 2 for w in transcript.split()), (
            f"STT deve produzir pelo menos uma palavra ≥ 2 chars, recebeu: {transcript!r}"
        )

        # 2. http_call disparado com headers corretos
        assert len(http_calls) == 1, f"Esperava 1 chamada http, recebeu {len(http_calls)}"
        sent_message, sent_headers = http_calls[0]
        assert sent_headers.get("Content-Type") == "application/json", (
            f"Content-Type esperado application/json, got {sent_headers.get('Content-Type')!r}"
        )
        # D-18.2: speaker_recognition_enabled=False → header x-jarvis-speaker NÃO presente
        assert "x-jarvis-speaker" not in sent_headers, (
            f"x-jarvis-speaker NÃO deve estar presente quando speaker_recognition_enabled=False, "
            f"headers recebidos: {sent_headers!r}"
        )

        # 3. SSE/LLM response acumulada e propagada
        assert result["response"] == "olá humano", (
            f"Response deve ser propagada, recebeu: {result['response']!r}"
        )

        # 4. TTS recebeu o texto completo
        assert tts_calls == ["olá humano"], (
            f"TTS deve receber exatamente a response, recebeu: {tts_calls!r}"
        )

        # 5. UI state sequence: listening → transcribing → thinking → speaking → idle
        expected_order = ["listening", "transcribing", "thinking", "speaking", "idle"]
        # Asserção de ordem: cada estado esperado aparece e mantém ordem relativa
        positions = []
        for state in expected_order:
            assert state in ui_states, (
                f"Estado {state!r} ausente em ui_states={ui_states!r}"
            )
            positions.append(ui_states.index(state))
        assert positions == sorted(positions), (
            f"Ordem dos estados quebrada: {ui_states!r}"
        )
    ```

    Commits atômicos:
    - `♻️ refactor(voice_modes): extrair run_ptt_once 1-shot determinístico para habilitar teste E2E (POL-04)`
    - `✅ test(e2e): cobrir pipeline PTT → STT → LLM(mock) → TTS via run_ptt_once (POL-04)`
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_e2e_pipeline.py -m e2e -v --tb=short 2>&1 | tail -25 && echo "---UNIT---" && uv run pytest -m "not e2e" -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def run_ptt_once" apps/desktop-py/src/jarvis_desktop/voice_modes.py` retorna exatamente 1 match
    - `grep "audio_provider\|http_call\|tts_play" apps/desktop-py/src/jarvis_desktop/voice_modes.py | wc -l` ≥ 6 (3 params na sig + 3 usos no corpo)
    - `grep "@pytest.mark.e2e" apps/desktop-py/tests/test_e2e_pipeline.py` retorna 1 match
    - `grep "def test_ptt_full_pipeline_with_mocked_llm" apps/desktop-py/tests/test_e2e_pipeline.py` retorna 1 match
    - `grep "x-jarvis-speaker" apps/desktop-py/tests/test_e2e_pipeline.py` retorna pelo menos 1 match (asserção de ausência)
    - `cd /root/jarvis/apps/desktop-py && uv run pytest tests/test_e2e_pipeline.py -m e2e -v` exits 0
    - `cd /root/jarvis/apps/desktop-py && uv run pytest -m "not e2e"` exits 0 (unit suite intacta)
    - `git log --oneline -2 apps/desktop-py/src/jarvis_desktop/voice_modes.py apps/desktop-py/tests/test_e2e_pipeline.py` mostra 2 commits referenciando POL-04
  </acceptance_criteria>
  <done>
    run_ptt_once exportada em voice_modes.py. test_e2e_pipeline.py passa. Unit suite continua intacta. 2 commits atômicos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar workflow .github/workflows/desktop-py-tests.yml</name>
  <files>.github/workflows/desktop-py-tests.yml</files>
  <read_first>
    - .github/workflows/ci-lockfile-ban.yml (padrão atual de workflow)
    - apps/desktop-py/pyproject.toml (verificar python_requires e dependencies extras)
  </read_first>
  <action>
    Criar `.github/workflows/desktop-py-tests.yml`:

    ```yaml
    name: CI — desktop-py tests
    on:
      pull_request:
        paths:
          - 'apps/desktop-py/**'
          - '.github/workflows/desktop-py-tests.yml'
      push:
        branches: [master, main, 'v3.*']
        paths:
          - 'apps/desktop-py/**'
          - '.github/workflows/desktop-py-tests.yml'

    jobs:
      unit-tests:
        name: Unit tests
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: apps/desktop-py
        steps:
          - uses: actions/checkout@v4

          - name: Install system audio deps
            run: sudo apt-get update && sudo apt-get install -y libportaudio2 libsndfile1 ffmpeg

          - name: Install uv
            uses: astral-sh/setup-uv@v3
            with:
              enable-cache: true

          - name: Set up Python
            run: uv python install 3.12

          - name: Install project
            run: uv sync --all-extras --dev

          - name: Run unit tests (not e2e)
            run: uv run pytest -m "not e2e" -v --tb=short

      e2e-tests:
        name: E2E pipeline tests
        runs-on: ubuntu-latest
        defaults:
          run:
            working-directory: apps/desktop-py
        steps:
          - uses: actions/checkout@v4

          - name: Install system audio deps (incl. espeak-ng for Kokoro)
            run: sudo apt-get update && sudo apt-get install -y libportaudio2 libsndfile1 ffmpeg espeak-ng

          - name: Install uv
            uses: astral-sh/setup-uv@v3
            with:
              enable-cache: true

          - name: Set up Python
            run: uv python install 3.12

          - name: Cache Whisper models
            uses: actions/cache@v4
            with:
              path: ~/.cache/huggingface
              key: huggingface-${{ runner.os }}-whisper-tiny-v1
              restore-keys: |
                huggingface-${{ runner.os }}-whisper-

          - name: Cache Kokoro models
            uses: actions/cache@v4
            with:
              path: ~/.cache/kokoro
              key: kokoro-${{ runner.os }}-v1

          - name: Install project
            run: uv sync --all-extras --dev

          - name: Run E2E tests
            run: uv run pytest -m e2e -v --tb=short
    ```

    Notas:
    - 2 jobs separados: unit-tests (rápido, sem cache de modelo) + e2e-tests (lento, com cache).
    - Cache key: `huggingface-${{ runner.os }}-whisper-tiny-v1` permite bump manual.
    - Job e2e instala espeak-ng (necessário para Kokoro em alguns paths E para regenerar fixture localmente se preciso).
    - `working-directory: apps/desktop-py` evita repetir `cd` em cada step.

    Commit:
    - `🤖 ci: adicionar workflow desktop-py-tests com unit + e2e jobs e cache HF`
  </action>
  <verify>
    <automated>test -f /root/jarvis/.github/workflows/desktop-py-tests.yml && python3 -c "import yaml; d=yaml.safe_load(open('/root/jarvis/.github/workflows/desktop-py-tests.yml')); assert set(d['jobs'].keys()) == {'unit-tests', 'e2e-tests'}, d['jobs'].keys(); print('OK', list(d['jobs'].keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.github/workflows/desktop-py-tests.yml` existe
    - `python3 -c "import yaml; print(list(yaml.safe_load(open('.github/workflows/desktop-py-tests.yml'))['jobs'].keys()))"` lista `['unit-tests', 'e2e-tests']`
    - `grep "actions/cache@v4" .github/workflows/desktop-py-tests.yml | wc -l` retorna 2 (huggingface + kokoro)
    - `grep "huggingface-\${{ runner.os }}-whisper-tiny" .github/workflows/desktop-py-tests.yml` retorna 1 match
    - `grep -E 'pytest -m "not e2e"' .github/workflows/desktop-py-tests.yml` retorna 1 match
    - `grep "pytest -m e2e" .github/workflows/desktop-py-tests.yml` retorna 1 match
    - `grep "libportaudio2\|libsndfile1\|espeak-ng" .github/workflows/desktop-py-tests.yml | wc -l` ≥ 3
  </acceptance_criteria>
  <done>
    Workflow YAML válido criado com 2 jobs (unit + e2e), cache HF, deps de áudio Linux incluindo espeak-ng, working_directory apps/desktop-py. 1 commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner | Workflow executa em runner ubuntu-latest com `pull_request` e `push` triggers |
| HF cache | Cache de modelos Whisper compartilhado entre runs via cache key |
| Test fixture | hello.wav é áudio sintético gerado por espeak-ng — sem dados pessoais reais |
| run_ptt_once injection points | audio_provider, http_call, tts_play são callables injetados pelo chamador. Em produção, _ptt_loop continua usando hardware real; run_ptt_once é apenas para testes e usos programáticos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-02-01 | Information Disclosure | hello.wav fixture | mitigate | Áudio gerado sinteticamente via espeak-ng com texto neutro ("olá jarvis"); não contém dados pessoais nem credenciais. README.md documenta a origem para auditoria futura. |
| T-90-02-02 | Information Disclosure | GH Actions secrets em logs | mitigate | Workflow não usa `secrets.*` — testes E2E são fechados (LLM mockado, sem chamadas a serviços externos com API key). Nenhum env var sensível é printado. |
| T-90-02-03 | Tampering | HF cache key | accept | Cache key `huggingface-${{ runner.os }}-whisper-tiny-v1` é determinística e não inclui conteúdo do PR. Cache poisoning improvável pois cache GH é scoped por branch/ref e somente repo collaborators podem invalidar via push. |
| T-90-02-04 | Denial of Service | E2E test latency | accept | Job e2e usa Whisper tiny (~75MB, ~1s init) + Kokoro cached. Tempo esperado 2-5min após cold cache. Aceitável para validação a cada PR. |
| T-90-02-05 | Elevation of Privilege | run_ptt_once callables | mitigate | Função é pública mas só chamada por código interno (testes) — callers injetam suas próprias funções; nenhuma callable vinda de input externo é executada. Risk surface limitada ao próprio repo. |
</threat_model>

<verification>
- Unit suite passa isolada: `cd apps/desktop-py && uv run pytest -m "not e2e" -v` exits 0
- E2E suite passa isolada: `cd apps/desktop-py && uv run pytest -m e2e -v` exits 0
- Workflow YAML válido (carregável via `yaml.safe_load`)
- 6 commits no git log (fixture + pyproject + conftest + refactor voice_modes + test_e2e + workflow)
- run_ptt_once existe e é função pública em voice_modes.py
- Header x-jarvis-speaker NÃO aparece no request quando speaker_recognition_enabled=False
</verification>

<success_criteria>
- Fixture hello.wav (16kHz mono 3s PT-BR via espeak-ng) existe e tem ~96KB
- Marker pytest e2e configurado em pyproject
- voice_modes.run_ptt_once exportada com 3 callbacks injetáveis
- test_ptt_full_pipeline_with_mocked_llm valida 5 asserções (D-18) incluindo ausência de x-jarvis-speaker
- Workflow `.github/workflows/desktop-py-tests.yml` válido, com 2 jobs (unit + e2e) e cache HF
- Suite unit existente continua passando
</success_criteria>

<output>
After completion, create `.planning/phases/90-polish-stability/90-02-SUMMARY.md`
</output>
