---
phase: quick
plan: 260426-jd3
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
autonomous: true
requirements:
  - DATABASE_PATH apontando para volume persistente em /app/data/jarvis.sqlite
must_haves:
  truths:
    - "O banco SQLite persiste entre reinicializações do container"
    - "DATABASE_PATH=/app/data/jarvis.sqlite está definido no ambiente do backend-ts"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Variável DATABASE_PATH no bloco environment do serviço backend-ts"
      contains: "DATABASE_PATH=/app/data/jarvis.sqlite"
  key_links:
    - from: "docker-compose.yml (environment.DATABASE_PATH)"
      to: "apps/backend-ts/src/memory/db.ts (process.env.DATABASE_PATH)"
      via: "variável de ambiente injetada pelo Docker Compose"
---

<objective>
Adicionar DATABASE_PATH=/app/data/jarvis.sqlite no bloco environment do serviço backend-ts no docker-compose.yml.

Purpose: O volume ./data:/app/data já está montado — sem DATABASE_PATH definido, o SQLite cai no fallback process.cwd()/jarvis.sqlite (dentro do container, não persistido). Com a variável apontando para /app/data, o banco fica no volume e sobrevive a reinicializações.
Output: docker-compose.yml atualizado com a variável de ambiente, banco SQLite persistente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docker-compose.yml
@apps/backend-ts/src/memory/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar DATABASE_PATH no environment do backend-ts</name>
  <files>docker-compose.yml</files>
  <action>
No bloco `environment` do serviço `backend-ts` (linhas 51-55), adicionar a variável:

```yaml
      - DATABASE_PATH=/app/data/jarvis.sqlite
```

O bloco environment final deve ficar assim:

```yaml
    environment:
      - BACKEND_TS_PORT=8001
      - NODE_ENV=production
      - CHROMA_HOST=chromadb
      - CHROMA_PORT=8000
      - DATABASE_PATH=/app/data/jarvis.sqlite
```

Nenhuma outra alteração. O volume `./data:/app/data` já existe no serviço — apenas a variável estava faltando.
  </action>
  <verify>
    <automated>grep -n "DATABASE_PATH=/app/data/jarvis.sqlite" /root/jarvis/docker-compose.yml</automated>
  </verify>
  <done>A linha DATABASE_PATH=/app/data/jarvis.sqlite aparece no bloco environment do serviço backend-ts no docker-compose.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| host → container | Caminho do volume montado (./data) é controlado pelo host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jd3-01 | Tampering | ./data volume (host) | accept | Diretório local do desenvolvedor; single-user, sem exposição de rede |
| T-jd3-02 | Information Disclosure | jarvis.sqlite em /app/data | accept | Banco local, sem dados sensíveis de terceiros; fora do container filesystem efêmero |
</threat_model>

<verification>
Após aplicar a mudança:

1. Verificar que a linha existe no arquivo: `grep "DATABASE_PATH" docker-compose.yml`
2. (Opcional) Subir o stack e confirmar persistência: `docker compose up -d backend-ts && docker compose exec backend-ts ls /app/data/`
3. O arquivo `jarvis.sqlite` deve aparecer em `./data/` no host após o backend iniciar.
</verification>

<success_criteria>
- `docker-compose.yml` contém `DATABASE_PATH=/app/data/jarvis.sqlite` no bloco environment do serviço backend-ts
- O banco SQLite é criado em `./data/jarvis.sqlite` (volume persistido) na próxima inicialização do container
</success_criteria>

<output>
Após conclusão, não é necessário criar SUMMARY.md para tasks quick. Commit com:
`📝 chore(docker): adicionar DATABASE_PATH no backend-ts para persistência do SQLite`
</output>
