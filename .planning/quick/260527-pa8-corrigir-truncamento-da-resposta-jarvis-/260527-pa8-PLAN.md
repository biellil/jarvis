---
phase: quick
plan: 260527-pa8
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop-py/src/jarvis_desktop/chat.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Resposta completa do JARVIS aparece no terminal sem truncamento"
    - "Segunda linha e linhas subsequentes aparecem com indent correto de 9 espaços"
    - "Label [jarvis] aparece em verde/bold como antes"
  artifacts:
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "Impressão da resposta via sys.stdout.write() com ANSI codes"
  key_links:
    - from: "_read_sse_stream"
      to: "sys.stdout.write"
      via: "ANSI escape codes para bold green, wrapping manual por linha"
---

<objective>
Corrigir truncamento da resposta do JARVIS no terminal Windows.

Purpose: console.print() do Rich faz cursor management que interfere com o Live panel (transient=True), resultando na segunda linha do wrap sendo apagada/sobrescrita. Usar sys.stdout.write() diretamente com ANSI codes evita esse problema.

Output: chat.py com _read_sse_stream usando sys.stdout.write() para imprimir a resposta final, mantendo bold green para [jarvis] e indent correto nas linhas subsequentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/desktop-py/src/jarvis_desktop/chat.py
@apps/desktop-py/src/jarvis_desktop/ui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Substituir console.print() por sys.stdout.write() em _read_sse_stream</name>
  <files>apps/desktop-py/src/jarvis_desktop/chat.py</files>
  <action>
    Em _read_sse_stream (linhas ~338-348), substituir o bloco de impressão que usa console.print() por sys.stdout.write() com ANSI codes.

    Remover:
    ```python
    if full_text.strip():
        display = full_text.rstrip("\n").replace("\n", "\n" + _RESPONSE_INDENT)
        safe = _markup_escape(display)
        if main_stream:
            console.print(f"{_LABEL_JARVIS} {safe}", highlight=False)
        else:
            console.print(f"{_RESPONSE_INDENT}{safe}", highlight=False)
    else:
        console.print()
    ```

    Substituir por:
    ```python
    if full_text.strip():
        display = full_text.rstrip("\n").replace("\n", "\n" + _RESPONSE_INDENT)
        if main_stream:
            # ANSI: bold (\x1b[1m) + green (\x1b[32m) + reset (\x1b[0m)
            # Bypass Rich console to avoid cursor management truncating wrapped lines
            sys.stdout.write(f"\x1b[1m\x1b[32m[jarvis]\x1b[0m {display}\n")
        else:
            sys.stdout.write(f"{_RESPONSE_INDENT}{display}\n")
        sys.stdout.flush()
    else:
        sys.stdout.write("\n")
        sys.stdout.flush()
    ```

    Também remover a import `from rich.markup import escape as _markup_escape` que estava dentro do método (se não for usada em mais nenhum outro lugar no escopo — verificar antes). Ela só era usada para sanitizar o texto antes de passar ao console.print() do Rich; com sys.stdout.write() não precisamos escapar markup.

    ATENÇÃO: Não alterar _LABEL_YOU nem _LABEL_JARVIS — essas constantes são usadas em outros console.print() no chat_loop (linhas ~511-512 em chat_loop). Apenas o bloco de print dentro de _read_sse_stream muda.

    ATENÇÃO: O import `from rich.markup import escape as _markup_escape` está DENTRO de _read_sse_stream como lazy import (linha ~307). Após a mudança, esse import pode ser removido somente de dentro dessa função. Verificar se não é usado em outra parte do arquivo antes de remover.
  </action>
  <verify>
    1. Executar o app e enviar uma mensagem longa (ex: "escreva uma frase longa com mais de 80 caracteres"):
       cd apps/desktop-py && python -m jarvis_desktop
    2. Confirmar que a resposta aparece completa, sem truncamento na segunda linha.
    3. Confirmar que "[jarvis]" aparece em verde/bold (ANSI funciona no Windows Terminal).
    4. Smoke test rápido dos testes existentes (não devem quebrar):
       cd apps/desktop-py && uv run pytest tests/test_chat.py -x -q 2>&1 | tail -20
  </verify>
  <done>
    Resposta longa do JARVIS aparece completa no terminal. Linha 1 = "[jarvis] texto...", linhas subsequentes = "         continuação" com indent de 9 espaços. Nenhum teste de chat_py quebrado.
  </done>
</task>

</tasks>

<verification>
- Testar manualmente com mensagem que force wrap (resposta > ~80 chars)
- Confirmar que label [jarvis] permanece visível em verde/bold
- Confirmar que segunda linha aparece com indent correto
- uv run pytest tests/test_chat.py -x -q passa sem erros
</verification>

<success_criteria>
Resposta completa do JARVIS visível no terminal Windows sem truncamento, independente do comprimento. Testes existentes continuam passando.
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260527-pa8-corrigir-truncamento-da-resposta-jarvis-/260527-pa8-SUMMARY.md` com o que foi alterado e resultado do teste manual.
</output>
