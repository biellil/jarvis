---
phase: 260427-s2t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/desktop/src/renderer/hooks/usePttHandler.ts
  - apps/desktop/src/renderer/hooks/useAudioRecorder.ts
autonomous: false
requirements:
  - QUICK-260427-s2t

must_haves:
  truths:
    - "Após a 2ª apertada do PTT, o orb sai de amarelo (listening) — vai para roxo (processing) ou volta para cyan (idle), nunca trava amarelo"
    - "Logs do console do renderer mostram cada passo do fluxo de stop: entrada do handler, ramo escolhido, antes/depois de stop(), tamanho do blob, dentro de onstop, antes/depois de sendAudio"
    - "Se mediaRecorder.onstop não disparar em até 3s, stopRecording() resolve com null + warn explícito (orb não trava em amarelo)"
    - "setState('idle') é chamado em TODOS os caminhos de saída do branch de stop (early return, blob pequeno, throw, timeout)"
  artifacts:
    - path: apps/desktop/src/renderer/hooks/usePttHandler.ts
      provides: "Handler PTT com logs detalhados em todos os pontos do fluxo de stop"
    - path: apps/desktop/src/renderer/hooks/useAudioRecorder.ts
      provides: "stopRecording() com timeout defensivo de 3s e addEventListener('stop') em vez de onstop"
  key_links:
    - from: usePttHandler.ts handler
      to: useAudioRecorder.stopRecording
      via: "log de [usePttHandler] antes/depois de stopRecording + log de [useAudioRecorder] dentro de stop addEventListener"
      pattern: "console\\.log.*\\[usePttHandler\\].*stopRecording"
    - from: useAudioRecorder.stopRecording
      to: "Promise resolve"
      via: "timeout 3s defensivo OU evento stop normal"
      pattern: "setTimeout.*3000.*resolve"
---

<objective>
Diagnosticar e mitigar o bug onde a 2ª apertada do PTT deixa o orb travado em amarelo (listening). Sem causa raiz confirmada (logs do renderer ausentes), esta passada foca em DIAGNÓSTICO (logging detalhado) + DEFESA (timeout na Promise + addEventListener mais seguro + setState('idle') garantido em todos os caminhos).

Purpose: Após esta passada, o usuário poderá reproduzir o bug com DevTools aberto e ter logs claros indicando EXATAMENTE onde o fluxo trava — entrada do handler, branch escolhido, stop() chamado mas onstop nunca disparou (timeout), blob pequeno, throw silencioso, etc. E mesmo sem identificar a causa, o orb não trava mais em amarelo (timeout defensivo + idle garantido).

Output: Dois arquivos modificados (usePttHandler.ts + useAudioRecorder.ts) com logs e defesas. Commit único em pt-BR. Instrução clara para usuário coletar logs do renderer e reportar de volta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@apps/desktop/src/renderer/hooks/usePttHandler.ts
@apps/desktop/src/renderer/hooks/useAudioRecorder.ts
@apps/desktop/src/renderer/src/voice/sendAudioAndHandle.ts
@apps/desktop/src/renderer/src/voice/voiceInputManager.ts
@apps/desktop/src/renderer/components/Orb/OrbContext.tsx

<bug_context>
**Sintoma:** Após 4 commits seguidos consertando PTT (e72aed1, 3d77a34, 6a8679d, c0939a8):
- 1ª apertada: orb vai para AMARELO (listening) — OK
- 2ª apertada: orb FICA AMARELO eternamente
- Logs do main NÃO mostram `[IPC:chat:send-audio]` após a 2ª apertada
- Sem stack traces visíveis nos logs do main

**Estado atual do código:**
- `usePttHandler.ts`: tem try/catch ao redor de stopRecording + sendAudioAndHandle, mas NÃO loga entrada do handler nem qual branch foi escolhido (if PTT vs else)
- `useAudioRecorder.ts.stopRecording`: usa `mediaRecorder.onstop = ...` (sobrescreve) em vez de addEventListener; SEM timeout defensivo na Promise; cache de stream + timeslice 100ms recém-introduzido (commit c0939a8)

**Hipóteses (em ordem de probabilidade):**
1. `mediaRecorder.onstop` nunca dispara → Promise trava → handler nunca completa → orb fica em listening. Causa possível: incompatibilidade Electron/Chromium entre cached stream + MediaRecorder.stop() recriado ou alguma condição anômala da stream.
2. Algum throw silencioso no caminho que não é logado.
3. handler nunca chega a executar a 2ª vez (off()/on() do useEffect perdeu o evento).

**Decisão:** Não vamos adivinhar a causa raiz. Vamos instrumentar o caminho com logs e adicionar defesas (timeout, addEventListener, setState('idle') garantido) para que:
- (a) na próxima reprodução o usuário veja EXATAMENTE onde trava
- (b) mesmo sem identificar, o orb não fica preso em amarelo (volta a idle via timeout)

**Estados do orb (confirmados em Orb.tsx + OrbContext.tsx):**
- idle = cyan, listening = amarelo, processing = roxo, responding = azul, awaiting-followup = sky
- Logo "amarelo eterno" = `state === 'listening'` que nunca transitou.
</bug_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar logs de diagnóstico em usePttHandler.ts e garantir setState('idle') em todos os caminhos</name>
  <files>apps/desktop/src/renderer/hooks/usePttHandler.ts</files>
  <action>
Modificar `usePttHandler.ts` para instrumentar o fluxo COMPLETO de PTT com logs prefixados `[usePttHandler]`. NÃO mudar a estrutura lógica do handler — apenas adicionar logs e garantir que `setState('idle')` é chamado em TODOS os caminhos de saída do branch de stop.

Logs obrigatórios (na ordem do fluxo):

1. **Entrada do handler** — primeiro statement dentro de `handlePttToggle`:
   ```ts
   const currentSource = voiceInputManager.getCurrentSource();
   console.log('[usePttHandler] ptt:action recebido — currentSource:', currentSource);
   ```

2. **Decisão de branch** — após o `if (...currentSource === 'ptt')`:
   - branch STOP: `console.log('[usePttHandler] branch=STOP — chamando stopRecording()');`
   - branch START: `console.log('[usePttHandler] branch=START — adquirindo mic');`

3. **Branch STOP (dentro do try)**:
   - antes do `await stopRecording()`: já logado em (2)
   - depois do `await stopRecording()`: `console.log('[usePttHandler] stopRecording resolveu — audioBuffer:', audioBuffer ? audioBuffer.length + ' bytes' : 'null');`
   - antes de `setState('idle')` no caso `!audioBuffer`: `console.log('[usePttHandler] audioBuffer null — setState(idle) e retornando');`
   - antes de `await sendAudioAndHandle(...)`: `console.log('[usePttHandler] chamando sendAudioAndHandle —', audioBuffer.length, 'bytes');`
   - depois de `await sendAudioAndHandle(...)` (após o catch, ainda dentro do try): `console.log('[usePttHandler] sendAudioAndHandle completou');`

4. **Catch do branch STOP** — manter o `console.error` existente, mas adicionar `setState('idle')` (já existe) e logar também:
   ```ts
   console.error('[usePttHandler] erro no branch STOP:', error);
   ```

5. **Defesa adicional — finally no branch STOP**:
   Envolver TODO o branch STOP em try/finally (mantendo o try/catch interno). No `finally`, garantir:
   ```ts
   } finally {
     // Defesa: setState('idle') só se ainda estivermos em 'listening'.
     // sendAudioAndHandle já transita pra processing→responding→idle no
     // caminho feliz; este finally é fallback contra bugs/throws perdidos.
     console.log('[usePttHandler] branch STOP finally — garantindo cleanup');
   }
   ```
   NOTA: NÃO chamar `setState('idle')` incondicional dentro desse finally — `sendAudioAndHandle` já tem invariante de idle no `finally` interno dele. Apenas logar para o usuário ver no console que o branch terminou.

6. **Branch START** (else):
   - depois de `voiceInputManager.acquire(...)` retornar grant: `console.log('[usePttHandler] grant adquirido — chamando startRecording()');`
   - depois de `await startRecording()`: `console.log('[usePttHandler] startRecording resolveu — setState(listening)');`
   - no catch do START: já tem `console.error`; manter.

**ESTRUTURA FINAL** do branch STOP (skeleton — preserve indentação real):
```ts
if (voiceInputManager.getCurrentSource() === 'ptt') {
  console.log('[usePttHandler] branch=STOP — chamando stopRecording()');
  voiceInputManager.release('ptt');
  try {
    const audioBuffer = await stopRecording();
    console.log('[usePttHandler] stopRecording resolveu — audioBuffer:',
      audioBuffer ? audioBuffer.length + ' bytes' : 'null');
    if (!audioBuffer) {
      console.log('[usePttHandler] audioBuffer null — setState(idle) e retornando');
      setState('idle');
      return;
    }
    console.log('[usePttHandler] chamando sendAudioAndHandle —', audioBuffer.length, 'bytes');
    await sendAudioAndHandle(audioBuffer, { ... });
    console.log('[usePttHandler] sendAudioAndHandle completou');
  } catch (error) {
    console.error('[usePttHandler] erro no branch STOP:', error);
    setToast({ message: 'Erro inesperado no PTT.', variant: 'error' });
    setState('idle');
  } finally {
    console.log('[usePttHandler] branch STOP finally — fluxo terminou');
  }
}
```

**NÃO MEXER em:**
- Estrutura do useEffect (off/on)
- Lógica de voiceInputManager
- Branch START além de adicionar os logs em (6)
- Dependências do useEffect
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20 || npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- usePttHandler.ts compila sem erros TS
- Branch STOP tem 7+ console.log/console.error com prefixo `[usePttHandler]` cobrindo: entrada, decisão de branch, antes de stop, depois de stop, blob null, antes de sendAudio, depois de sendAudio, finally
- Branch STOP envolvido em try/catch/finally (finally apenas loga; não chama setState)
- Branch START tem 3+ logs cobrindo: decisão, depois de grant, depois de startRecording
- Catch do branch STOP continua chamando setState('idle')
- `git diff apps/desktop/src/renderer/hooks/usePttHandler.ts` mostra apenas adição de logs + try/finally wrapper, nenhuma mudança de lógica
  </done>
</task>

<task type="auto">
  <name>Task 2: Endurecer stopRecording() com addEventListener + timeout 3s defensivo + logs</name>
  <files>apps/desktop/src/renderer/hooks/useAudioRecorder.ts</files>
  <action>
Modificar `useAudioRecorder.ts.stopRecording` para:

**1. Substituir `mediaRecorder.onstop = ...` por `mediaRecorder.addEventListener('stop', ..., { once: true })`**

Motivação: `onstop` propriedade é UMA SLOT — qualquer código que setou `onstop` antes (legado) seria sobrescrito, e setar AGORA pode ser pisoteado por um listener que outra parte da pipeline esteja registrando. `addEventListener` é aditivo + `{ once: true }` garante cleanup automático.

**2. Adicionar timeout defensivo de 3 segundos** — se o evento `stop` não disparar dentro de 3s, resolver a Promise com `null` + warn explícito.

Motivação: hipótese principal do bug é `onstop` nunca disparar. Sem timeout, a Promise trava forever → handler trava → orb fica em listening. Com timeout de 3s, no pior caso o orb volta para idle e o usuário vê warn no console identificando exatamente esse cenário.

**3. Adicionar logs detalhados** — todos prefixados `[useAudioRecorder]`:
- Entrada de stopRecording: `console.log('[useAudioRecorder] stopRecording chamado — recorder state:', mediaRecorder?.state);`
- Antes de `mediaRecorder.stop()`: `console.log('[useAudioRecorder] chamando mediaRecorder.stop() + armando timeout 3s');`
- Dentro do listener de 'stop' (primeira linha): `console.log('[useAudioRecorder] evento stop recebido — chunks:', chunksRef.current.length);`
- Antes do `resolve(bytes)` (sucesso): `console.log('[useAudioRecorder] resolvendo com', bytes.length, 'bytes');`
- Antes do `resolve(null)` por blob pequeno: já tem warn — manter
- Dentro do timeout: `console.warn('[useAudioRecorder] TIMEOUT 3s — onstop não disparou. Forçando resolve(null).');`

**ESTRUTURA FINAL DA PROMISE** (skeleton):
```ts
return new Promise((resolve) => {
  let resolved = false;
  const safeResolve = (value: Uint8Array | null) => {
    if (resolved) return;
    resolved = true;
    resolve(value);
  };

  const onStop = async () => {
    console.log('[useAudioRecorder] evento stop recebido — chunks:', chunksRef.current.length);
    clearTimeout(timeoutId);
    try {
      const webmBlob = new Blob(chunksRef.current, { type: 'audio/webm' });
      console.log('[useAudioRecorder] Recorded WebM blob:', webmBlob.size, 'bytes');

      if (webmBlob.size < 200) {
        console.warn(
          '[useAudioRecorder] WebM muito pequeno (',
          webmBlob.size,
          'bytes) — provavelmente toggle rápido demais. Descartando.',
        );
        setState({ isRecording: false, error: null });
        mediaRecorderRef.current = null;
        chunksRef.current = [];
        safeResolve(null);
        return;
      }

      const arrayBuffer = await webmBlob.arrayBuffer();
      const bytes = new Uint8Array(arrayBuffer);
      console.log('[useAudioRecorder] resolvendo com', bytes.length, 'bytes');

      setState({ isRecording: false, error: null });
      mediaRecorderRef.current = null;
      chunksRef.current = [];
      safeResolve(bytes);
    } catch (err) {
      console.error('[useAudioRecorder] stopRecording onstop error:', err);
      setState({
        isRecording: false,
        error: err instanceof Error
          ? `Failed to process audio: ${err.message}`
          : 'Failed to process audio',
      });
      mediaRecorderRef.current = null;
      chunksRef.current = [];
      safeResolve(null);
    }
  };

  const timeoutId = setTimeout(() => {
    console.warn(
      '[useAudioRecorder] TIMEOUT 3s — evento stop não disparou. Forçando resolve(null).',
      'recorder.state era:', mediaRecorder.state,
    );
    // Limpa listener pendente para evitar resolve duplo (safeResolve já protege)
    mediaRecorder.removeEventListener('stop', onStop);
    setState({ isRecording: false, error: 'stop timeout' });
    mediaRecorderRef.current = null;
    chunksRef.current = [];
    safeResolve(null);
  }, 3000);

  mediaRecorder.addEventListener('stop', onStop, { once: true });
  console.log('[useAudioRecorder] chamando mediaRecorder.stop() + armando timeout 3s');
  mediaRecorder.stop();
  setState({ isRecording: false, error: null });
});
```

**Logar entrada da função** (antes do early-return de inactive):
```ts
const stopRecording = useCallback(async (): Promise<Uint8Array | null> => {
  const mediaRecorder = mediaRecorderRef.current;
  console.log('[useAudioRecorder] stopRecording chamado — recorder state:', mediaRecorder?.state ?? 'null');

  if (!mediaRecorder || mediaRecorder.state === 'inactive') {
    console.warn('[useAudioRecorder] stopRecording called but not recording');
    return null;
  }
  // ... rest
```

**NÃO MEXER em:**
- startRecording (logs lá já existem)
- Cache de stream (streamRef)
- Cleanup do useEffect de unmount
- isStreamUsable
- Threshold de 200 bytes (já é defesa em profundidade)
  </action>
  <verify>
    <automated>cd /root/jarvis/apps/desktop && npx tsc --noEmit -p tsconfig.web.json 2>&1 | head -20 || npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- useAudioRecorder.ts compila sem erros TS
- `mediaRecorder.onstop = ...` substituído por `mediaRecorder.addEventListener('stop', onStop, { once: true })`
- Helper `safeResolve` previne resolve duplo entre timeout e evento normal
- `setTimeout(..., 3000)` armado antes de `mediaRecorder.stop()` e cancelado dentro de `onStop` via `clearTimeout`
- Timeout handler emite `console.warn` com prefixo `[useAudioRecorder] TIMEOUT 3s`, remove listener, faz cleanup de refs e resolve(null)
- 5+ logs novos cobrindo: entrada, chamada de stop, evento recebido, resolve com bytes, timeout
- `git diff apps/desktop/src/renderer/hooks/useAudioRecorder.ts` mostra apenas mudanças no escopo de stopRecording
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reproduzir bug com DevTools aberto e coletar logs do renderer</name>
  <what-built>
Logs detalhados em todo o fluxo PTT (start + stop) e timeout defensivo de 3s na Promise de stopRecording. O bug pode estar mitigado pelos próprios fixes (addEventListener + timeout) OU os logs vão revelar exatamente onde o fluxo trava.
  </what-built>
  <how-to-verify>
**Passo 1 — Build & rodar app:**
```bash
cd /root/jarvis/apps/desktop
pnpm dev
```

**Passo 2 — Abrir DevTools do renderer no Electron:**
- Com a janela do orb visível, pressione `Ctrl+Shift+I` (Linux/Windows) ou `Cmd+Opt+I` (macOS).
- Se não abrir: clique com botão direito no orb → "Inspect Element" (se habilitado), OU rode com `pnpm dev --inspect-renderer`.
- Vá para a aba **Console**.
- Limpe o console (botão 🚫 ou Ctrl+L).

**Passo 3 — Reproduzir o bug:**
1. Aperte o atalho global do PTT (configurado pelo usuário).
2. Fale alguma coisa por 1-2 segundos.
3. Aperte o atalho do PTT de novo (esta é a 2ª apertada, onde o bug acontece).
4. Espere até 5 segundos observando o orb.

**Passo 4 — Reportar:**
Copie e cole TODO o output do console do renderer (de `[usePttHandler] ptt:action recebido` para baixo) e descreva o comportamento visual do orb:
- Voltou pra idle (cyan)? → bug mitigado pelos fixes; logs ainda revelam se foi pelo caminho normal ou pelo timeout 3s
- Travou em listening (amarelo)? → bug NÃO mitigado; logs vão mostrar exatamente onde parou
- Foi pra processing (roxo) e voltou pra idle? → bug mitigado completamente

**Sinais a observar nos logs:**
- `[useAudioRecorder] TIMEOUT 3s` → confirma hipótese 1 (onstop nunca dispara). Próxima passada vai investigar por que MediaRecorder.stop() não emite stop event no Electron com cached stream.
- `[usePttHandler] erro no branch STOP` → throw que estava silencioso; mensagem de erro vai mostrar a causa.
- Sem nenhum log após `[usePttHandler] ptt:action recebido` na 2ª apertada → handler está sendo desmontado entre apertadas; problema é no useEffect off()/on().
- `[usePttHandler] sendAudioAndHandle completou` mas orb fica amarelo → setState não está propagando (problema em outro lugar).
  </how-to-verify>
  <resume-signal>Cole os logs do console do renderer + descreva o que o orb fez. Digite "approved" se quiser commitar mesmo se ainda houver problemas (logs/timeout já são valor entregue).</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compila em apps/desktop sem novos erros
- Logs detalhados aparecem no console do renderer durante operação normal
- Timeout de 3s comprovadamente dispara se o evento stop não disparar (verificável nos logs)
- Orb não fica preso em amarelo mesmo se onstop falhar (timeout força resolve(null) → handler chama setState('idle'))
</verification>

<success_criteria>
- Usuário consegue ver no console do renderer cada passo do fluxo PTT
- 2ª apertada do PTT NÃO deixa orb travado em amarelo eterno (timeout defensivo garante volta a idle em ≤3s no pior caso)
- Os logs coletados pelo usuário identificam univocamente em qual hipótese o bug se encaixa (timeout, throw silencioso, handler desmontado, ou caso normal)
- Commit em pt-BR seguindo Conventional Commits + emoji 🐛
- SEM linhas "Generated with Claude Code" / "Co-Authored-By: Claude"
</success_criteria>

<output>
After completion, create `.planning/quick/260427-s2t-fix-ptt-2a-apertada-nao-dispara-processa/260427-s2t-SUMMARY.md` documentando:
- Mudanças aplicadas (logs adicionados, addEventListener, timeout 3s)
- Hipóteses confirmadas/descartadas pelos logs coletados na Task 3
- Próxima ação recomendada (causa raiz identificada? Outro fix necessário?)

Commit message sugerido (pt-BR, NÃO incluir co-author):
```
🐛 fix(ptt): instrumentar fluxo de stop e adicionar timeout defensivo de 3s

- Adiciona logs detalhados em usePttHandler e useAudioRecorder cobrindo
  entrada do handler, branch escolhido, antes/depois de stop(), tamanho
  do blob, evento stop recebido, antes/depois de sendAudio
- Substitui mediaRecorder.onstop por addEventListener('stop', ..., { once: true })
  para evitar sobrescrita por listeners externos
- Adiciona timeout de 3s na Promise de stopRecording — se onstop não
  disparar, resolve com null + warn explícito (evita orb travado em amarelo)
- Envolve branch STOP do handler em try/catch/finally com log de fluxo
  terminado, mantendo setState('idle') no catch existente

Diagnóstico para o bug "2ª apertada do PTT deixa orb amarelo eterno".
Próximo passo: usuário roda app, abre DevTools (Ctrl+Shift+I) e reporta
logs do console do renderer após reproduzir o bug.
```
</output>
