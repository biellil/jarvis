---
type: quick
task_id: 260514-u41
autonomous: true
files_modified:
  - apps/desktop/src/main/index.ts
---

# Quick Task: Add DevTools Opener for UI Debugging

<objective>
Add `mainWindow.webContents.openDevTools()` call temporarily in `apps/desktop/src/main/index.ts` immediately after window creation to debug UI rendering issues.

Purpose: Enable immediate DevTools access to investigate UI rendering bug
Output: DevTools automatically open on window creation for debugging
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-quick.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/desktop/src/main/index.ts

## Current Issue
User needs to debug a UI rendering bug but cannot open DevTools easily. Adding automatic DevTools opener will streamline debugging workflow.

## Location
In `createWindow()` function, add the call after `mainWindow.loadURL()` or `mainWindow.loadFile()` completes, but before the `ready-to-show` event.
</context>

<tasks>

<task type="auto">
  <name>Add temporary DevTools opener</name>
  <files>apps/desktop/src/main/index.ts</files>
  <action>
In the `createWindow()` function, immediately after the if/else block that calls either `mainWindow.loadURL()` (dev) or `mainWindow.loadFile()` (production), add:

```typescript
// TEMPORARY: Auto-open DevTools for UI debugging
// TODO: Remove this line after debugging is complete
mainWindow.webContents.openDevTools();
```

Insert this after line 127 (after the if/else block for loading the renderer) and before the 'ready-to-show' event handler (line 130).

This is a TEMPORARY debugging aid - the comment clearly marks it for removal.
  </action>
  <verify>
File modified with openDevTools() call added with clear TEMPORARY comment
  </verify>
  <done>
DevTools opener added to createWindow() function with removal marker comment
  </done>
</task>

</tasks>

<verification>
- [ ] openDevTools() call added after window load
- [ ] Clear TEMPORARY comment added
- [ ] TODO comment indicates removal after debugging
- [ ] Code compiles without errors
</verification>

<success_criteria>
DevTools automatically open when JARVIS window is created, enabling immediate debugging access for UI rendering investigation.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-u41-adicionar-mainwindow-webcontents-opendev/260514-u41-SUMMARY.md`
</output>
