---
phase: 260427-u2k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gateway/package.json
  - apps/gateway/src/lib/logger.ts
  - apps/gateway/src/middleware/requestLog.ts
  - apps/gateway/src/middleware/errorHandler.ts
  - apps/gateway/src/middleware/validate.ts
  - apps/gateway/src/lib/proxy.ts
  - apps/gateway/src/routes/chat.ts
  - apps/gateway/src/routes/tool-calls.ts
  - apps/gateway/src/app.ts
  - apps/gateway/src/index.ts
  - apps/gateway/test/logger.test.ts
autonomous: true
requirements:
  - QUICK-260427-u2k

must_haves:
  truths:
    - "Em dev, ao iniciar o gateway o boot log sai colorido/legível (pino-pretty)"
    - "Em produção (NODE_ENV=production), todos os logs saem como JSON estruturado em uma linha"
    - "Cada request HTTP gera UMA linha de log no res.on('finish') contendo método, URL, status, duração em ms e reqId"
    - "Requests para /api/health NÃO geram log (skip por path)"
    - "Toda chamada de proxy para o backend-ts gera log com URL alvo, método, status, duração; falha de rede vira level=error"
    - "Erros tratados pelo errorHandler geram log level=error com status, code, message e reqId correlacionado; stack só sai em logs quando NODE_ENV !== 'production'"
    - "Validation fail gera log level=warn com a lista de paths que falharam, correlacionado pelo reqId"
    - "req.id está disponível em qualquer middleware/handler downstream para reuso"
    - "LOG_LEVEL é configurável via env (default: info)"
    - "Os testes existentes (chat, health, error, stream, tool-calls, chat-stream-auth) continuam passando"
  artifacts:
    - path: "apps/gateway/src/lib/logger.ts"
      provides: "Singleton pino logger + child(reqId) helper + tipos"
      exports: ["logger", "createRequestLogger"]
    - path: "apps/gateway/src/middleware/requestLog.ts"
      provides: "Middleware que injeta req.id e loga no finish, com skip para /api/health"
      exports: ["requestLog"]
    - path: "apps/gateway/test/logger.test.ts"
      provides: "Cobertura de request log + error handler logging"
      contains: "describe"
    - path: "apps/gateway/package.json"
      provides: "Dependências pino + pino-pretty em dependencies"
      contains: "pino"
  key_links:
    - from: "apps/gateway/src/app.ts"
      to: "apps/gateway/src/middleware/requestLog.ts"
      via: "app.use(requestLog) ANTES das rotas"
      pattern: "app\\.use\\(requestLog\\)"
    - from: "apps/gateway/src/middleware/errorHandler.ts"
      to: "apps/gateway/src/lib/logger.ts"
      via: "logger.error com reqId"
      pattern: "logger\\.(error|child)"
    - from: "apps/gateway/src/lib/proxy.ts"
      to: "apps/gateway/src/lib/logger.ts"
      via: "wrapper logProxyCall ou helper instrumentado"
      pattern: "logger\\.(info|error)"
    - from: "apps/gateway/src/routes/chat.ts"
      to: "apps/gateway/src/lib/proxy.ts"
      via: "uso do helper instrumentado em vez de undici.fetch direto"
      pattern: "proxyFetch|loggedFetch"
---

<objective>
Adicionar logs estruturados ao API gateway usando `pino` (JSON em prod) + `pino-pretty` (colorido em dev). Entregar visão completa: boot, request log, proxy log, erros e validation warnings — tudo correlacionado por `reqId` único por request.

Purpose: hoje o gateway só tem `console.log` no boot. Sem observabilidade fica impossível debugar por que uma chamada falhou em produção (ou em dev quando o backend-ts está com problema). Logs estruturados resolvem isso sem custo de infra.

Output: gateway logando 1 linha por request + 1 linha por proxy + erros formatados, com `req.id` propagado entre middlewares.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@apps/gateway/src/app.ts
@apps/gateway/src/index.ts
@apps/gateway/src/config.ts
@apps/gateway/src/middleware/errorHandler.ts
@apps/gateway/src/middleware/validate.ts
@apps/gateway/src/lib/proxy.ts
@apps/gateway/src/routes/chat.ts
@apps/gateway/src/routes/health.ts
@apps/gateway/src/routes/tool-calls.ts
@apps/gateway/package.json
@apps/gateway/test/health.test.ts
@apps/gateway/test/error.test.ts
@apps/gateway/test/chat.test.ts

<interfaces>
<!-- Contratos relevantes já no codebase. NÃO precisa explorar. -->

Stack: Express 5 + undici 8 + zod 4, ESM (`"type": "module"`), TypeScript strict, vitest 4 com `globals: true`.

config.ts:
```typescript
export const config = {
  backendTsUrl: process.env.BACKEND_TS_URL ?? "http://localhost:8001",
  gatewayPort: parseInt(process.env.GATEWAY_PORT ?? "3000", 10),
  apiKey: process.env.JARVIS_API_KEY as string | undefined,
} as const;
```

errorHandler atual (NÃO loga, só responde — IMPORTANTE: o teste GW-04 garante que o stack NUNCA aparece no body da response; manter):
```typescript
export const errorHandler: ErrorRequestHandler = (err, _req, res, _next) => {
  const status = err.status ?? err.statusCode ?? 500;
  const code = err.code ?? "INTERNAL_ERROR";
  const message = err.message ?? "An unexpected error occurred";
  res.status(status).json({ error: true, code, message });
};
```

validate atual:
```typescript
export function validate<T>(schema: z.ZodType<T>) {
  return (req, _res, next) => {
    const result = schema.safeParse(req.body);
    if (!result.success) {
      const err = new Error("Validation failed") as any;
      err.code = "VALIDATION_ERROR";
      err.details = result.error.issues; // <- Array<ZodIssue> com .path
      err.status = 400;
      return next(err);
    }
    req.body = result.data;
    next();
  };
}
```

proxy.ts atual: só exporta `SSE_HEADERS`. Importado por `routes/chat.ts`. Pode receber novos exports sem quebrar.

createApp atual:
```typescript
export function createApp() {
  const app = express();
  app.use(express.json());
  app.use("/api", chatRouter);
  app.use("/api", healthRouter);
  app.use("/api", toolCallsRouter);
  app.use(errorHandler);
  return app;
}
```

Padrão de teste existente (vitest com mock de undici):
```typescript
vi.mock("undici", () => ({ fetch: vi.fn() }));
import { fetch } from "undici";
import { createApp } from "../src/app.js";
const mockFetch = vi.mocked(fetch);
```

tsconfig.json: `strict: true`, `rootDir: src`, exclude `test`. Path imports usam `.js` (ESM module specifier).
vitest.config.ts: `globals: true, root: "."`.
</interfaces>

<gotchas>
1. **`/api/health` skip:** o router está montado em `/api`, então o middleware vê `req.url === "/api/health"`. Skipar por essa string exata.
2. **pino-pretty em testes:** rodar `pino-pretty` como transport spawn worker thread, polui stdout do vitest e pode flakar em CI. Configurar transport apenas se `NODE_ENV === 'development'` (NÃO em `test` nem em `production`). Em test/prod usar JSON puro.
3. **Stack em response:** o teste `GW-04` (`error.test.ts`) tem `expect(res.body).not.toHaveProperty("stack")`. NÃO adicionar stack ao body da response — apenas ao log.
4. **Tipagem de `req.id`:** declarar augmentation em `express-serve-static-core` no próprio `requestLog.ts` (ou arquivo `.d.ts` em `src/types/`) para evitar `(req as any).id` espalhado.
5. **Boot log:** trocar `console.log` em `index.ts` por `logger.info`.
6. **Streaming (`/api/chat/stream`):** quando `res.flushHeaders()` é chamado e bytes começam a fluir, `res.on('finish')` ainda dispara no `res.end()` — request log funciona normalmente. Apenas garantir que duração é medida do início até finish.
7. **Skip silencioso:** o middleware deve `next()` IMEDIATAMENTE para `/api/health` antes de qualquer overhead (sem gerar reqId nem registrar listener).
8. **pino-pretty em dependencies (não devDependencies):** usuário decidiu por simplicidade. Mesmo sem ser usado em prod runtime, fica em `dependencies` (planner respeitando decisão LOCKED).
9. **req.id já existe?:** Express 5 não adiciona `req.id` por padrão. A augmentation precisa ser global e tipada como `string`.
10. **mock de logger nos testes:** usar `vi.mock("../src/lib/logger.js", ...)` retornando objeto com spies em `info/warn/error/child` — o `child` deve retornar o próprio mock para encadear.
</gotchas>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logger singleton + dependências + tipagem req.id</name>
  <files>
    apps/gateway/package.json,
    apps/gateway/src/lib/logger.ts
  </files>
  <action>
    1. Editar `apps/gateway/package.json`: adicionar em `dependencies` (ordem alfabética):
       - `"pino": "^9.5.0"`
       - `"pino-pretty": "^11.3.0"`
       Rodar `cd apps/gateway && npm install` (ou `pnpm install` se houver `pnpm-lock.yaml` — verificar antes; se houver `package-lock.json`, usar `npm install`).

    2. Criar `apps/gateway/src/lib/logger.ts` com:
       - Import `pino, { type Logger }` de `"pino"`.
       - Ler `level = process.env.LOG_LEVEL ?? "info"`.
       - Ler `nodeEnv = process.env.NODE_ENV ?? "development"`.
       - Configurar transport: SOMENTE quando `nodeEnv === "development"`, usar
         ```ts
         transport: {
           target: "pino-pretty",
           options: { colorize: true, translateTime: "SYS:HH:MM:ss.l", ignore: "pid,hostname" }
         }
         ```
         Em qualquer outro env (`test`, `production`, etc.) NÃO passar transport — pino emite JSON puro.
       - Exportar `export const logger: Logger = pino({ level, ...(transport ? { transport } : {}), base: { app: "gateway" } });`
       - Exportar helper `export function createRequestLogger(reqId: string): Logger { return logger.child({ reqId }); }`.

    3. Criar (ou estender) augmentation de tipos para `req.id`. Opção mais simples: criar `apps/gateway/src/types/express.d.ts` contendo:
       ```ts
       import "express-serve-static-core";
       declare module "express-serve-static-core" {
         interface Request {
           id: string;
           log: import("pino").Logger;
         }
       }
       export {};
       ```
       Garantir que `tsconfig.json` inclua `src/**/*` (já inclui — não mexer).

    4. NÃO modificar `index.ts` ou `app.ts` ainda — Task 2 fará isso.
  </action>
  <verify>
    <automated>cd apps/gateway && npm install --silent && npx tsc --noEmit && node -e "import('./src/lib/logger.ts').catch(()=>process.exit(0))"</automated>
    Adicionalmente: `cat apps/gateway/package.json | grep -E '"pino"|"pino-pretty"'` deve retornar 2 linhas.
    `cd apps/gateway && npx tsc --noEmit` deve passar sem erros.
  </verify>
  <done>
    - `pino` e `pino-pretty` em `dependencies` do `apps/gateway/package.json` (não devDependencies).
    - `apps/gateway/src/lib/logger.ts` exporta `logger` e `createRequestLogger`.
    - `apps/gateway/src/types/express.d.ts` augmenta `Request` com `id: string` e `log: pino.Logger`.
    - `npx tsc --noEmit` passa.
    - Suite atual (`npm test` na pasta `apps/gateway`) continua verde (esta task é aditiva, não muda comportamento).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Request log middleware + integração no app.ts/index.ts + skip /api/health</name>
  <files>
    apps/gateway/src/middleware/requestLog.ts,
    apps/gateway/src/app.ts,
    apps/gateway/src/index.ts,
    apps/gateway/test/logger.test.ts
  </files>
  <behavior>
    - Test 1 (request log fires on finish): GET para uma rota válida (ex.: `/api/health` mockada de outra forma OU criar rota de teste) loga 1 linha com `level=info`, contendo `method`, `url`, `status`, `durationMs`, `reqId`.
      - Para validar pino sem mock pesado, mockar `apps/gateway/src/lib/logger.js` com `vi.mock` retornando `{ logger: { info: vi.fn(), warn: vi.fn(), error: vi.fn(), child: vi.fn(()=> ({ info, warn, error, child: vi.fn() })) } , createRequestLogger: vi.fn(()=>({...})) }`. Asserir que `info` foi chamado com objeto contendo `status` e `durationMs >= 0`.
    - Test 2 (skip /api/health): GET `/api/health` NÃO chama `logger.info` para request log. Pode logar de outras fontes (mas o spy de request log fica zerado).
    - Test 3 (req.id propagado): asserir que `createRequestLogger` foi chamado com uma string UUID-like (regex `/^[0-9a-f-]{36}$/i`).
  </behavior>
  <action>
    1. Criar `apps/gateway/src/middleware/requestLog.ts`:
       ```ts
       import type { Request, Response, NextFunction } from "express";
       import { randomUUID } from "node:crypto";
       import { logger, createRequestLogger } from "../lib/logger.js";

       const SKIP_PATHS = new Set<string>(["/api/health"]);

       export function requestLog(req: Request, res: Response, next: NextFunction): void {
         if (SKIP_PATHS.has(req.url)) return next();

         const reqId = randomUUID();
         const start = process.hrtime.bigint();
         req.id = reqId;
         req.log = createRequestLogger(reqId);

         res.on("finish", () => {
           const durationMs = Number(process.hrtime.bigint() - start) / 1_000_000;
           logger.info(
             {
               reqId,
               method: req.method,
               url: req.originalUrl ?? req.url,
               status: res.statusCode,
               durationMs: Math.round(durationMs * 100) / 100,
             },
             "request",
           );
         });

         next();
       }
       ```
       Importante: `req.url` (não `req.path`) para casar exatamente com `/api/health` antes de qualquer roteamento. Se preferir, usar `req.originalUrl`.

    2. Editar `apps/gateway/src/app.ts`: registrar `requestLog` ANTES das rotas:
       ```ts
       import { requestLog } from "./middleware/requestLog.js";
       // ...
       export function createApp() {
         const app = express();
         app.use(requestLog);          // <-- ANTES de express.json e rotas
         app.use(express.json());
         app.use("/api", chatRouter);
         app.use("/api", healthRouter);
         app.use("/api", toolCallsRouter);
         app.use(errorHandler);
         return app;
       }
       ```

    3. Editar `apps/gateway/src/index.ts`: trocar `console.log` por `logger.info`:
       ```ts
       import { createApp } from "./app.js";
       import { config } from "./config.js";
       import { logger } from "./lib/logger.js";

       const app = createApp();
       app.listen(config.gatewayPort, () => {
         logger.info({ port: config.gatewayPort }, "JARVIS Gateway listening");
       });
       ```

    4. Criar `apps/gateway/test/logger.test.ts` com 3 testes (request log dispara, skip /api/health, reqId propagado). Usar o pattern de mock já estabelecido:
       ```ts
       import { vi, describe, it, expect, beforeEach } from "vitest";
       import request from "supertest";

       const childMock = { info: vi.fn(), warn: vi.fn(), error: vi.fn(), child: vi.fn() };
       const loggerMock = {
         info: vi.fn(),
         warn: vi.fn(),
         error: vi.fn(),
         child: vi.fn(() => childMock),
       };
       vi.mock("../src/lib/logger.js", () => ({
         logger: loggerMock,
         createRequestLogger: vi.fn(() => childMock),
       }));
       vi.mock("undici", () => ({ fetch: vi.fn(async () => ({ ok: true, status: 200, json: async () => ({}) } as any)) }));

       import { createApp } from "../src/app.js";
       import { createRequestLogger } from "../src/lib/logger.js";

       const mockedCreateRequestLogger = vi.mocked(createRequestLogger);

       describe("GW-LOG: request logging", () => {
         beforeEach(() => {
           loggerMock.info.mockReset();
           loggerMock.warn.mockReset();
           loggerMock.error.mockReset();
           mockedCreateRequestLogger.mockClear();
         });

         it("logs one line on finish with status and durationMs", async () => {
           const app = createApp();
           await request(app).post("/api/chat").send({ message: "oi" });
           const calls = loggerMock.info.mock.calls.filter(
             (c) => typeof c[1] === "string" && c[1] === "request",
           );
           expect(calls.length).toBe(1);
           const payload = calls[0][0];
           expect(payload).toMatchObject({
             method: "POST",
             url: "/api/chat",
             status: expect.any(Number),
           });
           expect(payload.durationMs).toBeGreaterThanOrEqual(0);
           expect(payload.reqId).toMatch(/^[0-9a-f-]{36}$/i);
         });

         it("skips logging for /api/health", async () => {
           const app = createApp();
           await request(app).get("/api/health");
           const requestCalls = loggerMock.info.mock.calls.filter(
             (c) => typeof c[1] === "string" && c[1] === "request",
           );
           expect(requestCalls.length).toBe(0);
           expect(mockedCreateRequestLogger).not.toHaveBeenCalled();
         });

         it("creates a child logger with reqId UUID per request", async () => {
           const app = createApp();
           await request(app).post("/api/chat").send({ message: "oi" });
           expect(mockedCreateRequestLogger).toHaveBeenCalledTimes(1);
           const reqId = mockedCreateRequestLogger.mock.calls[0][0];
           expect(reqId).toMatch(/^[0-9a-f-]{36}$/i);
         });
       });
       ```

    5. Rodar a suite inteira para garantir que nenhum teste pré-existente quebrou. O mock de undici no logger.test.ts usa um valor padrão genérico — se algum teste de outra suíte falhar, NÃO mexer nessas suítes (cada arquivo de teste tem mock isolado).
  </action>
  <verify>
    <automated>cd apps/gateway && npx tsc --noEmit && npm test -- --run</automated>
    Esperado: todos os testes passam (incluindo os 3 novos em `test/logger.test.ts` e os existentes em `chat.test.ts`, `health.test.ts`, `error.test.ts`, `stream.test.ts`, `tool-calls.test.ts`, `chat-stream-auth.test.ts`).
  </verify>
  <done>
    - `requestLog` registrado em `app.ts` ANTES de `express.json()` e das rotas.
    - `/api/health` não dispara request log.
    - Cada request fora de `/api/health` ganha `req.id` (UUID v4) e `req.log` (child logger).
    - `index.ts` usa `logger.info` no boot em vez de `console.log`.
    - 3 testes novos passam; suite completa verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Proxy log + errorHandler logging + validate warn + helper loggedFetch</name>
  <files>
    apps/gateway/src/lib/proxy.ts,
    apps/gateway/src/middleware/errorHandler.ts,
    apps/gateway/src/middleware/validate.ts,
    apps/gateway/src/routes/chat.ts,
    apps/gateway/src/routes/tool-calls.ts,
    apps/gateway/test/logger.test.ts
  </files>
  <behavior>
    - Test 4 (proxy success log): chamar `POST /api/chat` com upstream OK loga `level=info` com `target` URL absoluta, `method=POST`, `status=200`, `durationMs >= 0`.
    - Test 5 (proxy error log): upstream throw (rede) loga `level=error` com mesma forma + `error.message`.
    - Test 6 (errorHandler logs com reqId): rota que joga erro 500 chama `logger.error` (ou `req.log.error`) com `status`, `code`, `message`. Em `NODE_ENV !== 'production'`, payload do log inclui `stack`. Body da response continua SEM stack (regressão GW-04).
    - Test 7 (validate fail loga warn): POST `/api/chat` sem body chama `logger.warn` (ou `req.log.warn`) com `code: "VALIDATION_ERROR"` e lista de paths que falharam. Body continua respondendo `{ error: true, code: "VALIDATION_ERROR" }`.
  </behavior>
  <action>
    1. Em `apps/gateway/src/lib/proxy.ts`, adicionar (mantendo `SSE_HEADERS`):
       ```ts
       import { fetch as undiciFetch, type RequestInit, type Response as UndiciResponse } from "undici";
       import { logger } from "./logger.js";
       import type { Logger } from "pino";

       export interface LoggedFetchOptions extends RequestInit {
         /** Logger filho (com reqId). Default: logger raiz. */
         log?: Logger;
       }

       export async function loggedFetch(
         url: string,
         options: LoggedFetchOptions = {},
       ): Promise<UndiciResponse> {
         const { log = logger, ...init } = options;
         const method = init.method ?? "GET";
         const start = process.hrtime.bigint();
         try {
           const res = await undiciFetch(url, init);
           const durationMs = Number(process.hrtime.bigint() - start) / 1_000_000;
           log.info(
             {
               target: url,
               method,
               status: res.status,
               durationMs: Math.round(durationMs * 100) / 100,
             },
             "proxy",
           );
           return res;
         } catch (err) {
           const durationMs = Number(process.hrtime.bigint() - start) / 1_000_000;
           log.error(
             {
               target: url,
               method,
               durationMs: Math.round(durationMs * 100) / 100,
               err: { message: (err as Error).message, name: (err as Error).name },
             },
             "proxy_error",
           );
           throw err;
         }
       }
       ```
       Manter `SSE_HEADERS` exportado intacto.

    2. Em `apps/gateway/src/routes/chat.ts`:
       - Trocar `import { fetch } from "undici";` + uso direto por `import { loggedFetch, SSE_HEADERS } from "../lib/proxy.js";`.
       - Em CADA chamada `fetch(...)` substituir por `loggedFetch(..., { ..., log: req.log })`.
       - Onde a chamada não tem `req` no escopo (não é o caso aqui — sempre tem), passar `logger` raiz.
       - NÃO mudar a semântica de tratamento de erro (continuar wrappando em `next(err)`).

    3. Em `apps/gateway/src/routes/tool-calls.ts`: mesma troca para `loggedFetch` passando `req.log`.

    4. Em `apps/gateway/src/routes/health.ts`: NÃO mudar. Health usa `fetch` direto, mas o middleware skipa o request log, e queremos manter health silencioso (zero ruído de healthcheck). Comentário no código: `// healthcheck: silencioso de propósito`.

    5. Reescrever `apps/gateway/src/middleware/errorHandler.ts`:
       ```ts
       import type { ErrorRequestHandler } from "express";
       import { logger } from "../lib/logger.js";

       const isProd = process.env.NODE_ENV === "production";

       export const errorHandler: ErrorRequestHandler = (err, req, res, _next) => {
         const status = err.status ?? err.statusCode ?? 500;
         const code = err.code ?? "INTERNAL_ERROR";
         const message = err.message ?? "An unexpected error occurred";

         const log = req.log ?? logger;
         log.error(
           {
             reqId: req.id,
             status,
             code,
             message,
             ...(isProd ? {} : { stack: err.stack }),
             ...(err.details ? { details: err.details } : {}),
           },
           "request_error",
         );

         // Body NUNCA inclui stack — preserva contrato GW-04.
         res.status(status).json({ error: true, code, message });
       };
       ```

    6. Editar `apps/gateway/src/middleware/validate.ts` para logar antes de chamar `next(err)`:
       ```ts
       import { z } from "zod";
       import type { Request, Response, NextFunction } from "express";
       import { logger } from "../lib/logger.js";

       export function validate<T>(schema: z.ZodType<T>) {
         return (req: Request, _res: Response, next: NextFunction) => {
           const result = schema.safeParse(req.body);
           if (!result.success) {
             const failedPaths = result.error.issues.map((i) => i.path.join("."));
             const log = req.log ?? logger;
             log.warn(
               {
                 reqId: req.id,
                 code: "VALIDATION_ERROR",
                 failedPaths,
                 url: req.originalUrl ?? req.url,
               },
               "validation_failed",
             );
             const err = new Error("Validation failed") as any;
             err.code = "VALIDATION_ERROR";
             err.details = result.error.issues;
             err.status = 400;
             return next(err);
           }
           req.body = result.data;
           next();
         };
       }

       export const ChatRequestSchema = z.object({
         message: z.string().min(1, "message is required"),
       });
       ```

    7. Adicionar em `apps/gateway/test/logger.test.ts` (mesmo arquivo do Task 2) os 4 testes de comportamento (4–7). Eles compartilham o mesmo `vi.mock` de logger e undici. Para o teste de proxy success, configurar o mock de undici dentro do `it`:
       ```ts
       describe("GW-LOG: proxy + error + validate logging", () => {
         beforeEach(() => {
           loggerMock.info.mockReset();
           loggerMock.warn.mockReset();
           loggerMock.error.mockReset();
         });

         it("logs proxy info on successful upstream call", async () => {
           const { fetch: mockedFetch } = await import("undici");
           vi.mocked(mockedFetch).mockResolvedValueOnce({
             ok: true, status: 200, json: async () => ({ message: "ok" }),
           } as any);
           await request(createApp()).post("/api/chat").send({ message: "oi" });
           const proxyCalls = loggerMock.info.mock.calls.filter(
             (c) => c[1] === "proxy",
           );
           expect(proxyCalls.length).toBe(1);
           expect(proxyCalls[0][0]).toMatchObject({
             method: "POST",
             status: 200,
             target: expect.stringContaining("/chat"),
           });
         });

         it("logs proxy_error when upstream throws", async () => {
           const { fetch: mockedFetch } = await import("undici");
           vi.mocked(mockedFetch).mockRejectedValueOnce(new Error("ECONNREFUSED"));
           await request(createApp()).post("/api/chat").send({ message: "oi" });
           const errorCalls = loggerMock.error.mock.calls.filter(
             (c) => c[1] === "proxy_error",
           );
           expect(errorCalls.length).toBe(1);
           expect(errorCalls[0][0].err.message).toBe("ECONNREFUSED");
         });

         it("logs request_error with status/code/message and never puts stack in response body", async () => {
           const { fetch: mockedFetch } = await import("undici");
           vi.mocked(mockedFetch).mockRejectedValueOnce(new Error("boom"));
           const res = await request(createApp()).post("/api/chat").send({ message: "oi" });
           const errCalls = loggerMock.error.mock.calls.filter(
             (c) => c[1] === "request_error",
           );
           expect(errCalls.length).toBe(1);
           expect(errCalls[0][0]).toMatchObject({ status: 500, code: expect.any(String) });
           expect(res.body).not.toHaveProperty("stack");
         });

         it("logs validation_failed with failedPaths when body is invalid", async () => {
           const res = await request(createApp()).post("/api/chat").send({});
           const warnCalls = loggerMock.warn.mock.calls.filter(
             (c) => c[1] === "validation_failed",
           );
           expect(warnCalls.length).toBe(1);
           expect(warnCalls[0][0]).toMatchObject({
             code: "VALIDATION_ERROR",
             failedPaths: expect.arrayContaining(["message"]),
           });
           expect(res.status).toBe(400);
         });
       });
       ```

    8. Rodar `cd apps/gateway && npx tsc --noEmit && npm test -- --run` e garantir que TODA a suíte passa, incluindo:
       - `chat.test.ts` (5 testes)
       - `health.test.ts` (3 testes)
       - `error.test.ts` (6 testes — atenção ao `expect(res.body).not.toHaveProperty("stack")`)
       - `stream.test.ts`, `tool-calls.test.ts`, `chat-stream-auth.test.ts`
       - `logger.test.ts` (7 testes — 3 do Task 2 + 4 deste task)
  </action>
  <verify>
    <automated>cd apps/gateway && npx tsc --noEmit && npm test -- --run</automated>
    Esperado: 0 falhas. `logger.test.ts` com 7 testes verdes. Nenhum teste pré-existente regrediu.
    Smoke manual opcional: `cd apps/gateway && NODE_ENV=development LOG_LEVEL=debug npm run dev` e fazer `curl -s -X POST http://localhost:3000/api/chat -H 'Content-Type: application/json' -d '{"message":"oi"}'` — observar 1 linha de boot, 1 linha de proxy e 1 linha de request no terminal (formato pino-pretty colorido). `curl http://localhost:3000/api/health` NÃO deve gerar linha de request log.
  </verify>
  <done>
    - `loggedFetch` exportado de `lib/proxy.ts` e usado em `routes/chat.ts` e `routes/tool-calls.ts`.
    - `errorHandler` loga `level=error` com `reqId`, `status`, `code`, `message` e `stack` (apenas em não-prod). Body da response continua sem `stack`.
    - `validate` loga `level=warn` com `failedPaths` e `reqId` quando rejeita.
    - Health route continua silenciosa (skip via middleware + uso direto de `fetch` mantido).
    - `cd apps/gateway && npm test -- --run` passa 100%.
    - Smoke manual mostra logs estruturados pretty em dev e JSON puros se rodado com `NODE_ENV=production`.
  </done>
</task>

</tasks>

<verification>
Verificação consolidada do plano:

1. **Build & types:**
   ```bash
   cd apps/gateway && npx tsc --noEmit
   ```
   Deve passar sem erros.

2. **Testes automatizados:**
   ```bash
   cd apps/gateway && npm test -- --run
   ```
   Esperado: TODOS os testes verdes — incluindo regressão de `error.test.ts` (`stack` NÃO no body) e os 7 novos em `logger.test.ts`.

3. **Smoke dev (manual, opcional):**
   ```bash
   cd apps/gateway && NODE_ENV=development LOG_LEVEL=info npm run dev
   ```
   Em outro terminal:
   ```bash
   curl -X POST http://localhost:3000/api/chat -H 'Content-Type: application/json' -d '{"message":"oi"}'
   curl http://localhost:3000/api/health
   curl -X POST http://localhost:3000/api/chat -H 'Content-Type: application/json' -d '{}'
   ```
   Esperado:
   - Boot log colorido com `port: 3000`.
   - Request 1: 1 linha `proxy` (target backend) + 1 linha `request` (POST /api/chat).
   - Request 2 (`/api/health`): ZERO linhas (silencioso).
   - Request 3 (validation fail): 1 linha `validation_failed` (warn) + 1 linha `request` com status 400.

4. **Smoke prod-like (manual, opcional):**
   ```bash
   NODE_ENV=production node --import tsx/esm apps/gateway/src/index.ts
   ```
   Saída deve ser JSON puro (1 obj por linha), sem cores, sem pretty.

5. **Lint commits:** o autor (Claude executor) faz commits seguindo CLAUDE.md — Conventional Commits + emoji em pt-BR, sem `Co-Authored-By` nem rodapé "Generated with". Sugestão de mensagem para a entrega:
   ```
   ✨ feat(gateway): adicionar logs estruturados com pino + pino-pretty

   - logger singleton (JSON em prod, pretty em dev)
   - request log middleware com reqId UUID por request
   - skip de /api/health para evitar ruído de healthcheck
   - proxy log via loggedFetch (URL, método, status, duração)
   - errorHandler agora loga level=error com correlação por reqId
   - validate loga warn com paths que falharam
   ```
</verification>

<success_criteria>
- `pino` e `pino-pretty` em `dependencies` do `apps/gateway/package.json`.
- `apps/gateway/src/lib/logger.ts` exporta singleton com transport pino-pretty SOMENTE em `NODE_ENV=development`.
- `LOG_LEVEL` configurável via env (default `info`).
- `requestLog` middleware registrado ANTES de qualquer rota em `createApp()`.
- `req.id` (UUID v4) e `req.log` (child logger pino) disponíveis em todos os handlers downstream — tipados via augmentation em `src/types/express.d.ts`.
- Requests para `/api/health` NÃO geram linha de request log (skip por path exato).
- `lib/proxy.ts` exporta `loggedFetch` que loga `level=info` no sucesso e `level=error` na exceção, ambos com `target/method/status/durationMs`.
- `routes/chat.ts` e `routes/tool-calls.ts` usam `loggedFetch` (não `undici.fetch` direto).
- `routes/health.ts` mantém `undici.fetch` direto (silencioso) — comentário no código explicando o porquê.
- `errorHandler` loga `level=error` com `reqId`, `status`, `code`, `message`; inclui `stack` no LOG apenas em não-prod; NUNCA inclui `stack` no body da response (regressão GW-04 protegida).
- `validate` loga `level=warn` com `code: VALIDATION_ERROR` e `failedPaths` antes de propagar o erro.
- `index.ts` faz boot log via `logger.info` (não `console.log`).
- `cd apps/gateway && npx tsc --noEmit` passa.
- `cd apps/gateway && npm test -- --run` passa 100% — incluindo os 7 testes novos em `test/logger.test.ts` e todos os pré-existentes (`chat`, `health`, `error`, `stream`, `tool-calls`, `chat-stream-auth`).
- Sem breaking change em nenhuma rota existente (status codes e bodies inalterados).
- Commits em pt-BR seguindo Conventional Commits + emoji (CLAUDE.md), sem `Co-Authored-By` nem rodapé "Generated with".
</success_criteria>

<output>
After completion, create `.planning/quick/260427-u2k-adicionar-logs-estruturados-no-api-gatew/260427-u2k-SUMMARY.md` documentando:
- Files modified (com linhas alteradas)
- Decisões tomadas durante execução (ex: versão final escolhida do pino, qualquer ajuste de tipagem)
- Saída de teste final (contagem aprovados/falhas)
- Snippet de log real (1 linha de cada: boot, request, proxy, error, validation_failed) demonstrando o formato em dev
- Quaisquer follow-ups (ex: instrumentar SSE com per-chunk metrics? — ficou fora do escopo desta entrega)
</output>
