---
phase: quick-260514-t67
plan: 01
type: quick
wave: 1
depends_on: []
autonomous: true
files_modified:
  - apps/desktop/src/main/actions/file-actions.ts
  - apps/desktop/electron.vite.config.ts
  - apps/desktop/package.json

must_haves:
  truths:
    - openFile fallback uses shell.openPath exclusively (no open package)
    - Build succeeds without 'open' externalization
    - "'open' package is not in dependencies"
  artifacts:
    - path: "apps/desktop/src/main/actions/file-actions.ts"
      provides: "openFileHandler without open import/usage"
      min_lines: 150
    - path: "apps/desktop/electron.vite.config.ts"
      provides: "MAIN_EXTERNALS without 'open' entry"
      min_lines: 100
  key_links:
    - from: "apps/desktop/src/main/actions/file-actions.ts"
      to: "electron.shell"
      via: "shell.openPath direct call"
      pattern: "shell\\.openPath"
---

<objective>
Remove the 'open' package dependency from JARVIS desktop app, replacing fallback logic in file-actions.ts with shell.openPath-only behavior.

Purpose: Simplify dependency tree — shell.openPath is cross-platform and built into Electron, making the 'open' fallback unnecessary. The fallback was added to handle edge cases but adds bundling complexity (platform-specific child process calls require externalization).

Output:
- file-actions.ts using only shell.openPath
- electron.vite.config.ts without 'open' in MAIN_EXTERNALS
- package.json without 'open' dependency
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/jarvis/.planning/STATE.md

Current file-actions.ts (line 14): imports 'open' package
Current file-actions.ts (lines 52-61): tries open(safePath) as fallback when shell.openPath returns error
Current electron.vite.config.ts (line 103): externalizes 'open' with comment "Platform-specific child process calls — cannot be bundled"
Current apps/desktop/package.json (line 42): declares "open": "11.0.0" dependency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 'open' usage from file-actions.ts</name>
  <files>apps/desktop/src/main/actions/file-actions.ts</files>
  <action>
Remove line 14 (import open from 'open';).

In openFileHandler function (lines 45-65):
- Keep shell.openPath call (lines 47-48)
- Remove fallback try/catch block (lines 52-61) that calls open(safePath)
- Simplify error handling: if shell.openPath returns errMsg, return failure directly

Result: openFileHandler returns { success: false, error: `shell.openPath failed: ${errMsg}` } when shell.openPath returns non-empty string, no fallback attempt.
  </action>
  <verify>
    <automated>grep -n "import open from" apps/desktop/src/main/actions/file-actions.ts || echo "PASS: no 'open' import found"</automated>
  </verify>
  <done>file-actions.ts has no import or usage of 'open' package; openFileHandler uses only shell.openPath</done>
</task>

<task type="auto">
  <name>Task 2: Remove 'open' from MAIN_EXTERNALS</name>
  <files>apps/desktop/electron.vite.config.ts</files>
  <action>
In MAIN_EXTERNALS array (lines 94-105):
- Remove line 103: 'open', // Platform-specific child process calls — cannot be bundled

Leave all other externals intact (electron, node:, @fugood/, bufferutil, utf-8-validate, onnxruntime-node, @huggingface/transformers, kokoro-js, sharp).
  </action>
  <verify>
    <automated>grep -n "'open'" apps/desktop/electron.vite.config.ts || echo "PASS: 'open' not in config"</automated>
  </verify>
  <done>MAIN_EXTERNALS does not reference 'open'</done>
</task>

<task type="auto">
  <name>Task 3: Uninstall 'open' package</name>
  <files>apps/desktop/package.json</files>
  <action>
Remove "open": "11.0.0" from dependencies object (line 42).

Run: pnpm --filter @jarvis/desktop remove open

Verify package.json no longer lists 'open' in dependencies.
  </action>
  <verify>
    <automated>grep -n '"open"' apps/desktop/package.json || echo "PASS: 'open' not in package.json"</automated>
  </verify>
  <done>'open' package is uninstalled; package.json has no 'open' dependency</done>
</task>

</tasks>

<verification>
Build verification:
```bash
pnpm --filter @jarvis/desktop build
```
Should succeed without errors about missing 'open' externalization.

Runtime verification (manual smoke test after execution):
- Open JARVIS desktop app in dev mode
- Trigger openFile action on a .txt file
- File should open in default editor (via shell.openPath)
</verification>

<success_criteria>
- file-actions.ts uses only shell.openPath (no 'open' import or fallback)
- electron.vite.config.ts MAIN_EXTERNALS does not include 'open'
- apps/desktop/package.json dependencies does not include 'open'
- pnpm --filter @jarvis/desktop build completes successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260514-t67-remover-depend-ncia-open-substituir-fall/260514-t67-SUMMARY.md`
</output>
