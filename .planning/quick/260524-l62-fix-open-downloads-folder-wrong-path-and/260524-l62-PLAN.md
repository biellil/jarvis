---
phase: quick
plan: 260524-l62
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/backend-ts/src/session/pc-tools.ts
  - apps/desktop-py/src/jarvis_desktop/pc_control.py
  - apps/desktop-py/src/jarvis_desktop/ui.py
  - apps/desktop-py/src/jarvis_desktop/chat.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dizer 'abrir pasta Downloads' abre a pasta Downloads (não home)"
    - "Texto de resposta do JARVIS aparece no terminal junto ao label [jarvis]"
    - "TTS fala E texto é exibido — não só um dos dois"
  artifacts:
    - path: "apps/backend-ts/src/session/pc-tools.ts"
      provides: "Tool description corrigida para guiar LLM a usar nomes simples de pasta"
    - path: "apps/desktop-py/src/jarvis_desktop/pc_control.py"
      provides: "open_folder valida path vazio antes de resolver"
    - path: "apps/desktop-py/src/jarvis_desktop/ui.py"
      provides: "_live_started flag que distingue live ativo de live instanciado-mas-parado"
    - path: "apps/desktop-py/src/jarvis_desktop/chat.py"
      provides: "Label [jarvis] impresso junto ao primeiro token, não antes do stream"
  key_links:
    - from: "pc-tools.ts createOpenFolderTool"
      to: "pc_control.py open_folder"
      via: "path param — LLM gera nome simples, alias dict resolve"
    - from: "_stream_response live_paused"
      to: "_post_task_resume live_paused"
      via: "_live_started flag evita stop/start aninhado"
---

<objective>
Corrigir dois bugs no cliente desktop Python do JARVIS:

1. `open_folder` abre home (`C:\Users\biel1`) em vez de Downloads porque o LLM gera `path: "~"` que não está no alias dict.
2. Resposta do JARVIS não aparece no terminal — TTS fala mas texto fica invisível, causado por (a) `live_paused()` aninhado que reinicia `_live` cedo demais, e (b) label `[jarvis]` impresso antes do stream SSE começar, ficando orphaned quando eventos agenticos imprimem linhas extras.

Purpose: JARVIS deve abrir a pasta correta e exibir sua resposta no terminal.
Output: 4 arquivos modificados, nenhum novo arquivo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@apps/backend-ts/src/session/pc-tools.ts
@apps/desktop-py/src/jarvis_desktop/pc_control.py
@apps/desktop-py/src/jarvis_desktop/ui.py
@apps/desktop-py/src/jarvis_desktop/chat.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix open_folder — LLM description + empty path guard</name>
  <files>apps/backend-ts/src/session/pc-tools.ts, apps/desktop-py/src/jarvis_desktop/pc_control.py</files>
  <action>
**pc-tools.ts — `createOpenFolderTool` schema `path` description:**

Replace the current description of the `path` field (lines 90-94) with:

```typescript
path: z
  .string()
  .describe(
    'Nome simples da pasta quando o usuário se referir a pastas comuns pelo nome ' +
    '(ex: "downloads", "documentos", "desktop", "imagens", "videos", "home"). ' +
    'Use caminhos absolutos ou com ~ APENAS quando o usuário fornecer o caminho ' +
    'explicitamente (ex: "~/projetos", "C:\\\\Users\\\\biel1\\\\trabalho"). ' +
    'NUNCA use "~" sozinho — use "home" para a pasta home do usuário.',
  ),
```

**pc_control.py — `open_folder()` — add empty path guard:**

At the top of `open_folder()`, after the function signature (line 208), insert BEFORE the `alias_key = ...` line:

```python
if not path or not path.strip():
    raise ValueError("Caminho da pasta não pode ser vazio. Especifique um nome de pasta (ex: 'downloads') ou caminho absoluto.")
```

This catches the case where the LLM generates an empty string or whitespace-only path.
  </action>
  <verify>
    <automated>cd apps/backend-ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compila sem erros. `open_folder("")` levanta ValueError com mensagem clara. LLM tool description instrui a usar nomes simples como "downloads".</done>
</task>

<task type="auto">
  <name>Task 2: Fix live_paused nesting — add _live_started flag to ui.py</name>
  <files>apps/desktop-py/src/jarvis_desktop/ui.py</files>
  <action>
**Root cause:** `live_paused()` verifica `_live is not None` (truthy mesmo quando Live está parado). Quando `_post_task_resume()` chama `live_paused()` dentro de um `live_paused()` já ativo em `_stream_response()`, o `finally` do inner context reinicia `_live` prematuramente.

**Fix — três mudanças em `ui.py`:**

1. Adicionar flag `_live_started: bool = False` na seção de module-level singleton state (após `_lock = threading.Lock()`):

```python
_live_started: bool = False
```

2. Em `init_ui()`, após `_live.start()`, adicionar:

```python
_live_started = True
```

   (Lembrar de adicionar `_live_started` no `global` statement da função: `global _console, _live, _live_started`)

3. Substituir a implementação de `live_paused()` inteira (linhas 157-180) por:

```python
def live_paused():
    """Context manager: stop Live rendering, yield, then restart.

    Uses _live_started flag to distinguish between Live instance existing
    but stopped (nested call) versus Live genuinely running.
    Nested calls (live already stopped by outer context) are no-ops.
    """
    from contextlib import contextmanager

    @contextmanager
    def _ctx():
        global _live, _live_started
        was_started = _live_started
        if was_started:
            _live.stop()
            _live_started = False
        try:
            yield
        finally:
            if was_started and _live is not None:
                try:
                    _live.start()
                    _live_started = True
                except Exception:
                    pass  # Best-effort — don't mask original exception or crash on shutdown

    return _ctx()
```

4. Em `cleanup_ui()`, após `_live.stop()` dentro do try, adicionar:

```python
_live_started = False
```

   (Adicionar `_live_started` ao `global` da função: `global _live, _live_started`)

5. Em `get_input()`, atualizar para usar a flag:

```python
def get_input(prompt: str = "") -> str:
    global _live, _live_started
    if _live is None or not _live_started:
        return input(prompt)
    _live.stop()
    _live_started = False
    try:
        return input(prompt)
    finally:
        if _live is not None:
            try:
                _live.start()
                _live_started = True
            except Exception:
                pass
```
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "from jarvis_desktop import ui; print('ui import ok'); print(hasattr(ui, '_live_started'))"</automated>
  </verify>
  <done>`_live_started` existe como atributo de módulo. `live_paused()` nested (inner context quando outer já parou o live) é no-op — não tenta stop/start em live já parado.</done>
</task>

<task type="auto">
  <name>Task 3: Fix [jarvis] label timing — imprimir junto ao primeiro token</name>
  <files>apps/desktop-py/src/jarvis_desktop/chat.py</files>
  <action>
**Root cause:** `_stream_response()` imprime `[jarvis] ` ANTES de entrar no `with _ui.live_paused()`. Em modo agêntico, eventos `task:plan` e `task:step:*` imprimem múltiplas linhas durante o stream, deixando o label orphaned em uma linha anterior, separado visualmente do texto da resposta.

**Fix — duas mudanças em `chat.py`:**

**A) Atualizar `_read_sse_stream()` — adicionar parâmetro `main_stream`:**

Alterar a assinatura de `_read_sse_stream` (linha 289-294) para:

```python
def _read_sse_stream(
    response,
    config: JarvisConfig,
    accumulate_for_tts: bool = True,
    main_stream: bool = False,
) -> str:
```

No corpo da função, alterar a inicialização de `at_line_start`:

```python
at_line_start = True  # sempre True agora — label será impresso com o primeiro token
```

No bloco de processamento de tokens plain (event_type is None), substituir:

```python
if at_line_start:
    console.print(_RESPONSE_INDENT, end="", markup=False, highlight=False)
    at_line_start = False
```

Por:

```python
if at_line_start:
    if main_stream:
        console.print(f"{_LABEL_JARVIS} ", end="", highlight=False)
    else:
        console.print(_RESPONSE_INDENT, end="", markup=False, highlight=False)
    at_line_start = False
```

**B) Atualizar `_stream_response()` — remover print antecipado do label, usar `main_stream=True`:**

Em `_stream_response()`, dentro do `with _ui.live_paused():`, substituir:

```python
with _ui.live_paused():
    _console().print(f"{_LABEL_JARVIS} ", end="", highlight=False)
    full_text = _read_sse_stream(response, config, accumulate_for_tts=True, first_prefix_printed=True)
```

Por:

```python
with _ui.live_paused():
    full_text = _read_sse_stream(response, config, accumulate_for_tts=True, main_stream=True)
```

**C) Atualizar `_await_input()` — substituir referências diretas a `_ui._live` por `_live_started`:**

Em `_await_input()` (linha 409), substituir:

```python
_ui._live.stop() if _ui._live else None
```

Por:

```python
if _ui._live_started and _ui._live:
    _ui._live.stop()
    _ui._live_started = False
```

E no `finally` (linhas 462-466), substituir:

```python
try:
    if _ui._live:
        _ui._live.start()
except Exception:
    pass
```

Por:

```python
try:
    if _ui._live and not _ui._live_started:
        _ui._live.start()
        _ui._live_started = True
except Exception:
    pass
```
  </action>
  <verify>
    <automated>cd apps/desktop-py && python -c "from jarvis_desktop import chat; import inspect; src = inspect.getsource(chat._read_sse_stream); assert 'main_stream' in src; assert 'first_prefix_printed' not in src; print('chat signatures ok')"</automated>
  </verify>
  <done>`_read_sse_stream` tem parâmetro `main_stream` (não `first_prefix_printed`). `_stream_response` não chama `console.print` com `_LABEL_JARVIS` antes do stream — label é impresso pelo primeiro token em `_read_sse_stream`. `_await_input` usa `_live_started` em vez de checar `_live` diretamente.</done>
</task>

</tasks>

<verification>
1. TypeScript compila sem erros: `cd apps/backend-ts && npx tsc --noEmit`
2. Python imports sem erros: `cd apps/desktop-py && python -c "from jarvis_desktop import ui, chat, pc_control"`
3. `_live_started` é `False` antes de `init_ui()` e `True` após: `python -c "import jarvis_desktop.ui as u; print(u._live_started)"`
4. `open_folder("")` levanta ValueError: `python -c "from jarvis_desktop.pc_control import open_folder; open_folder('')" 2>&1`
</verification>

<success_criteria>
- LLM gera `path: "downloads"` (não `"~"`) quando usuário diz "abre pasta Downloads" — guiado pela nova description
- `open_folder("")` levanta `ValueError` em vez de abrir home silenciosamente
- `live_paused()` aninhado é no-op — inner call não reinicia live que outer já parou
- Label `[jarvis]` aparece na mesma linha do primeiro token da resposta, mesmo em modo agêntico com eventos intermediários
</success_criteria>

<output>
Após conclusão, criar commit:
```
🐛 fix(desktop): open_folder path correto + label [jarvis] no terminal
```

Registrar em STATE.md Quick Tasks:
| 260524-l62 | fix open Downloads folder wrong path and message not showing in terminal | 2026-05-24 | {commit} | [260524-l62-fix-open-downloads-folder-wrong-path-and](.planning/quick/260524-l62-fix-open-downloads-folder-wrong-path-and/) |
</output>
