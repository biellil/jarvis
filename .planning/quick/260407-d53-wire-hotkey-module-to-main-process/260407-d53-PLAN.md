---
phase: quick
plan: 260407-d53
type: execute
wave: 1
depends_on: []
files_modified: [apps/desktop/src/main/index.ts]
autonomous: true
requirements: [ACTV-01]
gap_closure: true
must_haves:
  truths:
    - "Ctrl+Shift+J pressed globally triggers widget show/hide"
    - "Hotkey cleanup happens on app quit"
  artifacts:
    - path: "apps/desktop/src/main/index.ts"
      provides: "Hotkey integration in main process"
      contains: "registerHotkey"
  key_links:
    - from: "main/index.ts"
      to: "./hotkey"
      via: "import statement"
      pattern: "import.*hotkey"
    - from: "main/index.ts"
      to: "registerHotkey()"
      via: "function call after window creation"
      pattern: "registerHotkey\\(mainWindow"
    - from: "main/index.ts"
      to: "unregisterAll()"
      via: "cleanup in before-quit"
      pattern: "unregisterAll\\(\\)"
---

<objective>
Wire the existing hotkey module to the main process to enable global Ctrl+Shift+J activation

Purpose: Close the verification gap where the hotkey module exists but is never called
Output: Working global hotkey that shows/hides the widget
</objective>

<context>
The hotkey module (`apps/desktop/src/main/hotkey.ts`) is fully implemented with 14 tests passing. It exports:
- `registerHotkey(mainWindow: BrowserWindow): boolean` - Registers the global shortcut
- `unregisterAll(): void` - Cleanup function for app quit

The main process (`apps/desktop/src/main/index.ts`) creates the window but never imports or calls the hotkey module. This is the only blocker preventing ACTV-01 requirement from being satisfied.

Current main/index.ts structure:
- Line 9-13: Imports
- Line 67-78: app.whenReady() block where window is created
- Line 81-87: app.on('before-quit') cleanup handler
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire hotkey module to main process</name>
  <files>apps/desktop/src/main/index.ts</files>
  <action>
    Add the hotkey module integration with these exact changes:

    1. Add import after line 13 (with other local imports):
       ```typescript
       import { registerHotkey, unregisterAll } from './hotkey';
       ```

    2. Call registerHotkey after createWindow() in app.whenReady() (after line 69):
       ```typescript
       createWindow();
       const hotkeyRegistered = registerHotkey(mainWindow!);
       if (!hotkeyRegistered) {
         console.warn('Failed to register hotkey - already in use or system restriction');
       }
       ```

    3. Call unregisterAll in before-quit handler (after line 85, before destroyTray):
       ```typescript
       unregisterAll(); // Cleanup global shortcuts
       destroyTray(); // Cleanup tray icon
       ```

    Note: The hotkey module already handles all the logic - persisting to electron-store, fallback to tray activation if registration fails, and toggle show/hide behavior. We just need to wire it up.
  </action>
  <verify>
    <automated>pnpm --filter desktop test -- hotkey</automated>
  </verify>
  <done>
    - Import statement added for registerHotkey and unregisterAll
    - registerHotkey() called after window creation with warning on failure
    - unregisterAll() called in before-quit handler
    - Tests continue to pass
  </done>
</task>

</tasks>

<verification>
After this task:
1. Run the app and press Ctrl+Shift+J - widget should show/hide
2. Check console for any "Failed to register hotkey" warning
3. Quit the app - no errors about unhandled shortcuts
</verification>

<success_criteria>
- Hotkey module is imported in main/index.ts
- registerHotkey() is called after window creation
- unregisterAll() is called on app quit
- ACTV-01 requirement is satisfied
- Phase 12 verification gap is closed
</success_criteria>

<output>
After completion, this quick task will be logged in STATE.md
</output>