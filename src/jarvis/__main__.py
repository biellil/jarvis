"""Entry point for `python -m jarvis` and the `jarvis` CLI command."""


def main() -> None:
    """Main entry point — placeholder until core session is implemented."""
    pass


if __name__ == "__main__":
    main()
